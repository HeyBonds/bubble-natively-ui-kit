(() => {
  // node_modules/preact/dist/preact.module.js
  var n;
  var l;
  var u;
  var t;
  var i;
  var o;
  var r;
  var e;
  var f;
  var c;
  var s;
  var a;
  var h;
  var p = {};
  var v = [];
  var y = /acit|ex(?:s|g|n|p|$)|rph|grid|ows|mnc|ntw|ine[ch]|zoo|^ord|itera/i;
  var d = Array.isArray;
  function w(n3, l3) {
    for (var u3 in l3) n3[u3] = l3[u3];
    return n3;
  }
  function g(n3) {
    n3 && n3.parentNode && n3.parentNode.removeChild(n3);
  }
  function _(l3, u3, t3) {
    var i3, o3, r3, e3 = {};
    for (r3 in u3) "key" == r3 ? i3 = u3[r3] : "ref" == r3 ? o3 = u3[r3] : e3[r3] = u3[r3];
    if (arguments.length > 2 && (e3.children = arguments.length > 3 ? n.call(arguments, 2) : t3), "function" == typeof l3 && null != l3.defaultProps) for (r3 in l3.defaultProps) void 0 === e3[r3] && (e3[r3] = l3.defaultProps[r3]);
    return m(l3, e3, i3, o3, null);
  }
  function m(n3, t3, i3, o3, r3) {
    var e3 = { type: n3, props: t3, key: i3, ref: o3, __k: null, __: null, __b: 0, __e: null, __c: null, constructor: void 0, __v: null == r3 ? ++u : r3, __i: -1, __u: 0 };
    return null == r3 && null != l.vnode && l.vnode(e3), e3;
  }
  function b() {
    return { current: null };
  }
  function k(n3) {
    return n3.children;
  }
  function x(n3, l3) {
    this.props = n3, this.context = l3;
  }
  function S(n3, l3) {
    if (null == l3) return n3.__ ? S(n3.__, n3.__i + 1) : null;
    for (var u3; l3 < n3.__k.length; l3++) if (null != (u3 = n3.__k[l3]) && null != u3.__e) return u3.__e;
    return "function" == typeof n3.type ? S(n3) : null;
  }
  function C(n3) {
    var l3, u3;
    if (null != (n3 = n3.__) && null != n3.__c) {
      for (n3.__e = n3.__c.base = null, l3 = 0; l3 < n3.__k.length; l3++) if (null != (u3 = n3.__k[l3]) && null != u3.__e) {
        n3.__e = n3.__c.base = u3.__e;
        break;
      }
      return C(n3);
    }
  }
  function M(n3) {
    (!n3.__d && (n3.__d = true) && i.push(n3) && !$.__r++ || o != l.debounceRendering) && ((o = l.debounceRendering) || r)($);
  }
  function $() {
    for (var n3, u3, t3, o3, r3, f3, c3, s3 = 1; i.length; ) i.length > s3 && i.sort(e), n3 = i.shift(), s3 = i.length, n3.__d && (t3 = void 0, o3 = void 0, r3 = (o3 = (u3 = n3).__v).__e, f3 = [], c3 = [], u3.__P && ((t3 = w({}, o3)).__v = o3.__v + 1, l.vnode && l.vnode(t3), O(u3.__P, t3, o3, u3.__n, u3.__P.namespaceURI, 32 & o3.__u ? [r3] : null, f3, null == r3 ? S(o3) : r3, !!(32 & o3.__u), c3), t3.__v = o3.__v, t3.__.__k[t3.__i] = t3, N(f3, t3, c3), o3.__e = o3.__ = null, t3.__e != r3 && C(t3)));
    $.__r = 0;
  }
  function I(n3, l3, u3, t3, i3, o3, r3, e3, f3, c3, s3) {
    var a3, h3, y3, d3, w4, g4, _4, m3 = t3 && t3.__k || v, b2 = l3.length;
    for (f3 = P(u3, l3, m3, f3, b2), a3 = 0; a3 < b2; a3++) null != (y3 = u3.__k[a3]) && (h3 = -1 == y3.__i ? p : m3[y3.__i] || p, y3.__i = a3, g4 = O(n3, y3, h3, i3, o3, r3, e3, f3, c3, s3), d3 = y3.__e, y3.ref && h3.ref != y3.ref && (h3.ref && B(h3.ref, null, y3), s3.push(y3.ref, y3.__c || d3, y3)), null == w4 && null != d3 && (w4 = d3), (_4 = !!(4 & y3.__u)) || h3.__k === y3.__k ? f3 = A(y3, f3, n3, _4) : "function" == typeof y3.type && void 0 !== g4 ? f3 = g4 : d3 && (f3 = d3.nextSibling), y3.__u &= -7);
    return u3.__e = w4, f3;
  }
  function P(n3, l3, u3, t3, i3) {
    var o3, r3, e3, f3, c3, s3 = u3.length, a3 = s3, h3 = 0;
    for (n3.__k = new Array(i3), o3 = 0; o3 < i3; o3++) null != (r3 = l3[o3]) && "boolean" != typeof r3 && "function" != typeof r3 ? ("string" == typeof r3 || "number" == typeof r3 || "bigint" == typeof r3 || r3.constructor == String ? r3 = n3.__k[o3] = m(null, r3, null, null, null) : d(r3) ? r3 = n3.__k[o3] = m(k, { children: r3 }, null, null, null) : void 0 === r3.constructor && r3.__b > 0 ? r3 = n3.__k[o3] = m(r3.type, r3.props, r3.key, r3.ref ? r3.ref : null, r3.__v) : n3.__k[o3] = r3, f3 = o3 + h3, r3.__ = n3, r3.__b = n3.__b + 1, e3 = null, -1 != (c3 = r3.__i = L(r3, u3, f3, a3)) && (a3--, (e3 = u3[c3]) && (e3.__u |= 2)), null == e3 || null == e3.__v ? (-1 == c3 && (i3 > s3 ? h3-- : i3 < s3 && h3++), "function" != typeof r3.type && (r3.__u |= 4)) : c3 != f3 && (c3 == f3 - 1 ? h3-- : c3 == f3 + 1 ? h3++ : (c3 > f3 ? h3-- : h3++, r3.__u |= 4))) : n3.__k[o3] = null;
    if (a3) for (o3 = 0; o3 < s3; o3++) null != (e3 = u3[o3]) && 0 == (2 & e3.__u) && (e3.__e == t3 && (t3 = S(e3)), D(e3, e3));
    return t3;
  }
  function A(n3, l3, u3, t3) {
    var i3, o3;
    if ("function" == typeof n3.type) {
      for (i3 = n3.__k, o3 = 0; i3 && o3 < i3.length; o3++) i3[o3] && (i3[o3].__ = n3, l3 = A(i3[o3], l3, u3, t3));
      return l3;
    }
    n3.__e != l3 && (t3 && (l3 && n3.type && !l3.parentNode && (l3 = S(n3)), u3.insertBefore(n3.__e, l3 || null)), l3 = n3.__e);
    do {
      l3 = l3 && l3.nextSibling;
    } while (null != l3 && 8 == l3.nodeType);
    return l3;
  }
  function H(n3, l3) {
    return l3 = l3 || [], null == n3 || "boolean" == typeof n3 || (d(n3) ? n3.some(function(n4) {
      H(n4, l3);
    }) : l3.push(n3)), l3;
  }
  function L(n3, l3, u3, t3) {
    var i3, o3, r3, e3 = n3.key, f3 = n3.type, c3 = l3[u3], s3 = null != c3 && 0 == (2 & c3.__u);
    if (null === c3 && null == e3 || s3 && e3 == c3.key && f3 == c3.type) return u3;
    if (t3 > (s3 ? 1 : 0)) {
      for (i3 = u3 - 1, o3 = u3 + 1; i3 >= 0 || o3 < l3.length; ) if (null != (c3 = l3[r3 = i3 >= 0 ? i3-- : o3++]) && 0 == (2 & c3.__u) && e3 == c3.key && f3 == c3.type) return r3;
    }
    return -1;
  }
  function T(n3, l3, u3) {
    "-" == l3[0] ? n3.setProperty(l3, null == u3 ? "" : u3) : n3[l3] = null == u3 ? "" : "number" != typeof u3 || y.test(l3) ? u3 : u3 + "px";
  }
  function j(n3, l3, u3, t3, i3) {
    var o3, r3;
    n: if ("style" == l3) if ("string" == typeof u3) n3.style.cssText = u3;
    else {
      if ("string" == typeof t3 && (n3.style.cssText = t3 = ""), t3) for (l3 in t3) u3 && l3 in u3 || T(n3.style, l3, "");
      if (u3) for (l3 in u3) t3 && u3[l3] == t3[l3] || T(n3.style, l3, u3[l3]);
    }
    else if ("o" == l3[0] && "n" == l3[1]) o3 = l3 != (l3 = l3.replace(f, "$1")), r3 = l3.toLowerCase(), l3 = r3 in n3 || "onFocusOut" == l3 || "onFocusIn" == l3 ? r3.slice(2) : l3.slice(2), n3.l || (n3.l = {}), n3.l[l3 + o3] = u3, u3 ? t3 ? u3.u = t3.u : (u3.u = c, n3.addEventListener(l3, o3 ? a : s, o3)) : n3.removeEventListener(l3, o3 ? a : s, o3);
    else {
      if ("http://www.w3.org/2000/svg" == i3) l3 = l3.replace(/xlink(H|:h)/, "h").replace(/sName$/, "s");
      else if ("width" != l3 && "height" != l3 && "href" != l3 && "list" != l3 && "form" != l3 && "tabIndex" != l3 && "download" != l3 && "rowSpan" != l3 && "colSpan" != l3 && "role" != l3 && "popover" != l3 && l3 in n3) try {
        n3[l3] = null == u3 ? "" : u3;
        break n;
      } catch (n4) {
      }
      "function" == typeof u3 || (null == u3 || false === u3 && "-" != l3[4] ? n3.removeAttribute(l3) : n3.setAttribute(l3, "popover" == l3 && 1 == u3 ? "" : u3));
    }
  }
  function F(n3) {
    return function(u3) {
      if (this.l) {
        var t3 = this.l[u3.type + n3];
        if (null == u3.t) u3.t = c++;
        else if (u3.t < t3.u) return;
        return t3(l.event ? l.event(u3) : u3);
      }
    };
  }
  function O(n3, u3, t3, i3, o3, r3, e3, f3, c3, s3) {
    var a3, h3, p3, v3, y3, _4, m3, b2, S2, C4, M3, $3, P4, A4, H3, L3, T4, j4 = u3.type;
    if (void 0 !== u3.constructor) return null;
    128 & t3.__u && (c3 = !!(32 & t3.__u), r3 = [f3 = u3.__e = t3.__e]), (a3 = l.__b) && a3(u3);
    n: if ("function" == typeof j4) try {
      if (b2 = u3.props, S2 = "prototype" in j4 && j4.prototype.render, C4 = (a3 = j4.contextType) && i3[a3.__c], M3 = a3 ? C4 ? C4.props.value : a3.__ : i3, t3.__c ? m3 = (h3 = u3.__c = t3.__c).__ = h3.__E : (S2 ? u3.__c = h3 = new j4(b2, M3) : (u3.__c = h3 = new x(b2, M3), h3.constructor = j4, h3.render = E), C4 && C4.sub(h3), h3.state || (h3.state = {}), h3.__n = i3, p3 = h3.__d = true, h3.__h = [], h3._sb = []), S2 && null == h3.__s && (h3.__s = h3.state), S2 && null != j4.getDerivedStateFromProps && (h3.__s == h3.state && (h3.__s = w({}, h3.__s)), w(h3.__s, j4.getDerivedStateFromProps(b2, h3.__s))), v3 = h3.props, y3 = h3.state, h3.__v = u3, p3) S2 && null == j4.getDerivedStateFromProps && null != h3.componentWillMount && h3.componentWillMount(), S2 && null != h3.componentDidMount && h3.__h.push(h3.componentDidMount);
      else {
        if (S2 && null == j4.getDerivedStateFromProps && b2 !== v3 && null != h3.componentWillReceiveProps && h3.componentWillReceiveProps(b2, M3), u3.__v == t3.__v || !h3.__e && null != h3.shouldComponentUpdate && false === h3.shouldComponentUpdate(b2, h3.__s, M3)) {
          for (u3.__v != t3.__v && (h3.props = b2, h3.state = h3.__s, h3.__d = false), u3.__e = t3.__e, u3.__k = t3.__k, u3.__k.some(function(n4) {
            n4 && (n4.__ = u3);
          }), $3 = 0; $3 < h3._sb.length; $3++) h3.__h.push(h3._sb[$3]);
          h3._sb = [], h3.__h.length && e3.push(h3);
          break n;
        }
        null != h3.componentWillUpdate && h3.componentWillUpdate(b2, h3.__s, M3), S2 && null != h3.componentDidUpdate && h3.__h.push(function() {
          h3.componentDidUpdate(v3, y3, _4);
        });
      }
      if (h3.context = M3, h3.props = b2, h3.__P = n3, h3.__e = false, P4 = l.__r, A4 = 0, S2) {
        for (h3.state = h3.__s, h3.__d = false, P4 && P4(u3), a3 = h3.render(h3.props, h3.state, h3.context), H3 = 0; H3 < h3._sb.length; H3++) h3.__h.push(h3._sb[H3]);
        h3._sb = [];
      } else do {
        h3.__d = false, P4 && P4(u3), a3 = h3.render(h3.props, h3.state, h3.context), h3.state = h3.__s;
      } while (h3.__d && ++A4 < 25);
      h3.state = h3.__s, null != h3.getChildContext && (i3 = w(w({}, i3), h3.getChildContext())), S2 && !p3 && null != h3.getSnapshotBeforeUpdate && (_4 = h3.getSnapshotBeforeUpdate(v3, y3)), L3 = a3, null != a3 && a3.type === k && null == a3.key && (L3 = V(a3.props.children)), f3 = I(n3, d(L3) ? L3 : [L3], u3, t3, i3, o3, r3, e3, f3, c3, s3), h3.base = u3.__e, u3.__u &= -161, h3.__h.length && e3.push(h3), m3 && (h3.__E = h3.__ = null);
    } catch (n4) {
      if (u3.__v = null, c3 || null != r3) if (n4.then) {
        for (u3.__u |= c3 ? 160 : 128; f3 && 8 == f3.nodeType && f3.nextSibling; ) f3 = f3.nextSibling;
        r3[r3.indexOf(f3)] = null, u3.__e = f3;
      } else {
        for (T4 = r3.length; T4--; ) g(r3[T4]);
        z(u3);
      }
      else u3.__e = t3.__e, u3.__k = t3.__k, n4.then || z(u3);
      l.__e(n4, u3, t3);
    }
    else null == r3 && u3.__v == t3.__v ? (u3.__k = t3.__k, u3.__e = t3.__e) : f3 = u3.__e = q(t3.__e, u3, t3, i3, o3, r3, e3, c3, s3);
    return (a3 = l.diffed) && a3(u3), 128 & u3.__u ? void 0 : f3;
  }
  function z(n3) {
    n3 && n3.__c && (n3.__c.__e = true), n3 && n3.__k && n3.__k.forEach(z);
  }
  function N(n3, u3, t3) {
    for (var i3 = 0; i3 < t3.length; i3++) B(t3[i3], t3[++i3], t3[++i3]);
    l.__c && l.__c(u3, n3), n3.some(function(u4) {
      try {
        n3 = u4.__h, u4.__h = [], n3.some(function(n4) {
          n4.call(u4);
        });
      } catch (n4) {
        l.__e(n4, u4.__v);
      }
    });
  }
  function V(n3) {
    return "object" != typeof n3 || null == n3 || n3.__b && n3.__b > 0 ? n3 : d(n3) ? n3.map(V) : w({}, n3);
  }
  function q(u3, t3, i3, o3, r3, e3, f3, c3, s3) {
    var a3, h3, v3, y3, w4, _4, m3, b2 = i3.props || p, k4 = t3.props, x5 = t3.type;
    if ("svg" == x5 ? r3 = "http://www.w3.org/2000/svg" : "math" == x5 ? r3 = "http://www.w3.org/1998/Math/MathML" : r3 || (r3 = "http://www.w3.org/1999/xhtml"), null != e3) {
      for (a3 = 0; a3 < e3.length; a3++) if ((w4 = e3[a3]) && "setAttribute" in w4 == !!x5 && (x5 ? w4.localName == x5 : 3 == w4.nodeType)) {
        u3 = w4, e3[a3] = null;
        break;
      }
    }
    if (null == u3) {
      if (null == x5) return document.createTextNode(k4);
      u3 = document.createElementNS(r3, x5, k4.is && k4), c3 && (l.__m && l.__m(t3, e3), c3 = false), e3 = null;
    }
    if (null == x5) b2 === k4 || c3 && u3.data == k4 || (u3.data = k4);
    else {
      if (e3 = e3 && n.call(u3.childNodes), !c3 && null != e3) for (b2 = {}, a3 = 0; a3 < u3.attributes.length; a3++) b2[(w4 = u3.attributes[a3]).name] = w4.value;
      for (a3 in b2) if (w4 = b2[a3], "children" == a3) ;
      else if ("dangerouslySetInnerHTML" == a3) v3 = w4;
      else if (!(a3 in k4)) {
        if ("value" == a3 && "defaultValue" in k4 || "checked" == a3 && "defaultChecked" in k4) continue;
        j(u3, a3, null, w4, r3);
      }
      for (a3 in k4) w4 = k4[a3], "children" == a3 ? y3 = w4 : "dangerouslySetInnerHTML" == a3 ? h3 = w4 : "value" == a3 ? _4 = w4 : "checked" == a3 ? m3 = w4 : c3 && "function" != typeof w4 || b2[a3] === w4 || j(u3, a3, w4, b2[a3], r3);
      if (h3) c3 || v3 && (h3.__html == v3.__html || h3.__html == u3.innerHTML) || (u3.innerHTML = h3.__html), t3.__k = [];
      else if (v3 && (u3.innerHTML = ""), I("template" == t3.type ? u3.content : u3, d(y3) ? y3 : [y3], t3, i3, o3, "foreignObject" == x5 ? "http://www.w3.org/1999/xhtml" : r3, e3, f3, e3 ? e3[0] : i3.__k && S(i3, 0), c3, s3), null != e3) for (a3 = e3.length; a3--; ) g(e3[a3]);
      c3 || (a3 = "value", "progress" == x5 && null == _4 ? u3.removeAttribute("value") : null != _4 && (_4 !== u3[a3] || "progress" == x5 && !_4 || "option" == x5 && _4 != b2[a3]) && j(u3, a3, _4, b2[a3], r3), a3 = "checked", null != m3 && m3 != u3[a3] && j(u3, a3, m3, b2[a3], r3));
    }
    return u3;
  }
  function B(n3, u3, t3) {
    try {
      if ("function" == typeof n3) {
        var i3 = "function" == typeof n3.__u;
        i3 && n3.__u(), i3 && null == u3 || (n3.__u = n3(u3));
      } else n3.current = u3;
    } catch (n4) {
      l.__e(n4, t3);
    }
  }
  function D(n3, u3, t3) {
    var i3, o3;
    if (l.unmount && l.unmount(n3), (i3 = n3.ref) && (i3.current && i3.current != n3.__e || B(i3, null, u3)), null != (i3 = n3.__c)) {
      if (i3.componentWillUnmount) try {
        i3.componentWillUnmount();
      } catch (n4) {
        l.__e(n4, u3);
      }
      i3.base = i3.__P = null;
    }
    if (i3 = n3.__k) for (o3 = 0; o3 < i3.length; o3++) i3[o3] && D(i3[o3], u3, t3 || "function" != typeof n3.type);
    t3 || g(n3.__e), n3.__c = n3.__ = n3.__e = void 0;
  }
  function E(n3, l3, u3) {
    return this.constructor(n3, u3);
  }
  function G(u3, t3, i3) {
    var o3, r3, e3, f3;
    t3 == document && (t3 = document.documentElement), l.__ && l.__(u3, t3), r3 = (o3 = "function" == typeof i3) ? null : i3 && i3.__k || t3.__k, e3 = [], f3 = [], O(t3, u3 = (!o3 && i3 || t3).__k = _(k, null, [u3]), r3 || p, p, t3.namespaceURI, !o3 && i3 ? [i3] : r3 ? null : t3.firstChild ? n.call(t3.childNodes) : null, e3, !o3 && i3 ? i3 : r3 ? r3.__e : t3.firstChild, o3, f3), N(e3, u3, f3);
  }
  function J(n3, l3) {
    G(n3, l3, J);
  }
  function K(l3, u3, t3) {
    var i3, o3, r3, e3, f3 = w({}, l3.props);
    for (r3 in l3.type && l3.type.defaultProps && (e3 = l3.type.defaultProps), u3) "key" == r3 ? i3 = u3[r3] : "ref" == r3 ? o3 = u3[r3] : f3[r3] = void 0 === u3[r3] && null != e3 ? e3[r3] : u3[r3];
    return arguments.length > 2 && (f3.children = arguments.length > 3 ? n.call(arguments, 2) : t3), m(l3.type, f3, i3 || l3.key, o3 || l3.ref, null);
  }
  function Q(n3) {
    function l3(n4) {
      var u3, t3;
      return this.getChildContext || (u3 = /* @__PURE__ */ new Set(), (t3 = {})[l3.__c] = this, this.getChildContext = function() {
        return t3;
      }, this.componentWillUnmount = function() {
        u3 = null;
      }, this.shouldComponentUpdate = function(n5) {
        this.props.value != n5.value && u3.forEach(function(n6) {
          n6.__e = true, M(n6);
        });
      }, this.sub = function(n5) {
        u3.add(n5);
        var l4 = n5.componentWillUnmount;
        n5.componentWillUnmount = function() {
          u3 && u3.delete(n5), l4 && l4.call(n5);
        };
      }), n4.children;
    }
    return l3.__c = "__cC" + h++, l3.__ = n3, l3.Provider = l3.__l = (l3.Consumer = function(n4, l4) {
      return n4.children(l4);
    }).contextType = l3, l3;
  }
  n = v.slice, l = { __e: function(n3, l3, u3, t3) {
    for (var i3, o3, r3; l3 = l3.__; ) if ((i3 = l3.__c) && !i3.__) try {
      if ((o3 = i3.constructor) && null != o3.getDerivedStateFromError && (i3.setState(o3.getDerivedStateFromError(n3)), r3 = i3.__d), null != i3.componentDidCatch && (i3.componentDidCatch(n3, t3 || {}), r3 = i3.__d), r3) return i3.__E = i3;
    } catch (l4) {
      n3 = l4;
    }
    throw n3;
  } }, u = 0, t = function(n3) {
    return null != n3 && void 0 === n3.constructor;
  }, x.prototype.setState = function(n3, l3) {
    var u3;
    u3 = null != this.__s && this.__s != this.state ? this.__s : this.__s = w({}, this.state), "function" == typeof n3 && (n3 = n3(w({}, u3), this.props)), n3 && w(u3, n3), null != n3 && this.__v && (l3 && this._sb.push(l3), M(this));
  }, x.prototype.forceUpdate = function(n3) {
    this.__v && (this.__e = true, n3 && this.__h.push(n3), M(this));
  }, x.prototype.render = k, i = [], r = "function" == typeof Promise ? Promise.prototype.then.bind(Promise.resolve()) : setTimeout, e = function(n3, l3) {
    return n3.__v.__b - l3.__v.__b;
  }, $.__r = 0, f = /(PointerCapture)$|Capture$/i, c = 0, s = F(false), a = F(true), h = 0;

  // node_modules/preact/hooks/dist/hooks.module.js
  var t2;
  var r2;
  var u2;
  var i2;
  var o2 = 0;
  var f2 = [];
  var c2 = l;
  var e2 = c2.__b;
  var a2 = c2.__r;
  var v2 = c2.diffed;
  var l2 = c2.__c;
  var m2 = c2.unmount;
  var s2 = c2.__;
  function p2(n3, t3) {
    c2.__h && c2.__h(r2, n3, o2 || t3), o2 = 0;
    var u3 = r2.__H || (r2.__H = { __: [], __h: [] });
    return n3 >= u3.__.length && u3.__.push({}), u3.__[n3];
  }
  function d2(n3) {
    return o2 = 1, h2(D2, n3);
  }
  function h2(n3, u3, i3) {
    var o3 = p2(t2++, 2);
    if (o3.t = n3, !o3.__c && (o3.__ = [i3 ? i3(u3) : D2(void 0, u3), function(n4) {
      var t3 = o3.__N ? o3.__N[0] : o3.__[0], r3 = o3.t(t3, n4);
      t3 !== r3 && (o3.__N = [r3, o3.__[1]], o3.__c.setState({}));
    }], o3.__c = r2, !r2.__f)) {
      var f3 = function(n4, t3, r3) {
        if (!o3.__c.__H) return true;
        var u4 = o3.__c.__H.__.filter(function(n5) {
          return !!n5.__c;
        });
        if (u4.every(function(n5) {
          return !n5.__N;
        })) return !c3 || c3.call(this, n4, t3, r3);
        var i4 = o3.__c.props !== n4;
        return u4.forEach(function(n5) {
          if (n5.__N) {
            var t4 = n5.__[0];
            n5.__ = n5.__N, n5.__N = void 0, t4 !== n5.__[0] && (i4 = true);
          }
        }), c3 && c3.call(this, n4, t3, r3) || i4;
      };
      r2.__f = true;
      var c3 = r2.shouldComponentUpdate, e3 = r2.componentWillUpdate;
      r2.componentWillUpdate = function(n4, t3, r3) {
        if (this.__e) {
          var u4 = c3;
          c3 = void 0, f3(n4, t3, r3), c3 = u4;
        }
        e3 && e3.call(this, n4, t3, r3);
      }, r2.shouldComponentUpdate = f3;
    }
    return o3.__N || o3.__;
  }
  function y2(n3, u3) {
    var i3 = p2(t2++, 3);
    !c2.__s && C2(i3.__H, u3) && (i3.__ = n3, i3.u = u3, r2.__H.__h.push(i3));
  }
  function _2(n3, u3) {
    var i3 = p2(t2++, 4);
    !c2.__s && C2(i3.__H, u3) && (i3.__ = n3, i3.u = u3, r2.__h.push(i3));
  }
  function A2(n3) {
    return o2 = 5, T2(function() {
      return { current: n3 };
    }, []);
  }
  function F2(n3, t3, r3) {
    o2 = 6, _2(function() {
      if ("function" == typeof n3) {
        var r4 = n3(t3());
        return function() {
          n3(null), r4 && "function" == typeof r4 && r4();
        };
      }
      if (n3) return n3.current = t3(), function() {
        return n3.current = null;
      };
    }, null == r3 ? r3 : r3.concat(n3));
  }
  function T2(n3, r3) {
    var u3 = p2(t2++, 7);
    return C2(u3.__H, r3) && (u3.__ = n3(), u3.__H = r3, u3.__h = n3), u3.__;
  }
  function q2(n3, t3) {
    return o2 = 8, T2(function() {
      return n3;
    }, t3);
  }
  function x2(n3) {
    var u3 = r2.context[n3.__c], i3 = p2(t2++, 9);
    return i3.c = n3, u3 ? (null == i3.__ && (i3.__ = true, u3.sub(r2)), u3.props.value) : n3.__;
  }
  function P2(n3, t3) {
    c2.useDebugValue && c2.useDebugValue(t3 ? t3(n3) : n3);
  }
  function g2() {
    var n3 = p2(t2++, 11);
    if (!n3.__) {
      for (var u3 = r2.__v; null !== u3 && !u3.__m && null !== u3.__; ) u3 = u3.__;
      var i3 = u3.__m || (u3.__m = [0, 0]);
      n3.__ = "P" + i3[0] + "-" + i3[1]++;
    }
    return n3.__;
  }
  function j2() {
    for (var n3; n3 = f2.shift(); ) if (n3.__P && n3.__H) try {
      n3.__H.__h.forEach(z2), n3.__H.__h.forEach(B2), n3.__H.__h = [];
    } catch (t3) {
      n3.__H.__h = [], c2.__e(t3, n3.__v);
    }
  }
  c2.__b = function(n3) {
    r2 = null, e2 && e2(n3);
  }, c2.__ = function(n3, t3) {
    n3 && t3.__k && t3.__k.__m && (n3.__m = t3.__k.__m), s2 && s2(n3, t3);
  }, c2.__r = function(n3) {
    a2 && a2(n3), t2 = 0;
    var i3 = (r2 = n3.__c).__H;
    i3 && (u2 === r2 ? (i3.__h = [], r2.__h = [], i3.__.forEach(function(n4) {
      n4.__N && (n4.__ = n4.__N), n4.u = n4.__N = void 0;
    })) : (i3.__h.forEach(z2), i3.__h.forEach(B2), i3.__h = [], t2 = 0)), u2 = r2;
  }, c2.diffed = function(n3) {
    v2 && v2(n3);
    var t3 = n3.__c;
    t3 && t3.__H && (t3.__H.__h.length && (1 !== f2.push(t3) && i2 === c2.requestAnimationFrame || ((i2 = c2.requestAnimationFrame) || w2)(j2)), t3.__H.__.forEach(function(n4) {
      n4.u && (n4.__H = n4.u), n4.u = void 0;
    })), u2 = r2 = null;
  }, c2.__c = function(n3, t3) {
    t3.some(function(n4) {
      try {
        n4.__h.forEach(z2), n4.__h = n4.__h.filter(function(n5) {
          return !n5.__ || B2(n5);
        });
      } catch (r3) {
        t3.some(function(n5) {
          n5.__h && (n5.__h = []);
        }), t3 = [], c2.__e(r3, n4.__v);
      }
    }), l2 && l2(n3, t3);
  }, c2.unmount = function(n3) {
    m2 && m2(n3);
    var t3, r3 = n3.__c;
    r3 && r3.__H && (r3.__H.__.forEach(function(n4) {
      try {
        z2(n4);
      } catch (n5) {
        t3 = n5;
      }
    }), r3.__H = void 0, t3 && c2.__e(t3, r3.__v));
  };
  var k2 = "function" == typeof requestAnimationFrame;
  function w2(n3) {
    var t3, r3 = function() {
      clearTimeout(u3), k2 && cancelAnimationFrame(t3), setTimeout(n3);
    }, u3 = setTimeout(r3, 35);
    k2 && (t3 = requestAnimationFrame(r3));
  }
  function z2(n3) {
    var t3 = r2, u3 = n3.__c;
    "function" == typeof u3 && (n3.__c = void 0, u3()), r2 = t3;
  }
  function B2(n3) {
    var t3 = r2;
    n3.__c = n3.__(), r2 = t3;
  }
  function C2(n3, t3) {
    return !n3 || n3.length !== t3.length || t3.some(function(t4, r3) {
      return t4 !== n3[r3];
    });
  }
  function D2(n3, t3) {
    return "function" == typeof t3 ? t3(n3) : t3;
  }

  // node_modules/preact/compat/dist/compat.module.js
  function g3(n3, t3) {
    for (var e3 in t3) n3[e3] = t3[e3];
    return n3;
  }
  function E2(n3, t3) {
    for (var e3 in n3) if ("__source" !== e3 && !(e3 in t3)) return true;
    for (var r3 in t3) if ("__source" !== r3 && n3[r3] !== t3[r3]) return true;
    return false;
  }
  function C3(n3, t3) {
    var e3 = t3(), r3 = d2({ t: { __: e3, u: t3 } }), u3 = r3[0].t, o3 = r3[1];
    return _2(function() {
      u3.__ = e3, u3.u = t3, R(u3) && o3({ t: u3 });
    }, [n3, e3, t3]), y2(function() {
      return R(u3) && o3({ t: u3 }), n3(function() {
        R(u3) && o3({ t: u3 });
      });
    }, [n3]), e3;
  }
  function R(n3) {
    var t3, e3, r3 = n3.u, u3 = n3.__;
    try {
      var o3 = r3();
      return !((t3 = u3) === (e3 = o3) && (0 !== t3 || 1 / t3 == 1 / e3) || t3 != t3 && e3 != e3);
    } catch (n4) {
      return true;
    }
  }
  function x3(n3) {
    n3();
  }
  function w3(n3) {
    return n3;
  }
  function k3() {
    return [false, x3];
  }
  var I2 = _2;
  function N2(n3, t3) {
    this.props = n3, this.context = t3;
  }
  function M2(n3, e3) {
    function r3(n4) {
      var t3 = this.props.ref, r4 = t3 == n4.ref;
      return !r4 && t3 && (t3.call ? t3(null) : t3.current = null), e3 ? !e3(this.props, n4) || !r4 : E2(this.props, n4);
    }
    function u3(e4) {
      return this.shouldComponentUpdate = r3, _(n3, e4);
    }
    return u3.displayName = "Memo(" + (n3.displayName || n3.name) + ")", u3.prototype.isReactComponent = true, u3.__f = true, u3.type = n3, u3;
  }
  (N2.prototype = new x()).isPureReactComponent = true, N2.prototype.shouldComponentUpdate = function(n3, t3) {
    return E2(this.props, n3) || E2(this.state, t3);
  };
  var T3 = l.__b;
  l.__b = function(n3) {
    n3.type && n3.type.__f && n3.ref && (n3.props.ref = n3.ref, n3.ref = null), T3 && T3(n3);
  };
  var A3 = "undefined" != typeof Symbol && Symbol.for && /* @__PURE__ */ Symbol.for("react.forward_ref") || 3911;
  function D3(n3) {
    function t3(t4) {
      var e3 = g3({}, t4);
      return delete e3.ref, n3(e3, t4.ref || null);
    }
    return t3.$$typeof = A3, t3.render = n3, t3.prototype.isReactComponent = t3.__f = true, t3.displayName = "ForwardRef(" + (n3.displayName || n3.name) + ")", t3;
  }
  var L2 = function(n3, t3) {
    return null == n3 ? null : H(H(n3).map(t3));
  };
  var O2 = { map: L2, forEach: L2, count: function(n3) {
    return n3 ? H(n3).length : 0;
  }, only: function(n3) {
    var t3 = H(n3);
    if (1 !== t3.length) throw "Children.only";
    return t3[0];
  }, toArray: H };
  var U = l.__e;
  l.__e = function(n3, t3, e3, r3) {
    if (n3.then) {
      for (var u3, o3 = t3; o3 = o3.__; ) if ((u3 = o3.__c) && u3.__c) return null == t3.__e && (t3.__e = e3.__e, t3.__k = e3.__k), u3.__c(n3, t3);
    }
    U(n3, t3, e3, r3);
  };
  var F3 = l.unmount;
  function V2(n3, t3, e3) {
    return n3 && (n3.__c && n3.__c.__H && (n3.__c.__H.__.forEach(function(n4) {
      "function" == typeof n4.__c && n4.__c();
    }), n3.__c.__H = null), null != (n3 = g3({}, n3)).__c && (n3.__c.__P === e3 && (n3.__c.__P = t3), n3.__c.__e = true, n3.__c = null), n3.__k = n3.__k && n3.__k.map(function(n4) {
      return V2(n4, t3, e3);
    })), n3;
  }
  function W(n3, t3, e3) {
    return n3 && e3 && (n3.__v = null, n3.__k = n3.__k && n3.__k.map(function(n4) {
      return W(n4, t3, e3);
    }), n3.__c && n3.__c.__P === t3 && (n3.__e && e3.appendChild(n3.__e), n3.__c.__e = true, n3.__c.__P = e3)), n3;
  }
  function P3() {
    this.__u = 0, this.o = null, this.__b = null;
  }
  function j3(n3) {
    if (!n3.__) return null;
    var t3 = n3.__.__c;
    return t3 && t3.__a && t3.__a(n3);
  }
  function z3(n3) {
    var e3, r3, u3, o3 = null;
    function i3(i4) {
      if (e3 || (e3 = n3()).then(function(n4) {
        n4 && (o3 = n4.default || n4), u3 = true;
      }, function(n4) {
        r3 = n4, u3 = true;
      }), r3) throw r3;
      if (!u3) throw e3;
      return o3 ? _(o3, i4) : null;
    }
    return i3.displayName = "Lazy", i3.__f = true, i3;
  }
  function B3() {
    this.i = null, this.l = null;
  }
  l.unmount = function(n3) {
    var t3 = n3.__c;
    t3 && (t3.__z = true), t3 && t3.__R && t3.__R(), t3 && 32 & n3.__u && (n3.type = null), F3 && F3(n3);
  }, (P3.prototype = new x()).__c = function(n3, t3) {
    var e3 = t3.__c, r3 = this;
    null == r3.o && (r3.o = []), r3.o.push(e3);
    var u3 = j3(r3.__v), o3 = false, i3 = function() {
      o3 || r3.__z || (o3 = true, e3.__R = null, u3 ? u3(c3) : c3());
    };
    e3.__R = i3;
    var l3 = e3.__P;
    e3.__P = null;
    var c3 = function() {
      if (!--r3.__u) {
        if (r3.state.__a) {
          var n4 = r3.state.__a;
          r3.__v.__k[0] = W(n4, n4.__c.__P, n4.__c.__O);
        }
        var t4;
        for (r3.setState({ __a: r3.__b = null }); t4 = r3.o.pop(); ) t4.__P = l3, t4.forceUpdate();
      }
    };
    r3.__u++ || 32 & t3.__u || r3.setState({ __a: r3.__b = r3.__v.__k[0] }), n3.then(i3, i3);
  }, P3.prototype.componentWillUnmount = function() {
    this.o = [];
  }, P3.prototype.render = function(n3, e3) {
    if (this.__b) {
      if (this.__v.__k) {
        var r3 = document.createElement("div"), o3 = this.__v.__k[0].__c;
        this.__v.__k[0] = V2(this.__b, r3, o3.__O = o3.__P);
      }
      this.__b = null;
    }
    var i3 = e3.__a && _(k, null, n3.fallback);
    return i3 && (i3.__u &= -33), [_(k, null, e3.__a ? null : n3.children), i3];
  };
  var H2 = function(n3, t3, e3) {
    if (++e3[1] === e3[0] && n3.l.delete(t3), n3.props.revealOrder && ("t" !== n3.props.revealOrder[0] || !n3.l.size)) for (e3 = n3.i; e3; ) {
      for (; e3.length > 3; ) e3.pop()();
      if (e3[1] < e3[0]) break;
      n3.i = e3 = e3[2];
    }
  };
  function Z(n3) {
    return this.getChildContext = function() {
      return n3.context;
    }, n3.children;
  }
  function Y(n3) {
    var e3 = this, r3 = n3.h;
    if (e3.componentWillUnmount = function() {
      G(null, e3.v), e3.v = null, e3.h = null;
    }, e3.h && e3.h !== r3 && e3.componentWillUnmount(), !e3.v) {
      for (var u3 = e3.__v; null !== u3 && !u3.__m && null !== u3.__; ) u3 = u3.__;
      e3.h = r3, e3.v = { nodeType: 1, parentNode: r3, childNodes: [], __k: { __m: u3.__m }, contains: function() {
        return true;
      }, namespaceURI: r3.namespaceURI, insertBefore: function(n4, t3) {
        this.childNodes.push(n4), e3.h.insertBefore(n4, t3);
      }, removeChild: function(n4) {
        this.childNodes.splice(this.childNodes.indexOf(n4) >>> 1, 1), e3.h.removeChild(n4);
      } };
    }
    G(_(Z, { context: e3.context }, n3.__v), e3.v);
  }
  function $2(n3, e3) {
    var r3 = _(Y, { __v: n3, h: e3 });
    return r3.containerInfo = e3, r3;
  }
  (B3.prototype = new x()).__a = function(n3) {
    var t3 = this, e3 = j3(t3.__v), r3 = t3.l.get(n3);
    return r3[0]++, function(u3) {
      var o3 = function() {
        t3.props.revealOrder ? (r3.push(u3), H2(t3, n3, r3)) : u3();
      };
      e3 ? e3(o3) : o3();
    };
  }, B3.prototype.render = function(n3) {
    this.i = null, this.l = /* @__PURE__ */ new Map();
    var t3 = H(n3.children);
    n3.revealOrder && "b" === n3.revealOrder[0] && t3.reverse();
    for (var e3 = t3.length; e3--; ) this.l.set(t3[e3], this.i = [1, 0, this.i]);
    return n3.children;
  }, B3.prototype.componentDidUpdate = B3.prototype.componentDidMount = function() {
    var n3 = this;
    this.l.forEach(function(t3, e3) {
      H2(n3, e3, t3);
    });
  };
  var q3 = "undefined" != typeof Symbol && Symbol.for && /* @__PURE__ */ Symbol.for("react.element") || 60103;
  var G2 = /^(?:accent|alignment|arabic|baseline|cap|clip(?!PathU)|color|dominant|fill|flood|font|glyph(?!R)|horiz|image(!S)|letter|lighting|marker(?!H|W|U)|overline|paint|pointer|shape|stop|strikethrough|stroke|text(?!L)|transform|underline|unicode|units|v|vector|vert|word|writing|x(?!C))[A-Z]/;
  var J2 = /^on(Ani|Tra|Tou|BeforeInp|Compo)/;
  var K2 = /[A-Z0-9]/g;
  var Q2 = "undefined" != typeof document;
  var X = function(n3) {
    return ("undefined" != typeof Symbol && "symbol" == typeof /* @__PURE__ */ Symbol() ? /fil|che|rad/ : /fil|che|ra/).test(n3);
  };
  function nn(n3, t3, e3) {
    return null == t3.__k && (t3.textContent = ""), G(n3, t3), "function" == typeof e3 && e3(), n3 ? n3.__c : null;
  }
  function tn(n3, t3, e3) {
    return J(n3, t3), "function" == typeof e3 && e3(), n3 ? n3.__c : null;
  }
  x.prototype.isReactComponent = {}, ["componentWillMount", "componentWillReceiveProps", "componentWillUpdate"].forEach(function(t3) {
    Object.defineProperty(x.prototype, t3, { configurable: true, get: function() {
      return this["UNSAFE_" + t3];
    }, set: function(n3) {
      Object.defineProperty(this, t3, { configurable: true, writable: true, value: n3 });
    } });
  });
  var en = l.event;
  function rn() {
  }
  function un() {
    return this.cancelBubble;
  }
  function on() {
    return this.defaultPrevented;
  }
  l.event = function(n3) {
    return en && (n3 = en(n3)), n3.persist = rn, n3.isPropagationStopped = un, n3.isDefaultPrevented = on, n3.nativeEvent = n3;
  };
  var ln;
  var cn = { enumerable: false, configurable: true, get: function() {
    return this.class;
  } };
  var fn = l.vnode;
  l.vnode = function(n3) {
    "string" == typeof n3.type && (function(n4) {
      var t3 = n4.props, e3 = n4.type, u3 = {}, o3 = -1 === e3.indexOf("-");
      for (var i3 in t3) {
        var l3 = t3[i3];
        if (!("value" === i3 && "defaultValue" in t3 && null == l3 || Q2 && "children" === i3 && "noscript" === e3 || "class" === i3 || "className" === i3)) {
          var c3 = i3.toLowerCase();
          "defaultValue" === i3 && "value" in t3 && null == t3.value ? i3 = "value" : "download" === i3 && true === l3 ? l3 = "" : "translate" === c3 && "no" === l3 ? l3 = false : "o" === c3[0] && "n" === c3[1] ? "ondoubleclick" === c3 ? i3 = "ondblclick" : "onchange" !== c3 || "input" !== e3 && "textarea" !== e3 || X(t3.type) ? "onfocus" === c3 ? i3 = "onfocusin" : "onblur" === c3 ? i3 = "onfocusout" : J2.test(i3) && (i3 = c3) : c3 = i3 = "oninput" : o3 && G2.test(i3) ? i3 = i3.replace(K2, "-$&").toLowerCase() : null === l3 && (l3 = void 0), "oninput" === c3 && u3[i3 = c3] && (i3 = "oninputCapture"), u3[i3] = l3;
        }
      }
      "select" == e3 && u3.multiple && Array.isArray(u3.value) && (u3.value = H(t3.children).forEach(function(n5) {
        n5.props.selected = -1 != u3.value.indexOf(n5.props.value);
      })), "select" == e3 && null != u3.defaultValue && (u3.value = H(t3.children).forEach(function(n5) {
        n5.props.selected = u3.multiple ? -1 != u3.defaultValue.indexOf(n5.props.value) : u3.defaultValue == n5.props.value;
      })), t3.class && !t3.className ? (u3.class = t3.class, Object.defineProperty(u3, "className", cn)) : (t3.className && !t3.class || t3.class && t3.className) && (u3.class = u3.className = t3.className), n4.props = u3;
    })(n3), n3.$$typeof = q3, fn && fn(n3);
  };
  var an = l.__r;
  l.__r = function(n3) {
    an && an(n3), ln = n3.__c;
  };
  var sn = l.diffed;
  l.diffed = function(n3) {
    sn && sn(n3);
    var t3 = n3.props, e3 = n3.__e;
    null != e3 && "textarea" === n3.type && "value" in t3 && t3.value !== e3.value && (e3.value = null == t3.value ? "" : t3.value), ln = null;
  };
  var hn = { ReactCurrentDispatcher: { current: { readContext: function(n3) {
    return ln.__n[n3.__c].props.value;
  }, useCallback: q2, useContext: x2, useDebugValue: P2, useDeferredValue: w3, useEffect: y2, useId: g2, useImperativeHandle: F2, useInsertionEffect: I2, useLayoutEffect: _2, useMemo: T2, useReducer: h2, useRef: A2, useState: d2, useSyncExternalStore: C3, useTransition: k3 } } };
  function dn(n3) {
    return _.bind(null, n3);
  }
  function mn(n3) {
    return !!n3 && n3.$$typeof === q3;
  }
  function pn(n3) {
    return mn(n3) && n3.type === k;
  }
  function yn(n3) {
    return !!n3 && !!n3.displayName && ("string" == typeof n3.displayName || n3.displayName instanceof String) && n3.displayName.startsWith("Memo(");
  }
  function _n(n3) {
    return mn(n3) ? K.apply(null, arguments) : n3;
  }
  function bn(n3) {
    return !!n3.__k && (G(null, n3), true);
  }
  function Sn(n3) {
    return n3 && (n3.base || 1 === n3.nodeType && n3) || null;
  }
  var gn = function(n3, t3) {
    return n3(t3);
  };
  var En = function(n3, t3) {
    return n3(t3);
  };
  var Cn = k;
  var Rn = mn;
  var xn = { useState: d2, useId: g2, useReducer: h2, useEffect: y2, useLayoutEffect: _2, useInsertionEffect: I2, useTransition: k3, useDeferredValue: w3, useSyncExternalStore: C3, startTransition: x3, useRef: A2, useImperativeHandle: F2, useMemo: T2, useCallback: q2, useContext: x2, useDebugValue: P2, version: "18.3.1", Children: O2, render: nn, hydrate: tn, unmountComponentAtNode: bn, createPortal: $2, createElement: _, createContext: Q, createFactory: dn, cloneElement: _n, createRef: b, Fragment: k, isValidElement: mn, isElement: Rn, isFragment: pn, isMemo: yn, findDOMNode: Sn, Component: x, PureComponent: N2, memo: M2, forwardRef: D3, flushSync: En, unstable_batchedUpdates: gn, StrictMode: Cn, Suspense: P3, SuspenseList: B3, lazy: z3, __SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED: hn };

  // node_modules/preact/compat/client.mjs
  function createRoot(container2) {
    return {
      // eslint-disable-next-line
      render: function(children) {
        nn(children, container2);
      },
      // eslint-disable-next-line
      unmount: function() {
        bn(container2);
      }
    };
  }

  // node_modules/natively/dist/utils/globalThis.js
  function getGlobalContext() {
    if (typeof globalThis !== "undefined") {
      return globalThis;
    }
    if (typeof self !== "undefined") {
      return self;
    }
    if (typeof window !== "undefined") {
      return window;
    }
    if (typeof global !== "undefined") {
      return global;
    }
    return null;
  }
  var globalContext = getGlobalContext();
  var globalThis_default = globalContext;

  // node_modules/natively/dist/classes/Natively.js
  function asyncGeneratorStep(n3, t3, e3, r3, o3, a3, c3) {
    try {
      var i3 = n3[a3](c3), u3 = i3.value;
    } catch (n4) {
      return void e3(n4);
    }
    i3.done ? t3(u3) : Promise.resolve(u3).then(r3, o3);
  }
  function _asyncToGenerator(n3) {
    return function() {
      var t3 = this, e3 = arguments;
      return new Promise(function(r3, o3) {
        var a3 = n3.apply(t3, e3);
        function _next(n4) {
          asyncGeneratorStep(a3, r3, o3, _next, _throw, "next", n4);
        }
        function _throw(n4) {
          asyncGeneratorStep(a3, r3, o3, _next, _throw, "throw", n4);
        }
        _next(void 0);
      });
    };
  }
  function _defineProperty(e3, r3, t3) {
    return (r3 = _toPropertyKey(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey(t3) {
    var i3 = _toPrimitive(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var Natively = class {
    constructor() {
      var _globalContext$naviga, _globalContext$naviga2;
      _defineProperty(this, "isDebug", false);
      _defineProperty(this, "min_app_version", 0);
      _defineProperty(this, "app_version", 0);
      _defineProperty(this, "injected", false);
      _defineProperty(this, "observers", []);
      _defineProperty(this, "onNativeError", null);
      _defineProperty(this, "onTabChanged", null);
      _defineProperty(this, "isIOSApp", (globalThis_default === null || globalThis_default === void 0 || (_globalContext$naviga = globalThis_default.navigator) === null || _globalContext$naviga === void 0 || (_globalContext$naviga = _globalContext$naviga.userAgent) === null || _globalContext$naviga === void 0 ? void 0 : _globalContext$naviga.includes("Natively/iOS")) || false);
      _defineProperty(this, "isAndroidApp", (globalThis_default === null || globalThis_default === void 0 || (_globalContext$naviga2 = globalThis_default.navigator) === null || _globalContext$naviga2 === void 0 || (_globalContext$naviga2 = _globalContext$naviga2.userAgent) === null || _globalContext$naviga2 === void 0 ? void 0 : _globalContext$naviga2.includes("Natively/Android")) || false);
    }
    setDebug(isDebug) {
      if (globalThis_default) {
        globalThis_default.natively.isDebug = isDebug;
      }
    }
    notify(min, current) {
      if (globalThis_default) {
        globalThis_default.natively.injected = true;
        if (min) {
          globalThis_default.natively.min_app_version = min;
        }
        if (current) {
          globalThis_default.natively.app_version = current;
        }
      }
      var observers = globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.observers;
      if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isDebug) {
        console.log("[INFO] Notify observers: ", observers.length);
      }
      while (observers.length > 0) {
        var observer = observers.shift();
        observer === null || observer === void 0 || observer();
      }
    }
    addObserver(fn2) {
      if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.injected) {
        fn2();
      } else {
        if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isDebug) {
          console.log("[DEBUG] addObserver: ".concat(fn2));
        }
        globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.observers.push(fn2);
      }
    }
    trigger(respId, minVersion, callback, method, body) {
      var isTestVersion = globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.isDebug;
      if (!(globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.injected)) {
        globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.addObserver(() => {
          globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(respId, minVersion, callback, method, body);
        });
        return;
      }
      if (minVersion > (globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.app_version)) {
        if (isTestVersion) {
          alert("[ERROR] Please rebuild the app to use this functionality. App Version: ".concat(globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.app_version, ", feature version: ").concat(minVersion));
        }
        return;
      }
      if (callback) {
        var fullMethodName;
        if (respId) {
          fullMethodName = method + "_response_" + respId;
        } else {
          fullMethodName = method + "_response";
        }
        if (globalThis_default) {
          globalThis_default[fullMethodName] = function(resp, err) {
            globalThis_default === null || globalThis_default === void 0 || globalThis_default.$agent.response();
            if (err.message && isTestVersion) {
              alert("[ERROR] Error message: ".concat(err.message));
              return;
            }
            if (isTestVersion) {
              console.log("[DEBUG] Callback method: ".concat(fullMethodName, ", body: ").concat(JSON.stringify(resp), ", respId: ").concat(respId));
            }
            callback(resp);
          };
        }
        if (body) {
          body.response_id = respId;
        } else {
          body = {
            response_id: respId
          };
        }
      }
      if (isTestVersion) {
        console.log("[DEBUG] Trigger method: ".concat(method, ", body: ").concat(JSON.stringify(body)));
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.$agent.trigger(method, body);
    }
    setTabChangedHandler(callback) {
      if (typeof callback !== "function") {
        console.warn("[Natively] Tab changed handler must be a function");
        return;
      }
      this.onTabChanged = callback;
    }
    removeTabChangedHandler() {
      this.onTabChanged = null;
    }
    setErrorHandler(callback) {
      if (typeof callback !== "function") {
        return;
      }
      this.onNativeError = callback;
    }
    removeErrorHandler() {
      this.onNativeError = null;
    }
    setErrorScreen(showError) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "error_screen", {
        show_error: showError
      });
    }
    openLogger() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.$agent.natively_logger();
    }
    openConsole() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "app_console");
    }
    closeApp() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 11, void 0, "app_close");
    }
    showProgress(toggle) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 11, void 0, "app_show_progress", {
        toggle
      });
    }
    shareImage(image_url) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "share_image", {
        url: image_url
      });
    }
    shareText(text) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "share_text", {
        text
      });
    }
    shareTextAndImage(text, image_url) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "share_text_and_image", {
        url: image_url,
        text
      });
    }
    shareFile(file_url) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 2, void 0, "share_file", {
        url: file_url
      });
    }
    openExternalURL(url, external) {
      var params = {
        url: typeof url === "undefined" ? "https://buildnatively.com" : url,
        view: typeof external !== "undefined" && external ? "external" : "web"
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 18, void 0, "open_link", params);
    }
    openExternalApp(url) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "open_app", {
        url
      });
    }
    showAppToast(type, text) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "show_toast", {
        text: text || "",
        type: type || "DEFAULT"
      });
    }
    showAppBanner(type, title, description) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "show_banner", {
        type: type || "INFO",
        title: title || "",
        description: description || ""
      });
    }
    analyticsTrackEvent(name5, data) {
      var params = {
        name: name5
      };
      if (data) {
        params.data = data;
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 37, void 0, "analytics_log_event", params);
    }
    requestAppReview() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "request_review");
    }
    getInsets(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 36, callback, "get_insets");
    }
    getLocales(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 40, callback, "get_locales");
    }
    setLocale(locale, callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 40, callback, "set_locale", {
        locale
      });
    }
    setAppBackgroundColor(color) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 1, void 0, "app_background", {
        color
      });
    }
    setAppProgressColor(color) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 1, void 0, "app_progress", {
        color
      });
    }
    setAppSwipeNavigation(toggle) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "app_navigation", {
        toggle
      });
    }
    setAppPullToRefresh(toggle) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 1, void 0, "app_pull", {
        toggle
      });
    }
    setAppOrientation(orientation) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 3, void 0, "app_orientation", {
        orientation
      });
    }
    setAppStatusBarStyle(style) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "status_bar_style", {
        style
      });
    }
    hideLoadingScreen() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 17, void 0, "loading_screen", {
        show_loader: false,
        auto_hide: true
      });
    }
    showLoadingScreen(autoHide) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 17, void 0, "loading_screen", {
        show_loader: true,
        auto_hide: autoHide || false
      });
    }
    openAppSettings() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, void 0, "open_appsettings");
    }
    showTabBar() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 33, void 0, "show_tab_bar");
    }
    enableWakelock() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 34, void 0, "wakelock_enable");
    }
    disableWakelock() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 34, void 0, "wakelock_disable");
    }
    hideTabBar() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 33, void 0, "hide_tab_bar");
    }
    reloadWebview() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 35, void 0, "reset_webview");
    }
    hapticPattern(pattern, delay) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "haptic_pattern", {
        pattern,
        delay
      });
    }
    openPDF(options, callback) {
      var params = {
        url: options.url || "",
        base64: options.base64 || "",
        fileName: options.fileName || "pdf-".concat((/* @__PURE__ */ new Date()).getTime(), ".pdf"),
        download: options.download === true
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 38, callback, "open_pdf", params);
    }
    hapticImpact(type) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "haptic_impact", {
        type
      });
    }
    hapticNotification(type) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 22, void 0, "haptic_notification", {
        type
      });
    }
    sendPushNotification(appId, payload, player_ids, isPreview) {
      return _asyncToGenerator(function* () {
        var filtered = player_ids.filter((id) => id.length > 0);
        var include_player_ids = [...new Set(filtered)];
        var notification = {
          app_id: isPreview ? "be83022a-1d08-45d0-a07a-0c3655666e17" : appId,
          include_player_ids
        };
        if (payload.template_id) {
          notification.template_id = payload.template_id;
        } else {
          notification.headings = {
            en: payload.title || "Empty Title"
          };
          notification.contents = {
            en: payload.message || "Empty Message"
          };
          if (payload.subtitle) {
            notification.subtitle = {
              en: payload.subtitle
            };
          }
          if (payload.redirect_url) {
            notification.url = payload.redirect_url;
          }
        }
        var options = {
          method: "POST",
          headers: {
            "Content-type": "application/json"
          },
          body: JSON.stringify(notification)
        };
        return yield fetch("https://onesignal.com/api/v1/notifications", options);
      })();
    }
  };

  // node_modules/natively/dist/utils/generateID.js
  var generateID = () => Date.now().toString(36) + Math.random().toString(36).substring(2);

  // node_modules/natively/dist/classes/NativelyAppleSignInService.js
  function _defineProperty2(e3, r3, t3) {
    return (r3 = _toPropertyKey2(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey2(t3) {
    var i3 = _toPrimitive2(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive2(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyAppleSignInService2 = class {
    constructor() {
      _defineProperty2(this, "id", generateID());
    }
    signin(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 16, callback, "apple_signin", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyClipboard.js
  function _defineProperty3(e3, r3, t3) {
    return (r3 = _toPropertyKey3(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey3(t3) {
    var i3 = _toPrimitive3(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive3(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyClipboard = class {
    constructor() {
      _defineProperty3(this, "id", generateID());
    }
    copy(text) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 11, void 0, "clipboard_copy", {
        text
      });
    }
    paste(paste_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 11, paste_callback, "clipboard_paste");
    }
  };

  // node_modules/natively/dist/classes/NativelyInfo.js
  function _defineProperty4(e3, r3, t3) {
    return (r3 = _toPropertyKey4(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey4(t3) {
    var i3 = _toPrimitive4(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive4(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyInfo = class {
    constructor() {
      _defineProperty4(this, "id", generateID());
    }
    browserInfo() {
      var _globalContext$naviga, _globalContext$naviga2;
      var isNativeApp = typeof (globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.$agent) !== "undefined";
      var isIOSApp = (globalThis_default === null || globalThis_default === void 0 || (_globalContext$naviga = globalThis_default.navigator) === null || _globalContext$naviga === void 0 || (_globalContext$naviga = _globalContext$naviga.userAgent) === null || _globalContext$naviga === void 0 ? void 0 : _globalContext$naviga.includes("Natively/iOS")) || false;
      var isAndroidApp = (globalThis_default === null || globalThis_default === void 0 || (_globalContext$naviga2 = globalThis_default.navigator) === null || _globalContext$naviga2 === void 0 || (_globalContext$naviga2 = _globalContext$naviga2.userAgent) === null || _globalContext$naviga2 === void 0 ? void 0 : _globalContext$naviga2.includes("Natively/Android")) || false;
      return {
        isNativeApp,
        isIOSApp,
        isAndroidApp
      };
    }
    getAppInfo(app_info_callback) {
      if (!(globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively)) return;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, app_info_callback, "app_info");
    }
    connectivity(connectivity_callback) {
      if (!(globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively)) return;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 0, connectivity_callback, "connectivity");
    }
    app_state(app_state_callback) {
      if (!(globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively)) return;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 19, app_state_callback, "app_state");
    }
    keyboard_visibility(keyboard_visibility_callback) {
      if (!(globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively)) return;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 34, keyboard_visibility_callback, "keyboard_visibility");
    }
  };

  // node_modules/natively/dist/classes/NativelyNotifications.js
  function _defineProperty5(e3, r3, t3) {
    return (r3 = _toPropertyKey5(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey5(t3) {
    var i3 = _toPrimitive5(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive5(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyNotifications = class {
    constructor() {
      _defineProperty5(this, "id", generateID());
    }
    getOneSignalId(onesignal_playerid_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, onesignal_playerid_callback, "onesignal_playerid");
    }
    requestPermission(fallbackToSettings, push_register_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, push_register_callback, "push_register", {
        fallbackToSettings
      });
    }
    getPermissionStatus(push_permission_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, push_permission_callback, "push_permission");
    }
    getExternalId(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, callback, "onesignal_externalid");
    }
    setExternalId(data, callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, callback, "onesignal_setexternalid", data);
    }
    removeExternalId(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, callback, "onesignal_removeexternalid");
    }
  };

  // node_modules/natively/dist/classes/NativelyGeolocation.js
  function _defineProperty6(e3, r3, t3) {
    return (r3 = _toPropertyKey6(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey6(t3) {
    var i3 = _toPrimitive6(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive6(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyGeolocation = class {
    constructor() {
      _defineProperty6(this, "id", generateID());
    }
    getUserGeolocation(distance, geolocation_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, geolocation_callback, "geolocation", {
        distance
      });
    }
    requestPermission(geo_register_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, geo_register_callback, "geo_register");
    }
    getPermissionStatus(geo_permission_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, geo_permission_callback, "geo_permission");
    }
  };

  // node_modules/natively/dist/classes/NativelyLocation.js
  function _defineProperty7(e3, r3, t3) {
    return (r3 = _toPropertyKey7(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey7(t3) {
    var i3 = _toPrimitive7(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive7(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyLocation = class {
    constructor() {
      _defineProperty7(this, "id", generateID());
    }
    current(minAccuracyIOS, accuracyTypeIOS, priority_android, location_callback) {
      var fallback_to_settings = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : true;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 12, location_callback, "location_current", {
        minAccuracy: minAccuracyIOS,
        accuracyType: accuracyTypeIOS,
        priority: priority_android,
        fallbackToSettings: fallback_to_settings
      });
    }
    permission(location_permission_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 6, location_permission_callback, "location_permission");
    }
    start(interval, minAccuracyIOS, accuracyTypeIOS, priority_android, location_callback) {
      var fallback_to_settings = arguments.length > 5 && arguments[5] !== void 0 ? arguments[5] : true;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 12, location_callback, "location_start", {
        minAccuracy: minAccuracyIOS,
        accuracyType: accuracyTypeIOS,
        priority: priority_android,
        fallbackToSettings: fallback_to_settings,
        interval
      });
    }
    stop() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, void 0, "location_stop", {});
    }
    startBackground(interval, minAccuracyIOS, accuracyTypeIOS, priority_android, responseIdentifier, location_bg_callback) {
      var fallback_to_settings = arguments.length > 6 && arguments[6] !== void 0 ? arguments[6] : true;
      var params = {
        identifier: responseIdentifier !== null && responseIdentifier !== void 0 ? responseIdentifier : "empty",
        interval: interval !== null && interval !== void 0 ? interval : 1e3 * 60,
        minAccuracy: minAccuracyIOS !== null && minAccuracyIOS !== void 0 ? minAccuracyIOS : 50,
        accuracyType: accuracyTypeIOS !== null && accuracyTypeIOS !== void 0 ? accuracyTypeIOS : "Best",
        priority: priority_android !== null && priority_android !== void 0 ? priority_android : "BALANCED",
        fallbackToSettings: fallback_to_settings
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 12, location_bg_callback, "location_start_bg", params);
    }
    statusBackground(location_bg_status_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 20, location_bg_status_callback, "location_status_bg", {});
    }
    stopBackground(location_bg_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 4, location_bg_callback, "location_stop_bg", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyMessage.js
  function _defineProperty8(e3, r3, t3) {
    return (r3 = _toPropertyKey8(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey8(t3) {
    var i3 = _toPrimitive8(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive8(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyMessage = class {
    constructor() {
      _defineProperty8(this, "id", generateID());
    }
    sendSMS(body, recipient, send_sms_callback) {
      var params = {
        body: body !== null && body !== void 0 ? body : "",
        recipient: recipient !== null && recipient !== void 0 ? recipient : ""
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, send_sms_callback, "send_sms", params);
    }
    sendEmail(subject, body, recipient, send_email_callback) {
      var params = {
        subject: subject !== null && subject !== void 0 ? subject : "",
        body: body !== null && body !== void 0 ? body : "",
        recipient: recipient !== null && recipient !== void 0 ? recipient : ""
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, send_email_callback, "send_email", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyStorage.js
  function _defineProperty9(e3, r3, t3) {
    return (r3 = _toPropertyKey9(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey9(t3) {
    var i3 = _toPrimitive9(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive9(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyStorage = class {
    constructor() {
      _defineProperty9(this, "id", generateID());
    }
    setStorageValue(key, value) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, void 0, "set_storage_value", {
        key,
        value
      });
    }
    getStorageValue(key, get_storage_value_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, get_storage_value_callback, "get_storage_value", {
        key
      });
    }
    removeStorageValue(key) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, void 0, "remove_storage_value", {
        key
      });
    }
    resetStorage() {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, void 0, "reset_storage");
    }
  };

  // node_modules/natively/dist/classes/NativelyBiometrics.js
  function _defineProperty10(e3, r3, t3) {
    return (r3 = _toPropertyKey10(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey10(t3) {
    var i3 = _toPrimitive10(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive10(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyBiometrics = class {
    constructor(allowPass) {
      _defineProperty10(this, "id", generateID());
      _defineProperty10(this, "allowPass", void 0);
      this.allowPass = allowPass;
    }
    checkBiometricsSupport(biometrics_support_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_support_callback, "biometrics_support", {
        allowPass: this.allowPass
      });
    }
    checkCredentials(biometrics_has_credentials_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_has_credentials_callback, "biometrics_has_credentials");
    }
    verifyUserIdentify(biometrics_verify_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_verify_callback, "biometrics_verify", {
        allowPass: this.allowPass
      });
    }
    getUserCredentials(biometrics_auth_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_auth_callback, "biometrics_auth", {
        allowPass: this.allowPass
      });
    }
    removeUserCredentials(biometrics_remove_credentials_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_remove_credentials_callback, "biometrics_remove_credentials");
    }
    saveUserCredentials(login, password, biometrics_auth_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, biometrics_auth_callback, "biometrics_auth", {
        allowPass: this.allowPass,
        login,
        password
      });
    }
  };

  // node_modules/natively/dist/classes/NativelyDatePicker.js
  function _defineProperty11(e3, r3, t3) {
    return (r3 = _toPropertyKey11(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey11(t3) {
    var i3 = _toPrimitive11(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive11(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyDatePicker = class {
    constructor() {
      _defineProperty11(this, "id", generateID());
    }
    showDatePicker(title, description, type, style, datepicker_callback) {
      var params = {
        type: type !== null && type !== void 0 ? type : "DATE",
        style: style !== null && style !== void 0 ? style : "LIGHT",
        title: title !== null && title !== void 0 ? title : "",
        description: description !== null && description !== void 0 ? description : ""
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 0, datepicker_callback, "datepicker", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyCamera.js
  function _defineProperty12(e3, r3, t3) {
    return (r3 = _toPropertyKey12(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey12(t3) {
    var i3 = _toPrimitive12(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive12(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyCamera = class {
    constructor() {
      _defineProperty12(this, "id", generateID());
    }
    showCamera(type, quality, camera, open_camera_callback) {
      var params = {
        type: type !== null && type !== void 0 ? type : "photo",
        quality: quality !== null && quality !== void 0 ? quality : "high",
        camera: camera !== null && camera !== void 0 ? camera : "BACK"
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 2, open_camera_callback, "open_camera", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyHealth.js
  function ownKeys(e3, r3) {
    var t3 = Object.keys(e3);
    if (Object.getOwnPropertySymbols) {
      var o3 = Object.getOwnPropertySymbols(e3);
      r3 && (o3 = o3.filter(function(r4) {
        return Object.getOwnPropertyDescriptor(e3, r4).enumerable;
      })), t3.push.apply(t3, o3);
    }
    return t3;
  }
  function _objectSpread(e3) {
    for (var r3 = 1; r3 < arguments.length; r3++) {
      var t3 = null != arguments[r3] ? arguments[r3] : {};
      r3 % 2 ? ownKeys(Object(t3), true).forEach(function(r4) {
        _defineProperty13(e3, r4, t3[r4]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e3, Object.getOwnPropertyDescriptors(t3)) : ownKeys(Object(t3)).forEach(function(r4) {
        Object.defineProperty(e3, r4, Object.getOwnPropertyDescriptor(t3, r4));
      });
    }
    return e3;
  }
  function _defineProperty13(e3, r3, t3) {
    return (r3 = _toPropertyKey13(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey13(t3) {
    var i3 = _toPrimitive13(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive13(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyHealth = class {
    constructor() {
      _defineProperty13(this, "id", generateID());
    }
    available(available_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, available_callback, "health_available", {});
    }
    requestAuthorization(write_data_types, read_data_types, request_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, request_callback, "health_register", {
        write_data_types,
        read_data_types
      });
    }
    permissionStatus(data_type, callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, callback, "health_permission", {
        data_type
      });
    }
    getAllCharacteristics(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, callback, "health_get_all_characteristics", {});
    }
    getStatisticQuantity(data_type, interval, start_date, end_date, callback) {
      var obj = {
        data_type,
        interval
      };
      if (start_date) {
        obj.start_date = start_date.getTime();
      }
      if (end_date) {
        obj.end_date = end_date.getTime();
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, callback, "health_get_statistic_quantity", obj);
    }
    getDailySleepAnalysis(start_date, end_date, limit, callback) {
      var obj = {
        limit: limit || 100
      };
      if (start_date) {
        obj.start_date = start_date.getTime();
      }
      if (end_date) {
        obj.end_date = end_date.getTime();
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, callback, "health_get_daily_sleep_analysis", obj);
    }
    getWorkouts(start_date, end_date, limit, callback) {
      var obj = {
        limit: limit || 100
      };
      if (start_date) {
        obj.start_date = start_date.getTime();
      }
      if (end_date) {
        obj.end_date = end_date.getTime();
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 21, callback, "health_get_workouts", obj);
    }
    getHealthConnectStatus(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 40, callback, "health_connect_status", {});
    }
    installHealthConnect(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 40, callback, "health_connect_install", {});
    }
    writeWorkout(workout, callback) {
      var obj = _objectSpread({}, workout);
      if ((workout === null || workout === void 0 ? void 0 : workout.start_date) instanceof Date) {
        obj.start_date = workout.start_date.getTime();
      }
      if ((workout === null || workout === void 0 ? void 0 : workout.end_date) instanceof Date) {
        obj.end_date = workout.end_date.getTime();
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, callback, "health_write_workout", obj);
    }
    writeNutrition(nutrition, callback) {
      var obj = _objectSpread({}, nutrition);
      if ((nutrition === null || nutrition === void 0 ? void 0 : nutrition.start_date) instanceof Date) {
        obj.start_date = nutrition.start_date.getTime();
      }
      if ((nutrition === null || nutrition === void 0 ? void 0 : nutrition.end_date) instanceof Date) {
        obj.end_date = nutrition.end_date.getTime();
      }
      if (Array.isArray(nutrition === null || nutrition === void 0 ? void 0 : nutrition.items)) {
        obj.items = nutrition.items.map((item) => {
          var mappedItem = _objectSpread({}, item);
          if ((item === null || item === void 0 ? void 0 : item.start_date) instanceof Date) {
            mappedItem.start_date = item.start_date.getTime();
          }
          if ((item === null || item === void 0 ? void 0 : item.end_date) instanceof Date) {
            mappedItem.end_date = item.end_date.getTime();
          }
          return mappedItem;
        });
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, callback, "health_write_nutrition", obj);
    }
    getActivitySummary(start_date, end_date, callback) {
      var obj = {};
      if (start_date) {
        obj.start_date = start_date.getTime();
      }
      if (end_date) {
        obj.end_date = end_date.getTime();
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, callback, "health_get_activity_summary", obj);
    }
  };

  // node_modules/natively/dist/classes/NativelyScanner.js
  function _defineProperty14(e3, r3, t3) {
    return (r3 = _toPropertyKey14(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey14(t3) {
    var i3 = _toPrimitive14(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive14(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyScanner = class {
    constructor() {
      _defineProperty14(this, "id", generateID());
    }
    showScanner(open_scanner_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 2, open_scanner_callback, "open_scanner", {});
    }
    showDocumentScanner(jpegQuality, foregroundColor, backgroundColor, highlightColor, menuColor, defaultFlashMode, defaultScanOrientation, source, ocrLanguage, pageMode, defaultFilter, availableFilters, resultCallback) {
      var params = {};
      if (source) params.source = source;
      if (menuColor) params.menuColor = menuColor;
      if (pageMode) params.pageMode = pageMode;
      if (jpegQuality != null && !isNaN(jpegQuality)) params.jpegQuality = jpegQuality;
      if (highlightColor) params.highlightColor = highlightColor;
      if (foregroundColor) params.foregroundColor = foregroundColor;
      if (backgroundColor) params.backgroundColor = backgroundColor;
      if (defaultFlashMode) params.defaultFlashMode = defaultFlashMode;
      if (defaultScanOrientation) params.defaultScanOrientation = defaultScanOrientation;
      if (defaultFilter) params.defaultFilter = defaultFilter;
      if (availableFilters) params.availableFilters = availableFilters;
      if (ocrLanguage) {
        params.ocrConfiguration = {
          languages: [ocrLanguage]
        };
      }
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 21, resultCallback, "open_document_scanner", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyPurchases.js
  function _defineProperty15(e3, r3, t3) {
    return (r3 = _toPropertyKey15(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey15(t3) {
    var i3 = _toPrimitive15(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive15(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyPurchases = class {
    constructor() {
      _defineProperty15(this, "id", generateID());
    }
    login(login, customerEmail, login_callback) {
      var email = customerEmail !== null && customerEmail !== void 0 ? customerEmail : "";
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, login_callback, "purchases_login", {
        login,
        email
      });
    }
    logout(logout_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, logout_callback, "purchases_logout", {});
    }
    customerId(customer_id_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, customer_id_callback, "purchases_customerid", {});
    }
    restore(restore_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 10, restore_callback, "purchases_restore", {});
    }
    showPaywall(shouldShowCloseButton, offeringId, show_paywall_callback) {
      var params = {
        offeringId,
        shouldShowCloseButton
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 38, show_paywall_callback, "purchases_show_paywall", params);
    }
    showPaywallIfNeeded(entitlementId, shouldShowCloseButton, offeringId, show_paywall_if_needed_callback) {
      var params = {
        offeringName: offeringId,
        entitlementId,
        shouldShowCloseButton
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 38, show_paywall_if_needed_callback, "purchases_show_paywall_if_needed", params);
    }
    purchasePackage(packageId, purchase_callback, oldProductId, prorationMode) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, purchase_callback, "purchases_package", {
        packageId,
        oldProductId: oldProductId !== null && oldProductId !== void 0 ? oldProductId : null,
        prorationMode: prorationMode !== null && prorationMode !== void 0 ? prorationMode : null
      });
    }
    packagePrice(packageId, purchase_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 8, purchase_callback, "purchases_price", {
        packageId
      });
    }
  };

  // node_modules/natively/dist/classes/NativelyContacts.js
  function _defineProperty16(e3, r3, t3) {
    return (r3 = _toPropertyKey16(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey16(t3) {
    var i3 = _toPrimitive16(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive16(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyContacts = class {
    constructor() {
      _defineProperty16(this, "id", generateID());
    }
    getAllContacts(contacts_all_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, contacts_all_callback, "contacts_all", {});
    }
    createContact(firstName, lastName, email, phone, contacts_save_callback) {
      var params = {
        firstName,
        lastName: lastName !== null && lastName !== void 0 ? lastName : "",
        email: email !== null && email !== void 0 ? email : "",
        phone: phone !== null && phone !== void 0 ? phone : ""
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 3, contacts_save_callback, "contacts_save", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyMediaPicker.js
  function _defineProperty17(e3, r3, t3) {
    return (r3 = _toPropertyKey17(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey17(t3) {
    var i3 = _toPrimitive17(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive17(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyMediaPicker = class {
    constructor() {
      _defineProperty17(this, "id", generateID());
    }
    showMediaPicker(mediapicker_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 8, mediapicker_callback, "mediapicker", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyAudioRecorder.js
  function _defineProperty18(e3, r3, t3) {
    return (r3 = _toPropertyKey18(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey18(t3) {
    var i3 = _toPrimitive18(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive18(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyAudioRecorder = class {
    constructor() {
      _defineProperty18(this, "id", generateID());
    }
    showRecorder(max_duration, record_callback) {
      var params = {
        max_duration: max_duration !== null && max_duration !== void 0 ? max_duration : 0
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 13, record_callback, "record_start", params);
    }
  };

  // node_modules/natively/dist/classes/NativelyAudioPlayer.js
  function ownKeys2(e3, r3) {
    var t3 = Object.keys(e3);
    if (Object.getOwnPropertySymbols) {
      var o3 = Object.getOwnPropertySymbols(e3);
      r3 && (o3 = o3.filter(function(r4) {
        return Object.getOwnPropertyDescriptor(e3, r4).enumerable;
      })), t3.push.apply(t3, o3);
    }
    return t3;
  }
  function _objectSpread2(e3) {
    for (var r3 = 1; r3 < arguments.length; r3++) {
      var t3 = null != arguments[r3] ? arguments[r3] : {};
      r3 % 2 ? ownKeys2(Object(t3), true).forEach(function(r4) {
        _defineProperty19(e3, r4, t3[r4]);
      }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e3, Object.getOwnPropertyDescriptors(t3)) : ownKeys2(Object(t3)).forEach(function(r4) {
        Object.defineProperty(e3, r4, Object.getOwnPropertyDescriptor(t3, r4));
      });
    }
    return e3;
  }
  function _defineProperty19(e3, r3, t3) {
    return (r3 = _toPropertyKey19(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey19(t3) {
    var i3 = _toPrimitive19(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive19(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyAudioPlayer = class {
    constructor() {
      _defineProperty19(this, "id", generateID());
    }
    buildMetadata(metadata) {
      var params = {};
      if (!metadata) {
        return params;
      }
      if (metadata.id !== void 0) params.id = metadata.id;
      if (metadata.title !== void 0) params.title = metadata.title;
      if (metadata.artist !== void 0) params.artist = metadata.artist;
      if (metadata.album !== void 0) params.album = metadata.album;
      if (metadata.genre !== void 0) params.genre = metadata.genre;
      if (metadata.artwork !== void 0) params.artwork = metadata.artwork;
      if (metadata.duration != null) params.duration = metadata.duration;
      if (metadata.extras !== void 0) params.extras = metadata.extras;
      return params;
    }
    play(source, options, play_callback) {
      var _options$is_stream, _options$autoplay;
      var params = _objectSpread2({
        source,
        is_stream: (_options$is_stream = options === null || options === void 0 ? void 0 : options.is_stream) !== null && _options$is_stream !== void 0 ? _options$is_stream : false,
        autoplay: (_options$autoplay = options === null || options === void 0 ? void 0 : options.autoplay) !== null && _options$autoplay !== void 0 ? _options$autoplay : true
      }, this.buildMetadata(options));
      if ((options === null || options === void 0 ? void 0 : options.headers) !== void 0) params.headers = options.headers;
      if ((options === null || options === void 0 ? void 0 : options.start_position) != null) params.start_position = options.start_position;
      if ((options === null || options === void 0 ? void 0 : options.volume) != null) params.volume = options.volume;
      if ((options === null || options === void 0 ? void 0 : options.speed) != null) params.speed = options.speed;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, play_callback, "audio_play", params);
    }
    pause(pause_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, pause_callback, "audio_pause", {});
    }
    stop(stop_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, stop_callback, "audio_stop", {});
    }
    seek(position, seek_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, seek_callback, "audio_seek", {
        position
      });
    }
    queueAdd(source, options, queue_add_callback) {
      var _options$play_now, _options$is_stream2;
      var params = _objectSpread2({
        source,
        play_now: (_options$play_now = options === null || options === void 0 ? void 0 : options.play_now) !== null && _options$play_now !== void 0 ? _options$play_now : false,
        is_stream: (_options$is_stream2 = options === null || options === void 0 ? void 0 : options.is_stream) !== null && _options$is_stream2 !== void 0 ? _options$is_stream2 : false
      }, this.buildMetadata(options));
      if ((options === null || options === void 0 ? void 0 : options.headers) !== void 0) params.headers = options.headers;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, queue_add_callback, "audio_queue_add", params);
    }
    queueGet(queue_get_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, queue_get_callback, "audio_queue_get", {});
    }
    queueRemove(index2, queue_remove_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, queue_remove_callback, "audio_queue_remove", {
        index: index2
      });
    }
    setMetadata(metadata, set_metadata_callback) {
      var params = this.buildMetadata(metadata);
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, set_metadata_callback, "audio_set_metadata", params);
    }
    setVolume(volume, set_volume_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, set_volume_callback, "audio_set_volume", {
        volume
      });
    }
    setSpeed(speed, set_speed_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 39, set_speed_callback, "audio_set_speed", {
        speed
      });
    }
  };

  // node_modules/natively/dist/classes/NativelyAdmobBanner.js
  function _defineProperty20(e3, r3, t3) {
    return (r3 = _toPropertyKey20(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey20(t3) {
    var i3 = _toPrimitive20(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive20(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyAdmobBanner = class {
    constructor(config, setup_callback) {
      var _config$position, _config$sizeType, _config$custom_width, _config$custom_height;
      var preload_ad = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
      var preload_callback = arguments.length > 3 ? arguments[3] : void 0;
      var show_ad = arguments.length > 4 && arguments[4] !== void 0 ? arguments[4] : false;
      var show_callback = arguments.length > 5 ? arguments[5] : void 0;
      _defineProperty20(this, "id", generateID());
      var params = {};
      if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isAndroidApp) {
        var _config$androidUnitId;
        params.unitId = (_config$androidUnitId = config.androidUnitId) !== null && _config$androidUnitId !== void 0 ? _config$androidUnitId : "ca-app-pub-3940256099942544/6300978111";
      } else if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isIOSApp) {
        var _config$iOSUnitId;
        params.unitId = (_config$iOSUnitId = config.iOSUnitId) !== null && _config$iOSUnitId !== void 0 ? _config$iOSUnitId : "ca-app-pub-3940256099942544/2934735716";
      }
      params.position = (_config$position = config.position) !== null && _config$position !== void 0 ? _config$position : "BOTTOM";
      params.sizeType = (_config$sizeType = config.sizeType) !== null && _config$sizeType !== void 0 ? _config$sizeType : "AUTO";
      params.width = (_config$custom_width = config.custom_width) !== null && _config$custom_width !== void 0 ? _config$custom_width : 320;
      params.height = (_config$custom_height = config.custom_height) !== null && _config$custom_height !== void 0 ? _config$custom_height : 50;
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, (resp) => {
        setup_callback === null || setup_callback === void 0 || setup_callback(resp);
        if (preload_ad) {
          globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, (resp2) => {
            preload_callback === null || preload_callback === void 0 || preload_callback(resp2);
            if (show_ad) {
              globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, show_callback, "bannerad_show", {});
            }
          }, "bannerad_load", {});
        }
      }, "bannerad_setup", params);
    }
    loadAd(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "bannerad_load", {});
    }
    showBanner(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "bannerad_show", {});
    }
    hideBanner(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "bannerad_hide", {});
    }
    bannerIsReady(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "bannerad_ready", {});
    }
    bannerIsVisible(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "bannerad_visible", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyAdmobInterstitial.js
  function _defineProperty21(e3, r3, t3) {
    return (r3 = _toPropertyKey21(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey21(t3) {
    var i3 = _toPrimitive21(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive21(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyAdmobInterstitial = class {
    constructor() {
      var iOSUnitId = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : "ca-app-pub-3940256099942544/4411468910";
      var androidUnitId = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "ca-app-pub-3940256099942544/1033173712";
      var setup_callback = arguments.length > 2 ? arguments[2] : void 0;
      var auto_ad_reload = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : false;
      var auto_ad_reload_callback = arguments.length > 4 ? arguments[4] : void 0;
      _defineProperty21(this, "id", generateID());
      _defineProperty21(this, "auto_ad_reload", void 0);
      _defineProperty21(this, "auto_ad_reload_callback", void 0);
      _defineProperty21(this, "unitId", void 0);
      if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isAndroidApp) {
        this.unitId = androidUnitId;
      } else if (globalThis_default !== null && globalThis_default !== void 0 && globalThis_default.natively.isIOSApp) {
        this.unitId = iOSUnitId;
      }
      this.auto_ad_reload = auto_ad_reload;
      this.auto_ad_reload_callback = auto_ad_reload_callback;
      this.loadAd(setup_callback);
    }
    loadAd(callback) {
      var _this$unitId;
      var params = {
        unitId: (_this$unitId = this.unitId) !== null && _this$unitId !== void 0 ? _this$unitId : "ca-app-pub-3940256099942544/4411468910"
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "interstitialad_setup", params);
    }
    showInterstitialAd(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, (resp) => {
        callback(resp);
        if (resp.event === "DID_DISMISS_AD" && this.auto_ad_reload) {
          setTimeout(() => {
            this.loadAd(this.auto_ad_reload_callback);
          }, 500);
        }
      }, "interstitialad_show", {});
    }
    interstitialIsReady(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 14, callback, "interstitialad_ready", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyNFCService.js
  function _defineProperty22(e3, r3, t3) {
    return (r3 = _toPropertyKey22(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey22(t3) {
    var i3 = _toPrimitive22(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive22(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyNFCService = class {
    constructor(readAlertMessage, writeAlertMessage, readDetectedMessage, writeDetectedMessage) {
      _defineProperty22(this, "id", generateID());
      _defineProperty22(this, "readAlertMessage", void 0);
      _defineProperty22(this, "writeAlertMessage", void 0);
      _defineProperty22(this, "readDetectedMessage", void 0);
      _defineProperty22(this, "writeDetectedMessage", void 0);
      this.readAlertMessage = readAlertMessage;
      this.writeAlertMessage = writeAlertMessage;
      this.readDetectedMessage = readDetectedMessage;
      this.writeDetectedMessage = writeDetectedMessage;
    }
    read(callback) {
      var _this$readAlertMessag, _this$readDetectedMes;
      var params = {
        alertMessage: (_this$readAlertMessag = this.readAlertMessage) !== null && _this$readAlertMessag !== void 0 ? _this$readAlertMessag : "please set readAlertMessage",
        detectedMessage: (_this$readDetectedMes = this.readDetectedMessage) !== null && _this$readDetectedMes !== void 0 ? _this$readDetectedMes : "readDetectedMessage"
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 15, callback, "nfc_read", params);
    }
    write(recordId, recordData, recordDataType, callback) {
      var _this$writeAlertMessa, _this$writeDetectedMe;
      var params = {
        alertMessage: (_this$writeAlertMessa = this.writeAlertMessage) !== null && _this$writeAlertMessa !== void 0 ? _this$writeAlertMessa : "please set writeAlertMessage",
        detectedMessage: (_this$writeDetectedMe = this.writeDetectedMessage) !== null && _this$writeDetectedMe !== void 0 ? _this$writeDetectedMe : "please set writeDetectedMessage",
        recordData: recordData !== null && recordData !== void 0 ? recordData : "please set recordData",
        recordId: recordId !== null && recordId !== void 0 ? recordId : "please set recordId",
        recordDataType: recordDataType !== null && recordDataType !== void 0 ? recordDataType : "please set recordDataType"
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 15, callback, "nfc_write", params);
    }
    available(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 15, callback, "nfc_available", {});
    }
  };

  // node_modules/natively/dist/classes/NativelyFirebaseNotifications.js
  function _defineProperty23(e3, r3, t3) {
    return (r3 = _toPropertyKey23(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey23(t3) {
    var i3 = _toPrimitive23(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive23(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyFirebaseNotifications = class {
    constructor() {
      _defineProperty23(this, "id", generateID());
    }
    firebase_get_token(token_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, token_callback, "firebase_get_token");
    }
    firebase_get_apns_token(apns_token_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, apns_token_callback, "firebase_get_apns_token");
    }
    firebase_request_permission(permission_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, permission_callback, "firebase_request_permission");
    }
    firebase_has_permission(permission_status_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, permission_status_callback, "firebase_has_permission");
    }
    firebase_subscribe_to_topic(topic, subscribe_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, subscribe_callback, "firebase_subscribe_to_topic", {
        topic
      });
    }
    firebase_unsubscribe_from_topic(topic, unsubscribe_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 31, unsubscribe_callback, "firebase_unsubscribe_from_topic", {
        topic
      });
    }
  };

  // node_modules/natively/dist/classes/NativelyKlaviyoNotifications.js
  function _defineProperty24(e3, r3, t3) {
    return (r3 = _toPropertyKey24(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey24(t3) {
    var i3 = _toPrimitive24(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive24(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyKlaviyoNotifications = class {
    constructor() {
      _defineProperty24(this, "id", generateID());
    }
    setProfile(data, callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_set_profile", data);
    }
    getExternalId(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_external_id");
    }
    resetProfile(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_reset_profile");
    }
    registerToken(callback, token) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_register_token", {
        token
      });
    }
    pushPermission(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_push_permission");
    }
    pushRegister(callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(this.id, 32, callback, "klaviyo_push_register");
    }
  };

  // node_modules/natively/dist/classes/NativelyCalendar.js
  function _defineProperty25(e3, r3, t3) {
    return (r3 = _toPropertyKey25(r3)) in e3 ? Object.defineProperty(e3, r3, { value: t3, enumerable: true, configurable: true, writable: true }) : e3[r3] = t3, e3;
  }
  function _toPropertyKey25(t3) {
    var i3 = _toPrimitive25(t3, "string");
    return "symbol" == typeof i3 ? i3 : i3 + "";
  }
  function _toPrimitive25(t3, r3) {
    if ("object" != typeof t3 || !t3) return t3;
    var e3 = t3[Symbol.toPrimitive];
    if (void 0 !== e3) {
      var i3 = e3.call(t3, r3 || "default");
      if ("object" != typeof i3) return i3;
      throw new TypeError("@@toPrimitive must return a primitive value.");
    }
    return ("string" === r3 ? String : Number)(t3);
  }
  var NativelyCalendar = class {
    constructor() {
      _defineProperty25(this, "id", generateID());
    }
    createCalendarEvent(title, endDate, startDate, timezone, calendarId, description, create_calendar_event_callback) {
      var params = {
        title,
        timezone,
        calendarId,
        description,
        endDate: endDate === null || endDate === void 0 ? void 0 : endDate.toISOString(),
        startDate: startDate.toISOString()
      };
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 37, create_calendar_event_callback, "calendar_event", params);
    }
    retrieveCalendars(retrieve_calendars_callback) {
      globalThis_default === null || globalThis_default === void 0 || globalThis_default.natively.trigger(void 0, 37, retrieve_calendars_callback, "retrieve_calendars", {});
    }
  };

  // node_modules/natively/dist/inject.js
  if (globalThis_default) {
    globalThis_default.natively = new Natively();
    globalThis_default.NativelyAdmobBanner = NativelyAdmobBanner;
    globalThis_default.NativelyAdmobInterstitial = NativelyAdmobInterstitial;
    globalThis_default.NativelyAppleSignInService = NativelyAppleSignInService2;
    globalThis_default.NativelyAudioRecorder = NativelyAudioRecorder;
    globalThis_default.NativelyAudioPlayer = NativelyAudioPlayer;
    globalThis_default.NativelyBiometrics = NativelyBiometrics;
    globalThis_default.NativelyCamera = NativelyCamera;
    globalThis_default.NativelyClipboard = NativelyClipboard;
    globalThis_default.NativelyContacts = NativelyContacts;
    globalThis_default.NativelyDatePicker = NativelyDatePicker;
    globalThis_default.NativelyGeolocation = NativelyGeolocation;
    globalThis_default.NativelyHealth = NativelyHealth;
    globalThis_default.NativelyInfo = NativelyInfo;
    globalThis_default.NativelyLocation = NativelyLocation;
    globalThis_default.NativelyMediaPicker = NativelyMediaPicker;
    globalThis_default.NativelyMessage = NativelyMessage;
    globalThis_default.NativelyNFCService = NativelyNFCService;
    globalThis_default.NativelyNotifications = NativelyNotifications;
    globalThis_default.NativelyPurchases = NativelyPurchases;
    globalThis_default.NativelyScanner = NativelyScanner;
    globalThis_default.NativelyStorage = NativelyStorage;
    globalThis_default.NativelyCalendar = NativelyCalendar;
    globalThis_default.NativelyFirebaseNotifications = NativelyFirebaseNotifications;
    globalThis_default.NativelyKlaviyoNotifications = NativelyKlaviyoNotifications;
  }

  // node_modules/natively/dist/events.js
  globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.addObserver(() => globalThis_default === null || globalThis_default === void 0 ? void 0 : globalThis_default.natively.trigger(void 0, 0, (resp) => {
    if (globalThis_default) {
      globalThis_default.natively.min_app_version = resp.minSDKVersion;
      globalThis_default.natively.app_version = resp.sdkVersion;
    }
  }, "app_info", {}));

  // node_modules/mixpanel-browser/dist/mixpanel.module.js
  var win;
  if (typeof window === "undefined") {
    loc = {
      hostname: ""
    };
    win = {
      crypto: { randomUUID: function() {
        throw Error("unsupported");
      } },
      navigator: { userAgent: "", onLine: true },
      document: {
        createElement: function() {
          return {};
        },
        location: loc,
        referrer: ""
      },
      screen: { width: 0, height: 0 },
      location: loc,
      addEventListener: function() {
      },
      removeEventListener: function() {
      },
      dispatchEvent: function() {
      },
      CustomEvent: function() {
      }
    };
  } else {
    win = window;
  }
  var loc;
  function _array_like_to_array(arr, len) {
    if (len == null || len > arr.length) len = arr.length;
    for (var i3 = 0, arr2 = new Array(len); i3 < len; i3++) arr2[i3] = arr[i3];
    return arr2;
  }
  function asyncGeneratorStep2(gen, resolve2, reject2, _next, _throw, key, arg) {
    try {
      var info = gen[key](arg);
      var value = info.value;
    } catch (error) {
      reject2(error);
      return;
    }
    if (info.done) {
      resolve2(value);
    } else {
      Promise.resolve(value).then(_next, _throw);
    }
  }
  function _async_to_generator(fn2) {
    return function() {
      var self2 = this, args = arguments;
      return new Promise(function(resolve2, reject2) {
        var gen = fn2.apply(self2, args);
        function _next(value) {
          asyncGeneratorStep2(gen, resolve2, reject2, _next, _throw, "next", value);
        }
        function _throw(err) {
          asyncGeneratorStep2(gen, resolve2, reject2, _next, _throw, "throw", err);
        }
        _next(void 0);
      });
    };
  }
  function _construct(Parent, args, Class) {
    if (_is_native_reflect_construct()) {
      _construct = Reflect.construct;
    } else {
      _construct = function construct(Parent2, args2, Class2) {
        var a3 = [
          null
        ];
        a3.push.apply(a3, args2);
        var Constructor = Function.bind.apply(Parent2, a3);
        var instance = new Constructor();
        if (Class2) _set_prototype_of(instance, Class2.prototype);
        return instance;
      };
    }
    return _construct.apply(null, arguments);
  }
  function _defineProperties(target, props) {
    for (var i3 = 0; i3 < props.length; i3++) {
      var descriptor = props[i3];
      descriptor.enumerable = descriptor.enumerable || false;
      descriptor.configurable = true;
      if ("value" in descriptor) descriptor.writable = true;
      Object.defineProperty(target, descriptor.key, descriptor);
    }
  }
  function _create_class(Constructor, protoProps, staticProps) {
    if (protoProps) _defineProperties(Constructor.prototype, protoProps);
    return Constructor;
  }
  function _extends() {
    _extends = Object.assign || function(target) {
      for (var i3 = 1; i3 < arguments.length; i3++) {
        var source = arguments[i3];
        for (var key in source) {
          if (Object.prototype.hasOwnProperty.call(source, key)) {
            target[key] = source[key];
          }
        }
      }
      return target;
    };
    return _extends.apply(this, arguments);
  }
  function _get_prototype_of(o3) {
    _get_prototype_of = Object.setPrototypeOf ? Object.getPrototypeOf : function getPrototypeOf(o4) {
      return o4.__proto__ || Object.getPrototypeOf(o4);
    };
    return _get_prototype_of(o3);
  }
  function _inherits(subClass, superClass) {
    if (typeof superClass !== "function" && superClass !== null) {
      throw new TypeError("Super expression must either be null or a function");
    }
    subClass.prototype = Object.create(superClass && superClass.prototype, {
      constructor: {
        value: subClass,
        writable: true,
        configurable: true
      }
    });
    if (superClass) _set_prototype_of(subClass, superClass);
  }
  function _instanceof(left, right) {
    if (right != null && typeof Symbol !== "undefined" && right[Symbol.hasInstance]) {
      return !!right[Symbol.hasInstance](left);
    } else {
      return left instanceof right;
    }
  }
  function _is_native_function(fn2) {
    return Function.toString.call(fn2).indexOf("[native code]") !== -1;
  }
  function _object_without_properties_loose(source, excluded) {
    if (source == null) return {};
    var target = {};
    var sourceKeys = Object.keys(source);
    var key, i3;
    for (i3 = 0; i3 < sourceKeys.length; i3++) {
      key = sourceKeys[i3];
      if (excluded.indexOf(key) >= 0) continue;
      target[key] = source[key];
    }
    return target;
  }
  function _set_prototype_of(o3, p3) {
    _set_prototype_of = Object.setPrototypeOf || function setPrototypeOf(o4, p4) {
      o4.__proto__ = p4;
      return o4;
    };
    return _set_prototype_of(o3, p3);
  }
  function _type_of(obj) {
    "@swc/helpers - typeof";
    return obj && typeof Symbol !== "undefined" && obj.constructor === Symbol ? "symbol" : typeof obj;
  }
  function _unsupported_iterable_to_array(o3, minLen) {
    if (!o3) return;
    if (typeof o3 === "string") return _array_like_to_array(o3, minLen);
    var n3 = Object.prototype.toString.call(o3).slice(8, -1);
    if (n3 === "Object" && o3.constructor) n3 = o3.constructor.name;
    if (n3 === "Map" || n3 === "Set") return Array.from(n3);
    if (n3 === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n3)) return _array_like_to_array(o3, minLen);
  }
  function _wrap_native_super(Class) {
    var _cache = typeof Map === "function" ? /* @__PURE__ */ new Map() : void 0;
    _wrap_native_super = function wrapNativeSuper(Class2) {
      if (Class2 === null || !_is_native_function(Class2)) return Class2;
      if (typeof Class2 !== "function") {
        throw new TypeError("Super expression must either be null or a function");
      }
      if (typeof _cache !== "undefined") {
        if (_cache.has(Class2)) return _cache.get(Class2);
        _cache.set(Class2, Wrapper);
      }
      function Wrapper() {
        return _construct(Class2, arguments, _get_prototype_of(this).constructor);
      }
      Wrapper.prototype = Object.create(Class2.prototype, {
        constructor: {
          value: Wrapper,
          enumerable: false,
          writable: true,
          configurable: true
        }
      });
      return _set_prototype_of(Wrapper, Class2);
    };
    return _wrap_native_super(Class);
  }
  function _is_native_reflect_construct() {
    try {
      var result2 = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
      }));
    } catch (_4) {
    }
    return (_is_native_reflect_construct = function() {
      return !!result2;
    })();
  }
  function _create_for_of_iterator_helper_loose(o3, allowArrayLike) {
    var it = typeof Symbol !== "undefined" && o3[Symbol.iterator] || o3["@@iterator"];
    if (it) return (it = it.call(o3)).next.bind(it);
    if (Array.isArray(o3) || (it = _unsupported_iterable_to_array(o3)) || allowArrayLike) {
      if (it) o3 = it;
      var i3 = 0;
      return function() {
        if (i3 >= o3.length) {
          return {
            done: true
          };
        }
        return {
          done: false,
          value: o3[i3++]
        };
      };
    }
    throw new TypeError("Invalid attempt to iterate non-iterable instance.\\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
  }
  function _ts_generator(thisArg, body) {
    var f3, y3, t3, g4, _4 = {
      label: 0,
      sent: function() {
        if (t3[0] & 1) throw t3[1];
        return t3[1];
      },
      trys: [],
      ops: []
    };
    return g4 = {
      next: verb(0),
      "throw": verb(1),
      "return": verb(2)
    }, typeof Symbol === "function" && (g4[Symbol.iterator] = function() {
      return this;
    }), g4;
    function verb(n3) {
      return function(v3) {
        return step([
          n3,
          v3
        ]);
      };
    }
    function step(op) {
      if (f3) throw new TypeError("Generator is already executing.");
      while (_4) try {
        if (f3 = 1, y3 && (t3 = op[0] & 2 ? y3["return"] : op[0] ? y3["throw"] || ((t3 = y3["return"]) && t3.call(y3), 0) : y3.next) && !(t3 = t3.call(y3, op[1])).done) return t3;
        if (y3 = 0, t3) op = [
          op[0] & 2,
          t3.value
        ];
        switch (op[0]) {
          case 0:
          case 1:
            t3 = op;
            break;
          case 4:
            _4.label++;
            return {
              value: op[1],
              done: false
            };
          case 5:
            _4.label++;
            y3 = op[1];
            op = [
              0
            ];
            continue;
          case 7:
            op = _4.ops.pop();
            _4.trys.pop();
            continue;
          default:
            if (!(t3 = _4.trys, t3 = t3.length > 0 && t3[t3.length - 1]) && (op[0] === 6 || op[0] === 2)) {
              _4 = 0;
              continue;
            }
            if (op[0] === 3 && (!t3 || op[1] > t3[0] && op[1] < t3[3])) {
              _4.label = op[1];
              break;
            }
            if (op[0] === 6 && _4.label < t3[1]) {
              _4.label = t3[1];
              t3 = op;
              break;
            }
            if (t3 && _4.label < t3[2]) {
              _4.label = t3[2];
              _4.ops.push(op);
              break;
            }
            if (t3[2]) _4.ops.pop();
            _4.trys.pop();
            continue;
        }
        op = body.call(thisArg, _4);
      } catch (e3) {
        op = [
          6,
          e3
        ];
        y3 = 0;
      } finally {
        f3 = t3 = 0;
      }
      if (op[0] & 5) throw op[1];
      return {
        value: op[0] ? op[1] : void 0,
        done: true
      };
    }
  }
  function _ts_values(o3) {
    var s3 = typeof Symbol === "function" && Symbol.iterator, m3 = s3 && o3[s3], i3 = 0;
    if (m3) return m3.call(o3);
    if (o3 && typeof o3.length === "number") return {
      next: function() {
        if (o3 && i3 >= o3.length) o3 = void 0;
        return {
          value: o3 && o3[i3++],
          done: !o3
        };
      }
    };
    throw new TypeError(s3 ? "Object is not iterable." : "Symbol.iterator is not defined.");
  }
  var __defProp$1 = Object.defineProperty;
  var __defNormalProp$1 = function(obj, key, value) {
    return key in obj ? __defProp$1(obj, key, {
      enumerable: true,
      configurable: true,
      writable: true,
      value
    }) : obj[key] = value;
  };
  var __publicField$1 = function(obj, key, value) {
    return __defNormalProp$1(obj, (typeof key === "undefined" ? "undefined" : _type_of(key)) !== "symbol" ? key + "" : key, value);
  };
  var _a;
  var __defProp$1$1 = Object.defineProperty;
  var __defNormalProp$1$1 = function(obj, key, value) {
    return key in obj ? __defProp$1$1(obj, key, {
      enumerable: true,
      configurable: true,
      writable: true,
      value
    }) : obj[key] = value;
  };
  var __publicField$1$1 = function(obj, key, value) {
    return __defNormalProp$1$1(obj, (typeof key === "undefined" ? "undefined" : _type_of(key)) !== "symbol" ? key + "" : key, value);
  };
  var NodeType$3 = /* @__PURE__ */ (function(NodeType2) {
    NodeType2[NodeType2["Document"] = 0] = "Document";
    NodeType2[NodeType2["DocumentType"] = 1] = "DocumentType";
    NodeType2[NodeType2["Element"] = 2] = "Element";
    NodeType2[NodeType2["Text"] = 3] = "Text";
    NodeType2[NodeType2["CDATA"] = 4] = "CDATA";
    NodeType2[NodeType2["Comment"] = 5] = "Comment";
    return NodeType2;
  })(NodeType$3 || {});
  var testableAccessors$1 = {
    Node: [
      "childNodes",
      "parentNode",
      "parentElement",
      "textContent"
    ],
    ShadowRoot: [
      "host",
      "styleSheets"
    ],
    Element: [
      "shadowRoot",
      "querySelector",
      "querySelectorAll"
    ],
    MutationObserver: []
  };
  var testableMethods$1 = {
    Node: [
      "contains",
      "getRootNode"
    ],
    ShadowRoot: [
      "getSelection"
    ],
    Element: [],
    MutationObserver: [
      "constructor"
    ]
  };
  var untaintedBasePrototype$1 = {};
  var isAngularZonePresent$1 = function() {
    return !!globalThis.Zone;
  };
  function getUntaintedPrototype$1(key) {
    if (untaintedBasePrototype$1[key]) return untaintedBasePrototype$1[key];
    var defaultObj = globalThis[key];
    var defaultPrototype = defaultObj.prototype;
    var accessorNames = key in testableAccessors$1 ? testableAccessors$1[key] : void 0;
    var isUntaintedAccessors = Boolean(accessorNames && // @ts-expect-error 2345
    accessorNames.every(function(accessor) {
      var _a2, _b;
      return Boolean((_b = (_a2 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a2.get) == null ? void 0 : _b.toString().includes("[native code]"));
    }));
    var methodNames = key in testableMethods$1 ? testableMethods$1[key] : void 0;
    var isUntaintedMethods = Boolean(methodNames && methodNames.every(
      // @ts-expect-error 2345
      function(method) {
        var _a2;
        return typeof defaultPrototype[method] === "function" && ((_a2 = defaultPrototype[method]) == null ? void 0 : _a2.toString().includes("[native code]"));
      }
    ));
    if (isUntaintedAccessors && isUntaintedMethods && !isAngularZonePresent$1()) {
      untaintedBasePrototype$1[key] = defaultObj.prototype;
      return defaultObj.prototype;
    }
    try {
      var iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      var win2 = iframeEl.contentWindow;
      if (!win2) return defaultObj.prototype;
      var untaintedObject = win2[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype$1[key] = untaintedObject;
    } catch (e3) {
      return defaultPrototype;
    }
  }
  var untaintedAccessorCache$1 = {};
  function getUntaintedAccessor$1(key, instance, accessor) {
    var _a2;
    var cacheKey = key + "." + String(accessor);
    if (untaintedAccessorCache$1[cacheKey]) return untaintedAccessorCache$1[cacheKey].call(instance);
    var untaintedPrototype = getUntaintedPrototype$1(key);
    var untaintedAccessor = (_a2 = Object.getOwnPropertyDescriptor(untaintedPrototype, accessor)) == null ? void 0 : _a2.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache$1[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  var untaintedMethodCache$1 = {};
  function getUntaintedMethod$1(key, instance, method) {
    var cacheKey = key + "." + String(method);
    if (untaintedMethodCache$1[cacheKey]) return untaintedMethodCache$1[cacheKey].bind(instance);
    var untaintedPrototype = getUntaintedPrototype$1(key);
    var untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache$1[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "childNodes");
  }
  function parentNode$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "parentNode");
  }
  function parentElement$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "parentElement");
  }
  function textContent$1(n22) {
    return getUntaintedAccessor$1("Node", n22, "textContent");
  }
  function contains$1(n22, other) {
    return getUntaintedMethod$1("Node", n22, "contains")(other);
  }
  function getRootNode$1(n22) {
    return getUntaintedMethod$1("Node", n22, "getRootNode")();
  }
  function host$1(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor$1("ShadowRoot", n22, "host");
  }
  function styleSheets$1(n22) {
    return n22.styleSheets;
  }
  function shadowRoot$1(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor$1("Element", n22, "shadowRoot");
  }
  function querySelector$1(n22, selectors) {
    return getUntaintedAccessor$1("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll$1(n22, selectors) {
    return getUntaintedAccessor$1("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor$1() {
    return getUntaintedPrototype$1("MutationObserver").constructor;
  }
  function patch$1$1(source, name5, replacement) {
    try {
      if (!(name5 in source)) {
        return function() {
        };
      }
      var original = source[name5];
      var wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name5] = wrapped;
      return function() {
        source[name5] = original;
      };
    } catch (e3) {
      return function() {
      };
    }
  }
  var index$1 = {
    childNodes: childNodes$1,
    parentNode: parentNode$1,
    parentElement: parentElement$1,
    textContent: textContent$1,
    contains: contains$1,
    getRootNode: getRootNode$1,
    host: host$1,
    styleSheets: styleSheets$1,
    shadowRoot: shadowRoot$1,
    querySelector: querySelector$1,
    querySelectorAll: querySelectorAll$1,
    mutationObserver: mutationObserverCtor$1,
    patch: patch$1$1
  };
  function isElement(n22) {
    return n22.nodeType === n22.ELEMENT_NODE;
  }
  function isShadowRoot(n22) {
    var hostEl = (
      // anchor and textarea elements also have a `host` property
      // but only shadow roots have a `mode` property
      n22 && "host" in n22 && "mode" in n22 && index$1.host(n22) || null
    );
    return Boolean(hostEl && "shadowRoot" in hostEl && index$1.shadowRoot(hostEl) === n22);
  }
  function isNativeShadowDom(shadowRoot2) {
    return Object.prototype.toString.call(shadowRoot2) === "[object ShadowRoot]";
  }
  function fixBrowserCompatibilityIssuesInCSS(cssText) {
    if (cssText.includes(" background-clip: text;") && !cssText.includes(" -webkit-background-clip: text;")) {
      cssText = cssText.replace(/\sbackground-clip:\s*text;/g, " -webkit-background-clip: text; background-clip: text;");
    }
    return cssText;
  }
  function escapeImportStatement(rule2) {
    var cssText = rule2.cssText;
    if (cssText.split('"').length < 3) return cssText;
    var statement = [
      "@import",
      "url(" + JSON.stringify(rule2.href) + ")"
    ];
    if (rule2.layerName === "") {
      statement.push("layer");
    } else if (rule2.layerName) {
      statement.push("layer(" + rule2.layerName + ")");
    }
    if (rule2.supportsText) {
      statement.push("supports(" + rule2.supportsText + ")");
    }
    if (rule2.media.length) {
      statement.push(rule2.media.mediaText);
    }
    return statement.join(" ") + ";";
  }
  function stringifyStylesheet(s22) {
    try {
      var rules2 = s22.rules || s22.cssRules;
      if (!rules2) {
        return null;
      }
      var sheetHref = s22.href;
      if (!sheetHref && s22.ownerNode && s22.ownerNode.ownerDocument) {
        sheetHref = s22.ownerNode.ownerDocument.location.href;
      }
      var stringifiedRules = Array.from(rules2, function(rule2) {
        return stringifyRule(rule2, sheetHref);
      }).join("");
      return fixBrowserCompatibilityIssuesInCSS(stringifiedRules);
    } catch (error) {
      return null;
    }
  }
  function stringifyRule(rule2, sheetHref) {
    if (isCSSImportRule(rule2)) {
      var importStringified;
      try {
        importStringified = // we can access the imported stylesheet rules directly
        stringifyStylesheet(rule2.styleSheet) || // work around browser issues with the raw string `@import url(...)` statement
        escapeImportStatement(rule2);
      } catch (error) {
        importStringified = rule2.cssText;
      }
      if (rule2.styleSheet.href) {
        return absolutifyURLs(importStringified, rule2.styleSheet.href);
      }
      return importStringified;
    } else {
      var ruleStringified = rule2.cssText;
      if (isCSSStyleRule(rule2) && rule2.selectorText.includes(":")) {
        ruleStringified = fixSafariColons(ruleStringified);
      }
      if (sheetHref) {
        return absolutifyURLs(ruleStringified, sheetHref);
      }
      return ruleStringified;
    }
  }
  function fixSafariColons(cssStringified) {
    var regex = /(\[(?:[\w-]+)[^\\])(:(?:[\w-]+)\])/gm;
    return cssStringified.replace(regex, "$1\\$2");
  }
  function isCSSImportRule(rule2) {
    return "styleSheet" in rule2;
  }
  function isCSSStyleRule(rule2) {
    return "selectorText" in rule2;
  }
  var Mirror = /* @__PURE__ */ (function() {
    function Mirror2() {
      __publicField$1$1(this, "idNodeMap", /* @__PURE__ */ new Map());
      __publicField$1$1(this, "nodeMetaMap", /* @__PURE__ */ new WeakMap());
    }
    var _proto = Mirror2.prototype;
    _proto.getId = function getId3(n22) {
      var _a2;
      if (!n22) return -1;
      var id = (_a2 = this.getMeta(n22)) == null ? void 0 : _a2.id;
      return id != null ? id : -1;
    };
    _proto.getNode = function getNode2(id) {
      return this.idNodeMap.get(id) || null;
    };
    _proto.getIds = function getIds() {
      return Array.from(this.idNodeMap.keys());
    };
    _proto.getMeta = function getMeta(n22) {
      return this.nodeMetaMap.get(n22) || null;
    };
    _proto.removeNodeFromMap = function removeNodeFromMap2(n22) {
      var _this = this;
      var id = this.getId(n22);
      this.idNodeMap.delete(id);
      if (n22.childNodes) {
        n22.childNodes.forEach(function(childNode) {
          return _this.removeNodeFromMap(childNode);
        });
      }
    };
    _proto.has = function has2(id) {
      return this.idNodeMap.has(id);
    };
    _proto.hasNode = function hasNode(node2) {
      return this.nodeMetaMap.has(node2);
    };
    _proto.add = function add(n22, meta) {
      var id = meta.id;
      this.idNodeMap.set(id, n22);
      this.nodeMetaMap.set(n22, meta);
    };
    _proto.replace = function replace(id, n22) {
      var oldNode = this.getNode(id);
      if (oldNode) {
        var meta = this.nodeMetaMap.get(oldNode);
        if (meta) this.nodeMetaMap.set(n22, meta);
      }
      this.idNodeMap.set(id, n22);
    };
    _proto.reset = function reset2() {
      this.idNodeMap = /* @__PURE__ */ new Map();
      this.nodeMetaMap = /* @__PURE__ */ new WeakMap();
    };
    return Mirror2;
  })();
  function createMirror$2() {
    return new Mirror();
  }
  function maskInputValue(param) {
    var element = param.element, maskInputOptions = param.maskInputOptions, tagName = param.tagName, type = param.type, value = param.value, maskInputFn = param.maskInputFn;
    var text = value || "";
    var actualType = type && toLowerCase(type);
    if (maskInputOptions[tagName.toLowerCase()] || actualType && maskInputOptions[actualType]) {
      if (maskInputFn) {
        text = maskInputFn(text, element);
      } else {
        text = "*".repeat(text.length);
      }
    }
    return text;
  }
  function toLowerCase(str) {
    return str.toLowerCase();
  }
  var ORIGINAL_ATTRIBUTE_NAME = "__rrweb_original__";
  function is2DCanvasBlank(canvas) {
    var ctx = canvas.getContext("2d");
    if (!ctx) return true;
    var chunkSize = 50;
    for (var x22 = 0; x22 < canvas.width; x22 += chunkSize) {
      for (var y3 = 0; y3 < canvas.height; y3 += chunkSize) {
        var getImageData = ctx.getImageData;
        var originalGetImageData = ORIGINAL_ATTRIBUTE_NAME in getImageData ? getImageData[ORIGINAL_ATTRIBUTE_NAME] : getImageData;
        var pixelBuffer = new Uint32Array(
          // eslint-disable-next-line @typescript-eslint/no-unsafe-argument, @typescript-eslint/no-unsafe-member-access
          originalGetImageData.call(ctx, x22, y3, Math.min(chunkSize, canvas.width - x22), Math.min(chunkSize, canvas.height - y3)).data.buffer
        );
        if (pixelBuffer.some(function(pixel) {
          return pixel !== 0;
        })) return false;
      }
    }
    return true;
  }
  function getInputType(element) {
    var type = element.type;
    return element.hasAttribute("data-rr-is-password") ? "password" : type ? (
      // eslint-disable-next-line @typescript-eslint/no-unnecessary-type-assertion
      toLowerCase(type)
    ) : null;
  }
  function extractFileExtension(path, baseURL) {
    var url;
    try {
      url = new URL(path, baseURL != null ? baseURL : window.location.href);
    } catch (err) {
      return null;
    }
    var regex = /\.([0-9a-z]+)(?:$)/i;
    var match = url.pathname.match(regex);
    var _ref;
    return (_ref = match == null ? void 0 : match[1]) != null ? _ref : null;
  }
  function extractOrigin(url) {
    var origin = "";
    if (url.indexOf("//") > -1) {
      origin = url.split("/").slice(0, 3).join("/");
    } else {
      origin = url.split("/")[0];
    }
    origin = origin.split("?")[0];
    return origin;
  }
  var URL_IN_CSS_REF = /url\((?:(')([^']*)'|(")(.*?)"|([^)]*))\)/gm;
  var URL_PROTOCOL_MATCH = /^(?:[a-z+]+:)?\/\//i;
  var URL_WWW_MATCH = /^www\..*/i;
  var DATA_URI = /^(data:)([^,]*),(.*)/i;
  function absolutifyURLs(cssText, href) {
    return (cssText || "").replace(URL_IN_CSS_REF, function(origin, quote1, path1, quote2, path2, path3) {
      var filePath = path1 || path2 || path3;
      var maybeQuote = quote1 || quote2 || "";
      if (!filePath) {
        return origin;
      }
      if (URL_PROTOCOL_MATCH.test(filePath) || URL_WWW_MATCH.test(filePath)) {
        return "url(" + maybeQuote + filePath + maybeQuote + ")";
      }
      if (DATA_URI.test(filePath)) {
        return "url(" + maybeQuote + filePath + maybeQuote + ")";
      }
      if (filePath[0] === "/") {
        return "url(" + maybeQuote + (extractOrigin(href) + filePath) + maybeQuote + ")";
      }
      var stack = href.split("/");
      var parts = filePath.split("/");
      stack.pop();
      for (var _iterator = _create_for_of_iterator_helper_loose(parts), _step; !(_step = _iterator()).done; ) {
        var part = _step.value;
        if (part === ".") {
          continue;
        } else if (part === "..") {
          stack.pop();
        } else {
          stack.push(part);
        }
      }
      return "url(" + maybeQuote + stack.join("/") + maybeQuote + ")";
    });
  }
  function normalizeCssString(cssText, _testNoPxNorm) {
    if (_testNoPxNorm === void 0) _testNoPxNorm = false;
    if (_testNoPxNorm) {
      return cssText.replace(/(\/\*[^*]*\*\/)|[\s;]/g, "");
    } else {
      return cssText.replace(/(\/\*[^*]*\*\/)|[\s;]/g, "").replace(/0px/g, "0");
    }
  }
  function splitCssText(cssText, style, _testNoPxNorm) {
    if (_testNoPxNorm === void 0) _testNoPxNorm = false;
    var childNodes2 = Array.from(style.childNodes);
    var splits = [];
    var iterCount = 0;
    if (childNodes2.length > 1 && cssText && typeof cssText === "string") {
      var cssTextNorm = normalizeCssString(cssText, _testNoPxNorm);
      var normFactor = cssTextNorm.length / cssText.length;
      for (var i22 = 1; i22 < childNodes2.length; i22++) {
        if (childNodes2[i22].textContent && typeof childNodes2[i22].textContent === "string") {
          var textContentNorm = normalizeCssString(childNodes2[i22].textContent, _testNoPxNorm);
          var jLimit = 100;
          var j4 = 3;
          for (; j4 < textContentNorm.length; j4++) {
            if (
              // keep consuming css identifiers (to get a decent chunk more quickly)
              textContentNorm[j4].match(/[a-zA-Z0-9]/) || // substring needs to be unique to this section
              textContentNorm.indexOf(textContentNorm.substring(0, j4), 1) !== -1
            ) {
              continue;
            }
            break;
          }
          for (; j4 < textContentNorm.length; j4++) {
            var startSubstring = textContentNorm.substring(0, j4);
            var cssNormSplits = cssTextNorm.split(startSubstring);
            var splitNorm = -1;
            if (cssNormSplits.length === 2) {
              splitNorm = cssNormSplits[0].length;
            } else if (cssNormSplits.length > 2 && cssNormSplits[0] === "" && childNodes2[i22 - 1].textContent !== "") {
              splitNorm = cssTextNorm.indexOf(startSubstring, 1);
            } else if (cssNormSplits.length === 1) {
              startSubstring = startSubstring.substring(0, startSubstring.length - 1);
              cssNormSplits = cssTextNorm.split(startSubstring);
              if (cssNormSplits.length <= 1) {
                splits.push(cssText);
                return splits;
              }
              j4 = jLimit + 1;
            } else if (j4 === textContentNorm.length - 1) {
              splitNorm = cssTextNorm.indexOf(startSubstring);
            }
            if (cssNormSplits.length >= 2 && j4 > jLimit) {
              var prevTextContent = childNodes2[i22 - 1].textContent;
              if (prevTextContent && typeof prevTextContent === "string") {
                var prevMinLength = normalizeCssString(prevTextContent).length;
                splitNorm = cssTextNorm.indexOf(startSubstring, prevMinLength);
              }
              if (splitNorm === -1) {
                splitNorm = cssNormSplits[0].length;
              }
            }
            if (splitNorm !== -1) {
              var k4 = Math.floor(splitNorm / normFactor);
              for (; k4 > 0 && k4 < cssText.length; ) {
                iterCount += 1;
                if (iterCount > 50 * childNodes2.length) {
                  splits.push(cssText);
                  return splits;
                }
                var normPart = normalizeCssString(cssText.substring(0, k4), _testNoPxNorm);
                if (normPart.length === splitNorm) {
                  splits.push(cssText.substring(0, k4));
                  cssText = cssText.substring(k4);
                  cssTextNorm = cssTextNorm.substring(splitNorm);
                  break;
                } else if (normPart.length < splitNorm) {
                  k4 += Math.max(1, Math.floor((splitNorm - normPart.length) / normFactor));
                } else {
                  k4 -= Math.max(1, Math.floor((normPart.length - splitNorm) * normFactor));
                }
              }
              break;
            }
          }
        }
      }
    }
    splits.push(cssText);
    return splits;
  }
  function markCssSplits(cssText, style) {
    return splitCssText(cssText, style).join("/* rr_split */");
  }
  var _id = 1;
  var tagNameRegex = new RegExp("[^a-z0-9-_:]");
  var IGNORED_NODE = -2;
  function genId() {
    return _id++;
  }
  function getValidTagName$1(element) {
    if (_instanceof(element, HTMLFormElement)) {
      return "form";
    }
    var processedTagName = toLowerCase(element.tagName);
    if (tagNameRegex.test(processedTagName)) {
      return "div";
    }
    return processedTagName;
  }
  var canvasService;
  var canvasCtx;
  var SRCSET_NOT_SPACES = /^[^ \t\n\r\u000c]+/;
  var SRCSET_COMMAS_OR_SPACES = /^[, \t\n\r\u000c]+/;
  function getAbsoluteSrcsetString(doc, attributeValue) {
    if (attributeValue.trim() === "") {
      return attributeValue;
    }
    var pos = 0;
    function collectCharacters(regEx) {
      var chars2;
      var match = regEx.exec(attributeValue.substring(pos));
      if (match) {
        chars2 = match[0];
        pos += chars2.length;
        return chars2;
      }
      return "";
    }
    var output = [];
    while (true) {
      collectCharacters(SRCSET_COMMAS_OR_SPACES);
      if (pos >= attributeValue.length) {
        break;
      }
      var url = collectCharacters(SRCSET_NOT_SPACES);
      if (url.slice(-1) === ",") {
        url = absoluteToDoc(doc, url.substring(0, url.length - 1));
        output.push(url);
      } else {
        var descriptorsStr = "";
        url = absoluteToDoc(doc, url);
        var inParens = false;
        while (true) {
          var c22 = attributeValue.charAt(pos);
          if (c22 === "") {
            output.push((url + descriptorsStr).trim());
            break;
          } else if (!inParens) {
            if (c22 === ",") {
              pos += 1;
              output.push((url + descriptorsStr).trim());
              break;
            } else if (c22 === "(") {
              inParens = true;
            }
          } else {
            if (c22 === ")") {
              inParens = false;
            }
          }
          descriptorsStr += c22;
          pos += 1;
        }
      }
    }
    return output.join(", ");
  }
  var cachedDocument = /* @__PURE__ */ new WeakMap();
  function absoluteToDoc(doc, attributeValue) {
    if (!attributeValue || attributeValue.trim() === "") {
      return attributeValue;
    }
    return getHref(doc, attributeValue);
  }
  function isSVGElement(el) {
    return Boolean(el.tagName === "svg" || el.ownerSVGElement);
  }
  function getHref(doc, customHref) {
    var a22 = cachedDocument.get(doc);
    if (!a22) {
      a22 = doc.createElement("a");
      cachedDocument.set(doc, a22);
    }
    if (!customHref) {
      customHref = "";
    } else if (customHref.startsWith("blob:") || customHref.startsWith("data:")) {
      return customHref;
    }
    a22.setAttribute("href", customHref);
    return a22.href;
  }
  function transformAttribute(doc, tagName, name5, value) {
    if (!value) {
      return value;
    }
    if (name5 === "src" || name5 === "href" && !(tagName === "use" && value[0] === "#")) {
      return absoluteToDoc(doc, value);
    } else if (name5 === "xlink:href" && value[0] !== "#") {
      return absoluteToDoc(doc, value);
    } else if (name5 === "background" && (tagName === "table" || tagName === "td" || tagName === "th")) {
      return absoluteToDoc(doc, value);
    } else if (name5 === "srcset") {
      return getAbsoluteSrcsetString(doc, value);
    } else if (name5 === "style") {
      return absolutifyURLs(value, getHref(doc));
    } else if (tagName === "object" && name5 === "data") {
      return absoluteToDoc(doc, value);
    }
    return value;
  }
  function ignoreAttribute(tagName, name5, _value) {
    return (tagName === "video" || tagName === "audio") && name5 === "autoplay";
  }
  function _isBlockedElement(element, blockClass, blockSelector) {
    try {
      if (typeof blockClass === "string") {
        if (element.classList.contains(blockClass)) {
          return true;
        }
      } else {
        for (var eIndex = element.classList.length; eIndex--; ) {
          var className = element.classList[eIndex];
          if (blockClass.test(className)) {
            return true;
          }
        }
      }
      if (blockSelector) {
        return element.matches(blockSelector);
      }
    } catch (e22) {
    }
    return false;
  }
  function classMatchesRegex$1(node2, regex, checkAncestors) {
    if (!node2) return false;
    if (node2.nodeType !== node2.ELEMENT_NODE) {
      if (!checkAncestors) return false;
      return classMatchesRegex$1(index$1.parentNode(node2), regex, checkAncestors);
    }
    for (var eIndex = node2.classList.length; eIndex--; ) {
      var className = node2.classList[eIndex];
      if (regex.test(className)) {
        return true;
      }
    }
    if (!checkAncestors) return false;
    return classMatchesRegex$1(index$1.parentNode(node2), regex, checkAncestors);
  }
  function needMaskingText(node2, maskTextClass, maskTextSelector, checkAncestors) {
    var el;
    if (isElement(node2)) {
      el = node2;
      if (!index$1.childNodes(el).length) {
        return false;
      }
    } else if (index$1.parentElement(node2) === null) {
      return false;
    } else {
      el = index$1.parentElement(node2);
    }
    try {
      if (typeof maskTextClass === "string") {
        if (checkAncestors) {
          if (el.closest("." + maskTextClass)) return true;
        } else {
          if (el.classList.contains(maskTextClass)) return true;
        }
      } else {
        if (classMatchesRegex$1(el, maskTextClass, checkAncestors)) return true;
      }
      if (maskTextSelector) {
        if (checkAncestors) {
          if (el.closest(maskTextSelector)) return true;
        } else {
          if (el.matches(maskTextSelector)) return true;
        }
      }
    } catch (e22) {
    }
    return false;
  }
  function onceIframeLoaded(iframeEl, listener, iframeLoadTimeout) {
    var win2 = iframeEl.contentWindow;
    if (!win2) {
      return;
    }
    var fired = false;
    var readyState;
    try {
      readyState = win2.document.readyState;
    } catch (error) {
      return;
    }
    if (readyState !== "complete") {
      var timer3 = setTimeout(function() {
        if (!fired) {
          listener();
          fired = true;
        }
      }, iframeLoadTimeout);
      iframeEl.addEventListener("load", function() {
        clearTimeout(timer3);
        fired = true;
        listener();
      });
      return;
    }
    var blankUrl = "about:blank";
    if (win2.location.href !== blankUrl || iframeEl.src === blankUrl || iframeEl.src === "") {
      setTimeout(listener, 0);
      return iframeEl.addEventListener("load", listener);
    }
    iframeEl.addEventListener("load", listener);
  }
  function onceStylesheetLoaded(link, listener, styleSheetLoadTimeout) {
    var fired = false;
    var styleSheetLoaded;
    try {
      styleSheetLoaded = link.sheet;
    } catch (error) {
      return;
    }
    if (styleSheetLoaded) return;
    var timer3 = setTimeout(function() {
      if (!fired) {
        listener();
        fired = true;
      }
    }, styleSheetLoadTimeout);
    link.addEventListener("load", function() {
      clearTimeout(timer3);
      fired = true;
      listener();
    });
  }
  function serializeNode(n22, options) {
    var doc = options.doc, mirror2 = options.mirror, blockClass = options.blockClass, blockSelector = options.blockSelector, needsMask = options.needsMask, inlineStylesheet = options.inlineStylesheet, _options_maskInputOptions = options.maskInputOptions, maskInputOptions = _options_maskInputOptions === void 0 ? {} : _options_maskInputOptions, maskTextFn = options.maskTextFn, maskInputFn = options.maskInputFn, _options_dataURLOptions = options.dataURLOptions, dataURLOptions = _options_dataURLOptions === void 0 ? {} : _options_dataURLOptions, inlineImages = options.inlineImages, recordCanvas = options.recordCanvas, keepIframeSrcFn = options.keepIframeSrcFn, _options_newlyAddedElement = options.newlyAddedElement, newlyAddedElement = _options_newlyAddedElement === void 0 ? false : _options_newlyAddedElement, _options_cssCaptured = options.cssCaptured, cssCaptured = _options_cssCaptured === void 0 ? false : _options_cssCaptured;
    var rootId = getRootId(doc, mirror2);
    switch (n22.nodeType) {
      case n22.DOCUMENT_NODE:
        if (n22.compatMode !== "CSS1Compat") {
          return {
            type: NodeType$3.Document,
            childNodes: [],
            compatMode: n22.compatMode
          };
        } else {
          return {
            type: NodeType$3.Document,
            childNodes: []
          };
        }
      case n22.DOCUMENT_TYPE_NODE:
        return {
          type: NodeType$3.DocumentType,
          name: n22.name,
          publicId: n22.publicId,
          systemId: n22.systemId,
          rootId
        };
      case n22.ELEMENT_NODE:
        return serializeElementNode(n22, {
          doc,
          blockClass,
          blockSelector,
          inlineStylesheet,
          maskInputOptions,
          maskInputFn,
          dataURLOptions,
          inlineImages,
          recordCanvas,
          keepIframeSrcFn,
          newlyAddedElement,
          rootId
        });
      case n22.TEXT_NODE:
        return serializeTextNode(n22, {
          doc,
          needsMask,
          maskTextFn,
          rootId,
          cssCaptured
        });
      case n22.CDATA_SECTION_NODE:
        return {
          type: NodeType$3.CDATA,
          textContent: "",
          rootId
        };
      case n22.COMMENT_NODE:
        return {
          type: NodeType$3.Comment,
          textContent: index$1.textContent(n22) || "",
          rootId
        };
      default:
        return false;
    }
  }
  function getRootId(doc, mirror2) {
    if (!mirror2.hasNode(doc)) return void 0;
    var docId = mirror2.getId(doc);
    return docId === 1 ? void 0 : docId;
  }
  function serializeTextNode(n22, options) {
    var needsMask = options.needsMask, maskTextFn = options.maskTextFn, rootId = options.rootId, cssCaptured = options.cssCaptured;
    var parent = index$1.parentNode(n22);
    var parentTagName = parent && parent.tagName;
    var textContent2 = "";
    var isStyle = parentTagName === "STYLE" ? true : void 0;
    var isScript = parentTagName === "SCRIPT" ? true : void 0;
    if (isScript) {
      textContent2 = "SCRIPT_PLACEHOLDER";
    } else if (!cssCaptured) {
      textContent2 = index$1.textContent(n22);
      if (isStyle && textContent2) {
        textContent2 = absolutifyURLs(textContent2, getHref(options.doc));
      }
    }
    if (!isStyle && !isScript && textContent2 && needsMask) {
      textContent2 = maskTextFn ? maskTextFn(textContent2, index$1.parentElement(n22)) : textContent2.replace(/[\S]/g, "*");
    }
    return {
      type: NodeType$3.Text,
      textContent: textContent2 || "",
      rootId
    };
  }
  function serializeElementNode(n22, options) {
    var doc = options.doc, blockClass = options.blockClass, blockSelector = options.blockSelector, inlineStylesheet = options.inlineStylesheet, _options_maskInputOptions = options.maskInputOptions, maskInputOptions = _options_maskInputOptions === void 0 ? {} : _options_maskInputOptions, maskInputFn = options.maskInputFn, _options_dataURLOptions = options.dataURLOptions, dataURLOptions = _options_dataURLOptions === void 0 ? {} : _options_dataURLOptions, inlineImages = options.inlineImages, recordCanvas = options.recordCanvas, keepIframeSrcFn = options.keepIframeSrcFn, _options_newlyAddedElement = options.newlyAddedElement, newlyAddedElement = _options_newlyAddedElement === void 0 ? false : _options_newlyAddedElement, rootId = options.rootId;
    var needBlock = _isBlockedElement(n22, blockClass, blockSelector);
    var tagName = getValidTagName$1(n22);
    var attributes = {};
    var len = n22.attributes.length;
    for (var i22 = 0; i22 < len; i22++) {
      var attr = n22.attributes[i22];
      if (!ignoreAttribute(tagName, attr.name, attr.value)) {
        attributes[attr.name] = transformAttribute(doc, tagName, toLowerCase(attr.name), attr.value);
      }
    }
    if (tagName === "link" && inlineStylesheet) {
      var stylesheet = Array.from(doc.styleSheets).find(function(s22) {
        return s22.href === n22.href;
      });
      var cssText = null;
      if (stylesheet) {
        cssText = stringifyStylesheet(stylesheet);
      }
      if (cssText) {
        delete attributes.rel;
        delete attributes.href;
        attributes._cssText = cssText;
      }
    }
    if (tagName === "style" && n22.sheet) {
      var cssText1 = stringifyStylesheet(n22.sheet);
      if (cssText1) {
        if (n22.childNodes.length > 1) {
          cssText1 = markCssSplits(cssText1, n22);
        }
        attributes._cssText = cssText1;
      }
    }
    if (tagName === "input" || tagName === "textarea" || tagName === "select") {
      var value = n22.value;
      var checked = n22.checked;
      if (attributes.type !== "radio" && attributes.type !== "checkbox" && attributes.type !== "submit" && attributes.type !== "button" && value) {
        attributes.value = maskInputValue({
          element: n22,
          type: getInputType(n22),
          tagName,
          value,
          maskInputOptions,
          maskInputFn
        });
      } else if (checked) {
        attributes.checked = checked;
      }
    }
    if (tagName === "option") {
      if (n22.selected && !maskInputOptions["select"]) {
        attributes.selected = true;
      } else {
        delete attributes.selected;
      }
    }
    if (tagName === "dialog" && n22.open) {
      attributes.rr_open_mode = n22.matches("dialog:modal") ? "modal" : "non-modal";
    }
    if (tagName === "canvas" && recordCanvas) {
      if (n22.__context === "2d") {
        if (!is2DCanvasBlank(n22)) {
          attributes.rr_dataURL = n22.toDataURL(dataURLOptions.type, dataURLOptions.quality);
        }
      } else if (!("__context" in n22)) {
        var canvasDataURL = n22.toDataURL(dataURLOptions.type, dataURLOptions.quality);
        var blankCanvas = doc.createElement("canvas");
        blankCanvas.width = n22.width;
        blankCanvas.height = n22.height;
        var blankCanvasDataURL = blankCanvas.toDataURL(dataURLOptions.type, dataURLOptions.quality);
        if (canvasDataURL !== blankCanvasDataURL) {
          attributes.rr_dataURL = canvasDataURL;
        }
      }
    }
    if (tagName === "img" && inlineImages) {
      if (!canvasService) {
        canvasService = doc.createElement("canvas");
        canvasCtx = canvasService.getContext("2d");
      }
      var image = n22;
      var imageSrc = image.currentSrc || image.getAttribute("src") || "<unknown-src>";
      var priorCrossOrigin = image.crossOrigin;
      var recordInlineImage = function() {
        image.removeEventListener("load", recordInlineImage);
        try {
          canvasService.width = image.naturalWidth;
          canvasService.height = image.naturalHeight;
          canvasCtx.drawImage(image, 0, 0);
          attributes.rr_dataURL = canvasService.toDataURL(dataURLOptions.type, dataURLOptions.quality);
        } catch (err) {
          if (image.crossOrigin !== "anonymous") {
            image.crossOrigin = "anonymous";
            if (image.complete && image.naturalWidth !== 0) recordInlineImage();
            else image.addEventListener("load", recordInlineImage);
            return;
          } else {
            console.warn("Cannot inline img src=" + imageSrc + "! Error: " + err);
          }
        }
        if (image.crossOrigin === "anonymous") {
          priorCrossOrigin ? attributes.crossOrigin = priorCrossOrigin : image.removeAttribute("crossorigin");
        }
      };
      if (image.complete && image.naturalWidth !== 0) recordInlineImage();
      else image.addEventListener("load", recordInlineImage);
    }
    if (tagName === "audio" || tagName === "video") {
      var mediaAttributes = attributes;
      mediaAttributes.rr_mediaState = n22.paused ? "paused" : "played";
      mediaAttributes.rr_mediaCurrentTime = n22.currentTime;
      mediaAttributes.rr_mediaPlaybackRate = n22.playbackRate;
      mediaAttributes.rr_mediaMuted = n22.muted;
      mediaAttributes.rr_mediaLoop = n22.loop;
      mediaAttributes.rr_mediaVolume = n22.volume;
    }
    if (!newlyAddedElement) {
      if (n22.scrollLeft) {
        attributes.rr_scrollLeft = n22.scrollLeft;
      }
      if (n22.scrollTop) {
        attributes.rr_scrollTop = n22.scrollTop;
      }
    }
    if (needBlock) {
      var _n2_getBoundingClientRect = n22.getBoundingClientRect(), width = _n2_getBoundingClientRect.width, height = _n2_getBoundingClientRect.height;
      attributes = {
        class: attributes.class,
        rr_width: "" + width + "px",
        rr_height: "" + height + "px"
      };
    }
    if (tagName === "iframe" && !keepIframeSrcFn(attributes.src)) {
      if (!n22.contentDocument) {
        attributes.rr_src = attributes.src;
      }
      delete attributes.src;
    }
    var isCustomElement;
    try {
      if (customElements.get(tagName)) isCustomElement = true;
    } catch (e22) {
    }
    return {
      type: NodeType$3.Element,
      tagName,
      attributes,
      childNodes: [],
      isSVG: isSVGElement(n22) || void 0,
      needBlock,
      rootId,
      isCustom: isCustomElement
    };
  }
  function lowerIfExists(maybeAttr) {
    if (maybeAttr === void 0 || maybeAttr === null) {
      return "";
    } else {
      return maybeAttr.toLowerCase();
    }
  }
  function slimDOMExcluded(sn2, slimDOMOptions) {
    if (slimDOMOptions.comment && sn2.type === NodeType$3.Comment) {
      return true;
    } else if (sn2.type === NodeType$3.Element) {
      if (slimDOMOptions.script && // script tag
      (sn2.tagName === "script" || // (module)preload link
      sn2.tagName === "link" && (sn2.attributes.rel === "preload" && sn2.attributes.as === "script" || sn2.attributes.rel === "modulepreload") || // prefetch link
      sn2.tagName === "link" && sn2.attributes.rel === "prefetch" && typeof sn2.attributes.href === "string" && extractFileExtension(sn2.attributes.href) === "js")) {
        return true;
      } else if (slimDOMOptions.headFavicon && (sn2.tagName === "link" && sn2.attributes.rel === "shortcut icon" || sn2.tagName === "meta" && (lowerIfExists(sn2.attributes.name).match(/^msapplication-tile(image|color)$/) || lowerIfExists(sn2.attributes.name) === "application-name" || lowerIfExists(sn2.attributes.rel) === "icon" || lowerIfExists(sn2.attributes.rel) === "apple-touch-icon" || lowerIfExists(sn2.attributes.rel) === "shortcut icon"))) {
        return true;
      } else if (sn2.tagName === "meta") {
        if (slimDOMOptions.headMetaDescKeywords && lowerIfExists(sn2.attributes.name).match(/^description|keywords$/)) {
          return true;
        } else if (slimDOMOptions.headMetaSocial && (lowerIfExists(sn2.attributes.property).match(/^(og|twitter|fb):/) || // og = opengraph (facebook)
        lowerIfExists(sn2.attributes.name).match(/^(og|twitter):/) || lowerIfExists(sn2.attributes.name) === "pinterest")) {
          return true;
        } else if (slimDOMOptions.headMetaRobots && (lowerIfExists(sn2.attributes.name) === "robots" || lowerIfExists(sn2.attributes.name) === "googlebot" || lowerIfExists(sn2.attributes.name) === "bingbot")) {
          return true;
        } else if (slimDOMOptions.headMetaHttpEquiv && sn2.attributes["http-equiv"] !== void 0) {
          return true;
        } else if (slimDOMOptions.headMetaAuthorship && (lowerIfExists(sn2.attributes.name) === "author" || lowerIfExists(sn2.attributes.name) === "generator" || lowerIfExists(sn2.attributes.name) === "framework" || lowerIfExists(sn2.attributes.name) === "publisher" || lowerIfExists(sn2.attributes.name) === "progid" || lowerIfExists(sn2.attributes.property).match(/^article:/) || lowerIfExists(sn2.attributes.property).match(/^product:/))) {
          return true;
        } else if (slimDOMOptions.headMetaVerification && (lowerIfExists(sn2.attributes.name) === "google-site-verification" || lowerIfExists(sn2.attributes.name) === "yandex-verification" || lowerIfExists(sn2.attributes.name) === "csrf-token" || lowerIfExists(sn2.attributes.name) === "p:domain_verify" || lowerIfExists(sn2.attributes.name) === "verify-v1" || lowerIfExists(sn2.attributes.name) === "verification" || lowerIfExists(sn2.attributes.name) === "shopify-checkout-api-token")) {
          return true;
        }
      }
    }
    return false;
  }
  function serializeNodeWithId(n22, options) {
    var doc = options.doc, mirror2 = options.mirror, blockClass = options.blockClass, blockSelector = options.blockSelector, maskTextClass = options.maskTextClass, maskTextSelector = options.maskTextSelector, _options_skipChild = options.skipChild, skipChild = _options_skipChild === void 0 ? false : _options_skipChild, _options_inlineStylesheet = options.inlineStylesheet, inlineStylesheet = _options_inlineStylesheet === void 0 ? true : _options_inlineStylesheet, _options_maskInputOptions = options.maskInputOptions, maskInputOptions = _options_maskInputOptions === void 0 ? {} : _options_maskInputOptions, maskTextFn = options.maskTextFn, maskInputFn = options.maskInputFn, slimDOMOptions = options.slimDOMOptions, _options_dataURLOptions = options.dataURLOptions, dataURLOptions = _options_dataURLOptions === void 0 ? {} : _options_dataURLOptions, _options_inlineImages = options.inlineImages, inlineImages = _options_inlineImages === void 0 ? false : _options_inlineImages, _options_recordCanvas = options.recordCanvas, recordCanvas = _options_recordCanvas === void 0 ? false : _options_recordCanvas, onSerialize = options.onSerialize, onIframeLoad = options.onIframeLoad, _options_iframeLoadTimeout = options.iframeLoadTimeout, iframeLoadTimeout = _options_iframeLoadTimeout === void 0 ? 5e3 : _options_iframeLoadTimeout, onStylesheetLoad = options.onStylesheetLoad, _options_stylesheetLoadTimeout = options.stylesheetLoadTimeout, stylesheetLoadTimeout = _options_stylesheetLoadTimeout === void 0 ? 5e3 : _options_stylesheetLoadTimeout, _options_keepIframeSrcFn = options.keepIframeSrcFn, keepIframeSrcFn = _options_keepIframeSrcFn === void 0 ? function() {
      return false;
    } : _options_keepIframeSrcFn, _options_newlyAddedElement = options.newlyAddedElement, newlyAddedElement = _options_newlyAddedElement === void 0 ? false : _options_newlyAddedElement, _options_cssCaptured = options.cssCaptured, cssCaptured = _options_cssCaptured === void 0 ? false : _options_cssCaptured;
    var needsMask = options.needsMask;
    var _options_preserveWhiteSpace = options.preserveWhiteSpace, preserveWhiteSpace = _options_preserveWhiteSpace === void 0 ? true : _options_preserveWhiteSpace;
    if (!needsMask) {
      var checkAncestors = needsMask === void 0;
      needsMask = needMaskingText(n22, maskTextClass, maskTextSelector, checkAncestors);
    }
    var _serializedNode = serializeNode(n22, {
      doc,
      mirror: mirror2,
      blockClass,
      blockSelector,
      needsMask,
      inlineStylesheet,
      maskInputOptions,
      maskTextFn,
      maskInputFn,
      dataURLOptions,
      inlineImages,
      recordCanvas,
      keepIframeSrcFn,
      newlyAddedElement,
      cssCaptured
    });
    if (!_serializedNode) {
      console.warn(n22, "not serialized");
      return null;
    }
    var id;
    if (mirror2.hasNode(n22)) {
      id = mirror2.getId(n22);
    } else if (slimDOMExcluded(_serializedNode, slimDOMOptions) || !preserveWhiteSpace && _serializedNode.type === NodeType$3.Text && !_serializedNode.textContent.replace(/^\s+|\s+$/gm, "").length) {
      id = IGNORED_NODE;
    } else {
      id = genId();
    }
    var serializedNode = Object.assign(_serializedNode, {
      id
    });
    mirror2.add(n22, serializedNode);
    if (id === IGNORED_NODE) {
      return null;
    }
    if (onSerialize) {
      onSerialize(n22);
    }
    var recordChild = !skipChild;
    if (serializedNode.type === NodeType$3.Element) {
      recordChild = recordChild && !serializedNode.needBlock;
      delete serializedNode.needBlock;
      var shadowRootEl = index$1.shadowRoot(n22);
      if (shadowRootEl && isNativeShadowDom(shadowRootEl)) serializedNode.isShadowHost = true;
    }
    if ((serializedNode.type === NodeType$3.Document || serializedNode.type === NodeType$3.Element) && recordChild) {
      if (slimDOMOptions.headWhitespace && serializedNode.type === NodeType$3.Element && serializedNode.tagName === "head") {
        preserveWhiteSpace = false;
      }
      var bypassOptions = {
        doc,
        mirror: mirror2,
        blockClass,
        blockSelector,
        needsMask,
        maskTextClass,
        maskTextSelector,
        skipChild,
        inlineStylesheet,
        maskInputOptions,
        maskTextFn,
        maskInputFn,
        slimDOMOptions,
        dataURLOptions,
        inlineImages,
        recordCanvas,
        preserveWhiteSpace,
        onSerialize,
        onIframeLoad,
        iframeLoadTimeout,
        onStylesheetLoad,
        stylesheetLoadTimeout,
        keepIframeSrcFn,
        cssCaptured: false
      };
      if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "textarea" && serializedNode.attributes.value !== void 0) ;
      else {
        if (serializedNode.type === NodeType$3.Element && serializedNode.attributes._cssText !== void 0 && typeof serializedNode.attributes._cssText === "string") {
          bypassOptions.cssCaptured = true;
        }
        for (var _iterator = _create_for_of_iterator_helper_loose(Array.from(index$1.childNodes(n22))), _step; !(_step = _iterator()).done; ) {
          var childN = _step.value;
          var serializedChildNode = serializeNodeWithId(childN, bypassOptions);
          if (serializedChildNode) {
            serializedNode.childNodes.push(serializedChildNode);
          }
        }
      }
      var shadowRootEl1 = null;
      if (isElement(n22) && (shadowRootEl1 = index$1.shadowRoot(n22))) {
        for (var _iterator1 = _create_for_of_iterator_helper_loose(Array.from(index$1.childNodes(shadowRootEl1))), _step1; !(_step1 = _iterator1()).done; ) {
          var childN1 = _step1.value;
          var serializedChildNode1 = serializeNodeWithId(childN1, bypassOptions);
          if (serializedChildNode1) {
            isNativeShadowDom(shadowRootEl1) && (serializedChildNode1.isShadow = true);
            serializedNode.childNodes.push(serializedChildNode1);
          }
        }
      }
    }
    var parent = index$1.parentNode(n22);
    if (parent && isShadowRoot(parent) && isNativeShadowDom(parent)) {
      serializedNode.isShadow = true;
    }
    if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "iframe") {
      onceIframeLoaded(n22, function() {
        var iframeDoc = n22.contentDocument;
        if (iframeDoc && onIframeLoad) {
          var serializedIframeNode = serializeNodeWithId(iframeDoc, {
            doc: iframeDoc,
            mirror: mirror2,
            blockClass,
            blockSelector,
            needsMask,
            maskTextClass,
            maskTextSelector,
            skipChild: false,
            inlineStylesheet,
            maskInputOptions,
            maskTextFn,
            maskInputFn,
            slimDOMOptions,
            dataURLOptions,
            inlineImages,
            recordCanvas,
            preserveWhiteSpace,
            onSerialize,
            onIframeLoad,
            iframeLoadTimeout,
            onStylesheetLoad,
            stylesheetLoadTimeout,
            keepIframeSrcFn
          });
          if (serializedIframeNode) {
            onIframeLoad(n22, serializedIframeNode);
          }
        }
      }, iframeLoadTimeout);
    }
    if (serializedNode.type === NodeType$3.Element && serializedNode.tagName === "link" && typeof serializedNode.attributes.rel === "string" && (serializedNode.attributes.rel === "stylesheet" || serializedNode.attributes.rel === "preload" && typeof serializedNode.attributes.href === "string" && extractFileExtension(serializedNode.attributes.href) === "css")) {
      onceStylesheetLoaded(n22, function() {
        if (onStylesheetLoad) {
          var serializedLinkNode = serializeNodeWithId(n22, {
            doc,
            mirror: mirror2,
            blockClass,
            blockSelector,
            needsMask,
            maskTextClass,
            maskTextSelector,
            skipChild: false,
            inlineStylesheet,
            maskInputOptions,
            maskTextFn,
            maskInputFn,
            slimDOMOptions,
            dataURLOptions,
            inlineImages,
            recordCanvas,
            preserveWhiteSpace,
            onSerialize,
            onIframeLoad,
            iframeLoadTimeout,
            onStylesheetLoad,
            stylesheetLoadTimeout,
            keepIframeSrcFn
          });
          if (serializedLinkNode) {
            onStylesheetLoad(n22, serializedLinkNode);
          }
        }
      }, stylesheetLoadTimeout);
    }
    return serializedNode;
  }
  function snapshot(n22, options) {
    var _ref = options || {}, tmp = _ref.mirror, mirror2 = tmp === void 0 ? new Mirror() : tmp, _ref_blockClass = _ref.blockClass, blockClass = _ref_blockClass === void 0 ? "rr-block" : _ref_blockClass, _ref_blockSelector = _ref.blockSelector, blockSelector = _ref_blockSelector === void 0 ? null : _ref_blockSelector, _ref_maskTextClass = _ref.maskTextClass, maskTextClass = _ref_maskTextClass === void 0 ? "rr-mask" : _ref_maskTextClass, _ref_maskTextSelector = _ref.maskTextSelector, maskTextSelector = _ref_maskTextSelector === void 0 ? null : _ref_maskTextSelector, _ref_inlineStylesheet = _ref.inlineStylesheet, inlineStylesheet = _ref_inlineStylesheet === void 0 ? true : _ref_inlineStylesheet, _ref_inlineImages = _ref.inlineImages, inlineImages = _ref_inlineImages === void 0 ? false : _ref_inlineImages, _ref_recordCanvas = _ref.recordCanvas, recordCanvas = _ref_recordCanvas === void 0 ? false : _ref_recordCanvas, _ref_maskAllInputs = _ref.maskAllInputs, maskAllInputs = _ref_maskAllInputs === void 0 ? false : _ref_maskAllInputs, maskTextFn = _ref.maskTextFn, maskInputFn = _ref.maskInputFn, _ref_slimDOM = _ref.slimDOM, slimDOM = _ref_slimDOM === void 0 ? false : _ref_slimDOM, dataURLOptions = _ref.dataURLOptions, preserveWhiteSpace = _ref.preserveWhiteSpace, onSerialize = _ref.onSerialize, onIframeLoad = _ref.onIframeLoad, iframeLoadTimeout = _ref.iframeLoadTimeout, onStylesheetLoad = _ref.onStylesheetLoad, stylesheetLoadTimeout = _ref.stylesheetLoadTimeout, _ref_keepIframeSrcFn = _ref.keepIframeSrcFn, keepIframeSrcFn = _ref_keepIframeSrcFn === void 0 ? function() {
      return false;
    } : _ref_keepIframeSrcFn;
    var maskInputOptions = maskAllInputs === true ? {
      color: true,
      date: true,
      "datetime-local": true,
      email: true,
      month: true,
      number: true,
      range: true,
      search: true,
      tel: true,
      text: true,
      time: true,
      url: true,
      week: true,
      textarea: true,
      select: true,
      password: true,
      hidden: true
    } : maskAllInputs === false ? {
      password: true
    } : maskAllInputs;
    var slimDOMOptions = slimDOM === true || slimDOM === "all" ? (
      // if true: set of sensible options that should not throw away any information
      {
        script: true,
        comment: true,
        headFavicon: true,
        headWhitespace: true,
        headMetaDescKeywords: slimDOM === "all",
        // destructive
        headMetaSocial: true,
        headMetaRobots: true,
        headMetaHttpEquiv: true,
        headMetaAuthorship: true,
        headMetaVerification: true
      }
    ) : slimDOM === false ? {} : slimDOM;
    return serializeNodeWithId(n22, {
      doc: n22,
      mirror: mirror2,
      blockClass,
      blockSelector,
      maskTextClass,
      maskTextSelector,
      skipChild: false,
      inlineStylesheet,
      maskInputOptions,
      maskTextFn,
      maskInputFn,
      slimDOMOptions,
      dataURLOptions,
      inlineImages,
      recordCanvas,
      preserveWhiteSpace,
      onSerialize,
      onIframeLoad,
      iframeLoadTimeout,
      onStylesheetLoad,
      stylesheetLoadTimeout,
      keepIframeSrcFn,
      newlyAddedElement: false
    });
  }
  function getDefaultExportFromCjs$1(x22) {
    return x22 && x22.__esModule && Object.prototype.hasOwnProperty.call(x22, "default") ? x22["default"] : x22;
  }
  function getAugmentedNamespace$1(n22) {
    if (n22.__esModule) return n22;
    var f22 = n22.default;
    if (typeof f22 == "function") {
      var a22 = function a222() {
        if (_instanceof(this, a222)) {
          return Reflect.construct(f22, arguments, this.constructor);
        }
        return f22.apply(this, arguments);
      };
      a22.prototype = f22.prototype;
    } else a22 = {};
    Object.defineProperty(a22, "__esModule", {
      value: true
    });
    Object.keys(n22).forEach(function(k4) {
      var d3 = Object.getOwnPropertyDescriptor(n22, k4);
      Object.defineProperty(a22, k4, d3.get ? d3 : {
        enumerable: true,
        get: function get() {
          return n22[k4];
        }
      });
    });
    return a22;
  }
  var picocolors_browser$1 = {
    exports: {}
  };
  var x$1 = String;
  var create$1 = function create$12() {
    return {
      isColorSupported: false,
      reset: x$1,
      bold: x$1,
      dim: x$1,
      italic: x$1,
      underline: x$1,
      inverse: x$1,
      hidden: x$1,
      strikethrough: x$1,
      black: x$1,
      red: x$1,
      green: x$1,
      yellow: x$1,
      blue: x$1,
      magenta: x$1,
      cyan: x$1,
      white: x$1,
      gray: x$1,
      bgBlack: x$1,
      bgRed: x$1,
      bgGreen: x$1,
      bgYellow: x$1,
      bgBlue: x$1,
      bgMagenta: x$1,
      bgCyan: x$1,
      bgWhite: x$1
    };
  };
  picocolors_browser$1.exports = create$1();
  picocolors_browser$1.exports.createColors = create$1;
  var picocolors_browserExports$1 = picocolors_browser$1.exports;
  var __viteBrowserExternal$2 = {};
  var __viteBrowserExternal$1$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
    __proto__: null,
    default: __viteBrowserExternal$2
  }, Symbol.toStringTag, {
    value: "Module"
  }));
  var require$$2$1 = /* @__PURE__ */ getAugmentedNamespace$1(__viteBrowserExternal$1$1);
  var pico$1 = picocolors_browserExports$1;
  var terminalHighlight$1$1 = require$$2$1;
  var CssSyntaxError$3$1 = /* @__PURE__ */ (function(Error1) {
    _inherits(CssSyntaxError, Error1);
    function CssSyntaxError(message, line, column, source, file, plugin22) {
      var _this;
      _this = Error1.call(this, message) || this;
      _this.name = "CssSyntaxError";
      _this.reason = message;
      if (file) {
        _this.file = file;
      }
      if (source) {
        _this.source = source;
      }
      if (plugin22) {
        _this.plugin = plugin22;
      }
      if (typeof line !== "undefined" && typeof column !== "undefined") {
        if (typeof line === "number") {
          _this.line = line;
          _this.column = column;
        } else {
          _this.line = line.line;
          _this.column = line.column;
          _this.endLine = column.line;
          _this.endColumn = column.column;
        }
      }
      _this.setMessage();
      if (Error.captureStackTrace) {
        Error.captureStackTrace(_this, CssSyntaxError);
      }
      return _this;
    }
    var _proto = CssSyntaxError.prototype;
    _proto.setMessage = function setMessage() {
      this.message = this.plugin ? this.plugin + ": " : "";
      this.message += this.file ? this.file : "<css input>";
      if (typeof this.line !== "undefined") {
        this.message += ":" + this.line + ":" + this.column;
      }
      this.message += ": " + this.reason;
    };
    _proto.showSourceCode = function showSourceCode(color) {
      var _this = this;
      if (!this.source) return "";
      var css = this.source;
      if (color == null) color = pico$1.isColorSupported;
      if (terminalHighlight$1$1) {
        if (color) css = terminalHighlight$1$1(css);
      }
      var lines = css.split(/\r?\n/);
      var start = Math.max(this.line - 3, 0);
      var end = Math.min(this.line + 2, lines.length);
      var maxWidth = String(end).length;
      var mark, aside;
      if (color) {
        var _pico$1_createColors = pico$1.createColors(true), bold = _pico$1_createColors.bold, gray = _pico$1_createColors.gray, red = _pico$1_createColors.red;
        mark = function(text) {
          return bold(red(text));
        };
        aside = function(text) {
          return gray(text);
        };
      } else {
        mark = aside = function(str) {
          return str;
        };
      }
      return lines.slice(start, end).map(function(line, index2) {
        var number = start + 1 + index2;
        var gutter = " " + (" " + number).slice(-maxWidth) + " | ";
        if (number === _this.line) {
          var spacing = aside(gutter.replace(/\d/g, " ")) + line.slice(0, _this.column - 1).replace(/[^\t]/g, " ");
          return mark(">") + aside(gutter) + line + "\n " + spacing + mark("^");
        }
        return " " + aside(gutter) + line;
      }).join("\n");
    };
    _proto.toString = function toString2() {
      var code = this.showSourceCode();
      if (code) {
        code = "\n\n" + code + "\n";
      }
      return this.name + ": " + this.message + code;
    };
    return CssSyntaxError;
  })(_wrap_native_super(Error));
  var cssSyntaxError$1 = CssSyntaxError$3$1;
  CssSyntaxError$3$1.default = CssSyntaxError$3$1;
  var symbols$1 = {};
  symbols$1.isClean = /* @__PURE__ */ Symbol("isClean");
  symbols$1.my = /* @__PURE__ */ Symbol("my");
  var DEFAULT_RAW$1 = {
    after: "\n",
    beforeClose: "\n",
    beforeComment: "\n",
    beforeDecl: "\n",
    beforeOpen: " ",
    beforeRule: "\n",
    colon: ": ",
    commentLeft: " ",
    commentRight: " ",
    emptyBody: "",
    indent: "    ",
    semicolon: false
  };
  function capitalize$1(str) {
    return str[0].toUpperCase() + str.slice(1);
  }
  var Stringifier$2$1 = /* @__PURE__ */ (function() {
    function Stringifier(builder) {
      this.builder = builder;
    }
    var _proto = Stringifier.prototype;
    _proto.atrule = function atrule(node2, semicolon) {
      var name5 = "@" + node2.name;
      var params = node2.params ? this.rawValue(node2, "params") : "";
      if (typeof node2.raws.afterName !== "undefined") {
        name5 += node2.raws.afterName;
      } else if (params) {
        name5 += " ";
      }
      if (node2.nodes) {
        this.block(node2, name5 + params);
      } else {
        var end = (node2.raws.between || "") + (semicolon ? ";" : "");
        this.builder(name5 + params + end, node2);
      }
    };
    _proto.beforeAfter = function beforeAfter(node2, detect) {
      var value;
      if (node2.type === "decl") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (node2.type === "comment") {
        value = this.raw(node2, null, "beforeComment");
      } else if (detect === "before") {
        value = this.raw(node2, null, "beforeRule");
      } else {
        value = this.raw(node2, null, "beforeClose");
      }
      var buf = node2.parent;
      var depth = 0;
      while (buf && buf.type !== "root") {
        depth += 1;
        buf = buf.parent;
      }
      if (value.includes("\n")) {
        var indent = this.raw(node2, null, "indent");
        if (indent.length) {
          for (var step = 0; step < depth; step++) value += indent;
        }
      }
      return value;
    };
    _proto.block = function block(node2, start) {
      var between = this.raw(node2, "between", "beforeOpen");
      this.builder(start + between + "{", node2, "start");
      var after;
      if (node2.nodes && node2.nodes.length) {
        this.body(node2);
        after = this.raw(node2, "after");
      } else {
        after = this.raw(node2, "after", "emptyBody");
      }
      if (after) this.builder(after);
      this.builder("}", node2, "end");
    };
    _proto.body = function body(node2) {
      var last = node2.nodes.length - 1;
      while (last > 0) {
        if (node2.nodes[last].type !== "comment") break;
        last -= 1;
      }
      var semicolon = this.raw(node2, "semicolon");
      for (var i22 = 0; i22 < node2.nodes.length; i22++) {
        var child = node2.nodes[i22];
        var before = this.raw(child, "before");
        if (before) this.builder(before);
        this.stringify(child, last !== i22 || semicolon);
      }
    };
    _proto.comment = function comment2(node2) {
      var left = this.raw(node2, "left", "commentLeft");
      var right = this.raw(node2, "right", "commentRight");
      this.builder("/*" + left + node2.text + right + "*/", node2);
    };
    _proto.decl = function decl(node2, semicolon) {
      var between = this.raw(node2, "between", "colon");
      var string = node2.prop + between + this.rawValue(node2, "value");
      if (node2.important) {
        string += node2.raws.important || " !important";
      }
      if (semicolon) string += ";";
      this.builder(string, node2);
    };
    _proto.document = function document1(node2) {
      this.body(node2);
    };
    _proto.raw = function raw(node2, own, detect) {
      var value;
      if (!detect) detect = own;
      if (own) {
        value = node2.raws[own];
        if (typeof value !== "undefined") return value;
      }
      var parent = node2.parent;
      if (detect === "before") {
        if (!parent || parent.type === "root" && parent.first === node2) {
          return "";
        }
        if (parent && parent.type === "document") {
          return "";
        }
      }
      if (!parent) return DEFAULT_RAW$1[detect];
      var root2 = node2.root();
      if (!root2.rawCache) root2.rawCache = {};
      if (typeof root2.rawCache[detect] !== "undefined") {
        return root2.rawCache[detect];
      }
      if (detect === "before" || detect === "after") {
        return this.beforeAfter(node2, detect);
      } else {
        var method = "raw" + capitalize$1(detect);
        if (this[method]) {
          value = this[method](root2, node2);
        } else {
          root2.walk(function(i22) {
            value = i22.raws[own];
            if (typeof value !== "undefined") return false;
          });
        }
      }
      if (typeof value === "undefined") value = DEFAULT_RAW$1[detect];
      root2.rawCache[detect] = value;
      return value;
    };
    _proto.rawBeforeClose = function rawBeforeClose(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length > 0) {
          if (typeof i22.raws.after !== "undefined") {
            value = i22.raws.after;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawBeforeComment = function rawBeforeComment(root2, node2) {
      var value;
      root2.walkComments(function(i22) {
        if (typeof i22.raws.before !== "undefined") {
          value = i22.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeDecl = function rawBeforeDecl(root2, node2) {
      var value;
      root2.walkDecls(function(i22) {
        if (typeof i22.raws.before !== "undefined") {
          value = i22.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeRule");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeOpen = function rawBeforeOpen(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.type !== "decl") {
          value = i22.raws.between;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawBeforeRule = function rawBeforeRule(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && (i22.parent !== root2 || root2.first !== i22)) {
          if (typeof i22.raws.before !== "undefined") {
            value = i22.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawColon = function rawColon(root2) {
      var value;
      root2.walkDecls(function(i22) {
        if (typeof i22.raws.between !== "undefined") {
          value = i22.raws.between.replace(/[^\s:]/g, "");
          return false;
        }
      });
      return value;
    };
    _proto.rawEmptyBody = function rawEmptyBody(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length === 0) {
          value = i22.raws.after;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawIndent = function rawIndent(root2) {
      if (root2.raws.indent) return root2.raws.indent;
      var value;
      root2.walk(function(i22) {
        var p3 = i22.parent;
        if (p3 && p3 !== root2 && p3.parent && p3.parent === root2) {
          if (typeof i22.raws.before !== "undefined") {
            var parts = i22.raws.before.split("\n");
            value = parts[parts.length - 1];
            value = value.replace(/\S/g, "");
            return false;
          }
        }
      });
      return value;
    };
    _proto.rawSemicolon = function rawSemicolon(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length && i22.last.type === "decl") {
          value = i22.raws.semicolon;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawValue = function rawValue(node2, prop) {
      var value = node2[prop];
      var raw = node2.raws[prop];
      if (raw && raw.value === value) {
        return raw.raw;
      }
      return value;
    };
    _proto.root = function root2(node2) {
      this.body(node2);
      if (node2.raws.after) this.builder(node2.raws.after);
    };
    _proto.rule = function rule2(node2) {
      this.block(node2, this.rawValue(node2, "selector"));
      if (node2.raws.ownSemicolon) {
        this.builder(node2.raws.ownSemicolon, node2, "end");
      }
    };
    _proto.stringify = function stringify2(node2, semicolon) {
      if (!this[node2.type]) {
        throw new Error("Unknown AST node type " + node2.type + ". Maybe you need to change PostCSS stringifier.");
      }
      this[node2.type](node2, semicolon);
    };
    return Stringifier;
  })();
  var stringifier$1 = Stringifier$2$1;
  Stringifier$2$1.default = Stringifier$2$1;
  var Stringifier$1$1 = stringifier$1;
  function stringify$4$1(node2, builder) {
    var str = new Stringifier$1$1(builder);
    str.stringify(node2);
  }
  var stringify_1$1 = stringify$4$1;
  stringify$4$1.default = stringify$4$1;
  var isClean$2$1 = symbols$1.isClean;
  var my$2$1 = symbols$1.my;
  var CssSyntaxError$2$1 = cssSyntaxError$1;
  var Stringifier2$1 = stringifier$1;
  var stringify$3$1 = stringify_1$1;
  function cloneNode$1(obj, parent) {
    var cloned = new obj.constructor();
    for (var i22 in obj) {
      if (!Object.prototype.hasOwnProperty.call(obj, i22)) {
        continue;
      }
      if (i22 === "proxyCache") continue;
      var value = obj[i22];
      var type = typeof value === "undefined" ? "undefined" : _type_of(value);
      if (i22 === "parent" && type === "object") {
        if (parent) cloned[i22] = parent;
      } else if (i22 === "source") {
        cloned[i22] = value;
      } else if (Array.isArray(value)) {
        cloned[i22] = value.map(function(j4) {
          return cloneNode$1(j4, cloned);
        });
      } else {
        if (type === "object" && value !== null) value = cloneNode$1(value);
        cloned[i22] = value;
      }
    }
    return cloned;
  }
  var Node$4$1 = /* @__PURE__ */ (function() {
    function Node23(defaults) {
      if (defaults === void 0) defaults = {};
      this.raws = {};
      this[isClean$2$1] = false;
      this[my$2$1] = true;
      for (var name5 in defaults) {
        if (name5 === "nodes") {
          this.nodes = [];
          for (var _iterator = _create_for_of_iterator_helper_loose(defaults[name5]), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            if (typeof node2.clone === "function") {
              this.append(node2.clone());
            } else {
              this.append(node2);
            }
          }
        } else {
          this[name5] = defaults[name5];
        }
      }
    }
    var _proto = Node23.prototype;
    _proto.addToError = function addToError(error) {
      error.postcssNode = this;
      if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
        var s22 = this.source;
        error.stack = error.stack.replace(/\n\s{4}at /, "$&" + s22.input.from + ":" + s22.start.line + ":" + s22.start.column + "$&");
      }
      return error;
    };
    _proto.after = function after(add) {
      this.parent.insertAfter(this, add);
      return this;
    };
    _proto.assign = function assign(overrides) {
      if (overrides === void 0) overrides = {};
      for (var name5 in overrides) {
        this[name5] = overrides[name5];
      }
      return this;
    };
    _proto.before = function before(add) {
      this.parent.insertBefore(this, add);
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      delete this.raws.before;
      delete this.raws.after;
      if (!keepBetween) delete this.raws.between;
    };
    _proto.clone = function clone(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = cloneNode$1(this);
      for (var name5 in overrides) {
        cloned[name5] = overrides[name5];
      }
      return cloned;
    };
    _proto.cloneAfter = function cloneAfter(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertAfter(this, cloned);
      return cloned;
    };
    _proto.cloneBefore = function cloneBefore(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertBefore(this, cloned);
      return cloned;
    };
    _proto.error = function error(message, opts) {
      if (opts === void 0) opts = {};
      if (this.source) {
        var _this_rangeBy = this.rangeBy(opts), end = _this_rangeBy.end, start = _this_rangeBy.start;
        return this.source.input.error(message, {
          column: start.column,
          line: start.line
        }, {
          column: end.column,
          line: end.line
        }, opts);
      }
      return new CssSyntaxError$2$1(message);
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
          prop === "text") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.markDirty = function markDirty() {
      if (this[isClean$2$1]) {
        this[isClean$2$1] = false;
        var next = this;
        while (next = next.parent) {
          next[isClean$2$1] = false;
        }
      }
    };
    _proto.next = function next() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 + 1];
    };
    _proto.positionBy = function positionBy(opts, stringRepresentation) {
      var pos = this.source.start;
      if (opts.index) {
        pos = this.positionInside(opts.index, stringRepresentation);
      } else if (opts.word) {
        stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) pos = this.positionInside(index2, stringRepresentation);
      }
      return pos;
    };
    _proto.positionInside = function positionInside(index2, stringRepresentation) {
      var string = stringRepresentation || this.toString();
      var column = this.source.start.column;
      var line = this.source.start.line;
      for (var i22 = 0; i22 < index2; i22++) {
        if (string[i22] === "\n") {
          column = 1;
          line += 1;
        } else {
          column += 1;
        }
      }
      return {
        column,
        line
      };
    };
    _proto.prev = function prev() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 - 1];
    };
    _proto.rangeBy = function rangeBy(opts) {
      var start = {
        column: this.source.start.column,
        line: this.source.start.line
      };
      var end = this.source.end ? {
        column: this.source.end.column + 1,
        line: this.source.end.line
      } : {
        column: start.column + 1,
        line: start.line
      };
      if (opts.word) {
        var stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) {
          start = this.positionInside(index2, stringRepresentation);
          end = this.positionInside(index2 + opts.word.length, stringRepresentation);
        }
      } else {
        if (opts.start) {
          start = {
            column: opts.start.column,
            line: opts.start.line
          };
        } else if (opts.index) {
          start = this.positionInside(opts.index);
        }
        if (opts.end) {
          end = {
            column: opts.end.column,
            line: opts.end.line
          };
        } else if (typeof opts.endIndex === "number") {
          end = this.positionInside(opts.endIndex);
        } else if (opts.index) {
          end = this.positionInside(opts.index + 1);
        }
      }
      if (end.line < start.line || end.line === start.line && end.column <= start.column) {
        end = {
          column: start.column + 1,
          line: start.line
        };
      }
      return {
        end,
        start
      };
    };
    _proto.raw = function raw(prop, defaultType) {
      var str = new Stringifier2$1();
      return str.raw(this, prop, defaultType);
    };
    _proto.remove = function remove2() {
      if (this.parent) {
        this.parent.removeChild(this);
      }
      this.parent = void 0;
      return this;
    };
    _proto.replaceWith = function replaceWith() {
      for (var _len = arguments.length, nodes = new Array(_len), _key = 0; _key < _len; _key++) {
        nodes[_key] = arguments[_key];
      }
      if (this.parent) {
        var bookmark = this;
        var foundSelf = false;
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          if (node2 === this) {
            foundSelf = true;
          } else if (foundSelf) {
            this.parent.insertAfter(bookmark, node2);
            bookmark = node2;
          } else {
            this.parent.insertBefore(bookmark, node2);
          }
        }
        if (!foundSelf) {
          this.remove();
        }
      }
      return this;
    };
    _proto.root = function root2() {
      var result2 = this;
      while (result2.parent && result2.parent.type !== "document") {
        result2 = result2.parent;
      }
      return result2;
    };
    _proto.toJSON = function toJSON(_4, inputs) {
      var fixed = {};
      var emitInputs = inputs == null;
      inputs = inputs || /* @__PURE__ */ new Map();
      var inputsNextIndex = 0;
      for (var name5 in this) {
        if (!Object.prototype.hasOwnProperty.call(this, name5)) {
          continue;
        }
        if (name5 === "parent" || name5 === "proxyCache") continue;
        var value = this[name5];
        if (Array.isArray(value)) {
          fixed[name5] = value.map(function(i22) {
            if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && i22.toJSON) {
              return i22.toJSON(null, inputs);
            } else {
              return i22;
            }
          });
        } else if ((typeof value === "undefined" ? "undefined" : _type_of(value)) === "object" && value.toJSON) {
          fixed[name5] = value.toJSON(null, inputs);
        } else if (name5 === "source") {
          var inputId = inputs.get(value.input);
          if (inputId == null) {
            inputId = inputsNextIndex;
            inputs.set(value.input, inputsNextIndex);
            inputsNextIndex++;
          }
          fixed[name5] = {
            end: value.end,
            inputId,
            start: value.start
          };
        } else {
          fixed[name5] = value;
        }
      }
      if (emitInputs) {
        fixed.inputs = [].concat(inputs.keys()).map(function(input2) {
          return input2.toJSON();
        });
      }
      return fixed;
    };
    _proto.toProxy = function toProxy() {
      if (!this.proxyCache) {
        this.proxyCache = new Proxy(this, this.getProxyProcessor());
      }
      return this.proxyCache;
    };
    _proto.toString = function toString2(stringifier2) {
      if (stringifier2 === void 0) stringifier2 = stringify$3$1;
      if (stringifier2.stringify) stringifier2 = stringifier2.stringify;
      var result2 = "";
      stringifier2(this, function(i22) {
        result2 += i22;
      });
      return result2;
    };
    _proto.warn = function warn(result2, text, opts) {
      var data = {
        node: this
      };
      for (var i22 in opts) data[i22] = opts[i22];
      return result2.warn(text, data);
    };
    _create_class(Node23, [
      {
        key: "proxyOf",
        get: function get() {
          return this;
        }
      }
    ]);
    return Node23;
  })();
  var node$1 = Node$4$1;
  Node$4$1.default = Node$4$1;
  var Node$3$1 = node$1;
  var Declaration$4$1 = /* @__PURE__ */ (function(Node$3$12) {
    _inherits(Declaration, Node$3$12);
    function Declaration(defaults) {
      var _this;
      if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
        defaults = _extends({}, defaults, {
          value: String(defaults.value)
        });
      }
      _this = Node$3$12.call(this, defaults) || this;
      _this.type = "decl";
      return _this;
    }
    _create_class(Declaration, [
      {
        key: "variable",
        get: function get() {
          return this.prop.startsWith("--") || this.prop[0] === "$";
        }
      }
    ]);
    return Declaration;
  })(Node$3$1);
  var declaration$1 = Declaration$4$1;
  Declaration$4$1.default = Declaration$4$1;
  var urlAlphabet$1 = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
  var nanoid$1$1 = function(size) {
    if (size === void 0) size = 21;
    var id = "";
    var i22 = size;
    while (i22--) {
      id += urlAlphabet$1[Math.random() * 64 | 0];
    }
    return id;
  };
  var nonSecure$1 = {
    nanoid: nanoid$1$1
  };
  var SourceMapConsumer$2$1 = require$$2$1.SourceMapConsumer;
  var SourceMapGenerator$2$1 = require$$2$1.SourceMapGenerator;
  var existsSync$1 = require$$2$1.existsSync;
  var readFileSync$1 = require$$2$1.readFileSync;
  var dirname$1$1 = require$$2$1.dirname;
  var join$1 = require$$2$1.join;
  function fromBase64$1(str) {
    if (Buffer) {
      return Buffer.from(str, "base64").toString();
    } else {
      return window.atob(str);
    }
  }
  var PreviousMap$2$1 = /* @__PURE__ */ (function() {
    function PreviousMap(css, opts) {
      if (opts.map === false) return;
      this.loadAnnotation(css);
      this.inline = this.startWith(this.annotation, "data:");
      var prev = opts.map ? opts.map.prev : void 0;
      var text = this.loadMap(opts.from, prev);
      if (!this.mapFile && opts.from) {
        this.mapFile = opts.from;
      }
      if (this.mapFile) this.root = dirname$1$1(this.mapFile);
      if (text) this.text = text;
    }
    var _proto = PreviousMap.prototype;
    _proto.consumer = function consumer() {
      if (!this.consumerCache) {
        this.consumerCache = new SourceMapConsumer$2$1(this.text);
      }
      return this.consumerCache;
    };
    _proto.decodeInline = function decodeInline(text) {
      var baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
      var baseUri = /^data:application\/json;base64,/;
      var charsetUri = /^data:application\/json;charset=utf-?8,/;
      var uri = /^data:application\/json,/;
      if (charsetUri.test(text) || uri.test(text)) {
        return decodeURIComponent(text.substr(RegExp.lastMatch.length));
      }
      if (baseCharsetUri.test(text) || baseUri.test(text)) {
        return fromBase64$1(text.substr(RegExp.lastMatch.length));
      }
      var encoding = text.match(/data:application\/json;([^,]+),/)[1];
      throw new Error("Unsupported source map encoding " + encoding);
    };
    _proto.getAnnotationURL = function getAnnotationURL(sourceMapString) {
      return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
    };
    _proto.isMap = function isMap(map) {
      if ((typeof map === "undefined" ? "undefined" : _type_of(map)) !== "object") return false;
      return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
    };
    _proto.loadAnnotation = function loadAnnotation(css) {
      var comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
      if (!comments) return;
      var start = css.lastIndexOf(comments.pop());
      var end = css.indexOf("*/", start);
      if (start > -1 && end > -1) {
        this.annotation = this.getAnnotationURL(css.substring(start, end));
      }
    };
    _proto.loadFile = function loadFile(path) {
      this.root = dirname$1$1(path);
      if (existsSync$1(path)) {
        this.mapFile = path;
        return readFileSync$1(path, "utf-8").toString().trim();
      }
    };
    _proto.loadMap = function loadMap(file, prev) {
      if (prev === false) return false;
      if (prev) {
        if (typeof prev === "string") {
          return prev;
        } else if (typeof prev === "function") {
          var prevPath = prev(file);
          if (prevPath) {
            var map = this.loadFile(prevPath);
            if (!map) {
              throw new Error("Unable to load previous source map: " + prevPath.toString());
            }
            return map;
          }
        } else if (_instanceof(prev, SourceMapConsumer$2$1)) {
          return SourceMapGenerator$2$1.fromSourceMap(prev).toString();
        } else if (_instanceof(prev, SourceMapGenerator$2$1)) {
          return prev.toString();
        } else if (this.isMap(prev)) {
          return JSON.stringify(prev);
        } else {
          throw new Error("Unsupported previous source map format: " + prev.toString());
        }
      } else if (this.inline) {
        return this.decodeInline(this.annotation);
      } else if (this.annotation) {
        var map1 = this.annotation;
        if (file) map1 = join$1(dirname$1$1(file), map1);
        return this.loadFile(map1);
      }
    };
    _proto.startWith = function startWith(string, start) {
      if (!string) return false;
      return string.substr(0, start.length) === start;
    };
    _proto.withContent = function withContent() {
      return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
    };
    return PreviousMap;
  })();
  var previousMap$1 = PreviousMap$2$1;
  PreviousMap$2$1.default = PreviousMap$2$1;
  var SourceMapConsumer$1$1 = require$$2$1.SourceMapConsumer;
  var SourceMapGenerator$1$1 = require$$2$1.SourceMapGenerator;
  var fileURLToPath$1 = require$$2$1.fileURLToPath;
  var pathToFileURL$1$1 = require$$2$1.pathToFileURL;
  var isAbsolute$1 = require$$2$1.isAbsolute;
  var resolve$1$1 = require$$2$1.resolve;
  var nanoid$2 = nonSecure$1.nanoid;
  var terminalHighlight$2 = require$$2$1;
  var CssSyntaxError$1$1 = cssSyntaxError$1;
  var PreviousMap$1$1 = previousMap$1;
  var fromOffsetCache$1 = /* @__PURE__ */ Symbol("fromOffsetCache");
  var sourceMapAvailable$1$1 = Boolean(SourceMapConsumer$1$1 && SourceMapGenerator$1$1);
  var pathAvailable$1$1 = Boolean(resolve$1$1 && isAbsolute$1);
  var Input$4$1 = /* @__PURE__ */ (function() {
    function Input(css, opts) {
      if (opts === void 0) opts = {};
      if (css === null || typeof css === "undefined" || (typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && !css.toString) {
        throw new Error("PostCSS received " + css + " instead of CSS string");
      }
      this.css = css.toString();
      if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
        this.hasBOM = true;
        this.css = this.css.slice(1);
      } else {
        this.hasBOM = false;
      }
      if (opts.from) {
        if (!pathAvailable$1$1 || /^\w+:\/\//.test(opts.from) || isAbsolute$1(opts.from)) {
          this.file = opts.from;
        } else {
          this.file = resolve$1$1(opts.from);
        }
      }
      if (pathAvailable$1$1 && sourceMapAvailable$1$1) {
        var map = new PreviousMap$1$1(this.css, opts);
        if (map.text) {
          this.map = map;
          var file = map.consumer().file;
          if (!this.file && file) this.file = this.mapResolve(file);
        }
      }
      if (!this.file) {
        this.id = "<input css " + nanoid$2(6) + ">";
      }
      if (this.map) this.map.file = this.from;
    }
    var _proto = Input.prototype;
    _proto.error = function error(message, line, column, opts) {
      if (opts === void 0) opts = {};
      var result2, endLine, endColumn;
      if (line && (typeof line === "undefined" ? "undefined" : _type_of(line)) === "object") {
        var start = line;
        var end = column;
        if (typeof start.offset === "number") {
          var pos = this.fromOffset(start.offset);
          line = pos.line;
          column = pos.col;
        } else {
          line = start.line;
          column = start.column;
        }
        if (typeof end.offset === "number") {
          var pos1 = this.fromOffset(end.offset);
          endLine = pos1.line;
          endColumn = pos1.col;
        } else {
          endLine = end.line;
          endColumn = end.column;
        }
      } else if (!column) {
        var pos2 = this.fromOffset(line);
        line = pos2.line;
        column = pos2.col;
      }
      var origin = this.origin(line, column, endLine, endColumn);
      if (origin) {
        result2 = new CssSyntaxError$1$1(message, origin.endLine === void 0 ? origin.line : {
          column: origin.column,
          line: origin.line
        }, origin.endLine === void 0 ? origin.column : {
          column: origin.endColumn,
          line: origin.endLine
        }, origin.source, origin.file, opts.plugin);
      } else {
        result2 = new CssSyntaxError$1$1(message, endLine === void 0 ? line : {
          column,
          line
        }, endLine === void 0 ? column : {
          column: endColumn,
          line: endLine
        }, this.css, this.file, opts.plugin);
      }
      result2.input = {
        column,
        endColumn,
        endLine,
        line,
        source: this.css
      };
      if (this.file) {
        if (pathToFileURL$1$1) {
          result2.input.url = pathToFileURL$1$1(this.file).toString();
        }
        result2.input.file = this.file;
      }
      return result2;
    };
    _proto.fromOffset = function fromOffset(offset) {
      var lastLine, lineToIndex;
      if (!this[fromOffsetCache$1]) {
        var lines = this.css.split("\n");
        lineToIndex = new Array(lines.length);
        var prevIndex = 0;
        for (var i22 = 0, l22 = lines.length; i22 < l22; i22++) {
          lineToIndex[i22] = prevIndex;
          prevIndex += lines[i22].length + 1;
        }
        this[fromOffsetCache$1] = lineToIndex;
      } else {
        lineToIndex = this[fromOffsetCache$1];
      }
      lastLine = lineToIndex[lineToIndex.length - 1];
      var min = 0;
      if (offset >= lastLine) {
        min = lineToIndex.length - 1;
      } else {
        var max = lineToIndex.length - 2;
        var mid;
        while (min < max) {
          mid = min + (max - min >> 1);
          if (offset < lineToIndex[mid]) {
            max = mid - 1;
          } else if (offset >= lineToIndex[mid + 1]) {
            min = mid + 1;
          } else {
            min = mid;
            break;
          }
        }
      }
      return {
        col: offset - lineToIndex[min] + 1,
        line: min + 1
      };
    };
    _proto.mapResolve = function mapResolve(file) {
      if (/^\w+:\/\//.test(file)) {
        return file;
      }
      return resolve$1$1(this.map.consumer().sourceRoot || this.map.root || ".", file);
    };
    _proto.origin = function origin(line, column, endLine, endColumn) {
      if (!this.map) return false;
      var consumer = this.map.consumer();
      var from = consumer.originalPositionFor({
        column,
        line
      });
      if (!from.source) return false;
      var to;
      if (typeof endLine === "number") {
        to = consumer.originalPositionFor({
          column: endColumn,
          line: endLine
        });
      }
      var fromUrl;
      if (isAbsolute$1(from.source)) {
        fromUrl = pathToFileURL$1$1(from.source);
      } else {
        fromUrl = new URL(from.source, this.map.consumer().sourceRoot || pathToFileURL$1$1(this.map.mapFile));
      }
      var result2 = {
        column: from.column,
        endColumn: to && to.column,
        endLine: to && to.line,
        line: from.line,
        url: fromUrl.toString()
      };
      if (fromUrl.protocol === "file:") {
        if (fileURLToPath$1) {
          result2.file = fileURLToPath$1(fromUrl);
        } else {
          throw new Error("file: protocol is not available in this PostCSS build");
        }
      }
      var source = consumer.sourceContentFor(from.source);
      if (source) result2.source = source;
      return result2;
    };
    _proto.toJSON = function toJSON() {
      var json = {};
      for (var _i = 0, _iter = [
        "hasBOM",
        "css",
        "file",
        "id"
      ]; _i < _iter.length; _i++) {
        var name5 = _iter[_i];
        if (this[name5] != null) {
          json[name5] = this[name5];
        }
      }
      if (this.map) {
        json.map = _extends({}, this.map);
        if (json.map.consumerCache) {
          json.map.consumerCache = void 0;
        }
      }
      return json;
    };
    _create_class(Input, [
      {
        key: "from",
        get: function get() {
          return this.file || this.id;
        }
      }
    ]);
    return Input;
  })();
  var input$1 = Input$4$1;
  Input$4$1.default = Input$4$1;
  if (terminalHighlight$2 && terminalHighlight$2.registerInput) {
    terminalHighlight$2.registerInput(Input$4$1);
  }
  var SourceMapConsumer$3 = require$$2$1.SourceMapConsumer;
  var SourceMapGenerator$3 = require$$2$1.SourceMapGenerator;
  var dirname$2 = require$$2$1.dirname;
  var relative$1 = require$$2$1.relative;
  var resolve$2 = require$$2$1.resolve;
  var sep$1 = require$$2$1.sep;
  var pathToFileURL$2 = require$$2$1.pathToFileURL;
  var Input$3$1 = input$1;
  var sourceMapAvailable$2 = Boolean(SourceMapConsumer$3 && SourceMapGenerator$3);
  var pathAvailable$2 = Boolean(dirname$2 && resolve$2 && relative$1 && sep$1);
  var MapGenerator$2$1 = /* @__PURE__ */ (function() {
    function MapGenerator(stringify2, root2, opts, cssString) {
      this.stringify = stringify2;
      this.mapOpts = opts.map || {};
      this.root = root2;
      this.opts = opts;
      this.css = cssString;
      this.originalCSS = cssString;
      this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
      this.memoizedFileURLs = /* @__PURE__ */ new Map();
      this.memoizedPaths = /* @__PURE__ */ new Map();
      this.memoizedURLs = /* @__PURE__ */ new Map();
    }
    var _proto = MapGenerator.prototype;
    _proto.addAnnotation = function addAnnotation() {
      var content;
      if (this.isInline()) {
        content = "data:application/json;base64," + this.toBase64(this.map.toString());
      } else if (typeof this.mapOpts.annotation === "string") {
        content = this.mapOpts.annotation;
      } else if (typeof this.mapOpts.annotation === "function") {
        content = this.mapOpts.annotation(this.opts.to, this.root);
      } else {
        content = this.outputFile() + ".map";
      }
      var eol = "\n";
      if (this.css.includes("\r\n")) eol = "\r\n";
      this.css += eol + "/*# sourceMappingURL=" + content + " */";
    };
    _proto.applyPrevMaps = function applyPrevMaps() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.previous()), _step; !(_step = _iterator()).done; ) {
        var prev = _step.value;
        var from = this.toUrl(this.path(prev.file));
        var root2 = prev.root || dirname$2(prev.file);
        var map = void 0;
        if (this.mapOpts.sourcesContent === false) {
          map = new SourceMapConsumer$3(prev.text);
          if (map.sourcesContent) {
            map.sourcesContent = null;
          }
        } else {
          map = prev.consumer();
        }
        this.map.applySourceMap(map, from, this.toUrl(this.path(root2)));
      }
    };
    _proto.clearAnnotation = function clearAnnotation() {
      if (this.mapOpts.annotation === false) return;
      if (this.root) {
        var node2;
        for (var i22 = this.root.nodes.length - 1; i22 >= 0; i22--) {
          node2 = this.root.nodes[i22];
          if (node2.type !== "comment") continue;
          if (node2.text.indexOf("# sourceMappingURL=") === 0) {
            this.root.removeChild(i22);
          }
        }
      } else if (this.css) {
        this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
      }
    };
    _proto.generate = function generate() {
      this.clearAnnotation();
      if (pathAvailable$2 && sourceMapAvailable$2 && this.isMap()) {
        return this.generateMap();
      } else {
        var result2 = "";
        this.stringify(this.root, function(i22) {
          result2 += i22;
        });
        return [
          result2
        ];
      }
    };
    _proto.generateMap = function generateMap() {
      if (this.root) {
        this.generateString();
      } else if (this.previous().length === 1) {
        var prev = this.previous()[0].consumer();
        prev.file = this.outputFile();
        this.map = SourceMapGenerator$3.fromSourceMap(prev, {
          ignoreInvalidMapping: true
        });
      } else {
        this.map = new SourceMapGenerator$3({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        this.map.addMapping({
          generated: {
            column: 0,
            line: 1
          },
          original: {
            column: 0,
            line: 1
          },
          source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
        });
      }
      if (this.isSourcesContent()) this.setSourcesContent();
      if (this.root && this.previous().length > 0) this.applyPrevMaps();
      if (this.isAnnotation()) this.addAnnotation();
      if (this.isInline()) {
        return [
          this.css
        ];
      } else {
        return [
          this.css,
          this.map
        ];
      }
    };
    _proto.generateString = function generateString() {
      var _this = this;
      this.css = "";
      this.map = new SourceMapGenerator$3({
        file: this.outputFile(),
        ignoreInvalidMapping: true
      });
      var line = 1;
      var column = 1;
      var noSource = "<no source>";
      var mapping = {
        generated: {
          column: 0,
          line: 0
        },
        original: {
          column: 0,
          line: 0
        },
        source: ""
      };
      var lines, last;
      this.stringify(this.root, function(str, node2, type) {
        _this.css += str;
        if (node2 && type !== "end") {
          mapping.generated.line = line;
          mapping.generated.column = column - 1;
          if (node2.source && node2.source.start) {
            mapping.source = _this.sourcePath(node2);
            mapping.original.line = node2.source.start.line;
            mapping.original.column = node2.source.start.column - 1;
            _this.map.addMapping(mapping);
          } else {
            mapping.source = noSource;
            mapping.original.line = 1;
            mapping.original.column = 0;
            _this.map.addMapping(mapping);
          }
        }
        lines = str.match(/\n/g);
        if (lines) {
          line += lines.length;
          last = str.lastIndexOf("\n");
          column = str.length - last;
        } else {
          column += str.length;
        }
        if (node2 && type !== "start") {
          var p3 = node2.parent || {
            raws: {}
          };
          var childless = node2.type === "decl" || node2.type === "atrule" && !node2.nodes;
          if (!childless || node2 !== p3.last || p3.raws.semicolon) {
            if (node2.source && node2.source.end) {
              mapping.source = _this.sourcePath(node2);
              mapping.original.line = node2.source.end.line;
              mapping.original.column = node2.source.end.column - 1;
              mapping.generated.line = line;
              mapping.generated.column = column - 2;
              _this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              mapping.generated.line = line;
              mapping.generated.column = column - 1;
              _this.map.addMapping(mapping);
            }
          }
        }
      });
    };
    _proto.isAnnotation = function isAnnotation() {
      if (this.isInline()) {
        return true;
      }
      if (typeof this.mapOpts.annotation !== "undefined") {
        return this.mapOpts.annotation;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.annotation;
        });
      }
      return true;
    };
    _proto.isInline = function isInline() {
      if (typeof this.mapOpts.inline !== "undefined") {
        return this.mapOpts.inline;
      }
      var annotation = this.mapOpts.annotation;
      if (typeof annotation !== "undefined" && annotation !== true) {
        return false;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.inline;
        });
      }
      return true;
    };
    _proto.isMap = function isMap() {
      if (typeof this.opts.map !== "undefined") {
        return !!this.opts.map;
      }
      return this.previous().length > 0;
    };
    _proto.isSourcesContent = function isSourcesContent() {
      if (typeof this.mapOpts.sourcesContent !== "undefined") {
        return this.mapOpts.sourcesContent;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.withContent();
        });
      }
      return true;
    };
    _proto.outputFile = function outputFile() {
      if (this.opts.to) {
        return this.path(this.opts.to);
      } else if (this.opts.from) {
        return this.path(this.opts.from);
      } else {
        return "to.css";
      }
    };
    _proto.path = function path(file) {
      if (this.mapOpts.absolute) return file;
      if (file.charCodeAt(0) === 60) return file;
      if (/^\w+:\/\//.test(file)) return file;
      var cached = this.memoizedPaths.get(file);
      if (cached) return cached;
      var from = this.opts.to ? dirname$2(this.opts.to) : ".";
      if (typeof this.mapOpts.annotation === "string") {
        from = dirname$2(resolve$2(from, this.mapOpts.annotation));
      }
      var path2 = relative$1(from, file);
      this.memoizedPaths.set(file, path2);
      return path2;
    };
    _proto.previous = function previous() {
      var _this = this;
      if (!this.previousMaps) {
        this.previousMaps = [];
        if (this.root) {
          this.root.walk(function(node2) {
            if (node2.source && node2.source.input.map) {
              var map = node2.source.input.map;
              if (!_this.previousMaps.includes(map)) {
                _this.previousMaps.push(map);
              }
            }
          });
        } else {
          var input2 = new Input$3$1(this.originalCSS, this.opts);
          if (input2.map) this.previousMaps.push(input2.map);
        }
      }
      return this.previousMaps;
    };
    _proto.setSourcesContent = function setSourcesContent() {
      var _this = this;
      var already = {};
      if (this.root) {
        this.root.walk(function(node2) {
          if (node2.source) {
            var from2 = node2.source.input.from;
            if (from2 && !already[from2]) {
              already[from2] = true;
              var fromUrl = _this.usesFileUrls ? _this.toFileUrl(from2) : _this.toUrl(_this.path(from2));
              _this.map.setSourceContent(fromUrl, node2.source.input.css);
            }
          }
        });
      } else if (this.css) {
        var from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
        this.map.setSourceContent(from, this.css);
      }
    };
    _proto.sourcePath = function sourcePath(node2) {
      if (this.mapOpts.from) {
        return this.toUrl(this.mapOpts.from);
      } else if (this.usesFileUrls) {
        return this.toFileUrl(node2.source.input.from);
      } else {
        return this.toUrl(this.path(node2.source.input.from));
      }
    };
    _proto.toBase64 = function toBase64(str) {
      if (Buffer) {
        return Buffer.from(str).toString("base64");
      } else {
        return window.btoa(unescape(encodeURIComponent(str)));
      }
    };
    _proto.toFileUrl = function toFileUrl(path) {
      var cached = this.memoizedFileURLs.get(path);
      if (cached) return cached;
      if (pathToFileURL$2) {
        var fileURL = pathToFileURL$2(path).toString();
        this.memoizedFileURLs.set(path, fileURL);
        return fileURL;
      } else {
        throw new Error("`map.absolute` option is not available in this PostCSS build");
      }
    };
    _proto.toUrl = function toUrl(path) {
      var cached = this.memoizedURLs.get(path);
      if (cached) return cached;
      if (sep$1 === "\\") {
        path = path.replace(/\\/g, "/");
      }
      var url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
      this.memoizedURLs.set(path, url);
      return url;
    };
    return MapGenerator;
  })();
  var mapGenerator$1 = MapGenerator$2$1;
  var Node$2$1 = node$1;
  var Comment$4$1 = /* @__PURE__ */ (function(Node$2$12) {
    _inherits(Comment, Node$2$12);
    function Comment(defaults) {
      var _this;
      _this = Node$2$12.call(this, defaults) || this;
      _this.type = "comment";
      return _this;
    }
    return Comment;
  })(Node$2$1);
  var comment$1 = Comment$4$1;
  Comment$4$1.default = Comment$4$1;
  var isClean$1$1 = symbols$1.isClean;
  var my$1$1 = symbols$1.my;
  var Declaration$3$1 = declaration$1;
  var Comment$3$1 = comment$1;
  var Node$1$1 = node$1;
  var parse$4$1;
  var Rule$4$1;
  var AtRule$4$1;
  var Root$6$1;
  function cleanSource$1(nodes) {
    return nodes.map(function(i22) {
      if (i22.nodes) i22.nodes = cleanSource$1(i22.nodes);
      delete i22.source;
      return i22;
    });
  }
  function markDirtyUp$1(node2) {
    node2[isClean$1$1] = false;
    if (node2.proxyOf.nodes) {
      for (var _iterator = _create_for_of_iterator_helper_loose(node2.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var i22 = _step.value;
        markDirtyUp$1(i22);
      }
    }
  }
  var Container$7$1 = /* @__PURE__ */ (function(Node$1$12) {
    _inherits(Container, Node$1$12);
    function Container() {
      return Node$1$12.apply(this, arguments) || this;
    }
    var _proto = Container.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.last);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.push(node2);
        }
      }
      this.markDirty();
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      Node$1$12.prototype.cleanRaws.call(this, keepBetween);
      if (this.nodes) {
        for (var _iterator = _create_for_of_iterator_helper_loose(this.nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          node2.cleanRaws(keepBetween);
        }
      }
    };
    _proto.each = function each(callback) {
      if (!this.proxyOf.nodes) return void 0;
      var iterator = this.getIterator();
      var index2, result2;
      while (this.indexes[iterator] < this.proxyOf.nodes.length) {
        index2 = this.indexes[iterator];
        result2 = callback(this.proxyOf.nodes[index2], index2);
        if (result2 === false) break;
        this.indexes[iterator] += 1;
      }
      delete this.indexes[iterator];
      return result2;
    };
    _proto.every = function every(condition) {
      return this.nodes.every(condition);
    };
    _proto.getIterator = function getIterator() {
      if (!this.lastEach) this.lastEach = 0;
      if (!this.indexes) this.indexes = {};
      this.lastEach += 1;
      var iterator = this.lastEach;
      this.indexes[iterator] = 0;
      return iterator;
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (!node2[prop]) {
            return node2[prop];
          } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
            return function() {
              for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
                args[_key] = arguments[_key];
              }
              var _node2;
              return (_node2 = node2)[prop].apply(_node2, [].concat(args.map(function(i22) {
                if (typeof i22 === "function") {
                  return function(child, index2) {
                    return i22(child.toProxy(), index2);
                  };
                } else {
                  return i22;
                }
              })));
            };
          } else if (prop === "every" || prop === "some") {
            return function(cb) {
              return node2[prop](function(child) {
                for (var _len = arguments.length, other = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
                  other[_key - 1] = arguments[_key];
                }
                return cb.apply(void 0, [].concat([
                  child.toProxy()
                ], other));
              });
            };
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else if (prop === "nodes") {
            return node2.nodes.map(function(i22) {
              return i22.toProxy();
            });
          } else if (prop === "first" || prop === "last") {
            return node2[prop].toProxy();
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "name" || prop === "params" || prop === "selector") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.index = function index2(child) {
      if (typeof child === "number") return child;
      if (child.proxyOf) child = child.proxyOf;
      return this.proxyOf.nodes.indexOf(child);
    };
    _proto.insertAfter = function insertAfter(exist, add) {
      var existIndex = this.index(exist);
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex]).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex + 1, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex < index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.insertBefore = function insertBefore(exist, add) {
      var existIndex = this.index(exist);
      var type = existIndex === 0 ? "prepend" : false;
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex], type).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex <= index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.normalize = function normalize(nodes, sample) {
      var _this = this;
      if (typeof nodes === "string") {
        nodes = cleanSource$1(parse$4$1(nodes).nodes);
      } else if (typeof nodes === "undefined") {
        nodes = [];
      } else if (Array.isArray(nodes)) {
        nodes = nodes.slice(0);
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var i22 = _step.value;
          if (i22.parent) i22.parent.removeChild(i22, "ignore");
        }
      } else if (nodes.type === "root" && this.type !== "document") {
        nodes = nodes.nodes.slice(0);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var i21 = _step1.value;
          if (i21.parent) i21.parent.removeChild(i21, "ignore");
        }
      } else if (nodes.type) {
        nodes = [
          nodes
        ];
      } else if (nodes.prop) {
        if (typeof nodes.value === "undefined") {
          throw new Error("Value field is missed in node creation");
        } else if (typeof nodes.value !== "string") {
          nodes.value = String(nodes.value);
        }
        nodes = [
          new Declaration$3$1(nodes)
        ];
      } else if (nodes.selector) {
        nodes = [
          new Rule$4$1(nodes)
        ];
      } else if (nodes.name) {
        nodes = [
          new AtRule$4$1(nodes)
        ];
      } else if (nodes.text) {
        nodes = [
          new Comment$3$1(nodes)
        ];
      } else {
        throw new Error("Unknown node type in node creation");
      }
      var processed = nodes.map(function(i23) {
        if (!i23[my$1$1]) Container.rebuild(i23);
        i23 = i23.proxyOf;
        if (i23.parent) i23.parent.removeChild(i23);
        if (i23[isClean$1$1]) markDirtyUp$1(i23);
        if (typeof i23.raws.before === "undefined") {
          if (sample && typeof sample.raws.before !== "undefined") {
            i23.raws.before = sample.raws.before.replace(/\S/g, "");
          }
        }
        i23.parent = _this.proxyOf;
        return i23;
      });
      return processed;
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      children = children.reverse();
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.first, "prepend").reverse();
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.unshift(node2);
        }
        for (var id in this.indexes) {
          this.indexes[id] = this.indexes[id] + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.push = function push(child) {
      child.parent = this;
      this.proxyOf.nodes.push(child);
      return this;
    };
    _proto.removeAll = function removeAll() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        node2.parent = void 0;
      }
      this.proxyOf.nodes = [];
      this.markDirty();
      return this;
    };
    _proto.removeChild = function removeChild(child) {
      child = this.index(child);
      this.proxyOf.nodes[child].parent = void 0;
      this.proxyOf.nodes.splice(child, 1);
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (index2 >= child) {
          this.indexes[id] = index2 - 1;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.replaceValues = function replaceValues(pattern, opts, callback) {
      if (!callback) {
        callback = opts;
        opts = {};
      }
      this.walkDecls(function(decl) {
        if (opts.props && !opts.props.includes(decl.prop)) return;
        if (opts.fast && !decl.value.includes(opts.fast)) return;
        decl.value = decl.value.replace(pattern, callback);
      });
      this.markDirty();
      return this;
    };
    _proto.some = function some(condition) {
      return this.nodes.some(condition);
    };
    _proto.walk = function walk(callback) {
      return this.each(function(child, i22) {
        var result2;
        try {
          result2 = callback(child, i22);
        } catch (e22) {
          throw child.addToError(e22);
        }
        if (result2 !== false && child.walk) {
          result2 = child.walk(callback);
        }
        return result2;
      });
    };
    _proto.walkAtRules = function walkAtRules(name5, callback) {
      if (!callback) {
        callback = name5;
        return this.walk(function(child, i22) {
          if (child.type === "atrule") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(name5, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "atrule" && name5.test(child.name)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "atrule" && child.name === name5) {
          return callback(child, i22);
        }
      });
    };
    _proto.walkComments = function walkComments(callback) {
      return this.walk(function(child, i22) {
        if (child.type === "comment") {
          return callback(child, i22);
        }
      });
    };
    _proto.walkDecls = function walkDecls(prop, callback) {
      if (!callback) {
        callback = prop;
        return this.walk(function(child, i22) {
          if (child.type === "decl") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(prop, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "decl" && prop.test(child.prop)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "decl" && child.prop === prop) {
          return callback(child, i22);
        }
      });
    };
    _proto.walkRules = function walkRules(selector, callback) {
      if (!callback) {
        callback = selector;
        return this.walk(function(child, i22) {
          if (child.type === "rule") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(selector, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "rule" && selector.test(child.selector)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "rule" && child.selector === selector) {
          return callback(child, i22);
        }
      });
    };
    _create_class(Container, [
      {
        key: "first",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[0];
        }
      },
      {
        key: "last",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
        }
      }
    ]);
    return Container;
  })(Node$1$1);
  Container$7$1.registerParse = function(dependant) {
    parse$4$1 = dependant;
  };
  Container$7$1.registerRule = function(dependant) {
    Rule$4$1 = dependant;
  };
  Container$7$1.registerAtRule = function(dependant) {
    AtRule$4$1 = dependant;
  };
  Container$7$1.registerRoot = function(dependant) {
    Root$6$1 = dependant;
  };
  var container$1 = Container$7$1;
  Container$7$1.default = Container$7$1;
  Container$7$1.rebuild = function(node2) {
    if (node2.type === "atrule") {
      Object.setPrototypeOf(node2, AtRule$4$1.prototype);
    } else if (node2.type === "rule") {
      Object.setPrototypeOf(node2, Rule$4$1.prototype);
    } else if (node2.type === "decl") {
      Object.setPrototypeOf(node2, Declaration$3$1.prototype);
    } else if (node2.type === "comment") {
      Object.setPrototypeOf(node2, Comment$3$1.prototype);
    } else if (node2.type === "root") {
      Object.setPrototypeOf(node2, Root$6$1.prototype);
    }
    node2[my$1$1] = true;
    if (node2.nodes) {
      node2.nodes.forEach(function(child) {
        Container$7$1.rebuild(child);
      });
    }
  };
  var Container$6$1 = container$1;
  var LazyResult$4$1;
  var Processor$3$1;
  var Document$3$1 = /* @__PURE__ */ (function(Container$6$12) {
    _inherits(Document23, Container$6$12);
    function Document23(defaults) {
      var _this;
      _this = Container$6$12.call(this, _extends({
        type: "document"
      }, defaults)) || this;
      if (!_this.nodes) {
        _this.nodes = [];
      }
      return _this;
    }
    var _proto = Document23.prototype;
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$4$1(new Processor$3$1(), this, opts);
      return lazy.stringify();
    };
    return Document23;
  })(Container$6$1);
  Document$3$1.registerLazyResult = function(dependant) {
    LazyResult$4$1 = dependant;
  };
  Document$3$1.registerProcessor = function(dependant) {
    Processor$3$1 = dependant;
  };
  var document$1$1 = Document$3$1;
  Document$3$1.default = Document$3$1;
  var printed$1 = {};
  var warnOnce$2$1 = function warnOnce(message) {
    if (printed$1[message]) return;
    printed$1[message] = true;
    if (typeof console !== "undefined" && console.warn) {
      console.warn(message);
    }
  };
  var Warning$2$1 = /* @__PURE__ */ (function() {
    function Warning(text, opts) {
      if (opts === void 0) opts = {};
      this.type = "warning";
      this.text = text;
      if (opts.node && opts.node.source) {
        var range = opts.node.rangeBy(opts);
        this.line = range.start.line;
        this.column = range.start.column;
        this.endLine = range.end.line;
        this.endColumn = range.end.column;
      }
      for (var opt in opts) this[opt] = opts[opt];
    }
    var _proto = Warning.prototype;
    _proto.toString = function toString2() {
      if (this.node) {
        return this.node.error(this.text, {
          index: this.index,
          plugin: this.plugin,
          word: this.word
        }).message;
      }
      if (this.plugin) {
        return this.plugin + ": " + this.text;
      }
      return this.text;
    };
    return Warning;
  })();
  var warning$1 = Warning$2$1;
  Warning$2$1.default = Warning$2$1;
  var Warning$1$1 = warning$1;
  var Result$3$1 = /* @__PURE__ */ (function() {
    function Result(processor2, root2, opts) {
      this.processor = processor2;
      this.messages = [];
      this.root = root2;
      this.opts = opts;
      this.css = void 0;
      this.map = void 0;
    }
    var _proto = Result.prototype;
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.warn = function warn(text, opts) {
      if (opts === void 0) opts = {};
      if (!opts.plugin) {
        if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
          opts.plugin = this.lastPlugin.postcssPlugin;
        }
      }
      var warning2 = new Warning$1$1(text, opts);
      this.messages.push(warning2);
      return warning2;
    };
    _proto.warnings = function warnings() {
      return this.messages.filter(function(i22) {
        return i22.type === "warning";
      });
    };
    _create_class(Result, [
      {
        key: "content",
        get: function get() {
          return this.css;
        }
      }
    ]);
    return Result;
  })();
  var result$1 = Result$3$1;
  Result$3$1.default = Result$3$1;
  var SINGLE_QUOTE$1 = "'".charCodeAt(0);
  var DOUBLE_QUOTE$1 = '"'.charCodeAt(0);
  var BACKSLASH$1 = "\\".charCodeAt(0);
  var SLASH$1 = "/".charCodeAt(0);
  var NEWLINE$1 = "\n".charCodeAt(0);
  var SPACE$1 = " ".charCodeAt(0);
  var FEED$1 = "\f".charCodeAt(0);
  var TAB$1 = "	".charCodeAt(0);
  var CR$1 = "\r".charCodeAt(0);
  var OPEN_SQUARE$1 = "[".charCodeAt(0);
  var CLOSE_SQUARE$1 = "]".charCodeAt(0);
  var OPEN_PARENTHESES$1 = "(".charCodeAt(0);
  var CLOSE_PARENTHESES$1 = ")".charCodeAt(0);
  var OPEN_CURLY$1 = "{".charCodeAt(0);
  var CLOSE_CURLY$1 = "}".charCodeAt(0);
  var SEMICOLON$1 = ";".charCodeAt(0);
  var ASTERISK$1 = "*".charCodeAt(0);
  var COLON$1 = ":".charCodeAt(0);
  var AT$1 = "@".charCodeAt(0);
  var RE_AT_END$1 = /[\t\n\f\r "#'()/;[\\\]{}]/g;
  var RE_WORD_END$1 = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
  var RE_BAD_BRACKET$1 = /.[\r\n"'(/\\]/;
  var RE_HEX_ESCAPE$1 = /[\da-f]/i;
  var tokenize$1 = function tokenizer(input2, options) {
    if (options === void 0) options = {};
    var css = input2.css.valueOf();
    var ignore = options.ignoreErrors;
    var code, next, quote, content, escape;
    var escaped, escapePos, prev, n22, currentToken;
    var length = css.length;
    var pos = 0;
    var buffer = [];
    var returned = [];
    function position() {
      return pos;
    }
    function unclosed(what) {
      throw input2.error("Unclosed " + what, pos);
    }
    function endOfFile() {
      return returned.length === 0 && pos >= length;
    }
    function nextToken(opts) {
      if (returned.length) return returned.pop();
      if (pos >= length) return;
      var ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
      code = css.charCodeAt(pos);
      switch (code) {
        case NEWLINE$1:
        case SPACE$1:
        case TAB$1:
        case CR$1:
        case FEED$1: {
          next = pos;
          do {
            next += 1;
            code = css.charCodeAt(next);
          } while (code === SPACE$1 || code === NEWLINE$1 || code === TAB$1 || code === CR$1 || code === FEED$1);
          currentToken = [
            "space",
            css.slice(pos, next)
          ];
          pos = next - 1;
          break;
        }
        case OPEN_SQUARE$1:
        case CLOSE_SQUARE$1:
        case OPEN_CURLY$1:
        case CLOSE_CURLY$1:
        case COLON$1:
        case SEMICOLON$1:
        case CLOSE_PARENTHESES$1: {
          var controlChar = String.fromCharCode(code);
          currentToken = [
            controlChar,
            controlChar,
            pos
          ];
          break;
        }
        case OPEN_PARENTHESES$1: {
          prev = buffer.length ? buffer.pop()[1] : "";
          n22 = css.charCodeAt(pos + 1);
          if (prev === "url" && n22 !== SINGLE_QUOTE$1 && n22 !== DOUBLE_QUOTE$1 && n22 !== SPACE$1 && n22 !== NEWLINE$1 && n22 !== TAB$1 && n22 !== FEED$1 && n22 !== CR$1) {
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(")", next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos;
                  break;
                } else {
                  unclosed("bracket");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH$1) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = [
              "brackets",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            next = css.indexOf(")", pos + 1);
            content = css.slice(pos, next + 1);
            if (next === -1 || RE_BAD_BRACKET$1.test(content)) {
              currentToken = [
                "(",
                "(",
                pos
              ];
            } else {
              currentToken = [
                "brackets",
                content,
                pos,
                next
              ];
              pos = next;
            }
          }
          break;
        }
        case SINGLE_QUOTE$1:
        case DOUBLE_QUOTE$1: {
          quote = code === SINGLE_QUOTE$1 ? "'" : '"';
          next = pos;
          do {
            escaped = false;
            next = css.indexOf(quote, next + 1);
            if (next === -1) {
              if (ignore || ignoreUnclosed) {
                next = pos + 1;
                break;
              } else {
                unclosed("string");
              }
            }
            escapePos = next;
            while (css.charCodeAt(escapePos - 1) === BACKSLASH$1) {
              escapePos -= 1;
              escaped = !escaped;
            }
          } while (escaped);
          currentToken = [
            "string",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case AT$1: {
          RE_AT_END$1.lastIndex = pos + 1;
          RE_AT_END$1.test(css);
          if (RE_AT_END$1.lastIndex === 0) {
            next = css.length - 1;
          } else {
            next = RE_AT_END$1.lastIndex - 2;
          }
          currentToken = [
            "at-word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case BACKSLASH$1: {
          next = pos;
          escape = true;
          while (css.charCodeAt(next + 1) === BACKSLASH$1) {
            next += 1;
            escape = !escape;
          }
          code = css.charCodeAt(next + 1);
          if (escape && code !== SLASH$1 && code !== SPACE$1 && code !== NEWLINE$1 && code !== TAB$1 && code !== CR$1 && code !== FEED$1) {
            next += 1;
            if (RE_HEX_ESCAPE$1.test(css.charAt(next))) {
              while (RE_HEX_ESCAPE$1.test(css.charAt(next + 1))) {
                next += 1;
              }
              if (css.charCodeAt(next + 1) === SPACE$1) {
                next += 1;
              }
            }
          }
          currentToken = [
            "word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        default: {
          if (code === SLASH$1 && css.charCodeAt(pos + 1) === ASTERISK$1) {
            next = css.indexOf("*/", pos + 2) + 1;
            if (next === 0) {
              if (ignore || ignoreUnclosed) {
                next = css.length;
              } else {
                unclosed("comment");
              }
            }
            currentToken = [
              "comment",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            RE_WORD_END$1.lastIndex = pos + 1;
            RE_WORD_END$1.test(css);
            if (RE_WORD_END$1.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_WORD_END$1.lastIndex - 2;
            }
            currentToken = [
              "word",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            buffer.push(currentToken);
            pos = next;
          }
          break;
        }
      }
      pos++;
      return currentToken;
    }
    function back(token) {
      returned.push(token);
    }
    return {
      back,
      endOfFile,
      nextToken,
      position
    };
  };
  var Container$5$1 = container$1;
  var AtRule$3$1 = /* @__PURE__ */ (function(Container$5$12) {
    _inherits(AtRule, Container$5$12);
    function AtRule(defaults) {
      var _this;
      _this = Container$5$12.call(this, defaults) || this;
      _this.type = "atrule";
      return _this;
    }
    var _proto = AtRule.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5$1_prototype_append;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5$1_prototype_append = Container$5$12.prototype.append).call.apply(_Container$5$1_prototype_append, [].concat([
        this
      ], children));
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5$1_prototype_prepend;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5$1_prototype_prepend = Container$5$12.prototype.prepend).call.apply(_Container$5$1_prototype_prepend, [].concat([
        this
      ], children));
    };
    return AtRule;
  })(Container$5$1);
  var atRule$1 = AtRule$3$1;
  AtRule$3$1.default = AtRule$3$1;
  Container$5$1.registerAtRule(AtRule$3$1);
  var Container$4$1 = container$1;
  var LazyResult$3$1;
  var Processor$2$1;
  var Root$5$1 = /* @__PURE__ */ (function(Container$4$12) {
    _inherits(Root, Container$4$12);
    function Root(defaults) {
      var _this;
      _this = Container$4$12.call(this, defaults) || this;
      _this.type = "root";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    var _proto = Root.prototype;
    _proto.normalize = function normalize(child, sample, type) {
      var nodes = Container$4$12.prototype.normalize.call(this, child);
      if (sample) {
        if (type === "prepend") {
          if (this.nodes.length > 1) {
            sample.raws.before = this.nodes[1].raws.before;
          } else {
            delete sample.raws.before;
          }
        } else if (this.first !== sample) {
          for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            node2.raws.before = sample.raws.before;
          }
        }
      }
      return nodes;
    };
    _proto.removeChild = function removeChild(child, ignore) {
      var index2 = this.index(child);
      if (!ignore && index2 === 0 && this.nodes.length > 1) {
        this.nodes[1].raws.before = this.nodes[index2].raws.before;
      }
      return Container$4$12.prototype.removeChild.call(this, child);
    };
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$3$1(new Processor$2$1(), this, opts);
      return lazy.stringify();
    };
    return Root;
  })(Container$4$1);
  Root$5$1.registerLazyResult = function(dependant) {
    LazyResult$3$1 = dependant;
  };
  Root$5$1.registerProcessor = function(dependant) {
    Processor$2$1 = dependant;
  };
  var root$1 = Root$5$1;
  Root$5$1.default = Root$5$1;
  Container$4$1.registerRoot(Root$5$1);
  var list$2$1 = {
    comma: function comma(string) {
      return list$2$1.split(string, [
        ","
      ], true);
    },
    space: function space(string) {
      var spaces = [
        " ",
        "\n",
        "	"
      ];
      return list$2$1.split(string, spaces);
    },
    split: function split(string, separators, last) {
      var array = [];
      var current = "";
      var split4 = false;
      var func = 0;
      var inQuote = false;
      var prevQuote = "";
      var escape = false;
      for (var _iterator = _create_for_of_iterator_helper_loose(string), _step; !(_step = _iterator()).done; ) {
        var letter = _step.value;
        if (escape) {
          escape = false;
        } else if (letter === "\\") {
          escape = true;
        } else if (inQuote) {
          if (letter === prevQuote) {
            inQuote = false;
          }
        } else if (letter === '"' || letter === "'") {
          inQuote = true;
          prevQuote = letter;
        } else if (letter === "(") {
          func += 1;
        } else if (letter === ")") {
          if (func > 0) func -= 1;
        } else if (func === 0) {
          if (separators.includes(letter)) split4 = true;
        }
        if (split4) {
          if (current !== "") array.push(current.trim());
          current = "";
          split4 = false;
        } else {
          current += letter;
        }
      }
      if (last || current !== "") array.push(current.trim());
      return array;
    }
  };
  var list_1$1 = list$2$1;
  list$2$1.default = list$2$1;
  var Container$3$1 = container$1;
  var list$1$1 = list_1$1;
  var Rule$3$1 = /* @__PURE__ */ (function(Container$3$12) {
    _inherits(Rule, Container$3$12);
    function Rule(defaults) {
      var _this;
      _this = Container$3$12.call(this, defaults) || this;
      _this.type = "rule";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    _create_class(Rule, [
      {
        key: "selectors",
        get: function get() {
          return list$1$1.comma(this.selector);
        },
        set: function set2(values) {
          var match = this.selector ? this.selector.match(/,\s*/) : null;
          var sep2 = match ? match[0] : "," + this.raw("between", "beforeOpen");
          this.selector = values.join(sep2);
        }
      }
    ]);
    return Rule;
  })(Container$3$1);
  var rule$1 = Rule$3$1;
  Rule$3$1.default = Rule$3$1;
  Container$3$1.registerRule(Rule$3$1);
  var Declaration$2$1 = declaration$1;
  var tokenizer2$1 = tokenize$1;
  var Comment$2$1 = comment$1;
  var AtRule$2$1 = atRule$1;
  var Root$4$1 = root$1;
  var Rule$2$1 = rule$1;
  var SAFE_COMMENT_NEIGHBOR$1 = {
    empty: true,
    space: true
  };
  function findLastWithPosition$1(tokens) {
    for (var i22 = tokens.length - 1; i22 >= 0; i22--) {
      var token = tokens[i22];
      var pos = token[3] || token[2];
      if (pos) return pos;
    }
  }
  var Parser$1$1 = /* @__PURE__ */ (function() {
    function Parser(input2) {
      this.input = input2;
      this.root = new Root$4$1();
      this.current = this.root;
      this.spaces = "";
      this.semicolon = false;
      this.createTokenizer();
      this.root.source = {
        input: input2,
        start: {
          column: 1,
          line: 1,
          offset: 0
        }
      };
    }
    var _proto = Parser.prototype;
    _proto.atrule = function atrule(token) {
      var node2 = new AtRule$2$1();
      node2.name = token[1].slice(1);
      if (node2.name === "") {
        this.unnamedAtrule(node2, token);
      }
      this.init(node2, token[2]);
      var type;
      var prev;
      var shift;
      var last = false;
      var open = false;
      var params = [];
      var brackets = [];
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        type = token[0];
        if (type === "(" || type === "[") {
          brackets.push(type === "(" ? ")" : "]");
        } else if (type === "{" && brackets.length > 0) {
          brackets.push("}");
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
        }
        if (brackets.length === 0) {
          if (type === ";") {
            node2.source.end = this.getPosition(token[2]);
            node2.source.end.offset++;
            this.semicolon = true;
            break;
          } else if (type === "{") {
            open = true;
            break;
          } else if (type === "}") {
            if (params.length > 0) {
              shift = params.length - 1;
              prev = params[shift];
              while (prev && prev[0] === "space") {
                prev = params[--shift];
              }
              if (prev) {
                node2.source.end = this.getPosition(prev[3] || prev[2]);
                node2.source.end.offset++;
              }
            }
            this.end(token);
            break;
          } else {
            params.push(token);
          }
        } else {
          params.push(token);
        }
        if (this.tokenizer.endOfFile()) {
          last = true;
          break;
        }
      }
      node2.raws.between = this.spacesAndCommentsFromEnd(params);
      if (params.length) {
        node2.raws.afterName = this.spacesAndCommentsFromStart(params);
        this.raw(node2, "params", params);
        if (last) {
          token = params[params.length - 1];
          node2.source.end = this.getPosition(token[3] || token[2]);
          node2.source.end.offset++;
          this.spaces = node2.raws.between;
          node2.raws.between = "";
        }
      } else {
        node2.raws.afterName = "";
        node2.params = "";
      }
      if (open) {
        node2.nodes = [];
        this.current = node2;
      }
    };
    _proto.checkMissedSemicolon = function checkMissedSemicolon(tokens) {
      var colon = this.colon(tokens);
      if (colon === false) return;
      var founded = 0;
      var token;
      for (var j4 = colon - 1; j4 >= 0; j4--) {
        token = tokens[j4];
        if (token[0] !== "space") {
          founded += 1;
          if (founded === 2) break;
        }
      }
      throw this.input.error("Missed semicolon", token[0] === "word" ? token[3] + 1 : token[2]);
    };
    _proto.colon = function colon(tokens) {
      var brackets = 0;
      var token, type, prev;
      for (var _iterator = _create_for_of_iterator_helper_loose(tokens.entries()), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, i22 = _step_value[0], element = _step_value[1];
        token = element;
        type = token[0];
        if (type === "(") {
          brackets += 1;
        }
        if (type === ")") {
          brackets -= 1;
        }
        if (brackets === 0 && type === ":") {
          if (!prev) {
            this.doubleColon(token);
          } else if (prev[0] === "word" && prev[1] === "progid") {
            continue;
          } else {
            return i22;
          }
        }
        prev = token;
      }
      return false;
    };
    _proto.comment = function comment2(token) {
      var node2 = new Comment$2$1();
      this.init(node2, token[2]);
      node2.source.end = this.getPosition(token[3] || token[2]);
      node2.source.end.offset++;
      var text = token[1].slice(2, -2);
      if (/^\s*$/.test(text)) {
        node2.text = "";
        node2.raws.left = text;
        node2.raws.right = "";
      } else {
        var match = text.match(/^(\s*)([^]*\S)(\s*)$/);
        node2.text = match[2];
        node2.raws.left = match[1];
        node2.raws.right = match[3];
      }
    };
    _proto.createTokenizer = function createTokenizer() {
      this.tokenizer = tokenizer2$1(this.input);
    };
    _proto.decl = function decl(tokens, customProperty) {
      var node2 = new Declaration$2$1();
      this.init(node2, tokens[0][2]);
      var last = tokens[tokens.length - 1];
      if (last[0] === ";") {
        this.semicolon = true;
        tokens.pop();
      }
      node2.source.end = this.getPosition(last[3] || last[2] || findLastWithPosition$1(tokens));
      node2.source.end.offset++;
      while (tokens[0][0] !== "word") {
        if (tokens.length === 1) this.unknownWord(tokens);
        node2.raws.before += tokens.shift()[1];
      }
      node2.source.start = this.getPosition(tokens[0][2]);
      node2.prop = "";
      while (tokens.length) {
        var type = tokens[0][0];
        if (type === ":" || type === "space" || type === "comment") {
          break;
        }
        node2.prop += tokens.shift()[1];
      }
      node2.raws.between = "";
      var token;
      while (tokens.length) {
        token = tokens.shift();
        if (token[0] === ":") {
          node2.raws.between += token[1];
          break;
        } else {
          if (token[0] === "word" && /\w/.test(token[1])) {
            this.unknownWord([
              token
            ]);
          }
          node2.raws.between += token[1];
        }
      }
      if (node2.prop[0] === "_" || node2.prop[0] === "*") {
        node2.raws.before += node2.prop[0];
        node2.prop = node2.prop.slice(1);
      }
      var firstSpaces = [];
      var next;
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        firstSpaces.push(tokens.shift());
      }
      this.precheckMissedSemicolon(tokens);
      for (var i22 = tokens.length - 1; i22 >= 0; i22--) {
        token = tokens[i22];
        if (token[1].toLowerCase() === "!important") {
          node2.important = true;
          var string = this.stringFrom(tokens, i22);
          string = this.spacesFromEnd(tokens) + string;
          if (string !== " !important") node2.raws.important = string;
          break;
        } else if (token[1].toLowerCase() === "important") {
          var cache = tokens.slice(0);
          var str = "";
          for (var j4 = i22; j4 > 0; j4--) {
            var type1 = cache[j4][0];
            if (str.trim().indexOf("!") === 0 && type1 !== "space") {
              break;
            }
            str = cache.pop()[1] + str;
          }
          if (str.trim().indexOf("!") === 0) {
            node2.important = true;
            node2.raws.important = str;
            tokens = cache;
          }
        }
        if (token[0] !== "space" && token[0] !== "comment") {
          break;
        }
      }
      var hasWord = tokens.some(function(i23) {
        return i23[0] !== "space" && i23[0] !== "comment";
      });
      if (hasWord) {
        node2.raws.between += firstSpaces.map(function(i23) {
          return i23[1];
        }).join("");
        firstSpaces = [];
      }
      this.raw(node2, "value", firstSpaces.concat(tokens), customProperty);
      if (node2.value.includes(":") && !customProperty) {
        this.checkMissedSemicolon(tokens);
      }
    };
    _proto.doubleColon = function doubleColon(token) {
      throw this.input.error("Double colon", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    _proto.emptyRule = function emptyRule(token) {
      var node2 = new Rule$2$1();
      this.init(node2, token[2]);
      node2.selector = "";
      node2.raws.between = "";
      this.current = node2;
    };
    _proto.end = function end(token) {
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.semicolon = false;
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.spaces = "";
      if (this.current.parent) {
        this.current.source.end = this.getPosition(token[2]);
        this.current.source.end.offset++;
        this.current = this.current.parent;
      } else {
        this.unexpectedClose(token);
      }
    };
    _proto.endFile = function endFile() {
      if (this.current.parent) this.unclosedBlock();
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.root.source.end = this.getPosition(this.tokenizer.position());
    };
    _proto.freeSemicolon = function freeSemicolon(token) {
      this.spaces += token[1];
      if (this.current.nodes) {
        var prev = this.current.nodes[this.current.nodes.length - 1];
        if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
          prev.raws.ownSemicolon = this.spaces;
          this.spaces = "";
        }
      }
    };
    _proto.getPosition = function getPosition(offset) {
      var pos = this.input.fromOffset(offset);
      return {
        column: pos.col,
        line: pos.line,
        offset
      };
    };
    _proto.init = function init(node2, offset) {
      this.current.push(node2);
      node2.source = {
        input: this.input,
        start: this.getPosition(offset)
      };
      node2.raws.before = this.spaces;
      this.spaces = "";
      if (node2.type !== "comment") this.semicolon = false;
    };
    _proto.other = function other(start) {
      var end = false;
      var type = null;
      var colon = false;
      var bracket = null;
      var brackets = [];
      var customProperty = start[1].startsWith("--");
      var tokens = [];
      var token = start;
      while (token) {
        type = token[0];
        tokens.push(token);
        if (type === "(" || type === "[") {
          if (!bracket) bracket = token;
          brackets.push(type === "(" ? ")" : "]");
        } else if (customProperty && colon && type === "{") {
          if (!bracket) bracket = token;
          brackets.push("}");
        } else if (brackets.length === 0) {
          if (type === ";") {
            if (colon) {
              this.decl(tokens, customProperty);
              return;
            } else {
              break;
            }
          } else if (type === "{") {
            this.rule(tokens);
            return;
          } else if (type === "}") {
            this.tokenizer.back(tokens.pop());
            end = true;
            break;
          } else if (type === ":") {
            colon = true;
          }
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
          if (brackets.length === 0) bracket = null;
        }
        token = this.tokenizer.nextToken();
      }
      if (this.tokenizer.endOfFile()) end = true;
      if (brackets.length > 0) this.unclosedBracket(bracket);
      if (end && colon) {
        if (!customProperty) {
          while (tokens.length) {
            token = tokens[tokens.length - 1][0];
            if (token !== "space" && token !== "comment") break;
            this.tokenizer.back(tokens.pop());
          }
        }
        this.decl(tokens, customProperty);
      } else {
        this.unknownWord(tokens);
      }
    };
    _proto.parse = function parse3() {
      var token;
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        switch (token[0]) {
          case "space":
            this.spaces += token[1];
            break;
          case ";":
            this.freeSemicolon(token);
            break;
          case "}":
            this.end(token);
            break;
          case "comment":
            this.comment(token);
            break;
          case "at-word":
            this.atrule(token);
            break;
          case "{":
            this.emptyRule(token);
            break;
          default:
            this.other(token);
            break;
        }
      }
      this.endFile();
    };
    _proto.precheckMissedSemicolon = function precheckMissedSemicolon() {
    };
    _proto.raw = function raw(node2, prop, tokens, customProperty) {
      var token, type;
      var length = tokens.length;
      var value = "";
      var clean = true;
      var next, prev;
      for (var i22 = 0; i22 < length; i22 += 1) {
        token = tokens[i22];
        type = token[0];
        if (type === "space" && i22 === length - 1 && !customProperty) {
          clean = false;
        } else if (type === "comment") {
          prev = tokens[i22 - 1] ? tokens[i22 - 1][0] : "empty";
          next = tokens[i22 + 1] ? tokens[i22 + 1][0] : "empty";
          if (!SAFE_COMMENT_NEIGHBOR$1[prev] && !SAFE_COMMENT_NEIGHBOR$1[next]) {
            if (value.slice(-1) === ",") {
              clean = false;
            } else {
              value += token[1];
            }
          } else {
            clean = false;
          }
        } else {
          value += token[1];
        }
      }
      if (!clean) {
        var raw2 = tokens.reduce(function(all, i23) {
          return all + i23[1];
        }, "");
        node2.raws[prop] = {
          raw: raw2,
          value
        };
      }
      node2[prop] = value;
    };
    _proto.rule = function rule2(tokens) {
      tokens.pop();
      var node2 = new Rule$2$1();
      this.init(node2, tokens[0][2]);
      node2.raws.between = this.spacesAndCommentsFromEnd(tokens);
      this.raw(node2, "selector", tokens);
      this.current = node2;
    };
    _proto.spacesAndCommentsFromEnd = function spacesAndCommentsFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space" && lastTokenType !== "comment") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.spacesAndCommentsFromStart = function spacesAndCommentsFromStart(tokens) {
      var next;
      var spaces = "";
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        spaces += tokens.shift()[1];
      }
      return spaces;
    };
    _proto.spacesFromEnd = function spacesFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.stringFrom = function stringFrom(tokens, from) {
      var result2 = "";
      for (var i22 = from; i22 < tokens.length; i22++) {
        result2 += tokens[i22][1];
      }
      tokens.splice(from, tokens.length - from);
      return result2;
    };
    _proto.unclosedBlock = function unclosedBlock() {
      var pos = this.current.source.start;
      throw this.input.error("Unclosed block", pos.line, pos.column);
    };
    _proto.unclosedBracket = function unclosedBracket(bracket) {
      throw this.input.error("Unclosed bracket", {
        offset: bracket[2]
      }, {
        offset: bracket[2] + 1
      });
    };
    _proto.unexpectedClose = function unexpectedClose(token) {
      throw this.input.error("Unexpected }", {
        offset: token[2]
      }, {
        offset: token[2] + 1
      });
    };
    _proto.unknownWord = function unknownWord(tokens) {
      throw this.input.error("Unknown word", {
        offset: tokens[0][2]
      }, {
        offset: tokens[0][2] + tokens[0][1].length
      });
    };
    _proto.unnamedAtrule = function unnamedAtrule(node2, token) {
      throw this.input.error("At-rule without name", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    return Parser;
  })();
  var parser$1 = Parser$1$1;
  var Container$2$1 = container$1;
  var Parser2$1 = parser$1;
  var Input$2$1 = input$1;
  function parse$3$1(css, opts) {
    var input2 = new Input$2$1(css, opts);
    var parser2 = new Parser2$1(input2);
    try {
      parser2.parse();
    } catch (e22) {
      if (true) {
        if (e22.name === "CssSyntaxError" && opts && opts.from) {
          if (/\.scss$/i.test(opts.from)) {
            e22.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
          } else if (/\.sass/i.test(opts.from)) {
            e22.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
          } else if (/\.less$/i.test(opts.from)) {
            e22.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
          }
        }
      }
      throw e22;
    }
    return parser2.root;
  }
  var parse_1$1 = parse$3$1;
  parse$3$1.default = parse$3$1;
  Container$2$1.registerParse(parse$3$1);
  var isClean$3 = symbols$1.isClean;
  var my$3 = symbols$1.my;
  var MapGenerator$1$1 = mapGenerator$1;
  var stringify$2$1 = stringify_1$1;
  var Container$1$1 = container$1;
  var Document$2$1 = document$1$1;
  var warnOnce$1$1 = warnOnce$2$1;
  var Result$2$1 = result$1;
  var parse$2$1 = parse_1$1;
  var Root$3$1 = root$1;
  var TYPE_TO_CLASS_NAME$1 = {
    atrule: "AtRule",
    comment: "Comment",
    decl: "Declaration",
    document: "Document",
    root: "Root",
    rule: "Rule"
  };
  var PLUGIN_PROPS$1 = {
    AtRule: true,
    AtRuleExit: true,
    Comment: true,
    CommentExit: true,
    Declaration: true,
    DeclarationExit: true,
    Document: true,
    DocumentExit: true,
    Once: true,
    OnceExit: true,
    postcssPlugin: true,
    prepare: true,
    Root: true,
    RootExit: true,
    Rule: true,
    RuleExit: true
  };
  var NOT_VISITORS$1 = {
    Once: true,
    postcssPlugin: true,
    prepare: true
  };
  var CHILDREN$1 = 0;
  function isPromise$1(obj) {
    return (typeof obj === "undefined" ? "undefined" : _type_of(obj)) === "object" && typeof obj.then === "function";
  }
  function getEvents$1(node2) {
    var key = false;
    var type = TYPE_TO_CLASS_NAME$1[node2.type];
    if (node2.type === "decl") {
      key = node2.prop.toLowerCase();
    } else if (node2.type === "atrule") {
      key = node2.name.toLowerCase();
    }
    if (key && node2.append) {
      return [
        type,
        type + "-" + key,
        CHILDREN$1,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (key) {
      return [
        type,
        type + "-" + key,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (node2.append) {
      return [
        type,
        CHILDREN$1,
        type + "Exit"
      ];
    } else {
      return [
        type,
        type + "Exit"
      ];
    }
  }
  function toStack$1(node2) {
    var events;
    if (node2.type === "document") {
      events = [
        "Document",
        CHILDREN$1,
        "DocumentExit"
      ];
    } else if (node2.type === "root") {
      events = [
        "Root",
        CHILDREN$1,
        "RootExit"
      ];
    } else {
      events = getEvents$1(node2);
    }
    return {
      eventIndex: 0,
      events,
      iterator: 0,
      node: node2,
      visitorIndex: 0,
      visitors: []
    };
  }
  function cleanMarks$1(node2) {
    node2[isClean$3] = false;
    if (node2.nodes) node2.nodes.forEach(function(i22) {
      return cleanMarks$1(i22);
    });
    return node2;
  }
  var postcss$2$1 = {};
  var LazyResult$2$1 = /* @__PURE__ */ (function() {
    function LazyResult(processor2, css, opts) {
      var _this = this;
      this.stringified = false;
      this.processed = false;
      var root2;
      if ((typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && css !== null && (css.type === "root" || css.type === "document")) {
        root2 = cleanMarks$1(css);
      } else if (_instanceof(css, LazyResult) || _instanceof(css, Result$2$1)) {
        root2 = cleanMarks$1(css.root);
        if (css.map) {
          if (typeof opts.map === "undefined") opts.map = {};
          if (!opts.map.inline) opts.map.inline = false;
          opts.map.prev = css.map;
        }
      } else {
        var parser2 = parse$2$1;
        if (opts.syntax) parser2 = opts.syntax.parse;
        if (opts.parser) parser2 = opts.parser;
        if (parser2.parse) parser2 = parser2.parse;
        try {
          root2 = parser2(css, opts);
        } catch (error) {
          this.processed = true;
          this.error = error;
        }
        if (root2 && !root2[my$3]) {
          Container$1$1.rebuild(root2);
        }
      }
      this.result = new Result$2$1(processor2, root2, opts);
      this.helpers = _extends({}, postcss$2$1, {
        postcss: postcss$2$1,
        result: this.result
      });
      this.plugins = this.processor.plugins.map(function(plugin22) {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.prepare) {
          return _extends({}, plugin22, plugin22.prepare(_this.result));
        } else {
          return plugin22;
        }
      });
    }
    var _proto = LazyResult.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      if (this.processed) return Promise.resolve(this.result);
      if (!this.processing) {
        this.processing = this.runAsync();
      }
      return this.processing;
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.getAsyncError = function getAsyncError() {
      throw new Error("Use process(css).then(cb) to work with async plugins");
    };
    _proto.handleError = function handleError2(error, node2) {
      var plugin22 = this.result.lastPlugin;
      try {
        if (node2) node2.addToError(error);
        this.error = error;
        if (error.name === "CssSyntaxError" && !error.plugin) {
          error.plugin = plugin22.postcssPlugin;
          error.setMessage();
        } else if (plugin22.postcssVersion) {
          if (true) {
            var pluginName = plugin22.postcssPlugin;
            var pluginVer = plugin22.postcssVersion;
            var runtimeVer = this.result.processor.version;
            var a22 = pluginVer.split(".");
            var b2 = runtimeVer.split(".");
            if (a22[0] !== b2[0] || parseInt(a22[1]) > parseInt(b2[1])) {
              console.error("Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below.");
            }
          }
        }
      } catch (err) {
        if (console && console.error) console.error(err);
      }
      return error;
    };
    _proto.prepareVisitors = function prepareVisitors() {
      var _this = this;
      this.listeners = {};
      var add = function(plugin222, type, cb) {
        if (!_this.listeners[type]) _this.listeners[type] = [];
        _this.listeners[type].push([
          plugin222,
          cb
        ]);
      };
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object") {
          for (var event in plugin22) {
            if (!PLUGIN_PROPS$1[event] && /^[A-Z]/.test(event)) {
              throw new Error("Unknown event " + event + " in " + plugin22.postcssPlugin + ". Try to update PostCSS (" + this.processor.version + " now).");
            }
            if (!NOT_VISITORS$1[event]) {
              if (_type_of(plugin22[event]) === "object") {
                for (var filter in plugin22[event]) {
                  if (filter === "*") {
                    add(plugin22, event, plugin22[event][filter]);
                  } else {
                    add(plugin22, event + "-" + filter.toLowerCase(), plugin22[event][filter]);
                  }
                }
              } else if (typeof plugin22[event] === "function") {
                add(plugin22, event, plugin22[event]);
              }
            }
          }
        }
      }
      this.hasListener = Object.keys(this.listeners).length > 0;
    };
    _proto.runAsync = function runAsync() {
      var _this = this;
      return _async_to_generator(function() {
        var i22, plugin22, promise, error, root2, stack, promise1, e22, node2, _loop, _iterator, _step;
        return _ts_generator(this, function(_state) {
          switch (_state.label) {
            case 0:
              _this.plugin = 0;
              i22 = 0;
              _state.label = 1;
            case 1:
              if (!(i22 < _this.plugins.length)) return [
                3,
                6
              ];
              plugin22 = _this.plugins[i22];
              promise = _this.runOnRoot(plugin22);
              if (!isPromise$1(promise)) return [
                3,
                5
              ];
              _state.label = 2;
            case 2:
              _state.trys.push([
                2,
                4,
                ,
                5
              ]);
              return [
                4,
                promise
              ];
            case 3:
              _state.sent();
              return [
                3,
                5
              ];
            case 4:
              error = _state.sent();
              throw _this.handleError(error);
            case 5:
              i22++;
              return [
                3,
                1
              ];
            case 6:
              _this.prepareVisitors();
              if (!_this.hasListener) return [
                3,
                18
              ];
              root2 = _this.result.root;
              _state.label = 7;
            case 7:
              if (!!root2[isClean$3]) return [
                3,
                14
              ];
              root2[isClean$3] = true;
              stack = [
                toStack$1(root2)
              ];
              _state.label = 8;
            case 8:
              if (!(stack.length > 0)) return [
                3,
                13
              ];
              promise1 = _this.visitTick(stack);
              if (!isPromise$1(promise1)) return [
                3,
                12
              ];
              _state.label = 9;
            case 9:
              _state.trys.push([
                9,
                11,
                ,
                12
              ]);
              return [
                4,
                promise1
              ];
            case 10:
              _state.sent();
              return [
                3,
                12
              ];
            case 11:
              e22 = _state.sent();
              node2 = stack[stack.length - 1].node;
              throw _this.handleError(e22, node2);
            case 12:
              return [
                3,
                8
              ];
            case 13:
              return [
                3,
                7
              ];
            case 14:
              if (!_this.listeners.OnceExit) return [
                3,
                18
              ];
              _loop = function() {
                var _step_value, plugin222, visitor, roots, e23;
                return _ts_generator(this, function(_state2) {
                  switch (_state2.label) {
                    case 0:
                      _step_value = _step.value, plugin222 = _step_value[0], visitor = _step_value[1];
                      _this.result.lastPlugin = plugin222;
                      _state2.label = 1;
                    case 1:
                      _state2.trys.push([
                        1,
                        6,
                        ,
                        7
                      ]);
                      if (!(root2.type === "document")) return [
                        3,
                        3
                      ];
                      roots = root2.nodes.map(function(subRoot) {
                        return visitor(subRoot, _this.helpers);
                      });
                      return [
                        4,
                        Promise.all(roots)
                      ];
                    case 2:
                      _state2.sent();
                      return [
                        3,
                        5
                      ];
                    case 3:
                      return [
                        4,
                        visitor(root2, _this.helpers)
                      ];
                    case 4:
                      _state2.sent();
                      _state2.label = 5;
                    case 5:
                      return [
                        3,
                        7
                      ];
                    case 6:
                      e23 = _state2.sent();
                      throw _this.handleError(e23);
                    case 7:
                      return [
                        2
                      ];
                  }
                });
              };
              _iterator = _create_for_of_iterator_helper_loose(_this.listeners.OnceExit);
              _state.label = 15;
            case 15:
              if (!!(_step = _iterator()).done) return [
                3,
                18
              ];
              return [
                5,
                _ts_values(_loop())
              ];
            case 16:
              _state.sent();
              _state.label = 17;
            case 17:
              return [
                3,
                15
              ];
            case 18:
              _this.processed = true;
              return [
                2,
                _this.stringify()
              ];
          }
        });
      })();
    };
    _proto.runOnRoot = function runOnRoot(plugin22) {
      var _this = this;
      this.result.lastPlugin = plugin22;
      try {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.Once) {
          if (this.result.root.type === "document") {
            var roots = this.result.root.nodes.map(function(root2) {
              return plugin22.Once(root2, _this.helpers);
            });
            if (isPromise$1(roots[0])) {
              return Promise.all(roots);
            }
            return roots;
          }
          return plugin22.Once(this.result.root, this.helpers);
        } else if (typeof plugin22 === "function") {
          return plugin22(this.result.root, this.result);
        }
      } catch (error) {
        throw this.handleError(error);
      }
    };
    _proto.stringify = function stringify2() {
      if (this.error) throw this.error;
      if (this.stringified) return this.result;
      this.stringified = true;
      this.sync();
      var opts = this.result.opts;
      var str = stringify$2$1;
      if (opts.syntax) str = opts.syntax.stringify;
      if (opts.stringifier) str = opts.stringifier;
      if (str.stringify) str = str.stringify;
      var map = new MapGenerator$1$1(str, this.result.root, this.result.opts);
      var data = map.generate();
      this.result.css = data[0];
      this.result.map = data[1];
      return this.result;
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      if (this.processed) return this.result;
      this.processed = true;
      if (this.processing) {
        throw this.getAsyncError();
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        var promise = this.runOnRoot(plugin22);
        if (isPromise$1(promise)) {
          throw this.getAsyncError();
        }
      }
      this.prepareVisitors();
      if (this.hasListener) {
        var root2 = this.result.root;
        while (!root2[isClean$3]) {
          root2[isClean$3] = true;
          this.walkSync(root2);
        }
        if (this.listeners.OnceExit) {
          if (root2.type === "document") {
            for (var _iterator1 = _create_for_of_iterator_helper_loose(root2.nodes), _step1; !(_step1 = _iterator1()).done; ) {
              var subRoot = _step1.value;
              this.visitSync(this.listeners.OnceExit, subRoot);
            }
          } else {
            this.visitSync(this.listeners.OnceExit, root2);
          }
        }
      }
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this.opts)) {
          warnOnce$1$1("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.visitSync = function visitSync(visitors, node2) {
      for (var _iterator = _create_for_of_iterator_helper_loose(visitors), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, plugin22 = _step_value[0], visitor = _step_value[1];
        this.result.lastPlugin = plugin22;
        var promise = void 0;
        try {
          promise = visitor(node2, this.helpers);
        } catch (e22) {
          throw this.handleError(e22, node2.proxyOf);
        }
        if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
          return true;
        }
        if (isPromise$1(promise)) {
          throw this.getAsyncError();
        }
      }
    };
    _proto.visitTick = function visitTick(stack) {
      var visit2 = stack[stack.length - 1];
      var node2 = visit2.node, visitors = visit2.visitors;
      if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
        stack.pop();
        return;
      }
      if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
        var _visitors_visit2_visitorIndex = visitors[visit2.visitorIndex], plugin22 = _visitors_visit2_visitorIndex[0], visitor = _visitors_visit2_visitorIndex[1];
        visit2.visitorIndex += 1;
        if (visit2.visitorIndex === visitors.length) {
          visit2.visitors = [];
          visit2.visitorIndex = 0;
        }
        this.result.lastPlugin = plugin22;
        try {
          return visitor(node2.toProxy(), this.helpers);
        } catch (e22) {
          throw this.handleError(e22, node2);
        }
      }
      if (visit2.iterator !== 0) {
        var iterator = visit2.iterator;
        var child;
        while (child = node2.nodes[node2.indexes[iterator]]) {
          node2.indexes[iterator] += 1;
          if (!child[isClean$3]) {
            child[isClean$3] = true;
            stack.push(toStack$1(child));
            return;
          }
        }
        visit2.iterator = 0;
        delete node2.indexes[iterator];
      }
      var events = visit2.events;
      while (visit2.eventIndex < events.length) {
        var event = events[visit2.eventIndex];
        visit2.eventIndex += 1;
        if (event === CHILDREN$1) {
          if (node2.nodes && node2.nodes.length) {
            node2[isClean$3] = true;
            visit2.iterator = node2.getIterator();
          }
          return;
        } else if (this.listeners[event]) {
          visit2.visitors = this.listeners[event];
          return;
        }
      }
      stack.pop();
    };
    _proto.walkSync = function walkSync(node2) {
      var _this = this;
      node2[isClean$3] = true;
      var events = getEvents$1(node2);
      for (var _iterator = _create_for_of_iterator_helper_loose(events), _step; !(_step = _iterator()).done; ) {
        var event = _step.value;
        if (event === CHILDREN$1) {
          if (node2.nodes) {
            node2.each(function(child) {
              if (!child[isClean$3]) _this.walkSync(child);
            });
          }
        } else {
          var visitors = this.listeners[event];
          if (visitors) {
            if (this.visitSync(visitors, node2.toProxy())) return;
          }
        }
      }
    };
    _proto.warnings = function warnings() {
      return this.sync().warnings();
    };
    _create_class(LazyResult, [
      {
        key: "content",
        get: function get() {
          return this.stringify().content;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.stringify().css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.stringify().map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return this.sync().messages;
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          return this.sync().root;
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "LazyResult";
        }
      }
    ]);
    return LazyResult;
  })();
  LazyResult$2$1.registerPostcss = function(dependant) {
    postcss$2$1 = dependant;
  };
  var lazyResult$1 = LazyResult$2$1;
  LazyResult$2$1.default = LazyResult$2$1;
  Root$3$1.registerLazyResult(LazyResult$2$1);
  Document$2$1.registerLazyResult(LazyResult$2$1);
  var MapGenerator2$1 = mapGenerator$1;
  var stringify$1$1 = stringify_1$1;
  var warnOnce2$1 = warnOnce$2$1;
  var parse$1$1 = parse_1$1;
  var Result$1$1 = result$1;
  var NoWorkResult$1$1 = /* @__PURE__ */ (function() {
    function NoWorkResult(processor2, css, opts) {
      css = css.toString();
      this.stringified = false;
      this._processor = processor2;
      this._css = css;
      this._opts = opts;
      this._map = void 0;
      var root2;
      var str = stringify$1$1;
      this.result = new Result$1$1(this._processor, root2, this._opts);
      this.result.css = css;
      var self2 = this;
      Object.defineProperty(this.result, "root", {
        get: function get() {
          return self2.root;
        }
      });
      var map = new MapGenerator2$1(str, root2, this._opts, css);
      if (map.isMap()) {
        var _map_generate = map.generate(), generatedCSS = _map_generate[0], generatedMap = _map_generate[1];
        if (generatedCSS) {
          this.result.css = generatedCSS;
        }
        if (generatedMap) {
          this.result.map = generatedMap;
        }
      } else {
        map.clearAnnotation();
        this.result.css = map.css;
      }
    }
    var _proto = NoWorkResult.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      return Promise.resolve(this.result);
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this._opts)) {
          warnOnce2$1("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this._css;
    };
    _proto.warnings = function warnings() {
      return [];
    };
    _create_class(NoWorkResult, [
      {
        key: "content",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.result.map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return [];
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          if (this._root) {
            return this._root;
          }
          var root2;
          var parser2 = parse$1$1;
          try {
            root2 = parser2(this._css, this._opts);
          } catch (error) {
            this.error = error;
          }
          if (this.error) {
            throw this.error;
          } else {
            this._root = root2;
            return root2;
          }
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "NoWorkResult";
        }
      }
    ]);
    return NoWorkResult;
  })();
  var noWorkResult$1 = NoWorkResult$1$1;
  NoWorkResult$1$1.default = NoWorkResult$1$1;
  var NoWorkResult2$1 = noWorkResult$1;
  var LazyResult$1$1 = lazyResult$1;
  var Document$1$1 = document$1$1;
  var Root$2$1 = root$1;
  var Processor$1$1 = /* @__PURE__ */ (function() {
    function Processor(plugins) {
      if (plugins === void 0) plugins = [];
      this.version = "8.4.38";
      this.plugins = this.normalize(plugins);
    }
    var _proto = Processor.prototype;
    _proto.normalize = function normalize(plugins) {
      var normalized = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(plugins), _step; !(_step = _iterator()).done; ) {
        var i22 = _step.value;
        if (i22.postcss === true) {
          i22 = i22();
        } else if (i22.postcss) {
          i22 = i22.postcss;
        }
        if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && Array.isArray(i22.plugins)) {
          normalized = normalized.concat(i22.plugins);
        } else if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && i22.postcssPlugin) {
          normalized.push(i22);
        } else if (typeof i22 === "function") {
          normalized.push(i22);
        } else if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && (i22.parse || i22.stringify)) {
          if (true) {
            throw new Error("PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation.");
          }
        } else {
          throw new Error(i22 + " is not a PostCSS plugin");
        }
      }
      return normalized;
    };
    _proto.process = function process1(css, opts) {
      if (opts === void 0) opts = {};
      if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
        return new NoWorkResult2$1(this, css, opts);
      } else {
        return new LazyResult$1$1(this, css, opts);
      }
    };
    _proto.use = function use(plugin22) {
      this.plugins = this.plugins.concat(this.normalize([
        plugin22
      ]));
      return this;
    };
    return Processor;
  })();
  var processor$1 = Processor$1$1;
  Processor$1$1.default = Processor$1$1;
  Root$2$1.registerProcessor(Processor$1$1);
  Document$1$1.registerProcessor(Processor$1$1);
  var Declaration$1$1 = declaration$1;
  var PreviousMap2$1 = previousMap$1;
  var Comment$1$1 = comment$1;
  var AtRule$1$1 = atRule$1;
  var Input$1$1 = input$1;
  var Root$1$1 = root$1;
  var Rule$1$1 = rule$1;
  function fromJSON$1$1(json, inputs) {
    if (Array.isArray(json)) return json.map(function(n22) {
      return fromJSON$1$1(n22);
    });
    var ownInputs = json.inputs, defaults = _object_without_properties_loose(json, [
      "inputs"
    ]);
    if (ownInputs) {
      inputs = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(ownInputs), _step; !(_step = _iterator()).done; ) {
        var input2 = _step.value;
        var inputHydrated = _extends({}, input2, {
          __proto__: Input$1$1.prototype
        });
        if (inputHydrated.map) {
          inputHydrated.map = _extends({}, inputHydrated.map, {
            __proto__: PreviousMap2$1.prototype
          });
        }
        inputs.push(inputHydrated);
      }
    }
    if (defaults.nodes) {
      defaults.nodes = json.nodes.map(function(n22) {
        return fromJSON$1$1(n22, inputs);
      });
    }
    if (defaults.source) {
      var _defaults_source = defaults.source, inputId = _defaults_source.inputId, source = _object_without_properties_loose(_defaults_source, [
        "inputId"
      ]);
      defaults.source = source;
      if (inputId != null) {
        defaults.source.input = inputs[inputId];
      }
    }
    if (defaults.type === "root") {
      return new Root$1$1(defaults);
    } else if (defaults.type === "decl") {
      return new Declaration$1$1(defaults);
    } else if (defaults.type === "rule") {
      return new Rule$1$1(defaults);
    } else if (defaults.type === "comment") {
      return new Comment$1$1(defaults);
    } else if (defaults.type === "atrule") {
      return new AtRule$1$1(defaults);
    } else {
      throw new Error("Unknown node type: " + json.type);
    }
  }
  var fromJSON_1$1 = fromJSON$1$1;
  fromJSON$1$1.default = fromJSON$1$1;
  var CssSyntaxError2$1 = cssSyntaxError$1;
  var Declaration2$1 = declaration$1;
  var LazyResult2$1 = lazyResult$1;
  var Container2$1 = container$1;
  var Processor2$1 = processor$1;
  var stringify$5$1 = stringify_1$1;
  var fromJSON$2 = fromJSON_1$1;
  var Document22 = document$1$1;
  var Warning2$1 = warning$1;
  var Comment2$1 = comment$1;
  var AtRule2$1 = atRule$1;
  var Result2$1 = result$1;
  var Input2$1 = input$1;
  var parse$5 = parse_1$1;
  var list$3 = list_1$1;
  var Rule2$1 = rule$1;
  var Root2$1 = root$1;
  var Node2$1 = node$1;
  function postcss$3() {
    for (var _len = arguments.length, plugins = new Array(_len), _key = 0; _key < _len; _key++) {
      plugins[_key] = arguments[_key];
    }
    if (plugins.length === 1 && Array.isArray(plugins[0])) {
      plugins = plugins[0];
    }
    return new Processor2$1(plugins);
  }
  postcss$3.plugin = function plugin(name5, initializer) {
    var warningPrinted = false;
    function creator() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      if (console && console.warn && !warningPrinted) {
        warningPrinted = true;
        console.warn(name5 + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration");
        if (process.env.LANG && process.env.LANG.startsWith("cn")) {
          console.warn(name5 + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226");
        }
      }
      var transformer = initializer.apply(void 0, [].concat(args));
      transformer.postcssPlugin = name5;
      transformer.postcssVersion = new Processor2$1().version;
      return transformer;
    }
    var cache;
    Object.defineProperty(creator, "postcss", {
      get: function get() {
        if (!cache) cache = creator();
        return cache;
      }
    });
    creator.process = function(css, processOpts, pluginOpts) {
      return postcss$3([
        creator(pluginOpts)
      ]).process(css, processOpts);
    };
    return creator;
  };
  postcss$3.stringify = stringify$5$1;
  postcss$3.parse = parse$5;
  postcss$3.fromJSON = fromJSON$2;
  postcss$3.list = list$3;
  postcss$3.comment = function(defaults) {
    return new Comment2$1(defaults);
  };
  postcss$3.atRule = function(defaults) {
    return new AtRule2$1(defaults);
  };
  postcss$3.decl = function(defaults) {
    return new Declaration2$1(defaults);
  };
  postcss$3.rule = function(defaults) {
    return new Rule2$1(defaults);
  };
  postcss$3.root = function(defaults) {
    return new Root2$1(defaults);
  };
  postcss$3.document = function(defaults) {
    return new Document22(defaults);
  };
  postcss$3.CssSyntaxError = CssSyntaxError2$1;
  postcss$3.Declaration = Declaration2$1;
  postcss$3.Container = Container2$1;
  postcss$3.Processor = Processor2$1;
  postcss$3.Document = Document22;
  postcss$3.Comment = Comment2$1;
  postcss$3.Warning = Warning2$1;
  postcss$3.AtRule = AtRule2$1;
  postcss$3.Result = Result2$1;
  postcss$3.Input = Input2$1;
  postcss$3.Rule = Rule2$1;
  postcss$3.Root = Root2$1;
  postcss$3.Node = Node2$1;
  LazyResult2$1.registerPostcss(postcss$3);
  var postcss_1$1 = postcss$3;
  postcss$3.default = postcss$3;
  var postcss$1$1 = /* @__PURE__ */ getDefaultExportFromCjs$1(postcss_1$1);
  postcss$1$1.stringify;
  postcss$1$1.fromJSON;
  postcss$1$1.plugin;
  postcss$1$1.parse;
  postcss$1$1.list;
  postcss$1$1.document;
  postcss$1$1.comment;
  postcss$1$1.atRule;
  postcss$1$1.rule;
  postcss$1$1.decl;
  postcss$1$1.root;
  postcss$1$1.CssSyntaxError;
  postcss$1$1.Declaration;
  postcss$1$1.Container;
  postcss$1$1.Processor;
  postcss$1$1.Document;
  postcss$1$1.Comment;
  postcss$1$1.Warning;
  postcss$1$1.AtRule;
  postcss$1$1.Result;
  postcss$1$1.Input;
  postcss$1$1.Rule;
  postcss$1$1.Root;
  postcss$1$1.Node;
  var __defProp2 = Object.defineProperty;
  var __defNormalProp2 = function(obj, key, value) {
    return key in obj ? __defProp2(obj, key, {
      enumerable: true,
      configurable: true,
      writable: true,
      value
    }) : obj[key] = value;
  };
  var __publicField2 = function(obj, key, value) {
    return __defNormalProp2(obj, (typeof key === "undefined" ? "undefined" : _type_of(key)) !== "symbol" ? key + "" : key, value);
  };
  function getDefaultExportFromCjs$2(x22) {
    return x22 && x22.__esModule && Object.prototype.hasOwnProperty.call(x22, "default") ? x22["default"] : x22;
  }
  function getAugmentedNamespace$2(n22) {
    if (n22.__esModule) return n22;
    var f22 = n22.default;
    if (typeof f22 == "function") {
      var a22 = function a222() {
        if (_instanceof(this, a222)) {
          return Reflect.construct(f22, arguments, this.constructor);
        }
        return f22.apply(this, arguments);
      };
      a22.prototype = f22.prototype;
    } else a22 = {};
    Object.defineProperty(a22, "__esModule", {
      value: true
    });
    Object.keys(n22).forEach(function(k4) {
      var d3 = Object.getOwnPropertyDescriptor(n22, k4);
      Object.defineProperty(a22, k4, d3.get ? d3 : {
        enumerable: true,
        get: function get() {
          return n22[k4];
        }
      });
    });
    return a22;
  }
  var picocolors_browser$2 = {
    exports: {}
  };
  var x$2 = String;
  var create$2 = function create$22() {
    return {
      isColorSupported: false,
      reset: x$2,
      bold: x$2,
      dim: x$2,
      italic: x$2,
      underline: x$2,
      inverse: x$2,
      hidden: x$2,
      strikethrough: x$2,
      black: x$2,
      red: x$2,
      green: x$2,
      yellow: x$2,
      blue: x$2,
      magenta: x$2,
      cyan: x$2,
      white: x$2,
      gray: x$2,
      bgBlack: x$2,
      bgRed: x$2,
      bgGreen: x$2,
      bgYellow: x$2,
      bgBlue: x$2,
      bgMagenta: x$2,
      bgCyan: x$2,
      bgWhite: x$2
    };
  };
  picocolors_browser$2.exports = create$2();
  picocolors_browser$2.exports.createColors = create$2;
  var picocolors_browserExports$2 = picocolors_browser$2.exports;
  var __viteBrowserExternal$3 = {};
  var __viteBrowserExternal$1$2 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
    __proto__: null,
    default: __viteBrowserExternal$3
  }, Symbol.toStringTag, {
    value: "Module"
  }));
  var require$$2$2 = /* @__PURE__ */ getAugmentedNamespace$2(__viteBrowserExternal$1$2);
  var pico$2 = picocolors_browserExports$2;
  var terminalHighlight$1$2 = require$$2$2;
  var CssSyntaxError$3$2 = /* @__PURE__ */ (function(Error1) {
    _inherits(CssSyntaxError23, Error1);
    function CssSyntaxError23(message, line, column, source, file, plugin22) {
      var _this;
      _this = Error1.call(this, message) || this;
      _this.name = "CssSyntaxError";
      _this.reason = message;
      if (file) {
        _this.file = file;
      }
      if (source) {
        _this.source = source;
      }
      if (plugin22) {
        _this.plugin = plugin22;
      }
      if (typeof line !== "undefined" && typeof column !== "undefined") {
        if (typeof line === "number") {
          _this.line = line;
          _this.column = column;
        } else {
          _this.line = line.line;
          _this.column = line.column;
          _this.endLine = column.line;
          _this.endColumn = column.column;
        }
      }
      _this.setMessage();
      if (Error.captureStackTrace) {
        Error.captureStackTrace(_this, CssSyntaxError23);
      }
      return _this;
    }
    var _proto = CssSyntaxError23.prototype;
    _proto.setMessage = function setMessage() {
      this.message = this.plugin ? this.plugin + ": " : "";
      this.message += this.file ? this.file : "<css input>";
      if (typeof this.line !== "undefined") {
        this.message += ":" + this.line + ":" + this.column;
      }
      this.message += ": " + this.reason;
    };
    _proto.showSourceCode = function showSourceCode(color) {
      var _this = this;
      if (!this.source) return "";
      var css = this.source;
      if (color == null) color = pico$2.isColorSupported;
      if (terminalHighlight$1$2) {
        if (color) css = terminalHighlight$1$2(css);
      }
      var lines = css.split(/\r?\n/);
      var start = Math.max(this.line - 3, 0);
      var end = Math.min(this.line + 2, lines.length);
      var maxWidth = String(end).length;
      var mark, aside;
      if (color) {
        var _pico$2_createColors = pico$2.createColors(true), bold = _pico$2_createColors.bold, gray = _pico$2_createColors.gray, red = _pico$2_createColors.red;
        mark = function(text) {
          return bold(red(text));
        };
        aside = function(text) {
          return gray(text);
        };
      } else {
        mark = aside = function(str) {
          return str;
        };
      }
      return lines.slice(start, end).map(function(line, index2) {
        var number = start + 1 + index2;
        var gutter = " " + (" " + number).slice(-maxWidth) + " | ";
        if (number === _this.line) {
          var spacing = aside(gutter.replace(/\d/g, " ")) + line.slice(0, _this.column - 1).replace(/[^\t]/g, " ");
          return mark(">") + aside(gutter) + line + "\n " + spacing + mark("^");
        }
        return " " + aside(gutter) + line;
      }).join("\n");
    };
    _proto.toString = function toString2() {
      var code = this.showSourceCode();
      if (code) {
        code = "\n\n" + code + "\n";
      }
      return this.name + ": " + this.message + code;
    };
    return CssSyntaxError23;
  })(_wrap_native_super(Error));
  var cssSyntaxError$2 = CssSyntaxError$3$2;
  CssSyntaxError$3$2.default = CssSyntaxError$3$2;
  var symbols$2 = {};
  symbols$2.isClean = /* @__PURE__ */ Symbol("isClean");
  symbols$2.my = /* @__PURE__ */ Symbol("my");
  var DEFAULT_RAW$2 = {
    after: "\n",
    beforeClose: "\n",
    beforeComment: "\n",
    beforeDecl: "\n",
    beforeOpen: " ",
    beforeRule: "\n",
    colon: ": ",
    commentLeft: " ",
    commentRight: " ",
    emptyBody: "",
    indent: "    ",
    semicolon: false
  };
  function capitalize$2(str) {
    return str[0].toUpperCase() + str.slice(1);
  }
  var Stringifier$2$2 = /* @__PURE__ */ (function() {
    function Stringifier23(builder) {
      this.builder = builder;
    }
    var _proto = Stringifier23.prototype;
    _proto.atrule = function atrule(node2, semicolon) {
      var name5 = "@" + node2.name;
      var params = node2.params ? this.rawValue(node2, "params") : "";
      if (typeof node2.raws.afterName !== "undefined") {
        name5 += node2.raws.afterName;
      } else if (params) {
        name5 += " ";
      }
      if (node2.nodes) {
        this.block(node2, name5 + params);
      } else {
        var end = (node2.raws.between || "") + (semicolon ? ";" : "");
        this.builder(name5 + params + end, node2);
      }
    };
    _proto.beforeAfter = function beforeAfter(node2, detect) {
      var value;
      if (node2.type === "decl") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (node2.type === "comment") {
        value = this.raw(node2, null, "beforeComment");
      } else if (detect === "before") {
        value = this.raw(node2, null, "beforeRule");
      } else {
        value = this.raw(node2, null, "beforeClose");
      }
      var buf = node2.parent;
      var depth = 0;
      while (buf && buf.type !== "root") {
        depth += 1;
        buf = buf.parent;
      }
      if (value.includes("\n")) {
        var indent = this.raw(node2, null, "indent");
        if (indent.length) {
          for (var step = 0; step < depth; step++) value += indent;
        }
      }
      return value;
    };
    _proto.block = function block(node2, start) {
      var between = this.raw(node2, "between", "beforeOpen");
      this.builder(start + between + "{", node2, "start");
      var after;
      if (node2.nodes && node2.nodes.length) {
        this.body(node2);
        after = this.raw(node2, "after");
      } else {
        after = this.raw(node2, "after", "emptyBody");
      }
      if (after) this.builder(after);
      this.builder("}", node2, "end");
    };
    _proto.body = function body(node2) {
      var last = node2.nodes.length - 1;
      while (last > 0) {
        if (node2.nodes[last].type !== "comment") break;
        last -= 1;
      }
      var semicolon = this.raw(node2, "semicolon");
      for (var i22 = 0; i22 < node2.nodes.length; i22++) {
        var child = node2.nodes[i22];
        var before = this.raw(child, "before");
        if (before) this.builder(before);
        this.stringify(child, last !== i22 || semicolon);
      }
    };
    _proto.comment = function comment2(node2) {
      var left = this.raw(node2, "left", "commentLeft");
      var right = this.raw(node2, "right", "commentRight");
      this.builder("/*" + left + node2.text + right + "*/", node2);
    };
    _proto.decl = function decl(node2, semicolon) {
      var between = this.raw(node2, "between", "colon");
      var string = node2.prop + between + this.rawValue(node2, "value");
      if (node2.important) {
        string += node2.raws.important || " !important";
      }
      if (semicolon) string += ";";
      this.builder(string, node2);
    };
    _proto.document = function document1(node2) {
      this.body(node2);
    };
    _proto.raw = function raw(node2, own, detect) {
      var value;
      if (!detect) detect = own;
      if (own) {
        value = node2.raws[own];
        if (typeof value !== "undefined") return value;
      }
      var parent = node2.parent;
      if (detect === "before") {
        if (!parent || parent.type === "root" && parent.first === node2) {
          return "";
        }
        if (parent && parent.type === "document") {
          return "";
        }
      }
      if (!parent) return DEFAULT_RAW$2[detect];
      var root2 = node2.root();
      if (!root2.rawCache) root2.rawCache = {};
      if (typeof root2.rawCache[detect] !== "undefined") {
        return root2.rawCache[detect];
      }
      if (detect === "before" || detect === "after") {
        return this.beforeAfter(node2, detect);
      } else {
        var method = "raw" + capitalize$2(detect);
        if (this[method]) {
          value = this[method](root2, node2);
        } else {
          root2.walk(function(i22) {
            value = i22.raws[own];
            if (typeof value !== "undefined") return false;
          });
        }
      }
      if (typeof value === "undefined") value = DEFAULT_RAW$2[detect];
      root2.rawCache[detect] = value;
      return value;
    };
    _proto.rawBeforeClose = function rawBeforeClose(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length > 0) {
          if (typeof i22.raws.after !== "undefined") {
            value = i22.raws.after;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawBeforeComment = function rawBeforeComment(root2, node2) {
      var value;
      root2.walkComments(function(i22) {
        if (typeof i22.raws.before !== "undefined") {
          value = i22.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeDecl = function rawBeforeDecl(root2, node2) {
      var value;
      root2.walkDecls(function(i22) {
        if (typeof i22.raws.before !== "undefined") {
          value = i22.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeRule");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeOpen = function rawBeforeOpen(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.type !== "decl") {
          value = i22.raws.between;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawBeforeRule = function rawBeforeRule(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && (i22.parent !== root2 || root2.first !== i22)) {
          if (typeof i22.raws.before !== "undefined") {
            value = i22.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawColon = function rawColon(root2) {
      var value;
      root2.walkDecls(function(i22) {
        if (typeof i22.raws.between !== "undefined") {
          value = i22.raws.between.replace(/[^\s:]/g, "");
          return false;
        }
      });
      return value;
    };
    _proto.rawEmptyBody = function rawEmptyBody(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length === 0) {
          value = i22.raws.after;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawIndent = function rawIndent(root2) {
      if (root2.raws.indent) return root2.raws.indent;
      var value;
      root2.walk(function(i22) {
        var p3 = i22.parent;
        if (p3 && p3 !== root2 && p3.parent && p3.parent === root2) {
          if (typeof i22.raws.before !== "undefined") {
            var parts = i22.raws.before.split("\n");
            value = parts[parts.length - 1];
            value = value.replace(/\S/g, "");
            return false;
          }
        }
      });
      return value;
    };
    _proto.rawSemicolon = function rawSemicolon(root2) {
      var value;
      root2.walk(function(i22) {
        if (i22.nodes && i22.nodes.length && i22.last.type === "decl") {
          value = i22.raws.semicolon;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawValue = function rawValue(node2, prop) {
      var value = node2[prop];
      var raw = node2.raws[prop];
      if (raw && raw.value === value) {
        return raw.raw;
      }
      return value;
    };
    _proto.root = function root2(node2) {
      this.body(node2);
      if (node2.raws.after) this.builder(node2.raws.after);
    };
    _proto.rule = function rule2(node2) {
      this.block(node2, this.rawValue(node2, "selector"));
      if (node2.raws.ownSemicolon) {
        this.builder(node2.raws.ownSemicolon, node2, "end");
      }
    };
    _proto.stringify = function stringify2(node2, semicolon) {
      if (!this[node2.type]) {
        throw new Error("Unknown AST node type " + node2.type + ". Maybe you need to change PostCSS stringifier.");
      }
      this[node2.type](node2, semicolon);
    };
    return Stringifier23;
  })();
  var stringifier$2 = Stringifier$2$2;
  Stringifier$2$2.default = Stringifier$2$2;
  var Stringifier$1$2 = stringifier$2;
  function stringify$4$2(node2, builder) {
    var str = new Stringifier$1$2(builder);
    str.stringify(node2);
  }
  var stringify_1$2 = stringify$4$2;
  stringify$4$2.default = stringify$4$2;
  var isClean$2$2 = symbols$2.isClean;
  var my$2$2 = symbols$2.my;
  var CssSyntaxError$2$2 = cssSyntaxError$2;
  var Stringifier22 = stringifier$2;
  var stringify$3$2 = stringify_1$2;
  function cloneNode$2(obj, parent) {
    var cloned = new obj.constructor();
    for (var i22 in obj) {
      if (!Object.prototype.hasOwnProperty.call(obj, i22)) {
        continue;
      }
      if (i22 === "proxyCache") continue;
      var value = obj[i22];
      var type = typeof value === "undefined" ? "undefined" : _type_of(value);
      if (i22 === "parent" && type === "object") {
        if (parent) cloned[i22] = parent;
      } else if (i22 === "source") {
        cloned[i22] = value;
      } else if (Array.isArray(value)) {
        cloned[i22] = value.map(function(j4) {
          return cloneNode$2(j4, cloned);
        });
      } else {
        if (type === "object" && value !== null) value = cloneNode$2(value);
        cloned[i22] = value;
      }
    }
    return cloned;
  }
  var Node$4$2 = /* @__PURE__ */ (function() {
    function Node3(defaults) {
      if (defaults === void 0) defaults = {};
      this.raws = {};
      this[isClean$2$2] = false;
      this[my$2$2] = true;
      for (var name5 in defaults) {
        if (name5 === "nodes") {
          this.nodes = [];
          for (var _iterator = _create_for_of_iterator_helper_loose(defaults[name5]), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            if (typeof node2.clone === "function") {
              this.append(node2.clone());
            } else {
              this.append(node2);
            }
          }
        } else {
          this[name5] = defaults[name5];
        }
      }
    }
    var _proto = Node3.prototype;
    _proto.addToError = function addToError(error) {
      error.postcssNode = this;
      if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
        var s22 = this.source;
        error.stack = error.stack.replace(/\n\s{4}at /, "$&" + s22.input.from + ":" + s22.start.line + ":" + s22.start.column + "$&");
      }
      return error;
    };
    _proto.after = function after(add) {
      this.parent.insertAfter(this, add);
      return this;
    };
    _proto.assign = function assign(overrides) {
      if (overrides === void 0) overrides = {};
      for (var name5 in overrides) {
        this[name5] = overrides[name5];
      }
      return this;
    };
    _proto.before = function before(add) {
      this.parent.insertBefore(this, add);
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      delete this.raws.before;
      delete this.raws.after;
      if (!keepBetween) delete this.raws.between;
    };
    _proto.clone = function clone(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = cloneNode$2(this);
      for (var name5 in overrides) {
        cloned[name5] = overrides[name5];
      }
      return cloned;
    };
    _proto.cloneAfter = function cloneAfter(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertAfter(this, cloned);
      return cloned;
    };
    _proto.cloneBefore = function cloneBefore(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertBefore(this, cloned);
      return cloned;
    };
    _proto.error = function error(message, opts) {
      if (opts === void 0) opts = {};
      if (this.source) {
        var _this_rangeBy = this.rangeBy(opts), end = _this_rangeBy.end, start = _this_rangeBy.start;
        return this.source.input.error(message, {
          column: start.column,
          line: start.line
        }, {
          column: end.column,
          line: end.line
        }, opts);
      }
      return new CssSyntaxError$2$2(message);
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
          prop === "text") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.markDirty = function markDirty() {
      if (this[isClean$2$2]) {
        this[isClean$2$2] = false;
        var next = this;
        while (next = next.parent) {
          next[isClean$2$2] = false;
        }
      }
    };
    _proto.next = function next() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 + 1];
    };
    _proto.positionBy = function positionBy(opts, stringRepresentation) {
      var pos = this.source.start;
      if (opts.index) {
        pos = this.positionInside(opts.index, stringRepresentation);
      } else if (opts.word) {
        stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) pos = this.positionInside(index2, stringRepresentation);
      }
      return pos;
    };
    _proto.positionInside = function positionInside(index2, stringRepresentation) {
      var string = stringRepresentation || this.toString();
      var column = this.source.start.column;
      var line = this.source.start.line;
      for (var i22 = 0; i22 < index2; i22++) {
        if (string[i22] === "\n") {
          column = 1;
          line += 1;
        } else {
          column += 1;
        }
      }
      return {
        column,
        line
      };
    };
    _proto.prev = function prev() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 - 1];
    };
    _proto.rangeBy = function rangeBy(opts) {
      var start = {
        column: this.source.start.column,
        line: this.source.start.line
      };
      var end = this.source.end ? {
        column: this.source.end.column + 1,
        line: this.source.end.line
      } : {
        column: start.column + 1,
        line: start.line
      };
      if (opts.word) {
        var stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) {
          start = this.positionInside(index2, stringRepresentation);
          end = this.positionInside(index2 + opts.word.length, stringRepresentation);
        }
      } else {
        if (opts.start) {
          start = {
            column: opts.start.column,
            line: opts.start.line
          };
        } else if (opts.index) {
          start = this.positionInside(opts.index);
        }
        if (opts.end) {
          end = {
            column: opts.end.column,
            line: opts.end.line
          };
        } else if (typeof opts.endIndex === "number") {
          end = this.positionInside(opts.endIndex);
        } else if (opts.index) {
          end = this.positionInside(opts.index + 1);
        }
      }
      if (end.line < start.line || end.line === start.line && end.column <= start.column) {
        end = {
          column: start.column + 1,
          line: start.line
        };
      }
      return {
        end,
        start
      };
    };
    _proto.raw = function raw(prop, defaultType) {
      var str = new Stringifier22();
      return str.raw(this, prop, defaultType);
    };
    _proto.remove = function remove2() {
      if (this.parent) {
        this.parent.removeChild(this);
      }
      this.parent = void 0;
      return this;
    };
    _proto.replaceWith = function replaceWith() {
      for (var _len = arguments.length, nodes = new Array(_len), _key = 0; _key < _len; _key++) {
        nodes[_key] = arguments[_key];
      }
      if (this.parent) {
        var bookmark = this;
        var foundSelf = false;
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          if (node2 === this) {
            foundSelf = true;
          } else if (foundSelf) {
            this.parent.insertAfter(bookmark, node2);
            bookmark = node2;
          } else {
            this.parent.insertBefore(bookmark, node2);
          }
        }
        if (!foundSelf) {
          this.remove();
        }
      }
      return this;
    };
    _proto.root = function root2() {
      var result2 = this;
      while (result2.parent && result2.parent.type !== "document") {
        result2 = result2.parent;
      }
      return result2;
    };
    _proto.toJSON = function toJSON(_4, inputs) {
      var fixed = {};
      var emitInputs = inputs == null;
      inputs = inputs || /* @__PURE__ */ new Map();
      var inputsNextIndex = 0;
      for (var name5 in this) {
        if (!Object.prototype.hasOwnProperty.call(this, name5)) {
          continue;
        }
        if (name5 === "parent" || name5 === "proxyCache") continue;
        var value = this[name5];
        if (Array.isArray(value)) {
          fixed[name5] = value.map(function(i22) {
            if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && i22.toJSON) {
              return i22.toJSON(null, inputs);
            } else {
              return i22;
            }
          });
        } else if ((typeof value === "undefined" ? "undefined" : _type_of(value)) === "object" && value.toJSON) {
          fixed[name5] = value.toJSON(null, inputs);
        } else if (name5 === "source") {
          var inputId = inputs.get(value.input);
          if (inputId == null) {
            inputId = inputsNextIndex;
            inputs.set(value.input, inputsNextIndex);
            inputsNextIndex++;
          }
          fixed[name5] = {
            end: value.end,
            inputId,
            start: value.start
          };
        } else {
          fixed[name5] = value;
        }
      }
      if (emitInputs) {
        fixed.inputs = [].concat(inputs.keys()).map(function(input2) {
          return input2.toJSON();
        });
      }
      return fixed;
    };
    _proto.toProxy = function toProxy() {
      if (!this.proxyCache) {
        this.proxyCache = new Proxy(this, this.getProxyProcessor());
      }
      return this.proxyCache;
    };
    _proto.toString = function toString2(stringifier2) {
      if (stringifier2 === void 0) stringifier2 = stringify$3$2;
      if (stringifier2.stringify) stringifier2 = stringifier2.stringify;
      var result2 = "";
      stringifier2(this, function(i22) {
        result2 += i22;
      });
      return result2;
    };
    _proto.warn = function warn(result2, text, opts) {
      var data = {
        node: this
      };
      for (var i22 in opts) data[i22] = opts[i22];
      return result2.warn(text, data);
    };
    _create_class(Node3, [
      {
        key: "proxyOf",
        get: function get() {
          return this;
        }
      }
    ]);
    return Node3;
  })();
  var node$2 = Node$4$2;
  Node$4$2.default = Node$4$2;
  var Node$3$2 = node$2;
  var Declaration$4$2 = /* @__PURE__ */ (function(Node$3$22) {
    _inherits(Declaration23, Node$3$22);
    function Declaration23(defaults) {
      var _this;
      if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
        defaults = _extends({}, defaults, {
          value: String(defaults.value)
        });
      }
      _this = Node$3$22.call(this, defaults) || this;
      _this.type = "decl";
      return _this;
    }
    _create_class(Declaration23, [
      {
        key: "variable",
        get: function get() {
          return this.prop.startsWith("--") || this.prop[0] === "$";
        }
      }
    ]);
    return Declaration23;
  })(Node$3$2);
  var declaration$2 = Declaration$4$2;
  Declaration$4$2.default = Declaration$4$2;
  var urlAlphabet$2 = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
  var nanoid$1$2 = function(size) {
    if (size === void 0) size = 21;
    var id = "";
    var i22 = size;
    while (i22--) {
      id += urlAlphabet$2[Math.random() * 64 | 0];
    }
    return id;
  };
  var nonSecure$2 = {
    nanoid: nanoid$1$2
  };
  var SourceMapConsumer$2$2 = require$$2$2.SourceMapConsumer;
  var SourceMapGenerator$2$2 = require$$2$2.SourceMapGenerator;
  var existsSync$2 = require$$2$2.existsSync;
  var readFileSync$2 = require$$2$2.readFileSync;
  var dirname$1$2 = require$$2$2.dirname;
  var join$2 = require$$2$2.join;
  function fromBase64$2(str) {
    if (Buffer) {
      return Buffer.from(str, "base64").toString();
    } else {
      return window.atob(str);
    }
  }
  var PreviousMap$2$2 = /* @__PURE__ */ (function() {
    function PreviousMap23(css, opts) {
      if (opts.map === false) return;
      this.loadAnnotation(css);
      this.inline = this.startWith(this.annotation, "data:");
      var prev = opts.map ? opts.map.prev : void 0;
      var text = this.loadMap(opts.from, prev);
      if (!this.mapFile && opts.from) {
        this.mapFile = opts.from;
      }
      if (this.mapFile) this.root = dirname$1$2(this.mapFile);
      if (text) this.text = text;
    }
    var _proto = PreviousMap23.prototype;
    _proto.consumer = function consumer() {
      if (!this.consumerCache) {
        this.consumerCache = new SourceMapConsumer$2$2(this.text);
      }
      return this.consumerCache;
    };
    _proto.decodeInline = function decodeInline(text) {
      var baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
      var baseUri = /^data:application\/json;base64,/;
      var charsetUri = /^data:application\/json;charset=utf-?8,/;
      var uri = /^data:application\/json,/;
      if (charsetUri.test(text) || uri.test(text)) {
        return decodeURIComponent(text.substr(RegExp.lastMatch.length));
      }
      if (baseCharsetUri.test(text) || baseUri.test(text)) {
        return fromBase64$2(text.substr(RegExp.lastMatch.length));
      }
      var encoding = text.match(/data:application\/json;([^,]+),/)[1];
      throw new Error("Unsupported source map encoding " + encoding);
    };
    _proto.getAnnotationURL = function getAnnotationURL(sourceMapString) {
      return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
    };
    _proto.isMap = function isMap(map) {
      if ((typeof map === "undefined" ? "undefined" : _type_of(map)) !== "object") return false;
      return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
    };
    _proto.loadAnnotation = function loadAnnotation(css) {
      var comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
      if (!comments) return;
      var start = css.lastIndexOf(comments.pop());
      var end = css.indexOf("*/", start);
      if (start > -1 && end > -1) {
        this.annotation = this.getAnnotationURL(css.substring(start, end));
      }
    };
    _proto.loadFile = function loadFile(path) {
      this.root = dirname$1$2(path);
      if (existsSync$2(path)) {
        this.mapFile = path;
        return readFileSync$2(path, "utf-8").toString().trim();
      }
    };
    _proto.loadMap = function loadMap(file, prev) {
      if (prev === false) return false;
      if (prev) {
        if (typeof prev === "string") {
          return prev;
        } else if (typeof prev === "function") {
          var prevPath = prev(file);
          if (prevPath) {
            var map = this.loadFile(prevPath);
            if (!map) {
              throw new Error("Unable to load previous source map: " + prevPath.toString());
            }
            return map;
          }
        } else if (_instanceof(prev, SourceMapConsumer$2$2)) {
          return SourceMapGenerator$2$2.fromSourceMap(prev).toString();
        } else if (_instanceof(prev, SourceMapGenerator$2$2)) {
          return prev.toString();
        } else if (this.isMap(prev)) {
          return JSON.stringify(prev);
        } else {
          throw new Error("Unsupported previous source map format: " + prev.toString());
        }
      } else if (this.inline) {
        return this.decodeInline(this.annotation);
      } else if (this.annotation) {
        var map1 = this.annotation;
        if (file) map1 = join$2(dirname$1$2(file), map1);
        return this.loadFile(map1);
      }
    };
    _proto.startWith = function startWith(string, start) {
      if (!string) return false;
      return string.substr(0, start.length) === start;
    };
    _proto.withContent = function withContent() {
      return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
    };
    return PreviousMap23;
  })();
  var previousMap$2 = PreviousMap$2$2;
  PreviousMap$2$2.default = PreviousMap$2$2;
  var SourceMapConsumer$1$2 = require$$2$2.SourceMapConsumer;
  var SourceMapGenerator$1$2 = require$$2$2.SourceMapGenerator;
  var fileURLToPath$2 = require$$2$2.fileURLToPath;
  var pathToFileURL$1$2 = require$$2$2.pathToFileURL;
  var isAbsolute$2 = require$$2$2.isAbsolute;
  var resolve$1$2 = require$$2$2.resolve;
  var nanoid$3 = nonSecure$2.nanoid;
  var terminalHighlight$3 = require$$2$2;
  var CssSyntaxError$1$2 = cssSyntaxError$2;
  var PreviousMap$1$2 = previousMap$2;
  var fromOffsetCache$2 = /* @__PURE__ */ Symbol("fromOffsetCache");
  var sourceMapAvailable$1$2 = Boolean(SourceMapConsumer$1$2 && SourceMapGenerator$1$2);
  var pathAvailable$1$2 = Boolean(resolve$1$2 && isAbsolute$2);
  var Input$4$2 = /* @__PURE__ */ (function() {
    function Input23(css, opts) {
      if (opts === void 0) opts = {};
      if (css === null || typeof css === "undefined" || (typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && !css.toString) {
        throw new Error("PostCSS received " + css + " instead of CSS string");
      }
      this.css = css.toString();
      if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
        this.hasBOM = true;
        this.css = this.css.slice(1);
      } else {
        this.hasBOM = false;
      }
      if (opts.from) {
        if (!pathAvailable$1$2 || /^\w+:\/\//.test(opts.from) || isAbsolute$2(opts.from)) {
          this.file = opts.from;
        } else {
          this.file = resolve$1$2(opts.from);
        }
      }
      if (pathAvailable$1$2 && sourceMapAvailable$1$2) {
        var map = new PreviousMap$1$2(this.css, opts);
        if (map.text) {
          this.map = map;
          var file = map.consumer().file;
          if (!this.file && file) this.file = this.mapResolve(file);
        }
      }
      if (!this.file) {
        this.id = "<input css " + nanoid$3(6) + ">";
      }
      if (this.map) this.map.file = this.from;
    }
    var _proto = Input23.prototype;
    _proto.error = function error(message, line, column, opts) {
      if (opts === void 0) opts = {};
      var result2, endLine, endColumn;
      if (line && (typeof line === "undefined" ? "undefined" : _type_of(line)) === "object") {
        var start = line;
        var end = column;
        if (typeof start.offset === "number") {
          var pos = this.fromOffset(start.offset);
          line = pos.line;
          column = pos.col;
        } else {
          line = start.line;
          column = start.column;
        }
        if (typeof end.offset === "number") {
          var pos1 = this.fromOffset(end.offset);
          endLine = pos1.line;
          endColumn = pos1.col;
        } else {
          endLine = end.line;
          endColumn = end.column;
        }
      } else if (!column) {
        var pos2 = this.fromOffset(line);
        line = pos2.line;
        column = pos2.col;
      }
      var origin = this.origin(line, column, endLine, endColumn);
      if (origin) {
        result2 = new CssSyntaxError$1$2(message, origin.endLine === void 0 ? origin.line : {
          column: origin.column,
          line: origin.line
        }, origin.endLine === void 0 ? origin.column : {
          column: origin.endColumn,
          line: origin.endLine
        }, origin.source, origin.file, opts.plugin);
      } else {
        result2 = new CssSyntaxError$1$2(message, endLine === void 0 ? line : {
          column,
          line
        }, endLine === void 0 ? column : {
          column: endColumn,
          line: endLine
        }, this.css, this.file, opts.plugin);
      }
      result2.input = {
        column,
        endColumn,
        endLine,
        line,
        source: this.css
      };
      if (this.file) {
        if (pathToFileURL$1$2) {
          result2.input.url = pathToFileURL$1$2(this.file).toString();
        }
        result2.input.file = this.file;
      }
      return result2;
    };
    _proto.fromOffset = function fromOffset(offset) {
      var lastLine, lineToIndex;
      if (!this[fromOffsetCache$2]) {
        var lines = this.css.split("\n");
        lineToIndex = new Array(lines.length);
        var prevIndex = 0;
        for (var i22 = 0, l22 = lines.length; i22 < l22; i22++) {
          lineToIndex[i22] = prevIndex;
          prevIndex += lines[i22].length + 1;
        }
        this[fromOffsetCache$2] = lineToIndex;
      } else {
        lineToIndex = this[fromOffsetCache$2];
      }
      lastLine = lineToIndex[lineToIndex.length - 1];
      var min = 0;
      if (offset >= lastLine) {
        min = lineToIndex.length - 1;
      } else {
        var max = lineToIndex.length - 2;
        var mid;
        while (min < max) {
          mid = min + (max - min >> 1);
          if (offset < lineToIndex[mid]) {
            max = mid - 1;
          } else if (offset >= lineToIndex[mid + 1]) {
            min = mid + 1;
          } else {
            min = mid;
            break;
          }
        }
      }
      return {
        col: offset - lineToIndex[min] + 1,
        line: min + 1
      };
    };
    _proto.mapResolve = function mapResolve(file) {
      if (/^\w+:\/\//.test(file)) {
        return file;
      }
      return resolve$1$2(this.map.consumer().sourceRoot || this.map.root || ".", file);
    };
    _proto.origin = function origin(line, column, endLine, endColumn) {
      if (!this.map) return false;
      var consumer = this.map.consumer();
      var from = consumer.originalPositionFor({
        column,
        line
      });
      if (!from.source) return false;
      var to;
      if (typeof endLine === "number") {
        to = consumer.originalPositionFor({
          column: endColumn,
          line: endLine
        });
      }
      var fromUrl;
      if (isAbsolute$2(from.source)) {
        fromUrl = pathToFileURL$1$2(from.source);
      } else {
        fromUrl = new URL(from.source, this.map.consumer().sourceRoot || pathToFileURL$1$2(this.map.mapFile));
      }
      var result2 = {
        column: from.column,
        endColumn: to && to.column,
        endLine: to && to.line,
        line: from.line,
        url: fromUrl.toString()
      };
      if (fromUrl.protocol === "file:") {
        if (fileURLToPath$2) {
          result2.file = fileURLToPath$2(fromUrl);
        } else {
          throw new Error("file: protocol is not available in this PostCSS build");
        }
      }
      var source = consumer.sourceContentFor(from.source);
      if (source) result2.source = source;
      return result2;
    };
    _proto.toJSON = function toJSON() {
      var json = {};
      for (var _i = 0, _iter = [
        "hasBOM",
        "css",
        "file",
        "id"
      ]; _i < _iter.length; _i++) {
        var name5 = _iter[_i];
        if (this[name5] != null) {
          json[name5] = this[name5];
        }
      }
      if (this.map) {
        json.map = _extends({}, this.map);
        if (json.map.consumerCache) {
          json.map.consumerCache = void 0;
        }
      }
      return json;
    };
    _create_class(Input23, [
      {
        key: "from",
        get: function get() {
          return this.file || this.id;
        }
      }
    ]);
    return Input23;
  })();
  var input$2 = Input$4$2;
  Input$4$2.default = Input$4$2;
  if (terminalHighlight$3 && terminalHighlight$3.registerInput) {
    terminalHighlight$3.registerInput(Input$4$2);
  }
  var SourceMapConsumer$4 = require$$2$2.SourceMapConsumer;
  var SourceMapGenerator$4 = require$$2$2.SourceMapGenerator;
  var dirname$3 = require$$2$2.dirname;
  var relative$2 = require$$2$2.relative;
  var resolve$3 = require$$2$2.resolve;
  var sep$2 = require$$2$2.sep;
  var pathToFileURL$3 = require$$2$2.pathToFileURL;
  var Input$3$2 = input$2;
  var sourceMapAvailable$3 = Boolean(SourceMapConsumer$4 && SourceMapGenerator$4);
  var pathAvailable$3 = Boolean(dirname$3 && resolve$3 && relative$2 && sep$2);
  var MapGenerator$2$2 = /* @__PURE__ */ (function() {
    function MapGenerator23(stringify2, root2, opts, cssString) {
      this.stringify = stringify2;
      this.mapOpts = opts.map || {};
      this.root = root2;
      this.opts = opts;
      this.css = cssString;
      this.originalCSS = cssString;
      this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
      this.memoizedFileURLs = /* @__PURE__ */ new Map();
      this.memoizedPaths = /* @__PURE__ */ new Map();
      this.memoizedURLs = /* @__PURE__ */ new Map();
    }
    var _proto = MapGenerator23.prototype;
    _proto.addAnnotation = function addAnnotation() {
      var content;
      if (this.isInline()) {
        content = "data:application/json;base64," + this.toBase64(this.map.toString());
      } else if (typeof this.mapOpts.annotation === "string") {
        content = this.mapOpts.annotation;
      } else if (typeof this.mapOpts.annotation === "function") {
        content = this.mapOpts.annotation(this.opts.to, this.root);
      } else {
        content = this.outputFile() + ".map";
      }
      var eol = "\n";
      if (this.css.includes("\r\n")) eol = "\r\n";
      this.css += eol + "/*# sourceMappingURL=" + content + " */";
    };
    _proto.applyPrevMaps = function applyPrevMaps() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.previous()), _step; !(_step = _iterator()).done; ) {
        var prev = _step.value;
        var from = this.toUrl(this.path(prev.file));
        var root2 = prev.root || dirname$3(prev.file);
        var map = void 0;
        if (this.mapOpts.sourcesContent === false) {
          map = new SourceMapConsumer$4(prev.text);
          if (map.sourcesContent) {
            map.sourcesContent = null;
          }
        } else {
          map = prev.consumer();
        }
        this.map.applySourceMap(map, from, this.toUrl(this.path(root2)));
      }
    };
    _proto.clearAnnotation = function clearAnnotation() {
      if (this.mapOpts.annotation === false) return;
      if (this.root) {
        var node2;
        for (var i22 = this.root.nodes.length - 1; i22 >= 0; i22--) {
          node2 = this.root.nodes[i22];
          if (node2.type !== "comment") continue;
          if (node2.text.indexOf("# sourceMappingURL=") === 0) {
            this.root.removeChild(i22);
          }
        }
      } else if (this.css) {
        this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
      }
    };
    _proto.generate = function generate() {
      this.clearAnnotation();
      if (pathAvailable$3 && sourceMapAvailable$3 && this.isMap()) {
        return this.generateMap();
      } else {
        var result2 = "";
        this.stringify(this.root, function(i22) {
          result2 += i22;
        });
        return [
          result2
        ];
      }
    };
    _proto.generateMap = function generateMap() {
      if (this.root) {
        this.generateString();
      } else if (this.previous().length === 1) {
        var prev = this.previous()[0].consumer();
        prev.file = this.outputFile();
        this.map = SourceMapGenerator$4.fromSourceMap(prev, {
          ignoreInvalidMapping: true
        });
      } else {
        this.map = new SourceMapGenerator$4({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        this.map.addMapping({
          generated: {
            column: 0,
            line: 1
          },
          original: {
            column: 0,
            line: 1
          },
          source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
        });
      }
      if (this.isSourcesContent()) this.setSourcesContent();
      if (this.root && this.previous().length > 0) this.applyPrevMaps();
      if (this.isAnnotation()) this.addAnnotation();
      if (this.isInline()) {
        return [
          this.css
        ];
      } else {
        return [
          this.css,
          this.map
        ];
      }
    };
    _proto.generateString = function generateString() {
      var _this = this;
      this.css = "";
      this.map = new SourceMapGenerator$4({
        file: this.outputFile(),
        ignoreInvalidMapping: true
      });
      var line = 1;
      var column = 1;
      var noSource = "<no source>";
      var mapping = {
        generated: {
          column: 0,
          line: 0
        },
        original: {
          column: 0,
          line: 0
        },
        source: ""
      };
      var lines, last;
      this.stringify(this.root, function(str, node2, type) {
        _this.css += str;
        if (node2 && type !== "end") {
          mapping.generated.line = line;
          mapping.generated.column = column - 1;
          if (node2.source && node2.source.start) {
            mapping.source = _this.sourcePath(node2);
            mapping.original.line = node2.source.start.line;
            mapping.original.column = node2.source.start.column - 1;
            _this.map.addMapping(mapping);
          } else {
            mapping.source = noSource;
            mapping.original.line = 1;
            mapping.original.column = 0;
            _this.map.addMapping(mapping);
          }
        }
        lines = str.match(/\n/g);
        if (lines) {
          line += lines.length;
          last = str.lastIndexOf("\n");
          column = str.length - last;
        } else {
          column += str.length;
        }
        if (node2 && type !== "start") {
          var p3 = node2.parent || {
            raws: {}
          };
          var childless = node2.type === "decl" || node2.type === "atrule" && !node2.nodes;
          if (!childless || node2 !== p3.last || p3.raws.semicolon) {
            if (node2.source && node2.source.end) {
              mapping.source = _this.sourcePath(node2);
              mapping.original.line = node2.source.end.line;
              mapping.original.column = node2.source.end.column - 1;
              mapping.generated.line = line;
              mapping.generated.column = column - 2;
              _this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              mapping.generated.line = line;
              mapping.generated.column = column - 1;
              _this.map.addMapping(mapping);
            }
          }
        }
      });
    };
    _proto.isAnnotation = function isAnnotation() {
      if (this.isInline()) {
        return true;
      }
      if (typeof this.mapOpts.annotation !== "undefined") {
        return this.mapOpts.annotation;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.annotation;
        });
      }
      return true;
    };
    _proto.isInline = function isInline() {
      if (typeof this.mapOpts.inline !== "undefined") {
        return this.mapOpts.inline;
      }
      var annotation = this.mapOpts.annotation;
      if (typeof annotation !== "undefined" && annotation !== true) {
        return false;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.inline;
        });
      }
      return true;
    };
    _proto.isMap = function isMap() {
      if (typeof this.opts.map !== "undefined") {
        return !!this.opts.map;
      }
      return this.previous().length > 0;
    };
    _proto.isSourcesContent = function isSourcesContent() {
      if (typeof this.mapOpts.sourcesContent !== "undefined") {
        return this.mapOpts.sourcesContent;
      }
      if (this.previous().length) {
        return this.previous().some(function(i22) {
          return i22.withContent();
        });
      }
      return true;
    };
    _proto.outputFile = function outputFile() {
      if (this.opts.to) {
        return this.path(this.opts.to);
      } else if (this.opts.from) {
        return this.path(this.opts.from);
      } else {
        return "to.css";
      }
    };
    _proto.path = function path(file) {
      if (this.mapOpts.absolute) return file;
      if (file.charCodeAt(0) === 60) return file;
      if (/^\w+:\/\//.test(file)) return file;
      var cached = this.memoizedPaths.get(file);
      if (cached) return cached;
      var from = this.opts.to ? dirname$3(this.opts.to) : ".";
      if (typeof this.mapOpts.annotation === "string") {
        from = dirname$3(resolve$3(from, this.mapOpts.annotation));
      }
      var path2 = relative$2(from, file);
      this.memoizedPaths.set(file, path2);
      return path2;
    };
    _proto.previous = function previous() {
      var _this = this;
      if (!this.previousMaps) {
        this.previousMaps = [];
        if (this.root) {
          this.root.walk(function(node2) {
            if (node2.source && node2.source.input.map) {
              var map = node2.source.input.map;
              if (!_this.previousMaps.includes(map)) {
                _this.previousMaps.push(map);
              }
            }
          });
        } else {
          var input2 = new Input$3$2(this.originalCSS, this.opts);
          if (input2.map) this.previousMaps.push(input2.map);
        }
      }
      return this.previousMaps;
    };
    _proto.setSourcesContent = function setSourcesContent() {
      var _this = this;
      var already = {};
      if (this.root) {
        this.root.walk(function(node2) {
          if (node2.source) {
            var from2 = node2.source.input.from;
            if (from2 && !already[from2]) {
              already[from2] = true;
              var fromUrl = _this.usesFileUrls ? _this.toFileUrl(from2) : _this.toUrl(_this.path(from2));
              _this.map.setSourceContent(fromUrl, node2.source.input.css);
            }
          }
        });
      } else if (this.css) {
        var from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
        this.map.setSourceContent(from, this.css);
      }
    };
    _proto.sourcePath = function sourcePath(node2) {
      if (this.mapOpts.from) {
        return this.toUrl(this.mapOpts.from);
      } else if (this.usesFileUrls) {
        return this.toFileUrl(node2.source.input.from);
      } else {
        return this.toUrl(this.path(node2.source.input.from));
      }
    };
    _proto.toBase64 = function toBase64(str) {
      if (Buffer) {
        return Buffer.from(str).toString("base64");
      } else {
        return window.btoa(unescape(encodeURIComponent(str)));
      }
    };
    _proto.toFileUrl = function toFileUrl(path) {
      var cached = this.memoizedFileURLs.get(path);
      if (cached) return cached;
      if (pathToFileURL$3) {
        var fileURL = pathToFileURL$3(path).toString();
        this.memoizedFileURLs.set(path, fileURL);
        return fileURL;
      } else {
        throw new Error("`map.absolute` option is not available in this PostCSS build");
      }
    };
    _proto.toUrl = function toUrl(path) {
      var cached = this.memoizedURLs.get(path);
      if (cached) return cached;
      if (sep$2 === "\\") {
        path = path.replace(/\\/g, "/");
      }
      var url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
      this.memoizedURLs.set(path, url);
      return url;
    };
    return MapGenerator23;
  })();
  var mapGenerator$2 = MapGenerator$2$2;
  var Node$2$2 = node$2;
  var Comment$4$2 = /* @__PURE__ */ (function(Node$2$22) {
    _inherits(Comment23, Node$2$22);
    function Comment23(defaults) {
      var _this;
      _this = Node$2$22.call(this, defaults) || this;
      _this.type = "comment";
      return _this;
    }
    return Comment23;
  })(Node$2$2);
  var comment$2 = Comment$4$2;
  Comment$4$2.default = Comment$4$2;
  var isClean$1$2 = symbols$2.isClean;
  var my$1$2 = symbols$2.my;
  var Declaration$3$2 = declaration$2;
  var Comment$3$2 = comment$2;
  var Node$1$2 = node$2;
  var parse$4$2;
  var Rule$4$2;
  var AtRule$4$2;
  var Root$6$2;
  function cleanSource$2(nodes) {
    return nodes.map(function(i22) {
      if (i22.nodes) i22.nodes = cleanSource$2(i22.nodes);
      delete i22.source;
      return i22;
    });
  }
  function markDirtyUp$2(node2) {
    node2[isClean$1$2] = false;
    if (node2.proxyOf.nodes) {
      for (var _iterator = _create_for_of_iterator_helper_loose(node2.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var i22 = _step.value;
        markDirtyUp$2(i22);
      }
    }
  }
  var Container$7$2 = /* @__PURE__ */ (function(Node$1$22) {
    _inherits(Container23, Node$1$22);
    function Container23() {
      return Node$1$22.apply(this, arguments) || this;
    }
    var _proto = Container23.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.last);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.push(node2);
        }
      }
      this.markDirty();
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      Node$1$22.prototype.cleanRaws.call(this, keepBetween);
      if (this.nodes) {
        for (var _iterator = _create_for_of_iterator_helper_loose(this.nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          node2.cleanRaws(keepBetween);
        }
      }
    };
    _proto.each = function each(callback) {
      if (!this.proxyOf.nodes) return void 0;
      var iterator = this.getIterator();
      var index2, result2;
      while (this.indexes[iterator] < this.proxyOf.nodes.length) {
        index2 = this.indexes[iterator];
        result2 = callback(this.proxyOf.nodes[index2], index2);
        if (result2 === false) break;
        this.indexes[iterator] += 1;
      }
      delete this.indexes[iterator];
      return result2;
    };
    _proto.every = function every(condition) {
      return this.nodes.every(condition);
    };
    _proto.getIterator = function getIterator() {
      if (!this.lastEach) this.lastEach = 0;
      if (!this.indexes) this.indexes = {};
      this.lastEach += 1;
      var iterator = this.lastEach;
      this.indexes[iterator] = 0;
      return iterator;
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (!node2[prop]) {
            return node2[prop];
          } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
            return function() {
              for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
                args[_key] = arguments[_key];
              }
              var _node2;
              return (_node2 = node2)[prop].apply(_node2, [].concat(args.map(function(i22) {
                if (typeof i22 === "function") {
                  return function(child, index2) {
                    return i22(child.toProxy(), index2);
                  };
                } else {
                  return i22;
                }
              })));
            };
          } else if (prop === "every" || prop === "some") {
            return function(cb) {
              return node2[prop](function(child) {
                for (var _len = arguments.length, other = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
                  other[_key - 1] = arguments[_key];
                }
                return cb.apply(void 0, [].concat([
                  child.toProxy()
                ], other));
              });
            };
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else if (prop === "nodes") {
            return node2.nodes.map(function(i22) {
              return i22.toProxy();
            });
          } else if (prop === "first" || prop === "last") {
            return node2[prop].toProxy();
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "name" || prop === "params" || prop === "selector") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.index = function index2(child) {
      if (typeof child === "number") return child;
      if (child.proxyOf) child = child.proxyOf;
      return this.proxyOf.nodes.indexOf(child);
    };
    _proto.insertAfter = function insertAfter(exist, add) {
      var existIndex = this.index(exist);
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex]).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex + 1, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex < index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.insertBefore = function insertBefore(exist, add) {
      var existIndex = this.index(exist);
      var type = existIndex === 0 ? "prepend" : false;
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex], type).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex <= index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.normalize = function normalize(nodes, sample) {
      var _this = this;
      if (typeof nodes === "string") {
        nodes = cleanSource$2(parse$4$2(nodes).nodes);
      } else if (typeof nodes === "undefined") {
        nodes = [];
      } else if (Array.isArray(nodes)) {
        nodes = nodes.slice(0);
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var i22 = _step.value;
          if (i22.parent) i22.parent.removeChild(i22, "ignore");
        }
      } else if (nodes.type === "root" && this.type !== "document") {
        nodes = nodes.nodes.slice(0);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var i21 = _step1.value;
          if (i21.parent) i21.parent.removeChild(i21, "ignore");
        }
      } else if (nodes.type) {
        nodes = [
          nodes
        ];
      } else if (nodes.prop) {
        if (typeof nodes.value === "undefined") {
          throw new Error("Value field is missed in node creation");
        } else if (typeof nodes.value !== "string") {
          nodes.value = String(nodes.value);
        }
        nodes = [
          new Declaration$3$2(nodes)
        ];
      } else if (nodes.selector) {
        nodes = [
          new Rule$4$2(nodes)
        ];
      } else if (nodes.name) {
        nodes = [
          new AtRule$4$2(nodes)
        ];
      } else if (nodes.text) {
        nodes = [
          new Comment$3$2(nodes)
        ];
      } else {
        throw new Error("Unknown node type in node creation");
      }
      var processed = nodes.map(function(i23) {
        if (!i23[my$1$2]) Container23.rebuild(i23);
        i23 = i23.proxyOf;
        if (i23.parent) i23.parent.removeChild(i23);
        if (i23[isClean$1$2]) markDirtyUp$2(i23);
        if (typeof i23.raws.before === "undefined") {
          if (sample && typeof sample.raws.before !== "undefined") {
            i23.raws.before = sample.raws.before.replace(/\S/g, "");
          }
        }
        i23.parent = _this.proxyOf;
        return i23;
      });
      return processed;
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      children = children.reverse();
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.first, "prepend").reverse();
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.unshift(node2);
        }
        for (var id in this.indexes) {
          this.indexes[id] = this.indexes[id] + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.push = function push(child) {
      child.parent = this;
      this.proxyOf.nodes.push(child);
      return this;
    };
    _proto.removeAll = function removeAll() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        node2.parent = void 0;
      }
      this.proxyOf.nodes = [];
      this.markDirty();
      return this;
    };
    _proto.removeChild = function removeChild(child) {
      child = this.index(child);
      this.proxyOf.nodes[child].parent = void 0;
      this.proxyOf.nodes.splice(child, 1);
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (index2 >= child) {
          this.indexes[id] = index2 - 1;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.replaceValues = function replaceValues(pattern, opts, callback) {
      if (!callback) {
        callback = opts;
        opts = {};
      }
      this.walkDecls(function(decl) {
        if (opts.props && !opts.props.includes(decl.prop)) return;
        if (opts.fast && !decl.value.includes(opts.fast)) return;
        decl.value = decl.value.replace(pattern, callback);
      });
      this.markDirty();
      return this;
    };
    _proto.some = function some(condition) {
      return this.nodes.some(condition);
    };
    _proto.walk = function walk(callback) {
      return this.each(function(child, i22) {
        var result2;
        try {
          result2 = callback(child, i22);
        } catch (e22) {
          throw child.addToError(e22);
        }
        if (result2 !== false && child.walk) {
          result2 = child.walk(callback);
        }
        return result2;
      });
    };
    _proto.walkAtRules = function walkAtRules(name5, callback) {
      if (!callback) {
        callback = name5;
        return this.walk(function(child, i22) {
          if (child.type === "atrule") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(name5, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "atrule" && name5.test(child.name)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "atrule" && child.name === name5) {
          return callback(child, i22);
        }
      });
    };
    _proto.walkComments = function walkComments(callback) {
      return this.walk(function(child, i22) {
        if (child.type === "comment") {
          return callback(child, i22);
        }
      });
    };
    _proto.walkDecls = function walkDecls(prop, callback) {
      if (!callback) {
        callback = prop;
        return this.walk(function(child, i22) {
          if (child.type === "decl") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(prop, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "decl" && prop.test(child.prop)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "decl" && child.prop === prop) {
          return callback(child, i22);
        }
      });
    };
    _proto.walkRules = function walkRules(selector, callback) {
      if (!callback) {
        callback = selector;
        return this.walk(function(child, i22) {
          if (child.type === "rule") {
            return callback(child, i22);
          }
        });
      }
      if (_instanceof(selector, RegExp)) {
        return this.walk(function(child, i22) {
          if (child.type === "rule" && selector.test(child.selector)) {
            return callback(child, i22);
          }
        });
      }
      return this.walk(function(child, i22) {
        if (child.type === "rule" && child.selector === selector) {
          return callback(child, i22);
        }
      });
    };
    _create_class(Container23, [
      {
        key: "first",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[0];
        }
      },
      {
        key: "last",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
        }
      }
    ]);
    return Container23;
  })(Node$1$2);
  Container$7$2.registerParse = function(dependant) {
    parse$4$2 = dependant;
  };
  Container$7$2.registerRule = function(dependant) {
    Rule$4$2 = dependant;
  };
  Container$7$2.registerAtRule = function(dependant) {
    AtRule$4$2 = dependant;
  };
  Container$7$2.registerRoot = function(dependant) {
    Root$6$2 = dependant;
  };
  var container$2 = Container$7$2;
  Container$7$2.default = Container$7$2;
  Container$7$2.rebuild = function(node2) {
    if (node2.type === "atrule") {
      Object.setPrototypeOf(node2, AtRule$4$2.prototype);
    } else if (node2.type === "rule") {
      Object.setPrototypeOf(node2, Rule$4$2.prototype);
    } else if (node2.type === "decl") {
      Object.setPrototypeOf(node2, Declaration$3$2.prototype);
    } else if (node2.type === "comment") {
      Object.setPrototypeOf(node2, Comment$3$2.prototype);
    } else if (node2.type === "root") {
      Object.setPrototypeOf(node2, Root$6$2.prototype);
    }
    node2[my$1$2] = true;
    if (node2.nodes) {
      node2.nodes.forEach(function(child) {
        Container$7$2.rebuild(child);
      });
    }
  };
  var Container$6$2 = container$2;
  var LazyResult$4$2;
  var Processor$3$2;
  var Document$3$2 = /* @__PURE__ */ (function(Container$6$22) {
    _inherits(Document23, Container$6$22);
    function Document23(defaults) {
      var _this;
      _this = Container$6$22.call(this, _extends({
        type: "document"
      }, defaults)) || this;
      if (!_this.nodes) {
        _this.nodes = [];
      }
      return _this;
    }
    var _proto = Document23.prototype;
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$4$2(new Processor$3$2(), this, opts);
      return lazy.stringify();
    };
    return Document23;
  })(Container$6$2);
  Document$3$2.registerLazyResult = function(dependant) {
    LazyResult$4$2 = dependant;
  };
  Document$3$2.registerProcessor = function(dependant) {
    Processor$3$2 = dependant;
  };
  var document$1$2 = Document$3$2;
  Document$3$2.default = Document$3$2;
  var printed$2 = {};
  var warnOnce$2$2 = function warnOnce2(message) {
    if (printed$2[message]) return;
    printed$2[message] = true;
    if (typeof console !== "undefined" && console.warn) {
      console.warn(message);
    }
  };
  var Warning$2$2 = /* @__PURE__ */ (function() {
    function Warning23(text, opts) {
      if (opts === void 0) opts = {};
      this.type = "warning";
      this.text = text;
      if (opts.node && opts.node.source) {
        var range = opts.node.rangeBy(opts);
        this.line = range.start.line;
        this.column = range.start.column;
        this.endLine = range.end.line;
        this.endColumn = range.end.column;
      }
      for (var opt in opts) this[opt] = opts[opt];
    }
    var _proto = Warning23.prototype;
    _proto.toString = function toString2() {
      if (this.node) {
        return this.node.error(this.text, {
          index: this.index,
          plugin: this.plugin,
          word: this.word
        }).message;
      }
      if (this.plugin) {
        return this.plugin + ": " + this.text;
      }
      return this.text;
    };
    return Warning23;
  })();
  var warning$2 = Warning$2$2;
  Warning$2$2.default = Warning$2$2;
  var Warning$1$2 = warning$2;
  var Result$3$2 = /* @__PURE__ */ (function() {
    function Result23(processor2, root2, opts) {
      this.processor = processor2;
      this.messages = [];
      this.root = root2;
      this.opts = opts;
      this.css = void 0;
      this.map = void 0;
    }
    var _proto = Result23.prototype;
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.warn = function warn(text, opts) {
      if (opts === void 0) opts = {};
      if (!opts.plugin) {
        if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
          opts.plugin = this.lastPlugin.postcssPlugin;
        }
      }
      var warning2 = new Warning$1$2(text, opts);
      this.messages.push(warning2);
      return warning2;
    };
    _proto.warnings = function warnings() {
      return this.messages.filter(function(i22) {
        return i22.type === "warning";
      });
    };
    _create_class(Result23, [
      {
        key: "content",
        get: function get() {
          return this.css;
        }
      }
    ]);
    return Result23;
  })();
  var result$2 = Result$3$2;
  Result$3$2.default = Result$3$2;
  var SINGLE_QUOTE$2 = "'".charCodeAt(0);
  var DOUBLE_QUOTE$2 = '"'.charCodeAt(0);
  var BACKSLASH$2 = "\\".charCodeAt(0);
  var SLASH$2 = "/".charCodeAt(0);
  var NEWLINE$2 = "\n".charCodeAt(0);
  var SPACE$2 = " ".charCodeAt(0);
  var FEED$2 = "\f".charCodeAt(0);
  var TAB$2 = "	".charCodeAt(0);
  var CR$2 = "\r".charCodeAt(0);
  var OPEN_SQUARE$2 = "[".charCodeAt(0);
  var CLOSE_SQUARE$2 = "]".charCodeAt(0);
  var OPEN_PARENTHESES$2 = "(".charCodeAt(0);
  var CLOSE_PARENTHESES$2 = ")".charCodeAt(0);
  var OPEN_CURLY$2 = "{".charCodeAt(0);
  var CLOSE_CURLY$2 = "}".charCodeAt(0);
  var SEMICOLON$2 = ";".charCodeAt(0);
  var ASTERISK$2 = "*".charCodeAt(0);
  var COLON$2 = ":".charCodeAt(0);
  var AT$2 = "@".charCodeAt(0);
  var RE_AT_END$2 = /[\t\n\f\r "#'()/;[\\\]{}]/g;
  var RE_WORD_END$2 = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
  var RE_BAD_BRACKET$2 = /.[\r\n"'(/\\]/;
  var RE_HEX_ESCAPE$2 = /[\da-f]/i;
  var tokenize$2 = function tokenizer2(input2, options) {
    if (options === void 0) options = {};
    var css = input2.css.valueOf();
    var ignore = options.ignoreErrors;
    var code, next, quote, content, escape;
    var escaped, escapePos, prev, n22, currentToken;
    var length = css.length;
    var pos = 0;
    var buffer = [];
    var returned = [];
    function position() {
      return pos;
    }
    function unclosed(what) {
      throw input2.error("Unclosed " + what, pos);
    }
    function endOfFile() {
      return returned.length === 0 && pos >= length;
    }
    function nextToken(opts) {
      if (returned.length) return returned.pop();
      if (pos >= length) return;
      var ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
      code = css.charCodeAt(pos);
      switch (code) {
        case NEWLINE$2:
        case SPACE$2:
        case TAB$2:
        case CR$2:
        case FEED$2: {
          next = pos;
          do {
            next += 1;
            code = css.charCodeAt(next);
          } while (code === SPACE$2 || code === NEWLINE$2 || code === TAB$2 || code === CR$2 || code === FEED$2);
          currentToken = [
            "space",
            css.slice(pos, next)
          ];
          pos = next - 1;
          break;
        }
        case OPEN_SQUARE$2:
        case CLOSE_SQUARE$2:
        case OPEN_CURLY$2:
        case CLOSE_CURLY$2:
        case COLON$2:
        case SEMICOLON$2:
        case CLOSE_PARENTHESES$2: {
          var controlChar = String.fromCharCode(code);
          currentToken = [
            controlChar,
            controlChar,
            pos
          ];
          break;
        }
        case OPEN_PARENTHESES$2: {
          prev = buffer.length ? buffer.pop()[1] : "";
          n22 = css.charCodeAt(pos + 1);
          if (prev === "url" && n22 !== SINGLE_QUOTE$2 && n22 !== DOUBLE_QUOTE$2 && n22 !== SPACE$2 && n22 !== NEWLINE$2 && n22 !== TAB$2 && n22 !== FEED$2 && n22 !== CR$2) {
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(")", next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos;
                  break;
                } else {
                  unclosed("bracket");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH$2) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = [
              "brackets",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            next = css.indexOf(")", pos + 1);
            content = css.slice(pos, next + 1);
            if (next === -1 || RE_BAD_BRACKET$2.test(content)) {
              currentToken = [
                "(",
                "(",
                pos
              ];
            } else {
              currentToken = [
                "brackets",
                content,
                pos,
                next
              ];
              pos = next;
            }
          }
          break;
        }
        case SINGLE_QUOTE$2:
        case DOUBLE_QUOTE$2: {
          quote = code === SINGLE_QUOTE$2 ? "'" : '"';
          next = pos;
          do {
            escaped = false;
            next = css.indexOf(quote, next + 1);
            if (next === -1) {
              if (ignore || ignoreUnclosed) {
                next = pos + 1;
                break;
              } else {
                unclosed("string");
              }
            }
            escapePos = next;
            while (css.charCodeAt(escapePos - 1) === BACKSLASH$2) {
              escapePos -= 1;
              escaped = !escaped;
            }
          } while (escaped);
          currentToken = [
            "string",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case AT$2: {
          RE_AT_END$2.lastIndex = pos + 1;
          RE_AT_END$2.test(css);
          if (RE_AT_END$2.lastIndex === 0) {
            next = css.length - 1;
          } else {
            next = RE_AT_END$2.lastIndex - 2;
          }
          currentToken = [
            "at-word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case BACKSLASH$2: {
          next = pos;
          escape = true;
          while (css.charCodeAt(next + 1) === BACKSLASH$2) {
            next += 1;
            escape = !escape;
          }
          code = css.charCodeAt(next + 1);
          if (escape && code !== SLASH$2 && code !== SPACE$2 && code !== NEWLINE$2 && code !== TAB$2 && code !== CR$2 && code !== FEED$2) {
            next += 1;
            if (RE_HEX_ESCAPE$2.test(css.charAt(next))) {
              while (RE_HEX_ESCAPE$2.test(css.charAt(next + 1))) {
                next += 1;
              }
              if (css.charCodeAt(next + 1) === SPACE$2) {
                next += 1;
              }
            }
          }
          currentToken = [
            "word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        default: {
          if (code === SLASH$2 && css.charCodeAt(pos + 1) === ASTERISK$2) {
            next = css.indexOf("*/", pos + 2) + 1;
            if (next === 0) {
              if (ignore || ignoreUnclosed) {
                next = css.length;
              } else {
                unclosed("comment");
              }
            }
            currentToken = [
              "comment",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            RE_WORD_END$2.lastIndex = pos + 1;
            RE_WORD_END$2.test(css);
            if (RE_WORD_END$2.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_WORD_END$2.lastIndex - 2;
            }
            currentToken = [
              "word",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            buffer.push(currentToken);
            pos = next;
          }
          break;
        }
      }
      pos++;
      return currentToken;
    }
    function back(token) {
      returned.push(token);
    }
    return {
      back,
      endOfFile,
      nextToken,
      position
    };
  };
  var Container$5$2 = container$2;
  var AtRule$3$2 = /* @__PURE__ */ (function(Container$5$22) {
    _inherits(AtRule23, Container$5$22);
    function AtRule23(defaults) {
      var _this;
      _this = Container$5$22.call(this, defaults) || this;
      _this.type = "atrule";
      return _this;
    }
    var _proto = AtRule23.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5$2_prototype_append;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5$2_prototype_append = Container$5$22.prototype.append).call.apply(_Container$5$2_prototype_append, [].concat([
        this
      ], children));
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5$2_prototype_prepend;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5$2_prototype_prepend = Container$5$22.prototype.prepend).call.apply(_Container$5$2_prototype_prepend, [].concat([
        this
      ], children));
    };
    return AtRule23;
  })(Container$5$2);
  var atRule$2 = AtRule$3$2;
  AtRule$3$2.default = AtRule$3$2;
  Container$5$2.registerAtRule(AtRule$3$2);
  var Container$4$2 = container$2;
  var LazyResult$3$2;
  var Processor$2$2;
  var Root$5$2 = /* @__PURE__ */ (function(Container$4$22) {
    _inherits(Root23, Container$4$22);
    function Root23(defaults) {
      var _this;
      _this = Container$4$22.call(this, defaults) || this;
      _this.type = "root";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    var _proto = Root23.prototype;
    _proto.normalize = function normalize(child, sample, type) {
      var nodes = Container$4$22.prototype.normalize.call(this, child);
      if (sample) {
        if (type === "prepend") {
          if (this.nodes.length > 1) {
            sample.raws.before = this.nodes[1].raws.before;
          } else {
            delete sample.raws.before;
          }
        } else if (this.first !== sample) {
          for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            node2.raws.before = sample.raws.before;
          }
        }
      }
      return nodes;
    };
    _proto.removeChild = function removeChild(child, ignore) {
      var index2 = this.index(child);
      if (!ignore && index2 === 0 && this.nodes.length > 1) {
        this.nodes[1].raws.before = this.nodes[index2].raws.before;
      }
      return Container$4$22.prototype.removeChild.call(this, child);
    };
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$3$2(new Processor$2$2(), this, opts);
      return lazy.stringify();
    };
    return Root23;
  })(Container$4$2);
  Root$5$2.registerLazyResult = function(dependant) {
    LazyResult$3$2 = dependant;
  };
  Root$5$2.registerProcessor = function(dependant) {
    Processor$2$2 = dependant;
  };
  var root$2 = Root$5$2;
  Root$5$2.default = Root$5$2;
  Container$4$2.registerRoot(Root$5$2);
  var list$2$2 = {
    comma: function comma2(string) {
      return list$2$2.split(string, [
        ","
      ], true);
    },
    space: function space2(string) {
      var spaces = [
        " ",
        "\n",
        "	"
      ];
      return list$2$2.split(string, spaces);
    },
    split: function split2(string, separators, last) {
      var array = [];
      var current = "";
      var split4 = false;
      var func = 0;
      var inQuote = false;
      var prevQuote = "";
      var escape = false;
      for (var _iterator = _create_for_of_iterator_helper_loose(string), _step; !(_step = _iterator()).done; ) {
        var letter = _step.value;
        if (escape) {
          escape = false;
        } else if (letter === "\\") {
          escape = true;
        } else if (inQuote) {
          if (letter === prevQuote) {
            inQuote = false;
          }
        } else if (letter === '"' || letter === "'") {
          inQuote = true;
          prevQuote = letter;
        } else if (letter === "(") {
          func += 1;
        } else if (letter === ")") {
          if (func > 0) func -= 1;
        } else if (func === 0) {
          if (separators.includes(letter)) split4 = true;
        }
        if (split4) {
          if (current !== "") array.push(current.trim());
          current = "";
          split4 = false;
        } else {
          current += letter;
        }
      }
      if (last || current !== "") array.push(current.trim());
      return array;
    }
  };
  var list_1$2 = list$2$2;
  list$2$2.default = list$2$2;
  var Container$3$2 = container$2;
  var list$1$2 = list_1$2;
  var Rule$3$2 = /* @__PURE__ */ (function(Container$3$22) {
    _inherits(Rule23, Container$3$22);
    function Rule23(defaults) {
      var _this;
      _this = Container$3$22.call(this, defaults) || this;
      _this.type = "rule";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    _create_class(Rule23, [
      {
        key: "selectors",
        get: function get() {
          return list$1$2.comma(this.selector);
        },
        set: function set2(values) {
          var match = this.selector ? this.selector.match(/,\s*/) : null;
          var sep2 = match ? match[0] : "," + this.raw("between", "beforeOpen");
          this.selector = values.join(sep2);
        }
      }
    ]);
    return Rule23;
  })(Container$3$2);
  var rule$2 = Rule$3$2;
  Rule$3$2.default = Rule$3$2;
  Container$3$2.registerRule(Rule$3$2);
  var Declaration$2$2 = declaration$2;
  var tokenizer22 = tokenize$2;
  var Comment$2$2 = comment$2;
  var AtRule$2$2 = atRule$2;
  var Root$4$2 = root$2;
  var Rule$2$2 = rule$2;
  var SAFE_COMMENT_NEIGHBOR$2 = {
    empty: true,
    space: true
  };
  function findLastWithPosition$2(tokens) {
    for (var i22 = tokens.length - 1; i22 >= 0; i22--) {
      var token = tokens[i22];
      var pos = token[3] || token[2];
      if (pos) return pos;
    }
  }
  var Parser$1$2 = /* @__PURE__ */ (function() {
    function Parser23(input2) {
      this.input = input2;
      this.root = new Root$4$2();
      this.current = this.root;
      this.spaces = "";
      this.semicolon = false;
      this.createTokenizer();
      this.root.source = {
        input: input2,
        start: {
          column: 1,
          line: 1,
          offset: 0
        }
      };
    }
    var _proto = Parser23.prototype;
    _proto.atrule = function atrule(token) {
      var node2 = new AtRule$2$2();
      node2.name = token[1].slice(1);
      if (node2.name === "") {
        this.unnamedAtrule(node2, token);
      }
      this.init(node2, token[2]);
      var type;
      var prev;
      var shift;
      var last = false;
      var open = false;
      var params = [];
      var brackets = [];
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        type = token[0];
        if (type === "(" || type === "[") {
          brackets.push(type === "(" ? ")" : "]");
        } else if (type === "{" && brackets.length > 0) {
          brackets.push("}");
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
        }
        if (brackets.length === 0) {
          if (type === ";") {
            node2.source.end = this.getPosition(token[2]);
            node2.source.end.offset++;
            this.semicolon = true;
            break;
          } else if (type === "{") {
            open = true;
            break;
          } else if (type === "}") {
            if (params.length > 0) {
              shift = params.length - 1;
              prev = params[shift];
              while (prev && prev[0] === "space") {
                prev = params[--shift];
              }
              if (prev) {
                node2.source.end = this.getPosition(prev[3] || prev[2]);
                node2.source.end.offset++;
              }
            }
            this.end(token);
            break;
          } else {
            params.push(token);
          }
        } else {
          params.push(token);
        }
        if (this.tokenizer.endOfFile()) {
          last = true;
          break;
        }
      }
      node2.raws.between = this.spacesAndCommentsFromEnd(params);
      if (params.length) {
        node2.raws.afterName = this.spacesAndCommentsFromStart(params);
        this.raw(node2, "params", params);
        if (last) {
          token = params[params.length - 1];
          node2.source.end = this.getPosition(token[3] || token[2]);
          node2.source.end.offset++;
          this.spaces = node2.raws.between;
          node2.raws.between = "";
        }
      } else {
        node2.raws.afterName = "";
        node2.params = "";
      }
      if (open) {
        node2.nodes = [];
        this.current = node2;
      }
    };
    _proto.checkMissedSemicolon = function checkMissedSemicolon(tokens) {
      var colon = this.colon(tokens);
      if (colon === false) return;
      var founded = 0;
      var token;
      for (var j4 = colon - 1; j4 >= 0; j4--) {
        token = tokens[j4];
        if (token[0] !== "space") {
          founded += 1;
          if (founded === 2) break;
        }
      }
      throw this.input.error("Missed semicolon", token[0] === "word" ? token[3] + 1 : token[2]);
    };
    _proto.colon = function colon(tokens) {
      var brackets = 0;
      var token, type, prev;
      for (var _iterator = _create_for_of_iterator_helper_loose(tokens.entries()), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, i22 = _step_value[0], element = _step_value[1];
        token = element;
        type = token[0];
        if (type === "(") {
          brackets += 1;
        }
        if (type === ")") {
          brackets -= 1;
        }
        if (brackets === 0 && type === ":") {
          if (!prev) {
            this.doubleColon(token);
          } else if (prev[0] === "word" && prev[1] === "progid") {
            continue;
          } else {
            return i22;
          }
        }
        prev = token;
      }
      return false;
    };
    _proto.comment = function comment2(token) {
      var node2 = new Comment$2$2();
      this.init(node2, token[2]);
      node2.source.end = this.getPosition(token[3] || token[2]);
      node2.source.end.offset++;
      var text = token[1].slice(2, -2);
      if (/^\s*$/.test(text)) {
        node2.text = "";
        node2.raws.left = text;
        node2.raws.right = "";
      } else {
        var match = text.match(/^(\s*)([^]*\S)(\s*)$/);
        node2.text = match[2];
        node2.raws.left = match[1];
        node2.raws.right = match[3];
      }
    };
    _proto.createTokenizer = function createTokenizer() {
      this.tokenizer = tokenizer22(this.input);
    };
    _proto.decl = function decl(tokens, customProperty) {
      var node2 = new Declaration$2$2();
      this.init(node2, tokens[0][2]);
      var last = tokens[tokens.length - 1];
      if (last[0] === ";") {
        this.semicolon = true;
        tokens.pop();
      }
      node2.source.end = this.getPosition(last[3] || last[2] || findLastWithPosition$2(tokens));
      node2.source.end.offset++;
      while (tokens[0][0] !== "word") {
        if (tokens.length === 1) this.unknownWord(tokens);
        node2.raws.before += tokens.shift()[1];
      }
      node2.source.start = this.getPosition(tokens[0][2]);
      node2.prop = "";
      while (tokens.length) {
        var type = tokens[0][0];
        if (type === ":" || type === "space" || type === "comment") {
          break;
        }
        node2.prop += tokens.shift()[1];
      }
      node2.raws.between = "";
      var token;
      while (tokens.length) {
        token = tokens.shift();
        if (token[0] === ":") {
          node2.raws.between += token[1];
          break;
        } else {
          if (token[0] === "word" && /\w/.test(token[1])) {
            this.unknownWord([
              token
            ]);
          }
          node2.raws.between += token[1];
        }
      }
      if (node2.prop[0] === "_" || node2.prop[0] === "*") {
        node2.raws.before += node2.prop[0];
        node2.prop = node2.prop.slice(1);
      }
      var firstSpaces = [];
      var next;
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        firstSpaces.push(tokens.shift());
      }
      this.precheckMissedSemicolon(tokens);
      for (var i22 = tokens.length - 1; i22 >= 0; i22--) {
        token = tokens[i22];
        if (token[1].toLowerCase() === "!important") {
          node2.important = true;
          var string = this.stringFrom(tokens, i22);
          string = this.spacesFromEnd(tokens) + string;
          if (string !== " !important") node2.raws.important = string;
          break;
        } else if (token[1].toLowerCase() === "important") {
          var cache = tokens.slice(0);
          var str = "";
          for (var j4 = i22; j4 > 0; j4--) {
            var type1 = cache[j4][0];
            if (str.trim().indexOf("!") === 0 && type1 !== "space") {
              break;
            }
            str = cache.pop()[1] + str;
          }
          if (str.trim().indexOf("!") === 0) {
            node2.important = true;
            node2.raws.important = str;
            tokens = cache;
          }
        }
        if (token[0] !== "space" && token[0] !== "comment") {
          break;
        }
      }
      var hasWord = tokens.some(function(i23) {
        return i23[0] !== "space" && i23[0] !== "comment";
      });
      if (hasWord) {
        node2.raws.between += firstSpaces.map(function(i23) {
          return i23[1];
        }).join("");
        firstSpaces = [];
      }
      this.raw(node2, "value", firstSpaces.concat(tokens), customProperty);
      if (node2.value.includes(":") && !customProperty) {
        this.checkMissedSemicolon(tokens);
      }
    };
    _proto.doubleColon = function doubleColon(token) {
      throw this.input.error("Double colon", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    _proto.emptyRule = function emptyRule(token) {
      var node2 = new Rule$2$2();
      this.init(node2, token[2]);
      node2.selector = "";
      node2.raws.between = "";
      this.current = node2;
    };
    _proto.end = function end(token) {
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.semicolon = false;
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.spaces = "";
      if (this.current.parent) {
        this.current.source.end = this.getPosition(token[2]);
        this.current.source.end.offset++;
        this.current = this.current.parent;
      } else {
        this.unexpectedClose(token);
      }
    };
    _proto.endFile = function endFile() {
      if (this.current.parent) this.unclosedBlock();
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.root.source.end = this.getPosition(this.tokenizer.position());
    };
    _proto.freeSemicolon = function freeSemicolon(token) {
      this.spaces += token[1];
      if (this.current.nodes) {
        var prev = this.current.nodes[this.current.nodes.length - 1];
        if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
          prev.raws.ownSemicolon = this.spaces;
          this.spaces = "";
        }
      }
    };
    _proto.getPosition = function getPosition(offset) {
      var pos = this.input.fromOffset(offset);
      return {
        column: pos.col,
        line: pos.line,
        offset
      };
    };
    _proto.init = function init(node2, offset) {
      this.current.push(node2);
      node2.source = {
        input: this.input,
        start: this.getPosition(offset)
      };
      node2.raws.before = this.spaces;
      this.spaces = "";
      if (node2.type !== "comment") this.semicolon = false;
    };
    _proto.other = function other(start) {
      var end = false;
      var type = null;
      var colon = false;
      var bracket = null;
      var brackets = [];
      var customProperty = start[1].startsWith("--");
      var tokens = [];
      var token = start;
      while (token) {
        type = token[0];
        tokens.push(token);
        if (type === "(" || type === "[") {
          if (!bracket) bracket = token;
          brackets.push(type === "(" ? ")" : "]");
        } else if (customProperty && colon && type === "{") {
          if (!bracket) bracket = token;
          brackets.push("}");
        } else if (brackets.length === 0) {
          if (type === ";") {
            if (colon) {
              this.decl(tokens, customProperty);
              return;
            } else {
              break;
            }
          } else if (type === "{") {
            this.rule(tokens);
            return;
          } else if (type === "}") {
            this.tokenizer.back(tokens.pop());
            end = true;
            break;
          } else if (type === ":") {
            colon = true;
          }
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
          if (brackets.length === 0) bracket = null;
        }
        token = this.tokenizer.nextToken();
      }
      if (this.tokenizer.endOfFile()) end = true;
      if (brackets.length > 0) this.unclosedBracket(bracket);
      if (end && colon) {
        if (!customProperty) {
          while (tokens.length) {
            token = tokens[tokens.length - 1][0];
            if (token !== "space" && token !== "comment") break;
            this.tokenizer.back(tokens.pop());
          }
        }
        this.decl(tokens, customProperty);
      } else {
        this.unknownWord(tokens);
      }
    };
    _proto.parse = function parse3() {
      var token;
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        switch (token[0]) {
          case "space":
            this.spaces += token[1];
            break;
          case ";":
            this.freeSemicolon(token);
            break;
          case "}":
            this.end(token);
            break;
          case "comment":
            this.comment(token);
            break;
          case "at-word":
            this.atrule(token);
            break;
          case "{":
            this.emptyRule(token);
            break;
          default:
            this.other(token);
            break;
        }
      }
      this.endFile();
    };
    _proto.precheckMissedSemicolon = function precheckMissedSemicolon() {
    };
    _proto.raw = function raw(node2, prop, tokens, customProperty) {
      var token, type;
      var length = tokens.length;
      var value = "";
      var clean = true;
      var next, prev;
      for (var i22 = 0; i22 < length; i22 += 1) {
        token = tokens[i22];
        type = token[0];
        if (type === "space" && i22 === length - 1 && !customProperty) {
          clean = false;
        } else if (type === "comment") {
          prev = tokens[i22 - 1] ? tokens[i22 - 1][0] : "empty";
          next = tokens[i22 + 1] ? tokens[i22 + 1][0] : "empty";
          if (!SAFE_COMMENT_NEIGHBOR$2[prev] && !SAFE_COMMENT_NEIGHBOR$2[next]) {
            if (value.slice(-1) === ",") {
              clean = false;
            } else {
              value += token[1];
            }
          } else {
            clean = false;
          }
        } else {
          value += token[1];
        }
      }
      if (!clean) {
        var raw2 = tokens.reduce(function(all, i23) {
          return all + i23[1];
        }, "");
        node2.raws[prop] = {
          raw: raw2,
          value
        };
      }
      node2[prop] = value;
    };
    _proto.rule = function rule2(tokens) {
      tokens.pop();
      var node2 = new Rule$2$2();
      this.init(node2, tokens[0][2]);
      node2.raws.between = this.spacesAndCommentsFromEnd(tokens);
      this.raw(node2, "selector", tokens);
      this.current = node2;
    };
    _proto.spacesAndCommentsFromEnd = function spacesAndCommentsFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space" && lastTokenType !== "comment") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.spacesAndCommentsFromStart = function spacesAndCommentsFromStart(tokens) {
      var next;
      var spaces = "";
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        spaces += tokens.shift()[1];
      }
      return spaces;
    };
    _proto.spacesFromEnd = function spacesFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.stringFrom = function stringFrom(tokens, from) {
      var result2 = "";
      for (var i22 = from; i22 < tokens.length; i22++) {
        result2 += tokens[i22][1];
      }
      tokens.splice(from, tokens.length - from);
      return result2;
    };
    _proto.unclosedBlock = function unclosedBlock() {
      var pos = this.current.source.start;
      throw this.input.error("Unclosed block", pos.line, pos.column);
    };
    _proto.unclosedBracket = function unclosedBracket(bracket) {
      throw this.input.error("Unclosed bracket", {
        offset: bracket[2]
      }, {
        offset: bracket[2] + 1
      });
    };
    _proto.unexpectedClose = function unexpectedClose(token) {
      throw this.input.error("Unexpected }", {
        offset: token[2]
      }, {
        offset: token[2] + 1
      });
    };
    _proto.unknownWord = function unknownWord(tokens) {
      throw this.input.error("Unknown word", {
        offset: tokens[0][2]
      }, {
        offset: tokens[0][2] + tokens[0][1].length
      });
    };
    _proto.unnamedAtrule = function unnamedAtrule(node2, token) {
      throw this.input.error("At-rule without name", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    return Parser23;
  })();
  var parser$2 = Parser$1$2;
  var Container$2$2 = container$2;
  var Parser22 = parser$2;
  var Input$2$2 = input$2;
  function parse$3$2(css, opts) {
    var input2 = new Input$2$2(css, opts);
    var parser2 = new Parser22(input2);
    try {
      parser2.parse();
    } catch (e22) {
      if (true) {
        if (e22.name === "CssSyntaxError" && opts && opts.from) {
          if (/\.scss$/i.test(opts.from)) {
            e22.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
          } else if (/\.sass/i.test(opts.from)) {
            e22.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
          } else if (/\.less$/i.test(opts.from)) {
            e22.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
          }
        }
      }
      throw e22;
    }
    return parser2.root;
  }
  var parse_1$2 = parse$3$2;
  parse$3$2.default = parse$3$2;
  Container$2$2.registerParse(parse$3$2);
  var isClean$4 = symbols$2.isClean;
  var my$4 = symbols$2.my;
  var MapGenerator$1$2 = mapGenerator$2;
  var stringify$2$2 = stringify_1$2;
  var Container$1$2 = container$2;
  var Document$2$2 = document$1$2;
  var warnOnce$1$2 = warnOnce$2$2;
  var Result$2$2 = result$2;
  var parse$2$2 = parse_1$2;
  var Root$3$2 = root$2;
  var TYPE_TO_CLASS_NAME$2 = {
    atrule: "AtRule",
    comment: "Comment",
    decl: "Declaration",
    document: "Document",
    root: "Root",
    rule: "Rule"
  };
  var PLUGIN_PROPS$2 = {
    AtRule: true,
    AtRuleExit: true,
    Comment: true,
    CommentExit: true,
    Declaration: true,
    DeclarationExit: true,
    Document: true,
    DocumentExit: true,
    Once: true,
    OnceExit: true,
    postcssPlugin: true,
    prepare: true,
    Root: true,
    RootExit: true,
    Rule: true,
    RuleExit: true
  };
  var NOT_VISITORS$2 = {
    Once: true,
    postcssPlugin: true,
    prepare: true
  };
  var CHILDREN$2 = 0;
  function isPromise$2(obj) {
    return (typeof obj === "undefined" ? "undefined" : _type_of(obj)) === "object" && typeof obj.then === "function";
  }
  function getEvents$2(node2) {
    var key = false;
    var type = TYPE_TO_CLASS_NAME$2[node2.type];
    if (node2.type === "decl") {
      key = node2.prop.toLowerCase();
    } else if (node2.type === "atrule") {
      key = node2.name.toLowerCase();
    }
    if (key && node2.append) {
      return [
        type,
        type + "-" + key,
        CHILDREN$2,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (key) {
      return [
        type,
        type + "-" + key,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (node2.append) {
      return [
        type,
        CHILDREN$2,
        type + "Exit"
      ];
    } else {
      return [
        type,
        type + "Exit"
      ];
    }
  }
  function toStack$2(node2) {
    var events;
    if (node2.type === "document") {
      events = [
        "Document",
        CHILDREN$2,
        "DocumentExit"
      ];
    } else if (node2.type === "root") {
      events = [
        "Root",
        CHILDREN$2,
        "RootExit"
      ];
    } else {
      events = getEvents$2(node2);
    }
    return {
      eventIndex: 0,
      events,
      iterator: 0,
      node: node2,
      visitorIndex: 0,
      visitors: []
    };
  }
  function cleanMarks$2(node2) {
    node2[isClean$4] = false;
    if (node2.nodes) node2.nodes.forEach(function(i22) {
      return cleanMarks$2(i22);
    });
    return node2;
  }
  var postcss$2$2 = {};
  var LazyResult$2$2 = /* @__PURE__ */ (function() {
    function LazyResult23(processor2, css, opts) {
      var _this = this;
      this.stringified = false;
      this.processed = false;
      var root2;
      if ((typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && css !== null && (css.type === "root" || css.type === "document")) {
        root2 = cleanMarks$2(css);
      } else if (_instanceof(css, LazyResult23) || _instanceof(css, Result$2$2)) {
        root2 = cleanMarks$2(css.root);
        if (css.map) {
          if (typeof opts.map === "undefined") opts.map = {};
          if (!opts.map.inline) opts.map.inline = false;
          opts.map.prev = css.map;
        }
      } else {
        var parser2 = parse$2$2;
        if (opts.syntax) parser2 = opts.syntax.parse;
        if (opts.parser) parser2 = opts.parser;
        if (parser2.parse) parser2 = parser2.parse;
        try {
          root2 = parser2(css, opts);
        } catch (error) {
          this.processed = true;
          this.error = error;
        }
        if (root2 && !root2[my$4]) {
          Container$1$2.rebuild(root2);
        }
      }
      this.result = new Result$2$2(processor2, root2, opts);
      this.helpers = _extends({}, postcss$2$2, {
        postcss: postcss$2$2,
        result: this.result
      });
      this.plugins = this.processor.plugins.map(function(plugin22) {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.prepare) {
          return _extends({}, plugin22, plugin22.prepare(_this.result));
        } else {
          return plugin22;
        }
      });
    }
    var _proto = LazyResult23.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      if (this.processed) return Promise.resolve(this.result);
      if (!this.processing) {
        this.processing = this.runAsync();
      }
      return this.processing;
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.getAsyncError = function getAsyncError() {
      throw new Error("Use process(css).then(cb) to work with async plugins");
    };
    _proto.handleError = function handleError2(error, node2) {
      var plugin22 = this.result.lastPlugin;
      try {
        if (node2) node2.addToError(error);
        this.error = error;
        if (error.name === "CssSyntaxError" && !error.plugin) {
          error.plugin = plugin22.postcssPlugin;
          error.setMessage();
        } else if (plugin22.postcssVersion) {
          if (true) {
            var pluginName = plugin22.postcssPlugin;
            var pluginVer = plugin22.postcssVersion;
            var runtimeVer = this.result.processor.version;
            var a22 = pluginVer.split(".");
            var b2 = runtimeVer.split(".");
            if (a22[0] !== b2[0] || parseInt(a22[1]) > parseInt(b2[1])) {
              console.error("Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below.");
            }
          }
        }
      } catch (err) {
        if (console && console.error) console.error(err);
      }
      return error;
    };
    _proto.prepareVisitors = function prepareVisitors() {
      var _this = this;
      this.listeners = {};
      var add = function(plugin222, type, cb) {
        if (!_this.listeners[type]) _this.listeners[type] = [];
        _this.listeners[type].push([
          plugin222,
          cb
        ]);
      };
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object") {
          for (var event in plugin22) {
            if (!PLUGIN_PROPS$2[event] && /^[A-Z]/.test(event)) {
              throw new Error("Unknown event " + event + " in " + plugin22.postcssPlugin + ". Try to update PostCSS (" + this.processor.version + " now).");
            }
            if (!NOT_VISITORS$2[event]) {
              if (_type_of(plugin22[event]) === "object") {
                for (var filter in plugin22[event]) {
                  if (filter === "*") {
                    add(plugin22, event, plugin22[event][filter]);
                  } else {
                    add(plugin22, event + "-" + filter.toLowerCase(), plugin22[event][filter]);
                  }
                }
              } else if (typeof plugin22[event] === "function") {
                add(plugin22, event, plugin22[event]);
              }
            }
          }
        }
      }
      this.hasListener = Object.keys(this.listeners).length > 0;
    };
    _proto.runAsync = function runAsync() {
      var _this = this;
      return _async_to_generator(function() {
        var i22, plugin22, promise, error, root2, stack, promise1, e22, node2, _loop, _iterator, _step;
        return _ts_generator(this, function(_state) {
          switch (_state.label) {
            case 0:
              _this.plugin = 0;
              i22 = 0;
              _state.label = 1;
            case 1:
              if (!(i22 < _this.plugins.length)) return [
                3,
                6
              ];
              plugin22 = _this.plugins[i22];
              promise = _this.runOnRoot(plugin22);
              if (!isPromise$2(promise)) return [
                3,
                5
              ];
              _state.label = 2;
            case 2:
              _state.trys.push([
                2,
                4,
                ,
                5
              ]);
              return [
                4,
                promise
              ];
            case 3:
              _state.sent();
              return [
                3,
                5
              ];
            case 4:
              error = _state.sent();
              throw _this.handleError(error);
            case 5:
              i22++;
              return [
                3,
                1
              ];
            case 6:
              _this.prepareVisitors();
              if (!_this.hasListener) return [
                3,
                18
              ];
              root2 = _this.result.root;
              _state.label = 7;
            case 7:
              if (!!root2[isClean$4]) return [
                3,
                14
              ];
              root2[isClean$4] = true;
              stack = [
                toStack$2(root2)
              ];
              _state.label = 8;
            case 8:
              if (!(stack.length > 0)) return [
                3,
                13
              ];
              promise1 = _this.visitTick(stack);
              if (!isPromise$2(promise1)) return [
                3,
                12
              ];
              _state.label = 9;
            case 9:
              _state.trys.push([
                9,
                11,
                ,
                12
              ]);
              return [
                4,
                promise1
              ];
            case 10:
              _state.sent();
              return [
                3,
                12
              ];
            case 11:
              e22 = _state.sent();
              node2 = stack[stack.length - 1].node;
              throw _this.handleError(e22, node2);
            case 12:
              return [
                3,
                8
              ];
            case 13:
              return [
                3,
                7
              ];
            case 14:
              if (!_this.listeners.OnceExit) return [
                3,
                18
              ];
              _loop = function() {
                var _step_value, plugin222, visitor, roots, e23;
                return _ts_generator(this, function(_state2) {
                  switch (_state2.label) {
                    case 0:
                      _step_value = _step.value, plugin222 = _step_value[0], visitor = _step_value[1];
                      _this.result.lastPlugin = plugin222;
                      _state2.label = 1;
                    case 1:
                      _state2.trys.push([
                        1,
                        6,
                        ,
                        7
                      ]);
                      if (!(root2.type === "document")) return [
                        3,
                        3
                      ];
                      roots = root2.nodes.map(function(subRoot) {
                        return visitor(subRoot, _this.helpers);
                      });
                      return [
                        4,
                        Promise.all(roots)
                      ];
                    case 2:
                      _state2.sent();
                      return [
                        3,
                        5
                      ];
                    case 3:
                      return [
                        4,
                        visitor(root2, _this.helpers)
                      ];
                    case 4:
                      _state2.sent();
                      _state2.label = 5;
                    case 5:
                      return [
                        3,
                        7
                      ];
                    case 6:
                      e23 = _state2.sent();
                      throw _this.handleError(e23);
                    case 7:
                      return [
                        2
                      ];
                  }
                });
              };
              _iterator = _create_for_of_iterator_helper_loose(_this.listeners.OnceExit);
              _state.label = 15;
            case 15:
              if (!!(_step = _iterator()).done) return [
                3,
                18
              ];
              return [
                5,
                _ts_values(_loop())
              ];
            case 16:
              _state.sent();
              _state.label = 17;
            case 17:
              return [
                3,
                15
              ];
            case 18:
              _this.processed = true;
              return [
                2,
                _this.stringify()
              ];
          }
        });
      })();
    };
    _proto.runOnRoot = function runOnRoot(plugin22) {
      var _this = this;
      this.result.lastPlugin = plugin22;
      try {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.Once) {
          if (this.result.root.type === "document") {
            var roots = this.result.root.nodes.map(function(root2) {
              return plugin22.Once(root2, _this.helpers);
            });
            if (isPromise$2(roots[0])) {
              return Promise.all(roots);
            }
            return roots;
          }
          return plugin22.Once(this.result.root, this.helpers);
        } else if (typeof plugin22 === "function") {
          return plugin22(this.result.root, this.result);
        }
      } catch (error) {
        throw this.handleError(error);
      }
    };
    _proto.stringify = function stringify2() {
      if (this.error) throw this.error;
      if (this.stringified) return this.result;
      this.stringified = true;
      this.sync();
      var opts = this.result.opts;
      var str = stringify$2$2;
      if (opts.syntax) str = opts.syntax.stringify;
      if (opts.stringifier) str = opts.stringifier;
      if (str.stringify) str = str.stringify;
      var map = new MapGenerator$1$2(str, this.result.root, this.result.opts);
      var data = map.generate();
      this.result.css = data[0];
      this.result.map = data[1];
      return this.result;
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      if (this.processed) return this.result;
      this.processed = true;
      if (this.processing) {
        throw this.getAsyncError();
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        var promise = this.runOnRoot(plugin22);
        if (isPromise$2(promise)) {
          throw this.getAsyncError();
        }
      }
      this.prepareVisitors();
      if (this.hasListener) {
        var root2 = this.result.root;
        while (!root2[isClean$4]) {
          root2[isClean$4] = true;
          this.walkSync(root2);
        }
        if (this.listeners.OnceExit) {
          if (root2.type === "document") {
            for (var _iterator1 = _create_for_of_iterator_helper_loose(root2.nodes), _step1; !(_step1 = _iterator1()).done; ) {
              var subRoot = _step1.value;
              this.visitSync(this.listeners.OnceExit, subRoot);
            }
          } else {
            this.visitSync(this.listeners.OnceExit, root2);
          }
        }
      }
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this.opts)) {
          warnOnce$1$2("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.visitSync = function visitSync(visitors, node2) {
      for (var _iterator = _create_for_of_iterator_helper_loose(visitors), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, plugin22 = _step_value[0], visitor = _step_value[1];
        this.result.lastPlugin = plugin22;
        var promise = void 0;
        try {
          promise = visitor(node2, this.helpers);
        } catch (e22) {
          throw this.handleError(e22, node2.proxyOf);
        }
        if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
          return true;
        }
        if (isPromise$2(promise)) {
          throw this.getAsyncError();
        }
      }
    };
    _proto.visitTick = function visitTick(stack) {
      var visit2 = stack[stack.length - 1];
      var node2 = visit2.node, visitors = visit2.visitors;
      if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
        stack.pop();
        return;
      }
      if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
        var _visitors_visit2_visitorIndex = visitors[visit2.visitorIndex], plugin22 = _visitors_visit2_visitorIndex[0], visitor = _visitors_visit2_visitorIndex[1];
        visit2.visitorIndex += 1;
        if (visit2.visitorIndex === visitors.length) {
          visit2.visitors = [];
          visit2.visitorIndex = 0;
        }
        this.result.lastPlugin = plugin22;
        try {
          return visitor(node2.toProxy(), this.helpers);
        } catch (e22) {
          throw this.handleError(e22, node2);
        }
      }
      if (visit2.iterator !== 0) {
        var iterator = visit2.iterator;
        var child;
        while (child = node2.nodes[node2.indexes[iterator]]) {
          node2.indexes[iterator] += 1;
          if (!child[isClean$4]) {
            child[isClean$4] = true;
            stack.push(toStack$2(child));
            return;
          }
        }
        visit2.iterator = 0;
        delete node2.indexes[iterator];
      }
      var events = visit2.events;
      while (visit2.eventIndex < events.length) {
        var event = events[visit2.eventIndex];
        visit2.eventIndex += 1;
        if (event === CHILDREN$2) {
          if (node2.nodes && node2.nodes.length) {
            node2[isClean$4] = true;
            visit2.iterator = node2.getIterator();
          }
          return;
        } else if (this.listeners[event]) {
          visit2.visitors = this.listeners[event];
          return;
        }
      }
      stack.pop();
    };
    _proto.walkSync = function walkSync(node2) {
      var _this = this;
      node2[isClean$4] = true;
      var events = getEvents$2(node2);
      for (var _iterator = _create_for_of_iterator_helper_loose(events), _step; !(_step = _iterator()).done; ) {
        var event = _step.value;
        if (event === CHILDREN$2) {
          if (node2.nodes) {
            node2.each(function(child) {
              if (!child[isClean$4]) _this.walkSync(child);
            });
          }
        } else {
          var visitors = this.listeners[event];
          if (visitors) {
            if (this.visitSync(visitors, node2.toProxy())) return;
          }
        }
      }
    };
    _proto.warnings = function warnings() {
      return this.sync().warnings();
    };
    _create_class(LazyResult23, [
      {
        key: "content",
        get: function get() {
          return this.stringify().content;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.stringify().css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.stringify().map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return this.sync().messages;
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          return this.sync().root;
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "LazyResult";
        }
      }
    ]);
    return LazyResult23;
  })();
  LazyResult$2$2.registerPostcss = function(dependant) {
    postcss$2$2 = dependant;
  };
  var lazyResult$2 = LazyResult$2$2;
  LazyResult$2$2.default = LazyResult$2$2;
  Root$3$2.registerLazyResult(LazyResult$2$2);
  Document$2$2.registerLazyResult(LazyResult$2$2);
  var MapGenerator22 = mapGenerator$2;
  var stringify$1$2 = stringify_1$2;
  var warnOnce22 = warnOnce$2$2;
  var parse$1$2 = parse_1$2;
  var Result$1$2 = result$2;
  var NoWorkResult$1$2 = /* @__PURE__ */ (function() {
    function NoWorkResult23(processor2, css, opts) {
      css = css.toString();
      this.stringified = false;
      this._processor = processor2;
      this._css = css;
      this._opts = opts;
      this._map = void 0;
      var root2;
      var str = stringify$1$2;
      this.result = new Result$1$2(this._processor, root2, this._opts);
      this.result.css = css;
      var self2 = this;
      Object.defineProperty(this.result, "root", {
        get: function get() {
          return self2.root;
        }
      });
      var map = new MapGenerator22(str, root2, this._opts, css);
      if (map.isMap()) {
        var _map_generate = map.generate(), generatedCSS = _map_generate[0], generatedMap = _map_generate[1];
        if (generatedCSS) {
          this.result.css = generatedCSS;
        }
        if (generatedMap) {
          this.result.map = generatedMap;
        }
      } else {
        map.clearAnnotation();
        this.result.css = map.css;
      }
    }
    var _proto = NoWorkResult23.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      return Promise.resolve(this.result);
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this._opts)) {
          warnOnce22("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this._css;
    };
    _proto.warnings = function warnings() {
      return [];
    };
    _create_class(NoWorkResult23, [
      {
        key: "content",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.result.map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return [];
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          if (this._root) {
            return this._root;
          }
          var root2;
          var parser2 = parse$1$2;
          try {
            root2 = parser2(this._css, this._opts);
          } catch (error) {
            this.error = error;
          }
          if (this.error) {
            throw this.error;
          } else {
            this._root = root2;
            return root2;
          }
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "NoWorkResult";
        }
      }
    ]);
    return NoWorkResult23;
  })();
  var noWorkResult$2 = NoWorkResult$1$2;
  NoWorkResult$1$2.default = NoWorkResult$1$2;
  var NoWorkResult22 = noWorkResult$2;
  var LazyResult$1$2 = lazyResult$2;
  var Document$1$2 = document$1$2;
  var Root$2$2 = root$2;
  var Processor$1$2 = /* @__PURE__ */ (function() {
    function Processor23(plugins) {
      if (plugins === void 0) plugins = [];
      this.version = "8.4.38";
      this.plugins = this.normalize(plugins);
    }
    var _proto = Processor23.prototype;
    _proto.normalize = function normalize(plugins) {
      var normalized = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(plugins), _step; !(_step = _iterator()).done; ) {
        var i22 = _step.value;
        if (i22.postcss === true) {
          i22 = i22();
        } else if (i22.postcss) {
          i22 = i22.postcss;
        }
        if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && Array.isArray(i22.plugins)) {
          normalized = normalized.concat(i22.plugins);
        } else if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && i22.postcssPlugin) {
          normalized.push(i22);
        } else if (typeof i22 === "function") {
          normalized.push(i22);
        } else if ((typeof i22 === "undefined" ? "undefined" : _type_of(i22)) === "object" && (i22.parse || i22.stringify)) {
          if (true) {
            throw new Error("PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation.");
          }
        } else {
          throw new Error(i22 + " is not a PostCSS plugin");
        }
      }
      return normalized;
    };
    _proto.process = function process1(css, opts) {
      if (opts === void 0) opts = {};
      if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
        return new NoWorkResult22(this, css, opts);
      } else {
        return new LazyResult$1$2(this, css, opts);
      }
    };
    _proto.use = function use(plugin22) {
      this.plugins = this.plugins.concat(this.normalize([
        plugin22
      ]));
      return this;
    };
    return Processor23;
  })();
  var processor$2 = Processor$1$2;
  Processor$1$2.default = Processor$1$2;
  Root$2$2.registerProcessor(Processor$1$2);
  Document$1$2.registerProcessor(Processor$1$2);
  var Declaration$1$2 = declaration$2;
  var PreviousMap22 = previousMap$2;
  var Comment$1$2 = comment$2;
  var AtRule$1$2 = atRule$2;
  var Input$1$2 = input$2;
  var Root$1$2 = root$2;
  var Rule$1$2 = rule$2;
  function fromJSON$1$2(json, inputs) {
    if (Array.isArray(json)) return json.map(function(n22) {
      return fromJSON$1$2(n22);
    });
    var ownInputs = json.inputs, defaults = _object_without_properties_loose(json, [
      "inputs"
    ]);
    if (ownInputs) {
      inputs = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(ownInputs), _step; !(_step = _iterator()).done; ) {
        var input2 = _step.value;
        var inputHydrated = _extends({}, input2, {
          __proto__: Input$1$2.prototype
        });
        if (inputHydrated.map) {
          inputHydrated.map = _extends({}, inputHydrated.map, {
            __proto__: PreviousMap22.prototype
          });
        }
        inputs.push(inputHydrated);
      }
    }
    if (defaults.nodes) {
      defaults.nodes = json.nodes.map(function(n22) {
        return fromJSON$1$2(n22, inputs);
      });
    }
    if (defaults.source) {
      var _defaults_source = defaults.source, inputId = _defaults_source.inputId, source = _object_without_properties_loose(_defaults_source, [
        "inputId"
      ]);
      defaults.source = source;
      if (inputId != null) {
        defaults.source.input = inputs[inputId];
      }
    }
    if (defaults.type === "root") {
      return new Root$1$2(defaults);
    } else if (defaults.type === "decl") {
      return new Declaration$1$2(defaults);
    } else if (defaults.type === "rule") {
      return new Rule$1$2(defaults);
    } else if (defaults.type === "comment") {
      return new Comment$1$2(defaults);
    } else if (defaults.type === "atrule") {
      return new AtRule$1$2(defaults);
    } else {
      throw new Error("Unknown node type: " + json.type);
    }
  }
  var fromJSON_1$2 = fromJSON$1$2;
  fromJSON$1$2.default = fromJSON$1$2;
  var CssSyntaxError22 = cssSyntaxError$2;
  var Declaration22 = declaration$2;
  var LazyResult22 = lazyResult$2;
  var Container22 = container$2;
  var Processor22 = processor$2;
  var stringify$6 = stringify_1$2;
  var fromJSON$3 = fromJSON_1$2;
  var Document222 = document$1$2;
  var Warning22 = warning$2;
  var Comment22 = comment$2;
  var AtRule22 = atRule$2;
  var Result22 = result$2;
  var Input22 = input$2;
  var parse$6 = parse_1$2;
  var list$4 = list_1$2;
  var Rule22 = rule$2;
  var Root22 = root$2;
  var Node22 = node$2;
  function postcss$4() {
    for (var _len = arguments.length, plugins = new Array(_len), _key = 0; _key < _len; _key++) {
      plugins[_key] = arguments[_key];
    }
    if (plugins.length === 1 && Array.isArray(plugins[0])) {
      plugins = plugins[0];
    }
    return new Processor22(plugins);
  }
  postcss$4.plugin = function plugin2(name5, initializer) {
    var warningPrinted = false;
    function creator() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      if (console && console.warn && !warningPrinted) {
        warningPrinted = true;
        console.warn(name5 + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration");
        if (process.env.LANG && process.env.LANG.startsWith("cn")) {
          console.warn(name5 + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226");
        }
      }
      var transformer = initializer.apply(void 0, [].concat(args));
      transformer.postcssPlugin = name5;
      transformer.postcssVersion = new Processor22().version;
      return transformer;
    }
    var cache;
    Object.defineProperty(creator, "postcss", {
      get: function get() {
        if (!cache) cache = creator();
        return cache;
      }
    });
    creator.process = function(css, processOpts, pluginOpts) {
      return postcss$4([
        creator(pluginOpts)
      ]).process(css, processOpts);
    };
    return creator;
  };
  postcss$4.stringify = stringify$6;
  postcss$4.parse = parse$6;
  postcss$4.fromJSON = fromJSON$3;
  postcss$4.list = list$4;
  postcss$4.comment = function(defaults) {
    return new Comment22(defaults);
  };
  postcss$4.atRule = function(defaults) {
    return new AtRule22(defaults);
  };
  postcss$4.decl = function(defaults) {
    return new Declaration22(defaults);
  };
  postcss$4.rule = function(defaults) {
    return new Rule22(defaults);
  };
  postcss$4.root = function(defaults) {
    return new Root22(defaults);
  };
  postcss$4.document = function(defaults) {
    return new Document222(defaults);
  };
  postcss$4.CssSyntaxError = CssSyntaxError22;
  postcss$4.Declaration = Declaration22;
  postcss$4.Container = Container22;
  postcss$4.Processor = Processor22;
  postcss$4.Document = Document222;
  postcss$4.Comment = Comment22;
  postcss$4.Warning = Warning22;
  postcss$4.AtRule = AtRule22;
  postcss$4.Result = Result22;
  postcss$4.Input = Input22;
  postcss$4.Rule = Rule22;
  postcss$4.Root = Root22;
  postcss$4.Node = Node22;
  LazyResult22.registerPostcss(postcss$4);
  var postcss_1$2 = postcss$4;
  postcss$4.default = postcss$4;
  var postcss$1$2 = /* @__PURE__ */ getDefaultExportFromCjs$2(postcss_1$2);
  postcss$1$2.stringify;
  postcss$1$2.fromJSON;
  postcss$1$2.plugin;
  postcss$1$2.parse;
  postcss$1$2.list;
  postcss$1$2.document;
  postcss$1$2.comment;
  postcss$1$2.atRule;
  postcss$1$2.rule;
  postcss$1$2.decl;
  postcss$1$2.root;
  postcss$1$2.CssSyntaxError;
  postcss$1$2.Declaration;
  postcss$1$2.Container;
  postcss$1$2.Processor;
  postcss$1$2.Document;
  postcss$1$2.Comment;
  postcss$1$2.Warning;
  postcss$1$2.AtRule;
  postcss$1$2.Result;
  postcss$1$2.Input;
  postcss$1$2.Rule;
  postcss$1$2.Root;
  postcss$1$2.Node;
  var BaseRRNode = /* @__PURE__ */ (function() {
    function BaseRRNode2() {
      for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
        _args[_key] = arguments[_key];
      }
      __publicField2(this, "parentElement", null);
      __publicField2(this, "parentNode", null);
      __publicField2(this, "ownerDocument");
      __publicField2(this, "firstChild", null);
      __publicField2(this, "lastChild", null);
      __publicField2(this, "previousSibling", null);
      __publicField2(this, "nextSibling", null);
      __publicField2(this, "ELEMENT_NODE", 1);
      __publicField2(this, "TEXT_NODE", 3);
      __publicField2(this, "nodeType");
      __publicField2(this, "nodeName");
      __publicField2(this, "RRNodeType");
    }
    var _proto = BaseRRNode2.prototype;
    _proto.contains = function contains2(node2) {
      if (!_instanceof(node2, BaseRRNode2)) return false;
      else if (node2.ownerDocument !== this.ownerDocument) return false;
      else if (node2 === this) return true;
      while (node2.parentNode) {
        if (node2.parentNode === this) return true;
        node2 = node2.parentNode;
      }
      return false;
    };
    _proto.appendChild = function appendChild(_newChild) {
      throw new Error("RRDomException: Failed to execute 'appendChild' on 'RRNode': This RRNode type does not support this method.");
    };
    _proto.insertBefore = function insertBefore(_newChild, _refChild) {
      throw new Error("RRDomException: Failed to execute 'insertBefore' on 'RRNode': This RRNode type does not support this method.");
    };
    _proto.removeChild = function removeChild(_node) {
      throw new Error("RRDomException: Failed to execute 'removeChild' on 'RRNode': This RRNode type does not support this method.");
    };
    _proto.toString = function toString2() {
      return "RRNode";
    };
    _create_class(BaseRRNode2, [
      {
        key: "childNodes",
        get: function get() {
          var childNodes2 = [];
          var childIterator = this.firstChild;
          while (childIterator) {
            childNodes2.push(childIterator);
            childIterator = childIterator.nextSibling;
          }
          return childNodes2;
        }
      }
    ]);
    return BaseRRNode2;
  })();
  var testableAccessors$2 = {
    Node: [
      "childNodes",
      "parentNode",
      "parentElement",
      "textContent"
    ],
    ShadowRoot: [
      "host",
      "styleSheets"
    ],
    Element: [
      "shadowRoot",
      "querySelector",
      "querySelectorAll"
    ],
    MutationObserver: []
  };
  var testableMethods$2 = {
    Node: [
      "contains",
      "getRootNode"
    ],
    ShadowRoot: [
      "getSelection"
    ],
    Element: [],
    MutationObserver: [
      "constructor"
    ]
  };
  var untaintedBasePrototype$2 = {};
  var isAngularZonePresent$2 = function() {
    return !!globalThis.Zone;
  };
  function getUntaintedPrototype$2(key) {
    if (untaintedBasePrototype$2[key]) return untaintedBasePrototype$2[key];
    var defaultObj = globalThis[key];
    var defaultPrototype = defaultObj.prototype;
    var accessorNames = key in testableAccessors$2 ? testableAccessors$2[key] : void 0;
    var isUntaintedAccessors = Boolean(accessorNames && // @ts-expect-error 2345
    accessorNames.every(function(accessor) {
      var _a2, _b;
      return Boolean((_b = (_a2 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a2.get) == null ? void 0 : _b.toString().includes("[native code]"));
    }));
    var methodNames = key in testableMethods$2 ? testableMethods$2[key] : void 0;
    var isUntaintedMethods = Boolean(methodNames && methodNames.every(
      // @ts-expect-error 2345
      function(method) {
        var _a2;
        return typeof defaultPrototype[method] === "function" && ((_a2 = defaultPrototype[method]) == null ? void 0 : _a2.toString().includes("[native code]"));
      }
    ));
    if (isUntaintedAccessors && isUntaintedMethods && !isAngularZonePresent$2()) {
      untaintedBasePrototype$2[key] = defaultObj.prototype;
      return defaultObj.prototype;
    }
    try {
      var iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      var win2 = iframeEl.contentWindow;
      if (!win2) return defaultObj.prototype;
      var untaintedObject = win2[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype$2[key] = untaintedObject;
    } catch (e3) {
      return defaultPrototype;
    }
  }
  var untaintedAccessorCache$2 = {};
  function getUntaintedAccessor$2(key, instance, accessor) {
    var _a2;
    var cacheKey = key + "." + String(accessor);
    if (untaintedAccessorCache$2[cacheKey]) return untaintedAccessorCache$2[cacheKey].call(instance);
    var untaintedPrototype = getUntaintedPrototype$2(key);
    var untaintedAccessor = (_a2 = Object.getOwnPropertyDescriptor(untaintedPrototype, accessor)) == null ? void 0 : _a2.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache$2[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  var untaintedMethodCache$2 = {};
  function getUntaintedMethod$2(key, instance, method) {
    var cacheKey = key + "." + String(method);
    if (untaintedMethodCache$2[cacheKey]) return untaintedMethodCache$2[cacheKey].bind(instance);
    var untaintedPrototype = getUntaintedPrototype$2(key);
    var untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache$2[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes$2(n22) {
    return getUntaintedAccessor$2("Node", n22, "childNodes");
  }
  function parentNode$2(n22) {
    return getUntaintedAccessor$2("Node", n22, "parentNode");
  }
  function parentElement$2(n22) {
    return getUntaintedAccessor$2("Node", n22, "parentElement");
  }
  function textContent$2(n22) {
    return getUntaintedAccessor$2("Node", n22, "textContent");
  }
  function contains$2(n22, other) {
    return getUntaintedMethod$2("Node", n22, "contains")(other);
  }
  function getRootNode$2(n22) {
    return getUntaintedMethod$2("Node", n22, "getRootNode")();
  }
  function host$2(n22) {
    if (!n22 || !("host" in n22)) return null;
    return getUntaintedAccessor$2("ShadowRoot", n22, "host");
  }
  function styleSheets$2(n22) {
    return n22.styleSheets;
  }
  function shadowRoot$2(n22) {
    if (!n22 || !("shadowRoot" in n22)) return null;
    return getUntaintedAccessor$2("Element", n22, "shadowRoot");
  }
  function querySelector$2(n22, selectors) {
    return getUntaintedAccessor$2("Element", n22, "querySelector")(selectors);
  }
  function querySelectorAll$2(n22, selectors) {
    return getUntaintedAccessor$2("Element", n22, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor$2() {
    return getUntaintedPrototype$2("MutationObserver").constructor;
  }
  function patch$2(source, name5, replacement) {
    try {
      if (!(name5 in source)) {
        return function() {
        };
      }
      var original = source[name5];
      var wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name5] = wrapped;
      return function() {
        source[name5] = original;
      };
    } catch (e3) {
      return function() {
      };
    }
  }
  var index$2 = {
    childNodes: childNodes$2,
    parentNode: parentNode$2,
    parentElement: parentElement$2,
    textContent: textContent$2,
    contains: contains$2,
    getRootNode: getRootNode$2,
    host: host$2,
    styleSheets: styleSheets$2,
    shadowRoot: shadowRoot$2,
    querySelector: querySelector$2,
    querySelectorAll: querySelectorAll$2,
    mutationObserver: mutationObserverCtor$2,
    patch: patch$2
  };
  function on2(type, fn2, target) {
    if (target === void 0) target = document;
    var options = {
      capture: true,
      passive: true
    };
    target.addEventListener(type, fn2, options);
    return function() {
      return target.removeEventListener(type, fn2, options);
    };
  }
  var DEPARTED_MIRROR_ACCESS_WARNING = "Please stop import mirror directly. Instead of that,\r\nnow you can use replayer.getMirror() to access the mirror instance of a replayer,\r\nor you can use record.mirror to access the mirror instance during recording.";
  var _mirror = {
    map: {},
    getId: function getId() {
      console.error(DEPARTED_MIRROR_ACCESS_WARNING);
      return -1;
    },
    getNode: function getNode() {
      console.error(DEPARTED_MIRROR_ACCESS_WARNING);
      return null;
    },
    removeNodeFromMap: function removeNodeFromMap() {
      console.error(DEPARTED_MIRROR_ACCESS_WARNING);
    },
    has: function has() {
      console.error(DEPARTED_MIRROR_ACCESS_WARNING);
      return false;
    },
    reset: function reset() {
      console.error(DEPARTED_MIRROR_ACCESS_WARNING);
    }
  };
  if (typeof window !== "undefined" && window.Proxy && window.Reflect) {
    _mirror = new Proxy(_mirror, {
      get: function get(target, prop, receiver) {
        if (prop === "map") {
          console.error(DEPARTED_MIRROR_ACCESS_WARNING);
        }
        return Reflect.get(target, prop, receiver);
      }
    });
  }
  function throttle(func, wait, options) {
    if (options === void 0) options = {};
    var timeout = null;
    var previous = 0;
    return function() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      var now = Date.now();
      if (!previous && options.leading === false) {
        previous = now;
      }
      var remaining = wait - (now - previous);
      var context = this;
      if (remaining <= 0 || remaining > wait) {
        if (timeout) {
          clearTimeout(timeout);
          timeout = null;
        }
        previous = now;
        func.apply(context, args);
      } else if (!timeout && options.trailing !== false) {
        timeout = setTimeout(function() {
          previous = options.leading === false ? 0 : Date.now();
          timeout = null;
          func.apply(context, args);
        }, remaining);
      }
    };
  }
  function hookSetter(target, key, d3, isRevoked, win2) {
    if (win2 === void 0) win2 = window;
    var original = win2.Object.getOwnPropertyDescriptor(target, key);
    win2.Object.defineProperty(target, key, isRevoked ? d3 : {
      set: function set2(value) {
        var _this = this;
        setTimeout(function() {
          d3.set.call(_this, value);
        }, 0);
        if (original && original.set) {
          original.set.call(this, value);
        }
      }
    });
    return function() {
      return hookSetter(target, key, original || {}, true);
    };
  }
  var nowTimestamp = Date.now;
  if (!/* @__PURE__ */ /[1-9][0-9]{12}/.test(Date.now().toString())) {
    nowTimestamp = function() {
      return /* @__PURE__ */ (/* @__PURE__ */ new Date()).getTime();
    };
  }
  function getWindowScroll(win2) {
    var _a2, _b, _c, _d;
    var doc = win2.document;
    return {
      left: doc.scrollingElement ? doc.scrollingElement.scrollLeft : win2.pageXOffset !== void 0 ? win2.pageXOffset : doc.documentElement.scrollLeft || (doc == null ? void 0 : doc.body) && ((_a2 = index$2.parentElement(doc.body)) == null ? void 0 : _a2.scrollLeft) || ((_b = doc == null ? void 0 : doc.body) == null ? void 0 : _b.scrollLeft) || 0,
      top: doc.scrollingElement ? doc.scrollingElement.scrollTop : win2.pageYOffset !== void 0 ? win2.pageYOffset : (doc == null ? void 0 : doc.documentElement.scrollTop) || (doc == null ? void 0 : doc.body) && ((_c = index$2.parentElement(doc.body)) == null ? void 0 : _c.scrollTop) || ((_d = doc == null ? void 0 : doc.body) == null ? void 0 : _d.scrollTop) || 0
    };
  }
  function getWindowHeight() {
    return window.innerHeight || document.documentElement && document.documentElement.clientHeight || document.body && document.body.clientHeight;
  }
  function getWindowWidth() {
    return window.innerWidth || document.documentElement && document.documentElement.clientWidth || document.body && document.body.clientWidth;
  }
  function closestElementOfNode(node2) {
    if (!node2) {
      return null;
    }
    var el = node2.nodeType === node2.ELEMENT_NODE ? node2 : index$2.parentElement(node2);
    return el;
  }
  function isBlocked(node2, blockClass, blockSelector, checkAncestors) {
    if (!node2) {
      return false;
    }
    var el = closestElementOfNode(node2);
    if (!el) {
      return false;
    }
    try {
      if (typeof blockClass === "string") {
        if (el.classList.contains(blockClass)) return true;
        if (checkAncestors && el.closest("." + blockClass) !== null) return true;
      } else {
        if (classMatchesRegex$1(el, blockClass, checkAncestors)) return true;
      }
    } catch (e22) {
    }
    if (blockSelector) {
      if (el.matches(blockSelector)) return true;
      if (checkAncestors && el.closest(blockSelector) !== null) return true;
    }
    return false;
  }
  function isSerialized(n22, mirror2) {
    return mirror2.getId(n22) !== -1;
  }
  function isIgnored(n22, mirror2, slimDOMOptions) {
    if (n22.tagName === "TITLE" && slimDOMOptions.headTitleMutations) {
      return true;
    }
    return mirror2.getId(n22) === IGNORED_NODE;
  }
  function isAncestorRemoved(target, mirror2) {
    if (isShadowRoot(target)) {
      return false;
    }
    var id = mirror2.getId(target);
    if (!mirror2.has(id)) {
      return true;
    }
    var parent = index$2.parentNode(target);
    if (parent && parent.nodeType === target.DOCUMENT_NODE) {
      return false;
    }
    if (!parent) {
      return true;
    }
    return isAncestorRemoved(parent, mirror2);
  }
  function legacy_isTouchEvent(event) {
    return Boolean(event.changedTouches);
  }
  function polyfill$1(win2) {
    if (win2 === void 0) win2 = window;
    if ("NodeList" in win2 && !win2.NodeList.prototype.forEach) {
      win2.NodeList.prototype.forEach = Array.prototype.forEach;
    }
    if ("DOMTokenList" in win2 && !win2.DOMTokenList.prototype.forEach) {
      win2.DOMTokenList.prototype.forEach = Array.prototype.forEach;
    }
  }
  function isSerializedIframe(n22, mirror2) {
    return Boolean(n22.nodeName === "IFRAME" && mirror2.getMeta(n22));
  }
  function isSerializedStylesheet(n22, mirror2) {
    return Boolean(n22.nodeName === "LINK" && n22.nodeType === n22.ELEMENT_NODE && n22.getAttribute && n22.getAttribute("rel") === "stylesheet" && mirror2.getMeta(n22));
  }
  function hasShadowRoot(n22) {
    if (!n22) return false;
    if (_instanceof(n22, BaseRRNode) && "shadowRoot" in n22) {
      return Boolean(n22.shadowRoot);
    }
    return Boolean(index$2.shadowRoot(n22));
  }
  var StyleSheetMirror = /* @__PURE__ */ (function() {
    function StyleSheetMirror2() {
      __publicField$1(this, "id", 1);
      __publicField$1(this, "styleIDMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "idStyleMap", /* @__PURE__ */ new Map());
    }
    var _proto = StyleSheetMirror2.prototype;
    _proto.getId = function getId3(stylesheet) {
      var _this_styleIDMap_get;
      return (_this_styleIDMap_get = this.styleIDMap.get(stylesheet)) != null ? _this_styleIDMap_get : -1;
    };
    _proto.has = function has2(stylesheet) {
      return this.styleIDMap.has(stylesheet);
    };
    _proto.add = function add(stylesheet, id) {
      if (this.has(stylesheet)) return this.getId(stylesheet);
      var newId;
      if (id === void 0) {
        newId = this.id++;
      } else newId = id;
      this.styleIDMap.set(stylesheet, newId);
      this.idStyleMap.set(newId, stylesheet);
      return newId;
    };
    _proto.getStyle = function getStyle(id) {
      return this.idStyleMap.get(id) || null;
    };
    _proto.reset = function reset2() {
      this.styleIDMap = /* @__PURE__ */ new WeakMap();
      this.idStyleMap = /* @__PURE__ */ new Map();
      this.id = 1;
    };
    _proto.generateId = function generateId() {
      return this.id++;
    };
    return StyleSheetMirror2;
  })();
  function getShadowHost(n22) {
    var _a2;
    var shadowHost = null;
    if ("getRootNode" in n22 && ((_a2 = index$2.getRootNode(n22)) == null ? void 0 : _a2.nodeType) === Node.DOCUMENT_FRAGMENT_NODE && index$2.host(index$2.getRootNode(n22))) shadowHost = index$2.host(index$2.getRootNode(n22));
    return shadowHost;
  }
  function getRootShadowHost(n22) {
    var rootShadowHost = n22;
    var shadowHost;
    while (shadowHost = getShadowHost(rootShadowHost)) rootShadowHost = shadowHost;
    return rootShadowHost;
  }
  function shadowHostInDom(n22) {
    var doc = n22.ownerDocument;
    if (!doc) return false;
    var shadowHost = getRootShadowHost(n22);
    return index$2.contains(doc, shadowHost);
  }
  function inDom(n22) {
    var doc = n22.ownerDocument;
    if (!doc) return false;
    return index$2.contains(doc, n22) || shadowHostInDom(n22);
  }
  var EventType = /* @__PURE__ */ (function(EventType2) {
    EventType2[EventType2["DomContentLoaded"] = 0] = "DomContentLoaded";
    EventType2[EventType2["Load"] = 1] = "Load";
    EventType2[EventType2["FullSnapshot"] = 2] = "FullSnapshot";
    EventType2[EventType2["IncrementalSnapshot"] = 3] = "IncrementalSnapshot";
    EventType2[EventType2["Meta"] = 4] = "Meta";
    EventType2[EventType2["Custom"] = 5] = "Custom";
    EventType2[EventType2["Plugin"] = 6] = "Plugin";
    return EventType2;
  })(EventType || {});
  var IncrementalSource = /* @__PURE__ */ (function(IncrementalSource2) {
    IncrementalSource2[IncrementalSource2["Mutation"] = 0] = "Mutation";
    IncrementalSource2[IncrementalSource2["MouseMove"] = 1] = "MouseMove";
    IncrementalSource2[IncrementalSource2["MouseInteraction"] = 2] = "MouseInteraction";
    IncrementalSource2[IncrementalSource2["Scroll"] = 3] = "Scroll";
    IncrementalSource2[IncrementalSource2["ViewportResize"] = 4] = "ViewportResize";
    IncrementalSource2[IncrementalSource2["Input"] = 5] = "Input";
    IncrementalSource2[IncrementalSource2["TouchMove"] = 6] = "TouchMove";
    IncrementalSource2[IncrementalSource2["MediaInteraction"] = 7] = "MediaInteraction";
    IncrementalSource2[IncrementalSource2["StyleSheetRule"] = 8] = "StyleSheetRule";
    IncrementalSource2[IncrementalSource2["CanvasMutation"] = 9] = "CanvasMutation";
    IncrementalSource2[IncrementalSource2["Font"] = 10] = "Font";
    IncrementalSource2[IncrementalSource2["Log"] = 11] = "Log";
    IncrementalSource2[IncrementalSource2["Drag"] = 12] = "Drag";
    IncrementalSource2[IncrementalSource2["StyleDeclaration"] = 13] = "StyleDeclaration";
    IncrementalSource2[IncrementalSource2["Selection"] = 14] = "Selection";
    IncrementalSource2[IncrementalSource2["AdoptedStyleSheet"] = 15] = "AdoptedStyleSheet";
    IncrementalSource2[IncrementalSource2["CustomElement"] = 16] = "CustomElement";
    return IncrementalSource2;
  })(IncrementalSource || {});
  var MouseInteractions = /* @__PURE__ */ (function(MouseInteractions2) {
    MouseInteractions2[MouseInteractions2["MouseUp"] = 0] = "MouseUp";
    MouseInteractions2[MouseInteractions2["MouseDown"] = 1] = "MouseDown";
    MouseInteractions2[MouseInteractions2["Click"] = 2] = "Click";
    MouseInteractions2[MouseInteractions2["ContextMenu"] = 3] = "ContextMenu";
    MouseInteractions2[MouseInteractions2["DblClick"] = 4] = "DblClick";
    MouseInteractions2[MouseInteractions2["Focus"] = 5] = "Focus";
    MouseInteractions2[MouseInteractions2["Blur"] = 6] = "Blur";
    MouseInteractions2[MouseInteractions2["TouchStart"] = 7] = "TouchStart";
    MouseInteractions2[MouseInteractions2["TouchMove_Departed"] = 8] = "TouchMove_Departed";
    MouseInteractions2[MouseInteractions2["TouchEnd"] = 9] = "TouchEnd";
    MouseInteractions2[MouseInteractions2["TouchCancel"] = 10] = "TouchCancel";
    return MouseInteractions2;
  })(MouseInteractions || {});
  var PointerTypes = /* @__PURE__ */ (function(PointerTypes2) {
    PointerTypes2[PointerTypes2["Mouse"] = 0] = "Mouse";
    PointerTypes2[PointerTypes2["Pen"] = 1] = "Pen";
    PointerTypes2[PointerTypes2["Touch"] = 2] = "Touch";
    return PointerTypes2;
  })(PointerTypes || {});
  var CanvasContext = /* @__PURE__ */ (function(CanvasContext2) {
    CanvasContext2[CanvasContext2["2D"] = 0] = "2D";
    CanvasContext2[CanvasContext2["WebGL"] = 1] = "WebGL";
    CanvasContext2[CanvasContext2["WebGL2"] = 2] = "WebGL2";
    return CanvasContext2;
  })(CanvasContext || {});
  var MediaInteractions = /* @__PURE__ */ (function(MediaInteractions2) {
    MediaInteractions2[MediaInteractions2["Play"] = 0] = "Play";
    MediaInteractions2[MediaInteractions2["Pause"] = 1] = "Pause";
    MediaInteractions2[MediaInteractions2["Seeked"] = 2] = "Seeked";
    MediaInteractions2[MediaInteractions2["VolumeChange"] = 3] = "VolumeChange";
    MediaInteractions2[MediaInteractions2["RateChange"] = 4] = "RateChange";
    return MediaInteractions2;
  })(MediaInteractions || {});
  var NodeType = /* @__PURE__ */ (function(NodeType2) {
    NodeType2[NodeType2["Document"] = 0] = "Document";
    NodeType2[NodeType2["DocumentType"] = 1] = "DocumentType";
    NodeType2[NodeType2["Element"] = 2] = "Element";
    NodeType2[NodeType2["Text"] = 3] = "Text";
    NodeType2[NodeType2["CDATA"] = 4] = "CDATA";
    NodeType2[NodeType2["Comment"] = 5] = "Comment";
    return NodeType2;
  })(NodeType || {});
  function isNodeInLinkedList(n22) {
    return "__ln" in n22;
  }
  var DoubleLinkedList = /* @__PURE__ */ (function() {
    function DoubleLinkedList2() {
      __publicField$1(this, "length", 0);
      __publicField$1(this, "head", null);
      __publicField$1(this, "tail", null);
    }
    var _proto = DoubleLinkedList2.prototype;
    _proto.get = function get(position) {
      if (position >= this.length) {
        throw new Error("Position outside of list range");
      }
      var current = this.head;
      for (var index2 = 0; index2 < position; index2++) {
        current = (current == null ? void 0 : current.next) || null;
      }
      return current;
    };
    _proto.addNode = function addNode(n22) {
      var node2 = {
        value: n22,
        previous: null,
        next: null
      };
      n22.__ln = node2;
      if (n22.previousSibling && isNodeInLinkedList(n22.previousSibling)) {
        var current = n22.previousSibling.__ln.next;
        node2.next = current;
        node2.previous = n22.previousSibling.__ln;
        n22.previousSibling.__ln.next = node2;
        if (current) {
          current.previous = node2;
        }
      } else if (n22.nextSibling && isNodeInLinkedList(n22.nextSibling) && n22.nextSibling.__ln.previous) {
        var current1 = n22.nextSibling.__ln.previous;
        node2.previous = current1;
        node2.next = n22.nextSibling.__ln;
        n22.nextSibling.__ln.previous = node2;
        if (current1) {
          current1.next = node2;
        }
      } else {
        if (this.head) {
          this.head.previous = node2;
        }
        node2.next = this.head;
        this.head = node2;
      }
      if (node2.next === null) {
        this.tail = node2;
      }
      this.length++;
    };
    _proto.removeNode = function removeNode(n22) {
      var current = n22.__ln;
      if (!this.head) {
        return;
      }
      if (!current.previous) {
        this.head = current.next;
        if (this.head) {
          this.head.previous = null;
        } else {
          this.tail = null;
        }
      } else {
        current.previous.next = current.next;
        if (current.next) {
          current.next.previous = current.previous;
        } else {
          this.tail = current.previous;
        }
      }
      if (n22.__ln) {
        delete n22.__ln;
      }
      this.length--;
    };
    return DoubleLinkedList2;
  })();
  var moveKey = function(id, parentId) {
    return id + "@" + parentId;
  };
  var MutationBuffer = /* @__PURE__ */ (function() {
    function MutationBuffer2() {
      var _this = this;
      __publicField$1(this, "frozen", false);
      __publicField$1(this, "locked", false);
      __publicField$1(this, "texts", []);
      __publicField$1(this, "attributes", []);
      __publicField$1(this, "attributeMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "removes", []);
      __publicField$1(this, "mapRemoves", []);
      __publicField$1(this, "movedMap", {});
      __publicField$1(this, "addedSet", /* @__PURE__ */ new Set());
      __publicField$1(this, "movedSet", /* @__PURE__ */ new Set());
      __publicField$1(this, "droppedSet", /* @__PURE__ */ new Set());
      __publicField$1(this, "removesSubTreeCache", /* @__PURE__ */ new Set());
      __publicField$1(this, "mutationCb");
      __publicField$1(this, "blockClass");
      __publicField$1(this, "blockSelector");
      __publicField$1(this, "maskTextClass");
      __publicField$1(this, "maskTextSelector");
      __publicField$1(this, "inlineStylesheet");
      __publicField$1(this, "maskInputOptions");
      __publicField$1(this, "maskTextFn");
      __publicField$1(this, "maskInputFn");
      __publicField$1(this, "keepIframeSrcFn");
      __publicField$1(this, "recordCanvas");
      __publicField$1(this, "inlineImages");
      __publicField$1(this, "slimDOMOptions");
      __publicField$1(this, "dataURLOptions");
      __publicField$1(this, "doc");
      __publicField$1(this, "mirror");
      __publicField$1(this, "iframeManager");
      __publicField$1(this, "stylesheetManager");
      __publicField$1(this, "shadowDomManager");
      __publicField$1(this, "canvasManager");
      __publicField$1(this, "processedNodeManager");
      __publicField$1(this, "unattachedDoc");
      __publicField$1(this, "processMutations", function(mutations) {
        mutations.forEach(_this.processMutation);
        _this.emit();
      });
      __publicField$1(this, "emit", function() {
        if (_this.frozen || _this.locked) {
          return;
        }
        var adds = [];
        var addedIds = /* @__PURE__ */ new Set();
        var addList = new DoubleLinkedList();
        var getNextId = function(n23) {
          var ns = n23;
          var nextId2 = IGNORED_NODE;
          while (nextId2 === IGNORED_NODE) {
            ns = ns && ns.nextSibling;
            nextId2 = ns && _this.mirror.getId(ns);
          }
          return nextId2;
        };
        var pushAdd = function(n23) {
          var parent2 = index$2.parentNode(n23);
          if (!parent2 || !inDom(n23)) {
            return;
          }
          var cssCaptured = false;
          if (n23.nodeType === Node.TEXT_NODE) {
            var parentTag = parent2.tagName;
            if (parentTag === "TEXTAREA") {
              return;
            } else if (parentTag === "STYLE" && _this.addedSet.has(parent2)) {
              cssCaptured = true;
            }
          }
          var parentId3 = isShadowRoot(parent2) ? _this.mirror.getId(getShadowHost(n23)) : _this.mirror.getId(parent2);
          var nextId2 = getNextId(n23);
          if (parentId3 === -1 || nextId2 === -1) {
            return addList.addNode(n23);
          }
          var sn2 = serializeNodeWithId(n23, {
            doc: _this.doc,
            mirror: _this.mirror,
            blockClass: _this.blockClass,
            blockSelector: _this.blockSelector,
            maskTextClass: _this.maskTextClass,
            maskTextSelector: _this.maskTextSelector,
            skipChild: true,
            newlyAddedElement: true,
            inlineStylesheet: _this.inlineStylesheet,
            maskInputOptions: _this.maskInputOptions,
            maskTextFn: _this.maskTextFn,
            maskInputFn: _this.maskInputFn,
            slimDOMOptions: _this.slimDOMOptions,
            dataURLOptions: _this.dataURLOptions,
            recordCanvas: _this.recordCanvas,
            inlineImages: _this.inlineImages,
            onSerialize: function(currentN) {
              if (isSerializedIframe(currentN, _this.mirror)) {
                _this.iframeManager.addIframe(currentN);
              }
              if (isSerializedStylesheet(currentN, _this.mirror)) {
                _this.stylesheetManager.trackLinkElement(currentN);
              }
              if (hasShadowRoot(n23)) {
                _this.shadowDomManager.addShadowRoot(index$2.shadowRoot(n23), _this.doc);
              }
            },
            onIframeLoad: function(iframe, childSn) {
              _this.iframeManager.attachIframe(iframe, childSn);
              _this.shadowDomManager.observeAttachShadow(iframe);
            },
            onStylesheetLoad: function(link, childSn) {
              _this.stylesheetManager.attachLinkElement(link, childSn);
            },
            cssCaptured
          });
          if (sn2) {
            adds.push({
              parentId: parentId3,
              nextId: nextId2,
              node: sn2
            });
            addedIds.add(sn2.id);
          }
        };
        while (_this.mapRemoves.length) {
          _this.mirror.removeNodeFromMap(_this.mapRemoves.shift());
        }
        for (var _iterator = _create_for_of_iterator_helper_loose(_this.movedSet), _step; !(_step = _iterator()).done; ) {
          var n22 = _step.value;
          if (isParentRemoved(_this.removesSubTreeCache, n22, _this.mirror) && !_this.movedSet.has(index$2.parentNode(n22))) {
            continue;
          }
          pushAdd(n22);
        }
        for (var _iterator1 = _create_for_of_iterator_helper_loose(_this.addedSet), _step1; !(_step1 = _iterator1()).done; ) {
          var n21 = _step1.value;
          if (!isAncestorInSet(_this.droppedSet, n21) && !isParentRemoved(_this.removesSubTreeCache, n21, _this.mirror)) {
            pushAdd(n21);
          } else if (isAncestorInSet(_this.movedSet, n21)) {
            pushAdd(n21);
          } else {
            _this.droppedSet.add(n21);
          }
        }
        var candidate = null;
        while (addList.length) {
          var node2 = null;
          if (candidate) {
            var parentId = _this.mirror.getId(index$2.parentNode(candidate.value));
            var nextId = getNextId(candidate.value);
            if (parentId !== -1 && nextId !== -1) {
              node2 = candidate;
            }
          }
          if (!node2) {
            var tailNode = addList.tail;
            while (tailNode) {
              var _node = tailNode;
              tailNode = tailNode.previous;
              if (_node) {
                var parentId1 = _this.mirror.getId(index$2.parentNode(_node.value));
                var nextId1 = getNextId(_node.value);
                if (nextId1 === -1) continue;
                else if (parentId1 !== -1) {
                  node2 = _node;
                  break;
                } else {
                  var unhandledNode = _node.value;
                  var parent = index$2.parentNode(unhandledNode);
                  if (parent && parent.nodeType === Node.DOCUMENT_FRAGMENT_NODE) {
                    var shadowHost = index$2.host(parent);
                    var parentId2 = _this.mirror.getId(shadowHost);
                    if (parentId2 !== -1) {
                      node2 = _node;
                      break;
                    }
                  }
                }
              }
            }
          }
          if (!node2) {
            while (addList.head) {
              addList.removeNode(addList.head.value);
            }
            break;
          }
          candidate = node2.previous;
          addList.removeNode(node2.value);
          pushAdd(node2.value);
        }
        var payload = {
          texts: _this.texts.map(function(text) {
            var n23 = text.node;
            var parent2 = index$2.parentNode(n23);
            if (parent2 && parent2.tagName === "TEXTAREA") {
              _this.genTextAreaValueMutation(parent2);
            }
            return {
              id: _this.mirror.getId(n23),
              value: text.value
            };
          }).filter(function(text) {
            return !addedIds.has(text.id);
          }).filter(function(text) {
            return _this.mirror.has(text.id);
          }),
          attributes: _this.attributes.map(function(attribute) {
            var attributes = attribute.attributes;
            if (typeof attributes.style === "string") {
              var diffAsStr = JSON.stringify(attribute.styleDiff);
              var unchangedAsStr = JSON.stringify(attribute._unchangedStyles);
              if (diffAsStr.length < attributes.style.length) {
                if ((diffAsStr + unchangedAsStr).split("var(").length === attributes.style.split("var(").length) {
                  attributes.style = attribute.styleDiff;
                }
              }
            }
            return {
              id: _this.mirror.getId(attribute.node),
              attributes
            };
          }).filter(function(attribute) {
            return !addedIds.has(attribute.id);
          }).filter(function(attribute) {
            return _this.mirror.has(attribute.id);
          }),
          removes: _this.removes,
          adds
        };
        if (!payload.texts.length && !payload.attributes.length && !payload.removes.length && !payload.adds.length) {
          return;
        }
        _this.texts = [];
        _this.attributes = [];
        _this.attributeMap = /* @__PURE__ */ new WeakMap();
        _this.removes = [];
        _this.addedSet = /* @__PURE__ */ new Set();
        _this.movedSet = /* @__PURE__ */ new Set();
        _this.droppedSet = /* @__PURE__ */ new Set();
        _this.removesSubTreeCache = /* @__PURE__ */ new Set();
        _this.movedMap = {};
        _this.mutationCb(payload);
      });
      __publicField$1(this, "genTextAreaValueMutation", function(textarea) {
        var item = _this.attributeMap.get(textarea);
        if (!item) {
          item = {
            node: textarea,
            attributes: {},
            styleDiff: {},
            _unchangedStyles: {}
          };
          _this.attributes.push(item);
          _this.attributeMap.set(textarea, item);
        }
        var value = Array.from(index$2.childNodes(textarea), function(cn2) {
          return index$2.textContent(cn2) || "";
        }).join("");
        item.attributes.value = maskInputValue({
          element: textarea,
          maskInputOptions: _this.maskInputOptions,
          tagName: textarea.tagName,
          type: getInputType(textarea),
          value,
          maskInputFn: _this.maskInputFn
        });
      });
      __publicField$1(this, "processMutation", function(m3) {
        if (isIgnored(m3.target, _this.mirror, _this.slimDOMOptions)) {
          return;
        }
        switch (m3.type) {
          case "characterData": {
            var value = index$2.textContent(m3.target);
            if (!isBlocked(m3.target, _this.blockClass, _this.blockSelector, false) && value !== m3.oldValue) {
              _this.texts.push({
                value: needMaskingText(m3.target, _this.maskTextClass, _this.maskTextSelector, true) && value ? _this.maskTextFn ? _this.maskTextFn(value, closestElementOfNode(m3.target)) : value.replace(/[\S]/g, "*") : value,
                node: m3.target
              });
            }
            break;
          }
          case "attributes": {
            var target = m3.target;
            var attributeName = m3.attributeName;
            var value1 = m3.target.getAttribute(attributeName);
            if (attributeName === "value") {
              var type = getInputType(target);
              value1 = maskInputValue({
                element: target,
                maskInputOptions: _this.maskInputOptions,
                tagName: target.tagName,
                type,
                value: value1,
                maskInputFn: _this.maskInputFn
              });
            }
            if (isBlocked(m3.target, _this.blockClass, _this.blockSelector, false) || value1 === m3.oldValue) {
              return;
            }
            var item = _this.attributeMap.get(m3.target);
            if (target.tagName === "IFRAME" && attributeName === "src" && !_this.keepIframeSrcFn(value1)) {
              if (!target.contentDocument) {
                attributeName = "rr_src";
              } else {
                return;
              }
            }
            if (!item) {
              item = {
                node: m3.target,
                attributes: {},
                styleDiff: {},
                _unchangedStyles: {}
              };
              _this.attributes.push(item);
              _this.attributeMap.set(m3.target, item);
            }
            if (attributeName === "type" && target.tagName === "INPUT" && (m3.oldValue || "").toLowerCase() === "password") {
              target.setAttribute("data-rr-is-password", "true");
            }
            if (!ignoreAttribute(target.tagName, attributeName)) {
              item.attributes[attributeName] = transformAttribute(_this.doc, toLowerCase(target.tagName), toLowerCase(attributeName), value1);
              if (attributeName === "style") {
                if (!_this.unattachedDoc) {
                  try {
                    _this.unattachedDoc = document.implementation.createHTMLDocument();
                  } catch (e22) {
                    _this.unattachedDoc = _this.doc;
                  }
                }
                var old = _this.unattachedDoc.createElement("span");
                if (m3.oldValue) {
                  old.setAttribute("style", m3.oldValue);
                }
                for (var _iterator = _create_for_of_iterator_helper_loose(Array.from(target.style)), _step; !(_step = _iterator()).done; ) {
                  var pname = _step.value;
                  var newValue = target.style.getPropertyValue(pname);
                  var newPriority = target.style.getPropertyPriority(pname);
                  if (newValue !== old.style.getPropertyValue(pname) || newPriority !== old.style.getPropertyPriority(pname)) {
                    if (newPriority === "") {
                      item.styleDiff[pname] = newValue;
                    } else {
                      item.styleDiff[pname] = [
                        newValue,
                        newPriority
                      ];
                    }
                  } else {
                    item._unchangedStyles[pname] = [
                      newValue,
                      newPriority
                    ];
                  }
                }
                for (var _iterator1 = _create_for_of_iterator_helper_loose(Array.from(old.style)), _step1; !(_step1 = _iterator1()).done; ) {
                  var pname1 = _step1.value;
                  if (target.style.getPropertyValue(pname1) === "") {
                    item.styleDiff[pname1] = false;
                  }
                }
              } else if (attributeName === "open" && target.tagName === "DIALOG") {
                if (target.matches("dialog:modal")) {
                  item.attributes["rr_open_mode"] = "modal";
                } else {
                  item.attributes["rr_open_mode"] = "non-modal";
                }
              }
            }
            break;
          }
          case "childList": {
            if (isBlocked(m3.target, _this.blockClass, _this.blockSelector, true)) return;
            if (m3.target.tagName === "TEXTAREA") {
              _this.genTextAreaValueMutation(m3.target);
              return;
            }
            m3.addedNodes.forEach(function(n22) {
              return _this.genAdds(n22, m3.target);
            });
            m3.removedNodes.forEach(function(n22) {
              var nodeId = _this.mirror.getId(n22);
              var parentId = isShadowRoot(m3.target) ? _this.mirror.getId(index$2.host(m3.target)) : _this.mirror.getId(m3.target);
              if (isBlocked(m3.target, _this.blockClass, _this.blockSelector, false) || isIgnored(n22, _this.mirror, _this.slimDOMOptions) || !isSerialized(n22, _this.mirror)) {
                return;
              }
              if (_this.addedSet.has(n22)) {
                deepDelete(_this.addedSet, n22);
                _this.droppedSet.add(n22);
              } else if (_this.addedSet.has(m3.target) && nodeId === -1) ;
              else if (isAncestorRemoved(m3.target, _this.mirror)) ;
              else if (_this.movedSet.has(n22) && _this.movedMap[moveKey(nodeId, parentId)]) {
                deepDelete(_this.movedSet, n22);
              } else {
                _this.removes.push({
                  parentId,
                  id: nodeId,
                  isShadow: isShadowRoot(m3.target) && isNativeShadowDom(m3.target) ? true : void 0
                });
                processRemoves(n22, _this.removesSubTreeCache);
              }
              _this.mapRemoves.push(n22);
            });
            break;
          }
        }
      });
      __publicField$1(this, "genAdds", function(n22, target) {
        if (_this.processedNodeManager.inOtherBuffer(n22, _this)) return;
        if (_this.addedSet.has(n22) || _this.movedSet.has(n22)) return;
        if (_this.mirror.hasNode(n22)) {
          if (isIgnored(n22, _this.mirror, _this.slimDOMOptions)) {
            return;
          }
          _this.movedSet.add(n22);
          var targetId = null;
          if (target && _this.mirror.hasNode(target)) {
            targetId = _this.mirror.getId(target);
          }
          if (targetId && targetId !== -1) {
            _this.movedMap[moveKey(_this.mirror.getId(n22), targetId)] = true;
          }
        } else {
          _this.addedSet.add(n22);
          _this.droppedSet.delete(n22);
        }
        if (!isBlocked(n22, _this.blockClass, _this.blockSelector, false)) {
          index$2.childNodes(n22).forEach(function(childN) {
            return _this.genAdds(childN);
          });
          if (hasShadowRoot(n22)) {
            index$2.childNodes(index$2.shadowRoot(n22)).forEach(function(childN) {
              _this.processedNodeManager.add(childN, _this);
              _this.genAdds(childN, n22);
            });
          }
        }
      });
    }
    var _proto = MutationBuffer2.prototype;
    _proto.init = function init(options) {
      var _this = this;
      [
        "mutationCb",
        "blockClass",
        "blockSelector",
        "maskTextClass",
        "maskTextSelector",
        "inlineStylesheet",
        "maskInputOptions",
        "maskTextFn",
        "maskInputFn",
        "keepIframeSrcFn",
        "recordCanvas",
        "inlineImages",
        "slimDOMOptions",
        "dataURLOptions",
        "doc",
        "mirror",
        "iframeManager",
        "stylesheetManager",
        "shadowDomManager",
        "canvasManager",
        "processedNodeManager"
      ].forEach(function(key) {
        _this[key] = options[key];
      });
    };
    _proto.freeze = function freeze() {
      this.frozen = true;
      this.canvasManager.freeze();
    };
    _proto.unfreeze = function unfreeze() {
      this.frozen = false;
      this.canvasManager.unfreeze();
      this.emit();
    };
    _proto.isFrozen = function isFrozen() {
      return this.frozen;
    };
    _proto.lock = function lock() {
      this.locked = true;
      this.canvasManager.lock();
    };
    _proto.unlock = function unlock() {
      this.locked = false;
      this.canvasManager.unlock();
      this.emit();
    };
    _proto.reset = function reset2() {
      this.shadowDomManager.reset();
      this.canvasManager.reset();
    };
    return MutationBuffer2;
  })();
  function deepDelete(addsSet, n22) {
    addsSet.delete(n22);
    index$2.childNodes(n22).forEach(function(childN) {
      return deepDelete(addsSet, childN);
    });
  }
  function processRemoves(n22, cache) {
    var queue = [
      n22
    ];
    while (queue.length) {
      var next = queue.pop();
      if (cache.has(next)) continue;
      cache.add(next);
      index$2.childNodes(next).forEach(function(n222) {
        return queue.push(n222);
      });
    }
    return;
  }
  function isParentRemoved(removes, n22, mirror2) {
    if (removes.size === 0) return false;
    return _isParentRemoved(removes, n22);
  }
  function _isParentRemoved(removes, n22, _mirror2) {
    var node2 = index$2.parentNode(n22);
    if (!node2) return false;
    return removes.has(node2);
  }
  function isAncestorInSet(set2, n22) {
    if (set2.size === 0) return false;
    return _isAncestorInSet(set2, n22);
  }
  function _isAncestorInSet(set2, n22) {
    var parent = index$2.parentNode(n22);
    if (!parent) {
      return false;
    }
    if (set2.has(parent)) {
      return true;
    }
    return _isAncestorInSet(set2, parent);
  }
  var errorHandler;
  function registerErrorHandler(handler) {
    errorHandler = handler;
  }
  function unregisterErrorHandler() {
    errorHandler = void 0;
  }
  var callbackWrapper = function(cb) {
    if (!errorHandler) {
      return cb;
    }
    var rrwebWrapped = function() {
      for (var _len = arguments.length, rest = new Array(_len), _key = 0; _key < _len; _key++) {
        rest[_key] = arguments[_key];
      }
      try {
        return cb.apply(void 0, [].concat(rest));
      } catch (error) {
        if (errorHandler && errorHandler(error) === true) {
          return;
        }
        throw error;
      }
    };
    return rrwebWrapped;
  };
  var mutationBuffers = [];
  function getEventTarget(event) {
    try {
      if ("composedPath" in event) {
        var path = event.composedPath();
        if (path.length) {
          return path[0];
        }
      } else if ("path" in event && event.path.length) {
        return event.path[0];
      }
    } catch (e3) {
    }
    return event && event.target;
  }
  function initMutationObserver(options, rootEl) {
    var mutationBuffer = new MutationBuffer();
    mutationBuffers.push(mutationBuffer);
    mutationBuffer.init(options);
    var observer = new (mutationObserverCtor$2())(callbackWrapper(mutationBuffer.processMutations.bind(mutationBuffer)));
    observer.observe(rootEl, {
      attributes: true,
      attributeOldValue: true,
      characterData: true,
      characterDataOldValue: true,
      childList: true,
      subtree: true
    });
    return observer;
  }
  function initMoveObserver(param) {
    var mousemoveCb = param.mousemoveCb, sampling = param.sampling, doc = param.doc, mirror2 = param.mirror;
    if (sampling.mousemove === false) {
      return function() {
      };
    }
    var threshold = typeof sampling.mousemove === "number" ? sampling.mousemove : 50;
    var callbackThreshold = typeof sampling.mousemoveCallback === "number" ? sampling.mousemoveCallback : 500;
    var positions = [];
    var timeBaseline;
    var wrappedCb = throttle(callbackWrapper(function(source) {
      var totalOffset = Date.now() - timeBaseline;
      mousemoveCb(positions.map(function(p3) {
        p3.timeOffset -= totalOffset;
        return p3;
      }), source);
      positions = [];
      timeBaseline = null;
    }), callbackThreshold);
    var updatePosition = callbackWrapper(throttle(callbackWrapper(function(evt) {
      var target = getEventTarget(evt);
      var _ref = legacy_isTouchEvent(evt) ? evt.changedTouches[0] : evt, clientX = _ref.clientX, clientY = _ref.clientY;
      if (!timeBaseline) {
        timeBaseline = nowTimestamp();
      }
      positions.push({
        x: clientX,
        y: clientY,
        id: mirror2.getId(target),
        timeOffset: nowTimestamp() - timeBaseline
      });
      wrappedCb(typeof DragEvent !== "undefined" && _instanceof(evt, DragEvent) ? IncrementalSource.Drag : _instanceof(evt, MouseEvent) ? IncrementalSource.MouseMove : IncrementalSource.TouchMove);
    }), threshold, {
      trailing: false
    }));
    var handlers = [
      on2("mousemove", updatePosition, doc),
      on2("touchmove", updatePosition, doc),
      on2("drag", updatePosition, doc)
    ];
    return callbackWrapper(function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function initMouseInteractionObserver(param) {
    var mouseInteractionCb = param.mouseInteractionCb, doc = param.doc, mirror2 = param.mirror, blockClass = param.blockClass, blockSelector = param.blockSelector, sampling = param.sampling;
    if (sampling.mouseInteraction === false) {
      return function() {
      };
    }
    var disableMap = sampling.mouseInteraction === true || sampling.mouseInteraction === void 0 ? {} : sampling.mouseInteraction;
    var handlers = [];
    var currentPointerType = null;
    var getHandler = function(eventKey) {
      return function(event) {
        var target = getEventTarget(event);
        if (isBlocked(target, blockClass, blockSelector, true)) {
          return;
        }
        var pointerType = null;
        var thisEventKey = eventKey;
        if ("pointerType" in event) {
          switch (event.pointerType) {
            case "mouse":
              pointerType = PointerTypes.Mouse;
              break;
            case "touch":
              pointerType = PointerTypes.Touch;
              break;
            case "pen":
              pointerType = PointerTypes.Pen;
              break;
          }
          if (pointerType === PointerTypes.Touch) {
            if (MouseInteractions[eventKey] === MouseInteractions.MouseDown) {
              thisEventKey = "TouchStart";
            } else if (MouseInteractions[eventKey] === MouseInteractions.MouseUp) {
              thisEventKey = "TouchEnd";
            }
          } else if (pointerType === PointerTypes.Pen) ;
        } else if (legacy_isTouchEvent(event)) {
          pointerType = PointerTypes.Touch;
        }
        if (pointerType !== null) {
          currentPointerType = pointerType;
          if (thisEventKey.startsWith("Touch") && pointerType === PointerTypes.Touch || thisEventKey.startsWith("Mouse") && pointerType === PointerTypes.Mouse) {
            pointerType = null;
          }
        } else if (MouseInteractions[eventKey] === MouseInteractions.Click) {
          pointerType = currentPointerType;
          currentPointerType = null;
        }
        var e22 = legacy_isTouchEvent(event) ? event.changedTouches[0] : event;
        if (!e22) {
          return;
        }
        var id = mirror2.getId(target);
        var clientX = e22.clientX, clientY = e22.clientY;
        callbackWrapper(mouseInteractionCb)(_extends({
          type: MouseInteractions[thisEventKey],
          id,
          x: clientX,
          y: clientY
        }, pointerType !== null && {
          pointerType
        }));
      };
    };
    Object.keys(MouseInteractions).filter(function(key) {
      return Number.isNaN(Number(key)) && !key.endsWith("_Departed") && disableMap[key] !== false;
    }).forEach(function(eventKey) {
      var eventName = toLowerCase(eventKey);
      var handler = getHandler(eventKey);
      if (window.PointerEvent) {
        switch (MouseInteractions[eventKey]) {
          case MouseInteractions.MouseDown:
          case MouseInteractions.MouseUp:
            eventName = eventName.replace("mouse", "pointer");
            break;
          case MouseInteractions.TouchStart:
          case MouseInteractions.TouchEnd:
            return;
        }
      }
      handlers.push(on2(eventName, handler, doc));
    });
    return callbackWrapper(function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function initScrollObserver(param) {
    var scrollCb = param.scrollCb, doc = param.doc, mirror2 = param.mirror, blockClass = param.blockClass, blockSelector = param.blockSelector, sampling = param.sampling;
    var updatePosition = callbackWrapper(throttle(callbackWrapper(function(evt) {
      var target = getEventTarget(evt);
      if (!target || isBlocked(target, blockClass, blockSelector, true)) {
        return;
      }
      var id = mirror2.getId(target);
      if (target === doc && doc.defaultView) {
        var scrollLeftTop = getWindowScroll(doc.defaultView);
        scrollCb({
          id,
          x: scrollLeftTop.left,
          y: scrollLeftTop.top
        });
      } else {
        scrollCb({
          id,
          x: target.scrollLeft,
          y: target.scrollTop
        });
      }
    }), sampling.scroll || 100));
    return on2("scroll", updatePosition, doc);
  }
  function initViewportResizeObserver(param, param1) {
    var viewportResizeCb = param.viewportResizeCb;
    var win2 = param1.win;
    var lastH = -1;
    var lastW = -1;
    var updateDimension = callbackWrapper(throttle(callbackWrapper(function() {
      var height = getWindowHeight();
      var width = getWindowWidth();
      if (lastH !== height || lastW !== width) {
        viewportResizeCb({
          width: Number(width),
          height: Number(height)
        });
        lastH = height;
        lastW = width;
      }
    }), 200));
    return on2("resize", updateDimension, win2);
  }
  var INPUT_TAGS = [
    "INPUT",
    "TEXTAREA",
    "SELECT"
  ];
  var lastInputValueMap = /* @__PURE__ */ new WeakMap();
  function initInputObserver(param) {
    var inputCb = param.inputCb, doc = param.doc, mirror2 = param.mirror, blockClass = param.blockClass, blockSelector = param.blockSelector, ignoreClass = param.ignoreClass, ignoreSelector = param.ignoreSelector, maskInputOptions = param.maskInputOptions, maskInputFn = param.maskInputFn, sampling = param.sampling, userTriggeredOnInput = param.userTriggeredOnInput;
    function eventHandler(event) {
      var target = getEventTarget(event);
      var userTriggered = event.isTrusted;
      var tagName = target && target.tagName;
      if (target && tagName === "OPTION") {
        target = index$2.parentElement(target);
      }
      if (!target || !tagName || INPUT_TAGS.indexOf(tagName) < 0 || isBlocked(target, blockClass, blockSelector, true)) {
        return;
      }
      if (target.classList.contains(ignoreClass) || ignoreSelector && target.matches(ignoreSelector)) {
        return;
      }
      var text = target.value;
      var isChecked = false;
      var type = getInputType(target) || "";
      if (type === "radio" || type === "checkbox") {
        isChecked = target.checked;
      } else if (maskInputOptions[tagName.toLowerCase()] || maskInputOptions[type]) {
        text = maskInputValue({
          element: target,
          maskInputOptions,
          tagName,
          type,
          value: text,
          maskInputFn
        });
      }
      cbWithDedup(target, userTriggeredOnInput ? {
        text,
        isChecked,
        userTriggered
      } : {
        text,
        isChecked
      });
      var name5 = target.name;
      if (type === "radio" && name5 && isChecked) {
        doc.querySelectorAll('input[type="radio"][name="' + name5 + '"]').forEach(function(el) {
          if (el !== target) {
            var text2 = el.value;
            cbWithDedup(el, userTriggeredOnInput ? {
              text: text2,
              isChecked: !isChecked,
              userTriggered: false
            } : {
              text: text2,
              isChecked: !isChecked
            });
          }
        });
      }
    }
    function cbWithDedup(target, v22) {
      var lastInputValue = lastInputValueMap.get(target);
      if (!lastInputValue || lastInputValue.text !== v22.text || lastInputValue.isChecked !== v22.isChecked) {
        lastInputValueMap.set(target, v22);
        var id = mirror2.getId(target);
        callbackWrapper(inputCb)(_extends({}, v22, {
          id
        }));
      }
    }
    var events = sampling.input === "last" ? [
      "change"
    ] : [
      "input",
      "change"
    ];
    var handlers = events.map(function(eventName) {
      return on2(eventName, callbackWrapper(eventHandler), doc);
    });
    var currentWindow = doc.defaultView;
    if (!currentWindow) {
      return function() {
        handlers.forEach(function(h3) {
          return h3();
        });
      };
    }
    var propertyDescriptor = currentWindow.Object.getOwnPropertyDescriptor(currentWindow.HTMLInputElement.prototype, "value");
    var hookProperties = [
      [
        currentWindow.HTMLInputElement.prototype,
        "value"
      ],
      [
        currentWindow.HTMLInputElement.prototype,
        "checked"
      ],
      [
        currentWindow.HTMLSelectElement.prototype,
        "value"
      ],
      [
        currentWindow.HTMLTextAreaElement.prototype,
        "value"
      ],
      // Some UI library use selectedIndex to set select value
      [
        currentWindow.HTMLSelectElement.prototype,
        "selectedIndex"
      ],
      [
        currentWindow.HTMLOptionElement.prototype,
        "selected"
      ]
    ];
    if (propertyDescriptor && propertyDescriptor.set) {
      var _handlers;
      (_handlers = handlers).push.apply(_handlers, [].concat(hookProperties.map(function(p3) {
        return hookSetter(p3[0], p3[1], {
          set: function set2() {
            callbackWrapper(eventHandler)({
              target: this,
              isTrusted: false
            });
          }
        }, false, currentWindow);
      })));
    }
    return callbackWrapper(function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function getNestedCSSRulePositions(rule2) {
    var positions = [];
    function recurse(childRule, pos) {
      if (hasNestedCSSRule("CSSGroupingRule") && _instanceof(childRule.parentRule, CSSGroupingRule) || hasNestedCSSRule("CSSMediaRule") && _instanceof(childRule.parentRule, CSSMediaRule) || hasNestedCSSRule("CSSSupportsRule") && _instanceof(childRule.parentRule, CSSSupportsRule) || hasNestedCSSRule("CSSConditionRule") && _instanceof(childRule.parentRule, CSSConditionRule)) {
        var rules2 = Array.from(childRule.parentRule.cssRules);
        var index2 = rules2.indexOf(childRule);
        pos.unshift(index2);
      } else if (childRule.parentStyleSheet) {
        var rules21 = Array.from(childRule.parentStyleSheet.cssRules);
        var index21 = rules21.indexOf(childRule);
        pos.unshift(index21);
      }
      return pos;
    }
    return recurse(rule2, positions);
  }
  function getIdAndStyleId(sheet, mirror2, styleMirror) {
    var id, styleId;
    if (!sheet) return {};
    if (sheet.ownerNode) id = mirror2.getId(sheet.ownerNode);
    else styleId = styleMirror.getId(sheet);
    return {
      styleId,
      id
    };
  }
  function initStyleSheetObserver(param, param1) {
    var styleSheetRuleCb = param.styleSheetRuleCb, mirror2 = param.mirror, stylesheetManager = param.stylesheetManager;
    var win2 = param1.win;
    if (!win2.CSSStyleSheet || !win2.CSSStyleSheet.prototype) {
      return function() {
      };
    }
    var insertRule = win2.CSSStyleSheet.prototype.insertRule;
    win2.CSSStyleSheet.prototype.insertRule = new Proxy(insertRule, {
      apply: callbackWrapper(function(target, thisArg, argumentsList) {
        var rule2 = argumentsList[0], index2 = argumentsList[1];
        var _getIdAndStyleId = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
        if (id && id !== -1 || styleId && styleId !== -1) {
          styleSheetRuleCb({
            id,
            styleId,
            adds: [
              {
                rule: rule2,
                index: index2
              }
            ]
          });
        }
        return target.apply(thisArg, argumentsList);
      })
    });
    win2.CSSStyleSheet.prototype.addRule = function(selector, styleBlock, index2) {
      if (index2 === void 0) index2 = this.cssRules.length;
      var rule2 = selector + " { " + styleBlock + " }";
      return win2.CSSStyleSheet.prototype.insertRule.apply(this, [
        rule2,
        index2
      ]);
    };
    var deleteRule = win2.CSSStyleSheet.prototype.deleteRule;
    win2.CSSStyleSheet.prototype.deleteRule = new Proxy(deleteRule, {
      apply: callbackWrapper(function(target, thisArg, argumentsList) {
        var index2 = argumentsList[0];
        var _getIdAndStyleId = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
        if (id && id !== -1 || styleId && styleId !== -1) {
          styleSheetRuleCb({
            id,
            styleId,
            removes: [
              {
                index: index2
              }
            ]
          });
        }
        return target.apply(thisArg, argumentsList);
      })
    });
    win2.CSSStyleSheet.prototype.removeRule = function(index2) {
      return win2.CSSStyleSheet.prototype.deleteRule.apply(this, [
        index2
      ]);
    };
    var replace;
    if (win2.CSSStyleSheet.prototype.replace) {
      replace = win2.CSSStyleSheet.prototype.replace;
      win2.CSSStyleSheet.prototype.replace = new Proxy(replace, {
        apply: callbackWrapper(function(target, thisArg, argumentsList) {
          var text = argumentsList[0];
          var _getIdAndStyleId = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
          if (id && id !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id,
              styleId,
              replace: text
            });
          }
          return target.apply(thisArg, argumentsList);
        })
      });
    }
    var replaceSync;
    if (win2.CSSStyleSheet.prototype.replaceSync) {
      replaceSync = win2.CSSStyleSheet.prototype.replaceSync;
      win2.CSSStyleSheet.prototype.replaceSync = new Proxy(replaceSync, {
        apply: callbackWrapper(function(target, thisArg, argumentsList) {
          var text = argumentsList[0];
          var _getIdAndStyleId = getIdAndStyleId(thisArg, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
          if (id && id !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id,
              styleId,
              replaceSync: text
            });
          }
          return target.apply(thisArg, argumentsList);
        })
      });
    }
    var supportedNestedCSSRuleTypes = {};
    if (canMonkeyPatchNestedCSSRule("CSSGroupingRule")) {
      supportedNestedCSSRuleTypes.CSSGroupingRule = win2.CSSGroupingRule;
    } else {
      if (canMonkeyPatchNestedCSSRule("CSSMediaRule")) {
        supportedNestedCSSRuleTypes.CSSMediaRule = win2.CSSMediaRule;
      }
      if (canMonkeyPatchNestedCSSRule("CSSConditionRule")) {
        supportedNestedCSSRuleTypes.CSSConditionRule = win2.CSSConditionRule;
      }
      if (canMonkeyPatchNestedCSSRule("CSSSupportsRule")) {
        supportedNestedCSSRuleTypes.CSSSupportsRule = win2.CSSSupportsRule;
      }
    }
    var unmodifiedFunctions = {};
    Object.entries(supportedNestedCSSRuleTypes).forEach(function(param2) {
      var typeKey = param2[0], type = param2[1];
      unmodifiedFunctions[typeKey] = {
        // eslint-disable-next-line @typescript-eslint/unbound-method
        insertRule: type.prototype.insertRule,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        deleteRule: type.prototype.deleteRule
      };
      type.prototype.insertRule = new Proxy(unmodifiedFunctions[typeKey].insertRule, {
        apply: callbackWrapper(function(target, thisArg, argumentsList) {
          var rule2 = argumentsList[0], index2 = argumentsList[1];
          var _getIdAndStyleId = getIdAndStyleId(thisArg.parentStyleSheet, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
          if (id && id !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id,
              styleId,
              adds: [
                {
                  rule: rule2,
                  index: [].concat(getNestedCSSRulePositions(thisArg), [
                    index2 || 0
                  ])
                }
              ]
            });
          }
          return target.apply(thisArg, argumentsList);
        })
      });
      type.prototype.deleteRule = new Proxy(unmodifiedFunctions[typeKey].deleteRule, {
        apply: callbackWrapper(function(target, thisArg, argumentsList) {
          var index2 = argumentsList[0];
          var _getIdAndStyleId = getIdAndStyleId(thisArg.parentStyleSheet, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
          if (id && id !== -1 || styleId && styleId !== -1) {
            styleSheetRuleCb({
              id,
              styleId,
              removes: [
                {
                  index: [].concat(getNestedCSSRulePositions(thisArg), [
                    index2
                  ])
                }
              ]
            });
          }
          return target.apply(thisArg, argumentsList);
        })
      });
    });
    return callbackWrapper(function() {
      win2.CSSStyleSheet.prototype.insertRule = insertRule;
      win2.CSSStyleSheet.prototype.deleteRule = deleteRule;
      replace && (win2.CSSStyleSheet.prototype.replace = replace);
      replaceSync && (win2.CSSStyleSheet.prototype.replaceSync = replaceSync);
      Object.entries(supportedNestedCSSRuleTypes).forEach(function(param2) {
        var typeKey = param2[0], type = param2[1];
        type.prototype.insertRule = unmodifiedFunctions[typeKey].insertRule;
        type.prototype.deleteRule = unmodifiedFunctions[typeKey].deleteRule;
      });
    });
  }
  function initAdoptedStyleSheetObserver(param, host2) {
    var mirror2 = param.mirror, stylesheetManager = param.stylesheetManager;
    var _a2, _b, _c;
    var hostId = null;
    if (host2.nodeName === "#document") hostId = mirror2.getId(host2);
    else hostId = mirror2.getId(index$2.host(host2));
    var patchTarget = host2.nodeName === "#document" ? (_a2 = host2.defaultView) == null ? void 0 : _a2.Document : (_c = (_b = host2.ownerDocument) == null ? void 0 : _b.defaultView) == null ? void 0 : _c.ShadowRoot;
    var originalPropertyDescriptor = (patchTarget == null ? void 0 : patchTarget.prototype) ? Object.getOwnPropertyDescriptor(patchTarget == null ? void 0 : patchTarget.prototype, "adoptedStyleSheets") : void 0;
    if (hostId === null || hostId === -1 || !patchTarget || !originalPropertyDescriptor) return function() {
    };
    Object.defineProperty(host2, "adoptedStyleSheets", {
      configurable: originalPropertyDescriptor.configurable,
      enumerable: originalPropertyDescriptor.enumerable,
      get: function get() {
        var _a3;
        return (_a3 = originalPropertyDescriptor.get) == null ? void 0 : _a3.call(this);
      },
      set: function set2(sheets) {
        var _a3;
        var result2 = (_a3 = originalPropertyDescriptor.set) == null ? void 0 : _a3.call(this, sheets);
        if (hostId !== null && hostId !== -1) {
          try {
            stylesheetManager.adoptStyleSheets(sheets, hostId);
          } catch (e22) {
          }
        }
        return result2;
      }
    });
    return callbackWrapper(function() {
      Object.defineProperty(host2, "adoptedStyleSheets", {
        configurable: originalPropertyDescriptor.configurable,
        enumerable: originalPropertyDescriptor.enumerable,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        get: originalPropertyDescriptor.get,
        // eslint-disable-next-line @typescript-eslint/unbound-method
        set: originalPropertyDescriptor.set
      });
    });
  }
  function initStyleDeclarationObserver(param, param1) {
    var styleDeclarationCb = param.styleDeclarationCb, mirror2 = param.mirror, ignoreCSSAttributes = param.ignoreCSSAttributes, stylesheetManager = param.stylesheetManager;
    var win2 = param1.win;
    var setProperty = win2.CSSStyleDeclaration.prototype.setProperty;
    win2.CSSStyleDeclaration.prototype.setProperty = new Proxy(setProperty, {
      apply: callbackWrapper(function(target, thisArg, argumentsList) {
        var _a2;
        var property = argumentsList[0], value = argumentsList[1], priority = argumentsList[2];
        if (ignoreCSSAttributes.has(property)) {
          return setProperty.apply(thisArg, [
            property,
            value,
            priority
          ]);
        }
        var _getIdAndStyleId = getIdAndStyleId((_a2 = thisArg.parentRule) == null ? void 0 : _a2.parentStyleSheet, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
        if (id && id !== -1 || styleId && styleId !== -1) {
          styleDeclarationCb({
            id,
            styleId,
            set: {
              property,
              value,
              priority
            },
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            index: getNestedCSSRulePositions(thisArg.parentRule)
          });
        }
        return target.apply(thisArg, argumentsList);
      })
    });
    var removeProperty = win2.CSSStyleDeclaration.prototype.removeProperty;
    win2.CSSStyleDeclaration.prototype.removeProperty = new Proxy(removeProperty, {
      apply: callbackWrapper(function(target, thisArg, argumentsList) {
        var _a2;
        var property = argumentsList[0];
        if (ignoreCSSAttributes.has(property)) {
          return removeProperty.apply(thisArg, [
            property
          ]);
        }
        var _getIdAndStyleId = getIdAndStyleId((_a2 = thisArg.parentRule) == null ? void 0 : _a2.parentStyleSheet, mirror2, stylesheetManager.styleMirror), id = _getIdAndStyleId.id, styleId = _getIdAndStyleId.styleId;
        if (id && id !== -1 || styleId && styleId !== -1) {
          styleDeclarationCb({
            id,
            styleId,
            remove: {
              property
            },
            // eslint-disable-next-line @typescript-eslint/no-non-null-assertion
            index: getNestedCSSRulePositions(thisArg.parentRule)
          });
        }
        return target.apply(thisArg, argumentsList);
      })
    });
    return callbackWrapper(function() {
      win2.CSSStyleDeclaration.prototype.setProperty = setProperty;
      win2.CSSStyleDeclaration.prototype.removeProperty = removeProperty;
    });
  }
  function initMediaInteractionObserver(param) {
    var mediaInteractionCb = param.mediaInteractionCb, blockClass = param.blockClass, blockSelector = param.blockSelector, mirror2 = param.mirror, sampling = param.sampling, doc = param.doc;
    var handler = callbackWrapper(function(type) {
      return throttle(callbackWrapper(function(event) {
        var target = getEventTarget(event);
        if (!target || isBlocked(target, blockClass, blockSelector, true)) {
          return;
        }
        var currentTime = target.currentTime, volume = target.volume, muted = target.muted, playbackRate = target.playbackRate, loop = target.loop;
        mediaInteractionCb({
          type,
          id: mirror2.getId(target),
          currentTime,
          volume,
          muted,
          playbackRate,
          loop
        });
      }), sampling.media || 500);
    });
    var handlers = [
      on2("play", handler(MediaInteractions.Play), doc),
      on2("pause", handler(MediaInteractions.Pause), doc),
      on2("seeked", handler(MediaInteractions.Seeked), doc),
      on2("volumechange", handler(MediaInteractions.VolumeChange), doc),
      on2("ratechange", handler(MediaInteractions.RateChange), doc)
    ];
    return callbackWrapper(function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function initFontObserver(param) {
    var fontCb = param.fontCb, doc = param.doc;
    var win2 = doc.defaultView;
    if (!win2) {
      return function() {
      };
    }
    var handlers = [];
    var fontMap = /* @__PURE__ */ new WeakMap();
    var originalFontFace = win2.FontFace;
    win2.FontFace = function FontFace2(family, source, descriptors) {
      var fontFace = new originalFontFace(family, source, descriptors);
      fontMap.set(fontFace, {
        family,
        buffer: typeof source !== "string",
        descriptors,
        fontSource: typeof source === "string" ? source : JSON.stringify(Array.from(new Uint8Array(source)))
      });
      return fontFace;
    };
    var restoreHandler = patch$2(doc.fonts, "add", function(original) {
      return function(fontFace) {
        setTimeout(callbackWrapper(function() {
          var p3 = fontMap.get(fontFace);
          if (p3) {
            fontCb(p3);
            fontMap.delete(fontFace);
          }
        }), 0);
        return original.apply(this, [
          fontFace
        ]);
      };
    });
    handlers.push(function() {
      win2.FontFace = originalFontFace;
    });
    handlers.push(restoreHandler);
    return callbackWrapper(function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function initSelectionObserver(param) {
    var doc = param.doc, mirror2 = param.mirror, blockClass = param.blockClass, blockSelector = param.blockSelector, selectionCb = param.selectionCb;
    var collapsed = true;
    var updateSelection = callbackWrapper(function() {
      var selection = doc.getSelection();
      if (!selection || collapsed && (selection == null ? void 0 : selection.isCollapsed)) return;
      collapsed = selection.isCollapsed || false;
      var ranges = [];
      var count = selection.rangeCount || 0;
      for (var i22 = 0; i22 < count; i22++) {
        var range = selection.getRangeAt(i22);
        var startContainer = range.startContainer, startOffset = range.startOffset, endContainer = range.endContainer, endOffset = range.endOffset;
        var blocked = isBlocked(startContainer, blockClass, blockSelector, true) || isBlocked(endContainer, blockClass, blockSelector, true);
        if (blocked) continue;
        ranges.push({
          start: mirror2.getId(startContainer),
          startOffset,
          end: mirror2.getId(endContainer),
          endOffset
        });
      }
      selectionCb({
        ranges
      });
    });
    updateSelection();
    return on2("selectionchange", updateSelection);
  }
  function initCustomElementObserver(param) {
    var doc = param.doc, customElementCb = param.customElementCb;
    var win2 = doc.defaultView;
    if (!win2 || !win2.customElements) return function() {
    };
    var restoreHandler = patch$2(win2.customElements, "define", function(original) {
      return function(name5, constructor, options) {
        try {
          customElementCb({
            define: {
              name: name5
            }
          });
        } catch (e22) {
          console.warn("Custom element callback failed for " + name5);
        }
        return original.apply(this, [
          name5,
          constructor,
          options
        ]);
      };
    });
    return restoreHandler;
  }
  function mergeHooks(o22, hooks) {
    var mutationCb = o22.mutationCb, mousemoveCb = o22.mousemoveCb, mouseInteractionCb = o22.mouseInteractionCb, scrollCb = o22.scrollCb, viewportResizeCb = o22.viewportResizeCb, inputCb = o22.inputCb, mediaInteractionCb = o22.mediaInteractionCb, styleSheetRuleCb = o22.styleSheetRuleCb, styleDeclarationCb = o22.styleDeclarationCb, canvasMutationCb = o22.canvasMutationCb, fontCb = o22.fontCb, selectionCb = o22.selectionCb, customElementCb = o22.customElementCb;
    o22.mutationCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.mutation) {
        var _hooks;
        (_hooks = hooks).mutation.apply(_hooks, [].concat(p3));
      }
      mutationCb.apply(void 0, [].concat(p3));
    };
    o22.mousemoveCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.mousemove) {
        var _hooks;
        (_hooks = hooks).mousemove.apply(_hooks, [].concat(p3));
      }
      mousemoveCb.apply(void 0, [].concat(p3));
    };
    o22.mouseInteractionCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.mouseInteraction) {
        var _hooks;
        (_hooks = hooks).mouseInteraction.apply(_hooks, [].concat(p3));
      }
      mouseInteractionCb.apply(void 0, [].concat(p3));
    };
    o22.scrollCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.scroll) {
        var _hooks;
        (_hooks = hooks).scroll.apply(_hooks, [].concat(p3));
      }
      scrollCb.apply(void 0, [].concat(p3));
    };
    o22.viewportResizeCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.viewportResize) {
        var _hooks;
        (_hooks = hooks).viewportResize.apply(_hooks, [].concat(p3));
      }
      viewportResizeCb.apply(void 0, [].concat(p3));
    };
    o22.inputCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.input) {
        var _hooks;
        (_hooks = hooks).input.apply(_hooks, [].concat(p3));
      }
      inputCb.apply(void 0, [].concat(p3));
    };
    o22.mediaInteractionCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.mediaInteaction) {
        var _hooks;
        (_hooks = hooks).mediaInteaction.apply(_hooks, [].concat(p3));
      }
      mediaInteractionCb.apply(void 0, [].concat(p3));
    };
    o22.styleSheetRuleCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.styleSheetRule) {
        var _hooks;
        (_hooks = hooks).styleSheetRule.apply(_hooks, [].concat(p3));
      }
      styleSheetRuleCb.apply(void 0, [].concat(p3));
    };
    o22.styleDeclarationCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.styleDeclaration) {
        var _hooks;
        (_hooks = hooks).styleDeclaration.apply(_hooks, [].concat(p3));
      }
      styleDeclarationCb.apply(void 0, [].concat(p3));
    };
    o22.canvasMutationCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.canvasMutation) {
        var _hooks;
        (_hooks = hooks).canvasMutation.apply(_hooks, [].concat(p3));
      }
      canvasMutationCb.apply(void 0, [].concat(p3));
    };
    o22.fontCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.font) {
        var _hooks;
        (_hooks = hooks).font.apply(_hooks, [].concat(p3));
      }
      fontCb.apply(void 0, [].concat(p3));
    };
    o22.selectionCb = function() {
      for (var _len = arguments.length, p3 = new Array(_len), _key = 0; _key < _len; _key++) {
        p3[_key] = arguments[_key];
      }
      if (hooks.selection) {
        var _hooks;
        (_hooks = hooks).selection.apply(_hooks, [].concat(p3));
      }
      selectionCb.apply(void 0, [].concat(p3));
    };
    o22.customElementCb = function() {
      for (var _len = arguments.length, c22 = new Array(_len), _key = 0; _key < _len; _key++) {
        c22[_key] = arguments[_key];
      }
      if (hooks.customElement) {
        var _hooks;
        (_hooks = hooks).customElement.apply(_hooks, [].concat(c22));
      }
      customElementCb.apply(void 0, [].concat(c22));
    };
  }
  function initObservers(o22, hooks) {
    if (hooks === void 0) hooks = {};
    var currentWindow = o22.doc.defaultView;
    if (!currentWindow) {
      return function() {
      };
    }
    mergeHooks(o22, hooks);
    var mutationObserver;
    if (o22.recordDOM) {
      mutationObserver = initMutationObserver(o22, o22.doc);
    }
    var mousemoveHandler = initMoveObserver(o22);
    var mouseInteractionHandler = initMouseInteractionObserver(o22);
    var scrollHandler = initScrollObserver(o22);
    var viewportResizeHandler = initViewportResizeObserver(o22, {
      win: currentWindow
    });
    var inputHandler = initInputObserver(o22);
    var mediaInteractionHandler = initMediaInteractionObserver(o22);
    var styleSheetObserver = function() {
    };
    var adoptedStyleSheetObserver = function() {
    };
    var styleDeclarationObserver = function() {
    };
    var fontObserver = function() {
    };
    if (o22.recordDOM) {
      styleSheetObserver = initStyleSheetObserver(o22, {
        win: currentWindow
      });
      adoptedStyleSheetObserver = initAdoptedStyleSheetObserver(o22, o22.doc);
      styleDeclarationObserver = initStyleDeclarationObserver(o22, {
        win: currentWindow
      });
      if (o22.collectFonts) {
        fontObserver = initFontObserver(o22);
      }
    }
    var selectionObserver = initSelectionObserver(o22);
    var customElementObserver = initCustomElementObserver(o22);
    var pluginHandlers = [];
    for (var _iterator = _create_for_of_iterator_helper_loose(o22.plugins), _step; !(_step = _iterator()).done; ) {
      var plugin32 = _step.value;
      pluginHandlers.push(plugin32.observer(plugin32.callback, currentWindow, plugin32.options));
    }
    return callbackWrapper(function() {
      mutationBuffers.forEach(function(b2) {
        return b2.reset();
      });
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
      mousemoveHandler();
      mouseInteractionHandler();
      scrollHandler();
      viewportResizeHandler();
      inputHandler();
      mediaInteractionHandler();
      styleSheetObserver();
      adoptedStyleSheetObserver();
      styleDeclarationObserver();
      fontObserver();
      selectionObserver();
      customElementObserver();
      pluginHandlers.forEach(function(h3) {
        return h3();
      });
    });
  }
  function hasNestedCSSRule(prop) {
    return typeof window[prop] !== "undefined";
  }
  function canMonkeyPatchNestedCSSRule(prop) {
    return Boolean(typeof window[prop] !== "undefined" && // Note: Generally, this check _shouldn't_ be necessary
    // However, in some scenarios (e.g. jsdom) this can sometimes fail, so we check for it here
    window[prop].prototype && "insertRule" in window[prop].prototype && "deleteRule" in window[prop].prototype);
  }
  var CrossOriginIframeMirror = /* @__PURE__ */ (function() {
    function CrossOriginIframeMirror2(generateIdFn) {
      __publicField$1(this, "iframeIdToRemoteIdMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "iframeRemoteIdToIdMap", /* @__PURE__ */ new WeakMap());
      this.generateIdFn = generateIdFn;
    }
    var _proto = CrossOriginIframeMirror2.prototype;
    _proto.getId = function getId3(iframe, remoteId, idToRemoteMap, remoteToIdMap) {
      var idToRemoteIdMap = idToRemoteMap || this.getIdToRemoteIdMap(iframe);
      var remoteIdToIdMap = remoteToIdMap || this.getRemoteIdToIdMap(iframe);
      var id = idToRemoteIdMap.get(remoteId);
      if (!id) {
        id = this.generateIdFn();
        idToRemoteIdMap.set(remoteId, id);
        remoteIdToIdMap.set(id, remoteId);
      }
      return id;
    };
    _proto.getIds = function getIds(iframe, remoteId) {
      var _this = this;
      var idToRemoteIdMap = this.getIdToRemoteIdMap(iframe);
      var remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
      return remoteId.map(function(id) {
        return _this.getId(iframe, id, idToRemoteIdMap, remoteIdToIdMap);
      });
    };
    _proto.getRemoteId = function getRemoteId(iframe, id, map) {
      var remoteIdToIdMap = map || this.getRemoteIdToIdMap(iframe);
      if (typeof id !== "number") return id;
      var remoteId = remoteIdToIdMap.get(id);
      if (!remoteId) return -1;
      return remoteId;
    };
    _proto.getRemoteIds = function getRemoteIds(iframe, ids) {
      var _this = this;
      var remoteIdToIdMap = this.getRemoteIdToIdMap(iframe);
      return ids.map(function(id) {
        return _this.getRemoteId(iframe, id, remoteIdToIdMap);
      });
    };
    _proto.reset = function reset2(iframe) {
      if (!iframe) {
        this.iframeIdToRemoteIdMap = /* @__PURE__ */ new WeakMap();
        this.iframeRemoteIdToIdMap = /* @__PURE__ */ new WeakMap();
        return;
      }
      this.iframeIdToRemoteIdMap.delete(iframe);
      this.iframeRemoteIdToIdMap.delete(iframe);
    };
    _proto.getIdToRemoteIdMap = function getIdToRemoteIdMap(iframe) {
      var idToRemoteIdMap = this.iframeIdToRemoteIdMap.get(iframe);
      if (!idToRemoteIdMap) {
        idToRemoteIdMap = /* @__PURE__ */ new Map();
        this.iframeIdToRemoteIdMap.set(iframe, idToRemoteIdMap);
      }
      return idToRemoteIdMap;
    };
    _proto.getRemoteIdToIdMap = function getRemoteIdToIdMap(iframe) {
      var remoteIdToIdMap = this.iframeRemoteIdToIdMap.get(iframe);
      if (!remoteIdToIdMap) {
        remoteIdToIdMap = /* @__PURE__ */ new Map();
        this.iframeRemoteIdToIdMap.set(iframe, remoteIdToIdMap);
      }
      return remoteIdToIdMap;
    };
    return CrossOriginIframeMirror2;
  })();
  var IframeManager = /* @__PURE__ */ (function() {
    function IframeManager2(options) {
      __publicField$1(this, "iframes", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "crossOriginIframeMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "crossOriginIframeMirror", new CrossOriginIframeMirror(genId));
      __publicField$1(this, "crossOriginIframeStyleMirror");
      __publicField$1(this, "crossOriginIframeRootIdMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "mirror");
      __publicField$1(this, "mutationCb");
      __publicField$1(this, "wrappedEmit");
      __publicField$1(this, "loadListener");
      __publicField$1(this, "stylesheetManager");
      __publicField$1(this, "recordCrossOriginIframes");
      this.mutationCb = options.mutationCb;
      this.wrappedEmit = options.wrappedEmit;
      this.stylesheetManager = options.stylesheetManager;
      this.recordCrossOriginIframes = options.recordCrossOriginIframes;
      this.crossOriginIframeStyleMirror = new CrossOriginIframeMirror(this.stylesheetManager.styleMirror.generateId.bind(this.stylesheetManager.styleMirror));
      this.mirror = options.mirror;
      if (this.recordCrossOriginIframes) {
        window.addEventListener("message", this.handleMessage.bind(this));
      }
    }
    var _proto = IframeManager2.prototype;
    _proto.addIframe = function addIframe(iframeEl) {
      this.iframes.set(iframeEl, true);
      if (iframeEl.contentWindow) this.crossOriginIframeMap.set(iframeEl.contentWindow, iframeEl);
    };
    _proto.addLoadListener = function addLoadListener(cb) {
      this.loadListener = cb;
    };
    _proto.attachIframe = function attachIframe(iframeEl, childSn) {
      var _a2, _b;
      this.mutationCb({
        adds: [
          {
            parentId: this.mirror.getId(iframeEl),
            nextId: null,
            node: childSn
          }
        ],
        removes: [],
        texts: [],
        attributes: [],
        isAttachIframe: true
      });
      if (this.recordCrossOriginIframes) (_a2 = iframeEl.contentWindow) == null ? void 0 : _a2.addEventListener("message", this.handleMessage.bind(this));
      (_b = this.loadListener) == null ? void 0 : _b.call(this, iframeEl);
      if (iframeEl.contentDocument && iframeEl.contentDocument.adoptedStyleSheets && iframeEl.contentDocument.adoptedStyleSheets.length > 0) this.stylesheetManager.adoptStyleSheets(iframeEl.contentDocument.adoptedStyleSheets, this.mirror.getId(iframeEl.contentDocument));
    };
    _proto.handleMessage = function handleMessage(message) {
      var crossOriginMessageEvent = message;
      if (crossOriginMessageEvent.data.type !== "rrweb" || // To filter out the rrweb messages which are forwarded by some sites.
      crossOriginMessageEvent.origin !== crossOriginMessageEvent.data.origin) return;
      var iframeSourceWindow = message.source;
      if (!iframeSourceWindow) return;
      var iframeEl = this.crossOriginIframeMap.get(message.source);
      if (!iframeEl) return;
      var transformedEvent = this.transformCrossOriginEvent(iframeEl, crossOriginMessageEvent.data.event);
      if (transformedEvent) this.wrappedEmit(transformedEvent, crossOriginMessageEvent.data.isCheckout);
    };
    _proto.transformCrossOriginEvent = function transformCrossOriginEvent(iframeEl, e22) {
      var _this = this;
      var _a2;
      switch (e22.type) {
        case EventType.FullSnapshot: {
          this.crossOriginIframeMirror.reset(iframeEl);
          this.crossOriginIframeStyleMirror.reset(iframeEl);
          this.replaceIdOnNode(e22.data.node, iframeEl);
          var rootId = e22.data.node.id;
          this.crossOriginIframeRootIdMap.set(iframeEl, rootId);
          this.patchRootIdOnNode(e22.data.node, rootId);
          return {
            timestamp: e22.timestamp,
            type: EventType.IncrementalSnapshot,
            data: {
              source: IncrementalSource.Mutation,
              adds: [
                {
                  parentId: this.mirror.getId(iframeEl),
                  nextId: null,
                  node: e22.data.node
                }
              ],
              removes: [],
              texts: [],
              attributes: [],
              isAttachIframe: true
            }
          };
        }
        case EventType.Meta:
        case EventType.Load:
        case EventType.DomContentLoaded: {
          return false;
        }
        case EventType.Plugin: {
          return e22;
        }
        case EventType.Custom: {
          this.replaceIds(e22.data.payload, iframeEl, [
            "id",
            "parentId",
            "previousId",
            "nextId"
          ]);
          return e22;
        }
        case EventType.IncrementalSnapshot: {
          switch (e22.data.source) {
            case IncrementalSource.Mutation: {
              e22.data.adds.forEach(function(n22) {
                _this.replaceIds(n22, iframeEl, [
                  "parentId",
                  "nextId",
                  "previousId"
                ]);
                _this.replaceIdOnNode(n22.node, iframeEl);
                var rootId2 = _this.crossOriginIframeRootIdMap.get(iframeEl);
                rootId2 && _this.patchRootIdOnNode(n22.node, rootId2);
              });
              e22.data.removes.forEach(function(n22) {
                _this.replaceIds(n22, iframeEl, [
                  "parentId",
                  "id"
                ]);
              });
              e22.data.attributes.forEach(function(n22) {
                _this.replaceIds(n22, iframeEl, [
                  "id"
                ]);
              });
              e22.data.texts.forEach(function(n22) {
                _this.replaceIds(n22, iframeEl, [
                  "id"
                ]);
              });
              return e22;
            }
            case IncrementalSource.Drag:
            case IncrementalSource.TouchMove:
            case IncrementalSource.MouseMove: {
              e22.data.positions.forEach(function(p3) {
                _this.replaceIds(p3, iframeEl, [
                  "id"
                ]);
              });
              return e22;
            }
            case IncrementalSource.ViewportResize: {
              return false;
            }
            case IncrementalSource.MediaInteraction:
            case IncrementalSource.MouseInteraction:
            case IncrementalSource.Scroll:
            case IncrementalSource.CanvasMutation:
            case IncrementalSource.Input: {
              this.replaceIds(e22.data, iframeEl, [
                "id"
              ]);
              return e22;
            }
            case IncrementalSource.StyleSheetRule:
            case IncrementalSource.StyleDeclaration: {
              this.replaceIds(e22.data, iframeEl, [
                "id"
              ]);
              this.replaceStyleIds(e22.data, iframeEl, [
                "styleId"
              ]);
              return e22;
            }
            case IncrementalSource.Font: {
              return e22;
            }
            case IncrementalSource.Selection: {
              e22.data.ranges.forEach(function(range) {
                _this.replaceIds(range, iframeEl, [
                  "start",
                  "end"
                ]);
              });
              return e22;
            }
            case IncrementalSource.AdoptedStyleSheet: {
              this.replaceIds(e22.data, iframeEl, [
                "id"
              ]);
              this.replaceStyleIds(e22.data, iframeEl, [
                "styleIds"
              ]);
              (_a2 = e22.data.styles) == null ? void 0 : _a2.forEach(function(style) {
                _this.replaceStyleIds(style, iframeEl, [
                  "styleId"
                ]);
              });
              return e22;
            }
          }
        }
      }
      return false;
    };
    _proto.replace = function replace(iframeMirror, obj, iframeEl, keys) {
      for (var _iterator = _create_for_of_iterator_helper_loose(keys), _step; !(_step = _iterator()).done; ) {
        var key = _step.value;
        if (!Array.isArray(obj[key]) && typeof obj[key] !== "number") continue;
        if (Array.isArray(obj[key])) {
          obj[key] = iframeMirror.getIds(iframeEl, obj[key]);
        } else {
          obj[key] = iframeMirror.getId(iframeEl, obj[key]);
        }
      }
      return obj;
    };
    _proto.replaceIds = function replaceIds(obj, iframeEl, keys) {
      return this.replace(this.crossOriginIframeMirror, obj, iframeEl, keys);
    };
    _proto.replaceStyleIds = function replaceStyleIds(obj, iframeEl, keys) {
      return this.replace(this.crossOriginIframeStyleMirror, obj, iframeEl, keys);
    };
    _proto.replaceIdOnNode = function replaceIdOnNode(node2, iframeEl) {
      var _this = this;
      this.replaceIds(node2, iframeEl, [
        "id",
        "rootId"
      ]);
      if ("childNodes" in node2) {
        node2.childNodes.forEach(function(child) {
          _this.replaceIdOnNode(child, iframeEl);
        });
      }
    };
    _proto.patchRootIdOnNode = function patchRootIdOnNode(node2, rootId) {
      var _this = this;
      if (node2.type !== NodeType.Document && !node2.rootId) node2.rootId = rootId;
      if ("childNodes" in node2) {
        node2.childNodes.forEach(function(child) {
          _this.patchRootIdOnNode(child, rootId);
        });
      }
    };
    return IframeManager2;
  })();
  var ShadowDomManager = /* @__PURE__ */ (function() {
    function ShadowDomManager2(options) {
      __publicField$1(this, "shadowDoms", /* @__PURE__ */ new WeakSet());
      __publicField$1(this, "mutationCb");
      __publicField$1(this, "scrollCb");
      __publicField$1(this, "bypassOptions");
      __publicField$1(this, "mirror");
      __publicField$1(this, "restoreHandlers", []);
      this.mutationCb = options.mutationCb;
      this.scrollCb = options.scrollCb;
      this.bypassOptions = options.bypassOptions;
      this.mirror = options.mirror;
      this.init();
    }
    var _proto = ShadowDomManager2.prototype;
    _proto.init = function init() {
      this.reset();
      this.patchAttachShadow(Element, document);
    };
    _proto.addShadowRoot = function addShadowRoot(shadowRoot2, doc) {
      var _this = this;
      if (!isNativeShadowDom(shadowRoot2)) return;
      if (this.shadowDoms.has(shadowRoot2)) return;
      this.shadowDoms.add(shadowRoot2);
      var observer = initMutationObserver(_extends({}, this.bypassOptions, {
        doc,
        mutationCb: this.mutationCb,
        mirror: this.mirror,
        shadowDomManager: this
      }), shadowRoot2);
      this.restoreHandlers.push(function() {
        return observer.disconnect();
      });
      this.restoreHandlers.push(initScrollObserver(_extends({}, this.bypassOptions, {
        scrollCb: this.scrollCb,
        // https://gist.github.com/praveenpuglia/0832da687ed5a5d7a0907046c9ef1813
        // scroll is not allowed to pass the boundary, so we need to listen the shadow document
        doc: shadowRoot2,
        mirror: this.mirror
      })));
      setTimeout(function() {
        if (shadowRoot2.adoptedStyleSheets && shadowRoot2.adoptedStyleSheets.length > 0) _this.bypassOptions.stylesheetManager.adoptStyleSheets(shadowRoot2.adoptedStyleSheets, _this.mirror.getId(index$2.host(shadowRoot2)));
        _this.restoreHandlers.push(initAdoptedStyleSheetObserver({
          mirror: _this.mirror,
          stylesheetManager: _this.bypassOptions.stylesheetManager
        }, shadowRoot2));
      }, 0);
    };
    _proto.observeAttachShadow = function observeAttachShadow(iframeElement) {
      if (!iframeElement.contentWindow || !iframeElement.contentDocument) return;
      this.patchAttachShadow(iframeElement.contentWindow.Element, iframeElement.contentDocument);
    };
    _proto.patchAttachShadow = function patchAttachShadow(element, doc) {
      var manager = this;
      this.restoreHandlers.push(patch$2(element.prototype, "attachShadow", function(original) {
        return function(option) {
          var sRoot = original.call(this, option);
          var shadowRootEl = index$2.shadowRoot(this);
          if (shadowRootEl && inDom(this)) manager.addShadowRoot(shadowRootEl, doc);
          return sRoot;
        };
      }));
    };
    _proto.reset = function reset2() {
      this.restoreHandlers.forEach(function(handler) {
        try {
          handler();
        } catch (e22) {
        }
      });
      this.restoreHandlers = [];
      this.shadowDoms = /* @__PURE__ */ new WeakSet();
    };
    return ShadowDomManager2;
  })();
  var chars = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/";
  var lookup = typeof Uint8Array === "undefined" ? [] : new Uint8Array(256);
  for (i$1 = 0; i$1 < chars.length; i$1++) {
    lookup[chars.charCodeAt(i$1)] = i$1;
  }
  var i$1;
  var encode = function encode2(arraybuffer) {
    var bytes = new Uint8Array(arraybuffer), i22, len = bytes.length, base642 = "";
    for (i22 = 0; i22 < len; i22 += 3) {
      base642 += chars[bytes[i22] >> 2];
      base642 += chars[(bytes[i22] & 3) << 4 | bytes[i22 + 1] >> 4];
      base642 += chars[(bytes[i22 + 1] & 15) << 2 | bytes[i22 + 2] >> 6];
      base642 += chars[bytes[i22 + 2] & 63];
    }
    if (len % 3 === 2) {
      base642 = base642.substring(0, base642.length - 1) + "=";
    } else if (len % 3 === 1) {
      base642 = base642.substring(0, base642.length - 2) + "==";
    }
    return base642;
  };
  var canvasVarMap = /* @__PURE__ */ new Map();
  function variableListFor$1(ctx, ctor) {
    var contextMap = canvasVarMap.get(ctx);
    if (!contextMap) {
      contextMap = /* @__PURE__ */ new Map();
      canvasVarMap.set(ctx, contextMap);
    }
    if (!contextMap.has(ctor)) {
      contextMap.set(ctor, []);
    }
    return contextMap.get(ctor);
  }
  var saveWebGLVar = function(value, win2, ctx) {
    if (!value || !(isInstanceOfWebGLObject(value, win2) || (typeof value === "undefined" ? "undefined" : _type_of(value)) === "object")) return;
    var name5 = value.constructor.name;
    var list2 = variableListFor$1(ctx, name5);
    var index2 = list2.indexOf(value);
    if (index2 === -1) {
      index2 = list2.length;
      list2.push(value);
    }
    return index2;
  };
  function serializeArg(value, win2, ctx) {
    if (_instanceof(value, Array)) {
      return value.map(function(arg) {
        return serializeArg(arg, win2, ctx);
      });
    } else if (value === null) {
      return value;
    } else if (_instanceof(value, Float32Array) || _instanceof(value, Float64Array) || _instanceof(value, Int32Array) || _instanceof(value, Uint32Array) || _instanceof(value, Uint8Array) || _instanceof(value, Uint16Array) || _instanceof(value, Int16Array) || _instanceof(value, Int8Array) || _instanceof(value, Uint8ClampedArray)) {
      var name5 = value.constructor.name;
      return {
        rr_type: name5,
        args: [
          Object.values(value)
        ]
      };
    } else if (
      // SharedArrayBuffer disabled on most browsers due to spectre.
      // More info: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/SharedArrayBuffer/SharedArrayBuffer
      // value instanceof SharedArrayBuffer ||
      _instanceof(value, ArrayBuffer)
    ) {
      var name1 = value.constructor.name;
      var base642 = encode(value);
      return {
        rr_type: name1,
        base64: base642
      };
    } else if (_instanceof(value, DataView)) {
      var name22 = value.constructor.name;
      return {
        rr_type: name22,
        args: [
          serializeArg(value.buffer, win2, ctx),
          value.byteOffset,
          value.byteLength
        ]
      };
    } else if (_instanceof(value, HTMLImageElement)) {
      var name32 = value.constructor.name;
      var src = value.src;
      return {
        rr_type: name32,
        src
      };
    } else if (_instanceof(value, HTMLCanvasElement)) {
      var name42 = "HTMLImageElement";
      var src1 = value.toDataURL();
      return {
        rr_type: name42,
        src: src1
      };
    } else if (_instanceof(value, ImageData)) {
      var name52 = value.constructor.name;
      return {
        rr_type: name52,
        args: [
          serializeArg(value.data, win2, ctx),
          value.width,
          value.height
        ]
      };
    } else if (isInstanceOfWebGLObject(value, win2) || (typeof value === "undefined" ? "undefined" : _type_of(value)) === "object") {
      var name6 = value.constructor.name;
      var index2 = saveWebGLVar(value, win2, ctx);
      return {
        rr_type: name6,
        index: index2
      };
    }
    return value;
  }
  var serializeArgs = function(args, win2, ctx) {
    return args.map(function(arg) {
      return serializeArg(arg, win2, ctx);
    });
  };
  var isInstanceOfWebGLObject = function(value, win2) {
    var webGLConstructorNames = [
      "WebGLActiveInfo",
      "WebGLBuffer",
      "WebGLFramebuffer",
      "WebGLProgram",
      "WebGLRenderbuffer",
      "WebGLShader",
      "WebGLShaderPrecisionFormat",
      "WebGLTexture",
      "WebGLUniformLocation",
      "WebGLVertexArrayObject",
      // In old Chrome versions, value won't be an instanceof WebGLVertexArrayObject.
      "WebGLVertexArrayObjectOES"
    ];
    var supportedWebGLConstructorNames = webGLConstructorNames.filter(function(name5) {
      return typeof win2[name5] === "function";
    });
    return Boolean(supportedWebGLConstructorNames.find(function(name5) {
      return _instanceof(value, win2[name5]);
    }));
  };
  function initCanvas2DMutationObserver(cb, win2, blockClass, blockSelector) {
    var _loop = function() {
      var prop = _step.value;
      try {
        if (typeof win2.CanvasRenderingContext2D.prototype[prop] !== "function") {
          return "continue";
        }
        var restoreHandler = patch$2(win2.CanvasRenderingContext2D.prototype, prop, function(original) {
          return function() {
            var _this = this;
            for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
              args[_key] = arguments[_key];
            }
            if (!isBlocked(this.canvas, blockClass, blockSelector, true)) {
              setTimeout(function() {
                var recordArgs = serializeArgs(args, win2, _this);
                cb(_this.canvas, {
                  type: CanvasContext["2D"],
                  property: prop,
                  args: recordArgs
                });
              }, 0);
            }
            return original.apply(this, args);
          };
        });
        handlers.push(restoreHandler);
      } catch (e3) {
        var hookHandler = hookSetter(win2.CanvasRenderingContext2D.prototype, prop, {
          set: function set2(v22) {
            cb(this.canvas, {
              type: CanvasContext["2D"],
              property: prop,
              args: [
                v22
              ],
              setter: true
            });
          }
        });
        handlers.push(hookHandler);
      }
    };
    var handlers = [];
    var props2D = Object.getOwnPropertyNames(win2.CanvasRenderingContext2D.prototype);
    for (var _iterator = _create_for_of_iterator_helper_loose(props2D), _step; !(_step = _iterator()).done; ) _loop();
    return function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    };
  }
  function getNormalizedContextName(contextType) {
    return contextType === "experimental-webgl" ? "webgl" : contextType;
  }
  function initCanvasContextObserver(win2, blockClass, blockSelector, setPreserveDrawingBufferToTrue) {
    var handlers = [];
    try {
      var restoreHandler = patch$2(win2.HTMLCanvasElement.prototype, "getContext", function(original) {
        return function(contextType) {
          for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
            args[_key - 1] = arguments[_key];
          }
          if (!isBlocked(this, blockClass, blockSelector, true)) {
            var ctxName = getNormalizedContextName(contextType);
            if (!("__context" in this)) this.__context = ctxName;
            if (setPreserveDrawingBufferToTrue && [
              "webgl",
              "webgl2"
            ].includes(ctxName)) {
              if (args[0] && _type_of(args[0]) === "object") {
                var contextAttributes = args[0];
                if (!contextAttributes.preserveDrawingBuffer) {
                  contextAttributes.preserveDrawingBuffer = true;
                }
              } else {
                args.splice(0, 1, {
                  preserveDrawingBuffer: true
                });
              }
            }
          }
          return original.apply(this, [].concat([
            contextType
          ], args));
        };
      });
      handlers.push(restoreHandler);
    } catch (e3) {
      console.error("failed to patch HTMLCanvasElement.prototype.getContext");
    }
    return function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    };
  }
  function patchGLPrototype(prototype, type, cb, blockClass, blockSelector, win2) {
    var _loop = function() {
      var prop = _step.value;
      if (
        //prop.startsWith('get') ||  // e.g. getProgramParameter, but too risky
        [
          "isContextLost",
          "canvas",
          "drawingBufferWidth",
          "drawingBufferHeight"
        ].includes(prop)
      ) {
        return "continue";
      }
      try {
        if (typeof prototype[prop] !== "function") {
          return "continue";
        }
        var restoreHandler = patch$2(prototype, prop, function(original) {
          return function() {
            for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
              args[_key] = arguments[_key];
            }
            var result2 = original.apply(this, args);
            saveWebGLVar(result2, win2, this);
            if ("tagName" in this.canvas && !isBlocked(this.canvas, blockClass, blockSelector, true)) {
              var recordArgs = serializeArgs(args, win2, this);
              var mutation = {
                type,
                property: prop,
                args: recordArgs
              };
              cb(this.canvas, mutation);
            }
            return result2;
          };
        });
        handlers.push(restoreHandler);
      } catch (e3) {
        var hookHandler = hookSetter(prototype, prop, {
          set: function set2(v22) {
            cb(this.canvas, {
              type,
              property: prop,
              args: [
                v22
              ],
              setter: true
            });
          }
        });
        handlers.push(hookHandler);
      }
    };
    var handlers = [];
    var props = Object.getOwnPropertyNames(prototype);
    for (var _iterator = _create_for_of_iterator_helper_loose(props), _step; !(_step = _iterator()).done; ) _loop();
    return handlers;
  }
  function initCanvasWebGLMutationObserver(cb, win2, blockClass, blockSelector) {
    var _handlers;
    var handlers = [];
    (_handlers = handlers).push.apply(_handlers, [].concat(patchGLPrototype(win2.WebGLRenderingContext.prototype, CanvasContext.WebGL, cb, blockClass, blockSelector, win2)));
    if (typeof win2.WebGL2RenderingContext !== "undefined") {
      var _handlers1;
      (_handlers1 = handlers).push.apply(_handlers1, [].concat(patchGLPrototype(win2.WebGL2RenderingContext.prototype, CanvasContext.WebGL2, cb, blockClass, blockSelector, win2)));
    }
    return function() {
      handlers.forEach(function(h3) {
        return h3();
      });
    };
  }
  var encodedJs = "";
  var decodeBase64 = function(base642) {
    return Uint8Array.from(atob(base642), function(c22) {
      return c22.charCodeAt(0);
    });
  };
  var blob = typeof window !== "undefined" && window.Blob && new Blob([
    decodeBase64(encodedJs)
  ], {
    type: "text/javascript;charset=utf-8"
  });
  function WorkerWrapper(options) {
    var objURL;
    try {
      objURL = blob && (window.URL || window.webkitURL).createObjectURL(blob);
      if (!objURL) throw "";
      var worker = new Worker(objURL, {
        name: options == null ? void 0 : options.name
      });
      worker.addEventListener("error", function() {
        (window.URL || window.webkitURL).revokeObjectURL(objURL);
      });
      return worker;
    } catch (e22) {
      return new Worker("data:text/javascript;base64," + encodedJs, {
        name: options == null ? void 0 : options.name
      });
    } finally {
      objURL && (window.URL || window.webkitURL).revokeObjectURL(objURL);
    }
  }
  var CanvasManager = /* @__PURE__ */ (function() {
    function CanvasManager2(options) {
      var _this = this;
      __publicField$1(this, "pendingCanvasMutations", /* @__PURE__ */ new Map());
      __publicField$1(this, "rafStamps", {
        latestId: 0,
        invokeId: null
      });
      __publicField$1(this, "mirror");
      __publicField$1(this, "mutationCb");
      __publicField$1(this, "resetObservers");
      __publicField$1(this, "frozen", false);
      __publicField$1(this, "locked", false);
      __publicField$1(this, "processMutation", function(target, mutation) {
        var newFrame = _this.rafStamps.invokeId && _this.rafStamps.latestId !== _this.rafStamps.invokeId;
        if (newFrame || !_this.rafStamps.invokeId) _this.rafStamps.invokeId = _this.rafStamps.latestId;
        if (!_this.pendingCanvasMutations.has(target)) {
          _this.pendingCanvasMutations.set(target, []);
        }
        _this.pendingCanvasMutations.get(target).push(mutation);
      });
      var _options_sampling = options.sampling, sampling = _options_sampling === void 0 ? "all" : _options_sampling, win2 = options.win, blockClass = options.blockClass, blockSelector = options.blockSelector, recordCanvas = options.recordCanvas, dataURLOptions = options.dataURLOptions;
      this.mutationCb = options.mutationCb;
      this.mirror = options.mirror;
      if (recordCanvas && sampling === "all") this.initCanvasMutationObserver(win2, blockClass, blockSelector);
      if (recordCanvas && typeof sampling === "number") this.initCanvasFPSObserver(sampling, win2, blockClass, blockSelector, {
        dataURLOptions
      });
    }
    var _proto = CanvasManager2.prototype;
    _proto.reset = function reset2() {
      this.pendingCanvasMutations.clear();
      this.resetObservers && this.resetObservers();
    };
    _proto.freeze = function freeze() {
      this.frozen = true;
    };
    _proto.unfreeze = function unfreeze() {
      this.frozen = false;
    };
    _proto.lock = function lock() {
      this.locked = true;
    };
    _proto.unlock = function unlock() {
      this.locked = false;
    };
    _proto.initCanvasFPSObserver = function initCanvasFPSObserver(fps, win2, blockClass, blockSelector, options) {
      var _this = this;
      var canvasContextReset = initCanvasContextObserver(win2, blockClass, blockSelector, true);
      var snapshotInProgressMap = /* @__PURE__ */ new Map();
      var worker = new WorkerWrapper();
      worker.onmessage = function(e22) {
        var id = e22.data.id;
        snapshotInProgressMap.set(id, false);
        if (!("base64" in e22.data)) return;
        var _e2_data = e22.data, base642 = _e2_data.base64, type = _e2_data.type, width = _e2_data.width, height = _e2_data.height;
        _this.mutationCb({
          id,
          type: CanvasContext["2D"],
          commands: [
            {
              property: "clearRect",
              // wipe canvas
              args: [
                0,
                0,
                width,
                height
              ]
            },
            {
              property: "drawImage",
              // draws (semi-transparent) image
              args: [
                {
                  rr_type: "ImageBitmap",
                  args: [
                    {
                      rr_type: "Blob",
                      data: [
                        {
                          rr_type: "ArrayBuffer",
                          base64: base642
                        }
                      ],
                      type
                    }
                  ]
                },
                0,
                0
              ]
            }
          ]
        });
      };
      var timeBetweenSnapshots = 1e3 / fps;
      var lastSnapshotTime = 0;
      var rafId;
      var getCanvas = function() {
        var matchedCanvas = [];
        win2.document.querySelectorAll("canvas").forEach(function(canvas) {
          if (!isBlocked(canvas, blockClass, blockSelector, true)) {
            matchedCanvas.push(canvas);
          }
        });
        return matchedCanvas;
      };
      var takeCanvasSnapshots = function(timestamp) {
        if (lastSnapshotTime && timestamp - lastSnapshotTime < timeBetweenSnapshots) {
          rafId = requestAnimationFrame(takeCanvasSnapshots);
          return;
        }
        lastSnapshotTime = timestamp;
        var _this1 = _this;
        getCanvas().forEach(/* @__PURE__ */ _async_to_generator(function(canvas) {
          var _a2, id, context, bitmap;
          return _ts_generator(this, function(_state) {
            switch (_state.label) {
              case 0:
                id = _this1.mirror.getId(canvas);
                if (snapshotInProgressMap.get(id)) return [
                  2
                ];
                if (canvas.width === 0 || canvas.height === 0) return [
                  2
                ];
                snapshotInProgressMap.set(id, true);
                if ([
                  "webgl",
                  "webgl2"
                ].includes(canvas.__context)) {
                  context = canvas.getContext(canvas.__context);
                  if (((_a2 = context == null ? void 0 : context.getContextAttributes()) == null ? void 0 : _a2.preserveDrawingBuffer) === false) {
                    context.clear(context.COLOR_BUFFER_BIT);
                  }
                }
                return [
                  4,
                  createImageBitmap(canvas)
                ];
              case 1:
                bitmap = _state.sent();
                worker.postMessage({
                  id,
                  bitmap,
                  width: canvas.width,
                  height: canvas.height,
                  dataURLOptions: options.dataURLOptions
                }, [
                  bitmap
                ]);
                return [
                  2
                ];
            }
          });
        }));
        rafId = requestAnimationFrame(takeCanvasSnapshots);
      };
      rafId = requestAnimationFrame(takeCanvasSnapshots);
      this.resetObservers = function() {
        canvasContextReset();
        cancelAnimationFrame(rafId);
      };
    };
    _proto.initCanvasMutationObserver = function initCanvasMutationObserver(win2, blockClass, blockSelector) {
      this.startRAFTimestamping();
      this.startPendingCanvasMutationFlusher();
      var canvasContextReset = initCanvasContextObserver(win2, blockClass, blockSelector, false);
      var canvas2DReset = initCanvas2DMutationObserver(this.processMutation.bind(this), win2, blockClass, blockSelector);
      var canvasWebGL1and2Reset = initCanvasWebGLMutationObserver(this.processMutation.bind(this), win2, blockClass, blockSelector);
      this.resetObservers = function() {
        canvasContextReset();
        canvas2DReset();
        canvasWebGL1and2Reset();
      };
    };
    _proto.startPendingCanvasMutationFlusher = function startPendingCanvasMutationFlusher() {
      var _this = this;
      requestAnimationFrame(function() {
        return _this.flushPendingCanvasMutations();
      });
    };
    _proto.startRAFTimestamping = function startRAFTimestamping() {
      var _this = this;
      var setLatestRAFTimestamp = function(timestamp) {
        _this.rafStamps.latestId = timestamp;
        requestAnimationFrame(setLatestRAFTimestamp);
      };
      requestAnimationFrame(setLatestRAFTimestamp);
    };
    _proto.flushPendingCanvasMutations = function flushPendingCanvasMutations() {
      var _this = this;
      this.pendingCanvasMutations.forEach(function(_values, canvas) {
        var id = _this.mirror.getId(canvas);
        _this.flushPendingCanvasMutationFor(canvas, id);
      });
      requestAnimationFrame(function() {
        return _this.flushPendingCanvasMutations();
      });
    };
    _proto.flushPendingCanvasMutationFor = function flushPendingCanvasMutationFor(canvas, id) {
      if (this.frozen || this.locked) {
        return;
      }
      var valuesWithType = this.pendingCanvasMutations.get(canvas);
      if (!valuesWithType || id === -1) return;
      var values = valuesWithType.map(function(value) {
        value.type;
        var rest = _object_without_properties_loose(value, [
          "type"
        ]);
        return rest;
      });
      var type = valuesWithType[0].type;
      this.mutationCb({
        id,
        type,
        commands: values
      });
      this.pendingCanvasMutations.delete(canvas);
    };
    return CanvasManager2;
  })();
  var StylesheetManager = /* @__PURE__ */ (function() {
    function StylesheetManager2(options) {
      __publicField$1(this, "trackedLinkElements", /* @__PURE__ */ new WeakSet());
      __publicField$1(this, "mutationCb");
      __publicField$1(this, "adoptedStyleSheetCb");
      __publicField$1(this, "styleMirror", new StyleSheetMirror());
      this.mutationCb = options.mutationCb;
      this.adoptedStyleSheetCb = options.adoptedStyleSheetCb;
    }
    var _proto = StylesheetManager2.prototype;
    _proto.attachLinkElement = function attachLinkElement(linkEl, childSn) {
      if ("_cssText" in childSn.attributes) this.mutationCb({
        adds: [],
        removes: [],
        texts: [],
        attributes: [
          {
            id: childSn.id,
            attributes: childSn.attributes
          }
        ]
      });
      this.trackLinkElement(linkEl);
    };
    _proto.trackLinkElement = function trackLinkElement(linkEl) {
      if (this.trackedLinkElements.has(linkEl)) return;
      this.trackedLinkElements.add(linkEl);
      this.trackStylesheetInLinkElement(linkEl);
    };
    _proto.adoptStyleSheets = function adoptStyleSheets(sheets, hostId) {
      var _this, _loop = function() {
        var sheet = _step.value;
        var styleId = void 0;
        if (!_this.styleMirror.has(sheet)) {
          styleId = _this.styleMirror.add(sheet);
          styles.push({
            styleId,
            rules: Array.from(sheet.rules || CSSRule, function(r22, index2) {
              return {
                rule: stringifyRule(r22, sheet.href),
                index: index2
              };
            })
          });
        } else styleId = _this.styleMirror.getId(sheet);
        adoptedStyleSheetData.styleIds.push(styleId);
      };
      if (sheets.length === 0) return;
      var adoptedStyleSheetData = {
        id: hostId,
        styleIds: []
      };
      var styles = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(sheets), _step; !(_step = _iterator()).done; ) _this = this, _loop();
      if (styles.length > 0) adoptedStyleSheetData.styles = styles;
      this.adoptedStyleSheetCb(adoptedStyleSheetData);
    };
    _proto.reset = function reset2() {
      this.styleMirror.reset();
      this.trackedLinkElements = /* @__PURE__ */ new WeakSet();
    };
    _proto.trackStylesheetInLinkElement = function trackStylesheetInLinkElement(_linkEl) {
    };
    return StylesheetManager2;
  })();
  var ProcessedNodeManager = /* @__PURE__ */ (function() {
    function ProcessedNodeManager2() {
      __publicField$1(this, "nodeMap", /* @__PURE__ */ new WeakMap());
      __publicField$1(this, "active", false);
    }
    var _proto = ProcessedNodeManager2.prototype;
    _proto.inOtherBuffer = function inOtherBuffer(node2, thisBuffer) {
      var buffers = this.nodeMap.get(node2);
      return buffers && Array.from(buffers).some(function(buffer) {
        return buffer !== thisBuffer;
      });
    };
    _proto.add = function add(node2, buffer) {
      var _this = this;
      if (!this.active) {
        this.active = true;
        requestAnimationFrame(function() {
          _this.nodeMap = /* @__PURE__ */ new WeakMap();
          _this.active = false;
        });
      }
      this.nodeMap.set(node2, (this.nodeMap.get(node2) || /* @__PURE__ */ new Set()).add(buffer));
    };
    _proto.destroy = function destroy() {
    };
    return ProcessedNodeManager2;
  })();
  var wrappedEmit;
  var takeFullSnapshot$1;
  var canvasManager;
  var recording = false;
  try {
    if (Array.from([
      1
    ], function(x22) {
      return x22 * 2;
    })[0] !== 2) {
      cleanFrame = document.createElement("iframe");
      document.body.appendChild(cleanFrame);
      Array.from = ((_a = cleanFrame.contentWindow) == null ? void 0 : _a.Array.from) || Array.from;
      document.body.removeChild(cleanFrame);
    }
  } catch (err) {
    console.debug("Unable to override Array.from", err);
  }
  var cleanFrame;
  var mirror = createMirror$2();
  function record(options) {
    if (options === void 0) options = {};
    var emit = options.emit, checkoutEveryNms = options.checkoutEveryNms, checkoutEveryNth = options.checkoutEveryNth, _options_blockClass = options.blockClass, blockClass = _options_blockClass === void 0 ? "rr-block" : _options_blockClass, _options_blockSelector = options.blockSelector, blockSelector = _options_blockSelector === void 0 ? null : _options_blockSelector, _options_ignoreClass = options.ignoreClass, ignoreClass = _options_ignoreClass === void 0 ? "rr-ignore" : _options_ignoreClass, _options_ignoreSelector = options.ignoreSelector, ignoreSelector = _options_ignoreSelector === void 0 ? null : _options_ignoreSelector, _options_maskTextClass = options.maskTextClass, maskTextClass = _options_maskTextClass === void 0 ? "rr-mask" : _options_maskTextClass, _options_maskTextSelector = options.maskTextSelector, maskTextSelector = _options_maskTextSelector === void 0 ? null : _options_maskTextSelector, _options_inlineStylesheet = options.inlineStylesheet, inlineStylesheet = _options_inlineStylesheet === void 0 ? true : _options_inlineStylesheet, maskAllInputs = options.maskAllInputs, _maskInputOptions = options.maskInputOptions, _slimDOMOptions = options.slimDOMOptions, maskInputFn = options.maskInputFn, maskTextFn = options.maskTextFn, hooks = options.hooks, packFn = options.packFn, _options_sampling = options.sampling, sampling = _options_sampling === void 0 ? {} : _options_sampling, _options_dataURLOptions = options.dataURLOptions, dataURLOptions = _options_dataURLOptions === void 0 ? {} : _options_dataURLOptions, mousemoveWait = options.mousemoveWait, _options_recordDOM = options.recordDOM, recordDOM = _options_recordDOM === void 0 ? true : _options_recordDOM, _options_recordCanvas = options.recordCanvas, recordCanvas = _options_recordCanvas === void 0 ? false : _options_recordCanvas, _options_recordCrossOriginIframes = options.recordCrossOriginIframes, recordCrossOriginIframes = _options_recordCrossOriginIframes === void 0 ? false : _options_recordCrossOriginIframes, _options_recordAfter = options.recordAfter, recordAfter = _options_recordAfter === void 0 ? options.recordAfter === "DOMContentLoaded" ? options.recordAfter : "load" : _options_recordAfter, _options_userTriggeredOnInput = options.userTriggeredOnInput, userTriggeredOnInput = _options_userTriggeredOnInput === void 0 ? false : _options_userTriggeredOnInput, _options_collectFonts = options.collectFonts, collectFonts = _options_collectFonts === void 0 ? false : _options_collectFonts, _options_inlineImages = options.inlineImages, inlineImages = _options_inlineImages === void 0 ? false : _options_inlineImages, plugins = options.plugins, _options_keepIframeSrcFn = options.keepIframeSrcFn, keepIframeSrcFn = _options_keepIframeSrcFn === void 0 ? function() {
      return false;
    } : _options_keepIframeSrcFn, _options_ignoreCSSAttributes = options.ignoreCSSAttributes, ignoreCSSAttributes = _options_ignoreCSSAttributes === void 0 ? /* @__PURE__ */ new Set([]) : _options_ignoreCSSAttributes, errorHandler2 = options.errorHandler;
    registerErrorHandler(errorHandler2);
    var inEmittingFrame = recordCrossOriginIframes ? window.parent === window : true;
    var passEmitsToParent = false;
    if (!inEmittingFrame) {
      try {
        if (window.parent.document) {
          passEmitsToParent = false;
        }
      } catch (e22) {
        passEmitsToParent = true;
      }
    }
    if (inEmittingFrame && !emit) {
      throw new Error("emit function is required");
    }
    if (!inEmittingFrame && !passEmitsToParent) {
      return function() {
      };
    }
    if (mousemoveWait !== void 0 && sampling.mousemove === void 0) {
      sampling.mousemove = mousemoveWait;
    }
    mirror.reset();
    var maskInputOptions = maskAllInputs === true ? {
      color: true,
      date: true,
      "datetime-local": true,
      email: true,
      month: true,
      number: true,
      range: true,
      search: true,
      tel: true,
      text: true,
      time: true,
      url: true,
      week: true,
      textarea: true,
      select: true,
      password: true,
      hidden: true
    } : _maskInputOptions !== void 0 ? _maskInputOptions : {
      password: true
    };
    var slimDOMOptions = _slimDOMOptions === true || _slimDOMOptions === "all" ? {
      script: true,
      comment: true,
      headFavicon: true,
      headWhitespace: true,
      headMetaSocial: true,
      headMetaRobots: true,
      headMetaHttpEquiv: true,
      headMetaVerification: true,
      // the following are off for slimDOMOptions === true,
      // as they destroy some (hidden) info:
      headMetaAuthorship: _slimDOMOptions === "all",
      headMetaDescKeywords: _slimDOMOptions === "all",
      headTitleMutations: _slimDOMOptions === "all"
    } : _slimDOMOptions ? _slimDOMOptions : {};
    polyfill$1();
    var lastFullSnapshotEvent;
    var incrementalSnapshotCount = 0;
    var eventProcessor = function(e22) {
      for (var _iterator2 = _create_for_of_iterator_helper_loose(plugins || []), _step2; !(_step2 = _iterator2()).done; ) {
        var plugin33 = _step2.value;
        if (plugin33.eventProcessor) {
          e22 = plugin33.eventProcessor(e22);
        }
      }
      if (packFn && // Disable packing events which will be emitted to parent frames.
      !passEmitsToParent) {
        e22 = packFn(e22);
      }
      return e22;
    };
    wrappedEmit = function(r22, isCheckout) {
      var _a2;
      var e22 = r22;
      e22.timestamp = nowTimestamp();
      if (((_a2 = mutationBuffers[0]) == null ? void 0 : _a2.isFrozen()) && e22.type !== EventType.FullSnapshot && !(e22.type === EventType.IncrementalSnapshot && e22.data.source === IncrementalSource.Mutation)) {
        mutationBuffers.forEach(function(buf) {
          return buf.unfreeze();
        });
      }
      if (inEmittingFrame) {
        emit == null ? void 0 : emit(eventProcessor(e22), isCheckout);
      } else if (passEmitsToParent) {
        var message = {
          type: "rrweb",
          event: eventProcessor(e22),
          origin: window.location.origin,
          isCheckout
        };
        window.parent.postMessage(message, "*");
      }
      if (e22.type === EventType.FullSnapshot) {
        lastFullSnapshotEvent = e22;
        incrementalSnapshotCount = 0;
      } else if (e22.type === EventType.IncrementalSnapshot) {
        if (e22.data.source === IncrementalSource.Mutation && e22.data.isAttachIframe) {
          return;
        }
        incrementalSnapshotCount++;
        var exceedCount = checkoutEveryNth && incrementalSnapshotCount >= checkoutEveryNth;
        var exceedTime = checkoutEveryNms && e22.timestamp - lastFullSnapshotEvent.timestamp > checkoutEveryNms;
        if (exceedCount || exceedTime) {
          takeFullSnapshot$1(true);
        }
      }
    };
    var wrappedMutationEmit = function(m3) {
      wrappedEmit({
        type: EventType.IncrementalSnapshot,
        data: _extends({
          source: IncrementalSource.Mutation
        }, m3)
      });
    };
    var wrappedScrollEmit = function(p3) {
      return wrappedEmit({
        type: EventType.IncrementalSnapshot,
        data: _extends({
          source: IncrementalSource.Scroll
        }, p3)
      });
    };
    var wrappedCanvasMutationEmit = function(p3) {
      return wrappedEmit({
        type: EventType.IncrementalSnapshot,
        data: _extends({
          source: IncrementalSource.CanvasMutation
        }, p3)
      });
    };
    var wrappedAdoptedStyleSheetEmit = function(a22) {
      return wrappedEmit({
        type: EventType.IncrementalSnapshot,
        data: _extends({
          source: IncrementalSource.AdoptedStyleSheet
        }, a22)
      });
    };
    var stylesheetManager = new StylesheetManager({
      mutationCb: wrappedMutationEmit,
      adoptedStyleSheetCb: wrappedAdoptedStyleSheetEmit
    });
    var iframeManager = new IframeManager({
      mirror,
      mutationCb: wrappedMutationEmit,
      stylesheetManager,
      recordCrossOriginIframes,
      wrappedEmit
    });
    for (var _iterator = _create_for_of_iterator_helper_loose(plugins || []), _step; !(_step = _iterator()).done; ) {
      var plugin32 = _step.value;
      if (plugin32.getMirror) plugin32.getMirror({
        nodeMirror: mirror,
        crossOriginIframeMirror: iframeManager.crossOriginIframeMirror,
        crossOriginIframeStyleMirror: iframeManager.crossOriginIframeStyleMirror
      });
    }
    var processedNodeManager = new ProcessedNodeManager();
    canvasManager = new CanvasManager({
      recordCanvas,
      mutationCb: wrappedCanvasMutationEmit,
      win: window,
      blockClass,
      blockSelector,
      mirror,
      sampling: sampling.canvas,
      dataURLOptions
    });
    var shadowDomManager = new ShadowDomManager({
      mutationCb: wrappedMutationEmit,
      scrollCb: wrappedScrollEmit,
      bypassOptions: {
        blockClass,
        blockSelector,
        maskTextClass,
        maskTextSelector,
        inlineStylesheet,
        maskInputOptions,
        dataURLOptions,
        maskTextFn,
        maskInputFn,
        recordCanvas,
        inlineImages,
        sampling,
        slimDOMOptions,
        iframeManager,
        stylesheetManager,
        canvasManager,
        keepIframeSrcFn,
        processedNodeManager
      },
      mirror
    });
    takeFullSnapshot$1 = function(isCheckout) {
      if (isCheckout === void 0) isCheckout = false;
      if (!recordDOM) {
        return;
      }
      wrappedEmit({
        type: EventType.Meta,
        data: {
          href: window.location.href,
          width: getWindowWidth(),
          height: getWindowHeight()
        }
      }, isCheckout);
      stylesheetManager.reset();
      shadowDomManager.init();
      mutationBuffers.forEach(function(buf) {
        return buf.lock();
      });
      var node2 = snapshot(document, {
        mirror,
        blockClass,
        blockSelector,
        maskTextClass,
        maskTextSelector,
        inlineStylesheet,
        maskAllInputs: maskInputOptions,
        maskTextFn,
        maskInputFn,
        slimDOM: slimDOMOptions,
        dataURLOptions,
        recordCanvas,
        inlineImages,
        onSerialize: function(n22) {
          if (isSerializedIframe(n22, mirror)) {
            iframeManager.addIframe(n22);
          }
          if (isSerializedStylesheet(n22, mirror)) {
            stylesheetManager.trackLinkElement(n22);
          }
          if (hasShadowRoot(n22)) {
            shadowDomManager.addShadowRoot(index$2.shadowRoot(n22), document);
          }
        },
        onIframeLoad: function(iframe, childSn) {
          iframeManager.attachIframe(iframe, childSn);
          shadowDomManager.observeAttachShadow(iframe);
        },
        onStylesheetLoad: function(linkEl, childSn) {
          stylesheetManager.attachLinkElement(linkEl, childSn);
        },
        keepIframeSrcFn
      });
      if (!node2) {
        return console.warn("Failed to snapshot the document");
      }
      wrappedEmit({
        type: EventType.FullSnapshot,
        data: {
          node: node2,
          initialOffset: getWindowScroll(window)
        }
      }, isCheckout);
      mutationBuffers.forEach(function(buf) {
        return buf.unlock();
      });
      if (document.adoptedStyleSheets && document.adoptedStyleSheets.length > 0) stylesheetManager.adoptStyleSheets(document.adoptedStyleSheets, mirror.getId(document));
    };
    try {
      var handlers = [];
      var observe = function(doc) {
        var _a2;
        return callbackWrapper(initObservers)({
          mutationCb: wrappedMutationEmit,
          mousemoveCb: function(positions, source) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: {
                source,
                positions
              }
            });
          },
          mouseInteractionCb: function(d3) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.MouseInteraction
              }, d3)
            });
          },
          scrollCb: wrappedScrollEmit,
          viewportResizeCb: function(d3) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.ViewportResize
              }, d3)
            });
          },
          inputCb: function(v22) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.Input
              }, v22)
            });
          },
          mediaInteractionCb: function(p3) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.MediaInteraction
              }, p3)
            });
          },
          styleSheetRuleCb: function(r22) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.StyleSheetRule
              }, r22)
            });
          },
          styleDeclarationCb: function(r22) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.StyleDeclaration
              }, r22)
            });
          },
          canvasMutationCb: wrappedCanvasMutationEmit,
          fontCb: function(p3) {
            return wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.Font
              }, p3)
            });
          },
          selectionCb: function(p3) {
            wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.Selection
              }, p3)
            });
          },
          customElementCb: function(c22) {
            wrappedEmit({
              type: EventType.IncrementalSnapshot,
              data: _extends({
                source: IncrementalSource.CustomElement
              }, c22)
            });
          },
          blockClass,
          ignoreClass,
          ignoreSelector,
          maskTextClass,
          maskTextSelector,
          maskInputOptions,
          inlineStylesheet,
          sampling,
          recordDOM,
          recordCanvas,
          inlineImages,
          userTriggeredOnInput,
          collectFonts,
          doc,
          maskInputFn,
          maskTextFn,
          keepIframeSrcFn,
          blockSelector,
          slimDOMOptions,
          dataURLOptions,
          mirror,
          iframeManager,
          stylesheetManager,
          shadowDomManager,
          processedNodeManager,
          canvasManager,
          ignoreCSSAttributes,
          plugins: ((_a2 = plugins == null ? void 0 : plugins.filter(function(p3) {
            return p3.observer;
          })) == null ? void 0 : _a2.map(function(p3) {
            return {
              observer: p3.observer,
              options: p3.options,
              callback: function(payload) {
                return wrappedEmit({
                  type: EventType.Plugin,
                  data: {
                    plugin: p3.name,
                    payload
                  }
                });
              }
            };
          })) || []
        }, hooks);
      };
      iframeManager.addLoadListener(function(iframeEl) {
        try {
          handlers.push(observe(iframeEl.contentDocument));
        } catch (error) {
          console.warn(error);
        }
      });
      var init = function() {
        takeFullSnapshot$1();
        handlers.push(observe(document));
        recording = true;
      };
      if (document.readyState === "interactive" || document.readyState === "complete") {
        init();
      } else {
        handlers.push(on2("DOMContentLoaded", function() {
          wrappedEmit({
            type: EventType.DomContentLoaded,
            data: {}
          });
          if (recordAfter === "DOMContentLoaded") init();
        }));
        handlers.push(on2("load", function() {
          wrappedEmit({
            type: EventType.Load,
            data: {}
          });
          if (recordAfter === "load") init();
        }, window));
      }
      return function() {
        handlers.forEach(function(handler) {
          try {
            handler();
          } catch (error) {
            var msg = String(error).toLowerCase();
            if (!msg.includes("cross-origin")) {
              console.warn(error);
            }
          }
        });
        processedNodeManager.destroy();
        recording = false;
        unregisterErrorHandler();
      };
    } catch (error) {
      console.warn(error);
    }
  }
  record.addCustomEvent = function(tag, payload) {
    if (!recording) {
      throw new Error("please add custom event after start recording");
    }
    wrappedEmit({
      type: EventType.Custom,
      data: {
        tag,
        payload
      }
    });
  };
  record.freezePage = function() {
    mutationBuffers.forEach(function(buf) {
      return buf.freeze();
    });
  };
  record.takeFullSnapshot = function(isCheckout) {
    if (!recording) {
      throw new Error("please take full snapshot after start recording");
    }
    takeFullSnapshot$1(isCheckout);
  };
  record.mirror = mirror;
  var n2;
  !(function(t22) {
    t22[t22.NotStarted = 0] = "NotStarted", t22[t22.Running = 1] = "Running", t22[t22.Stopped = 2] = "Stopped";
  })(n2 || (n2 = {}));
  record.addCustomEvent;
  record.freezePage;
  record.takeFullSnapshot;
  var testableAccessors = {
    Node: [
      "childNodes",
      "parentNode",
      "parentElement",
      "textContent"
    ],
    ShadowRoot: [
      "host",
      "styleSheets"
    ],
    Element: [
      "shadowRoot",
      "querySelector",
      "querySelectorAll"
    ],
    MutationObserver: []
  };
  var testableMethods = {
    Node: [
      "contains",
      "getRootNode"
    ],
    ShadowRoot: [
      "getSelection"
    ],
    Element: [],
    MutationObserver: [
      "constructor"
    ]
  };
  var untaintedBasePrototype = {};
  var isAngularZonePresent = function() {
    return !!globalThis.Zone;
  };
  function getUntaintedPrototype(key) {
    if (untaintedBasePrototype[key]) return untaintedBasePrototype[key];
    var defaultObj = globalThis[key];
    var defaultPrototype = defaultObj.prototype;
    var accessorNames = key in testableAccessors ? testableAccessors[key] : void 0;
    var isUntaintedAccessors = Boolean(accessorNames && // @ts-expect-error 2345
    accessorNames.every(function(accessor) {
      var _a2, _b;
      return Boolean((_b = (_a2 = Object.getOwnPropertyDescriptor(defaultPrototype, accessor)) == null ? void 0 : _a2.get) == null ? void 0 : _b.toString().includes("[native code]"));
    }));
    var methodNames = key in testableMethods ? testableMethods[key] : void 0;
    var isUntaintedMethods = Boolean(methodNames && methodNames.every(
      // @ts-expect-error 2345
      function(method) {
        var _a2;
        return typeof defaultPrototype[method] === "function" && ((_a2 = defaultPrototype[method]) == null ? void 0 : _a2.toString().includes("[native code]"));
      }
    ));
    if (isUntaintedAccessors && isUntaintedMethods && !isAngularZonePresent()) {
      untaintedBasePrototype[key] = defaultObj.prototype;
      return defaultObj.prototype;
    }
    try {
      var iframeEl = document.createElement("iframe");
      document.body.appendChild(iframeEl);
      var win2 = iframeEl.contentWindow;
      if (!win2) return defaultObj.prototype;
      var untaintedObject = win2[key].prototype;
      document.body.removeChild(iframeEl);
      if (!untaintedObject) return defaultPrototype;
      return untaintedBasePrototype[key] = untaintedObject;
    } catch (e3) {
      return defaultPrototype;
    }
  }
  var untaintedAccessorCache = {};
  function getUntaintedAccessor(key, instance, accessor) {
    var _a2;
    var cacheKey = key + "." + String(accessor);
    if (untaintedAccessorCache[cacheKey]) return untaintedAccessorCache[cacheKey].call(instance);
    var untaintedPrototype = getUntaintedPrototype(key);
    var untaintedAccessor = (_a2 = Object.getOwnPropertyDescriptor(untaintedPrototype, accessor)) == null ? void 0 : _a2.get;
    if (!untaintedAccessor) return instance[accessor];
    untaintedAccessorCache[cacheKey] = untaintedAccessor;
    return untaintedAccessor.call(instance);
  }
  var untaintedMethodCache = {};
  function getUntaintedMethod(key, instance, method) {
    var cacheKey = key + "." + String(method);
    if (untaintedMethodCache[cacheKey]) return untaintedMethodCache[cacheKey].bind(instance);
    var untaintedPrototype = getUntaintedPrototype(key);
    var untaintedMethod = untaintedPrototype[method];
    if (typeof untaintedMethod !== "function") return instance[method];
    untaintedMethodCache[cacheKey] = untaintedMethod;
    return untaintedMethod.bind(instance);
  }
  function childNodes(n3) {
    return getUntaintedAccessor("Node", n3, "childNodes");
  }
  function parentNode(n3) {
    return getUntaintedAccessor("Node", n3, "parentNode");
  }
  function parentElement(n3) {
    return getUntaintedAccessor("Node", n3, "parentElement");
  }
  function textContent(n3) {
    return getUntaintedAccessor("Node", n3, "textContent");
  }
  function contains(n3, other) {
    return getUntaintedMethod("Node", n3, "contains")(other);
  }
  function getRootNode(n3) {
    return getUntaintedMethod("Node", n3, "getRootNode")();
  }
  function host(n3) {
    if (!n3 || !("host" in n3)) return null;
    return getUntaintedAccessor("ShadowRoot", n3, "host");
  }
  function styleSheets(n3) {
    return n3.styleSheets;
  }
  function shadowRoot(n3) {
    if (!n3 || !("shadowRoot" in n3)) return null;
    return getUntaintedAccessor("Element", n3, "shadowRoot");
  }
  function querySelector(n3, selectors) {
    return getUntaintedAccessor("Element", n3, "querySelector")(selectors);
  }
  function querySelectorAll(n3, selectors) {
    return getUntaintedAccessor("Element", n3, "querySelectorAll")(selectors);
  }
  function mutationObserverCtor() {
    return getUntaintedPrototype("MutationObserver").constructor;
  }
  function patch$1(source, name5, replacement) {
    try {
      if (!(name5 in source)) {
        return function() {
        };
      }
      var original = source[name5];
      var wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name5] = wrapped;
      return function() {
        source[name5] = original;
      };
    } catch (e3) {
      return function() {
      };
    }
  }
  var index = {
    childNodes,
    parentNode,
    parentElement,
    textContent,
    contains,
    getRootNode,
    host,
    styleSheets,
    shadowRoot,
    querySelector,
    querySelectorAll,
    mutationObserver: mutationObserverCtor,
    patch: patch$1
  };
  function classMatchesRegex(node2, regex, checkAncestors) {
    if (!node2) return false;
    if (node2.nodeType !== node2.ELEMENT_NODE) {
      return classMatchesRegex(index.parentNode(node2), regex);
    }
    for (var eIndex = node2.classList.length; eIndex--; ) {
      var className = node2.classList[eIndex];
      if (regex.test(className)) {
        return true;
      }
    }
    return classMatchesRegex(index.parentNode(node2), regex);
  }
  function getDefaultExportFromCjs(x22) {
    return x22 && x22.__esModule && Object.prototype.hasOwnProperty.call(x22, "default") ? x22["default"] : x22;
  }
  function getAugmentedNamespace(n3) {
    if (n3.__esModule) return n3;
    var f3 = n3.default;
    if (typeof f3 == "function") {
      var a3 = function a22() {
        if (_instanceof(this, a22)) {
          return Reflect.construct(f3, arguments, this.constructor);
        }
        return f3.apply(this, arguments);
      };
      a3.prototype = f3.prototype;
    } else a3 = {};
    Object.defineProperty(a3, "__esModule", {
      value: true
    });
    Object.keys(n3).forEach(function(k4) {
      var d3 = Object.getOwnPropertyDescriptor(n3, k4);
      Object.defineProperty(a3, k4, d3.get ? d3 : {
        enumerable: true,
        get: function get() {
          return n3[k4];
        }
      });
    });
    return a3;
  }
  var picocolors_browser = {
    exports: {}
  };
  var x4 = String;
  var create = function create2() {
    return {
      isColorSupported: false,
      reset: x4,
      bold: x4,
      dim: x4,
      italic: x4,
      underline: x4,
      inverse: x4,
      hidden: x4,
      strikethrough: x4,
      black: x4,
      red: x4,
      green: x4,
      yellow: x4,
      blue: x4,
      magenta: x4,
      cyan: x4,
      white: x4,
      gray: x4,
      bgBlack: x4,
      bgRed: x4,
      bgGreen: x4,
      bgYellow: x4,
      bgBlue: x4,
      bgMagenta: x4,
      bgCyan: x4,
      bgWhite: x4
    };
  };
  picocolors_browser.exports = create();
  picocolors_browser.exports.createColors = create;
  var picocolors_browserExports = picocolors_browser.exports;
  var __viteBrowserExternal = {};
  var __viteBrowserExternal$1 = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
    __proto__: null,
    default: __viteBrowserExternal
  }, Symbol.toStringTag, {
    value: "Module"
  }));
  var require$$2 = /* @__PURE__ */ getAugmentedNamespace(__viteBrowserExternal$1);
  var pico = picocolors_browserExports;
  var terminalHighlight$1 = require$$2;
  var CssSyntaxError$3 = /* @__PURE__ */ (function(Error1) {
    _inherits(CssSyntaxError, Error1);
    function CssSyntaxError(message, line, column, source, file, plugin22) {
      var _this;
      _this = Error1.call(this, message) || this;
      _this.name = "CssSyntaxError";
      _this.reason = message;
      if (file) {
        _this.file = file;
      }
      if (source) {
        _this.source = source;
      }
      if (plugin22) {
        _this.plugin = plugin22;
      }
      if (typeof line !== "undefined" && typeof column !== "undefined") {
        if (typeof line === "number") {
          _this.line = line;
          _this.column = column;
        } else {
          _this.line = line.line;
          _this.column = line.column;
          _this.endLine = column.line;
          _this.endColumn = column.column;
        }
      }
      _this.setMessage();
      if (Error.captureStackTrace) {
        Error.captureStackTrace(_this, CssSyntaxError);
      }
      return _this;
    }
    var _proto = CssSyntaxError.prototype;
    _proto.setMessage = function setMessage() {
      this.message = this.plugin ? this.plugin + ": " : "";
      this.message += this.file ? this.file : "<css input>";
      if (typeof this.line !== "undefined") {
        this.message += ":" + this.line + ":" + this.column;
      }
      this.message += ": " + this.reason;
    };
    _proto.showSourceCode = function showSourceCode(color) {
      var _this = this;
      if (!this.source) return "";
      var css = this.source;
      if (color == null) color = pico.isColorSupported;
      if (terminalHighlight$1) {
        if (color) css = terminalHighlight$1(css);
      }
      var lines = css.split(/\r?\n/);
      var start = Math.max(this.line - 3, 0);
      var end = Math.min(this.line + 2, lines.length);
      var maxWidth = String(end).length;
      var mark, aside;
      if (color) {
        var _pico_createColors = pico.createColors(true), bold = _pico_createColors.bold, gray = _pico_createColors.gray, red = _pico_createColors.red;
        mark = function(text) {
          return bold(red(text));
        };
        aside = function(text) {
          return gray(text);
        };
      } else {
        mark = aside = function(str) {
          return str;
        };
      }
      return lines.slice(start, end).map(function(line, index2) {
        var number = start + 1 + index2;
        var gutter = " " + (" " + number).slice(-maxWidth) + " | ";
        if (number === _this.line) {
          var spacing = aside(gutter.replace(/\d/g, " ")) + line.slice(0, _this.column - 1).replace(/[^\t]/g, " ");
          return mark(">") + aside(gutter) + line + "\n " + spacing + mark("^");
        }
        return " " + aside(gutter) + line;
      }).join("\n");
    };
    _proto.toString = function toString2() {
      var code = this.showSourceCode();
      if (code) {
        code = "\n\n" + code + "\n";
      }
      return this.name + ": " + this.message + code;
    };
    return CssSyntaxError;
  })(_wrap_native_super(Error));
  var cssSyntaxError = CssSyntaxError$3;
  CssSyntaxError$3.default = CssSyntaxError$3;
  var symbols = {};
  symbols.isClean = /* @__PURE__ */ Symbol("isClean");
  symbols.my = /* @__PURE__ */ Symbol("my");
  var DEFAULT_RAW = {
    after: "\n",
    beforeClose: "\n",
    beforeComment: "\n",
    beforeDecl: "\n",
    beforeOpen: " ",
    beforeRule: "\n",
    colon: ": ",
    commentLeft: " ",
    commentRight: " ",
    emptyBody: "",
    indent: "    ",
    semicolon: false
  };
  function capitalize(str) {
    return str[0].toUpperCase() + str.slice(1);
  }
  var Stringifier$2 = /* @__PURE__ */ (function() {
    function Stringifier(builder) {
      this.builder = builder;
    }
    var _proto = Stringifier.prototype;
    _proto.atrule = function atrule(node2, semicolon) {
      var name5 = "@" + node2.name;
      var params = node2.params ? this.rawValue(node2, "params") : "";
      if (typeof node2.raws.afterName !== "undefined") {
        name5 += node2.raws.afterName;
      } else if (params) {
        name5 += " ";
      }
      if (node2.nodes) {
        this.block(node2, name5 + params);
      } else {
        var end = (node2.raws.between || "") + (semicolon ? ";" : "");
        this.builder(name5 + params + end, node2);
      }
    };
    _proto.beforeAfter = function beforeAfter(node2, detect) {
      var value;
      if (node2.type === "decl") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (node2.type === "comment") {
        value = this.raw(node2, null, "beforeComment");
      } else if (detect === "before") {
        value = this.raw(node2, null, "beforeRule");
      } else {
        value = this.raw(node2, null, "beforeClose");
      }
      var buf = node2.parent;
      var depth = 0;
      while (buf && buf.type !== "root") {
        depth += 1;
        buf = buf.parent;
      }
      if (value.includes("\n")) {
        var indent = this.raw(node2, null, "indent");
        if (indent.length) {
          for (var step = 0; step < depth; step++) value += indent;
        }
      }
      return value;
    };
    _proto.block = function block(node2, start) {
      var between = this.raw(node2, "between", "beforeOpen");
      this.builder(start + between + "{", node2, "start");
      var after;
      if (node2.nodes && node2.nodes.length) {
        this.body(node2);
        after = this.raw(node2, "after");
      } else {
        after = this.raw(node2, "after", "emptyBody");
      }
      if (after) this.builder(after);
      this.builder("}", node2, "end");
    };
    _proto.body = function body(node2) {
      var last = node2.nodes.length - 1;
      while (last > 0) {
        if (node2.nodes[last].type !== "comment") break;
        last -= 1;
      }
      var semicolon = this.raw(node2, "semicolon");
      for (var i3 = 0; i3 < node2.nodes.length; i3++) {
        var child = node2.nodes[i3];
        var before = this.raw(child, "before");
        if (before) this.builder(before);
        this.stringify(child, last !== i3 || semicolon);
      }
    };
    _proto.comment = function comment2(node2) {
      var left = this.raw(node2, "left", "commentLeft");
      var right = this.raw(node2, "right", "commentRight");
      this.builder("/*" + left + node2.text + right + "*/", node2);
    };
    _proto.decl = function decl(node2, semicolon) {
      var between = this.raw(node2, "between", "colon");
      var string = node2.prop + between + this.rawValue(node2, "value");
      if (node2.important) {
        string += node2.raws.important || " !important";
      }
      if (semicolon) string += ";";
      this.builder(string, node2);
    };
    _proto.document = function document1(node2) {
      this.body(node2);
    };
    _proto.raw = function raw(node2, own, detect) {
      var value;
      if (!detect) detect = own;
      if (own) {
        value = node2.raws[own];
        if (typeof value !== "undefined") return value;
      }
      var parent = node2.parent;
      if (detect === "before") {
        if (!parent || parent.type === "root" && parent.first === node2) {
          return "";
        }
        if (parent && parent.type === "document") {
          return "";
        }
      }
      if (!parent) return DEFAULT_RAW[detect];
      var root2 = node2.root();
      if (!root2.rawCache) root2.rawCache = {};
      if (typeof root2.rawCache[detect] !== "undefined") {
        return root2.rawCache[detect];
      }
      if (detect === "before" || detect === "after") {
        return this.beforeAfter(node2, detect);
      } else {
        var method = "raw" + capitalize(detect);
        if (this[method]) {
          value = this[method](root2, node2);
        } else {
          root2.walk(function(i3) {
            value = i3.raws[own];
            if (typeof value !== "undefined") return false;
          });
        }
      }
      if (typeof value === "undefined") value = DEFAULT_RAW[detect];
      root2.rawCache[detect] = value;
      return value;
    };
    _proto.rawBeforeClose = function rawBeforeClose(root2) {
      var value;
      root2.walk(function(i3) {
        if (i3.nodes && i3.nodes.length > 0) {
          if (typeof i3.raws.after !== "undefined") {
            value = i3.raws.after;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawBeforeComment = function rawBeforeComment(root2, node2) {
      var value;
      root2.walkComments(function(i3) {
        if (typeof i3.raws.before !== "undefined") {
          value = i3.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeDecl");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeDecl = function rawBeforeDecl(root2, node2) {
      var value;
      root2.walkDecls(function(i3) {
        if (typeof i3.raws.before !== "undefined") {
          value = i3.raws.before;
          if (value.includes("\n")) {
            value = value.replace(/[^\n]+$/, "");
          }
          return false;
        }
      });
      if (typeof value === "undefined") {
        value = this.raw(node2, null, "beforeRule");
      } else if (value) {
        value = value.replace(/\S/g, "");
      }
      return value;
    };
    _proto.rawBeforeOpen = function rawBeforeOpen(root2) {
      var value;
      root2.walk(function(i3) {
        if (i3.type !== "decl") {
          value = i3.raws.between;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawBeforeRule = function rawBeforeRule(root2) {
      var value;
      root2.walk(function(i3) {
        if (i3.nodes && (i3.parent !== root2 || root2.first !== i3)) {
          if (typeof i3.raws.before !== "undefined") {
            value = i3.raws.before;
            if (value.includes("\n")) {
              value = value.replace(/[^\n]+$/, "");
            }
            return false;
          }
        }
      });
      if (value) value = value.replace(/\S/g, "");
      return value;
    };
    _proto.rawColon = function rawColon(root2) {
      var value;
      root2.walkDecls(function(i3) {
        if (typeof i3.raws.between !== "undefined") {
          value = i3.raws.between.replace(/[^\s:]/g, "");
          return false;
        }
      });
      return value;
    };
    _proto.rawEmptyBody = function rawEmptyBody(root2) {
      var value;
      root2.walk(function(i3) {
        if (i3.nodes && i3.nodes.length === 0) {
          value = i3.raws.after;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawIndent = function rawIndent(root2) {
      if (root2.raws.indent) return root2.raws.indent;
      var value;
      root2.walk(function(i3) {
        var p3 = i3.parent;
        if (p3 && p3 !== root2 && p3.parent && p3.parent === root2) {
          if (typeof i3.raws.before !== "undefined") {
            var parts = i3.raws.before.split("\n");
            value = parts[parts.length - 1];
            value = value.replace(/\S/g, "");
            return false;
          }
        }
      });
      return value;
    };
    _proto.rawSemicolon = function rawSemicolon(root2) {
      var value;
      root2.walk(function(i3) {
        if (i3.nodes && i3.nodes.length && i3.last.type === "decl") {
          value = i3.raws.semicolon;
          if (typeof value !== "undefined") return false;
        }
      });
      return value;
    };
    _proto.rawValue = function rawValue(node2, prop) {
      var value = node2[prop];
      var raw = node2.raws[prop];
      if (raw && raw.value === value) {
        return raw.raw;
      }
      return value;
    };
    _proto.root = function root2(node2) {
      this.body(node2);
      if (node2.raws.after) this.builder(node2.raws.after);
    };
    _proto.rule = function rule2(node2) {
      this.block(node2, this.rawValue(node2, "selector"));
      if (node2.raws.ownSemicolon) {
        this.builder(node2.raws.ownSemicolon, node2, "end");
      }
    };
    _proto.stringify = function stringify2(node2, semicolon) {
      if (!this[node2.type]) {
        throw new Error("Unknown AST node type " + node2.type + ". Maybe you need to change PostCSS stringifier.");
      }
      this[node2.type](node2, semicolon);
    };
    return Stringifier;
  })();
  var stringifier = Stringifier$2;
  Stringifier$2.default = Stringifier$2;
  var Stringifier$1 = stringifier;
  function stringify$4(node2, builder) {
    var str = new Stringifier$1(builder);
    str.stringify(node2);
  }
  var stringify_1 = stringify$4;
  stringify$4.default = stringify$4;
  var isClean$2 = symbols.isClean;
  var my$2 = symbols.my;
  var CssSyntaxError$2 = cssSyntaxError;
  var Stringifier2 = stringifier;
  var stringify$3 = stringify_1;
  function cloneNode(obj, parent) {
    var cloned = new obj.constructor();
    for (var i3 in obj) {
      if (!Object.prototype.hasOwnProperty.call(obj, i3)) {
        continue;
      }
      if (i3 === "proxyCache") continue;
      var value = obj[i3];
      var type = typeof value === "undefined" ? "undefined" : _type_of(value);
      if (i3 === "parent" && type === "object") {
        if (parent) cloned[i3] = parent;
      } else if (i3 === "source") {
        cloned[i3] = value;
      } else if (Array.isArray(value)) {
        cloned[i3] = value.map(function(j4) {
          return cloneNode(j4, cloned);
        });
      } else {
        if (type === "object" && value !== null) value = cloneNode(value);
        cloned[i3] = value;
      }
    }
    return cloned;
  }
  var Node$4 = /* @__PURE__ */ (function() {
    function Node1(defaults) {
      if (defaults === void 0) defaults = {};
      this.raws = {};
      this[isClean$2] = false;
      this[my$2] = true;
      for (var name5 in defaults) {
        if (name5 === "nodes") {
          this.nodes = [];
          for (var _iterator = _create_for_of_iterator_helper_loose(defaults[name5]), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            if (typeof node2.clone === "function") {
              this.append(node2.clone());
            } else {
              this.append(node2);
            }
          }
        } else {
          this[name5] = defaults[name5];
        }
      }
    }
    var _proto = Node1.prototype;
    _proto.addToError = function addToError(error) {
      error.postcssNode = this;
      if (error.stack && this.source && /\n\s{4}at /.test(error.stack)) {
        var s3 = this.source;
        error.stack = error.stack.replace(/\n\s{4}at /, "$&" + s3.input.from + ":" + s3.start.line + ":" + s3.start.column + "$&");
      }
      return error;
    };
    _proto.after = function after(add) {
      this.parent.insertAfter(this, add);
      return this;
    };
    _proto.assign = function assign(overrides) {
      if (overrides === void 0) overrides = {};
      for (var name5 in overrides) {
        this[name5] = overrides[name5];
      }
      return this;
    };
    _proto.before = function before(add) {
      this.parent.insertBefore(this, add);
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      delete this.raws.before;
      delete this.raws.after;
      if (!keepBetween) delete this.raws.between;
    };
    _proto.clone = function clone(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = cloneNode(this);
      for (var name5 in overrides) {
        cloned[name5] = overrides[name5];
      }
      return cloned;
    };
    _proto.cloneAfter = function cloneAfter(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertAfter(this, cloned);
      return cloned;
    };
    _proto.cloneBefore = function cloneBefore(overrides) {
      if (overrides === void 0) overrides = {};
      var cloned = this.clone(overrides);
      this.parent.insertBefore(this, cloned);
      return cloned;
    };
    _proto.error = function error(message, opts) {
      if (opts === void 0) opts = {};
      if (this.source) {
        var _this_rangeBy = this.rangeBy(opts), end = _this_rangeBy.end, start = _this_rangeBy.start;
        return this.source.input.error(message, {
          column: start.column,
          line: start.line
        }, {
          column: end.column,
          line: end.line
        }, opts);
      }
      return new CssSyntaxError$2(message);
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "prop" || prop === "value" || prop === "name" || prop === "params" || prop === "important" || /* c8 ignore next */
          prop === "text") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.markDirty = function markDirty() {
      if (this[isClean$2]) {
        this[isClean$2] = false;
        var next = this;
        while (next = next.parent) {
          next[isClean$2] = false;
        }
      }
    };
    _proto.next = function next() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 + 1];
    };
    _proto.positionBy = function positionBy(opts, stringRepresentation) {
      var pos = this.source.start;
      if (opts.index) {
        pos = this.positionInside(opts.index, stringRepresentation);
      } else if (opts.word) {
        stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) pos = this.positionInside(index2, stringRepresentation);
      }
      return pos;
    };
    _proto.positionInside = function positionInside(index2, stringRepresentation) {
      var string = stringRepresentation || this.toString();
      var column = this.source.start.column;
      var line = this.source.start.line;
      for (var i3 = 0; i3 < index2; i3++) {
        if (string[i3] === "\n") {
          column = 1;
          line += 1;
        } else {
          column += 1;
        }
      }
      return {
        column,
        line
      };
    };
    _proto.prev = function prev() {
      if (!this.parent) return void 0;
      var index2 = this.parent.index(this);
      return this.parent.nodes[index2 - 1];
    };
    _proto.rangeBy = function rangeBy(opts) {
      var start = {
        column: this.source.start.column,
        line: this.source.start.line
      };
      var end = this.source.end ? {
        column: this.source.end.column + 1,
        line: this.source.end.line
      } : {
        column: start.column + 1,
        line: start.line
      };
      if (opts.word) {
        var stringRepresentation = this.toString();
        var index2 = stringRepresentation.indexOf(opts.word);
        if (index2 !== -1) {
          start = this.positionInside(index2, stringRepresentation);
          end = this.positionInside(index2 + opts.word.length, stringRepresentation);
        }
      } else {
        if (opts.start) {
          start = {
            column: opts.start.column,
            line: opts.start.line
          };
        } else if (opts.index) {
          start = this.positionInside(opts.index);
        }
        if (opts.end) {
          end = {
            column: opts.end.column,
            line: opts.end.line
          };
        } else if (typeof opts.endIndex === "number") {
          end = this.positionInside(opts.endIndex);
        } else if (opts.index) {
          end = this.positionInside(opts.index + 1);
        }
      }
      if (end.line < start.line || end.line === start.line && end.column <= start.column) {
        end = {
          column: start.column + 1,
          line: start.line
        };
      }
      return {
        end,
        start
      };
    };
    _proto.raw = function raw(prop, defaultType) {
      var str = new Stringifier2();
      return str.raw(this, prop, defaultType);
    };
    _proto.remove = function remove2() {
      if (this.parent) {
        this.parent.removeChild(this);
      }
      this.parent = void 0;
      return this;
    };
    _proto.replaceWith = function replaceWith() {
      for (var _len = arguments.length, nodes = new Array(_len), _key = 0; _key < _len; _key++) {
        nodes[_key] = arguments[_key];
      }
      if (this.parent) {
        var bookmark = this;
        var foundSelf = false;
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          if (node2 === this) {
            foundSelf = true;
          } else if (foundSelf) {
            this.parent.insertAfter(bookmark, node2);
            bookmark = node2;
          } else {
            this.parent.insertBefore(bookmark, node2);
          }
        }
        if (!foundSelf) {
          this.remove();
        }
      }
      return this;
    };
    _proto.root = function root2() {
      var result2 = this;
      while (result2.parent && result2.parent.type !== "document") {
        result2 = result2.parent;
      }
      return result2;
    };
    _proto.toJSON = function toJSON(_4, inputs) {
      var fixed = {};
      var emitInputs = inputs == null;
      inputs = inputs || /* @__PURE__ */ new Map();
      var inputsNextIndex = 0;
      for (var name5 in this) {
        if (!Object.prototype.hasOwnProperty.call(this, name5)) {
          continue;
        }
        if (name5 === "parent" || name5 === "proxyCache") continue;
        var value = this[name5];
        if (Array.isArray(value)) {
          fixed[name5] = value.map(function(i3) {
            if ((typeof i3 === "undefined" ? "undefined" : _type_of(i3)) === "object" && i3.toJSON) {
              return i3.toJSON(null, inputs);
            } else {
              return i3;
            }
          });
        } else if ((typeof value === "undefined" ? "undefined" : _type_of(value)) === "object" && value.toJSON) {
          fixed[name5] = value.toJSON(null, inputs);
        } else if (name5 === "source") {
          var inputId = inputs.get(value.input);
          if (inputId == null) {
            inputId = inputsNextIndex;
            inputs.set(value.input, inputsNextIndex);
            inputsNextIndex++;
          }
          fixed[name5] = {
            end: value.end,
            inputId,
            start: value.start
          };
        } else {
          fixed[name5] = value;
        }
      }
      if (emitInputs) {
        fixed.inputs = [].concat(inputs.keys()).map(function(input2) {
          return input2.toJSON();
        });
      }
      return fixed;
    };
    _proto.toProxy = function toProxy() {
      if (!this.proxyCache) {
        this.proxyCache = new Proxy(this, this.getProxyProcessor());
      }
      return this.proxyCache;
    };
    _proto.toString = function toString2(stringifier2) {
      if (stringifier2 === void 0) stringifier2 = stringify$3;
      if (stringifier2.stringify) stringifier2 = stringifier2.stringify;
      var result2 = "";
      stringifier2(this, function(i3) {
        result2 += i3;
      });
      return result2;
    };
    _proto.warn = function warn(result2, text, opts) {
      var data = {
        node: this
      };
      for (var i3 in opts) data[i3] = opts[i3];
      return result2.warn(text, data);
    };
    _create_class(Node1, [
      {
        key: "proxyOf",
        get: function get() {
          return this;
        }
      }
    ]);
    return Node1;
  })();
  var node = Node$4;
  Node$4.default = Node$4;
  var Node$3 = node;
  var Declaration$4 = /* @__PURE__ */ (function(Node$32) {
    _inherits(Declaration, Node$32);
    function Declaration(defaults) {
      var _this;
      if (defaults && typeof defaults.value !== "undefined" && typeof defaults.value !== "string") {
        defaults = _extends({}, defaults, {
          value: String(defaults.value)
        });
      }
      _this = Node$32.call(this, defaults) || this;
      _this.type = "decl";
      return _this;
    }
    _create_class(Declaration, [
      {
        key: "variable",
        get: function get() {
          return this.prop.startsWith("--") || this.prop[0] === "$";
        }
      }
    ]);
    return Declaration;
  })(Node$3);
  var declaration = Declaration$4;
  Declaration$4.default = Declaration$4;
  var urlAlphabet = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
  var nanoid$1 = function(size) {
    if (size === void 0) size = 21;
    var id = "";
    var i3 = size;
    while (i3--) {
      id += urlAlphabet[Math.random() * 64 | 0];
    }
    return id;
  };
  var nonSecure = {
    nanoid: nanoid$1
  };
  var SourceMapConsumer$2 = require$$2.SourceMapConsumer;
  var SourceMapGenerator$2 = require$$2.SourceMapGenerator;
  var existsSync = require$$2.existsSync;
  var readFileSync = require$$2.readFileSync;
  var dirname$1 = require$$2.dirname;
  var join = require$$2.join;
  function fromBase64(str) {
    if (Buffer) {
      return Buffer.from(str, "base64").toString();
    } else {
      return window.atob(str);
    }
  }
  var PreviousMap$2 = /* @__PURE__ */ (function() {
    function PreviousMap(css, opts) {
      if (opts.map === false) return;
      this.loadAnnotation(css);
      this.inline = this.startWith(this.annotation, "data:");
      var prev = opts.map ? opts.map.prev : void 0;
      var text = this.loadMap(opts.from, prev);
      if (!this.mapFile && opts.from) {
        this.mapFile = opts.from;
      }
      if (this.mapFile) this.root = dirname$1(this.mapFile);
      if (text) this.text = text;
    }
    var _proto = PreviousMap.prototype;
    _proto.consumer = function consumer() {
      if (!this.consumerCache) {
        this.consumerCache = new SourceMapConsumer$2(this.text);
      }
      return this.consumerCache;
    };
    _proto.decodeInline = function decodeInline(text) {
      var baseCharsetUri = /^data:application\/json;charset=utf-?8;base64,/;
      var baseUri = /^data:application\/json;base64,/;
      var charsetUri = /^data:application\/json;charset=utf-?8,/;
      var uri = /^data:application\/json,/;
      if (charsetUri.test(text) || uri.test(text)) {
        return decodeURIComponent(text.substr(RegExp.lastMatch.length));
      }
      if (baseCharsetUri.test(text) || baseUri.test(text)) {
        return fromBase64(text.substr(RegExp.lastMatch.length));
      }
      var encoding = text.match(/data:application\/json;([^,]+),/)[1];
      throw new Error("Unsupported source map encoding " + encoding);
    };
    _proto.getAnnotationURL = function getAnnotationURL(sourceMapString) {
      return sourceMapString.replace(/^\/\*\s*# sourceMappingURL=/, "").trim();
    };
    _proto.isMap = function isMap(map) {
      if ((typeof map === "undefined" ? "undefined" : _type_of(map)) !== "object") return false;
      return typeof map.mappings === "string" || typeof map._mappings === "string" || Array.isArray(map.sections);
    };
    _proto.loadAnnotation = function loadAnnotation(css) {
      var comments = css.match(/\/\*\s*# sourceMappingURL=/gm);
      if (!comments) return;
      var start = css.lastIndexOf(comments.pop());
      var end = css.indexOf("*/", start);
      if (start > -1 && end > -1) {
        this.annotation = this.getAnnotationURL(css.substring(start, end));
      }
    };
    _proto.loadFile = function loadFile(path) {
      this.root = dirname$1(path);
      if (existsSync(path)) {
        this.mapFile = path;
        return readFileSync(path, "utf-8").toString().trim();
      }
    };
    _proto.loadMap = function loadMap(file, prev) {
      if (prev === false) return false;
      if (prev) {
        if (typeof prev === "string") {
          return prev;
        } else if (typeof prev === "function") {
          var prevPath = prev(file);
          if (prevPath) {
            var map = this.loadFile(prevPath);
            if (!map) {
              throw new Error("Unable to load previous source map: " + prevPath.toString());
            }
            return map;
          }
        } else if (_instanceof(prev, SourceMapConsumer$2)) {
          return SourceMapGenerator$2.fromSourceMap(prev).toString();
        } else if (_instanceof(prev, SourceMapGenerator$2)) {
          return prev.toString();
        } else if (this.isMap(prev)) {
          return JSON.stringify(prev);
        } else {
          throw new Error("Unsupported previous source map format: " + prev.toString());
        }
      } else if (this.inline) {
        return this.decodeInline(this.annotation);
      } else if (this.annotation) {
        var map1 = this.annotation;
        if (file) map1 = join(dirname$1(file), map1);
        return this.loadFile(map1);
      }
    };
    _proto.startWith = function startWith(string, start) {
      if (!string) return false;
      return string.substr(0, start.length) === start;
    };
    _proto.withContent = function withContent() {
      return !!(this.consumer().sourcesContent && this.consumer().sourcesContent.length > 0);
    };
    return PreviousMap;
  })();
  var previousMap = PreviousMap$2;
  PreviousMap$2.default = PreviousMap$2;
  var SourceMapConsumer$1 = require$$2.SourceMapConsumer;
  var SourceMapGenerator$1 = require$$2.SourceMapGenerator;
  var fileURLToPath = require$$2.fileURLToPath;
  var pathToFileURL$1 = require$$2.pathToFileURL;
  var isAbsolute = require$$2.isAbsolute;
  var resolve$1 = require$$2.resolve;
  var nanoid = nonSecure.nanoid;
  var terminalHighlight = require$$2;
  var CssSyntaxError$1 = cssSyntaxError;
  var PreviousMap$1 = previousMap;
  var fromOffsetCache = /* @__PURE__ */ Symbol("fromOffsetCache");
  var sourceMapAvailable$1 = Boolean(SourceMapConsumer$1 && SourceMapGenerator$1);
  var pathAvailable$1 = Boolean(resolve$1 && isAbsolute);
  var Input$4 = /* @__PURE__ */ (function() {
    function Input(css, opts) {
      if (opts === void 0) opts = {};
      if (css === null || typeof css === "undefined" || (typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && !css.toString) {
        throw new Error("PostCSS received " + css + " instead of CSS string");
      }
      this.css = css.toString();
      if (this.css[0] === "\uFEFF" || this.css[0] === "\uFFFE") {
        this.hasBOM = true;
        this.css = this.css.slice(1);
      } else {
        this.hasBOM = false;
      }
      if (opts.from) {
        if (!pathAvailable$1 || /^\w+:\/\//.test(opts.from) || isAbsolute(opts.from)) {
          this.file = opts.from;
        } else {
          this.file = resolve$1(opts.from);
        }
      }
      if (pathAvailable$1 && sourceMapAvailable$1) {
        var map = new PreviousMap$1(this.css, opts);
        if (map.text) {
          this.map = map;
          var file = map.consumer().file;
          if (!this.file && file) this.file = this.mapResolve(file);
        }
      }
      if (!this.file) {
        this.id = "<input css " + nanoid(6) + ">";
      }
      if (this.map) this.map.file = this.from;
    }
    var _proto = Input.prototype;
    _proto.error = function error(message, line, column, opts) {
      if (opts === void 0) opts = {};
      var result2, endLine, endColumn;
      if (line && (typeof line === "undefined" ? "undefined" : _type_of(line)) === "object") {
        var start = line;
        var end = column;
        if (typeof start.offset === "number") {
          var pos = this.fromOffset(start.offset);
          line = pos.line;
          column = pos.col;
        } else {
          line = start.line;
          column = start.column;
        }
        if (typeof end.offset === "number") {
          var pos1 = this.fromOffset(end.offset);
          endLine = pos1.line;
          endColumn = pos1.col;
        } else {
          endLine = end.line;
          endColumn = end.column;
        }
      } else if (!column) {
        var pos2 = this.fromOffset(line);
        line = pos2.line;
        column = pos2.col;
      }
      var origin = this.origin(line, column, endLine, endColumn);
      if (origin) {
        result2 = new CssSyntaxError$1(message, origin.endLine === void 0 ? origin.line : {
          column: origin.column,
          line: origin.line
        }, origin.endLine === void 0 ? origin.column : {
          column: origin.endColumn,
          line: origin.endLine
        }, origin.source, origin.file, opts.plugin);
      } else {
        result2 = new CssSyntaxError$1(message, endLine === void 0 ? line : {
          column,
          line
        }, endLine === void 0 ? column : {
          column: endColumn,
          line: endLine
        }, this.css, this.file, opts.plugin);
      }
      result2.input = {
        column,
        endColumn,
        endLine,
        line,
        source: this.css
      };
      if (this.file) {
        if (pathToFileURL$1) {
          result2.input.url = pathToFileURL$1(this.file).toString();
        }
        result2.input.file = this.file;
      }
      return result2;
    };
    _proto.fromOffset = function fromOffset(offset) {
      var lastLine, lineToIndex;
      if (!this[fromOffsetCache]) {
        var lines = this.css.split("\n");
        lineToIndex = new Array(lines.length);
        var prevIndex = 0;
        for (var i3 = 0, l3 = lines.length; i3 < l3; i3++) {
          lineToIndex[i3] = prevIndex;
          prevIndex += lines[i3].length + 1;
        }
        this[fromOffsetCache] = lineToIndex;
      } else {
        lineToIndex = this[fromOffsetCache];
      }
      lastLine = lineToIndex[lineToIndex.length - 1];
      var min = 0;
      if (offset >= lastLine) {
        min = lineToIndex.length - 1;
      } else {
        var max = lineToIndex.length - 2;
        var mid;
        while (min < max) {
          mid = min + (max - min >> 1);
          if (offset < lineToIndex[mid]) {
            max = mid - 1;
          } else if (offset >= lineToIndex[mid + 1]) {
            min = mid + 1;
          } else {
            min = mid;
            break;
          }
        }
      }
      return {
        col: offset - lineToIndex[min] + 1,
        line: min + 1
      };
    };
    _proto.mapResolve = function mapResolve(file) {
      if (/^\w+:\/\//.test(file)) {
        return file;
      }
      return resolve$1(this.map.consumer().sourceRoot || this.map.root || ".", file);
    };
    _proto.origin = function origin(line, column, endLine, endColumn) {
      if (!this.map) return false;
      var consumer = this.map.consumer();
      var from = consumer.originalPositionFor({
        column,
        line
      });
      if (!from.source) return false;
      var to;
      if (typeof endLine === "number") {
        to = consumer.originalPositionFor({
          column: endColumn,
          line: endLine
        });
      }
      var fromUrl;
      if (isAbsolute(from.source)) {
        fromUrl = pathToFileURL$1(from.source);
      } else {
        fromUrl = new URL(from.source, this.map.consumer().sourceRoot || pathToFileURL$1(this.map.mapFile));
      }
      var result2 = {
        column: from.column,
        endColumn: to && to.column,
        endLine: to && to.line,
        line: from.line,
        url: fromUrl.toString()
      };
      if (fromUrl.protocol === "file:") {
        if (fileURLToPath) {
          result2.file = fileURLToPath(fromUrl);
        } else {
          throw new Error("file: protocol is not available in this PostCSS build");
        }
      }
      var source = consumer.sourceContentFor(from.source);
      if (source) result2.source = source;
      return result2;
    };
    _proto.toJSON = function toJSON() {
      var json = {};
      for (var _i = 0, _iter = [
        "hasBOM",
        "css",
        "file",
        "id"
      ]; _i < _iter.length; _i++) {
        var name5 = _iter[_i];
        if (this[name5] != null) {
          json[name5] = this[name5];
        }
      }
      if (this.map) {
        json.map = _extends({}, this.map);
        if (json.map.consumerCache) {
          json.map.consumerCache = void 0;
        }
      }
      return json;
    };
    _create_class(Input, [
      {
        key: "from",
        get: function get() {
          return this.file || this.id;
        }
      }
    ]);
    return Input;
  })();
  var input = Input$4;
  Input$4.default = Input$4;
  if (terminalHighlight && terminalHighlight.registerInput) {
    terminalHighlight.registerInput(Input$4);
  }
  var SourceMapConsumer = require$$2.SourceMapConsumer;
  var SourceMapGenerator = require$$2.SourceMapGenerator;
  var dirname = require$$2.dirname;
  var relative = require$$2.relative;
  var resolve$4 = require$$2.resolve;
  var sep = require$$2.sep;
  var pathToFileURL = require$$2.pathToFileURL;
  var Input$3 = input;
  var sourceMapAvailable = Boolean(SourceMapConsumer && SourceMapGenerator);
  var pathAvailable = Boolean(dirname && resolve$4 && relative && sep);
  var MapGenerator$2 = /* @__PURE__ */ (function() {
    function MapGenerator(stringify2, root2, opts, cssString) {
      this.stringify = stringify2;
      this.mapOpts = opts.map || {};
      this.root = root2;
      this.opts = opts;
      this.css = cssString;
      this.originalCSS = cssString;
      this.usesFileUrls = !this.mapOpts.from && this.mapOpts.absolute;
      this.memoizedFileURLs = /* @__PURE__ */ new Map();
      this.memoizedPaths = /* @__PURE__ */ new Map();
      this.memoizedURLs = /* @__PURE__ */ new Map();
    }
    var _proto = MapGenerator.prototype;
    _proto.addAnnotation = function addAnnotation() {
      var content;
      if (this.isInline()) {
        content = "data:application/json;base64," + this.toBase64(this.map.toString());
      } else if (typeof this.mapOpts.annotation === "string") {
        content = this.mapOpts.annotation;
      } else if (typeof this.mapOpts.annotation === "function") {
        content = this.mapOpts.annotation(this.opts.to, this.root);
      } else {
        content = this.outputFile() + ".map";
      }
      var eol = "\n";
      if (this.css.includes("\r\n")) eol = "\r\n";
      this.css += eol + "/*# sourceMappingURL=" + content + " */";
    };
    _proto.applyPrevMaps = function applyPrevMaps() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.previous()), _step; !(_step = _iterator()).done; ) {
        var prev = _step.value;
        var from = this.toUrl(this.path(prev.file));
        var root2 = prev.root || dirname(prev.file);
        var map = void 0;
        if (this.mapOpts.sourcesContent === false) {
          map = new SourceMapConsumer(prev.text);
          if (map.sourcesContent) {
            map.sourcesContent = null;
          }
        } else {
          map = prev.consumer();
        }
        this.map.applySourceMap(map, from, this.toUrl(this.path(root2)));
      }
    };
    _proto.clearAnnotation = function clearAnnotation() {
      if (this.mapOpts.annotation === false) return;
      if (this.root) {
        var node2;
        for (var i3 = this.root.nodes.length - 1; i3 >= 0; i3--) {
          node2 = this.root.nodes[i3];
          if (node2.type !== "comment") continue;
          if (node2.text.indexOf("# sourceMappingURL=") === 0) {
            this.root.removeChild(i3);
          }
        }
      } else if (this.css) {
        this.css = this.css.replace(/\n*?\/\*#[\S\s]*?\*\/$/gm, "");
      }
    };
    _proto.generate = function generate() {
      this.clearAnnotation();
      if (pathAvailable && sourceMapAvailable && this.isMap()) {
        return this.generateMap();
      } else {
        var result2 = "";
        this.stringify(this.root, function(i3) {
          result2 += i3;
        });
        return [
          result2
        ];
      }
    };
    _proto.generateMap = function generateMap() {
      if (this.root) {
        this.generateString();
      } else if (this.previous().length === 1) {
        var prev = this.previous()[0].consumer();
        prev.file = this.outputFile();
        this.map = SourceMapGenerator.fromSourceMap(prev, {
          ignoreInvalidMapping: true
        });
      } else {
        this.map = new SourceMapGenerator({
          file: this.outputFile(),
          ignoreInvalidMapping: true
        });
        this.map.addMapping({
          generated: {
            column: 0,
            line: 1
          },
          original: {
            column: 0,
            line: 1
          },
          source: this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>"
        });
      }
      if (this.isSourcesContent()) this.setSourcesContent();
      if (this.root && this.previous().length > 0) this.applyPrevMaps();
      if (this.isAnnotation()) this.addAnnotation();
      if (this.isInline()) {
        return [
          this.css
        ];
      } else {
        return [
          this.css,
          this.map
        ];
      }
    };
    _proto.generateString = function generateString() {
      var _this = this;
      this.css = "";
      this.map = new SourceMapGenerator({
        file: this.outputFile(),
        ignoreInvalidMapping: true
      });
      var line = 1;
      var column = 1;
      var noSource = "<no source>";
      var mapping = {
        generated: {
          column: 0,
          line: 0
        },
        original: {
          column: 0,
          line: 0
        },
        source: ""
      };
      var lines, last;
      this.stringify(this.root, function(str, node2, type) {
        _this.css += str;
        if (node2 && type !== "end") {
          mapping.generated.line = line;
          mapping.generated.column = column - 1;
          if (node2.source && node2.source.start) {
            mapping.source = _this.sourcePath(node2);
            mapping.original.line = node2.source.start.line;
            mapping.original.column = node2.source.start.column - 1;
            _this.map.addMapping(mapping);
          } else {
            mapping.source = noSource;
            mapping.original.line = 1;
            mapping.original.column = 0;
            _this.map.addMapping(mapping);
          }
        }
        lines = str.match(/\n/g);
        if (lines) {
          line += lines.length;
          last = str.lastIndexOf("\n");
          column = str.length - last;
        } else {
          column += str.length;
        }
        if (node2 && type !== "start") {
          var p3 = node2.parent || {
            raws: {}
          };
          var childless = node2.type === "decl" || node2.type === "atrule" && !node2.nodes;
          if (!childless || node2 !== p3.last || p3.raws.semicolon) {
            if (node2.source && node2.source.end) {
              mapping.source = _this.sourcePath(node2);
              mapping.original.line = node2.source.end.line;
              mapping.original.column = node2.source.end.column - 1;
              mapping.generated.line = line;
              mapping.generated.column = column - 2;
              _this.map.addMapping(mapping);
            } else {
              mapping.source = noSource;
              mapping.original.line = 1;
              mapping.original.column = 0;
              mapping.generated.line = line;
              mapping.generated.column = column - 1;
              _this.map.addMapping(mapping);
            }
          }
        }
      });
    };
    _proto.isAnnotation = function isAnnotation() {
      if (this.isInline()) {
        return true;
      }
      if (typeof this.mapOpts.annotation !== "undefined") {
        return this.mapOpts.annotation;
      }
      if (this.previous().length) {
        return this.previous().some(function(i3) {
          return i3.annotation;
        });
      }
      return true;
    };
    _proto.isInline = function isInline() {
      if (typeof this.mapOpts.inline !== "undefined") {
        return this.mapOpts.inline;
      }
      var annotation = this.mapOpts.annotation;
      if (typeof annotation !== "undefined" && annotation !== true) {
        return false;
      }
      if (this.previous().length) {
        return this.previous().some(function(i3) {
          return i3.inline;
        });
      }
      return true;
    };
    _proto.isMap = function isMap() {
      if (typeof this.opts.map !== "undefined") {
        return !!this.opts.map;
      }
      return this.previous().length > 0;
    };
    _proto.isSourcesContent = function isSourcesContent() {
      if (typeof this.mapOpts.sourcesContent !== "undefined") {
        return this.mapOpts.sourcesContent;
      }
      if (this.previous().length) {
        return this.previous().some(function(i3) {
          return i3.withContent();
        });
      }
      return true;
    };
    _proto.outputFile = function outputFile() {
      if (this.opts.to) {
        return this.path(this.opts.to);
      } else if (this.opts.from) {
        return this.path(this.opts.from);
      } else {
        return "to.css";
      }
    };
    _proto.path = function path(file) {
      if (this.mapOpts.absolute) return file;
      if (file.charCodeAt(0) === 60) return file;
      if (/^\w+:\/\//.test(file)) return file;
      var cached = this.memoizedPaths.get(file);
      if (cached) return cached;
      var from = this.opts.to ? dirname(this.opts.to) : ".";
      if (typeof this.mapOpts.annotation === "string") {
        from = dirname(resolve$4(from, this.mapOpts.annotation));
      }
      var path2 = relative(from, file);
      this.memoizedPaths.set(file, path2);
      return path2;
    };
    _proto.previous = function previous() {
      var _this = this;
      if (!this.previousMaps) {
        this.previousMaps = [];
        if (this.root) {
          this.root.walk(function(node2) {
            if (node2.source && node2.source.input.map) {
              var map = node2.source.input.map;
              if (!_this.previousMaps.includes(map)) {
                _this.previousMaps.push(map);
              }
            }
          });
        } else {
          var input2 = new Input$3(this.originalCSS, this.opts);
          if (input2.map) this.previousMaps.push(input2.map);
        }
      }
      return this.previousMaps;
    };
    _proto.setSourcesContent = function setSourcesContent() {
      var _this = this;
      var already = {};
      if (this.root) {
        this.root.walk(function(node2) {
          if (node2.source) {
            var from2 = node2.source.input.from;
            if (from2 && !already[from2]) {
              already[from2] = true;
              var fromUrl = _this.usesFileUrls ? _this.toFileUrl(from2) : _this.toUrl(_this.path(from2));
              _this.map.setSourceContent(fromUrl, node2.source.input.css);
            }
          }
        });
      } else if (this.css) {
        var from = this.opts.from ? this.toUrl(this.path(this.opts.from)) : "<no source>";
        this.map.setSourceContent(from, this.css);
      }
    };
    _proto.sourcePath = function sourcePath(node2) {
      if (this.mapOpts.from) {
        return this.toUrl(this.mapOpts.from);
      } else if (this.usesFileUrls) {
        return this.toFileUrl(node2.source.input.from);
      } else {
        return this.toUrl(this.path(node2.source.input.from));
      }
    };
    _proto.toBase64 = function toBase64(str) {
      if (Buffer) {
        return Buffer.from(str).toString("base64");
      } else {
        return window.btoa(unescape(encodeURIComponent(str)));
      }
    };
    _proto.toFileUrl = function toFileUrl(path) {
      var cached = this.memoizedFileURLs.get(path);
      if (cached) return cached;
      if (pathToFileURL) {
        var fileURL = pathToFileURL(path).toString();
        this.memoizedFileURLs.set(path, fileURL);
        return fileURL;
      } else {
        throw new Error("`map.absolute` option is not available in this PostCSS build");
      }
    };
    _proto.toUrl = function toUrl(path) {
      var cached = this.memoizedURLs.get(path);
      if (cached) return cached;
      if (sep === "\\") {
        path = path.replace(/\\/g, "/");
      }
      var url = encodeURI(path).replace(/[#?]/g, encodeURIComponent);
      this.memoizedURLs.set(path, url);
      return url;
    };
    return MapGenerator;
  })();
  var mapGenerator = MapGenerator$2;
  var Node$2 = node;
  var Comment$4 = /* @__PURE__ */ (function(Node$22) {
    _inherits(Comment, Node$22);
    function Comment(defaults) {
      var _this;
      _this = Node$22.call(this, defaults) || this;
      _this.type = "comment";
      return _this;
    }
    return Comment;
  })(Node$2);
  var comment = Comment$4;
  Comment$4.default = Comment$4;
  var isClean$1 = symbols.isClean;
  var my$1 = symbols.my;
  var Declaration$3 = declaration;
  var Comment$3 = comment;
  var Node$1 = node;
  var parse$4;
  var Rule$4;
  var AtRule$4;
  var Root$6;
  function cleanSource(nodes) {
    return nodes.map(function(i3) {
      if (i3.nodes) i3.nodes = cleanSource(i3.nodes);
      delete i3.source;
      return i3;
    });
  }
  function markDirtyUp(node2) {
    node2[isClean$1] = false;
    if (node2.proxyOf.nodes) {
      for (var _iterator = _create_for_of_iterator_helper_loose(node2.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var i3 = _step.value;
        markDirtyUp(i3);
      }
    }
  }
  var Container$7 = /* @__PURE__ */ (function(Node$12) {
    _inherits(Container, Node$12);
    function Container() {
      return Node$12.apply(this, arguments) || this;
    }
    var _proto = Container.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.last);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.push(node2);
        }
      }
      this.markDirty();
      return this;
    };
    _proto.cleanRaws = function cleanRaws(keepBetween) {
      Node$12.prototype.cleanRaws.call(this, keepBetween);
      if (this.nodes) {
        for (var _iterator = _create_for_of_iterator_helper_loose(this.nodes), _step; !(_step = _iterator()).done; ) {
          var node2 = _step.value;
          node2.cleanRaws(keepBetween);
        }
      }
    };
    _proto.each = function each(callback) {
      if (!this.proxyOf.nodes) return void 0;
      var iterator = this.getIterator();
      var index2, result2;
      while (this.indexes[iterator] < this.proxyOf.nodes.length) {
        index2 = this.indexes[iterator];
        result2 = callback(this.proxyOf.nodes[index2], index2);
        if (result2 === false) break;
        this.indexes[iterator] += 1;
      }
      delete this.indexes[iterator];
      return result2;
    };
    _proto.every = function every(condition) {
      return this.nodes.every(condition);
    };
    _proto.getIterator = function getIterator() {
      if (!this.lastEach) this.lastEach = 0;
      if (!this.indexes) this.indexes = {};
      this.lastEach += 1;
      var iterator = this.lastEach;
      this.indexes[iterator] = 0;
      return iterator;
    };
    _proto.getProxyProcessor = function getProxyProcessor() {
      return {
        get: function get(node2, prop) {
          if (prop === "proxyOf") {
            return node2;
          } else if (!node2[prop]) {
            return node2[prop];
          } else if (prop === "each" || typeof prop === "string" && prop.startsWith("walk")) {
            return function() {
              for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
                args[_key] = arguments[_key];
              }
              var _node2;
              return (_node2 = node2)[prop].apply(_node2, [].concat(args.map(function(i3) {
                if (typeof i3 === "function") {
                  return function(child, index2) {
                    return i3(child.toProxy(), index2);
                  };
                } else {
                  return i3;
                }
              })));
            };
          } else if (prop === "every" || prop === "some") {
            return function(cb) {
              return node2[prop](function(child) {
                for (var _len = arguments.length, other = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
                  other[_key - 1] = arguments[_key];
                }
                return cb.apply(void 0, [].concat([
                  child.toProxy()
                ], other));
              });
            };
          } else if (prop === "root") {
            return function() {
              return node2.root().toProxy();
            };
          } else if (prop === "nodes") {
            return node2.nodes.map(function(i3) {
              return i3.toProxy();
            });
          } else if (prop === "first" || prop === "last") {
            return node2[prop].toProxy();
          } else {
            return node2[prop];
          }
        },
        set: function set2(node2, prop, value) {
          if (node2[prop] === value) return true;
          node2[prop] = value;
          if (prop === "name" || prop === "params" || prop === "selector") {
            node2.markDirty();
          }
          return true;
        }
      };
    };
    _proto.index = function index2(child) {
      if (typeof child === "number") return child;
      if (child.proxyOf) child = child.proxyOf;
      return this.proxyOf.nodes.indexOf(child);
    };
    _proto.insertAfter = function insertAfter(exist, add) {
      var existIndex = this.index(exist);
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex]).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex + 1, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex < index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.insertBefore = function insertBefore(exist, add) {
      var existIndex = this.index(exist);
      var type = existIndex === 0 ? "prepend" : false;
      var nodes = this.normalize(add, this.proxyOf.nodes[existIndex], type).reverse();
      existIndex = this.index(exist);
      for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        this.proxyOf.nodes.splice(existIndex, 0, node2);
      }
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (existIndex <= index2) {
          this.indexes[id] = index2 + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.normalize = function normalize(nodes, sample) {
      var _this = this;
      if (typeof nodes === "string") {
        nodes = cleanSource(parse$4(nodes).nodes);
      } else if (typeof nodes === "undefined") {
        nodes = [];
      } else if (Array.isArray(nodes)) {
        nodes = nodes.slice(0);
        for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
          var i3 = _step.value;
          if (i3.parent) i3.parent.removeChild(i3, "ignore");
        }
      } else if (nodes.type === "root" && this.type !== "document") {
        nodes = nodes.nodes.slice(0);
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var i1 = _step1.value;
          if (i1.parent) i1.parent.removeChild(i1, "ignore");
        }
      } else if (nodes.type) {
        nodes = [
          nodes
        ];
      } else if (nodes.prop) {
        if (typeof nodes.value === "undefined") {
          throw new Error("Value field is missed in node creation");
        } else if (typeof nodes.value !== "string") {
          nodes.value = String(nodes.value);
        }
        nodes = [
          new Declaration$3(nodes)
        ];
      } else if (nodes.selector) {
        nodes = [
          new Rule$4(nodes)
        ];
      } else if (nodes.name) {
        nodes = [
          new AtRule$4(nodes)
        ];
      } else if (nodes.text) {
        nodes = [
          new Comment$3(nodes)
        ];
      } else {
        throw new Error("Unknown node type in node creation");
      }
      var processed = nodes.map(function(i4) {
        if (!i4[my$1]) Container.rebuild(i4);
        i4 = i4.proxyOf;
        if (i4.parent) i4.parent.removeChild(i4);
        if (i4[isClean$1]) markDirtyUp(i4);
        if (typeof i4.raws.before === "undefined") {
          if (sample && typeof sample.raws.before !== "undefined") {
            i4.raws.before = sample.raws.before.replace(/\S/g, "");
          }
        }
        i4.parent = _this.proxyOf;
        return i4;
      });
      return processed;
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      children = children.reverse();
      for (var _iterator = _create_for_of_iterator_helper_loose(children), _step; !(_step = _iterator()).done; ) {
        var child = _step.value;
        var nodes = this.normalize(child, this.first, "prepend").reverse();
        for (var _iterator1 = _create_for_of_iterator_helper_loose(nodes), _step1; !(_step1 = _iterator1()).done; ) {
          var node2 = _step1.value;
          this.proxyOf.nodes.unshift(node2);
        }
        for (var id in this.indexes) {
          this.indexes[id] = this.indexes[id] + nodes.length;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.push = function push(child) {
      child.parent = this;
      this.proxyOf.nodes.push(child);
      return this;
    };
    _proto.removeAll = function removeAll() {
      for (var _iterator = _create_for_of_iterator_helper_loose(this.proxyOf.nodes), _step; !(_step = _iterator()).done; ) {
        var node2 = _step.value;
        node2.parent = void 0;
      }
      this.proxyOf.nodes = [];
      this.markDirty();
      return this;
    };
    _proto.removeChild = function removeChild(child) {
      child = this.index(child);
      this.proxyOf.nodes[child].parent = void 0;
      this.proxyOf.nodes.splice(child, 1);
      var index2;
      for (var id in this.indexes) {
        index2 = this.indexes[id];
        if (index2 >= child) {
          this.indexes[id] = index2 - 1;
        }
      }
      this.markDirty();
      return this;
    };
    _proto.replaceValues = function replaceValues(pattern, opts, callback) {
      if (!callback) {
        callback = opts;
        opts = {};
      }
      this.walkDecls(function(decl) {
        if (opts.props && !opts.props.includes(decl.prop)) return;
        if (opts.fast && !decl.value.includes(opts.fast)) return;
        decl.value = decl.value.replace(pattern, callback);
      });
      this.markDirty();
      return this;
    };
    _proto.some = function some(condition) {
      return this.nodes.some(condition);
    };
    _proto.walk = function walk(callback) {
      return this.each(function(child, i3) {
        var result2;
        try {
          result2 = callback(child, i3);
        } catch (e3) {
          throw child.addToError(e3);
        }
        if (result2 !== false && child.walk) {
          result2 = child.walk(callback);
        }
        return result2;
      });
    };
    _proto.walkAtRules = function walkAtRules(name5, callback) {
      if (!callback) {
        callback = name5;
        return this.walk(function(child, i3) {
          if (child.type === "atrule") {
            return callback(child, i3);
          }
        });
      }
      if (_instanceof(name5, RegExp)) {
        return this.walk(function(child, i3) {
          if (child.type === "atrule" && name5.test(child.name)) {
            return callback(child, i3);
          }
        });
      }
      return this.walk(function(child, i3) {
        if (child.type === "atrule" && child.name === name5) {
          return callback(child, i3);
        }
      });
    };
    _proto.walkComments = function walkComments(callback) {
      return this.walk(function(child, i3) {
        if (child.type === "comment") {
          return callback(child, i3);
        }
      });
    };
    _proto.walkDecls = function walkDecls(prop, callback) {
      if (!callback) {
        callback = prop;
        return this.walk(function(child, i3) {
          if (child.type === "decl") {
            return callback(child, i3);
          }
        });
      }
      if (_instanceof(prop, RegExp)) {
        return this.walk(function(child, i3) {
          if (child.type === "decl" && prop.test(child.prop)) {
            return callback(child, i3);
          }
        });
      }
      return this.walk(function(child, i3) {
        if (child.type === "decl" && child.prop === prop) {
          return callback(child, i3);
        }
      });
    };
    _proto.walkRules = function walkRules(selector, callback) {
      if (!callback) {
        callback = selector;
        return this.walk(function(child, i3) {
          if (child.type === "rule") {
            return callback(child, i3);
          }
        });
      }
      if (_instanceof(selector, RegExp)) {
        return this.walk(function(child, i3) {
          if (child.type === "rule" && selector.test(child.selector)) {
            return callback(child, i3);
          }
        });
      }
      return this.walk(function(child, i3) {
        if (child.type === "rule" && child.selector === selector) {
          return callback(child, i3);
        }
      });
    };
    _create_class(Container, [
      {
        key: "first",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[0];
        }
      },
      {
        key: "last",
        get: function get() {
          if (!this.proxyOf.nodes) return void 0;
          return this.proxyOf.nodes[this.proxyOf.nodes.length - 1];
        }
      }
    ]);
    return Container;
  })(Node$1);
  Container$7.registerParse = function(dependant) {
    parse$4 = dependant;
  };
  Container$7.registerRule = function(dependant) {
    Rule$4 = dependant;
  };
  Container$7.registerAtRule = function(dependant) {
    AtRule$4 = dependant;
  };
  Container$7.registerRoot = function(dependant) {
    Root$6 = dependant;
  };
  var container = Container$7;
  Container$7.default = Container$7;
  Container$7.rebuild = function(node2) {
    if (node2.type === "atrule") {
      Object.setPrototypeOf(node2, AtRule$4.prototype);
    } else if (node2.type === "rule") {
      Object.setPrototypeOf(node2, Rule$4.prototype);
    } else if (node2.type === "decl") {
      Object.setPrototypeOf(node2, Declaration$3.prototype);
    } else if (node2.type === "comment") {
      Object.setPrototypeOf(node2, Comment$3.prototype);
    } else if (node2.type === "root") {
      Object.setPrototypeOf(node2, Root$6.prototype);
    }
    node2[my$1] = true;
    if (node2.nodes) {
      node2.nodes.forEach(function(child) {
        Container$7.rebuild(child);
      });
    }
  };
  var Container$6 = container;
  var LazyResult$4;
  var Processor$3;
  var Document$3 = /* @__PURE__ */ (function(Container$62) {
    _inherits(Document, Container$62);
    function Document(defaults) {
      var _this;
      _this = Container$62.call(this, _extends({
        type: "document"
      }, defaults)) || this;
      if (!_this.nodes) {
        _this.nodes = [];
      }
      return _this;
    }
    var _proto = Document.prototype;
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$4(new Processor$3(), this, opts);
      return lazy.stringify();
    };
    return Document;
  })(Container$6);
  Document$3.registerLazyResult = function(dependant) {
    LazyResult$4 = dependant;
  };
  Document$3.registerProcessor = function(dependant) {
    Processor$3 = dependant;
  };
  var document$1$3 = Document$3;
  Document$3.default = Document$3;
  var printed = {};
  var warnOnce$2 = function warnOnce3(message) {
    if (printed[message]) return;
    printed[message] = true;
    if (typeof console !== "undefined" && console.warn) {
      console.warn(message);
    }
  };
  var Warning$2 = /* @__PURE__ */ (function() {
    function Warning(text, opts) {
      if (opts === void 0) opts = {};
      this.type = "warning";
      this.text = text;
      if (opts.node && opts.node.source) {
        var range = opts.node.rangeBy(opts);
        this.line = range.start.line;
        this.column = range.start.column;
        this.endLine = range.end.line;
        this.endColumn = range.end.column;
      }
      for (var opt in opts) this[opt] = opts[opt];
    }
    var _proto = Warning.prototype;
    _proto.toString = function toString2() {
      if (this.node) {
        return this.node.error(this.text, {
          index: this.index,
          plugin: this.plugin,
          word: this.word
        }).message;
      }
      if (this.plugin) {
        return this.plugin + ": " + this.text;
      }
      return this.text;
    };
    return Warning;
  })();
  var warning = Warning$2;
  Warning$2.default = Warning$2;
  var Warning$1 = warning;
  var Result$3 = /* @__PURE__ */ (function() {
    function Result(processor2, root2, opts) {
      this.processor = processor2;
      this.messages = [];
      this.root = root2;
      this.opts = opts;
      this.css = void 0;
      this.map = void 0;
    }
    var _proto = Result.prototype;
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.warn = function warn(text, opts) {
      if (opts === void 0) opts = {};
      if (!opts.plugin) {
        if (this.lastPlugin && this.lastPlugin.postcssPlugin) {
          opts.plugin = this.lastPlugin.postcssPlugin;
        }
      }
      var warning2 = new Warning$1(text, opts);
      this.messages.push(warning2);
      return warning2;
    };
    _proto.warnings = function warnings() {
      return this.messages.filter(function(i3) {
        return i3.type === "warning";
      });
    };
    _create_class(Result, [
      {
        key: "content",
        get: function get() {
          return this.css;
        }
      }
    ]);
    return Result;
  })();
  var result = Result$3;
  Result$3.default = Result$3;
  var SINGLE_QUOTE = "'".charCodeAt(0);
  var DOUBLE_QUOTE = '"'.charCodeAt(0);
  var BACKSLASH = "\\".charCodeAt(0);
  var SLASH = "/".charCodeAt(0);
  var NEWLINE = "\n".charCodeAt(0);
  var SPACE = " ".charCodeAt(0);
  var FEED = "\f".charCodeAt(0);
  var TAB = "	".charCodeAt(0);
  var CR = "\r".charCodeAt(0);
  var OPEN_SQUARE = "[".charCodeAt(0);
  var CLOSE_SQUARE = "]".charCodeAt(0);
  var OPEN_PARENTHESES = "(".charCodeAt(0);
  var CLOSE_PARENTHESES = ")".charCodeAt(0);
  var OPEN_CURLY = "{".charCodeAt(0);
  var CLOSE_CURLY = "}".charCodeAt(0);
  var SEMICOLON = ";".charCodeAt(0);
  var ASTERISK = "*".charCodeAt(0);
  var COLON = ":".charCodeAt(0);
  var AT = "@".charCodeAt(0);
  var RE_AT_END = /[\t\n\f\r "#'()/;[\\\]{}]/g;
  var RE_WORD_END = /[\t\n\f\r !"#'():;@[\\\]{}]|\/(?=\*)/g;
  var RE_BAD_BRACKET = /.[\r\n"'(/\\]/;
  var RE_HEX_ESCAPE = /[\da-f]/i;
  var tokenize = function tokenizer3(input2, options) {
    if (options === void 0) options = {};
    var css = input2.css.valueOf();
    var ignore = options.ignoreErrors;
    var code, next, quote, content, escape;
    var escaped, escapePos, prev, n3, currentToken;
    var length = css.length;
    var pos = 0;
    var buffer = [];
    var returned = [];
    function position() {
      return pos;
    }
    function unclosed(what) {
      throw input2.error("Unclosed " + what, pos);
    }
    function endOfFile() {
      return returned.length === 0 && pos >= length;
    }
    function nextToken(opts) {
      if (returned.length) return returned.pop();
      if (pos >= length) return;
      var ignoreUnclosed = opts ? opts.ignoreUnclosed : false;
      code = css.charCodeAt(pos);
      switch (code) {
        case NEWLINE:
        case SPACE:
        case TAB:
        case CR:
        case FEED: {
          next = pos;
          do {
            next += 1;
            code = css.charCodeAt(next);
          } while (code === SPACE || code === NEWLINE || code === TAB || code === CR || code === FEED);
          currentToken = [
            "space",
            css.slice(pos, next)
          ];
          pos = next - 1;
          break;
        }
        case OPEN_SQUARE:
        case CLOSE_SQUARE:
        case OPEN_CURLY:
        case CLOSE_CURLY:
        case COLON:
        case SEMICOLON:
        case CLOSE_PARENTHESES: {
          var controlChar = String.fromCharCode(code);
          currentToken = [
            controlChar,
            controlChar,
            pos
          ];
          break;
        }
        case OPEN_PARENTHESES: {
          prev = buffer.length ? buffer.pop()[1] : "";
          n3 = css.charCodeAt(pos + 1);
          if (prev === "url" && n3 !== SINGLE_QUOTE && n3 !== DOUBLE_QUOTE && n3 !== SPACE && n3 !== NEWLINE && n3 !== TAB && n3 !== FEED && n3 !== CR) {
            next = pos;
            do {
              escaped = false;
              next = css.indexOf(")", next + 1);
              if (next === -1) {
                if (ignore || ignoreUnclosed) {
                  next = pos;
                  break;
                } else {
                  unclosed("bracket");
                }
              }
              escapePos = next;
              while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
                escapePos -= 1;
                escaped = !escaped;
              }
            } while (escaped);
            currentToken = [
              "brackets",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            next = css.indexOf(")", pos + 1);
            content = css.slice(pos, next + 1);
            if (next === -1 || RE_BAD_BRACKET.test(content)) {
              currentToken = [
                "(",
                "(",
                pos
              ];
            } else {
              currentToken = [
                "brackets",
                content,
                pos,
                next
              ];
              pos = next;
            }
          }
          break;
        }
        case SINGLE_QUOTE:
        case DOUBLE_QUOTE: {
          quote = code === SINGLE_QUOTE ? "'" : '"';
          next = pos;
          do {
            escaped = false;
            next = css.indexOf(quote, next + 1);
            if (next === -1) {
              if (ignore || ignoreUnclosed) {
                next = pos + 1;
                break;
              } else {
                unclosed("string");
              }
            }
            escapePos = next;
            while (css.charCodeAt(escapePos - 1) === BACKSLASH) {
              escapePos -= 1;
              escaped = !escaped;
            }
          } while (escaped);
          currentToken = [
            "string",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case AT: {
          RE_AT_END.lastIndex = pos + 1;
          RE_AT_END.test(css);
          if (RE_AT_END.lastIndex === 0) {
            next = css.length - 1;
          } else {
            next = RE_AT_END.lastIndex - 2;
          }
          currentToken = [
            "at-word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        case BACKSLASH: {
          next = pos;
          escape = true;
          while (css.charCodeAt(next + 1) === BACKSLASH) {
            next += 1;
            escape = !escape;
          }
          code = css.charCodeAt(next + 1);
          if (escape && code !== SLASH && code !== SPACE && code !== NEWLINE && code !== TAB && code !== CR && code !== FEED) {
            next += 1;
            if (RE_HEX_ESCAPE.test(css.charAt(next))) {
              while (RE_HEX_ESCAPE.test(css.charAt(next + 1))) {
                next += 1;
              }
              if (css.charCodeAt(next + 1) === SPACE) {
                next += 1;
              }
            }
          }
          currentToken = [
            "word",
            css.slice(pos, next + 1),
            pos,
            next
          ];
          pos = next;
          break;
        }
        default: {
          if (code === SLASH && css.charCodeAt(pos + 1) === ASTERISK) {
            next = css.indexOf("*/", pos + 2) + 1;
            if (next === 0) {
              if (ignore || ignoreUnclosed) {
                next = css.length;
              } else {
                unclosed("comment");
              }
            }
            currentToken = [
              "comment",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            pos = next;
          } else {
            RE_WORD_END.lastIndex = pos + 1;
            RE_WORD_END.test(css);
            if (RE_WORD_END.lastIndex === 0) {
              next = css.length - 1;
            } else {
              next = RE_WORD_END.lastIndex - 2;
            }
            currentToken = [
              "word",
              css.slice(pos, next + 1),
              pos,
              next
            ];
            buffer.push(currentToken);
            pos = next;
          }
          break;
        }
      }
      pos++;
      return currentToken;
    }
    function back(token) {
      returned.push(token);
    }
    return {
      back,
      endOfFile,
      nextToken,
      position
    };
  };
  var Container$5 = container;
  var AtRule$3 = /* @__PURE__ */ (function(Container$52) {
    _inherits(AtRule, Container$52);
    function AtRule(defaults) {
      var _this;
      _this = Container$52.call(this, defaults) || this;
      _this.type = "atrule";
      return _this;
    }
    var _proto = AtRule.prototype;
    _proto.append = function append() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5_prototype_append;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5_prototype_append = Container$52.prototype.append).call.apply(_Container$5_prototype_append, [].concat([
        this
      ], children));
    };
    _proto.prepend = function prepend() {
      for (var _len = arguments.length, children = new Array(_len), _key = 0; _key < _len; _key++) {
        children[_key] = arguments[_key];
      }
      var _Container$5_prototype_prepend;
      if (!this.proxyOf.nodes) this.nodes = [];
      return (_Container$5_prototype_prepend = Container$52.prototype.prepend).call.apply(_Container$5_prototype_prepend, [].concat([
        this
      ], children));
    };
    return AtRule;
  })(Container$5);
  var atRule = AtRule$3;
  AtRule$3.default = AtRule$3;
  Container$5.registerAtRule(AtRule$3);
  var Container$4 = container;
  var LazyResult$3;
  var Processor$2;
  var Root$5 = /* @__PURE__ */ (function(Container$42) {
    _inherits(Root, Container$42);
    function Root(defaults) {
      var _this;
      _this = Container$42.call(this, defaults) || this;
      _this.type = "root";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    var _proto = Root.prototype;
    _proto.normalize = function normalize(child, sample, type) {
      var nodes = Container$42.prototype.normalize.call(this, child);
      if (sample) {
        if (type === "prepend") {
          if (this.nodes.length > 1) {
            sample.raws.before = this.nodes[1].raws.before;
          } else {
            delete sample.raws.before;
          }
        } else if (this.first !== sample) {
          for (var _iterator = _create_for_of_iterator_helper_loose(nodes), _step; !(_step = _iterator()).done; ) {
            var node2 = _step.value;
            node2.raws.before = sample.raws.before;
          }
        }
      }
      return nodes;
    };
    _proto.removeChild = function removeChild(child, ignore) {
      var index2 = this.index(child);
      if (!ignore && index2 === 0 && this.nodes.length > 1) {
        this.nodes[1].raws.before = this.nodes[index2].raws.before;
      }
      return Container$42.prototype.removeChild.call(this, child);
    };
    _proto.toResult = function toResult(opts) {
      if (opts === void 0) opts = {};
      var lazy = new LazyResult$3(new Processor$2(), this, opts);
      return lazy.stringify();
    };
    return Root;
  })(Container$4);
  Root$5.registerLazyResult = function(dependant) {
    LazyResult$3 = dependant;
  };
  Root$5.registerProcessor = function(dependant) {
    Processor$2 = dependant;
  };
  var root = Root$5;
  Root$5.default = Root$5;
  Container$4.registerRoot(Root$5);
  var list$2 = {
    comma: function comma3(string) {
      return list$2.split(string, [
        ","
      ], true);
    },
    space: function space3(string) {
      var spaces = [
        " ",
        "\n",
        "	"
      ];
      return list$2.split(string, spaces);
    },
    split: function split3(string, separators, last) {
      var array = [];
      var current = "";
      var split4 = false;
      var func = 0;
      var inQuote = false;
      var prevQuote = "";
      var escape = false;
      for (var _iterator = _create_for_of_iterator_helper_loose(string), _step; !(_step = _iterator()).done; ) {
        var letter = _step.value;
        if (escape) {
          escape = false;
        } else if (letter === "\\") {
          escape = true;
        } else if (inQuote) {
          if (letter === prevQuote) {
            inQuote = false;
          }
        } else if (letter === '"' || letter === "'") {
          inQuote = true;
          prevQuote = letter;
        } else if (letter === "(") {
          func += 1;
        } else if (letter === ")") {
          if (func > 0) func -= 1;
        } else if (func === 0) {
          if (separators.includes(letter)) split4 = true;
        }
        if (split4) {
          if (current !== "") array.push(current.trim());
          current = "";
          split4 = false;
        } else {
          current += letter;
        }
      }
      if (last || current !== "") array.push(current.trim());
      return array;
    }
  };
  var list_1 = list$2;
  list$2.default = list$2;
  var Container$3 = container;
  var list$1 = list_1;
  var Rule$3 = /* @__PURE__ */ (function(Container$32) {
    _inherits(Rule, Container$32);
    function Rule(defaults) {
      var _this;
      _this = Container$32.call(this, defaults) || this;
      _this.type = "rule";
      if (!_this.nodes) _this.nodes = [];
      return _this;
    }
    _create_class(Rule, [
      {
        key: "selectors",
        get: function get() {
          return list$1.comma(this.selector);
        },
        set: function set2(values) {
          var match = this.selector ? this.selector.match(/,\s*/) : null;
          var sep2 = match ? match[0] : "," + this.raw("between", "beforeOpen");
          this.selector = values.join(sep2);
        }
      }
    ]);
    return Rule;
  })(Container$3);
  var rule = Rule$3;
  Rule$3.default = Rule$3;
  Container$3.registerRule(Rule$3);
  var Declaration$2 = declaration;
  var tokenizer23 = tokenize;
  var Comment$2 = comment;
  var AtRule$2 = atRule;
  var Root$4 = root;
  var Rule$2 = rule;
  var SAFE_COMMENT_NEIGHBOR = {
    empty: true,
    space: true
  };
  function findLastWithPosition(tokens) {
    for (var i3 = tokens.length - 1; i3 >= 0; i3--) {
      var token = tokens[i3];
      var pos = token[3] || token[2];
      if (pos) return pos;
    }
  }
  var Parser$1 = /* @__PURE__ */ (function() {
    function Parser(input2) {
      this.input = input2;
      this.root = new Root$4();
      this.current = this.root;
      this.spaces = "";
      this.semicolon = false;
      this.createTokenizer();
      this.root.source = {
        input: input2,
        start: {
          column: 1,
          line: 1,
          offset: 0
        }
      };
    }
    var _proto = Parser.prototype;
    _proto.atrule = function atrule(token) {
      var node2 = new AtRule$2();
      node2.name = token[1].slice(1);
      if (node2.name === "") {
        this.unnamedAtrule(node2, token);
      }
      this.init(node2, token[2]);
      var type;
      var prev;
      var shift;
      var last = false;
      var open = false;
      var params = [];
      var brackets = [];
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        type = token[0];
        if (type === "(" || type === "[") {
          brackets.push(type === "(" ? ")" : "]");
        } else if (type === "{" && brackets.length > 0) {
          brackets.push("}");
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
        }
        if (brackets.length === 0) {
          if (type === ";") {
            node2.source.end = this.getPosition(token[2]);
            node2.source.end.offset++;
            this.semicolon = true;
            break;
          } else if (type === "{") {
            open = true;
            break;
          } else if (type === "}") {
            if (params.length > 0) {
              shift = params.length - 1;
              prev = params[shift];
              while (prev && prev[0] === "space") {
                prev = params[--shift];
              }
              if (prev) {
                node2.source.end = this.getPosition(prev[3] || prev[2]);
                node2.source.end.offset++;
              }
            }
            this.end(token);
            break;
          } else {
            params.push(token);
          }
        } else {
          params.push(token);
        }
        if (this.tokenizer.endOfFile()) {
          last = true;
          break;
        }
      }
      node2.raws.between = this.spacesAndCommentsFromEnd(params);
      if (params.length) {
        node2.raws.afterName = this.spacesAndCommentsFromStart(params);
        this.raw(node2, "params", params);
        if (last) {
          token = params[params.length - 1];
          node2.source.end = this.getPosition(token[3] || token[2]);
          node2.source.end.offset++;
          this.spaces = node2.raws.between;
          node2.raws.between = "";
        }
      } else {
        node2.raws.afterName = "";
        node2.params = "";
      }
      if (open) {
        node2.nodes = [];
        this.current = node2;
      }
    };
    _proto.checkMissedSemicolon = function checkMissedSemicolon(tokens) {
      var colon = this.colon(tokens);
      if (colon === false) return;
      var founded = 0;
      var token;
      for (var j4 = colon - 1; j4 >= 0; j4--) {
        token = tokens[j4];
        if (token[0] !== "space") {
          founded += 1;
          if (founded === 2) break;
        }
      }
      throw this.input.error("Missed semicolon", token[0] === "word" ? token[3] + 1 : token[2]);
    };
    _proto.colon = function colon(tokens) {
      var brackets = 0;
      var token, type, prev;
      for (var _iterator = _create_for_of_iterator_helper_loose(tokens.entries()), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, i3 = _step_value[0], element = _step_value[1];
        token = element;
        type = token[0];
        if (type === "(") {
          brackets += 1;
        }
        if (type === ")") {
          brackets -= 1;
        }
        if (brackets === 0 && type === ":") {
          if (!prev) {
            this.doubleColon(token);
          } else if (prev[0] === "word" && prev[1] === "progid") {
            continue;
          } else {
            return i3;
          }
        }
        prev = token;
      }
      return false;
    };
    _proto.comment = function comment2(token) {
      var node2 = new Comment$2();
      this.init(node2, token[2]);
      node2.source.end = this.getPosition(token[3] || token[2]);
      node2.source.end.offset++;
      var text = token[1].slice(2, -2);
      if (/^\s*$/.test(text)) {
        node2.text = "";
        node2.raws.left = text;
        node2.raws.right = "";
      } else {
        var match = text.match(/^(\s*)([^]*\S)(\s*)$/);
        node2.text = match[2];
        node2.raws.left = match[1];
        node2.raws.right = match[3];
      }
    };
    _proto.createTokenizer = function createTokenizer() {
      this.tokenizer = tokenizer23(this.input);
    };
    _proto.decl = function decl(tokens, customProperty) {
      var node2 = new Declaration$2();
      this.init(node2, tokens[0][2]);
      var last = tokens[tokens.length - 1];
      if (last[0] === ";") {
        this.semicolon = true;
        tokens.pop();
      }
      node2.source.end = this.getPosition(last[3] || last[2] || findLastWithPosition(tokens));
      node2.source.end.offset++;
      while (tokens[0][0] !== "word") {
        if (tokens.length === 1) this.unknownWord(tokens);
        node2.raws.before += tokens.shift()[1];
      }
      node2.source.start = this.getPosition(tokens[0][2]);
      node2.prop = "";
      while (tokens.length) {
        var type = tokens[0][0];
        if (type === ":" || type === "space" || type === "comment") {
          break;
        }
        node2.prop += tokens.shift()[1];
      }
      node2.raws.between = "";
      var token;
      while (tokens.length) {
        token = tokens.shift();
        if (token[0] === ":") {
          node2.raws.between += token[1];
          break;
        } else {
          if (token[0] === "word" && /\w/.test(token[1])) {
            this.unknownWord([
              token
            ]);
          }
          node2.raws.between += token[1];
        }
      }
      if (node2.prop[0] === "_" || node2.prop[0] === "*") {
        node2.raws.before += node2.prop[0];
        node2.prop = node2.prop.slice(1);
      }
      var firstSpaces = [];
      var next;
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        firstSpaces.push(tokens.shift());
      }
      this.precheckMissedSemicolon(tokens);
      for (var i3 = tokens.length - 1; i3 >= 0; i3--) {
        token = tokens[i3];
        if (token[1].toLowerCase() === "!important") {
          node2.important = true;
          var string = this.stringFrom(tokens, i3);
          string = this.spacesFromEnd(tokens) + string;
          if (string !== " !important") node2.raws.important = string;
          break;
        } else if (token[1].toLowerCase() === "important") {
          var cache = tokens.slice(0);
          var str = "";
          for (var j4 = i3; j4 > 0; j4--) {
            var type1 = cache[j4][0];
            if (str.trim().indexOf("!") === 0 && type1 !== "space") {
              break;
            }
            str = cache.pop()[1] + str;
          }
          if (str.trim().indexOf("!") === 0) {
            node2.important = true;
            node2.raws.important = str;
            tokens = cache;
          }
        }
        if (token[0] !== "space" && token[0] !== "comment") {
          break;
        }
      }
      var hasWord = tokens.some(function(i4) {
        return i4[0] !== "space" && i4[0] !== "comment";
      });
      if (hasWord) {
        node2.raws.between += firstSpaces.map(function(i4) {
          return i4[1];
        }).join("");
        firstSpaces = [];
      }
      this.raw(node2, "value", firstSpaces.concat(tokens), customProperty);
      if (node2.value.includes(":") && !customProperty) {
        this.checkMissedSemicolon(tokens);
      }
    };
    _proto.doubleColon = function doubleColon(token) {
      throw this.input.error("Double colon", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    _proto.emptyRule = function emptyRule(token) {
      var node2 = new Rule$2();
      this.init(node2, token[2]);
      node2.selector = "";
      node2.raws.between = "";
      this.current = node2;
    };
    _proto.end = function end(token) {
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.semicolon = false;
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.spaces = "";
      if (this.current.parent) {
        this.current.source.end = this.getPosition(token[2]);
        this.current.source.end.offset++;
        this.current = this.current.parent;
      } else {
        this.unexpectedClose(token);
      }
    };
    _proto.endFile = function endFile() {
      if (this.current.parent) this.unclosedBlock();
      if (this.current.nodes && this.current.nodes.length) {
        this.current.raws.semicolon = this.semicolon;
      }
      this.current.raws.after = (this.current.raws.after || "") + this.spaces;
      this.root.source.end = this.getPosition(this.tokenizer.position());
    };
    _proto.freeSemicolon = function freeSemicolon(token) {
      this.spaces += token[1];
      if (this.current.nodes) {
        var prev = this.current.nodes[this.current.nodes.length - 1];
        if (prev && prev.type === "rule" && !prev.raws.ownSemicolon) {
          prev.raws.ownSemicolon = this.spaces;
          this.spaces = "";
        }
      }
    };
    _proto.getPosition = function getPosition(offset) {
      var pos = this.input.fromOffset(offset);
      return {
        column: pos.col,
        line: pos.line,
        offset
      };
    };
    _proto.init = function init(node2, offset) {
      this.current.push(node2);
      node2.source = {
        input: this.input,
        start: this.getPosition(offset)
      };
      node2.raws.before = this.spaces;
      this.spaces = "";
      if (node2.type !== "comment") this.semicolon = false;
    };
    _proto.other = function other(start) {
      var end = false;
      var type = null;
      var colon = false;
      var bracket = null;
      var brackets = [];
      var customProperty = start[1].startsWith("--");
      var tokens = [];
      var token = start;
      while (token) {
        type = token[0];
        tokens.push(token);
        if (type === "(" || type === "[") {
          if (!bracket) bracket = token;
          brackets.push(type === "(" ? ")" : "]");
        } else if (customProperty && colon && type === "{") {
          if (!bracket) bracket = token;
          brackets.push("}");
        } else if (brackets.length === 0) {
          if (type === ";") {
            if (colon) {
              this.decl(tokens, customProperty);
              return;
            } else {
              break;
            }
          } else if (type === "{") {
            this.rule(tokens);
            return;
          } else if (type === "}") {
            this.tokenizer.back(tokens.pop());
            end = true;
            break;
          } else if (type === ":") {
            colon = true;
          }
        } else if (type === brackets[brackets.length - 1]) {
          brackets.pop();
          if (brackets.length === 0) bracket = null;
        }
        token = this.tokenizer.nextToken();
      }
      if (this.tokenizer.endOfFile()) end = true;
      if (brackets.length > 0) this.unclosedBracket(bracket);
      if (end && colon) {
        if (!customProperty) {
          while (tokens.length) {
            token = tokens[tokens.length - 1][0];
            if (token !== "space" && token !== "comment") break;
            this.tokenizer.back(tokens.pop());
          }
        }
        this.decl(tokens, customProperty);
      } else {
        this.unknownWord(tokens);
      }
    };
    _proto.parse = function parse3() {
      var token;
      while (!this.tokenizer.endOfFile()) {
        token = this.tokenizer.nextToken();
        switch (token[0]) {
          case "space":
            this.spaces += token[1];
            break;
          case ";":
            this.freeSemicolon(token);
            break;
          case "}":
            this.end(token);
            break;
          case "comment":
            this.comment(token);
            break;
          case "at-word":
            this.atrule(token);
            break;
          case "{":
            this.emptyRule(token);
            break;
          default:
            this.other(token);
            break;
        }
      }
      this.endFile();
    };
    _proto.precheckMissedSemicolon = function precheckMissedSemicolon() {
    };
    _proto.raw = function raw(node2, prop, tokens, customProperty) {
      var token, type;
      var length = tokens.length;
      var value = "";
      var clean = true;
      var next, prev;
      for (var i3 = 0; i3 < length; i3 += 1) {
        token = tokens[i3];
        type = token[0];
        if (type === "space" && i3 === length - 1 && !customProperty) {
          clean = false;
        } else if (type === "comment") {
          prev = tokens[i3 - 1] ? tokens[i3 - 1][0] : "empty";
          next = tokens[i3 + 1] ? tokens[i3 + 1][0] : "empty";
          if (!SAFE_COMMENT_NEIGHBOR[prev] && !SAFE_COMMENT_NEIGHBOR[next]) {
            if (value.slice(-1) === ",") {
              clean = false;
            } else {
              value += token[1];
            }
          } else {
            clean = false;
          }
        } else {
          value += token[1];
        }
      }
      if (!clean) {
        var raw2 = tokens.reduce(function(all, i4) {
          return all + i4[1];
        }, "");
        node2.raws[prop] = {
          raw: raw2,
          value
        };
      }
      node2[prop] = value;
    };
    _proto.rule = function rule2(tokens) {
      tokens.pop();
      var node2 = new Rule$2();
      this.init(node2, tokens[0][2]);
      node2.raws.between = this.spacesAndCommentsFromEnd(tokens);
      this.raw(node2, "selector", tokens);
      this.current = node2;
    };
    _proto.spacesAndCommentsFromEnd = function spacesAndCommentsFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space" && lastTokenType !== "comment") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.spacesAndCommentsFromStart = function spacesAndCommentsFromStart(tokens) {
      var next;
      var spaces = "";
      while (tokens.length) {
        next = tokens[0][0];
        if (next !== "space" && next !== "comment") break;
        spaces += tokens.shift()[1];
      }
      return spaces;
    };
    _proto.spacesFromEnd = function spacesFromEnd(tokens) {
      var lastTokenType;
      var spaces = "";
      while (tokens.length) {
        lastTokenType = tokens[tokens.length - 1][0];
        if (lastTokenType !== "space") break;
        spaces = tokens.pop()[1] + spaces;
      }
      return spaces;
    };
    _proto.stringFrom = function stringFrom(tokens, from) {
      var result2 = "";
      for (var i3 = from; i3 < tokens.length; i3++) {
        result2 += tokens[i3][1];
      }
      tokens.splice(from, tokens.length - from);
      return result2;
    };
    _proto.unclosedBlock = function unclosedBlock() {
      var pos = this.current.source.start;
      throw this.input.error("Unclosed block", pos.line, pos.column);
    };
    _proto.unclosedBracket = function unclosedBracket(bracket) {
      throw this.input.error("Unclosed bracket", {
        offset: bracket[2]
      }, {
        offset: bracket[2] + 1
      });
    };
    _proto.unexpectedClose = function unexpectedClose(token) {
      throw this.input.error("Unexpected }", {
        offset: token[2]
      }, {
        offset: token[2] + 1
      });
    };
    _proto.unknownWord = function unknownWord(tokens) {
      throw this.input.error("Unknown word", {
        offset: tokens[0][2]
      }, {
        offset: tokens[0][2] + tokens[0][1].length
      });
    };
    _proto.unnamedAtrule = function unnamedAtrule(node2, token) {
      throw this.input.error("At-rule without name", {
        offset: token[2]
      }, {
        offset: token[2] + token[1].length
      });
    };
    return Parser;
  })();
  var parser = Parser$1;
  var Container$2 = container;
  var Parser2 = parser;
  var Input$2 = input;
  function parse$3(css, opts) {
    var input2 = new Input$2(css, opts);
    var parser2 = new Parser2(input2);
    try {
      parser2.parse();
    } catch (e3) {
      if (true) {
        if (e3.name === "CssSyntaxError" && opts && opts.from) {
          if (/\.scss$/i.test(opts.from)) {
            e3.message += "\nYou tried to parse SCSS with the standard CSS parser; try again with the postcss-scss parser";
          } else if (/\.sass/i.test(opts.from)) {
            e3.message += "\nYou tried to parse Sass with the standard CSS parser; try again with the postcss-sass parser";
          } else if (/\.less$/i.test(opts.from)) {
            e3.message += "\nYou tried to parse Less with the standard CSS parser; try again with the postcss-less parser";
          }
        }
      }
      throw e3;
    }
    return parser2.root;
  }
  var parse_1 = parse$3;
  parse$3.default = parse$3;
  Container$2.registerParse(parse$3);
  var isClean = symbols.isClean;
  var my = symbols.my;
  var MapGenerator$1 = mapGenerator;
  var stringify$2 = stringify_1;
  var Container$1 = container;
  var Document$2 = document$1$3;
  var warnOnce$1 = warnOnce$2;
  var Result$2 = result;
  var parse$2 = parse_1;
  var Root$3 = root;
  var TYPE_TO_CLASS_NAME = {
    atrule: "AtRule",
    comment: "Comment",
    decl: "Declaration",
    document: "Document",
    root: "Root",
    rule: "Rule"
  };
  var PLUGIN_PROPS = {
    AtRule: true,
    AtRuleExit: true,
    Comment: true,
    CommentExit: true,
    Declaration: true,
    DeclarationExit: true,
    Document: true,
    DocumentExit: true,
    Once: true,
    OnceExit: true,
    postcssPlugin: true,
    prepare: true,
    Root: true,
    RootExit: true,
    Rule: true,
    RuleExit: true
  };
  var NOT_VISITORS = {
    Once: true,
    postcssPlugin: true,
    prepare: true
  };
  var CHILDREN = 0;
  function isPromise(obj) {
    return (typeof obj === "undefined" ? "undefined" : _type_of(obj)) === "object" && typeof obj.then === "function";
  }
  function getEvents(node2) {
    var key = false;
    var type = TYPE_TO_CLASS_NAME[node2.type];
    if (node2.type === "decl") {
      key = node2.prop.toLowerCase();
    } else if (node2.type === "atrule") {
      key = node2.name.toLowerCase();
    }
    if (key && node2.append) {
      return [
        type,
        type + "-" + key,
        CHILDREN,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (key) {
      return [
        type,
        type + "-" + key,
        type + "Exit",
        type + "Exit-" + key
      ];
    } else if (node2.append) {
      return [
        type,
        CHILDREN,
        type + "Exit"
      ];
    } else {
      return [
        type,
        type + "Exit"
      ];
    }
  }
  function toStack(node2) {
    var events;
    if (node2.type === "document") {
      events = [
        "Document",
        CHILDREN,
        "DocumentExit"
      ];
    } else if (node2.type === "root") {
      events = [
        "Root",
        CHILDREN,
        "RootExit"
      ];
    } else {
      events = getEvents(node2);
    }
    return {
      eventIndex: 0,
      events,
      iterator: 0,
      node: node2,
      visitorIndex: 0,
      visitors: []
    };
  }
  function cleanMarks(node2) {
    node2[isClean] = false;
    if (node2.nodes) node2.nodes.forEach(function(i3) {
      return cleanMarks(i3);
    });
    return node2;
  }
  var postcss$2 = {};
  var LazyResult$2 = /* @__PURE__ */ (function() {
    function LazyResult(processor2, css, opts) {
      var _this = this;
      this.stringified = false;
      this.processed = false;
      var root2;
      if ((typeof css === "undefined" ? "undefined" : _type_of(css)) === "object" && css !== null && (css.type === "root" || css.type === "document")) {
        root2 = cleanMarks(css);
      } else if (_instanceof(css, LazyResult) || _instanceof(css, Result$2)) {
        root2 = cleanMarks(css.root);
        if (css.map) {
          if (typeof opts.map === "undefined") opts.map = {};
          if (!opts.map.inline) opts.map.inline = false;
          opts.map.prev = css.map;
        }
      } else {
        var parser2 = parse$2;
        if (opts.syntax) parser2 = opts.syntax.parse;
        if (opts.parser) parser2 = opts.parser;
        if (parser2.parse) parser2 = parser2.parse;
        try {
          root2 = parser2(css, opts);
        } catch (error) {
          this.processed = true;
          this.error = error;
        }
        if (root2 && !root2[my]) {
          Container$1.rebuild(root2);
        }
      }
      this.result = new Result$2(processor2, root2, opts);
      this.helpers = _extends({}, postcss$2, {
        postcss: postcss$2,
        result: this.result
      });
      this.plugins = this.processor.plugins.map(function(plugin22) {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.prepare) {
          return _extends({}, plugin22, plugin22.prepare(_this.result));
        } else {
          return plugin22;
        }
      });
    }
    var _proto = LazyResult.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      if (this.processed) return Promise.resolve(this.result);
      if (!this.processing) {
        this.processing = this.runAsync();
      }
      return this.processing;
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.getAsyncError = function getAsyncError() {
      throw new Error("Use process(css).then(cb) to work with async plugins");
    };
    _proto.handleError = function handleError2(error, node2) {
      var plugin22 = this.result.lastPlugin;
      try {
        if (node2) node2.addToError(error);
        this.error = error;
        if (error.name === "CssSyntaxError" && !error.plugin) {
          error.plugin = plugin22.postcssPlugin;
          error.setMessage();
        } else if (plugin22.postcssVersion) {
          if (true) {
            var pluginName = plugin22.postcssPlugin;
            var pluginVer = plugin22.postcssVersion;
            var runtimeVer = this.result.processor.version;
            var a3 = pluginVer.split(".");
            var b2 = runtimeVer.split(".");
            if (a3[0] !== b2[0] || parseInt(a3[1]) > parseInt(b2[1])) {
              console.error("Unknown error from PostCSS plugin. Your current PostCSS version is " + runtimeVer + ", but " + pluginName + " uses " + pluginVer + ". Perhaps this is the source of the error below.");
            }
          }
        }
      } catch (err) {
        if (console && console.error) console.error(err);
      }
      return error;
    };
    _proto.prepareVisitors = function prepareVisitors() {
      var _this = this;
      this.listeners = {};
      var add = function(plugin23, type, cb) {
        if (!_this.listeners[type]) _this.listeners[type] = [];
        _this.listeners[type].push([
          plugin23,
          cb
        ]);
      };
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object") {
          for (var event in plugin22) {
            if (!PLUGIN_PROPS[event] && /^[A-Z]/.test(event)) {
              throw new Error("Unknown event " + event + " in " + plugin22.postcssPlugin + ". Try to update PostCSS (" + this.processor.version + " now).");
            }
            if (!NOT_VISITORS[event]) {
              if (_type_of(plugin22[event]) === "object") {
                for (var filter in plugin22[event]) {
                  if (filter === "*") {
                    add(plugin22, event, plugin22[event][filter]);
                  } else {
                    add(plugin22, event + "-" + filter.toLowerCase(), plugin22[event][filter]);
                  }
                }
              } else if (typeof plugin22[event] === "function") {
                add(plugin22, event, plugin22[event]);
              }
            }
          }
        }
      }
      this.hasListener = Object.keys(this.listeners).length > 0;
    };
    _proto.runAsync = function runAsync() {
      var _this = this;
      return _async_to_generator(function() {
        var i3, plugin22, promise, error, root2, stack, promise1, e3, node2, _loop, _iterator, _step;
        return _ts_generator(this, function(_state) {
          switch (_state.label) {
            case 0:
              _this.plugin = 0;
              i3 = 0;
              _state.label = 1;
            case 1:
              if (!(i3 < _this.plugins.length)) return [
                3,
                6
              ];
              plugin22 = _this.plugins[i3];
              promise = _this.runOnRoot(plugin22);
              if (!isPromise(promise)) return [
                3,
                5
              ];
              _state.label = 2;
            case 2:
              _state.trys.push([
                2,
                4,
                ,
                5
              ]);
              return [
                4,
                promise
              ];
            case 3:
              _state.sent();
              return [
                3,
                5
              ];
            case 4:
              error = _state.sent();
              throw _this.handleError(error);
            case 5:
              i3++;
              return [
                3,
                1
              ];
            case 6:
              _this.prepareVisitors();
              if (!_this.hasListener) return [
                3,
                18
              ];
              root2 = _this.result.root;
              _state.label = 7;
            case 7:
              if (!!root2[isClean]) return [
                3,
                14
              ];
              root2[isClean] = true;
              stack = [
                toStack(root2)
              ];
              _state.label = 8;
            case 8:
              if (!(stack.length > 0)) return [
                3,
                13
              ];
              promise1 = _this.visitTick(stack);
              if (!isPromise(promise1)) return [
                3,
                12
              ];
              _state.label = 9;
            case 9:
              _state.trys.push([
                9,
                11,
                ,
                12
              ]);
              return [
                4,
                promise1
              ];
            case 10:
              _state.sent();
              return [
                3,
                12
              ];
            case 11:
              e3 = _state.sent();
              node2 = stack[stack.length - 1].node;
              throw _this.handleError(e3, node2);
            case 12:
              return [
                3,
                8
              ];
            case 13:
              return [
                3,
                7
              ];
            case 14:
              if (!_this.listeners.OnceExit) return [
                3,
                18
              ];
              _loop = function() {
                var _step_value, plugin23, visitor, roots, e4;
                return _ts_generator(this, function(_state2) {
                  switch (_state2.label) {
                    case 0:
                      _step_value = _step.value, plugin23 = _step_value[0], visitor = _step_value[1];
                      _this.result.lastPlugin = plugin23;
                      _state2.label = 1;
                    case 1:
                      _state2.trys.push([
                        1,
                        6,
                        ,
                        7
                      ]);
                      if (!(root2.type === "document")) return [
                        3,
                        3
                      ];
                      roots = root2.nodes.map(function(subRoot) {
                        return visitor(subRoot, _this.helpers);
                      });
                      return [
                        4,
                        Promise.all(roots)
                      ];
                    case 2:
                      _state2.sent();
                      return [
                        3,
                        5
                      ];
                    case 3:
                      return [
                        4,
                        visitor(root2, _this.helpers)
                      ];
                    case 4:
                      _state2.sent();
                      _state2.label = 5;
                    case 5:
                      return [
                        3,
                        7
                      ];
                    case 6:
                      e4 = _state2.sent();
                      throw _this.handleError(e4);
                    case 7:
                      return [
                        2
                      ];
                  }
                });
              };
              _iterator = _create_for_of_iterator_helper_loose(_this.listeners.OnceExit);
              _state.label = 15;
            case 15:
              if (!!(_step = _iterator()).done) return [
                3,
                18
              ];
              return [
                5,
                _ts_values(_loop())
              ];
            case 16:
              _state.sent();
              _state.label = 17;
            case 17:
              return [
                3,
                15
              ];
            case 18:
              _this.processed = true;
              return [
                2,
                _this.stringify()
              ];
          }
        });
      })();
    };
    _proto.runOnRoot = function runOnRoot(plugin22) {
      var _this = this;
      this.result.lastPlugin = plugin22;
      try {
        if ((typeof plugin22 === "undefined" ? "undefined" : _type_of(plugin22)) === "object" && plugin22.Once) {
          if (this.result.root.type === "document") {
            var roots = this.result.root.nodes.map(function(root2) {
              return plugin22.Once(root2, _this.helpers);
            });
            if (isPromise(roots[0])) {
              return Promise.all(roots);
            }
            return roots;
          }
          return plugin22.Once(this.result.root, this.helpers);
        } else if (typeof plugin22 === "function") {
          return plugin22(this.result.root, this.result);
        }
      } catch (error) {
        throw this.handleError(error);
      }
    };
    _proto.stringify = function stringify2() {
      if (this.error) throw this.error;
      if (this.stringified) return this.result;
      this.stringified = true;
      this.sync();
      var opts = this.result.opts;
      var str = stringify$2;
      if (opts.syntax) str = opts.syntax.stringify;
      if (opts.stringifier) str = opts.stringifier;
      if (str.stringify) str = str.stringify;
      var map = new MapGenerator$1(str, this.result.root, this.result.opts);
      var data = map.generate();
      this.result.css = data[0];
      this.result.map = data[1];
      return this.result;
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      if (this.processed) return this.result;
      this.processed = true;
      if (this.processing) {
        throw this.getAsyncError();
      }
      for (var _iterator = _create_for_of_iterator_helper_loose(this.plugins), _step; !(_step = _iterator()).done; ) {
        var plugin22 = _step.value;
        var promise = this.runOnRoot(plugin22);
        if (isPromise(promise)) {
          throw this.getAsyncError();
        }
      }
      this.prepareVisitors();
      if (this.hasListener) {
        var root2 = this.result.root;
        while (!root2[isClean]) {
          root2[isClean] = true;
          this.walkSync(root2);
        }
        if (this.listeners.OnceExit) {
          if (root2.type === "document") {
            for (var _iterator1 = _create_for_of_iterator_helper_loose(root2.nodes), _step1; !(_step1 = _iterator1()).done; ) {
              var subRoot = _step1.value;
              this.visitSync(this.listeners.OnceExit, subRoot);
            }
          } else {
            this.visitSync(this.listeners.OnceExit, root2);
          }
        }
      }
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this.opts)) {
          warnOnce$1("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this.css;
    };
    _proto.visitSync = function visitSync(visitors, node2) {
      for (var _iterator = _create_for_of_iterator_helper_loose(visitors), _step; !(_step = _iterator()).done; ) {
        var _step_value = _step.value, plugin22 = _step_value[0], visitor = _step_value[1];
        this.result.lastPlugin = plugin22;
        var promise = void 0;
        try {
          promise = visitor(node2, this.helpers);
        } catch (e3) {
          throw this.handleError(e3, node2.proxyOf);
        }
        if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
          return true;
        }
        if (isPromise(promise)) {
          throw this.getAsyncError();
        }
      }
    };
    _proto.visitTick = function visitTick(stack) {
      var visit2 = stack[stack.length - 1];
      var node2 = visit2.node, visitors = visit2.visitors;
      if (node2.type !== "root" && node2.type !== "document" && !node2.parent) {
        stack.pop();
        return;
      }
      if (visitors.length > 0 && visit2.visitorIndex < visitors.length) {
        var _visitors_visit2_visitorIndex = visitors[visit2.visitorIndex], plugin22 = _visitors_visit2_visitorIndex[0], visitor = _visitors_visit2_visitorIndex[1];
        visit2.visitorIndex += 1;
        if (visit2.visitorIndex === visitors.length) {
          visit2.visitors = [];
          visit2.visitorIndex = 0;
        }
        this.result.lastPlugin = plugin22;
        try {
          return visitor(node2.toProxy(), this.helpers);
        } catch (e3) {
          throw this.handleError(e3, node2);
        }
      }
      if (visit2.iterator !== 0) {
        var iterator = visit2.iterator;
        var child;
        while (child = node2.nodes[node2.indexes[iterator]]) {
          node2.indexes[iterator] += 1;
          if (!child[isClean]) {
            child[isClean] = true;
            stack.push(toStack(child));
            return;
          }
        }
        visit2.iterator = 0;
        delete node2.indexes[iterator];
      }
      var events = visit2.events;
      while (visit2.eventIndex < events.length) {
        var event = events[visit2.eventIndex];
        visit2.eventIndex += 1;
        if (event === CHILDREN) {
          if (node2.nodes && node2.nodes.length) {
            node2[isClean] = true;
            visit2.iterator = node2.getIterator();
          }
          return;
        } else if (this.listeners[event]) {
          visit2.visitors = this.listeners[event];
          return;
        }
      }
      stack.pop();
    };
    _proto.walkSync = function walkSync(node2) {
      var _this = this;
      node2[isClean] = true;
      var events = getEvents(node2);
      for (var _iterator = _create_for_of_iterator_helper_loose(events), _step; !(_step = _iterator()).done; ) {
        var event = _step.value;
        if (event === CHILDREN) {
          if (node2.nodes) {
            node2.each(function(child) {
              if (!child[isClean]) _this.walkSync(child);
            });
          }
        } else {
          var visitors = this.listeners[event];
          if (visitors) {
            if (this.visitSync(visitors, node2.toProxy())) return;
          }
        }
      }
    };
    _proto.warnings = function warnings() {
      return this.sync().warnings();
    };
    _create_class(LazyResult, [
      {
        key: "content",
        get: function get() {
          return this.stringify().content;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.stringify().css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.stringify().map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return this.sync().messages;
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          return this.sync().root;
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "LazyResult";
        }
      }
    ]);
    return LazyResult;
  })();
  LazyResult$2.registerPostcss = function(dependant) {
    postcss$2 = dependant;
  };
  var lazyResult = LazyResult$2;
  LazyResult$2.default = LazyResult$2;
  Root$3.registerLazyResult(LazyResult$2);
  Document$2.registerLazyResult(LazyResult$2);
  var MapGenerator2 = mapGenerator;
  var stringify$1 = stringify_1;
  var warnOnce23 = warnOnce$2;
  var parse$1 = parse_1;
  var Result$1 = result;
  var NoWorkResult$1 = /* @__PURE__ */ (function() {
    function NoWorkResult(processor2, css, opts) {
      css = css.toString();
      this.stringified = false;
      this._processor = processor2;
      this._css = css;
      this._opts = opts;
      this._map = void 0;
      var root2;
      var str = stringify$1;
      this.result = new Result$1(this._processor, root2, this._opts);
      this.result.css = css;
      var self2 = this;
      Object.defineProperty(this.result, "root", {
        get: function get() {
          return self2.root;
        }
      });
      var map = new MapGenerator2(str, root2, this._opts, css);
      if (map.isMap()) {
        var _map_generate = map.generate(), generatedCSS = _map_generate[0], generatedMap = _map_generate[1];
        if (generatedCSS) {
          this.result.css = generatedCSS;
        }
        if (generatedMap) {
          this.result.map = generatedMap;
        }
      } else {
        map.clearAnnotation();
        this.result.css = map.css;
      }
    }
    var _proto = NoWorkResult.prototype;
    _proto.async = function async() {
      if (this.error) return Promise.reject(this.error);
      return Promise.resolve(this.result);
    };
    _proto.catch = function _catch(onRejected) {
      return this.async().catch(onRejected);
    };
    _proto.finally = function _finally(onFinally) {
      return this.async().then(onFinally, onFinally);
    };
    _proto.sync = function sync() {
      if (this.error) throw this.error;
      return this.result;
    };
    _proto.then = function then(onFulfilled, onRejected) {
      if (true) {
        if (!("from" in this._opts)) {
          warnOnce23("Without `from` option PostCSS could generate wrong source map and will not find Browserslist config. Set it to CSS file path or to `undefined` to prevent this warning.");
        }
      }
      return this.async().then(onFulfilled, onRejected);
    };
    _proto.toString = function toString2() {
      return this._css;
    };
    _proto.warnings = function warnings() {
      return [];
    };
    _create_class(NoWorkResult, [
      {
        key: "content",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "css",
        get: function get() {
          return this.result.css;
        }
      },
      {
        key: "map",
        get: function get() {
          return this.result.map;
        }
      },
      {
        key: "messages",
        get: function get() {
          return [];
        }
      },
      {
        key: "opts",
        get: function get() {
          return this.result.opts;
        }
      },
      {
        key: "processor",
        get: function get() {
          return this.result.processor;
        }
      },
      {
        key: "root",
        get: function get() {
          if (this._root) {
            return this._root;
          }
          var root2;
          var parser2 = parse$1;
          try {
            root2 = parser2(this._css, this._opts);
          } catch (error) {
            this.error = error;
          }
          if (this.error) {
            throw this.error;
          } else {
            this._root = root2;
            return root2;
          }
        }
      },
      {
        key: Symbol.toStringTag,
        get: function get() {
          return "NoWorkResult";
        }
      }
    ]);
    return NoWorkResult;
  })();
  var noWorkResult = NoWorkResult$1;
  NoWorkResult$1.default = NoWorkResult$1;
  var NoWorkResult2 = noWorkResult;
  var LazyResult$1 = lazyResult;
  var Document$1 = document$1$3;
  var Root$2 = root;
  var Processor$1 = /* @__PURE__ */ (function() {
    function Processor(plugins) {
      if (plugins === void 0) plugins = [];
      this.version = "8.4.38";
      this.plugins = this.normalize(plugins);
    }
    var _proto = Processor.prototype;
    _proto.normalize = function normalize(plugins) {
      var normalized = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(plugins), _step; !(_step = _iterator()).done; ) {
        var i3 = _step.value;
        if (i3.postcss === true) {
          i3 = i3();
        } else if (i3.postcss) {
          i3 = i3.postcss;
        }
        if ((typeof i3 === "undefined" ? "undefined" : _type_of(i3)) === "object" && Array.isArray(i3.plugins)) {
          normalized = normalized.concat(i3.plugins);
        } else if ((typeof i3 === "undefined" ? "undefined" : _type_of(i3)) === "object" && i3.postcssPlugin) {
          normalized.push(i3);
        } else if (typeof i3 === "function") {
          normalized.push(i3);
        } else if ((typeof i3 === "undefined" ? "undefined" : _type_of(i3)) === "object" && (i3.parse || i3.stringify)) {
          if (true) {
            throw new Error("PostCSS syntaxes cannot be used as plugins. Instead, please use one of the syntax/parser/stringifier options as outlined in your PostCSS runner documentation.");
          }
        } else {
          throw new Error(i3 + " is not a PostCSS plugin");
        }
      }
      return normalized;
    };
    _proto.process = function process1(css, opts) {
      if (opts === void 0) opts = {};
      if (!this.plugins.length && !opts.parser && !opts.stringifier && !opts.syntax) {
        return new NoWorkResult2(this, css, opts);
      } else {
        return new LazyResult$1(this, css, opts);
      }
    };
    _proto.use = function use(plugin22) {
      this.plugins = this.plugins.concat(this.normalize([
        plugin22
      ]));
      return this;
    };
    return Processor;
  })();
  var processor = Processor$1;
  Processor$1.default = Processor$1;
  Root$2.registerProcessor(Processor$1);
  Document$1.registerProcessor(Processor$1);
  var Declaration$1 = declaration;
  var PreviousMap2 = previousMap;
  var Comment$1 = comment;
  var AtRule$1 = atRule;
  var Input$1 = input;
  var Root$1 = root;
  var Rule$1 = rule;
  function fromJSON$1(json, inputs) {
    if (Array.isArray(json)) return json.map(function(n3) {
      return fromJSON$1(n3);
    });
    var ownInputs = json.inputs, defaults = _object_without_properties_loose(json, [
      "inputs"
    ]);
    if (ownInputs) {
      inputs = [];
      for (var _iterator = _create_for_of_iterator_helper_loose(ownInputs), _step; !(_step = _iterator()).done; ) {
        var input2 = _step.value;
        var inputHydrated = _extends({}, input2, {
          __proto__: Input$1.prototype
        });
        if (inputHydrated.map) {
          inputHydrated.map = _extends({}, inputHydrated.map, {
            __proto__: PreviousMap2.prototype
          });
        }
        inputs.push(inputHydrated);
      }
    }
    if (defaults.nodes) {
      defaults.nodes = json.nodes.map(function(n3) {
        return fromJSON$1(n3, inputs);
      });
    }
    if (defaults.source) {
      var _defaults_source = defaults.source, inputId = _defaults_source.inputId, source = _object_without_properties_loose(_defaults_source, [
        "inputId"
      ]);
      defaults.source = source;
      if (inputId != null) {
        defaults.source.input = inputs[inputId];
      }
    }
    if (defaults.type === "root") {
      return new Root$1(defaults);
    } else if (defaults.type === "decl") {
      return new Declaration$1(defaults);
    } else if (defaults.type === "rule") {
      return new Rule$1(defaults);
    } else if (defaults.type === "comment") {
      return new Comment$1(defaults);
    } else if (defaults.type === "atrule") {
      return new AtRule$1(defaults);
    } else {
      throw new Error("Unknown node type: " + json.type);
    }
  }
  var fromJSON_1 = fromJSON$1;
  fromJSON$1.default = fromJSON$1;
  var CssSyntaxError2 = cssSyntaxError;
  var Declaration2 = declaration;
  var LazyResult2 = lazyResult;
  var Container2 = container;
  var Processor2 = processor;
  var stringify$5 = stringify_1;
  var fromJSON = fromJSON_1;
  var Document2 = document$1$3;
  var Warning2 = warning;
  var Comment2 = comment;
  var AtRule2 = atRule;
  var Result2 = result;
  var Input2 = input;
  var parse = parse_1;
  var list = list_1;
  var Rule2 = rule;
  var Root2 = root;
  var Node2 = node;
  function postcss() {
    for (var _len = arguments.length, plugins = new Array(_len), _key = 0; _key < _len; _key++) {
      plugins[_key] = arguments[_key];
    }
    if (plugins.length === 1 && Array.isArray(plugins[0])) {
      plugins = plugins[0];
    }
    return new Processor2(plugins);
  }
  postcss.plugin = function plugin3(name5, initializer) {
    var warningPrinted = false;
    function creator() {
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      if (console && console.warn && !warningPrinted) {
        warningPrinted = true;
        console.warn(name5 + ": postcss.plugin was deprecated. Migration guide:\nhttps://evilmartians.com/chronicles/postcss-8-plugin-migration");
        if (process.env.LANG && process.env.LANG.startsWith("cn")) {
          console.warn(name5 + ": \u91CC\u9762 postcss.plugin \u88AB\u5F03\u7528. \u8FC1\u79FB\u6307\u5357:\nhttps://www.w3ctech.com/topic/2226");
        }
      }
      var transformer = initializer.apply(void 0, [].concat(args));
      transformer.postcssPlugin = name5;
      transformer.postcssVersion = new Processor2().version;
      return transformer;
    }
    var cache;
    Object.defineProperty(creator, "postcss", {
      get: function get() {
        if (!cache) cache = creator();
        return cache;
      }
    });
    creator.process = function(css, processOpts, pluginOpts) {
      return postcss([
        creator(pluginOpts)
      ]).process(css, processOpts);
    };
    return creator;
  };
  postcss.stringify = stringify$5;
  postcss.parse = parse;
  postcss.fromJSON = fromJSON;
  postcss.list = list;
  postcss.comment = function(defaults) {
    return new Comment2(defaults);
  };
  postcss.atRule = function(defaults) {
    return new AtRule2(defaults);
  };
  postcss.decl = function(defaults) {
    return new Declaration2(defaults);
  };
  postcss.rule = function(defaults) {
    return new Rule2(defaults);
  };
  postcss.root = function(defaults) {
    return new Root2(defaults);
  };
  postcss.document = function(defaults) {
    return new Document2(defaults);
  };
  postcss.CssSyntaxError = CssSyntaxError2;
  postcss.Declaration = Declaration2;
  postcss.Container = Container2;
  postcss.Processor = Processor2;
  postcss.Document = Document2;
  postcss.Comment = Comment2;
  postcss.Warning = Warning2;
  postcss.AtRule = AtRule2;
  postcss.Result = Result2;
  postcss.Input = Input2;
  postcss.Rule = Rule2;
  postcss.Root = Root2;
  postcss.Node = Node2;
  LazyResult2.registerPostcss(postcss);
  var postcss_1 = postcss;
  postcss.default = postcss;
  var postcss$1 = /* @__PURE__ */ getDefaultExportFromCjs(postcss_1);
  postcss$1.stringify;
  postcss$1.fromJSON;
  postcss$1.plugin;
  postcss$1.parse;
  postcss$1.list;
  postcss$1.document;
  postcss$1.comment;
  postcss$1.atRule;
  postcss$1.rule;
  postcss$1.decl;
  postcss$1.root;
  postcss$1.CssSyntaxError;
  postcss$1.Declaration;
  postcss$1.Container;
  postcss$1.Processor;
  postcss$1.Document;
  postcss$1.Comment;
  postcss$1.Warning;
  postcss$1.AtRule;
  postcss$1.Result;
  postcss$1.Input;
  postcss$1.Rule;
  postcss$1.Root;
  postcss$1.Node;
  var __defProp = Object.defineProperty;
  var __defNormalProp = function(obj, key, value) {
    return key in obj ? __defProp(obj, key, {
      enumerable: true,
      configurable: true,
      writable: true,
      value
    }) : obj[key] = value;
  };
  var __publicField = function(obj, key, value) {
    return __defNormalProp(obj, (typeof key === "undefined" ? "undefined" : _type_of(key)) !== "symbol" ? key + "" : key, value);
  };
  function patch(source, name5, replacement) {
    try {
      if (!(name5 in source)) {
        return function() {
        };
      }
      var original = source[name5];
      var wrapped = replacement(original);
      if (typeof wrapped === "function") {
        wrapped.prototype = wrapped.prototype || {};
        Object.defineProperties(wrapped, {
          __rrweb_original__: {
            enumerable: false,
            value: original
          }
        });
      }
      source[name5] = wrapped;
      return function() {
        source[name5] = original;
      };
    } catch (e3) {
      return function() {
      };
    }
  }
  var StackFrame = /* @__PURE__ */ (function() {
    function StackFrame2(obj) {
      __publicField(this, "fileName");
      __publicField(this, "functionName");
      __publicField(this, "lineNumber");
      __publicField(this, "columnNumber");
      this.fileName = obj.fileName || "";
      this.functionName = obj.functionName || "";
      this.lineNumber = obj.lineNumber;
      this.columnNumber = obj.columnNumber;
    }
    var _proto = StackFrame2.prototype;
    _proto.toString = function toString2() {
      var lineNumber = this.lineNumber || "";
      var columnNumber = this.columnNumber || "";
      if (this.functionName) return this.functionName + " (" + this.fileName + ":" + lineNumber + ":" + columnNumber + ")";
      return this.fileName + ":" + lineNumber + ":" + columnNumber;
    };
    return StackFrame2;
  })();
  var FIREFOX_SAFARI_STACK_REGEXP = /(^|@)\S+:\d+/;
  var CHROME_IE_STACK_REGEXP = /^\s*at .*(\S+:\d+|\(native\))/m;
  var SAFARI_NATIVE_CODE_REGEXP = /^(eval@)?(\[native code])?$/;
  var ErrorStackParser = {
    /**
    * Given an Error object, extract the most information from it.
    */
    parse: function parse2(error) {
      if (!error) {
        return [];
      }
      if (
        // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        typeof error.stacktrace !== "undefined" || // eslint-disable-next-line @typescript-eslint/ban-ts-comment
        // @ts-ignore
        typeof error["opera#sourceloc"] !== "undefined"
      ) {
        return this.parseOpera(error);
      } else if (error.stack && error.stack.match(CHROME_IE_STACK_REGEXP)) {
        return this.parseV8OrIE(error);
      } else if (error.stack) {
        return this.parseFFOrSafari(error);
      } else {
        console.warn("[console-record-plugin]: Failed to parse error object:", error);
        return [];
      }
    },
    // Separate line and column numbers from a string of the form: (URI:Line:Column)
    extractLocation: function extractLocation(urlLike) {
      if (urlLike.indexOf(":") === -1) {
        return [
          urlLike
        ];
      }
      var regExp = /(.+?)(?::(\d+))?(?::(\d+))?$/;
      var parts = regExp.exec(urlLike.replace(/[()]/g, ""));
      if (!parts) throw new Error("Cannot parse given url: " + urlLike);
      return [
        parts[1],
        parts[2] || void 0,
        parts[3] || void 0
      ];
    },
    parseV8OrIE: function parseV8OrIE(error) {
      var filtered = error.stack.split("\n").filter(function(line) {
        return !!line.match(CHROME_IE_STACK_REGEXP);
      }, this);
      return filtered.map(function(line) {
        if (line.indexOf("(eval ") > -1) {
          line = line.replace(/eval code/g, "eval").replace(/(\(eval at [^()]*)|(\),.*$)/g, "");
        }
        var sanitizedLine = line.replace(/^\s+/, "").replace(/\(eval code/g, "(");
        var location = sanitizedLine.match(/ (\((.+):(\d+):(\d+)\)$)/);
        sanitizedLine = location ? sanitizedLine.replace(location[0], "") : sanitizedLine;
        var tokens = sanitizedLine.split(/\s+/).slice(1);
        var locationParts = this.extractLocation(location ? location[1] : tokens.pop());
        var functionName = tokens.join(" ") || void 0;
        var fileName = [
          "eval",
          "<anonymous>"
        ].indexOf(locationParts[0]) > -1 ? void 0 : locationParts[0];
        return new StackFrame({
          functionName,
          fileName,
          lineNumber: locationParts[1],
          columnNumber: locationParts[2]
        });
      }, this);
    },
    parseFFOrSafari: function parseFFOrSafari(error) {
      var filtered = error.stack.split("\n").filter(function(line) {
        return !line.match(SAFARI_NATIVE_CODE_REGEXP);
      }, this);
      return filtered.map(function(line) {
        if (line.indexOf(" > eval") > -1) {
          line = line.replace(/ line (\d+)(?: > eval line \d+)* > eval:\d+:\d+/g, ":$1");
        }
        if (line.indexOf("@") === -1 && line.indexOf(":") === -1) {
          return new StackFrame({
            functionName: line
          });
        } else {
          var functionNameRegex = /((.*".+"[^@]*)?[^@]*)(?:@)/;
          var matches = line.match(functionNameRegex);
          var functionName = matches && matches[1] ? matches[1] : void 0;
          var locationParts = this.extractLocation(line.replace(functionNameRegex, ""));
          return new StackFrame({
            functionName,
            fileName: locationParts[0],
            lineNumber: locationParts[1],
            columnNumber: locationParts[2]
          });
        }
      }, this);
    },
    parseOpera: function parseOpera(e3) {
      if (!e3.stacktrace || e3.message.indexOf("\n") > -1 && e3.message.split("\n").length > e3.stacktrace.split("\n").length) {
        return this.parseOpera9(e3);
      } else if (!e3.stack) {
        return this.parseOpera10(e3);
      } else {
        return this.parseOpera11(e3);
      }
    },
    parseOpera9: function parseOpera9(e3) {
      var lineRE = /Line (\d+).*script (?:in )?(\S+)/i;
      var lines = e3.message.split("\n");
      var result2 = [];
      for (var i3 = 2, len = lines.length; i3 < len; i3 += 2) {
        var match = lineRE.exec(lines[i3]);
        if (match) {
          result2.push(new StackFrame({
            fileName: match[2],
            lineNumber: parseFloat(match[1])
          }));
        }
      }
      return result2;
    },
    parseOpera10: function parseOpera10(e3) {
      var lineRE = /Line (\d+).*script (?:in )?(\S+)(?:: In function (\S+))?$/i;
      var lines = e3.stacktrace.split("\n");
      var result2 = [];
      for (var i3 = 0, len = lines.length; i3 < len; i3 += 2) {
        var match = lineRE.exec(lines[i3]);
        if (match) {
          result2.push(new StackFrame({
            functionName: match[3] || void 0,
            fileName: match[2],
            lineNumber: parseFloat(match[1])
          }));
        }
      }
      return result2;
    },
    // Opera 10.65+ Error.stack very similar to FF/Safari
    parseOpera11: function parseOpera11(error) {
      var filtered = error.stack.split("\n").filter(function(line) {
        return !!line.match(FIREFOX_SAFARI_STACK_REGEXP) && !line.match(/^Error created at/);
      }, this);
      return filtered.map(function(line) {
        var tokens = line.split("@");
        var locationParts = this.extractLocation(tokens.pop());
        var functionCall = tokens.shift() || "";
        var functionName = functionCall.replace(/<anonymous function(: (\w+))?>/, "$2").replace(/\([^)]*\)/g, "") || void 0;
        return new StackFrame({
          functionName,
          fileName: locationParts[0],
          lineNumber: locationParts[1],
          columnNumber: locationParts[2]
        });
      }, this);
    }
  };
  function pathToSelector(node2) {
    if (!node2 || !node2.outerHTML) {
      return "";
    }
    var path = "";
    while (node2.parentElement) {
      var name5 = node2.localName;
      if (!name5) {
        break;
      }
      name5 = name5.toLowerCase();
      var parent = node2.parentElement;
      var domSiblings = [];
      if (parent.children && parent.children.length > 0) {
        for (var i3 = 0; i3 < parent.children.length; i3++) {
          var sibling = parent.children[i3];
          if (sibling.localName && sibling.localName.toLowerCase) {
            if (sibling.localName.toLowerCase() === name5) {
              domSiblings.push(sibling);
            }
          }
        }
      }
      if (domSiblings.length > 1) {
        name5 += ":eq(" + domSiblings.indexOf(node2) + ")";
      }
      path = name5 + (path ? ">" + path : "");
      node2 = parent;
    }
    return path;
  }
  function isObject(obj) {
    return Object.prototype.toString.call(obj) === "[object Object]";
  }
  function isObjTooDeep(obj, limit) {
    if (limit === 0) {
      return true;
    }
    var keys = Object.keys(obj);
    for (var _iterator = _create_for_of_iterator_helper_loose(keys), _step; !(_step = _iterator()).done; ) {
      var key = _step.value;
      if (isObject(obj[key]) && isObjTooDeep(obj[key], limit - 1)) {
        return true;
      }
    }
    return false;
  }
  function stringify(obj, stringifyOptions) {
    var options = {
      numOfKeysLimit: 50,
      depthOfLimit: 4
    };
    Object.assign(options, stringifyOptions);
    var stack = [];
    var keys = [];
    return JSON.stringify(obj, function(key, value) {
      if (stack.length > 0) {
        var thisPos = stack.indexOf(this);
        ~thisPos ? stack.splice(thisPos + 1) : stack.push(this);
        ~thisPos ? keys.splice(thisPos, Infinity, key) : keys.push(key);
        if (~stack.indexOf(value)) {
          if (stack[0] === value) {
            value = "[Circular ~]";
          } else {
            value = "[Circular ~." + keys.slice(0, stack.indexOf(value)).join(".") + "]";
          }
        }
      } else {
        stack.push(value);
      }
      if (value === null) return value;
      if (value === void 0) return "undefined";
      if (shouldIgnore(value)) {
        return toString2(value);
      }
      if ((typeof value === "undefined" ? "undefined" : _type_of(value)) === "bigint") {
        return value.toString() + "n";
      }
      if (_instanceof(value, Event)) {
        var eventResult = {};
        for (var eventKey in value) {
          var eventValue = value[eventKey];
          if (Array.isArray(eventValue)) {
            eventResult[eventKey] = pathToSelector(eventValue.length ? eventValue[0] : null);
          } else {
            eventResult[eventKey] = eventValue;
          }
        }
        return eventResult;
      } else if (_instanceof(value, Node)) {
        if (_instanceof(value, HTMLElement)) {
          return value ? value.outerHTML : "";
        }
        return value.nodeName;
      } else if (_instanceof(value, Error)) {
        return value.stack ? value.stack + "\nEnd of stack for Error object" : value.name + ": " + value.message;
      }
      return value;
    });
    function shouldIgnore(_obj) {
      if (isObject(_obj) && Object.keys(_obj).length > options.numOfKeysLimit) {
        return true;
      }
      if (typeof _obj === "function") {
        return true;
      }
      if (isObject(_obj) && isObjTooDeep(_obj, options.depthOfLimit)) {
        return true;
      }
      return false;
    }
    function toString2(_obj) {
      var str = _obj.toString();
      if (options.stringLengthLimit && str.length > options.stringLengthLimit) {
        str = "" + str.slice(0, options.stringLengthLimit) + "...";
      }
      return str;
    }
  }
  var defaultLogOptions = {
    level: [
      "assert",
      "clear",
      "count",
      "countReset",
      "debug",
      "dir",
      "dirxml",
      "error",
      "group",
      "groupCollapsed",
      "groupEnd",
      "info",
      "log",
      "table",
      "time",
      "timeEnd",
      "timeLog",
      "trace",
      "warn"
    ],
    lengthThreshold: 1e3,
    logger: "console"
  };
  function initLogObserver(cb, win2, options) {
    var logOptions = options ? Object.assign({}, defaultLogOptions, options) : defaultLogOptions;
    var loggerType = logOptions.logger;
    if (!loggerType) {
      return function() {
      };
    }
    var logger4;
    if (typeof loggerType === "string") {
      logger4 = win2[loggerType];
    } else {
      logger4 = loggerType;
    }
    var logCount = 0;
    var inStack = false;
    var cancelHandlers = [];
    if (logOptions.level.includes("error")) {
      var errorHandler2 = function(event) {
        var message = event.message, error = event.error;
        var trace = ErrorStackParser.parse(error).map(function(stackFrame) {
          return stackFrame.toString();
        });
        var payload = [
          stringify(message, logOptions.stringifyOptions)
        ];
        cb({
          level: "error",
          trace,
          payload
        });
      };
      win2.addEventListener("error", errorHandler2);
      cancelHandlers.push(function() {
        win2.removeEventListener("error", errorHandler2);
      });
      var unhandledrejectionHandler = function(event) {
        var error;
        var payload;
        if (_instanceof(event.reason, Error)) {
          error = event.reason;
          payload = [
            stringify("Uncaught (in promise) " + error.name + ": " + error.message, logOptions.stringifyOptions)
          ];
        } else {
          error = new Error();
          payload = [
            stringify("Uncaught (in promise)", logOptions.stringifyOptions),
            stringify(event.reason, logOptions.stringifyOptions)
          ];
        }
        var trace = ErrorStackParser.parse(error).map(function(stackFrame) {
          return stackFrame.toString();
        });
        cb({
          level: "error",
          trace,
          payload
        });
      };
      win2.addEventListener("unhandledrejection", unhandledrejectionHandler);
      cancelHandlers.push(function() {
        win2.removeEventListener("unhandledrejection", unhandledrejectionHandler);
      });
    }
    for (var _iterator = _create_for_of_iterator_helper_loose(logOptions.level), _step; !(_step = _iterator()).done; ) {
      var levelType = _step.value;
      cancelHandlers.push(replace(logger4, levelType));
    }
    return function() {
      cancelHandlers.forEach(function(h3) {
        return h3();
      });
    };
    function replace(_logger, level) {
      var _this = this;
      if (!_logger[level]) {
        return function() {
        };
      }
      return patch(_logger, level, function(original) {
        var _this1 = _this;
        return function() {
          for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
            args[_key] = arguments[_key];
          }
          original.apply(_this1, args);
          if (level === "assert" && !!args[0]) {
            return;
          }
          if (inStack) {
            return;
          }
          inStack = true;
          try {
            var trace = ErrorStackParser.parse(new Error()).map(function(stackFrame) {
              return stackFrame.toString();
            }).splice(1);
            var argsForPayload = level === "assert" ? args.slice(1) : args;
            var payload = argsForPayload.map(function(s3) {
              return stringify(s3, logOptions.stringifyOptions);
            });
            logCount++;
            if (logCount < logOptions.lengthThreshold) {
              cb({
                level,
                trace,
                payload
              });
            } else if (logCount === logOptions.lengthThreshold) {
              cb({
                level: "warn",
                trace: [],
                payload: [
                  stringify("The number of log records reached the threshold.")
                ]
              });
            }
          } catch (error) {
            original.apply(void 0, [].concat([
              "rrweb logger error:",
              error
            ], args));
          } finally {
            inStack = false;
          }
        };
      });
    }
  }
  var PLUGIN_NAME = "rrweb/console@1";
  var getRecordConsolePlugin = function(options) {
    return {
      name: PLUGIN_NAME,
      observer: initLogObserver,
      options
    };
  };
  var setImmediate = win["setImmediate"];
  var builtInProp;
  var cycle;
  var schedulingQueue;
  var ToString = Object.prototype.toString;
  var timer = typeof setImmediate !== "undefined" ? function timer2(fn2) {
    return setImmediate(fn2);
  } : setTimeout;
  try {
    Object.defineProperty({}, "x", {});
    builtInProp = function builtInProp2(obj, name5, val, config) {
      return Object.defineProperty(obj, name5, {
        value: val,
        writable: true,
        configurable: config !== false
      });
    };
  } catch (err) {
    builtInProp = function builtInProp2(obj, name5, val) {
      obj[name5] = val;
      return obj;
    };
  }
  schedulingQueue = /* @__PURE__ */ (function Queue() {
    var first, last, item;
    function Item(fn2, self2) {
      this.fn = fn2;
      this.self = self2;
      this.next = void 0;
    }
    return {
      add: function add(fn2, self2) {
        item = new Item(fn2, self2);
        if (last) {
          last.next = item;
        } else {
          first = item;
        }
        last = item;
        item = void 0;
      },
      drain: function drain() {
        var f3 = first;
        first = last = cycle = void 0;
        while (f3) {
          f3.fn.call(f3.self);
          f3 = f3.next;
        }
      }
    };
  })();
  function schedule(fn2, self2) {
    schedulingQueue.add(fn2, self2);
    if (!cycle) {
      cycle = timer(schedulingQueue.drain);
    }
  }
  function isThenable(o3) {
    var _then, oType = typeof o3;
    if (o3 !== null && (oType === "object" || oType === "function")) {
      _then = o3.then;
    }
    return typeof _then === "function" ? _then : false;
  }
  function notify() {
    for (var i3 = 0; i3 < this.chain.length; i3++) {
      notifyIsolated(
        this,
        this.state === 1 ? this.chain[i3].success : this.chain[i3].failure,
        this.chain[i3]
      );
    }
    this.chain.length = 0;
  }
  function notifyIsolated(self2, cb, chain) {
    var ret, _then;
    try {
      if (cb === false) {
        chain.reject(self2.msg);
      } else {
        if (cb === true) {
          ret = self2.msg;
        } else {
          ret = cb.call(void 0, self2.msg);
        }
        if (ret === chain.promise) {
          chain.reject(TypeError("Promise-chain cycle"));
        } else if (_then = isThenable(ret)) {
          _then.call(ret, chain.resolve, chain.reject);
        } else {
          chain.resolve(ret);
        }
      }
    } catch (err) {
      chain.reject(err);
    }
  }
  function resolve(msg) {
    var _then, self2 = this;
    if (self2.triggered) {
      return;
    }
    self2.triggered = true;
    if (self2.def) {
      self2 = self2.def;
    }
    try {
      if (_then = isThenable(msg)) {
        schedule(function() {
          var defWrapper = new MakeDefWrapper(self2);
          try {
            _then.call(
              msg,
              function $resolve$() {
                resolve.apply(defWrapper, arguments);
              },
              function $reject$() {
                reject.apply(defWrapper, arguments);
              }
            );
          } catch (err) {
            reject.call(defWrapper, err);
          }
        });
      } else {
        self2.msg = msg;
        self2.state = 1;
        if (self2.chain.length > 0) {
          schedule(notify, self2);
        }
      }
    } catch (err) {
      reject.call(new MakeDefWrapper(self2), err);
    }
  }
  function reject(msg) {
    var self2 = this;
    if (self2.triggered) {
      return;
    }
    self2.triggered = true;
    if (self2.def) {
      self2 = self2.def;
    }
    self2.msg = msg;
    self2.state = 2;
    if (self2.chain.length > 0) {
      schedule(notify, self2);
    }
  }
  function iteratePromises(Constructor, arr, resolver, rejecter) {
    for (var idx = 0; idx < arr.length; idx++) {
      (function IIFE(idx2) {
        Constructor.resolve(arr[idx2]).then(
          function $resolver$(msg) {
            resolver(idx2, msg);
          },
          rejecter
        );
      })(idx);
    }
  }
  function MakeDefWrapper(self2) {
    this.def = self2;
    this.triggered = false;
  }
  function MakeDef(self2) {
    this.promise = self2;
    this.state = 0;
    this.triggered = false;
    this.chain = [];
    this.msg = void 0;
  }
  function NpoPromise(executor) {
    if (typeof executor !== "function") {
      throw TypeError("Not a function");
    }
    if (this["__NPO__"] !== 0) {
      throw TypeError("Not a promise");
    }
    this["__NPO__"] = 1;
    var def = new MakeDef(this);
    this["then"] = function then(success, failure) {
      var o3 = {
        success: typeof success === "function" ? success : true,
        failure: typeof failure === "function" ? failure : false
      };
      o3.promise = new this.constructor(function extractChain(resolve2, reject2) {
        if (typeof resolve2 !== "function" || typeof reject2 !== "function") {
          throw TypeError("Not a function");
        }
        o3.resolve = resolve2;
        o3.reject = reject2;
      });
      def.chain.push(o3);
      if (def.state !== 0) {
        schedule(notify, def);
      }
      return o3.promise;
    };
    this["catch"] = function $catch$(failure) {
      return this.then(void 0, failure);
    };
    try {
      executor.call(
        void 0,
        function publicResolve(msg) {
          resolve.call(def, msg);
        },
        function publicReject(msg) {
          reject.call(def, msg);
        }
      );
    } catch (err) {
      reject.call(def, err);
    }
  }
  var PromisePrototype = builtInProp(
    {},
    "constructor",
    NpoPromise,
    /*configurable=*/
    false
  );
  NpoPromise.prototype = PromisePrototype;
  builtInProp(
    PromisePrototype,
    "__NPO__",
    0,
    /*configurable=*/
    false
  );
  builtInProp(NpoPromise, "resolve", function Promise$resolve(msg) {
    var Constructor = this;
    if (msg && typeof msg === "object" && msg["__NPO__"] === 1) {
      return msg;
    }
    return new Constructor(function executor(resolve2, reject2) {
      if (typeof resolve2 !== "function" || typeof reject2 !== "function") {
        throw TypeError("Not a function");
      }
      resolve2(msg);
    });
  });
  builtInProp(NpoPromise, "reject", function Promise$reject(msg) {
    return new this(function executor(resolve2, reject2) {
      if (typeof resolve2 !== "function" || typeof reject2 !== "function") {
        throw TypeError("Not a function");
      }
      reject2(msg);
    });
  });
  builtInProp(NpoPromise, "all", function Promise$all(arr) {
    var Constructor = this;
    if (ToString.call(arr) !== "[object Array]") {
      return Constructor.reject(TypeError("Not an array"));
    }
    if (arr.length === 0) {
      return Constructor.resolve([]);
    }
    return new Constructor(function executor(resolve2, reject2) {
      if (typeof resolve2 !== "function" || typeof reject2 !== "function") {
        throw TypeError("Not a function");
      }
      var len = arr.length, msgs = Array(len), count = 0;
      iteratePromises(Constructor, arr, function resolver(idx, msg) {
        msgs[idx] = msg;
        if (++count === len) {
          resolve2(msgs);
        }
      }, reject2);
    });
  });
  builtInProp(NpoPromise, "race", function Promise$race(arr) {
    var Constructor = this;
    if (ToString.call(arr) !== "[object Array]") {
      return Constructor.reject(TypeError("Not an array"));
    }
    return new Constructor(function executor(resolve2, reject2) {
      if (typeof resolve2 !== "function" || typeof reject2 !== "function") {
        throw TypeError("Not a function");
      }
      iteratePromises(Constructor, arr, function resolver(idx, msg) {
        resolve2(msg);
      }, reject2);
    });
  });
  var PromisePolyfill;
  if (typeof Promise !== "undefined" && Promise.toString().indexOf("[native code]") !== -1) {
    PromisePolyfill = Promise;
  } else {
    PromisePolyfill = NpoPromise;
  }
  var Config = {
    DEBUG: false,
    LIB_VERSION: "2.74.0"
  };
  var MAX_RECORDING_MS = 24 * 60 * 60 * 1e3;
  var MAX_VALUE_FOR_MIN_RECORDING_MS = 8 * 1e3;
  var ArrayProto = Array.prototype;
  var FuncProto = Function.prototype;
  var ObjProto = Object.prototype;
  var slice = ArrayProto.slice;
  var toString = ObjProto.toString;
  var hasOwnProperty = ObjProto.hasOwnProperty;
  var windowConsole = win.console;
  var navigator2 = win.navigator;
  var document$1 = win.document;
  var windowOpera = win.opera;
  var screen = win.screen;
  var userAgent = navigator2.userAgent;
  var nativeBind = FuncProto.bind;
  var nativeForEach = ArrayProto.forEach;
  var nativeIndexOf = ArrayProto.indexOf;
  var nativeMap = ArrayProto.map;
  var nativeIsArray = Array.isArray;
  var breaker = {};
  var _3 = {
    trim: function(str) {
      return str.replace(/^[\s\uFEFF\xA0]+|[\s\uFEFF\xA0]+$/g, "");
    }
  };
  var console$1 = {
    /** @type {function(...*)} */
    log: function() {
      if (Config.DEBUG && !_3.isUndefined(windowConsole) && windowConsole) {
        try {
          windowConsole.log.apply(windowConsole, arguments);
        } catch (err) {
          _3.each(arguments, function(arg) {
            windowConsole.log(arg);
          });
        }
      }
    },
    /** @type {function(...*)} */
    warn: function() {
      if (Config.DEBUG && !_3.isUndefined(windowConsole) && windowConsole) {
        var args = ["Mixpanel warning:"].concat(_3.toArray(arguments));
        try {
          windowConsole.warn.apply(windowConsole, args);
        } catch (err) {
          _3.each(args, function(arg) {
            windowConsole.warn(arg);
          });
        }
      }
    },
    /** @type {function(...*)} */
    error: function() {
      if (Config.DEBUG && !_3.isUndefined(windowConsole) && windowConsole) {
        var args = ["Mixpanel error:"].concat(_3.toArray(arguments));
        try {
          windowConsole.error.apply(windowConsole, args);
        } catch (err) {
          _3.each(args, function(arg) {
            windowConsole.error(arg);
          });
        }
      }
    },
    /** @type {function(...*)} */
    critical: function() {
      if (!_3.isUndefined(windowConsole) && windowConsole) {
        var args = ["Mixpanel error:"].concat(_3.toArray(arguments));
        try {
          windowConsole.error.apply(windowConsole, args);
        } catch (err) {
          _3.each(args, function(arg) {
            windowConsole.error(arg);
          });
        }
      }
    }
  };
  var log_func_with_prefix = function(func, prefix) {
    return function() {
      arguments[0] = "[" + prefix + "] " + arguments[0];
      return func.apply(console$1, arguments);
    };
  };
  var console_with_prefix = function(prefix) {
    return {
      log: log_func_with_prefix(console$1.log, prefix),
      error: log_func_with_prefix(console$1.error, prefix),
      critical: log_func_with_prefix(console$1.critical, prefix)
    };
  };
  var safewrap = function(f3) {
    return function() {
      try {
        return f3.apply(this, arguments);
      } catch (e3) {
        console$1.critical("Implementation error. Please turn on debug and contact support@mixpanel.com.");
        if (Config.DEBUG) {
          console$1.critical(e3);
        }
      }
    };
  };
  var safewrapClass = function(klass) {
    var proto = klass.prototype;
    for (var func in proto) {
      if (typeof proto[func] === "function") {
        proto[func] = safewrap(proto[func]);
      }
    }
  };
  _3.bind = function(func, context) {
    var args, bound;
    if (nativeBind && func.bind === nativeBind) {
      return nativeBind.apply(func, slice.call(arguments, 1));
    }
    if (!_3.isFunction(func)) {
      throw new TypeError();
    }
    args = slice.call(arguments, 2);
    bound = function() {
      if (!(this instanceof bound)) {
        return func.apply(context, args.concat(slice.call(arguments)));
      }
      var ctor = {};
      ctor.prototype = func.prototype;
      var self2 = new ctor();
      ctor.prototype = null;
      var result2 = func.apply(self2, args.concat(slice.call(arguments)));
      if (Object(result2) === result2) {
        return result2;
      }
      return self2;
    };
    return bound;
  };
  _3.each = function(obj, iterator, context) {
    if (obj === null || obj === void 0) {
      return;
    }
    if (nativeForEach && obj.forEach === nativeForEach) {
      obj.forEach(iterator, context);
    } else if (obj.length === +obj.length) {
      for (var i3 = 0, l3 = obj.length; i3 < l3; i3++) {
        if (i3 in obj && iterator.call(context, obj[i3], i3, obj) === breaker) {
          return;
        }
      }
    } else {
      for (var key in obj) {
        if (hasOwnProperty.call(obj, key)) {
          if (iterator.call(context, obj[key], key, obj) === breaker) {
            return;
          }
        }
      }
    }
  };
  _3.extend = function(obj) {
    _3.each(slice.call(arguments, 1), function(source) {
      for (var prop in source) {
        if (source[prop] !== void 0) {
          obj[prop] = source[prop];
        }
      }
    });
    return obj;
  };
  _3.isArray = nativeIsArray || function(obj) {
    return toString.call(obj) === "[object Array]";
  };
  _3.isFunction = function(f3) {
    try {
      return /^\s*\bfunction\b/.test(f3);
    } catch (x5) {
      return false;
    }
  };
  _3.isArguments = function(obj) {
    return !!(obj && hasOwnProperty.call(obj, "callee"));
  };
  _3.toArray = function(iterable) {
    if (!iterable) {
      return [];
    }
    if (iterable.toArray) {
      return iterable.toArray();
    }
    if (_3.isArray(iterable)) {
      return slice.call(iterable);
    }
    if (_3.isArguments(iterable)) {
      return slice.call(iterable);
    }
    return _3.values(iterable);
  };
  _3.map = function(arr, callback, context) {
    if (nativeMap && arr.map === nativeMap) {
      return arr.map(callback, context);
    } else {
      var results = [];
      _3.each(arr, function(item) {
        results.push(callback.call(context, item));
      });
      return results;
    }
  };
  _3.keys = function(obj) {
    var results = [];
    if (obj === null) {
      return results;
    }
    _3.each(obj, function(value, key) {
      results[results.length] = key;
    });
    return results;
  };
  _3.values = function(obj) {
    var results = [];
    if (obj === null) {
      return results;
    }
    _3.each(obj, function(value) {
      results[results.length] = value;
    });
    return results;
  };
  _3.include = function(obj, target) {
    var found = false;
    if (obj === null) {
      return found;
    }
    if (nativeIndexOf && obj.indexOf === nativeIndexOf) {
      return obj.indexOf(target) != -1;
    }
    _3.each(obj, function(value) {
      if (found || (found = value === target)) {
        return breaker;
      }
    });
    return found;
  };
  _3.includes = function(str, needle) {
    return str.indexOf(needle) !== -1;
  };
  _3.inherit = function(subclass, superclass) {
    subclass.prototype = new superclass();
    subclass.prototype.constructor = subclass;
    subclass.superclass = superclass.prototype;
    return subclass;
  };
  _3.isObject = function(obj) {
    return obj === Object(obj) && !_3.isArray(obj);
  };
  _3.isEmptyObject = function(obj) {
    if (_3.isObject(obj)) {
      for (var key in obj) {
        if (hasOwnProperty.call(obj, key)) {
          return false;
        }
      }
      return true;
    }
    return false;
  };
  _3.isUndefined = function(obj) {
    return obj === void 0;
  };
  _3.isString = function(obj) {
    return toString.call(obj) == "[object String]";
  };
  _3.isDate = function(obj) {
    return toString.call(obj) == "[object Date]";
  };
  _3.isNumber = function(obj) {
    return toString.call(obj) == "[object Number]";
  };
  _3.isElement = function(obj) {
    return !!(obj && obj.nodeType === 1);
  };
  _3.encodeDates = function(obj) {
    _3.each(obj, function(v3, k4) {
      if (_3.isDate(v3)) {
        obj[k4] = _3.formatDate(v3);
      } else if (_3.isObject(v3)) {
        obj[k4] = _3.encodeDates(v3);
      }
    });
    return obj;
  };
  _3.timestamp = function() {
    Date.now = Date.now || function() {
      return +/* @__PURE__ */ new Date();
    };
    return Date.now();
  };
  _3.formatDate = function(d3) {
    function pad(n3) {
      return n3 < 10 ? "0" + n3 : n3;
    }
    return d3.getUTCFullYear() + "-" + pad(d3.getUTCMonth() + 1) + "-" + pad(d3.getUTCDate()) + "T" + pad(d3.getUTCHours()) + ":" + pad(d3.getUTCMinutes()) + ":" + pad(d3.getUTCSeconds());
  };
  _3.strip_empty_properties = function(p3) {
    var ret = {};
    _3.each(p3, function(v3, k4) {
      if (_3.isString(v3) && v3.length > 0) {
        ret[k4] = v3;
      }
    });
    return ret;
  };
  _3.truncate = function(obj, length) {
    var ret;
    if (typeof obj === "string") {
      ret = obj.slice(0, length);
    } else if (_3.isArray(obj)) {
      ret = [];
      _3.each(obj, function(val) {
        ret.push(_3.truncate(val, length));
      });
    } else if (_3.isObject(obj)) {
      ret = {};
      _3.each(obj, function(val, key) {
        ret[key] = _3.truncate(val, length);
      });
    } else {
      ret = obj;
    }
    return ret;
  };
  _3.JSONEncode = /* @__PURE__ */ (function() {
    return function(mixed_val) {
      var value = mixed_val;
      var quote = function(string) {
        var escapable = /[\\"\x00-\x1f\x7f-\x9f\u00ad\u0600-\u0604\u070f\u17b4\u17b5\u200c-\u200f\u2028-\u202f\u2060-\u206f\ufeff\ufff0-\uffff]/g;
        var meta = {
          // table of character substitutions
          "\b": "\\b",
          "	": "\\t",
          "\n": "\\n",
          "\f": "\\f",
          "\r": "\\r",
          '"': '\\"',
          "\\": "\\\\"
        };
        escapable.lastIndex = 0;
        return escapable.test(string) ? '"' + string.replace(escapable, function(a3) {
          var c3 = meta[a3];
          return typeof c3 === "string" ? c3 : "\\u" + ("0000" + a3.charCodeAt(0).toString(16)).slice(-4);
        }) + '"' : '"' + string + '"';
      };
      var str = function(key, holder) {
        var gap = "";
        var indent = "    ";
        var i3 = 0;
        var k4 = "";
        var v3 = "";
        var length = 0;
        var mind = gap;
        var partial = [];
        var value2 = holder[key];
        if (value2 && typeof value2 === "object" && typeof value2.toJSON === "function") {
          value2 = value2.toJSON(key);
        }
        switch (typeof value2) {
          case "string":
            return quote(value2);
          case "number":
            return isFinite(value2) ? String(value2) : "null";
          case "boolean":
          case "null":
            return String(value2);
          case "object":
            if (!value2) {
              return "null";
            }
            gap += indent;
            partial = [];
            if (toString.apply(value2) === "[object Array]") {
              length = value2.length;
              for (i3 = 0; i3 < length; i3 += 1) {
                partial[i3] = str(i3, value2) || "null";
              }
              v3 = partial.length === 0 ? "[]" : gap ? "[\n" + gap + partial.join(",\n" + gap) + "\n" + mind + "]" : "[" + partial.join(",") + "]";
              gap = mind;
              return v3;
            }
            for (k4 in value2) {
              if (hasOwnProperty.call(value2, k4)) {
                v3 = str(k4, value2);
                if (v3) {
                  partial.push(quote(k4) + (gap ? ": " : ":") + v3);
                }
              }
            }
            v3 = partial.length === 0 ? "{}" : gap ? "{" + partial.join(",") + mind + "}" : "{" + partial.join(",") + "}";
            gap = mind;
            return v3;
        }
      };
      return str("", {
        "": value
      });
    };
  })();
  _3.JSONDecode = (function() {
    var at, ch, escapee = {
      '"': '"',
      "\\": "\\",
      "/": "/",
      "b": "\b",
      "f": "\f",
      "n": "\n",
      "r": "\r",
      "t": "	"
    }, text, error = function(m3) {
      var e3 = new SyntaxError(m3);
      e3.at = at;
      e3.text = text;
      throw e3;
    }, next = function(c3) {
      if (c3 && c3 !== ch) {
        error("Expected '" + c3 + "' instead of '" + ch + "'");
      }
      ch = text.charAt(at);
      at += 1;
      return ch;
    }, number = function() {
      var number2, string2 = "";
      if (ch === "-") {
        string2 = "-";
        next("-");
      }
      while (ch >= "0" && ch <= "9") {
        string2 += ch;
        next();
      }
      if (ch === ".") {
        string2 += ".";
        while (next() && ch >= "0" && ch <= "9") {
          string2 += ch;
        }
      }
      if (ch === "e" || ch === "E") {
        string2 += ch;
        next();
        if (ch === "-" || ch === "+") {
          string2 += ch;
          next();
        }
        while (ch >= "0" && ch <= "9") {
          string2 += ch;
          next();
        }
      }
      number2 = +string2;
      if (!isFinite(number2)) {
        error("Bad number");
      } else {
        return number2;
      }
    }, string = function() {
      var hex, i3, string2 = "", uffff;
      if (ch === '"') {
        while (next()) {
          if (ch === '"') {
            next();
            return string2;
          }
          if (ch === "\\") {
            next();
            if (ch === "u") {
              uffff = 0;
              for (i3 = 0; i3 < 4; i3 += 1) {
                hex = parseInt(next(), 16);
                if (!isFinite(hex)) {
                  break;
                }
                uffff = uffff * 16 + hex;
              }
              string2 += String.fromCharCode(uffff);
            } else if (typeof escapee[ch] === "string") {
              string2 += escapee[ch];
            } else {
              break;
            }
          } else {
            string2 += ch;
          }
        }
      }
      error("Bad string");
    }, white = function() {
      while (ch && ch <= " ") {
        next();
      }
    }, word = function() {
      switch (ch) {
        case "t":
          next("t");
          next("r");
          next("u");
          next("e");
          return true;
        case "f":
          next("f");
          next("a");
          next("l");
          next("s");
          next("e");
          return false;
        case "n":
          next("n");
          next("u");
          next("l");
          next("l");
          return null;
      }
      error('Unexpected "' + ch + '"');
    }, value, array = function() {
      var array2 = [];
      if (ch === "[") {
        next("[");
        white();
        if (ch === "]") {
          next("]");
          return array2;
        }
        while (ch) {
          array2.push(value());
          white();
          if (ch === "]") {
            next("]");
            return array2;
          }
          next(",");
          white();
        }
      }
      error("Bad array");
    }, object = function() {
      var key, object2 = {};
      if (ch === "{") {
        next("{");
        white();
        if (ch === "}") {
          next("}");
          return object2;
        }
        while (ch) {
          key = string();
          white();
          next(":");
          if (Object.hasOwnProperty.call(object2, key)) {
            error('Duplicate key "' + key + '"');
          }
          object2[key] = value();
          white();
          if (ch === "}") {
            next("}");
            return object2;
          }
          next(",");
          white();
        }
      }
      error("Bad object");
    };
    value = function() {
      white();
      switch (ch) {
        case "{":
          return object();
        case "[":
          return array();
        case '"':
          return string();
        case "-":
          return number();
        default:
          return ch >= "0" && ch <= "9" ? number() : word();
      }
    };
    return function(source) {
      var result2;
      text = source;
      at = 0;
      ch = " ";
      result2 = value();
      white();
      if (ch) {
        error("Syntax error");
      }
      return result2;
    };
  })();
  _3.base64Encode = function(data) {
    var b64 = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=";
    var o1, o22, o3, h1, h22, h3, h4, bits, i3 = 0, ac = 0, enc = "", tmp_arr = [];
    if (!data) {
      return data;
    }
    data = _3.utf8Encode(data);
    do {
      o1 = data.charCodeAt(i3++);
      o22 = data.charCodeAt(i3++);
      o3 = data.charCodeAt(i3++);
      bits = o1 << 16 | o22 << 8 | o3;
      h1 = bits >> 18 & 63;
      h22 = bits >> 12 & 63;
      h3 = bits >> 6 & 63;
      h4 = bits & 63;
      tmp_arr[ac++] = b64.charAt(h1) + b64.charAt(h22) + b64.charAt(h3) + b64.charAt(h4);
    } while (i3 < data.length);
    enc = tmp_arr.join("");
    switch (data.length % 3) {
      case 1:
        enc = enc.slice(0, -2) + "==";
        break;
      case 2:
        enc = enc.slice(0, -1) + "=";
        break;
    }
    return enc;
  };
  _3.utf8Encode = function(string) {
    string = (string + "").replace(/\r\n/g, "\n").replace(/\r/g, "\n");
    var utftext = "", start, end;
    var stringl = 0, n3;
    start = end = 0;
    stringl = string.length;
    for (n3 = 0; n3 < stringl; n3++) {
      var c1 = string.charCodeAt(n3);
      var enc = null;
      if (c1 < 128) {
        end++;
      } else if (c1 > 127 && c1 < 2048) {
        enc = String.fromCharCode(c1 >> 6 | 192, c1 & 63 | 128);
      } else {
        enc = String.fromCharCode(c1 >> 12 | 224, c1 >> 6 & 63 | 128, c1 & 63 | 128);
      }
      if (enc !== null) {
        if (end > start) {
          utftext += string.substring(start, end);
        }
        utftext += enc;
        start = end = n3 + 1;
      }
    }
    if (end > start) {
      utftext += string.substring(start, string.length);
    }
    return utftext;
  };
  _3.UUID = function() {
    try {
      return win["crypto"]["randomUUID"]();
    } catch (err) {
      var uuid = new Array(36);
      for (var i3 = 0; i3 < 36; i3++) {
        uuid[i3] = Math.floor(Math.random() * 16);
      }
      uuid[14] = 4;
      uuid[19] = uuid[19] &= -5;
      uuid[19] = uuid[19] |= 1 << 3;
      uuid[8] = uuid[13] = uuid[18] = uuid[23] = "-";
      return _3.map(uuid, function(x5) {
        return x5.toString(16);
      }).join("");
    }
  };
  var BLOCKED_UA_STRS = [
    "ahrefsbot",
    "ahrefssiteaudit",
    "amazonbot",
    "baiduspider",
    "bingbot",
    "bingpreview",
    "chrome-lighthouse",
    "facebookexternal",
    "petalbot",
    "pinterest",
    "screaming frog",
    "yahoo! slurp",
    "yandex",
    // a whole bunch of goog-specific crawlers
    // https://developers.google.com/search/docs/advanced/crawling/overview-google-crawlers
    "adsbot-google",
    "apis-google",
    "duplexweb-google",
    "feedfetcher-google",
    "google favicon",
    "google web preview",
    "google-read-aloud",
    "googlebot",
    "googleweblight",
    "mediapartners-google",
    "storebot-google"
  ];
  _3.isBlockedUA = function(ua) {
    var i3;
    ua = ua.toLowerCase();
    for (i3 = 0; i3 < BLOCKED_UA_STRS.length; i3++) {
      if (ua.indexOf(BLOCKED_UA_STRS[i3]) !== -1) {
        return true;
      }
    }
    return false;
  };
  _3.HTTPBuildQuery = function(formdata, arg_separator) {
    var use_val, use_key, tmp_arr = [];
    if (_3.isUndefined(arg_separator)) {
      arg_separator = "&";
    }
    _3.each(formdata, function(val, key) {
      use_val = encodeURIComponent(val.toString());
      use_key = encodeURIComponent(key);
      tmp_arr[tmp_arr.length] = use_key + "=" + use_val;
    });
    return tmp_arr.join(arg_separator);
  };
  _3.getQueryParam = function(url, param) {
    param = param.replace(/[[]/g, "\\[").replace(/[\]]/g, "\\]");
    var regexS = "[\\?&]" + param + "=([^&#]*)", regex = new RegExp(regexS), results = regex.exec(url);
    if (results === null || results && typeof results[1] !== "string" && results[1].length) {
      return "";
    } else {
      var result2 = results[1];
      try {
        result2 = decodeURIComponent(result2);
      } catch (err) {
        console$1.error("Skipping decoding for malformed query param: " + result2);
      }
      return result2.replace(/\+/g, " ");
    }
  };
  _3.cookie = {
    get: function(name5) {
      var nameEQ = name5 + "=";
      var ca = document$1.cookie.split(";");
      for (var i3 = 0; i3 < ca.length; i3++) {
        var c3 = ca[i3];
        while (c3.charAt(0) == " ") {
          c3 = c3.substring(1, c3.length);
        }
        if (c3.indexOf(nameEQ) === 0) {
          return decodeURIComponent(c3.substring(nameEQ.length, c3.length));
        }
      }
      return null;
    },
    parse: function(name5) {
      var cookie;
      try {
        cookie = _3.JSONDecode(_3.cookie.get(name5)) || {};
      } catch (err) {
      }
      return cookie;
    },
    set_seconds: function(name5, value, seconds, is_cross_subdomain, is_secure, is_cross_site, domain_override) {
      var cdomain = "", expires = "", secure = "";
      if (domain_override) {
        cdomain = "; domain=" + domain_override;
      } else if (is_cross_subdomain) {
        var domain = extract_domain(document$1.location.hostname);
        cdomain = domain ? "; domain=." + domain : "";
      }
      if (seconds) {
        var date = /* @__PURE__ */ new Date();
        date.setTime(date.getTime() + seconds * 1e3);
        expires = "; expires=" + date.toGMTString();
      }
      if (is_cross_site) {
        is_secure = true;
        secure = "; SameSite=None";
      }
      if (is_secure) {
        secure += "; secure";
      }
      document$1.cookie = name5 + "=" + encodeURIComponent(value) + expires + "; path=/" + cdomain + secure;
    },
    set: function(name5, value, days, is_cross_subdomain, is_secure, is_cross_site, domain_override) {
      var cdomain = "", expires = "", secure = "";
      if (domain_override) {
        cdomain = "; domain=" + domain_override;
      } else if (is_cross_subdomain) {
        var domain = extract_domain(document$1.location.hostname);
        cdomain = domain ? "; domain=." + domain : "";
      }
      if (days) {
        var date = /* @__PURE__ */ new Date();
        date.setTime(date.getTime() + days * 24 * 60 * 60 * 1e3);
        expires = "; expires=" + date.toGMTString();
      }
      if (is_cross_site) {
        is_secure = true;
        secure = "; SameSite=None";
      }
      if (is_secure) {
        secure += "; secure";
      }
      var new_cookie_val = name5 + "=" + encodeURIComponent(value) + expires + "; path=/" + cdomain + secure;
      document$1.cookie = new_cookie_val;
      return new_cookie_val;
    },
    remove: function(name5, is_cross_subdomain, domain_override) {
      _3.cookie.set(name5, "", -1, is_cross_subdomain, false, false, domain_override);
    }
  };
  var _testStorageSupported = function(storage) {
    var supported = true;
    try {
      var key = "__mplss_" + cheap_guid(8), val = "xyz";
      storage.setItem(key, val);
      if (storage.getItem(key) !== val) {
        supported = false;
      }
      storage.removeItem(key);
    } catch (err) {
      supported = false;
    }
    return supported;
  };
  var _localStorageSupported = null;
  var localStorageSupported = function(storage, forceCheck) {
    if (_localStorageSupported !== null && !forceCheck) {
      return _localStorageSupported;
    }
    return _localStorageSupported = _testStorageSupported(storage || win.localStorage);
  };
  var _sessionStorageSupported = null;
  var sessionStorageSupported = function(storage, forceCheck) {
    if (_sessionStorageSupported !== null && !forceCheck) {
      return _sessionStorageSupported;
    }
    return _sessionStorageSupported = _testStorageSupported(storage || win.sessionStorage);
  };
  function _storageWrapper(storage, name5, is_supported_fn) {
    var log_error = function(msg) {
      console$1.error(name5 + " error: " + msg);
    };
    return {
      is_supported: function(forceCheck) {
        var supported = is_supported_fn(storage, forceCheck);
        if (!supported) {
          console$1.error(name5 + " unsupported");
        }
        return supported;
      },
      error: log_error,
      get: function(key) {
        try {
          return storage.getItem(key);
        } catch (err) {
          log_error(err);
        }
        return null;
      },
      parse: function(key) {
        try {
          return _3.JSONDecode(storage.getItem(key)) || {};
        } catch (err) {
        }
        return null;
      },
      set: function(key, value) {
        try {
          storage.setItem(key, value);
        } catch (err) {
          log_error(err);
        }
      },
      remove: function(key) {
        try {
          storage.removeItem(key);
        } catch (err) {
          log_error(err);
        }
      }
    };
  }
  var windowLocalStorage = null;
  var windowSessionStorage = null;
  try {
    windowLocalStorage = win.localStorage;
    windowSessionStorage = win.sessionStorage;
  } catch (_err) {
  }
  _3.localStorage = _storageWrapper(windowLocalStorage, "localStorage", localStorageSupported);
  _3.sessionStorage = _storageWrapper(windowSessionStorage, "sessionStorage", sessionStorageSupported);
  _3.register_event = (function() {
    var register_event = function(element, type, handler, oldSchool, useCapture) {
      if (!element) {
        console$1.error("No valid element provided to register_event");
        return;
      }
      if (element.addEventListener && !oldSchool) {
        element.addEventListener(type, handler, !!useCapture);
      } else {
        var ontype = "on" + type;
        var old_handler = element[ontype];
        element[ontype] = makeHandler(element, handler, old_handler);
      }
    };
    function makeHandler(element, new_handler, old_handlers) {
      var handler = function(event) {
        event = event || fixEvent(win.event);
        if (!event) {
          return void 0;
        }
        var ret = true;
        var old_result, new_result;
        if (_3.isFunction(old_handlers)) {
          old_result = old_handlers(event);
        }
        new_result = new_handler.call(element, event);
        if (false === old_result || false === new_result) {
          ret = false;
        }
        return ret;
      };
      return handler;
    }
    function fixEvent(event) {
      if (event) {
        event.preventDefault = fixEvent.preventDefault;
        event.stopPropagation = fixEvent.stopPropagation;
      }
      return event;
    }
    fixEvent.preventDefault = function() {
      this.returnValue = false;
    };
    fixEvent.stopPropagation = function() {
      this.cancelBubble = true;
    };
    return register_event;
  })();
  var TOKEN_MATCH_REGEX = new RegExp('^(\\w*)\\[(\\w+)([=~\\|\\^\\$\\*]?)=?"?([^\\]"]*)"?\\]$');
  _3.dom_query = /* @__PURE__ */ (function() {
    function getAllChildren(e3) {
      return e3.all ? e3.all : e3.getElementsByTagName("*");
    }
    var bad_whitespace = /[\t\r\n]/g;
    function hasClass(elem, selector) {
      var className = " " + selector + " ";
      return (" " + elem.className + " ").replace(bad_whitespace, " ").indexOf(className) >= 0;
    }
    function getElementsBySelector(selector) {
      if (!document$1.getElementsByTagName) {
        return [];
      }
      var tokens = selector.split(" ");
      var token, bits, tagName, found, foundCount, i3, j4, k4, elements, currentContextIndex;
      var currentContext = [document$1];
      for (i3 = 0; i3 < tokens.length; i3++) {
        token = tokens[i3].replace(/^\s+/, "").replace(/\s+$/, "");
        if (token.indexOf("#") > -1) {
          bits = token.split("#");
          tagName = bits[0];
          var id = bits[1];
          var element = document$1.getElementById(id);
          if (!element || tagName && element.nodeName.toLowerCase() != tagName) {
            return [];
          }
          currentContext = [element];
          continue;
        }
        if (token.indexOf(".") > -1) {
          bits = token.split(".");
          tagName = bits[0];
          var className = bits[1];
          if (!tagName) {
            tagName = "*";
          }
          found = [];
          foundCount = 0;
          for (j4 = 0; j4 < currentContext.length; j4++) {
            if (tagName == "*") {
              elements = getAllChildren(currentContext[j4]);
            } else {
              elements = currentContext[j4].getElementsByTagName(tagName);
            }
            for (k4 = 0; k4 < elements.length; k4++) {
              found[foundCount++] = elements[k4];
            }
          }
          currentContext = [];
          currentContextIndex = 0;
          for (j4 = 0; j4 < found.length; j4++) {
            if (found[j4].className && _3.isString(found[j4].className) && // some SVG elements have classNames which are not strings
            hasClass(found[j4], className)) {
              currentContext[currentContextIndex++] = found[j4];
            }
          }
          continue;
        }
        var token_match = token.match(TOKEN_MATCH_REGEX);
        if (token_match) {
          tagName = token_match[1];
          var attrName = token_match[2];
          var attrOperator = token_match[3];
          var attrValue = token_match[4];
          if (!tagName) {
            tagName = "*";
          }
          found = [];
          foundCount = 0;
          for (j4 = 0; j4 < currentContext.length; j4++) {
            if (tagName == "*") {
              elements = getAllChildren(currentContext[j4]);
            } else {
              elements = currentContext[j4].getElementsByTagName(tagName);
            }
            for (k4 = 0; k4 < elements.length; k4++) {
              found[foundCount++] = elements[k4];
            }
          }
          currentContext = [];
          currentContextIndex = 0;
          var checkFunction;
          switch (attrOperator) {
            case "=":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName) == attrValue;
              };
              break;
            case "~":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName).match(new RegExp("\\b" + attrValue + "\\b"));
              };
              break;
            case "|":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName).match(new RegExp("^" + attrValue + "-?"));
              };
              break;
            case "^":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName).indexOf(attrValue) === 0;
              };
              break;
            case "$":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName).lastIndexOf(attrValue) == e3.getAttribute(attrName).length - attrValue.length;
              };
              break;
            case "*":
              checkFunction = function(e3) {
                return e3.getAttribute(attrName).indexOf(attrValue) > -1;
              };
              break;
            default:
              checkFunction = function(e3) {
                return e3.getAttribute(attrName);
              };
          }
          currentContext = [];
          currentContextIndex = 0;
          for (j4 = 0; j4 < found.length; j4++) {
            if (checkFunction(found[j4])) {
              currentContext[currentContextIndex++] = found[j4];
            }
          }
          continue;
        }
        tagName = token;
        found = [];
        foundCount = 0;
        for (j4 = 0; j4 < currentContext.length; j4++) {
          elements = currentContext[j4].getElementsByTagName(tagName);
          for (k4 = 0; k4 < elements.length; k4++) {
            found[foundCount++] = elements[k4];
          }
        }
        currentContext = found;
      }
      return currentContext;
    }
    return function(query) {
      if (_3.isElement(query)) {
        return [query];
      } else if (_3.isObject(query) && !_3.isUndefined(query.length)) {
        return query;
      } else {
        return getElementsBySelector.call(this, query);
      }
    };
  })();
  var CAMPAIGN_KEYWORDS = ["utm_source", "utm_medium", "utm_campaign", "utm_content", "utm_term", "utm_id", "utm_source_platform", "utm_campaign_id", "utm_creative_format", "utm_marketing_tactic"];
  var CLICK_IDS = ["dclid", "fbclid", "gclid", "ko_click_id", "li_fat_id", "msclkid", "sccid", "ttclid", "twclid", "wbraid"];
  _3.info = {
    campaignParams: function(default_value) {
      var kw = "", params = {};
      _3.each(CAMPAIGN_KEYWORDS, function(kwkey) {
        kw = _3.getQueryParam(document$1.URL, kwkey);
        if (kw.length) {
          params[kwkey] = kw;
        } else if (default_value !== void 0) {
          params[kwkey] = default_value;
        }
      });
      return params;
    },
    clickParams: function() {
      var id = "", params = {};
      _3.each(CLICK_IDS, function(idkey) {
        id = _3.getQueryParam(document$1.URL, idkey);
        if (id.length) {
          params[idkey] = id;
        }
      });
      return params;
    },
    marketingParams: function() {
      return _3.extend(_3.info.campaignParams(), _3.info.clickParams());
    },
    searchEngine: function(referrer) {
      if (referrer.search("https?://(.*)google.([^/?]*)") === 0) {
        return "google";
      } else if (referrer.search("https?://(.*)bing.com") === 0) {
        return "bing";
      } else if (referrer.search("https?://(.*)yahoo.com") === 0) {
        return "yahoo";
      } else if (referrer.search("https?://(.*)duckduckgo.com") === 0) {
        return "duckduckgo";
      } else {
        return null;
      }
    },
    searchInfo: function(referrer) {
      var search = _3.info.searchEngine(referrer), param = search != "yahoo" ? "q" : "p", ret = {};
      if (search !== null) {
        ret["$search_engine"] = search;
        var keyword = _3.getQueryParam(referrer, param);
        if (keyword.length) {
          ret["mp_keyword"] = keyword;
        }
      }
      return ret;
    },
    /**
     * This function detects which browser is running this script.
     * The order of the checks are important since many user agents
     * include key words used in later checks.
     */
    browser: function(user_agent, vendor, opera) {
      vendor = vendor || "";
      if (opera || _3.includes(user_agent, " OPR/")) {
        if (_3.includes(user_agent, "Mini")) {
          return "Opera Mini";
        }
        return "Opera";
      } else if (/(BlackBerry|PlayBook|BB10)/i.test(user_agent)) {
        return "BlackBerry";
      } else if (_3.includes(user_agent, "IEMobile") || _3.includes(user_agent, "WPDesktop")) {
        return "Internet Explorer Mobile";
      } else if (_3.includes(user_agent, "SamsungBrowser/")) {
        return "Samsung Internet";
      } else if (_3.includes(user_agent, "Edge") || _3.includes(user_agent, "Edg/")) {
        return "Microsoft Edge";
      } else if (_3.includes(user_agent, "FBIOS")) {
        return "Facebook Mobile";
      } else if (_3.includes(user_agent, "Whale/")) {
        return "Whale Browser";
      } else if (_3.includes(user_agent, "Chrome")) {
        return "Chrome";
      } else if (_3.includes(user_agent, "CriOS")) {
        return "Chrome iOS";
      } else if (_3.includes(user_agent, "UCWEB") || _3.includes(user_agent, "UCBrowser")) {
        return "UC Browser";
      } else if (_3.includes(user_agent, "FxiOS")) {
        return "Firefox iOS";
      } else if (_3.includes(vendor, "Apple")) {
        if (_3.includes(user_agent, "Mobile")) {
          return "Mobile Safari";
        }
        return "Safari";
      } else if (_3.includes(user_agent, "Android")) {
        return "Android Mobile";
      } else if (_3.includes(user_agent, "Konqueror")) {
        return "Konqueror";
      } else if (_3.includes(user_agent, "Firefox")) {
        return "Firefox";
      } else if (_3.includes(user_agent, "MSIE") || _3.includes(user_agent, "Trident/")) {
        return "Internet Explorer";
      } else if (_3.includes(user_agent, "Gecko")) {
        return "Mozilla";
      } else {
        return "";
      }
    },
    /**
     * This function detects which browser version is running this script,
     * parsing major and minor version (e.g., 42.1). User agent strings from:
     * http://www.useragentstring.com/pages/useragentstring.php
     */
    browserVersion: function(userAgent2, vendor, opera) {
      var browser2 = _3.info.browser(userAgent2, vendor, opera);
      var versionRegexs = {
        "Internet Explorer Mobile": /rv:(\d+(\.\d+)?)/,
        "Microsoft Edge": /Edge?\/(\d+(\.\d+)?)/,
        "Chrome": /Chrome\/(\d+(\.\d+)?)/,
        "Chrome iOS": /CriOS\/(\d+(\.\d+)?)/,
        "UC Browser": /(UCBrowser|UCWEB)\/(\d+(\.\d+)?)/,
        "Safari": /Version\/(\d+(\.\d+)?)/,
        "Mobile Safari": /Version\/(\d+(\.\d+)?)/,
        "Opera": /(Opera|OPR)\/(\d+(\.\d+)?)/,
        "Firefox": /Firefox\/(\d+(\.\d+)?)/,
        "Firefox iOS": /FxiOS\/(\d+(\.\d+)?)/,
        "Konqueror": /Konqueror:(\d+(\.\d+)?)/,
        "BlackBerry": /BlackBerry (\d+(\.\d+)?)/,
        "Android Mobile": /android\s(\d+(\.\d+)?)/,
        "Samsung Internet": /SamsungBrowser\/(\d+(\.\d+)?)/,
        "Internet Explorer": /(rv:|MSIE )(\d+(\.\d+)?)/,
        "Mozilla": /rv:(\d+(\.\d+)?)/,
        "Whale Browser": /Whale\/(\d+(\.\d+)?)/
      };
      var regex = versionRegexs[browser2];
      if (regex === void 0) {
        return null;
      }
      var matches = userAgent2.match(regex);
      if (!matches) {
        return null;
      }
      return parseFloat(matches[matches.length - 2]);
    },
    os: function() {
      var a3 = userAgent;
      if (/Windows/i.test(a3)) {
        if (/Phone/.test(a3) || /WPDesktop/.test(a3)) {
          return "Windows Phone";
        }
        return "Windows";
      } else if (/(iPhone|iPad|iPod)/.test(a3)) {
        return "iOS";
      } else if (/Android/.test(a3)) {
        return "Android";
      } else if (/(BlackBerry|PlayBook|BB10)/i.test(a3)) {
        return "BlackBerry";
      } else if (/Mac/i.test(a3)) {
        return "Mac OS X";
      } else if (/Linux/.test(a3)) {
        return "Linux";
      } else if (/CrOS/.test(a3)) {
        return "Chrome OS";
      } else {
        return "";
      }
    },
    device: function(user_agent) {
      if (/Windows Phone/i.test(user_agent) || /WPDesktop/.test(user_agent)) {
        return "Windows Phone";
      } else if (/iPad/.test(user_agent)) {
        return "iPad";
      } else if (/iPod/.test(user_agent)) {
        return "iPod Touch";
      } else if (/iPhone/.test(user_agent)) {
        return "iPhone";
      } else if (/(BlackBerry|PlayBook|BB10)/i.test(user_agent)) {
        return "BlackBerry";
      } else if (/Android/.test(user_agent)) {
        return "Android";
      } else {
        return "";
      }
    },
    referringDomain: function(referrer) {
      var split4 = referrer.split("/");
      if (split4.length >= 3) {
        return split4[2];
      }
      return "";
    },
    currentUrl: function() {
      return win.location.href;
    },
    properties: function(extra_props) {
      if (typeof extra_props !== "object") {
        extra_props = {};
      }
      return _3.extend(_3.strip_empty_properties({
        "$os": _3.info.os(),
        "$browser": _3.info.browser(userAgent, navigator2.vendor, windowOpera),
        "$referrer": document$1.referrer,
        "$referring_domain": _3.info.referringDomain(document$1.referrer),
        "$device": _3.info.device(userAgent)
      }), {
        "$current_url": _3.info.currentUrl(),
        "$browser_version": _3.info.browserVersion(userAgent, navigator2.vendor, windowOpera),
        "$screen_height": screen.height,
        "$screen_width": screen.width,
        "mp_lib": "web",
        "$lib_version": Config.LIB_VERSION,
        "$insert_id": cheap_guid(),
        "time": _3.timestamp() / 1e3
        // epoch time in seconds
      }, _3.strip_empty_properties(extra_props));
    },
    people_properties: function() {
      return _3.extend(_3.strip_empty_properties({
        "$os": _3.info.os(),
        "$browser": _3.info.browser(userAgent, navigator2.vendor, windowOpera)
      }), {
        "$browser_version": _3.info.browserVersion(userAgent, navigator2.vendor, windowOpera)
      });
    },
    mpPageViewProperties: function() {
      return _3.strip_empty_properties({
        "current_page_title": document$1.title,
        "current_domain": win.location.hostname,
        "current_url_path": win.location.pathname,
        "current_url_protocol": win.location.protocol,
        "current_url_search": win.location.search
      });
    }
  };
  var batchedThrottle = function(fn2, waitMs) {
    var timeoutPromise = null;
    var throttledItems = [];
    return function(item) {
      var self2 = this;
      throttledItems.push(item);
      if (!timeoutPromise) {
        timeoutPromise = new PromisePolyfill(function(resolve2) {
          setTimeout(function() {
            var returnValue = fn2.apply(self2, [throttledItems]);
            timeoutPromise = null;
            throttledItems = [];
            resolve2(returnValue);
          }, waitMs);
        });
      }
      return timeoutPromise;
    };
  };
  var cheap_guid = function(maxlen) {
    var guid = Math.random().toString(36).substring(2, 10) + Math.random().toString(36).substring(2, 10);
    return maxlen ? guid.substring(0, maxlen) : guid;
  };
  var generateTraceparent = function() {
    var traceID = _3.UUID().replace(/-/g, "");
    var parentID = _3.UUID().replace(/-/g, "").substring(0, 16);
    var traceFlags = "01";
    return "00-" + traceID + "-" + parentID + "-" + traceFlags;
  };
  var SIMPLE_DOMAIN_MATCH_REGEX = /[a-z0-9][a-z0-9-]*\.[a-z]+$/i;
  var DOMAIN_MATCH_REGEX = /[a-z0-9][a-z0-9-]+\.[a-z.]{2,6}$/i;
  var extract_domain = function(hostname) {
    var domain_regex = DOMAIN_MATCH_REGEX;
    var parts = hostname.split(".");
    var tld = parts[parts.length - 1];
    if (tld.length > 4 || tld === "com" || tld === "org") {
      domain_regex = SIMPLE_DOMAIN_MATCH_REGEX;
    }
    var matches = hostname.match(domain_regex);
    return matches ? matches[0] : "";
  };
  var isOnline = function() {
    var onLine = win.navigator["onLine"];
    return _3.isUndefined(onLine) || onLine;
  };
  var NOOP_FUNC = function() {
  };
  var JSONStringify = null;
  var JSONParse = null;
  if (typeof JSON !== "undefined") {
    JSONStringify = JSON.stringify;
    JSONParse = JSON.parse;
  }
  JSONStringify = JSONStringify || _3.JSONEncode;
  JSONParse = JSONParse || _3.JSONDecode;
  var canUseCompressionStream = function(userAgent2, vendor, opera) {
    if (!win.CompressionStream) {
      return false;
    }
    var browser2 = _3.info.browser(userAgent2, vendor, opera);
    var version4 = _3.info.browserVersion(userAgent2, vendor, opera);
    if (browser2 === "Safari" || browser2 === "Mobile Safari") {
      if (version4 >= 16.4 && version4 < 16.6) {
        return false;
      }
    }
    return true;
  };
  _3["info"] = _3.info;
  _3["info"]["browser"] = _3.info.browser;
  _3["info"]["browserVersion"] = _3.info.browserVersion;
  _3["info"]["device"] = _3.info.device;
  _3["info"]["properties"] = _3.info.properties;
  _3["isBlockedUA"] = _3.isBlockedUA;
  _3["isEmptyObject"] = _3.isEmptyObject;
  _3["isObject"] = _3.isObject;
  _3["JSONDecode"] = _3.JSONDecode;
  _3["JSONEncode"] = _3.JSONEncode;
  _3["toArray"] = _3.toArray;
  _3["NPO"] = NpoPromise;
  var MIXPANEL_DB_NAME = "mixpanelBrowserDb";
  var RECORDING_EVENTS_STORE_NAME = "mixpanelRecordingEvents";
  var RECORDING_REGISTRY_STORE_NAME = "mixpanelRecordingRegistry";
  var DB_VERSION = 1;
  var OBJECT_STORES = [RECORDING_EVENTS_STORE_NAME, RECORDING_REGISTRY_STORE_NAME];
  var IDBStorageWrapper = function(storeName) {
    this.dbPromise = null;
    this.storeName = storeName;
  };
  IDBStorageWrapper.prototype._openDb = function() {
    return new PromisePolyfill(function(resolve2, reject2) {
      var openRequest = win.indexedDB.open(MIXPANEL_DB_NAME, DB_VERSION);
      openRequest["onerror"] = function() {
        reject2(openRequest.error);
      };
      openRequest["onsuccess"] = function() {
        resolve2(openRequest.result);
      };
      openRequest["onupgradeneeded"] = function(ev) {
        var db = ev.target.result;
        OBJECT_STORES.forEach(function(storeName) {
          db.createObjectStore(storeName);
        });
      };
    });
  };
  IDBStorageWrapper.prototype.init = function() {
    if (!win.indexedDB) {
      return PromisePolyfill.reject("indexedDB is not supported in this browser");
    }
    if (!this.dbPromise) {
      this.dbPromise = this._openDb();
    }
    return this.dbPromise.then(function(dbOrError) {
      if (dbOrError instanceof win["IDBDatabase"]) {
        return PromisePolyfill.resolve();
      } else {
        return PromisePolyfill.reject(dbOrError);
      }
    });
  };
  IDBStorageWrapper.prototype.isInitialized = function() {
    return !!this.dbPromise;
  };
  IDBStorageWrapper.prototype.makeTransaction = function(mode, storeCb) {
    var storeName = this.storeName;
    var doTransaction = function(db) {
      return new PromisePolyfill(function(resolve2, reject2) {
        var transaction = db.transaction(storeName, mode);
        transaction.oncomplete = function() {
          resolve2(transaction);
        };
        transaction.onabort = transaction.onerror = function() {
          reject2(transaction.error);
        };
        storeCb(transaction.objectStore(storeName));
      });
    };
    return this.dbPromise.then(doTransaction).catch(function(err) {
      if (err && err["name"] === "InvalidStateError") {
        this.dbPromise = this._openDb();
        return this.dbPromise.then(doTransaction);
      } else {
        return PromisePolyfill.reject(err);
      }
    }.bind(this));
  };
  IDBStorageWrapper.prototype.setItem = function(key, value) {
    return this.makeTransaction("readwrite", function(objectStore) {
      objectStore.put(value, key);
    });
  };
  IDBStorageWrapper.prototype.getItem = function(key) {
    var req;
    return this.makeTransaction("readonly", function(objectStore) {
      req = objectStore.get(key);
    }).then(function() {
      return req.result;
    });
  };
  IDBStorageWrapper.prototype.removeItem = function(key) {
    return this.makeTransaction("readwrite", function(objectStore) {
      objectStore.delete(key);
    });
  };
  IDBStorageWrapper.prototype.getAll = function() {
    var req;
    return this.makeTransaction("readonly", function(objectStore) {
      req = objectStore.getAll();
    }).then(function() {
      return req.result;
    });
  };
  var GDPR_DEFAULT_PERSISTENCE_PREFIX = "__mp_opt_in_out_";
  function optIn(token, options) {
    _optInOut(true, token, options);
  }
  function optOut(token, options) {
    _optInOut(false, token, options);
  }
  function hasOptedIn(token, options) {
    return _getStorageValue(token, options) === "1";
  }
  function hasOptedOut(token, options) {
    if (_hasDoNotTrackFlagOn(options)) {
      console$1.warn('This browser has "Do Not Track" enabled. This will prevent the Mixpanel SDK from sending any data. To ignore the "Do Not Track" browser setting, initialize the Mixpanel instance with the config "ignore_dnt: true"');
      return true;
    }
    var optedOut = _getStorageValue(token, options) === "0";
    if (optedOut) {
      console$1.warn("You are opted out of Mixpanel tracking. This will prevent the Mixpanel SDK from sending any data.");
    }
    return optedOut;
  }
  function addOptOutCheckMixpanelLib(method) {
    return _addOptOutCheck(method, function(name5) {
      return this.get_config(name5);
    });
  }
  function addOptOutCheckMixpanelPeople(method) {
    return _addOptOutCheck(method, function(name5) {
      return this._get_config(name5);
    });
  }
  function addOptOutCheckMixpanelGroup(method) {
    return _addOptOutCheck(method, function(name5) {
      return this._get_config(name5);
    });
  }
  function clearOptInOut(token, options) {
    options = options || {};
    _getStorage(options).remove(
      _getStorageKey(token, options),
      !!options.crossSubdomainCookie,
      options.cookieDomain
    );
  }
  function _getStorage(options) {
    options = options || {};
    return options.persistenceType === "localStorage" ? _3.localStorage : _3.cookie;
  }
  function _getStorageKey(token, options) {
    options = options || {};
    return (options.persistencePrefix || GDPR_DEFAULT_PERSISTENCE_PREFIX) + token;
  }
  function _getStorageValue(token, options) {
    return _getStorage(options).get(_getStorageKey(token, options));
  }
  function _hasDoNotTrackFlagOn(options) {
    if (options && options.ignoreDnt) {
      return false;
    }
    var win$1 = options && options.window || win;
    var nav = win$1["navigator"] || {};
    var hasDntOn = false;
    _3.each([
      nav["doNotTrack"],
      // standard
      nav["msDoNotTrack"],
      win$1["doNotTrack"]
    ], function(dntValue) {
      if (_3.includes([true, 1, "1", "yes"], dntValue)) {
        hasDntOn = true;
      }
    });
    return hasDntOn;
  }
  function _optInOut(optValue, token, options) {
    if (!_3.isString(token) || !token.length) {
      console$1.error("gdpr." + (optValue ? "optIn" : "optOut") + " called with an invalid token");
      return;
    }
    options = options || {};
    _getStorage(options).set(
      _getStorageKey(token, options),
      optValue ? 1 : 0,
      _3.isNumber(options.cookieExpiration) ? options.cookieExpiration : null,
      !!options.crossSubdomainCookie,
      !!options.secureCookie,
      !!options.crossSiteCookie,
      options.cookieDomain
    );
    if (options.track && optValue) {
      options.track(options.trackEventName || "$opt_in", options.trackProperties, {
        "send_immediately": true
      });
    }
  }
  function _addOptOutCheck(method, getConfigValue) {
    return function() {
      var optedOut = false;
      try {
        var token = getConfigValue.call(this, "token");
        var ignoreDnt = getConfigValue.call(this, "ignore_dnt");
        var persistenceType = getConfigValue.call(this, "opt_out_tracking_persistence_type");
        var persistencePrefix = getConfigValue.call(this, "opt_out_tracking_cookie_prefix");
        var win2 = getConfigValue.call(this, "window");
        if (token) {
          optedOut = hasOptedOut(token, {
            ignoreDnt,
            persistenceType,
            persistencePrefix,
            window: win2
          });
        }
      } catch (err) {
        console$1.error("Unexpected error when checking tracking opt-out status: " + err);
      }
      if (!optedOut) {
        return method.apply(this, arguments);
      }
      var callback = arguments[arguments.length - 1];
      if (typeof callback === "function") {
        callback(0);
      }
      return;
    };
  }
  var logger$6 = console_with_prefix("lock");
  var SharedLock = function(key, options) {
    options = options || {};
    this.storageKey = key;
    this.storage = options.storage || win.localStorage;
    this.pollIntervalMS = options.pollIntervalMS || 100;
    this.timeoutMS = options.timeoutMS || 2e3;
    this.promiseImpl = options.promiseImpl || PromisePolyfill;
  };
  SharedLock.prototype.withLock = function(lockedCB, pid) {
    var Promise2 = this.promiseImpl;
    return new Promise2(_3.bind(function(resolve2, reject2) {
      var i3 = pid || (/* @__PURE__ */ new Date()).getTime() + "|" + Math.random();
      var startTime = (/* @__PURE__ */ new Date()).getTime();
      var key = this.storageKey;
      var pollIntervalMS = this.pollIntervalMS;
      var timeoutMS = this.timeoutMS;
      var storage = this.storage;
      var keyX = key + ":X";
      var keyY = key + ":Y";
      var keyZ = key + ":Z";
      var delay = function(cb) {
        if ((/* @__PURE__ */ new Date()).getTime() - startTime > timeoutMS) {
          logger$6.error("Timeout waiting for mutex on " + key + "; clearing lock. [" + i3 + "]");
          storage.removeItem(keyZ);
          storage.removeItem(keyY);
          loop();
          return;
        }
        setTimeout(function() {
          try {
            cb();
          } catch (err) {
            reject2(err);
          }
        }, pollIntervalMS * (Math.random() + 0.1));
      };
      var waitFor = function(predicate, cb) {
        if (predicate()) {
          cb();
        } else {
          delay(function() {
            waitFor(predicate, cb);
          });
        }
      };
      var getSetY = function() {
        var valY = storage.getItem(keyY);
        if (valY && valY !== i3) {
          return false;
        } else {
          storage.setItem(keyY, i3);
          if (storage.getItem(keyY) === i3) {
            return true;
          } else {
            if (!localStorageSupported(storage, true)) {
              reject2(new Error("localStorage support dropped while acquiring lock"));
            }
            return false;
          }
        }
      };
      var loop = function() {
        storage.setItem(keyX, i3);
        waitFor(getSetY, function() {
          if (storage.getItem(keyX) === i3) {
            criticalSection();
            return;
          }
          delay(function() {
            if (storage.getItem(keyY) !== i3) {
              loop();
              return;
            }
            waitFor(function() {
              return !storage.getItem(keyZ);
            }, criticalSection);
          });
        });
      };
      var criticalSection = function() {
        storage.setItem(keyZ, "1");
        var removeLock = function() {
          storage.removeItem(keyZ);
          if (storage.getItem(keyY) === i3) {
            storage.removeItem(keyY);
          }
          if (storage.getItem(keyX) === i3) {
            storage.removeItem(keyX);
          }
        };
        lockedCB().then(function(ret) {
          removeLock();
          resolve2(ret);
        }).catch(function(err) {
          removeLock();
          reject2(err);
        });
      };
      try {
        if (localStorageSupported(storage, true)) {
          loop();
        } else {
          throw new Error("localStorage support check failed");
        }
      } catch (err) {
        reject2(err);
      }
    }, this));
  };
  var LocalStorageWrapper = function(storageOverride) {
    this.storage = storageOverride || win.localStorage;
  };
  LocalStorageWrapper.prototype.init = function() {
    return PromisePolyfill.resolve();
  };
  LocalStorageWrapper.prototype.isInitialized = function() {
    return true;
  };
  LocalStorageWrapper.prototype.setItem = function(key, value) {
    return new PromisePolyfill(_3.bind(function(resolve2, reject2) {
      try {
        this.storage.setItem(key, JSONStringify(value));
      } catch (e3) {
        reject2(e3);
      }
      resolve2();
    }, this));
  };
  LocalStorageWrapper.prototype.getItem = function(key) {
    return new PromisePolyfill(_3.bind(function(resolve2, reject2) {
      var item;
      try {
        item = JSONParse(this.storage.getItem(key));
      } catch (e3) {
        reject2(e3);
      }
      resolve2(item);
    }, this));
  };
  LocalStorageWrapper.prototype.removeItem = function(key) {
    return new PromisePolyfill(_3.bind(function(resolve2, reject2) {
      try {
        this.storage.removeItem(key);
      } catch (e3) {
        reject2(e3);
      }
      resolve2();
    }, this));
  };
  var logger$5 = console_with_prefix("batch");
  var RequestQueue = function(storageKey, options) {
    options = options || {};
    this.storageKey = storageKey;
    this.usePersistence = options.usePersistence;
    if (this.usePersistence) {
      this.queueStorage = options.queueStorage || new LocalStorageWrapper();
      this.lock = new SharedLock(storageKey, {
        storage: options.sharedLockStorage || win.localStorage,
        timeoutMS: options.sharedLockTimeoutMS
      });
    }
    this.reportError = options.errorReporter || _3.bind(logger$5.error, logger$5);
    this.pid = options.pid || null;
    this.memQueue = [];
    this.initialized = false;
    if (options.enqueueThrottleMs) {
      this.enqueuePersisted = batchedThrottle(_3.bind(this._enqueuePersisted, this), options.enqueueThrottleMs);
    } else {
      this.enqueuePersisted = _3.bind(function(queueEntry) {
        return this._enqueuePersisted([queueEntry]);
      }, this);
    }
  };
  RequestQueue.prototype.ensureInit = function() {
    if (this.initialized || !this.usePersistence) {
      return PromisePolyfill.resolve();
    }
    return this.queueStorage.init().then(_3.bind(function() {
      this.initialized = true;
    }, this)).catch(_3.bind(function(err) {
      this.reportError("Error initializing queue persistence. Disabling persistence", err);
      this.initialized = true;
      this.usePersistence = false;
    }, this));
  };
  RequestQueue.prototype.enqueue = function(item, flushInterval) {
    var queueEntry = {
      "id": cheap_guid(),
      "flushAfter": (/* @__PURE__ */ new Date()).getTime() + flushInterval * 2,
      "payload": item
    };
    if (!this.usePersistence) {
      this.memQueue.push(queueEntry);
      return PromisePolyfill.resolve(true);
    } else {
      return this.enqueuePersisted(queueEntry);
    }
  };
  RequestQueue.prototype._enqueuePersisted = function(queueEntries) {
    var enqueueItem = _3.bind(function() {
      return this.ensureInit().then(_3.bind(function() {
        return this.readFromStorage();
      }, this)).then(_3.bind(function(storedQueue) {
        return this.saveToStorage(storedQueue.concat(queueEntries));
      }, this)).then(_3.bind(function(succeeded) {
        if (succeeded) {
          this.memQueue = this.memQueue.concat(queueEntries);
        }
        return succeeded;
      }, this)).catch(_3.bind(function(err) {
        this.reportError("Error enqueueing items", err, queueEntries);
        return false;
      }, this));
    }, this);
    return this.lock.withLock(enqueueItem, this.pid).catch(_3.bind(function(err) {
      this.reportError("Error acquiring storage lock", err);
      return false;
    }, this));
  };
  RequestQueue.prototype.fillBatch = function(batchSize) {
    var batch = this.memQueue.slice(0, batchSize);
    if (this.usePersistence && batch.length < batchSize) {
      return this.ensureInit().then(_3.bind(function() {
        return this.readFromStorage();
      }, this)).then(_3.bind(function(storedQueue) {
        if (storedQueue.length) {
          var idsInBatch = {};
          _3.each(batch, function(item2) {
            idsInBatch[item2["id"]] = true;
          });
          for (var i3 = 0; i3 < storedQueue.length; i3++) {
            var item = storedQueue[i3];
            if ((/* @__PURE__ */ new Date()).getTime() > item["flushAfter"] && !idsInBatch[item["id"]]) {
              item.orphaned = true;
              batch.push(item);
              if (batch.length >= batchSize) {
                break;
              }
            }
          }
        }
        return batch;
      }, this));
    } else {
      return PromisePolyfill.resolve(batch);
    }
  };
  var filterOutIDsAndInvalid = function(items, idSet) {
    var filteredItems = [];
    _3.each(items, function(item) {
      if (item["id"] && !idSet[item["id"]]) {
        filteredItems.push(item);
      }
    });
    return filteredItems;
  };
  RequestQueue.prototype.removeItemsByID = function(ids) {
    var idSet = {};
    _3.each(ids, function(id) {
      idSet[id] = true;
    });
    this.memQueue = filterOutIDsAndInvalid(this.memQueue, idSet);
    if (!this.usePersistence) {
      return PromisePolyfill.resolve(true);
    } else {
      var removeFromStorage = _3.bind(function() {
        return this.ensureInit().then(_3.bind(function() {
          return this.readFromStorage();
        }, this)).then(_3.bind(function(storedQueue) {
          storedQueue = filterOutIDsAndInvalid(storedQueue, idSet);
          return this.saveToStorage(storedQueue);
        }, this)).then(_3.bind(function() {
          return this.readFromStorage();
        }, this)).then(_3.bind(function(storedQueue) {
          for (var i3 = 0; i3 < storedQueue.length; i3++) {
            var item = storedQueue[i3];
            if (item["id"] && !!idSet[item["id"]]) {
              throw new Error("Item not removed from storage");
            }
          }
          return true;
        }, this)).catch(_3.bind(function(err) {
          this.reportError("Error removing items", err, ids);
          return false;
        }, this));
      }, this);
      return this.lock.withLock(removeFromStorage, this.pid).catch(_3.bind(function(err) {
        this.reportError("Error acquiring storage lock", err);
        if (!localStorageSupported(this.lock.storage, true)) {
          return removeFromStorage().then(_3.bind(function(success) {
            if (!success) {
              return this.queueStorage.removeItem(this.storageKey).then(function() {
                return success;
              });
            }
            return success;
          }, this)).catch(_3.bind(function(err2) {
            this.reportError("Error clearing queue", err2);
            return false;
          }, this));
        } else {
          return false;
        }
      }, this));
    }
  };
  var updatePayloads = function(existingItems, itemsToUpdate) {
    var newItems = [];
    _3.each(existingItems, function(item) {
      var id = item["id"];
      if (id in itemsToUpdate) {
        var newPayload = itemsToUpdate[id];
        if (newPayload !== null) {
          item["payload"] = newPayload;
          newItems.push(item);
        }
      } else {
        newItems.push(item);
      }
    });
    return newItems;
  };
  RequestQueue.prototype.updatePayloads = function(itemsToUpdate) {
    this.memQueue = updatePayloads(this.memQueue, itemsToUpdate);
    if (!this.usePersistence) {
      return PromisePolyfill.resolve(true);
    } else {
      return this.lock.withLock(_3.bind(function lockAcquired() {
        return this.ensureInit().then(_3.bind(function() {
          return this.readFromStorage();
        }, this)).then(_3.bind(function(storedQueue) {
          storedQueue = updatePayloads(storedQueue, itemsToUpdate);
          return this.saveToStorage(storedQueue);
        }, this)).catch(_3.bind(function(err) {
          this.reportError("Error updating items", itemsToUpdate, err);
          return false;
        }, this));
      }, this), this.pid).catch(_3.bind(function(err) {
        this.reportError("Error acquiring storage lock", err);
        return false;
      }, this));
    }
  };
  RequestQueue.prototype.readFromStorage = function() {
    return this.ensureInit().then(_3.bind(function() {
      return this.queueStorage.getItem(this.storageKey);
    }, this)).then(_3.bind(function(storageEntry) {
      if (storageEntry) {
        if (!_3.isArray(storageEntry)) {
          this.reportError("Invalid storage entry:", storageEntry);
          storageEntry = null;
        }
      }
      return storageEntry || [];
    }, this)).catch(_3.bind(function(err) {
      this.reportError("Error retrieving queue", err);
      return [];
    }, this));
  };
  RequestQueue.prototype.saveToStorage = function(queue) {
    return this.ensureInit().then(_3.bind(function() {
      return this.queueStorage.setItem(this.storageKey, queue);
    }, this)).then(function() {
      return true;
    }).catch(_3.bind(function(err) {
      this.reportError("Error saving queue", err);
      return false;
    }, this));
  };
  RequestQueue.prototype.clear = function() {
    this.memQueue = [];
    if (this.usePersistence) {
      return this.ensureInit().then(_3.bind(function() {
        return this.queueStorage.removeItem(this.storageKey);
      }, this));
    } else {
      return PromisePolyfill.resolve();
    }
  };
  var MAX_RETRY_INTERVAL_MS = 10 * 60 * 1e3;
  var logger$4 = console_with_prefix("batch");
  var RequestBatcher = function(storageKey, options) {
    this.errorReporter = options.errorReporter;
    this.queue = new RequestQueue(storageKey, {
      errorReporter: _3.bind(this.reportError, this),
      queueStorage: options.queueStorage,
      sharedLockStorage: options.sharedLockStorage,
      sharedLockTimeoutMS: options.sharedLockTimeoutMS,
      usePersistence: options.usePersistence,
      enqueueThrottleMs: options.enqueueThrottleMs
    });
    this.libConfig = options.libConfig;
    this.sendRequest = options.sendRequestFunc;
    this.beforeSendHook = options.beforeSendHook;
    this.stopAllBatching = options.stopAllBatchingFunc;
    this.batchSize = this.libConfig["batch_size"];
    this.flushInterval = this.libConfig["batch_flush_interval_ms"];
    this.stopped = !this.libConfig["batch_autostart"];
    this.consecutiveRemovalFailures = 0;
    this.itemIdsSentSuccessfully = {};
    this.flushOnlyOnInterval = options.flushOnlyOnInterval || false;
    this._flushPromise = null;
  };
  RequestBatcher.prototype.enqueue = function(item) {
    return this.queue.enqueue(item, this.flushInterval);
  };
  RequestBatcher.prototype.start = function() {
    this.stopped = false;
    this.consecutiveRemovalFailures = 0;
    return this.flush();
  };
  RequestBatcher.prototype.stop = function() {
    this.stopped = true;
    if (this.timeoutID) {
      clearTimeout(this.timeoutID);
      this.timeoutID = null;
    }
  };
  RequestBatcher.prototype.clear = function() {
    return this.queue.clear();
  };
  RequestBatcher.prototype.resetBatchSize = function() {
    this.batchSize = this.libConfig["batch_size"];
  };
  RequestBatcher.prototype.resetFlush = function() {
    this.scheduleFlush(this.libConfig["batch_flush_interval_ms"]);
  };
  RequestBatcher.prototype.scheduleFlush = function(flushMS) {
    this.flushInterval = flushMS;
    if (!this.stopped) {
      this.timeoutID = setTimeout(_3.bind(function() {
        if (!this.stopped) {
          this._flushPromise = this.flush();
        }
      }, this), this.flushInterval);
    }
  };
  RequestBatcher.prototype.sendRequestPromise = function(data, options) {
    return new PromisePolyfill(_3.bind(function(resolve2) {
      this.sendRequest(data, options, resolve2);
    }, this));
  };
  RequestBatcher.prototype.flush = function(options) {
    if (this.requestInProgress) {
      logger$4.log("Flush: Request already in progress");
      return PromisePolyfill.resolve();
    }
    this.requestInProgress = true;
    options = options || {};
    var timeoutMS = this.libConfig["batch_request_timeout_ms"];
    var startTime = (/* @__PURE__ */ new Date()).getTime();
    var currentBatchSize = this.batchSize;
    return this.queue.fillBatch(currentBatchSize).then(_3.bind(function(batch) {
      var attemptSecondaryFlush = batch.length === currentBatchSize;
      var dataForRequest = [];
      var transformedItems = {};
      _3.each(batch, function(item) {
        var payload = item["payload"];
        if (this.beforeSendHook && !item.orphaned) {
          payload = this.beforeSendHook(payload);
        }
        if (payload) {
          if (payload["event"] && payload["properties"]) {
            payload["properties"] = _3.extend(
              {},
              payload["properties"],
              { "mp_sent_by_lib_version": Config.LIB_VERSION }
            );
          }
          var addPayload = true;
          var itemId = item["id"];
          if (itemId) {
            if ((this.itemIdsSentSuccessfully[itemId] || 0) > 5) {
              this.reportError("[dupe] item ID sent too many times, not sending", {
                item,
                batchSize: batch.length,
                timesSent: this.itemIdsSentSuccessfully[itemId]
              });
              addPayload = false;
            }
          } else {
            this.reportError("[dupe] found item with no ID", { item });
          }
          if (addPayload) {
            dataForRequest.push(payload);
          }
        }
        transformedItems[item["id"]] = payload;
      }, this);
      if (dataForRequest.length < 1) {
        this.requestInProgress = false;
        this.resetFlush();
        return PromisePolyfill.resolve();
      }
      var removeItemsFromQueue = _3.bind(function() {
        return this.queue.removeItemsByID(
          _3.map(batch, function(item) {
            return item["id"];
          })
        ).then(_3.bind(function(succeeded) {
          _3.each(batch, _3.bind(function(item) {
            var itemId = item["id"];
            if (itemId) {
              this.itemIdsSentSuccessfully[itemId] = this.itemIdsSentSuccessfully[itemId] || 0;
              this.itemIdsSentSuccessfully[itemId]++;
              if (this.itemIdsSentSuccessfully[itemId] > 5) {
                this.reportError("[dupe] item ID sent too many times", {
                  item,
                  batchSize: batch.length,
                  timesSent: this.itemIdsSentSuccessfully[itemId]
                });
              }
            } else {
              this.reportError("[dupe] found item with no ID while removing", { item });
            }
          }, this));
          if (succeeded) {
            this.consecutiveRemovalFailures = 0;
            if (this.flushOnlyOnInterval && !attemptSecondaryFlush) {
              this.resetFlush();
              return PromisePolyfill.resolve();
            } else {
              return this.flush();
            }
          } else {
            if (++this.consecutiveRemovalFailures > 5) {
              this.reportError("Too many queue failures; disabling batching system.");
              this.stopAllBatching();
            } else {
              this.resetFlush();
            }
            return PromisePolyfill.resolve();
          }
        }, this));
      }, this);
      var batchSendCallback = _3.bind(function(res) {
        this.requestInProgress = false;
        try {
          if (options.unloading) {
            return this.queue.updatePayloads(transformedItems);
          } else if (_3.isObject(res) && res.error === "timeout" && (/* @__PURE__ */ new Date()).getTime() - startTime >= timeoutMS) {
            this.reportError("Network timeout; retrying");
            return this.flush();
          } else if (_3.isObject(res) && (res.httpStatusCode >= 500 || res.httpStatusCode === 429 || res.httpStatusCode <= 0 && !isOnline() || res.error === "timeout")) {
            var retryMS = this.flushInterval * 2;
            if (res.retryAfter) {
              retryMS = parseInt(res.retryAfter, 10) * 1e3 || retryMS;
            }
            retryMS = Math.min(MAX_RETRY_INTERVAL_MS, retryMS);
            this.reportError("Error; retry in " + retryMS + " ms");
            this.scheduleFlush(retryMS);
            return PromisePolyfill.resolve();
          } else if (_3.isObject(res) && res.httpStatusCode === 413) {
            if (batch.length > 1) {
              var halvedBatchSize = Math.max(1, Math.floor(currentBatchSize / 2));
              this.batchSize = Math.min(this.batchSize, halvedBatchSize, batch.length - 1);
              this.reportError("413 response; reducing batch size to " + this.batchSize);
              this.resetFlush();
              return PromisePolyfill.resolve();
            } else {
              this.reportError("Single-event request too large; dropping", batch);
              this.resetBatchSize();
              return removeItemsFromQueue();
            }
          } else {
            return removeItemsFromQueue();
          }
        } catch (err) {
          this.reportError("Error handling API response", err);
          this.resetFlush();
        }
      }, this);
      var requestOptions = {
        method: "POST",
        verbose: true,
        ignore_json_errors: true,
        // eslint-disable-line camelcase
        timeout_ms: timeoutMS
        // eslint-disable-line camelcase
      };
      if (options.unloading) {
        requestOptions.transport = "sendBeacon";
      }
      logger$4.log("MIXPANEL REQUEST:", dataForRequest);
      return this.sendRequestPromise(dataForRequest, requestOptions).then(batchSendCallback);
    }, this)).catch(_3.bind(function(err) {
      this.reportError("Error flushing request queue", err);
      this.resetFlush();
    }, this));
  };
  RequestBatcher.prototype.reportError = function(msg, err) {
    logger$4.error.apply(logger$4.error, arguments);
    if (this.errorReporter) {
      try {
        if (!(err instanceof Error)) {
          err = new Error(msg);
        }
        this.errorReporter(msg, err);
      } catch (err2) {
        logger$4.error(err2);
      }
    }
  };
  var isRecordingExpired = function(serializedRecording) {
    var now = Date.now();
    return !serializedRecording || now > serializedRecording["maxExpires"] || now > serializedRecording["idleExpires"];
  };
  var RECORD_ENQUEUE_THROTTLE_MS = 250;
  var EV_CHANGE = "change";
  var EV_CLICK = "click";
  var EV_HASHCHANGE = "hashchange";
  var EV_INPUT = "input";
  var EV_LOAD = "load";
  var EV_MP_LOCATION_CHANGE = "mp_locationchange";
  var EV_POPSTATE = "popstate";
  var EV_SCROLLEND = "scrollend";
  var EV_SCROLL = "scroll";
  var EV_SELECT = "select";
  var EV_SUBMIT = "submit";
  var EV_TOGGLE = "toggle";
  var EV_VISIBILITYCHANGE = "visibilitychange";
  var CLICK_EVENT_PROPS = [
    "clientX",
    "clientY",
    "offsetX",
    "offsetY",
    "pageX",
    "pageY",
    "screenX",
    "screenY",
    "x",
    "y"
  ];
  var OPT_IN_CLASSES = ["mp-include"];
  var OPT_OUT_CLASSES = ["mp-no-track"];
  var SENSITIVE_DATA_CLASSES = OPT_OUT_CLASSES.concat(["mp-sensitive"]);
  var TRACKED_ATTRS = [
    "aria-label",
    "aria-labelledby",
    "aria-describedby",
    "href",
    "name",
    "role",
    "title",
    "type"
  ];
  var INTERACTIVE_ARIA_ROLES = {
    "button": true,
    "checkbox": true,
    "combobox": true,
    "grid": true,
    "link": true,
    "listbox": true,
    "menu": true,
    "menubar": true,
    "menuitem": true,
    "menuitemcheckbox": true,
    "menuitemradio": true,
    "navigation": true,
    "option": true,
    "radio": true,
    "radiogroup": true,
    "searchbox": true,
    "slider": true,
    "spinbutton": true,
    "switch": true,
    "tab": true,
    "tablist": true,
    "textbox": true,
    "tree": true,
    "treegrid": true,
    "treeitem": true
  };
  var ALWAYS_NON_INTERACTIVE_TAGS = {
    // Document metadata
    "base": true,
    "head": true,
    "html": true,
    "link": true,
    "meta": true,
    "script": true,
    "style": true,
    "title": true,
    // Text formatting
    "br": true,
    "hr": true,
    "wbr": true,
    // Other
    "noscript": true,
    "picture": true,
    "source": true,
    "template": true,
    "track": true
  };
  var TEXT_CONTAINER_TAGS = {
    "article": true,
    "div": true,
    "h1": true,
    "h2": true,
    "h3": true,
    "h4": true,
    "h5": true,
    "h6": true,
    "p": true,
    "section": true,
    "span": true
  };
  var EVENT_HANDLER_ATTRIBUTES = [
    "onclick",
    "onmousedown",
    "onmouseup",
    "onpointerdown",
    "onpointerup",
    "ontouchend",
    "ontouchstart"
  ];
  var MAX_DEPTH = 5;
  var logger$3 = console_with_prefix("autocapture");
  function getClasses(el) {
    var classes = {};
    var classList = getClassName(el).split(" ");
    for (var i3 = 0; i3 < classList.length; i3++) {
      var cls = classList[i3];
      if (cls) {
        classes[cls] = true;
      }
    }
    return classes;
  }
  function getClassName(el) {
    switch (typeof el.className) {
      case "string":
        return el.className;
      case "object":
        return el.className.baseVal || el.getAttribute("class") || "";
      default:
        return "";
    }
  }
  function getPreviousElementSibling(el) {
    if (el.previousElementSibling) {
      return el.previousElementSibling;
    } else {
      do {
        el = el.previousSibling;
      } while (el && !isElementNode(el));
      return el;
    }
  }
  function getPropertiesFromElement(el, ev, blockAttrsSet, extraAttrs, allowElementCallback, allowSelectors) {
    var props = {
      "$classes": getClassName(el).split(" "),
      "$tag_name": el.tagName.toLowerCase()
    };
    var elId = el.id;
    if (elId) {
      props["$id"] = elId;
    }
    if (shouldTrackElementDetails(el, ev, allowElementCallback, allowSelectors)) {
      _3.each(TRACKED_ATTRS.concat(extraAttrs), function(attr) {
        if (el.hasAttribute(attr) && !blockAttrsSet[attr]) {
          var attrVal = el.getAttribute(attr);
          if (shouldTrackValue(attrVal)) {
            props["$attr-" + attr] = attrVal;
          }
        }
      });
    }
    var nthChild = 1;
    var nthOfType = 1;
    var currentElem = el;
    while (currentElem = getPreviousElementSibling(currentElem)) {
      nthChild++;
      if (currentElem.tagName === el.tagName) {
        nthOfType++;
      }
    }
    props["$nth_child"] = nthChild;
    props["$nth_of_type"] = nthOfType;
    return props;
  }
  function getPropsForDOMEvent(ev, config) {
    var allowElementCallback = config.allowElementCallback;
    var allowSelectors = config.allowSelectors || [];
    var blockAttrs = config.blockAttrs || [];
    var blockElementCallback = config.blockElementCallback;
    var blockSelectors = config.blockSelectors || [];
    var captureTextContent = config.captureTextContent || false;
    var captureExtraAttrs = config.captureExtraAttrs || [];
    var capturedForHeatMap = config.capturedForHeatMap || false;
    var blockAttrsSet = {};
    _3.each(blockAttrs, function(attr) {
      blockAttrsSet[attr] = true;
    });
    var props = null;
    var target = typeof ev.target === "undefined" ? ev.srcElement : ev.target;
    if (isTextNode(target)) {
      target = target.parentNode;
    }
    if (shouldTrackDomEvent(target, ev) && isElementAllowed(target, ev, allowElementCallback, allowSelectors) && !isElementBlocked(target, ev, blockElementCallback, blockSelectors)) {
      var targetElementList = [target];
      var curEl = target;
      while (curEl.parentNode && !isTag(curEl, "body")) {
        targetElementList.push(curEl.parentNode);
        curEl = curEl.parentNode;
      }
      var elementsJson = [];
      var href, explicitNoTrack = false;
      _3.each(targetElementList, function(el) {
        var shouldTrackDetails = shouldTrackElementDetails(el, ev, allowElementCallback, allowSelectors);
        if (!blockAttrsSet["href"] && el.tagName.toLowerCase() === "a") {
          href = el.getAttribute("href");
          href = shouldTrackDetails && shouldTrackValue(href) && href;
        }
        if (isElementBlocked(el, ev, blockElementCallback, blockSelectors)) {
          explicitNoTrack = true;
        }
        elementsJson.push(getPropertiesFromElement(el, ev, blockAttrsSet, captureExtraAttrs, allowElementCallback, allowSelectors));
      }, this);
      if (!explicitNoTrack) {
        var docElement = document$1["documentElement"];
        props = {
          "$event_type": ev.type,
          "$host": win.location.host,
          "$pathname": win.location.pathname,
          "$elements": elementsJson,
          "$el_attr__href": href,
          "$viewportHeight": Math.max(docElement["clientHeight"], win["innerHeight"] || 0),
          "$viewportWidth": Math.max(docElement["clientWidth"], win["innerWidth"] || 0),
          "$pageHeight": document$1["body"]["offsetHeight"] || 0,
          "$pageWidth": document$1["body"]["offsetWidth"] || 0
        };
        _3.each(captureExtraAttrs, function(attr) {
          if (!blockAttrsSet[attr] && target.hasAttribute(attr)) {
            var attrVal = target.getAttribute(attr);
            if (shouldTrackValue(attrVal)) {
              props["$el_attr__" + attr] = attrVal;
            }
          }
        });
        if (captureTextContent) {
          elementText = getSafeText(target, ev, allowElementCallback, allowSelectors);
          if (elementText && elementText.length) {
            props["$el_text"] = elementText;
          }
        }
        if (ev.type === EV_CLICK) {
          _3.each(CLICK_EVENT_PROPS, function(prop) {
            if (prop in ev) {
              props["$" + prop] = ev[prop];
            }
          });
          if (capturedForHeatMap) {
            props["$captured_for_heatmap"] = true;
          }
          target = guessRealClickTarget(ev);
        }
        if (captureTextContent) {
          var elementText = getSafeText(target, ev, allowElementCallback, allowSelectors);
          if (elementText && elementText.length) {
            props["$el_text"] = elementText;
          }
        }
        if (target) {
          if (!isElementAllowed(target, ev, allowElementCallback, allowSelectors) || isElementBlocked(target, ev, blockElementCallback, blockSelectors)) {
            return null;
          }
          var targetProps = getPropertiesFromElement(target, ev, blockAttrsSet, captureExtraAttrs, allowElementCallback, allowSelectors);
          props["$target"] = targetProps;
          props["$el_classes"] = targetProps["$classes"];
          _3.extend(props, _3.strip_empty_properties({
            "$el_id": targetProps["$id"],
            "$el_tag_name": targetProps["$tag_name"]
          }));
        }
      }
    }
    return props;
  }
  function getSafeText(el, ev, allowElementCallback, allowSelectors) {
    var elText = "";
    if (shouldTrackElementDetails(el, ev, allowElementCallback, allowSelectors) && el.childNodes && el.childNodes.length) {
      _3.each(el.childNodes, function(child) {
        if (isTextNode(child) && child.textContent) {
          elText += _3.trim(child.textContent).split(/(\s+)/).filter(shouldTrackValue).join("").replace(/[\r\n]/g, " ").replace(/[ ]+/g, " ").substring(0, 255);
        }
      });
    }
    return _3.trim(elText);
  }
  function guessRealClickTarget(ev) {
    var target = ev.target;
    var composedPath = ev["composedPath"]();
    for (var i3 = 0; i3 < composedPath.length; i3++) {
      var node2 = composedPath[i3];
      if (isTag(node2, "a") || isTag(node2, "button") || isTag(node2, "input") || isTag(node2, "select") || node2.getAttribute && node2.getAttribute("role") === "button") {
        target = node2;
        break;
      }
      if (node2 === target) {
        break;
      }
    }
    return target;
  }
  function isElementAllowed(el, ev, allowElementCallback, allowSelectors) {
    if (allowElementCallback) {
      try {
        if (!allowElementCallback(el, ev)) {
          return false;
        }
      } catch (err) {
        logger$3.critical("Error while checking element in allowElementCallback", err);
        return false;
      }
    }
    if (!allowSelectors.length) {
      return true;
    }
    for (var i3 = 0; i3 < allowSelectors.length; i3++) {
      var sel = allowSelectors[i3];
      try {
        if (el["matches"](sel)) {
          return true;
        }
      } catch (err) {
        logger$3.critical("Error while checking selector: " + sel, err);
      }
    }
    return false;
  }
  function isElementBlocked(el, ev, blockElementCallback, blockSelectors) {
    var i3;
    if (blockElementCallback) {
      try {
        if (blockElementCallback(el, ev)) {
          return true;
        }
      } catch (err) {
        logger$3.critical("Error while checking element in blockElementCallback", err);
        return true;
      }
    }
    if (blockSelectors && blockSelectors.length) {
      for (i3 = 0; i3 < blockSelectors.length; i3++) {
        var sel = blockSelectors[i3];
        try {
          if (el["matches"](sel)) {
            return true;
          }
        } catch (err) {
          logger$3.critical("Error while checking selector: " + sel, err);
        }
      }
    }
    var classes = getClasses(el);
    for (i3 = 0; i3 < OPT_OUT_CLASSES.length; i3++) {
      if (classes[OPT_OUT_CLASSES[i3]]) {
        return true;
      }
    }
    return false;
  }
  function isElementNode(node2) {
    return node2 && node2.nodeType === 1;
  }
  function isTag(el, tag) {
    return el && el.tagName && el.tagName.toLowerCase() === tag.toLowerCase();
  }
  function isTextNode(node2) {
    return node2 && node2.nodeType === 3;
  }
  function minDOMApisSupported() {
    try {
      var testEl = document$1.createElement("div");
      return !!testEl["matches"];
    } catch (err) {
      return false;
    }
  }
  function weakSetSupported() {
    return typeof WeakSet !== "undefined";
  }
  function shouldTrackDomEvent(el, ev) {
    if (!el || isTag(el, "html") || !isElementNode(el)) {
      return false;
    }
    var tag = el.tagName.toLowerCase();
    switch (tag) {
      case "form":
        return ev.type === EV_SUBMIT;
      case "input":
        if (["button", "submit"].indexOf(el.getAttribute("type")) === -1) {
          return ev.type === EV_CHANGE;
        } else {
          return ev.type === EV_CLICK;
        }
      case "select":
      case "textarea":
        return ev.type === EV_CHANGE;
      default:
        return ev.type === EV_CLICK;
    }
  }
  function elementLooksSensitive(el) {
    var name5 = (el.name || el.id || "").toString().toLowerCase();
    if (typeof name5 === "string") {
      var sensitiveNameRegex = /^cc|cardnum|ccnum|creditcard|csc|cvc|cvv|exp|pass|pwd|routing|seccode|securitycode|securitynum|socialsec|socsec|ssn/i;
      if (sensitiveNameRegex.test(name5.replace(/[^a-zA-Z0-9]/g, ""))) {
        return true;
      }
    }
    return false;
  }
  function shouldTrackElementDetails(el, ev, allowElementCallback, allowSelectors) {
    var i3;
    if (!isElementAllowed(el, ev, allowElementCallback, allowSelectors)) {
      return false;
    }
    for (var curEl = el; curEl.parentNode && !isTag(curEl, "body"); curEl = curEl.parentNode) {
      var classes = getClasses(curEl);
      for (i3 = 0; i3 < SENSITIVE_DATA_CLASSES.length; i3++) {
        if (classes[SENSITIVE_DATA_CLASSES[i3]]) {
          return false;
        }
      }
    }
    var elClasses = getClasses(el);
    for (i3 = 0; i3 < OPT_IN_CLASSES.length; i3++) {
      if (elClasses[OPT_IN_CLASSES[i3]]) {
        return true;
      }
    }
    if (isTag(el, "input") || isTag(el, "select") || isTag(el, "textarea") || el.getAttribute("contenteditable") === "true") {
      return false;
    }
    var type = el.type || "";
    if (typeof type === "string") {
      switch (type.toLowerCase()) {
        case "hidden":
          return false;
        case "password":
          return false;
      }
    }
    if (elementLooksSensitive(el)) {
      return false;
    }
    return true;
  }
  function shouldTrackValue(value) {
    if (value === null || _3.isUndefined(value)) {
      return false;
    }
    if (typeof value === "string") {
      value = _3.trim(value);
      var ccRegex = /^(?:(4[0-9]{12}(?:[0-9]{3})?)|(5[1-5][0-9]{14})|(6(?:011|5[0-9]{2})[0-9]{12})|(3[47][0-9]{13})|(3(?:0[0-5]|[68][0-9])[0-9]{11})|((?:2131|1800|35[0-9]{3})[0-9]{11}))$/;
      if (ccRegex.test((value || "").replace(/[- ]/g, ""))) {
        return false;
      }
      var ssnRegex = /(^\d{3}-?\d{2}-?\d{4}$)/;
      if (ssnRegex.test(value)) {
        return false;
      }
    }
    return true;
  }
  function getPolyfillScrollEndFunction(originalFunction) {
    var supportsScrollEnd = "onscrollend" in win;
    var polyfillFunction = safewrap(originalFunction);
    var polyfillEvent = EV_SCROLLEND;
    if (!supportsScrollEnd) {
      var scrollTimer = null;
      var scrollDelayMs = 100;
      polyfillFunction = safewrap(function() {
        clearTimeout(scrollTimer);
        scrollTimer = setTimeout(originalFunction, scrollDelayMs);
      });
      polyfillEvent = EV_SCROLL;
    }
    return {
      listener: polyfillFunction,
      eventType: polyfillEvent
    };
  }
  function hasInlineEventHandlers(element) {
    for (var i3 = 0; i3 < EVENT_HANDLER_ATTRIBUTES.length; i3++) {
      if (element.hasAttribute(EVENT_HANDLER_ATTRIBUTES[i3])) {
        return true;
      }
    }
    return false;
  }
  function hasInteractiveAriaRole(element) {
    var role = element.getAttribute("role");
    if (!role) return false;
    var primaryRole = role.trim().split(/\s+/)[0].toLowerCase();
    return INTERACTIVE_ARIA_ROLES[primaryRole];
  }
  function hasAnyInteractivityIndicators(element) {
    var tagName = element.tagName.toLowerCase();
    if (tagName === "button" || tagName === "input" || tagName === "select" || tagName === "textarea" || tagName === "details" || tagName === "dialog") {
      return true;
    }
    if (element.isContentEditable) {
      return true;
    }
    if (element.onclick || element.onmousedown || element.onmouseup || element.ontouchstart || element.ontouchend) {
      return true;
    }
    if (hasInlineEventHandlers(element)) {
      return true;
    }
    if (hasInteractiveAriaRole(element)) {
      return true;
    }
    if (tagName === "a" && element.hasAttribute("href")) {
      return true;
    }
    if (element.hasAttribute("tabindex")) {
      return true;
    }
    return false;
  }
  function isDefinitelyNonInteractive(element) {
    if (!element || !element.tagName) {
      return true;
    }
    var tagName = element.tagName.toLowerCase();
    if (ALWAYS_NON_INTERACTIVE_TAGS[tagName]) {
      return true;
    }
    if (hasAnyInteractivityIndicators(element)) {
      return false;
    }
    var parent = element.parentElement;
    var depth = 0;
    while (parent && depth < MAX_DEPTH) {
      if (hasAnyInteractivityIndicators(parent)) {
        return false;
      }
      if (parent.getRootNode && parent.getRootNode() !== document$1) {
        var root2 = parent.getRootNode();
        if (root2.host && hasAnyInteractivityIndicators(root2.host)) {
          return false;
        }
      }
      parent = parent.parentElement;
      depth++;
    }
    if (TEXT_CONTAINER_TAGS[tagName]) {
      return true;
    }
    return false;
  }
  function getClickEventComposedPath(event) {
    if ("composedPath" in event) {
      return event["composedPath"]();
    }
    return [];
  }
  function getClickEventTargetElement(event) {
    var path = getClickEventComposedPath(event);
    if (path && path.length > 0) {
      return path[0];
    }
    return event["target"] || event["srcElement"];
  }
  var COMMON_MASK_CLASSES_SELECTOR = ".mp-mask, .fs-mask, .amp-mask, .rr-mask, .ph-mask";
  var RRWEB_PASSWORD_ATTRIBUTE = "data-rr-is-password";
  var ALWAYS_MASKED_INPUT_TYPES = ["password", "email", "tel", "hidden"];
  function normalizeSelectors(selector) {
    if (!selector) {
      return [];
    }
    if (Array.isArray(selector)) {
      return selector;
    }
    return [selector];
  }
  function getPrivacyConfig(mixpanelInstance) {
    var privacyConfig = {
      input: {
        maskingSelector: "",
        unmaskingSelector: "",
        maskAll: true
      },
      text: {
        maskingSelector: "",
        unmaskingSelector: "",
        maskAll: true
      }
    };
    var maskInputSelector = mixpanelInstance.get_config("record_mask_input_selector");
    var unmaskInputSelector = mixpanelInstance.get_config("record_unmask_input_selector");
    var maskAllInputs = mixpanelInstance.get_config("record_mask_all_inputs");
    privacyConfig.input.maskingSelector = normalizeSelectors(maskInputSelector).join(",");
    privacyConfig.input.unmaskingSelector = normalizeSelectors(unmaskInputSelector).join(",");
    if (maskAllInputs !== void 0) {
      privacyConfig.input.maskAll = maskAllInputs;
    }
    var maskTextSelector = mixpanelInstance.get_config("record_mask_text_selector");
    var unmaskTextSelector = mixpanelInstance.get_config("record_unmask_text_selector");
    var maskAllText = mixpanelInstance.get_config("record_mask_all_text");
    var legacyMaskTextClass = mixpanelInstance.get_config("record_mask_text_class");
    var textMaskingSelectors = normalizeSelectors(maskTextSelector);
    if (legacyMaskTextClass) {
      if (legacyMaskTextClass instanceof RegExp) {
        privacyConfig.text._legacyClassRegex = legacyMaskTextClass;
      } else {
        var classSelector = "." + legacyMaskTextClass;
        if (textMaskingSelectors.indexOf(classSelector) === -1) {
          textMaskingSelectors.push(classSelector);
        }
      }
    }
    privacyConfig.text.maskingSelector = textMaskingSelectors.join(",");
    privacyConfig.text.unmaskingSelector = normalizeSelectors(unmaskTextSelector).join(",");
    if (maskAllText === void 0 && maskTextSelector !== void 0) {
      privacyConfig.text.maskAll = false;
    } else if (maskAllText !== void 0) {
      privacyConfig.text.maskAll = maskAllText;
    }
    return privacyConfig;
  }
  function elementMatchesSelector(element, selector) {
    if (!selector) {
      return false;
    }
    return !!element.closest(selector);
  }
  function shouldMaskInput(element, privacyConfig) {
    var inputType = (element.getAttribute("type") || "").toLowerCase();
    if (ALWAYS_MASKED_INPUT_TYPES.indexOf(inputType) !== -1) {
      return true;
    }
    var autocomplete = (element.getAttribute("autocomplete") || "").toLowerCase();
    if (autocomplete && autocomplete !== "" && autocomplete !== "off") {
      return true;
    }
    if (element.hasAttribute(RRWEB_PASSWORD_ATTRIBUTE)) {
      return true;
    }
    if (elementLooksSensitive(element)) {
      return true;
    }
    if (privacyConfig.input.maskAll) {
      if (elementMatchesSelector(element, privacyConfig.input.unmaskingSelector)) {
        return false;
      }
      return true;
    } else {
      if (elementMatchesSelector(element, privacyConfig.input.maskingSelector)) {
        return true;
      }
      if (elementMatchesSelector(element, COMMON_MASK_CLASSES_SELECTOR)) {
        return true;
      }
      return false;
    }
  }
  function shouldMaskText(element, privacyConfig) {
    if (!element) {
      return false;
    }
    if (privacyConfig.text._legacyClassRegex) {
      if (classMatchesRegex(element, privacyConfig.text._legacyClassRegex)) {
        return true;
      }
    }
    if (privacyConfig.text.maskAll) {
      if (elementMatchesSelector(element, privacyConfig.text.unmaskingSelector)) {
        return false;
      }
      return true;
    } else {
      if (elementMatchesSelector(element, privacyConfig.text.maskingSelector)) {
        return true;
      }
      if (elementMatchesSelector(element, COMMON_MASK_CLASSES_SELECTOR)) {
        return true;
      }
      return false;
    }
  }
  var logger$2 = console_with_prefix("recorder");
  var CompressionStream = win["CompressionStream"];
  var RECORDER_BATCHER_LIB_CONFIG = {
    "batch_size": 1e3,
    "batch_flush_interval_ms": 10 * 1e3,
    "batch_request_timeout_ms": 90 * 1e3,
    "batch_autostart": true
  };
  var ACTIVE_SOURCES = /* @__PURE__ */ new Set([
    IncrementalSource.MouseMove,
    IncrementalSource.MouseInteraction,
    IncrementalSource.Scroll,
    IncrementalSource.ViewportResize,
    IncrementalSource.Input,
    IncrementalSource.TouchMove,
    IncrementalSource.MediaInteraction,
    IncrementalSource.Drag,
    IncrementalSource.Selection
  ]);
  function isUserEvent(ev) {
    return ev.type === EventType.IncrementalSnapshot && ACTIVE_SOURCES.has(ev.data.source);
  }
  var SessionRecording = function(options) {
    this._mixpanel = options.mixpanelInstance;
    this._onIdleTimeout = options.onIdleTimeout || NOOP_FUNC;
    this._onMaxLengthReached = options.onMaxLengthReached || NOOP_FUNC;
    this._onBatchSent = options.onBatchSent || NOOP_FUNC;
    this._rrwebRecord = options.rrwebRecord || null;
    this._stopRecording = null;
    this.replayId = options.replayId;
    this.batchStartUrl = options.batchStartUrl || null;
    this.replayStartUrl = options.replayStartUrl || null;
    this.idleExpires = options.idleExpires || null;
    this.maxExpires = options.maxExpires || null;
    this.replayStartTime = options.replayStartTime || null;
    this.lastEventTimestamp = options.lastEventTimestamp || null;
    this.seqNo = options.seqNo || 0;
    this.idleTimeoutId = null;
    this.maxTimeoutId = null;
    this.recordMaxMs = MAX_RECORDING_MS;
    this.recordMinMs = 0;
    var usePersistence = localStorageSupported(options.sharedLockStorage, true) && !this.getConfig("disable_persistence");
    this.batcherKey = "__mprec_" + this.getConfig("name") + "_" + this.getConfig("token") + "_" + this.replayId;
    this.queueStorage = new IDBStorageWrapper(RECORDING_EVENTS_STORE_NAME);
    this.batcher = new RequestBatcher(this.batcherKey, {
      errorReporter: this.reportError.bind(this),
      flushOnlyOnInterval: true,
      libConfig: RECORDER_BATCHER_LIB_CONFIG,
      sendRequestFunc: this.flushEventsWithOptOut.bind(this),
      queueStorage: this.queueStorage,
      sharedLockStorage: options.sharedLockStorage,
      usePersistence,
      stopAllBatchingFunc: this.stopRecording.bind(this),
      // increased throttle and shared lock timeout because recording events are very high frequency.
      // this will minimize the amount of lock contention between enqueued events.
      // for session recordings there is a lock for each tab anyway, so there's no risk of deadlock between tabs.
      enqueueThrottleMs: RECORD_ENQUEUE_THROTTLE_MS,
      sharedLockTimeoutMS: 10 * 1e3
    });
  };
  SessionRecording.prototype.getUserIdInfo = function() {
    if (this.finalFlushUserIdInfo) {
      return this.finalFlushUserIdInfo;
    }
    var userIdInfo = {
      "distinct_id": String(this._mixpanel.get_distinct_id())
    };
    var deviceId = this._mixpanel.get_property("$device_id");
    if (deviceId) {
      userIdInfo["$device_id"] = deviceId;
    }
    var userId = this._mixpanel.get_property("$user_id");
    if (userId) {
      userIdInfo["$user_id"] = userId;
    }
    return userIdInfo;
  };
  SessionRecording.prototype.unloadPersistedData = function() {
    this.batcher.stop();
    return this.queueStorage.init().catch(function() {
      this.reportError("Error initializing IndexedDB storage for unloading persisted data.");
    }.bind(this)).then(function() {
      if (this.getDurationMs() < this._getRecordMinMs()) {
        return this.queueStorage.removeItem(this.batcherKey);
      }
      return this.batcher.flush().then(function() {
        return this.queueStorage.removeItem(this.batcherKey);
      }.bind(this));
    }.bind(this));
  };
  SessionRecording.prototype.getConfig = function(configVar) {
    return this._mixpanel.get_config(configVar);
  };
  SessionRecording.prototype.get_config = function(configVar) {
    return this.getConfig(configVar);
  };
  SessionRecording.prototype.startRecording = function(shouldStopBatcher) {
    if (this._rrwebRecord === null) {
      this.reportError("rrweb record function not provided. ");
      return;
    }
    if (this._stopRecording !== null) {
      logger$2.log("Recording already in progress, skipping startRecording.");
      return;
    }
    this.recordMaxMs = this.getConfig("record_max_ms");
    if (this.recordMaxMs > MAX_RECORDING_MS) {
      this.recordMaxMs = MAX_RECORDING_MS;
      logger$2.critical("record_max_ms cannot be greater than " + MAX_RECORDING_MS + "ms. Capping value.");
    }
    if (!this.maxExpires) {
      this.maxExpires = (/* @__PURE__ */ new Date()).getTime() + this.recordMaxMs;
    }
    this.recordMinMs = this._getRecordMinMs();
    if (!this.replayStartTime) {
      this.replayStartTime = (/* @__PURE__ */ new Date()).getTime();
      this.batchStartUrl = _3.info.currentUrl();
      this.replayStartUrl = _3.info.currentUrl();
    }
    if (shouldStopBatcher || this.recordMinMs > 0) {
      this.batcher.stop();
    } else {
      this.batcher.start();
    }
    var resetIdleTimeout = function() {
      clearTimeout(this.idleTimeoutId);
      var idleTimeoutMs = this.getConfig("record_idle_timeout_ms");
      this.idleTimeoutId = setTimeout(this._onIdleTimeout, idleTimeoutMs);
      this.idleExpires = (/* @__PURE__ */ new Date()).getTime() + idleTimeoutMs;
    }.bind(this);
    resetIdleTimeout();
    var blockSelector = this.getConfig("record_block_selector");
    if (blockSelector === "" || blockSelector === null) {
      blockSelector = void 0;
    }
    var privacyConfig = getPrivacyConfig(this._mixpanel);
    try {
      this._stopRecording = this._rrwebRecord({
        "emit": function(ev) {
          if (this.idleExpires && this.idleExpires < ev.timestamp) {
            this._onIdleTimeout();
            return;
          }
          if (isUserEvent(ev)) {
            if (this.batcher.stopped && (/* @__PURE__ */ new Date()).getTime() - this.replayStartTime >= this.recordMinMs) {
              this.batcher.start();
            }
            resetIdleTimeout();
          }
          this.__enqueuePromise = this.batcher.enqueue(ev);
          if (this.lastEventTimestamp === null || ev.timestamp > this.lastEventTimestamp) {
            this.lastEventTimestamp = ev.timestamp;
          }
        }.bind(this),
        "blockClass": this.getConfig("record_block_class"),
        "blockSelector": blockSelector,
        "collectFonts": this.getConfig("record_collect_fonts"),
        "dataURLOptions": {
          // canvas image options (https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/toDataURL)
          "type": "image/webp",
          "quality": 0.6
        },
        // mask all inputs and text by default, unmasking is applied via callbacks maskInputFn and maskTextFn
        "maskAllInputs": true,
        "maskTextSelector": "*",
        "maskInputFn": this._getMaskFn(shouldMaskInput, privacyConfig),
        "maskTextFn": this._getMaskFn(shouldMaskText, privacyConfig),
        "recordCanvas": this.getConfig("record_canvas"),
        "sampling": {
          "canvas": 15
        },
        "plugins": this.getConfig("record_console") ? [
          getRecordConsolePlugin({
            stringifyOptions: {
              stringLengthLimit: 1e3,
              numOfKeysLimit: 50,
              depthOfLimit: 2
            }
          })
        ] : []
      });
    } catch (err) {
      this.reportError("Unexpected error when starting rrweb recording.", err);
    }
    if (typeof this._stopRecording !== "function") {
      this.reportError("rrweb failed to start, skipping this recording.");
      this._stopRecording = null;
      this.stopRecording();
      return;
    }
    var maxTimeoutMs = this.maxExpires - (/* @__PURE__ */ new Date()).getTime();
    this.maxTimeoutId = setTimeout(this._onMaxLengthReached.bind(this), maxTimeoutMs);
  };
  SessionRecording.prototype.stopRecording = function(skipFlush) {
    this.finalFlushUserIdInfo = this.getUserIdInfo();
    if (!this.isRrwebStopped()) {
      try {
        this._stopRecording();
      } catch (err) {
        this.reportError("Error with rrweb stopRecording", err);
      }
      this._stopRecording = null;
    }
    var flushPromise;
    if (this.batcher.stopped) {
      flushPromise = this.batcher.clear();
    } else if (!skipFlush) {
      flushPromise = this.batcher.flush();
    }
    this.batcher.stop();
    clearTimeout(this.idleTimeoutId);
    clearTimeout(this.maxTimeoutId);
    return flushPromise;
  };
  SessionRecording.prototype.isRrwebStopped = function() {
    return this._stopRecording === null;
  };
  SessionRecording.prototype.flushEventsWithOptOut = function(data, options, cb) {
    var onOptOut = function(code) {
      if (code === 0) {
        this.stopRecording();
        cb({ error: "Tracking has been opted out, stopping recording." });
      }
    }.bind(this);
    this._flushEvents(data, options, cb, onOptOut);
  };
  SessionRecording.prototype.serialize = function() {
    var tabId;
    try {
      tabId = this._mixpanel.get_tab_id();
    } catch (e3) {
      this.reportError("Error getting tab ID for serialization ", e3);
      tabId = null;
    }
    return {
      "replayId": this.replayId,
      "seqNo": this.seqNo,
      "replayStartTime": this.replayStartTime,
      "batchStartUrl": this.batchStartUrl,
      "replayStartUrl": this.replayStartUrl,
      "lastEventTimestamp": this.lastEventTimestamp,
      "idleExpires": this.idleExpires,
      "maxExpires": this.maxExpires,
      "tabId": tabId
    };
  };
  SessionRecording.deserialize = function(serializedRecording, options) {
    var recording2 = new SessionRecording(_3.extend({}, options, {
      replayId: serializedRecording["replayId"],
      batchStartUrl: serializedRecording["batchStartUrl"],
      replayStartUrl: serializedRecording["replayStartUrl"],
      idleExpires: serializedRecording["idleExpires"],
      maxExpires: serializedRecording["maxExpires"],
      replayStartTime: serializedRecording["replayStartTime"],
      lastEventTimestamp: serializedRecording["lastEventTimestamp"],
      seqNo: serializedRecording["seqNo"],
      sharedLockStorage: options.sharedLockStorage
    }));
    return recording2;
  };
  SessionRecording.prototype._sendRequest = function(currentReplayId, reqParams, reqBody, callback) {
    var onSuccess = function(response, responseBody) {
      if (response.status === 200 && this.replayId === currentReplayId) {
        this.seqNo++;
        this.batchStartUrl = _3.info.currentUrl();
      }
      this._onBatchSent();
      callback({
        status: 0,
        httpStatusCode: response.status,
        responseBody,
        retryAfter: response.headers.get("Retry-After")
      });
    }.bind(this);
    var apiHost = this._mixpanel.get_api_host && this._mixpanel.get_api_host("record") || this.getConfig("api_host");
    win["fetch"](apiHost + "/" + this.getConfig("api_routes")["record"] + "?" + new URLSearchParams(reqParams), {
      "method": "POST",
      "headers": {
        "Authorization": "Basic " + btoa(this.getConfig("token") + ":"),
        "Content-Type": "application/octet-stream"
      },
      "body": reqBody
    }).then(function(response) {
      response.json().then(function(responseBody) {
        onSuccess(response, responseBody);
      }).catch(function(error) {
        callback({ error });
      });
    }).catch(function(error) {
      callback({ error, httpStatusCode: 0 });
    });
  };
  SessionRecording.prototype._flushEvents = addOptOutCheckMixpanelLib(function(data, options, callback) {
    var numEvents = data.length;
    if (numEvents > 0) {
      var replayId = this.replayId;
      var batchStartTime = Infinity;
      var batchEndTime = -Infinity;
      var hasFullSnapshot = false;
      for (var i3 = 0; i3 < numEvents; i3++) {
        batchStartTime = Math.min(batchStartTime, data[i3].timestamp);
        batchEndTime = Math.max(batchEndTime, data[i3].timestamp);
        if (data[i3].type === EventType.FullSnapshot) {
          hasFullSnapshot = true;
        }
      }
      if (this.seqNo === 0) {
        if (!hasFullSnapshot) {
          callback({ error: "First batch does not contain a full snapshot. Aborting recording." });
          this.stopRecording(true);
          return;
        }
        this.replayStartTime = batchStartTime;
      } else if (!this.replayStartTime) {
        this.reportError("Replay start time not set but seqNo is not 0. Using current batch start time as a fallback.");
        this.replayStartTime = batchStartTime;
      }
      var replayLengthMs = batchEndTime - this.replayStartTime;
      var reqParams = {
        "$current_url": this.batchStartUrl,
        "$lib_version": Config.LIB_VERSION,
        "batch_start_time": batchStartTime / 1e3,
        "mp_lib": "web",
        "replay_id": replayId,
        "replay_length_ms": replayLengthMs,
        "replay_start_time": this.replayStartTime / 1e3,
        "replay_start_url": this.replayStartUrl,
        "seq": this.seqNo
      };
      var eventsJson = JSON.stringify(data);
      Object.assign(reqParams, this.getUserIdInfo());
      if (canUseCompressionStream(userAgent, navigator2.vendor, windowOpera)) {
        var jsonStream = new Blob([eventsJson], { type: "application/json" }).stream();
        var gzipStream = jsonStream.pipeThrough(new CompressionStream("gzip"));
        new Response(gzipStream).blob().then(function(compressedBlob) {
          reqParams["format"] = "gzip";
          this._sendRequest(replayId, reqParams, compressedBlob, callback);
        }.bind(this));
      } else {
        reqParams["format"] = "body";
        this._sendRequest(replayId, reqParams, eventsJson, callback);
      }
    }
  });
  SessionRecording.prototype.reportError = function(msg, err) {
    logger$2.error.apply(logger$2.error, arguments);
    try {
      if (!err && !(msg instanceof Error)) {
        msg = new Error(msg);
      }
      this.getConfig("error_reporter")(msg, err);
    } catch (err2) {
      logger$2.error(err2);
    }
  };
  SessionRecording.prototype.getDurationMs = function() {
    if (this.replayStartTime === null) {
      return 0;
    }
    if (this.lastEventTimestamp === null) {
      return (/* @__PURE__ */ new Date()).getTime() - this.replayStartTime;
    }
    return this.lastEventTimestamp - this.replayStartTime;
  };
  SessionRecording.prototype._getRecordMinMs = function() {
    var configValue = this.getConfig("record_min_ms");
    if (configValue > MAX_VALUE_FOR_MIN_RECORDING_MS) {
      logger$2.critical("record_min_ms cannot be greater than " + MAX_VALUE_FOR_MIN_RECORDING_MS + "ms. Capping value.");
      return MAX_VALUE_FOR_MIN_RECORDING_MS;
    }
    return configValue;
  };
  SessionRecording.prototype._getMaskFn = function(shouldMaskFn, privacyConfig) {
    return function(text, element) {
      if (!text.trim().length) {
        return "";
      }
      var shouldMask = true;
      try {
        shouldMask = shouldMaskFn(element, privacyConfig);
      } catch (err) {
        this.reportError("Error checking if text should be masked, defaulting to masked", err);
      }
      if (shouldMask) {
        var textLength = Math.min(text.length, 1e4);
        return "*".repeat(textLength);
      } else {
        return text;
      }
    }.bind(this);
  };
  var RecordingRegistry = function(options) {
    this.idb = new IDBStorageWrapper(RECORDING_REGISTRY_STORE_NAME);
    this.errorReporter = options.errorReporter;
    this.mixpanelInstance = options.mixpanelInstance;
    this.sharedLockStorage = options.sharedLockStorage;
  };
  RecordingRegistry.prototype.isPersistenceEnabled = function() {
    return !this.mixpanelInstance.get_config("disable_persistence");
  };
  RecordingRegistry.prototype.handleError = function(err) {
    this.errorReporter("IndexedDB error: ", err);
  };
  RecordingRegistry.prototype.setActiveRecording = function(serializedRecording) {
    if (!this.isPersistenceEnabled()) {
      return PromisePolyfill.resolve();
    }
    var tabId = serializedRecording["tabId"];
    if (!tabId) {
      console.warn("No tab ID is set, cannot persist recording metadata.");
      return PromisePolyfill.resolve();
    }
    return this.idb.init().then(function() {
      return this.idb.setItem(tabId, serializedRecording);
    }.bind(this)).catch(this.handleError.bind(this));
  };
  RecordingRegistry.prototype.getActiveRecording = function() {
    if (!this.isPersistenceEnabled()) {
      return PromisePolyfill.resolve(null);
    }
    return this.idb.init().then(function() {
      return this.idb.getItem(this.mixpanelInstance.get_tab_id());
    }.bind(this)).then(function(serializedRecording) {
      return isRecordingExpired(serializedRecording) ? null : serializedRecording;
    }.bind(this)).catch(this.handleError.bind(this));
  };
  RecordingRegistry.prototype.clearActiveRecording = function() {
    if (this.isPersistenceEnabled()) {
      return this.markActiveRecordingExpired();
    } else {
      return this.deleteActiveRecording();
    }
  };
  RecordingRegistry.prototype.markActiveRecordingExpired = function() {
    return this.getActiveRecording().then(function(serializedRecording) {
      if (serializedRecording) {
        serializedRecording["maxExpires"] = 0;
        return this.setActiveRecording(serializedRecording);
      }
    }.bind(this)).catch(this.handleError.bind(this));
  };
  RecordingRegistry.prototype.deleteActiveRecording = function() {
    if (this.idb.isInitialized()) {
      return this.idb.removeItem(this.mixpanelInstance.get_tab_id()).catch(this.handleError.bind(this));
    } else {
      return PromisePolyfill.resolve();
    }
  };
  RecordingRegistry.prototype.flushInactiveRecordings = function() {
    if (!this.isPersistenceEnabled()) {
      return PromisePolyfill.resolve([]);
    }
    return this.idb.init().then(function() {
      return this.idb.getAll();
    }.bind(this)).then(function(serializedRecordings) {
      var unloadPromises = serializedRecordings.filter(function(serializedRecording) {
        return isRecordingExpired(serializedRecording);
      }).map(function(serializedRecording) {
        var sessionRecording = SessionRecording.deserialize(serializedRecording, {
          mixpanelInstance: this.mixpanelInstance,
          sharedLockStorage: this.sharedLockStorage
        });
        return sessionRecording.unloadPersistedData().then(function() {
          return this.idb.removeItem(serializedRecording["tabId"]);
        }.bind(this)).catch(this.handleError.bind(this));
      }.bind(this));
      return PromisePolyfill.all(unloadPromises);
    }.bind(this)).catch(this.handleError.bind(this));
  };
  var logger$1 = console_with_prefix("recorder");
  var MixpanelRecorder = function(mixpanelInstance, rrwebRecord, sharedLockStorage) {
    this.mixpanelInstance = mixpanelInstance;
    this.rrwebRecord = rrwebRecord || record;
    this.sharedLockStorage = sharedLockStorage;
    this.recordingRegistry = new RecordingRegistry({
      mixpanelInstance: this.mixpanelInstance,
      errorReporter: logger$1.error,
      sharedLockStorage
    });
    this._flushInactivePromise = this.recordingRegistry.flushInactiveRecordings();
    this.activeRecording = null;
    this.stopRecordingInProgress = false;
  };
  MixpanelRecorder.prototype.startRecording = function(options) {
    options = options || {};
    if (this.activeRecording && !this.activeRecording.isRrwebStopped()) {
      logger$1.log("Recording already in progress, skipping startRecording.");
      return;
    }
    var onIdleTimeout = function() {
      logger$1.log("Idle timeout reached, restarting recording.");
      this.resetRecording();
    }.bind(this);
    var onMaxLengthReached = function() {
      logger$1.log("Max recording length reached, stopping recording.");
      this.resetRecording();
    }.bind(this);
    var onBatchSent = function() {
      this.recordingRegistry.setActiveRecording(this.activeRecording.serialize());
      this["__flushPromise"] = this.activeRecording.batcher._flushPromise;
    }.bind(this);
    var sessionRecordingOptions = {
      mixpanelInstance: this.mixpanelInstance,
      onBatchSent,
      onIdleTimeout,
      onMaxLengthReached,
      replayId: _3.UUID(),
      rrwebRecord: this.rrwebRecord,
      sharedLockStorage: this.sharedLockStorage
    };
    if (options.activeSerializedRecording) {
      this.activeRecording = SessionRecording.deserialize(options.activeSerializedRecording, sessionRecordingOptions);
    } else {
      this.activeRecording = new SessionRecording(sessionRecordingOptions);
    }
    this.activeRecording.startRecording(options.shouldStopBatcher);
    return this.recordingRegistry.setActiveRecording(this.activeRecording.serialize());
  };
  MixpanelRecorder.prototype.stopRecording = function() {
    this.stopRecordingInProgress = true;
    return this._stopCurrentRecording(false, true).then(function() {
      return this.recordingRegistry.clearActiveRecording();
    }.bind(this)).then(function() {
      this.stopRecordingInProgress = false;
    }.bind(this));
  };
  MixpanelRecorder.prototype.pauseRecording = function() {
    return this._stopCurrentRecording(false);
  };
  MixpanelRecorder.prototype._stopCurrentRecording = function(skipFlush, disableActiveRecording) {
    if (this.activeRecording) {
      var stopRecordingPromise = this.activeRecording.stopRecording(skipFlush);
      if (disableActiveRecording) {
        this.activeRecording = null;
      }
      return stopRecordingPromise;
    }
    return PromisePolyfill.resolve();
  };
  MixpanelRecorder.prototype.resumeRecording = function(startNewIfInactive) {
    if (this.activeRecording && this.activeRecording.isRrwebStopped()) {
      this.activeRecording.startRecording(false);
      return PromisePolyfill.resolve(null);
    }
    return this.recordingRegistry.getActiveRecording().then(function(activeSerializedRecording) {
      if (activeSerializedRecording && !this.stopRecordingInProgress) {
        return this.startRecording({ activeSerializedRecording });
      } else if (startNewIfInactive) {
        return this.startRecording({ shouldStopBatcher: false });
      } else {
        logger$1.log("No resumable recording found.");
        return null;
      }
    }.bind(this));
  };
  MixpanelRecorder.prototype.resetRecording = function() {
    this.stopRecording();
    this.startRecording({ shouldStopBatcher: true });
  };
  MixpanelRecorder.prototype.getActiveReplayId = function() {
    if (this.activeRecording && !this.activeRecording.isRrwebStopped()) {
      return this.activeRecording.replayId;
    } else {
      return null;
    }
  };
  Object.defineProperty(MixpanelRecorder.prototype, "replayId", {
    get: function() {
      return this.getActiveReplayId();
    }
  });
  win["__mp_recorder"] = MixpanelRecorder;
  var DEFAULT_RAGE_CLICK_THRESHOLD_PX = 30;
  var DEFAULT_RAGE_CLICK_TIMEOUT_MS = 1e3;
  var DEFAULT_RAGE_CLICK_CLICK_COUNT = 4;
  var DEFAULT_RAGE_CLICK_INTERACTIVE_ELEMENTS_ONLY = false;
  function RageClickTracker() {
    this.clicks = [];
  }
  RageClickTracker.prototype.isRageClick = function(event, options) {
    options = options || {};
    var thresholdPx = options["threshold_px"] || DEFAULT_RAGE_CLICK_THRESHOLD_PX;
    var timeoutMs = options["timeout_ms"] || DEFAULT_RAGE_CLICK_TIMEOUT_MS;
    var clickCount = options["click_count"] || DEFAULT_RAGE_CLICK_CLICK_COUNT;
    var interactiveElementsOnly = options["interactive_elements_only"] || DEFAULT_RAGE_CLICK_INTERACTIVE_ELEMENTS_ONLY;
    if (interactiveElementsOnly) {
      var target = getClickEventTargetElement(event);
      if (!target || isDefinitelyNonInteractive(target)) {
        return false;
      }
    }
    var timestamp = Date.now();
    var x5 = event["pageX"], y3 = event["pageY"];
    var lastClick = this.clicks[this.clicks.length - 1];
    if (lastClick && timestamp - lastClick.timestamp < timeoutMs && Math.sqrt(Math.pow(x5 - lastClick.x, 2) + Math.pow(y3 - lastClick.y, 2)) < thresholdPx) {
      this.clicks.push({ x: x5, y: y3, timestamp });
      if (this.clicks.length >= clickCount) {
        this.clicks = [];
        return true;
      }
    } else {
      this.clicks = [{ x: x5, y: y3, timestamp }];
    }
    return false;
  };
  function ShadowDOMObserver(changeCallback, observerConfig) {
    this.changeCallback = changeCallback || function() {
    };
    this.observerConfig = observerConfig;
    this.observedShadowRoots = null;
    this.shadowObservers = [];
  }
  ShadowDOMObserver.prototype.getEventTarget = function(event) {
    if (!this.observedShadowRoots) {
      return;
    }
    return getClickEventTargetElement(event);
  };
  ShadowDOMObserver.prototype.observeFromEvent = function(event) {
    if (!this.observedShadowRoots) {
      return;
    }
    var path = getClickEventComposedPath(event);
    for (var i3 = 0; i3 < path.length; i3++) {
      var element = path[i3];
      if (element && element.shadowRoot) {
        this.observeShadowRoot(element.shadowRoot);
      }
    }
  };
  ShadowDOMObserver.prototype.observeShadowRoot = function(shadowRoot2) {
    if (!this.observedShadowRoots || this.observedShadowRoots.has(shadowRoot2)) {
      return;
    }
    var self2 = this;
    try {
      this.observedShadowRoots.add(shadowRoot2);
      var observer = new window.MutationObserver(function() {
        self2.changeCallback();
      });
      observer.observe(shadowRoot2, this.observerConfig);
      this.shadowObservers.push(observer);
    } catch (e3) {
      logger$3.critical("Error while observing shadow root", e3);
    }
  };
  ShadowDOMObserver.prototype.start = function() {
    if (this.observedShadowRoots) {
      return;
    }
    if (!weakSetSupported()) {
      logger$3.critical("Shadow DOM observation unavailable: WeakSet not supported");
      return;
    }
    this.observedShadowRoots = /* @__PURE__ */ new WeakSet();
  };
  ShadowDOMObserver.prototype.stop = function() {
    if (!this.observedShadowRoots) {
      return;
    }
    for (var i3 = 0; i3 < this.shadowObservers.length; i3++) {
      try {
        this.shadowObservers[i3].disconnect();
      } catch (e3) {
        logger$3.critical("Error while disconnecting shadow DOM observer", e3);
      }
    }
    this.shadowObservers = [];
    this.observedShadowRoots = null;
  };
  var DEFAULT_DEAD_CLICK_TIMEOUT_MS = 500;
  var INTERACTION_EVENTS = [EV_CHANGE, EV_INPUT, EV_SUBMIT, EV_SELECT, EV_TOGGLE];
  var LAYOUT_EVENTS = [EV_SCROLLEND];
  var NAVIGATION_EVENTS = [EV_HASHCHANGE];
  var MUTATION_OBSERVER_CONFIG = {
    characterData: true,
    childList: true,
    subtree: true,
    attributes: true,
    attributeFilter: ["style", "class", "hidden", "checked", "selected", "value", "display", "visibility"]
  };
  function DeadClickTracker(onDeadClickCallback) {
    this.eventListeners = [];
    this.mutationObserver = null;
    this.shadowDOMObserver = null;
    this.isTracking = false;
    this.lastChangeEventTimestamp = 0;
    this.pendingClicks = [];
    this.onDeadClickCallback = onDeadClickCallback;
    this.processingActive = false;
    this.processingTimeout = null;
  }
  DeadClickTracker.prototype.addClick = function(event) {
    var element = this.shadowDOMObserver && this.shadowDOMObserver.getEventTarget(event);
    if (!element) {
      element = event["target"] || event["srcElement"];
    }
    if (!element || isDefinitelyNonInteractive(element)) {
      return false;
    }
    if (this.shadowDOMObserver) {
      this.shadowDOMObserver.observeFromEvent(event);
    }
    this.pendingClicks.push({
      element,
      event,
      timestamp: Date.now()
    });
    return true;
  };
  DeadClickTracker.prototype.trackClick = function(event, config) {
    if (!this.isTracking) {
      return false;
    }
    var added = this.addClick(event);
    if (added) {
      this.triggerProcessing(config);
    }
    return added;
  };
  DeadClickTracker.prototype.getDeadClicks = function(config) {
    if (this.pendingClicks.length === 0) {
      return [];
    }
    var timeoutMs = config["timeout_ms"];
    var now = Date.now();
    var clicksToEvaluate = this.pendingClicks.slice();
    this.pendingClicks = [];
    var deadClicks = [];
    for (var i3 = 0; i3 < clicksToEvaluate.length; i3++) {
      var click = clicksToEvaluate[i3];
      if (now - click.timestamp >= timeoutMs) {
        if (!this.hasChangesAfter(click.timestamp)) {
          deadClicks.push(click);
        }
      } else {
        this.pendingClicks.push(click);
      }
    }
    return deadClicks;
  };
  DeadClickTracker.prototype.hasChangesAfter = function(timestamp) {
    return this.lastChangeEventTimestamp >= timestamp - 100;
  };
  DeadClickTracker.prototype.recordChangeEvent = function() {
    this.lastChangeEventTimestamp = Date.now();
  };
  DeadClickTracker.prototype.triggerProcessing = function(config) {
    if (this.processingActive) {
      return;
    }
    this.processingActive = true;
    this.processRecursively(config);
  };
  DeadClickTracker.prototype.processRecursively = function(config) {
    if (!this.isTracking || !this.onDeadClickCallback) {
      this.processingActive = false;
      return;
    }
    var timeoutMs = config["timeout_ms"];
    var self2 = this;
    this.processingTimeout = setTimeout(function() {
      if (!self2.processingActive) {
        return;
      }
      var deadClicks = self2.getDeadClicks(config);
      for (var i3 = 0; i3 < deadClicks.length; i3++) {
        self2.onDeadClickCallback(deadClicks[i3].event);
      }
      if (self2.pendingClicks.length > 0) {
        self2.processRecursively(config);
      } else {
        self2.processingActive = false;
      }
    }, timeoutMs);
  };
  DeadClickTracker.prototype.startTracking = function() {
    if (this.isTracking) {
      return;
    }
    this.isTracking = true;
    var self2 = this;
    INTERACTION_EVENTS.forEach(function(event) {
      var handler = function() {
        self2.recordChangeEvent();
      };
      document.addEventListener(event, handler, { capture: true, passive: true });
      self2.eventListeners.push({ target: document, event, handler, options: { capture: true, passive: true } });
    });
    NAVIGATION_EVENTS.forEach(function(event) {
      var handler = function() {
        self2.recordChangeEvent();
      };
      window.addEventListener(event, handler);
      self2.eventListeners.push({ target: window, event, handler });
    });
    LAYOUT_EVENTS.forEach(function(event) {
      var handler = function() {
        self2.recordChangeEvent();
      };
      window.addEventListener(event, handler, { passive: true });
      self2.eventListeners.push({ target: window, event, handler, options: { passive: true } });
    });
    var selectionHandler = function() {
      self2.recordChangeEvent();
    };
    document.addEventListener("selectionchange", selectionHandler);
    self2.eventListeners.push({ target: document, event: "selectionchange", handler: selectionHandler });
    if (window.MutationObserver) {
      try {
        this.mutationObserver = new window.MutationObserver(function() {
          self2.recordChangeEvent();
        });
        this.mutationObserver.observe(document.body || document.documentElement, MUTATION_OBSERVER_CONFIG);
      } catch (e3) {
        logger$3.critical("Error while setting up mutation observer", e3);
      }
    }
    if (window.customElements) {
      try {
        this.shadowDOMObserver = new ShadowDOMObserver(
          function() {
            self2.recordChangeEvent();
          },
          MUTATION_OBSERVER_CONFIG
        );
        this.shadowDOMObserver.start();
      } catch (e3) {
        logger$3.critical("Error while setting up shadow DOM observer", e3);
        this.shadowDOMObserver = null;
      }
    }
  };
  DeadClickTracker.prototype.stopTracking = function() {
    if (!this.isTracking) {
      return;
    }
    this.isTracking = false;
    this.pendingClicks = [];
    this.lastChangeEventTimestamp = 0;
    this.processingActive = false;
    if (this.processingTimeout) {
      clearTimeout(this.processingTimeout);
      this.processingTimeout = null;
    }
    for (var i3 = 0; i3 < this.eventListeners.length; i3++) {
      var listener = this.eventListeners[i3];
      try {
        listener.target.removeEventListener(listener.event, listener.handler, listener.options);
      } catch (e3) {
        logger$3.critical("Error while removing event listener", e3);
      }
    }
    this.eventListeners = [];
    if (this.mutationObserver) {
      try {
        this.mutationObserver.disconnect();
      } catch (e3) {
        logger$3.critical("Error while disconnecting mutation observer", e3);
      }
      this.mutationObserver = null;
    }
    if (this.shadowDOMObserver) {
      try {
        this.shadowDOMObserver.stop();
      } catch (e3) {
        logger$3.critical("Error while stopping shadow DOM observer", e3);
      }
      this.shadowDOMObserver = null;
    }
  };
  var AUTOCAPTURE_CONFIG_KEY = "autocapture";
  var LEGACY_PAGEVIEW_CONFIG_KEY = "track_pageview";
  var PAGEVIEW_OPTION_FULL_URL = "full-url";
  var PAGEVIEW_OPTION_URL_WITH_PATH_AND_QUERY_STRING = "url-with-path-and-query-string";
  var PAGEVIEW_OPTION_URL_WITH_PATH = "url-with-path";
  var CONFIG_ALLOW_ELEMENT_CALLBACK = "allow_element_callback";
  var CONFIG_ALLOW_SELECTORS = "allow_selectors";
  var CONFIG_ALLOW_URL_REGEXES = "allow_url_regexes";
  var CONFIG_BLOCK_ATTRS = "block_attrs";
  var CONFIG_BLOCK_ELEMENT_CALLBACK = "block_element_callback";
  var CONFIG_BLOCK_SELECTORS = "block_selectors";
  var CONFIG_BLOCK_URL_REGEXES = "block_url_regexes";
  var CONFIG_CAPTURE_EXTRA_ATTRS = "capture_extra_attrs";
  var CONFIG_CAPTURE_TEXT_CONTENT = "capture_text_content";
  var CONFIG_SCROLL_CAPTURE_ALL = "scroll_capture_all";
  var CONFIG_SCROLL_CHECKPOINTS = "scroll_depth_percent_checkpoints";
  var CONFIG_TRACK_CLICK = "click";
  var CONFIG_TRACK_DEAD_CLICK = "dead_click";
  var CONFIG_TRACK_INPUT = "input";
  var CONFIG_TRACK_PAGEVIEW = "pageview";
  var CONFIG_TRACK_RAGE_CLICK = "rage_click";
  var CONFIG_TRACK_SCROLL = "scroll";
  var CONFIG_TRACK_PAGE_LEAVE = "page_leave";
  var CONFIG_TRACK_SUBMIT = "submit";
  var CONFIG_DEFAULTS$1 = {};
  CONFIG_DEFAULTS$1[CONFIG_ALLOW_SELECTORS] = [];
  CONFIG_DEFAULTS$1[CONFIG_ALLOW_URL_REGEXES] = [];
  CONFIG_DEFAULTS$1[CONFIG_BLOCK_ATTRS] = [];
  CONFIG_DEFAULTS$1[CONFIG_BLOCK_ELEMENT_CALLBACK] = null;
  CONFIG_DEFAULTS$1[CONFIG_BLOCK_SELECTORS] = [];
  CONFIG_DEFAULTS$1[CONFIG_BLOCK_URL_REGEXES] = [];
  CONFIG_DEFAULTS$1[CONFIG_CAPTURE_EXTRA_ATTRS] = [];
  CONFIG_DEFAULTS$1[CONFIG_CAPTURE_TEXT_CONTENT] = false;
  CONFIG_DEFAULTS$1[CONFIG_SCROLL_CAPTURE_ALL] = false;
  CONFIG_DEFAULTS$1[CONFIG_SCROLL_CHECKPOINTS] = [25, 50, 75, 100];
  CONFIG_DEFAULTS$1[CONFIG_TRACK_CLICK] = true;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_DEAD_CLICK] = true;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_INPUT] = true;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_PAGEVIEW] = PAGEVIEW_OPTION_FULL_URL;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_RAGE_CLICK] = true;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_SCROLL] = true;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_PAGE_LEAVE] = false;
  CONFIG_DEFAULTS$1[CONFIG_TRACK_SUBMIT] = true;
  var DEFAULT_PROPS = {
    "$mp_autocapture": true
  };
  var MP_EV_CLICK = "$mp_click";
  var MP_EV_DEAD_CLICK = "$mp_dead_click";
  var MP_EV_INPUT = "$mp_input_change";
  var MP_EV_RAGE_CLICK = "$mp_rage_click";
  var MP_EV_SCROLL = "$mp_scroll";
  var MP_EV_SUBMIT = "$mp_submit";
  var MP_EV_PAGE_LEAVE = "$mp_page_leave";
  var Autocapture = function(mp) {
    this.mp = mp;
    this.maxScrollViewDepth = 0;
    this.hasTrackedScrollSession = false;
    this.previousScrollHeight = 0;
  };
  Autocapture.prototype.init = function() {
    if (!minDOMApisSupported()) {
      logger$3.critical("Autocapture unavailable: missing required DOM APIs");
      return;
    }
    this.initPageListeners();
    this.initPageviewTracking();
    this.initClickTracking();
    this.initDeadClickTracking();
    this.initInputTracking();
    this.initScrollTracking();
    this.initSubmitTracking();
    this.initRageClickTracking();
    this.initPageLeaveTracking();
  };
  Autocapture.prototype.getFullConfig = function() {
    var autocaptureConfig = this.mp.get_config(AUTOCAPTURE_CONFIG_KEY);
    if (!autocaptureConfig) {
      return {};
    } else if (_3.isObject(autocaptureConfig)) {
      return _3.extend({}, CONFIG_DEFAULTS$1, autocaptureConfig);
    } else {
      return CONFIG_DEFAULTS$1;
    }
  };
  Autocapture.prototype.getConfig = function(key) {
    return this.getFullConfig()[key];
  };
  Autocapture.prototype.currentUrlBlocked = function() {
    var i3;
    var currentUrl = _3.info.currentUrl();
    var allowUrlRegexes = this.getConfig(CONFIG_ALLOW_URL_REGEXES) || [];
    if (allowUrlRegexes.length) {
      var allowed = false;
      for (i3 = 0; i3 < allowUrlRegexes.length; i3++) {
        var allowRegex = allowUrlRegexes[i3];
        try {
          if (currentUrl.match(allowRegex)) {
            allowed = true;
            break;
          }
        } catch (err) {
          logger$3.critical("Error while checking block URL regex: " + allowRegex, err);
          return true;
        }
      }
      if (!allowed) {
        return true;
      }
    }
    var blockUrlRegexes = this.getConfig(CONFIG_BLOCK_URL_REGEXES) || [];
    if (!blockUrlRegexes || !blockUrlRegexes.length) {
      return false;
    }
    for (i3 = 0; i3 < blockUrlRegexes.length; i3++) {
      try {
        if (currentUrl.match(blockUrlRegexes[i3])) {
          return true;
        }
      } catch (err) {
        logger$3.critical("Error while checking block URL regex: " + blockUrlRegexes[i3], err);
        return true;
      }
    }
    return false;
  };
  Autocapture.prototype.pageviewTrackingConfig = function() {
    if (this.mp.get_config(AUTOCAPTURE_CONFIG_KEY)) {
      return this.getConfig(CONFIG_TRACK_PAGEVIEW);
    } else {
      return this.mp.get_config(LEGACY_PAGEVIEW_CONFIG_KEY);
    }
  };
  Autocapture.prototype.trackDomEvent = function(ev, mpEventName) {
    if (this.currentUrlBlocked()) {
      return;
    }
    var isCapturedForHeatMap = this.mp.is_recording_heatmap_data() && (mpEventName === MP_EV_CLICK && !this.getConfig(CONFIG_TRACK_CLICK) || mpEventName === MP_EV_RAGE_CLICK && !this._getClickTrackingConfig(CONFIG_TRACK_RAGE_CLICK) || mpEventName === MP_EV_DEAD_CLICK && !this._getClickTrackingConfig(CONFIG_TRACK_DEAD_CLICK));
    var props = getPropsForDOMEvent(ev, {
      allowElementCallback: this.getConfig(CONFIG_ALLOW_ELEMENT_CALLBACK),
      allowSelectors: this.getConfig(CONFIG_ALLOW_SELECTORS),
      blockAttrs: this.getConfig(CONFIG_BLOCK_ATTRS),
      blockElementCallback: this.getConfig(CONFIG_BLOCK_ELEMENT_CALLBACK),
      blockSelectors: this.getConfig(CONFIG_BLOCK_SELECTORS),
      captureExtraAttrs: this.getConfig(CONFIG_CAPTURE_EXTRA_ATTRS),
      captureTextContent: this.getConfig(CONFIG_CAPTURE_TEXT_CONTENT),
      capturedForHeatMap: isCapturedForHeatMap
    });
    if (props) {
      _3.extend(props, DEFAULT_PROPS);
      this.mp.track(mpEventName, props);
    }
  };
  Autocapture.prototype.initPageListeners = function() {
    win.removeEventListener(EV_POPSTATE, this.listenerPopstate);
    win.removeEventListener(EV_HASHCHANGE, this.listenerHashchange);
    if (!this.pageviewTrackingConfig() && !this.getConfig(CONFIG_TRACK_PAGE_LEAVE) && !this.mp.get_config("record_heatmap_data")) {
      return;
    }
    this.listenerPopstate = function() {
      win.dispatchEvent(new Event(EV_MP_LOCATION_CHANGE));
    };
    this.listenerHashchange = function() {
      win.dispatchEvent(new Event(EV_MP_LOCATION_CHANGE));
    };
    win.addEventListener(EV_POPSTATE, this.listenerPopstate);
    win.addEventListener(EV_HASHCHANGE, this.listenerHashchange);
    var nativePushState = win.history.pushState;
    if (typeof nativePushState === "function") {
      win.history.pushState = function(state2, unused, url) {
        nativePushState.call(win.history, state2, unused, url);
        win.dispatchEvent(new Event(EV_MP_LOCATION_CHANGE));
      };
    }
    var nativeReplaceState = win.history.replaceState;
    if (typeof nativeReplaceState === "function") {
      win.history.replaceState = function(state2, unused, url) {
        nativeReplaceState.call(win.history, state2, unused, url);
        win.dispatchEvent(new Event(EV_MP_LOCATION_CHANGE));
      };
    }
  };
  Autocapture.prototype._getClickTrackingConfig = function(configKey) {
    var config = this.getConfig(configKey);
    if (!config) {
      return null;
    }
    if (config === true) {
      return {};
    }
    if (typeof config === "object") {
      return config;
    }
    return {};
  };
  Autocapture.prototype._trackPageLeave = function(ev, currentUrl, currentScrollHeight) {
    if (this.hasTrackedScrollSession) {
      return;
    }
    if (!this.getConfig(CONFIG_TRACK_PAGE_LEAVE) && !this.mp.is_recording_heatmap_data()) {
      return;
    }
    this.hasTrackedScrollSession = true;
    var viewportHeight = Math.max(document$1.documentElement.clientHeight, win.innerHeight || 0);
    var scrollPercentage = Math.round(Math.max(this.maxScrollViewDepth - viewportHeight, 0) / (currentScrollHeight - viewportHeight) * 100);
    var foldLinePercentage = Math.round(viewportHeight / currentScrollHeight * 100);
    if (currentScrollHeight <= viewportHeight) {
      scrollPercentage = 100;
      foldLinePercentage = 100;
    }
    var props = _3.extend({
      "$max_scroll_view_depth": this.maxScrollViewDepth,
      "$max_scroll_percentage": scrollPercentage,
      "$fold_line_percentage": foldLinePercentage,
      "$scroll_height": currentScrollHeight,
      "$event_type": ev.type,
      "$current_url": currentUrl || _3.info.currentUrl(),
      "$viewportHeight": viewportHeight,
      // This is the fold line
      "$viewportWidth": Math.max(document$1.documentElement.clientWidth, win.innerWidth || 0),
      "$captured_for_heatmap": this.mp.is_recording_heatmap_data()
    }, DEFAULT_PROPS);
    this.mp.track(MP_EV_PAGE_LEAVE, props, { transport: "sendBeacon" });
  };
  Autocapture.prototype._initScrollDepthTracking = function() {
    win.removeEventListener(EV_SCROLL, this.listenerScrollDepth);
    win.removeEventListener(EV_SCROLLEND, this.listenerScrollDepth);
    if (!this.mp.get_config("record_heatmap_data")) {
      return;
    }
    logger$3.log("Initializing scroll depth tracking");
    this.maxScrollViewDepth = Math.max(document$1.documentElement.clientHeight, win.innerHeight || 0);
    var updateScrollDepth = function() {
      if (this.currentUrlBlocked()) {
        return;
      }
      var scrollViewHeight = Math.max(document$1.documentElement.clientHeight, win.innerHeight || 0) + win.scrollY;
      if (scrollViewHeight > this.maxScrollViewDepth) {
        this.maxScrollViewDepth = scrollViewHeight;
      }
      this.previousScrollHeight = document$1.body.scrollHeight;
    }.bind(this);
    var scrollEndPolyfill = getPolyfillScrollEndFunction(updateScrollDepth);
    this.listenerScrollDepth = scrollEndPolyfill.listener;
    win.addEventListener(scrollEndPolyfill.eventType, this.listenerScrollDepth);
  };
  Autocapture.prototype.initClickTracking = function() {
    win.removeEventListener(EV_CLICK, this.listenerClick);
    if (!this.getConfig(CONFIG_TRACK_CLICK) && !this.mp.get_config("record_heatmap_data")) {
      return;
    }
    logger$3.log("Initializing click tracking");
    this.listenerClick = function(ev) {
      if (!this.getConfig(CONFIG_TRACK_CLICK) && !this.mp.is_recording_heatmap_data()) {
        return;
      }
      this.trackDomEvent(ev, MP_EV_CLICK);
    }.bind(this);
    win.addEventListener(EV_CLICK, this.listenerClick);
  };
  Autocapture.prototype.initDeadClickTracking = function() {
    var deadClickConfig = this._getClickTrackingConfig(CONFIG_TRACK_DEAD_CLICK);
    if (!deadClickConfig && !this.mp.get_config("record_heatmap_data")) {
      this.stopDeadClickTracking();
      return;
    }
    logger$3.log("Initializing dead click tracking");
    if (!this._deadClickTracker) {
      this._deadClickTracker = new DeadClickTracker(function(deadClickEvent) {
        this.trackDomEvent(deadClickEvent, MP_EV_DEAD_CLICK);
      }.bind(this));
      this._deadClickTracker.startTracking();
    }
    if (!this.listenerDeadClick) {
      this.listenerDeadClick = function(ev) {
        var currentDeadClickConfig = this._getClickTrackingConfig(CONFIG_TRACK_DEAD_CLICK);
        if (!currentDeadClickConfig && !this.mp.is_recording_heatmap_data()) {
          return;
        }
        if (this.currentUrlBlocked()) {
          return;
        }
        var normalizedConfig = currentDeadClickConfig || {};
        if (!normalizedConfig["timeout_ms"]) {
          normalizedConfig["timeout_ms"] = DEFAULT_DEAD_CLICK_TIMEOUT_MS;
        }
        this._deadClickTracker.trackClick(ev, normalizedConfig);
      }.bind(this);
      win.addEventListener(EV_CLICK, this.listenerDeadClick);
    }
  };
  Autocapture.prototype.initInputTracking = function() {
    win.removeEventListener(EV_CHANGE, this.listenerChange);
    if (!this.getConfig(CONFIG_TRACK_INPUT)) {
      return;
    }
    logger$3.log("Initializing input tracking");
    this.listenerChange = function(ev) {
      if (!this.getConfig(CONFIG_TRACK_INPUT)) {
        return;
      }
      this.trackDomEvent(ev, MP_EV_INPUT);
    }.bind(this);
    win.addEventListener(EV_CHANGE, this.listenerChange);
  };
  Autocapture.prototype.initPageviewTracking = function() {
    win.removeEventListener(EV_MP_LOCATION_CHANGE, this.listenerLocationchange);
    if (!this.pageviewTrackingConfig()) {
      return;
    }
    logger$3.log("Initializing pageview tracking");
    var previousTrackedUrl = "";
    var tracked = false;
    if (!this.currentUrlBlocked()) {
      tracked = this.mp.track_pageview(DEFAULT_PROPS);
    }
    if (tracked) {
      previousTrackedUrl = _3.info.currentUrl();
    }
    this.listenerLocationchange = safewrap(function() {
      if (this.currentUrlBlocked()) {
        return;
      }
      var currentUrl = _3.info.currentUrl();
      var shouldTrack = false;
      var didPathChange = currentUrl.split("#")[0].split("?")[0] !== previousTrackedUrl.split("#")[0].split("?")[0];
      var trackPageviewOption = this.pageviewTrackingConfig();
      if (trackPageviewOption === PAGEVIEW_OPTION_FULL_URL) {
        shouldTrack = currentUrl !== previousTrackedUrl;
      } else if (trackPageviewOption === PAGEVIEW_OPTION_URL_WITH_PATH_AND_QUERY_STRING) {
        shouldTrack = currentUrl.split("#")[0] !== previousTrackedUrl.split("#")[0];
      } else if (trackPageviewOption === PAGEVIEW_OPTION_URL_WITH_PATH) {
        shouldTrack = didPathChange;
      }
      if (shouldTrack) {
        var tracked2 = this.mp.track_pageview(DEFAULT_PROPS);
        if (tracked2) {
          previousTrackedUrl = currentUrl;
        }
        if (didPathChange) {
          this.lastScrollCheckpoint = 0;
          logger$3.log("Path change: re-initializing scroll depth checkpoints");
        }
      }
    }.bind(this));
    win.addEventListener(EV_MP_LOCATION_CHANGE, this.listenerLocationchange);
  };
  Autocapture.prototype.initRageClickTracking = function() {
    win.removeEventListener(EV_CLICK, this.listenerRageClick);
    var rageClickConfig = this._getClickTrackingConfig(CONFIG_TRACK_RAGE_CLICK);
    if (!rageClickConfig && !this.mp.get_config("record_heatmap_data")) {
      return;
    }
    logger$3.log("Initializing rage click tracking");
    if (!this._rageClickTracker) {
      this._rageClickTracker = new RageClickTracker();
    }
    this.listenerRageClick = function(ev) {
      var currentRageClickConfig = this._getClickTrackingConfig(CONFIG_TRACK_RAGE_CLICK);
      if (!currentRageClickConfig && !this.mp.is_recording_heatmap_data()) {
        return;
      }
      if (this.currentUrlBlocked()) {
        return;
      }
      if (this._rageClickTracker.isRageClick(ev, currentRageClickConfig)) {
        this.trackDomEvent(ev, MP_EV_RAGE_CLICK);
      }
    }.bind(this);
    win.addEventListener(EV_CLICK, this.listenerRageClick);
  };
  Autocapture.prototype.initScrollTracking = function() {
    win.removeEventListener(EV_SCROLLEND, this.listenerScroll);
    win.removeEventListener(EV_SCROLL, this.listenerScroll);
    if (!this.getConfig(CONFIG_TRACK_SCROLL)) {
      return;
    }
    logger$3.log("Initializing scroll tracking");
    this.lastScrollCheckpoint = 0;
    var scrollTrackFunction = function() {
      if (!this.getConfig(CONFIG_TRACK_SCROLL)) {
        return;
      }
      if (this.currentUrlBlocked()) {
        return;
      }
      var shouldTrack = this.getConfig(CONFIG_SCROLL_CAPTURE_ALL);
      var scrollCheckpoints = (this.getConfig(CONFIG_SCROLL_CHECKPOINTS) || []).slice().sort(function(a3, b2) {
        return a3 - b2;
      });
      var scrollTop = win.scrollY;
      var props = _3.extend({ "$scroll_top": scrollTop }, DEFAULT_PROPS);
      try {
        var scrollHeight = document$1.body.scrollHeight;
        var scrollPercentage = Math.round(scrollTop / (scrollHeight - win.innerHeight) * 100);
        props["$scroll_height"] = scrollHeight;
        props["$scroll_percentage"] = scrollPercentage;
        if (scrollPercentage > this.lastScrollCheckpoint) {
          for (var i3 = 0; i3 < scrollCheckpoints.length; i3++) {
            var checkpoint = scrollCheckpoints[i3];
            if (scrollPercentage >= checkpoint && this.lastScrollCheckpoint < checkpoint) {
              props["$scroll_checkpoint"] = checkpoint;
              this.lastScrollCheckpoint = checkpoint;
              shouldTrack = true;
            }
          }
        }
      } catch (err) {
        logger$3.critical("Error while calculating scroll percentage", err);
      }
      if (shouldTrack) {
        this.mp.track(MP_EV_SCROLL, props);
      }
    }.bind(this);
    var scrollEndPolyfill = getPolyfillScrollEndFunction(scrollTrackFunction);
    this.listenerScroll = scrollEndPolyfill.listener;
    win.addEventListener(scrollEndPolyfill.eventType, this.listenerScroll);
  };
  Autocapture.prototype.initSubmitTracking = function() {
    win.removeEventListener(EV_SUBMIT, this.listenerSubmit);
    if (!this.getConfig(CONFIG_TRACK_SUBMIT)) {
      return;
    }
    logger$3.log("Initializing submit tracking");
    this.listenerSubmit = function(ev) {
      if (!this.getConfig(CONFIG_TRACK_SUBMIT)) {
        return;
      }
      this.trackDomEvent(ev, MP_EV_SUBMIT);
    }.bind(this);
    win.addEventListener(EV_SUBMIT, this.listenerSubmit);
  };
  Autocapture.prototype.initPageLeaveTracking = function() {
    document$1.removeEventListener(EV_VISIBILITYCHANGE, this.listenerPageLeaveVisibilitychange);
    win.removeEventListener(EV_MP_LOCATION_CHANGE, this.listenerPageLeaveLocationchange);
    win.removeEventListener(EV_LOAD, this.listenerPageLoad);
    if (!this.getConfig(CONFIG_TRACK_PAGE_LEAVE) && !this.mp.get_config("record_heatmap_data")) {
      return;
    }
    logger$3.log("Initializing page visibility tracking.");
    this._initScrollDepthTracking();
    var previousTrackedUrl = _3.info.currentUrl();
    this.listenerPageLoad = function() {
      this.previousScrollHeight = document$1.body.scrollHeight;
    }.bind(this);
    win.addEventListener(EV_LOAD, this.listenerPageLoad);
    this.listenerPageLeaveLocationchange = safewrap(function(ev) {
      if (this.currentUrlBlocked()) {
        return;
      }
      var currentUrl = _3.info.currentUrl();
      var shouldTrack = currentUrl !== previousTrackedUrl;
      if (shouldTrack) {
        this._trackPageLeave(ev, previousTrackedUrl, this.previousScrollHeight);
        previousTrackedUrl = currentUrl;
        this.maxScrollViewDepth = Math.max(document$1.documentElement.clientHeight, win.innerHeight || 0);
        this.previousScrollHeight = document$1.body.scrollHeight;
        this.hasTrackedScrollSession = false;
      }
    }.bind(this));
    win.addEventListener(EV_MP_LOCATION_CHANGE, this.listenerPageLeaveLocationchange);
    this.listenerPageLeaveVisibilitychange = function(ev) {
      if (document$1.hidden) {
        this._trackPageLeave(ev, previousTrackedUrl, this.previousScrollHeight);
      }
    }.bind(this);
    document$1.addEventListener(EV_VISIBILITYCHANGE, this.listenerPageLeaveVisibilitychange);
  };
  Autocapture.prototype.stopDeadClickTracking = function() {
    if (this.listenerDeadClick) {
      win.removeEventListener(EV_CLICK, this.listenerDeadClick);
      this.listenerDeadClick = null;
    }
    if (this._deadClickTracker) {
      this._deadClickTracker.stopTracking();
      this._deadClickTracker = null;
    }
  };
  safewrapClass(Autocapture);
  var logger = console_with_prefix("flags");
  var FLAGS_CONFIG_KEY = "flags";
  var CONFIG_CONTEXT = "context";
  var CONFIG_DEFAULTS = {};
  CONFIG_DEFAULTS[CONFIG_CONTEXT] = {};
  var FeatureFlagManager = function(initOptions) {
    this.fetch = win["fetch"];
    this.getFullApiRoute = initOptions.getFullApiRoute;
    this.getMpConfig = initOptions.getConfigFunc;
    this.setMpConfig = initOptions.setConfigFunc;
    this.getMpProperty = initOptions.getPropertyFunc;
    this.track = initOptions.trackingFunc;
  };
  FeatureFlagManager.prototype.init = function() {
    if (!this.minApisSupported()) {
      logger.critical("Feature Flags unavailable: missing minimum required APIs");
      return;
    }
    this.flags = null;
    this.fetchFlags();
    this.trackedFeatures = /* @__PURE__ */ new Set();
  };
  FeatureFlagManager.prototype.getFullConfig = function() {
    var ffConfig = this.getMpConfig(FLAGS_CONFIG_KEY);
    if (!ffConfig) {
      return {};
    } else if (_3.isObject(ffConfig)) {
      return _3.extend({}, CONFIG_DEFAULTS, ffConfig);
    } else {
      return CONFIG_DEFAULTS;
    }
  };
  FeatureFlagManager.prototype.getConfig = function(key) {
    return this.getFullConfig()[key];
  };
  FeatureFlagManager.prototype.isSystemEnabled = function() {
    return !!this.getMpConfig(FLAGS_CONFIG_KEY);
  };
  FeatureFlagManager.prototype.updateContext = function(newContext, options) {
    if (!this.isSystemEnabled()) {
      logger.critical("Feature Flags not enabled, cannot update context");
      return Promise.resolve();
    }
    var ffConfig = this.getMpConfig(FLAGS_CONFIG_KEY);
    if (!_3.isObject(ffConfig)) {
      ffConfig = {};
    }
    var oldContext = options && options["replace"] ? {} : this.getConfig(CONFIG_CONTEXT);
    ffConfig[CONFIG_CONTEXT] = _3.extend({}, oldContext, newContext);
    this.setMpConfig(FLAGS_CONFIG_KEY, ffConfig);
    return this.fetchFlags();
  };
  FeatureFlagManager.prototype.areFlagsReady = function() {
    if (!this.isSystemEnabled()) {
      logger.error("Feature Flags not enabled");
    }
    return !!this.flags;
  };
  FeatureFlagManager.prototype.fetchFlags = function() {
    if (!this.isSystemEnabled()) {
      return Promise.resolve();
    }
    var distinctId = this.getMpProperty("distinct_id");
    var deviceId = this.getMpProperty("$device_id");
    var traceparent = generateTraceparent();
    logger.log("Fetching flags for distinct ID: " + distinctId);
    var context = _3.extend({ "distinct_id": distinctId, "device_id": deviceId }, this.getConfig(CONFIG_CONTEXT));
    var searchParams = new URLSearchParams();
    searchParams.set("context", JSON.stringify(context));
    searchParams.set("token", this.getMpConfig("token"));
    searchParams.set("mp_lib", "web");
    searchParams.set("$lib_version", Config.LIB_VERSION);
    var url = this.getFullApiRoute() + "?" + searchParams.toString();
    this._fetchInProgressStartTime = Date.now();
    this.fetchPromise = this.fetch.call(win, url, {
      "method": "GET",
      "headers": {
        "Authorization": "Basic " + btoa(this.getMpConfig("token") + ":"),
        "traceparent": traceparent
      }
    }).then(function(response) {
      this.markFetchComplete();
      return response.json().then(function(responseBody) {
        var responseFlags = responseBody["flags"];
        if (!responseFlags) {
          throw new Error("No flags in API response");
        }
        var flags = /* @__PURE__ */ new Map();
        _3.each(responseFlags, function(data, key) {
          flags.set(key, {
            "key": data["variant_key"],
            "value": data["variant_value"],
            "experiment_id": data["experiment_id"],
            "is_experiment_active": data["is_experiment_active"],
            "is_qa_tester": data["is_qa_tester"]
          });
        });
        this.flags = flags;
        this._traceparent = traceparent;
      }.bind(this)).catch(function(error) {
        this.markFetchComplete();
        logger.error(error);
      }.bind(this));
    }.bind(this)).catch(function(error) {
      this.markFetchComplete();
      logger.error(error);
    }.bind(this));
    return this.fetchPromise;
  };
  FeatureFlagManager.prototype.markFetchComplete = function() {
    if (!this._fetchInProgressStartTime) {
      logger.error("Fetch in progress started time not set, cannot mark fetch complete");
      return;
    }
    this._fetchStartTime = this._fetchInProgressStartTime;
    this._fetchCompleteTime = Date.now();
    this._fetchLatency = this._fetchCompleteTime - this._fetchStartTime;
    this._fetchInProgressStartTime = null;
  };
  FeatureFlagManager.prototype.getVariant = function(featureName, fallback) {
    if (!this.fetchPromise) {
      return new Promise(function(resolve2) {
        logger.critical("Feature Flags not initialized");
        resolve2(fallback);
      });
    }
    return this.fetchPromise.then(function() {
      return this.getVariantSync(featureName, fallback);
    }.bind(this)).catch(function(error) {
      logger.error(error);
      return fallback;
    });
  };
  FeatureFlagManager.prototype.getVariantSync = function(featureName, fallback) {
    if (!this.areFlagsReady()) {
      logger.log("Flags not loaded yet");
      return fallback;
    }
    var feature = this.flags.get(featureName);
    if (!feature) {
      logger.log('No flag found: "' + featureName + '"');
      return fallback;
    }
    this.trackFeatureCheck(featureName, feature);
    return feature;
  };
  FeatureFlagManager.prototype.getVariantValue = function(featureName, fallbackValue) {
    return this.getVariant(featureName, { "value": fallbackValue }).then(function(feature) {
      return feature["value"];
    }).catch(function(error) {
      logger.error(error);
      return fallbackValue;
    });
  };
  FeatureFlagManager.prototype.getFeatureData = function(featureName, fallbackValue) {
    logger.critical("mixpanel.flags.get_feature_data() is deprecated and will be removed in a future release. Use mixpanel.flags.get_variant_value() instead.");
    return this.getVariantValue(featureName, fallbackValue);
  };
  FeatureFlagManager.prototype.getVariantValueSync = function(featureName, fallbackValue) {
    return this.getVariantSync(featureName, { "value": fallbackValue })["value"];
  };
  FeatureFlagManager.prototype.isEnabled = function(featureName, fallbackValue) {
    return this.getVariantValue(featureName).then(function() {
      return this.isEnabledSync(featureName, fallbackValue);
    }.bind(this)).catch(function(error) {
      logger.error(error);
      return fallbackValue;
    });
  };
  FeatureFlagManager.prototype.isEnabledSync = function(featureName, fallbackValue) {
    fallbackValue = fallbackValue || false;
    var val = this.getVariantValueSync(featureName, fallbackValue);
    if (val !== true && val !== false) {
      logger.error('Feature flag "' + featureName + '" value: ' + val + " is not a boolean; returning fallback value: " + fallbackValue);
      val = fallbackValue;
    }
    return val;
  };
  FeatureFlagManager.prototype.trackFeatureCheck = function(featureName, feature) {
    if (this.trackedFeatures.has(featureName)) {
      return;
    }
    this.trackedFeatures.add(featureName);
    var trackingProperties = {
      "Experiment name": featureName,
      "Variant name": feature["key"],
      "$experiment_type": "feature_flag",
      "Variant fetch start time": new Date(this._fetchStartTime).toISOString(),
      "Variant fetch complete time": new Date(this._fetchCompleteTime).toISOString(),
      "Variant fetch latency (ms)": this._fetchLatency,
      "Variant fetch traceparent": this._traceparent
    };
    if (feature["experiment_id"] !== "undefined") {
      trackingProperties["$experiment_id"] = feature["experiment_id"];
    }
    if (feature["is_experiment_active"] !== "undefined") {
      trackingProperties["$is_experiment_active"] = feature["is_experiment_active"];
    }
    if (feature["is_qa_tester"] !== "undefined") {
      trackingProperties["$is_qa_tester"] = feature["is_qa_tester"];
    }
    this.track("$experiment_started", trackingProperties);
  };
  FeatureFlagManager.prototype.minApisSupported = function() {
    return !!this.fetch && typeof Promise !== "undefined" && typeof Map !== "undefined" && typeof Set !== "undefined";
  };
  safewrapClass(FeatureFlagManager);
  FeatureFlagManager.prototype["are_flags_ready"] = FeatureFlagManager.prototype.areFlagsReady;
  FeatureFlagManager.prototype["get_variant"] = FeatureFlagManager.prototype.getVariant;
  FeatureFlagManager.prototype["get_variant_sync"] = FeatureFlagManager.prototype.getVariantSync;
  FeatureFlagManager.prototype["get_variant_value"] = FeatureFlagManager.prototype.getVariantValue;
  FeatureFlagManager.prototype["get_variant_value_sync"] = FeatureFlagManager.prototype.getVariantValueSync;
  FeatureFlagManager.prototype["is_enabled"] = FeatureFlagManager.prototype.isEnabled;
  FeatureFlagManager.prototype["is_enabled_sync"] = FeatureFlagManager.prototype.isEnabledSync;
  FeatureFlagManager.prototype["update_context"] = FeatureFlagManager.prototype.updateContext;
  FeatureFlagManager.prototype["get_feature_data"] = FeatureFlagManager.prototype.getFeatureData;
  var DomTracker = function() {
  };
  DomTracker.prototype.create_properties = function() {
  };
  DomTracker.prototype.event_handler = function() {
  };
  DomTracker.prototype.after_track_handler = function() {
  };
  DomTracker.prototype.init = function(mixpanel_instance) {
    this.mp = mixpanel_instance;
    return this;
  };
  DomTracker.prototype.track = function(query, event_name, properties, user_callback) {
    var that = this;
    var elements = _3.dom_query(query);
    if (elements.length === 0) {
      console$1.error("The DOM query (" + query + ") returned 0 elements");
      return;
    }
    _3.each(elements, function(element) {
      _3.register_event(element, this.override_event, function(e3) {
        var options = {};
        var props = that.create_properties(properties, this);
        var timeout = that.mp.get_config("track_links_timeout");
        that.event_handler(e3, this, options);
        window.setTimeout(that.track_callback(user_callback, props, options, true), timeout);
        that.mp.track(event_name, props, that.track_callback(user_callback, props, options));
      });
    }, this);
    return true;
  };
  DomTracker.prototype.track_callback = function(user_callback, props, options, timeout_occured) {
    timeout_occured = timeout_occured || false;
    var that = this;
    return function() {
      if (options.callback_fired) {
        return;
      }
      options.callback_fired = true;
      if (user_callback && user_callback(timeout_occured, props) === false) {
        return;
      }
      that.after_track_handler(props, options, timeout_occured);
    };
  };
  DomTracker.prototype.create_properties = function(properties, element) {
    var props;
    if (typeof properties === "function") {
      props = properties(element);
    } else {
      props = _3.extend({}, properties);
    }
    return props;
  };
  var LinkTracker = function() {
    this.override_event = "click";
  };
  _3.inherit(LinkTracker, DomTracker);
  LinkTracker.prototype.create_properties = function(properties, element) {
    var props = LinkTracker.superclass.create_properties.apply(this, arguments);
    if (element.href) {
      props["url"] = element.href;
    }
    return props;
  };
  LinkTracker.prototype.event_handler = function(evt, element, options) {
    options.new_tab = evt.which === 2 || evt.metaKey || evt.ctrlKey || element.target === "_blank";
    options.href = element.href;
    if (!options.new_tab) {
      evt.preventDefault();
    }
  };
  LinkTracker.prototype.after_track_handler = function(props, options) {
    if (options.new_tab) {
      return;
    }
    setTimeout(function() {
      window.location = options.href;
    }, 0);
  };
  var FormTracker = function() {
    this.override_event = "submit";
  };
  _3.inherit(FormTracker, DomTracker);
  FormTracker.prototype.event_handler = function(evt, element, options) {
    options.element = element;
    evt.preventDefault();
  };
  FormTracker.prototype.after_track_handler = function(props, options) {
    setTimeout(function() {
      options.element.submit();
    }, 0);
  };
  var SET_ACTION = "$set";
  var SET_ONCE_ACTION = "$set_once";
  var UNSET_ACTION = "$unset";
  var ADD_ACTION = "$add";
  var APPEND_ACTION = "$append";
  var UNION_ACTION = "$union";
  var REMOVE_ACTION = "$remove";
  var DELETE_ACTION = "$delete";
  var apiActions = {
    set_action: function(prop, to) {
      var data = {};
      var $set = {};
      if (_3.isObject(prop)) {
        _3.each(prop, function(v3, k4) {
          if (!this._is_reserved_property(k4)) {
            $set[k4] = v3;
          }
        }, this);
      } else {
        $set[prop] = to;
      }
      data[SET_ACTION] = $set;
      return data;
    },
    unset_action: function(prop) {
      var data = {};
      var $unset = [];
      if (!_3.isArray(prop)) {
        prop = [prop];
      }
      _3.each(prop, function(k4) {
        if (!this._is_reserved_property(k4)) {
          $unset.push(k4);
        }
      }, this);
      data[UNSET_ACTION] = $unset;
      return data;
    },
    set_once_action: function(prop, to) {
      var data = {};
      var $set_once = {};
      if (_3.isObject(prop)) {
        _3.each(prop, function(v3, k4) {
          if (!this._is_reserved_property(k4)) {
            $set_once[k4] = v3;
          }
        }, this);
      } else {
        $set_once[prop] = to;
      }
      data[SET_ONCE_ACTION] = $set_once;
      return data;
    },
    union_action: function(list_name, values) {
      var data = {};
      var $union = {};
      if (_3.isObject(list_name)) {
        _3.each(list_name, function(v3, k4) {
          if (!this._is_reserved_property(k4)) {
            $union[k4] = _3.isArray(v3) ? v3 : [v3];
          }
        }, this);
      } else {
        $union[list_name] = _3.isArray(values) ? values : [values];
      }
      data[UNION_ACTION] = $union;
      return data;
    },
    append_action: function(list_name, value) {
      var data = {};
      var $append = {};
      if (_3.isObject(list_name)) {
        _3.each(list_name, function(v3, k4) {
          if (!this._is_reserved_property(k4)) {
            $append[k4] = v3;
          }
        }, this);
      } else {
        $append[list_name] = value;
      }
      data[APPEND_ACTION] = $append;
      return data;
    },
    remove_action: function(list_name, value) {
      var data = {};
      var $remove = {};
      if (_3.isObject(list_name)) {
        _3.each(list_name, function(v3, k4) {
          if (!this._is_reserved_property(k4)) {
            $remove[k4] = v3;
          }
        }, this);
      } else {
        $remove[list_name] = value;
      }
      data[REMOVE_ACTION] = $remove;
      return data;
    },
    delete_action: function() {
      var data = {};
      data[DELETE_ACTION] = "";
      return data;
    }
  };
  var MixpanelGroup = function() {
  };
  _3.extend(MixpanelGroup.prototype, apiActions);
  MixpanelGroup.prototype._init = function(mixpanel_instance, group_key, group_id) {
    this._mixpanel = mixpanel_instance;
    this._group_key = group_key;
    this._group_id = group_id;
  };
  MixpanelGroup.prototype.set = addOptOutCheckMixpanelGroup(function(prop, to, callback) {
    var data = this.set_action(prop, to);
    if (_3.isObject(prop)) {
      callback = to;
    }
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype.set_once = addOptOutCheckMixpanelGroup(function(prop, to, callback) {
    var data = this.set_once_action(prop, to);
    if (_3.isObject(prop)) {
      callback = to;
    }
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype.unset = addOptOutCheckMixpanelGroup(function(prop, callback) {
    var data = this.unset_action(prop);
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype.union = addOptOutCheckMixpanelGroup(function(list_name, values, callback) {
    if (_3.isObject(list_name)) {
      callback = values;
    }
    var data = this.union_action(list_name, values);
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype["delete"] = addOptOutCheckMixpanelGroup(function(callback) {
    var data = this.delete_action();
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype.remove = addOptOutCheckMixpanelGroup(function(list_name, value, callback) {
    var data = this.remove_action(list_name, value);
    return this._send_request(data, callback);
  });
  MixpanelGroup.prototype._send_request = function(data, callback) {
    data["$group_key"] = this._group_key;
    data["$group_id"] = this._group_id;
    data["$token"] = this._get_config("token");
    var date_encoded_data = _3.encodeDates(data);
    return this._mixpanel._track_or_batch({
      type: "groups",
      data: date_encoded_data,
      endpoint: this._mixpanel.get_api_host("groups") + "/" + this._get_config("api_routes")["groups"],
      batcher: this._mixpanel.request_batchers.groups
    }, callback);
  };
  MixpanelGroup.prototype._is_reserved_property = function(prop) {
    return prop === "$group_key" || prop === "$group_id";
  };
  MixpanelGroup.prototype._get_config = function(conf) {
    return this._mixpanel.get_config(conf);
  };
  MixpanelGroup.prototype.toString = function() {
    return this._mixpanel.toString() + ".group." + this._group_key + "." + this._group_id;
  };
  MixpanelGroup.prototype["remove"] = MixpanelGroup.prototype.remove;
  MixpanelGroup.prototype["set"] = MixpanelGroup.prototype.set;
  MixpanelGroup.prototype["set_once"] = MixpanelGroup.prototype.set_once;
  MixpanelGroup.prototype["union"] = MixpanelGroup.prototype.union;
  MixpanelGroup.prototype["unset"] = MixpanelGroup.prototype.unset;
  MixpanelGroup.prototype["toString"] = MixpanelGroup.prototype.toString;
  var MixpanelPeople = function() {
  };
  _3.extend(MixpanelPeople.prototype, apiActions);
  MixpanelPeople.prototype._init = function(mixpanel_instance) {
    this._mixpanel = mixpanel_instance;
  };
  MixpanelPeople.prototype.set = addOptOutCheckMixpanelPeople(function(prop, to, callback) {
    var data = this.set_action(prop, to);
    if (_3.isObject(prop)) {
      callback = to;
    }
    if (this._get_config("save_referrer")) {
      this._mixpanel["persistence"].update_referrer_info(document.referrer);
    }
    data[SET_ACTION] = _3.extend(
      {},
      _3.info.people_properties(),
      data[SET_ACTION]
    );
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.set_once = addOptOutCheckMixpanelPeople(function(prop, to, callback) {
    var data = this.set_once_action(prop, to);
    if (_3.isObject(prop)) {
      callback = to;
    }
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.unset = addOptOutCheckMixpanelPeople(function(prop, callback) {
    var data = this.unset_action(prop);
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.increment = addOptOutCheckMixpanelPeople(function(prop, by, callback) {
    var data = {};
    var $add = {};
    if (_3.isObject(prop)) {
      _3.each(prop, function(v3, k4) {
        if (!this._is_reserved_property(k4)) {
          if (isNaN(parseFloat(v3))) {
            console$1.error("Invalid increment value passed to mixpanel.people.increment - must be a number");
            return;
          } else {
            $add[k4] = v3;
          }
        }
      }, this);
      callback = by;
    } else {
      if (_3.isUndefined(by)) {
        by = 1;
      }
      $add[prop] = by;
    }
    data[ADD_ACTION] = $add;
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.append = addOptOutCheckMixpanelPeople(function(list_name, value, callback) {
    if (_3.isObject(list_name)) {
      callback = value;
    }
    var data = this.append_action(list_name, value);
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.remove = addOptOutCheckMixpanelPeople(function(list_name, value, callback) {
    if (_3.isObject(list_name)) {
      callback = value;
    }
    var data = this.remove_action(list_name, value);
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.union = addOptOutCheckMixpanelPeople(function(list_name, values, callback) {
    if (_3.isObject(list_name)) {
      callback = values;
    }
    var data = this.union_action(list_name, values);
    return this._send_request(data, callback);
  });
  MixpanelPeople.prototype.track_charge = addOptOutCheckMixpanelPeople(function() {
    console$1.error("mixpanel.people.track_charge() is deprecated and no longer has any effect.");
  });
  MixpanelPeople.prototype.clear_charges = function(callback) {
    return this.set("$transactions", [], callback);
  };
  MixpanelPeople.prototype.delete_user = function() {
    if (!this._identify_called()) {
      console$1.error("mixpanel.people.delete_user() requires you to call identify() first");
      return;
    }
    var data = { "$delete": this._mixpanel.get_distinct_id() };
    return this._send_request(data);
  };
  MixpanelPeople.prototype.toString = function() {
    return this._mixpanel.toString() + ".people";
  };
  MixpanelPeople.prototype._send_request = function(data, callback) {
    data["$token"] = this._get_config("token");
    data["$distinct_id"] = this._mixpanel.get_distinct_id();
    var device_id = this._mixpanel.get_property("$device_id");
    var user_id = this._mixpanel.get_property("$user_id");
    var had_persisted_distinct_id = this._mixpanel.get_property("$had_persisted_distinct_id");
    if (device_id) {
      data["$device_id"] = device_id;
    }
    if (user_id) {
      data["$user_id"] = user_id;
    }
    if (had_persisted_distinct_id) {
      data["$had_persisted_distinct_id"] = had_persisted_distinct_id;
    }
    var date_encoded_data = _3.encodeDates(data);
    if (!this._identify_called()) {
      this._enqueue(data);
      if (!_3.isUndefined(callback)) {
        if (this._get_config("verbose")) {
          callback({ status: -1, error: null });
        } else {
          callback(-1);
        }
      }
      return _3.truncate(date_encoded_data, 255);
    }
    return this._mixpanel._track_or_batch({
      type: "people",
      data: date_encoded_data,
      endpoint: this._mixpanel.get_api_host("people") + "/" + this._get_config("api_routes")["engage"],
      batcher: this._mixpanel.request_batchers.people
    }, callback);
  };
  MixpanelPeople.prototype._get_config = function(conf_var) {
    return this._mixpanel.get_config(conf_var);
  };
  MixpanelPeople.prototype._identify_called = function() {
    return this._mixpanel._flags.identify_called === true;
  };
  MixpanelPeople.prototype._enqueue = function(data) {
    if (SET_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(SET_ACTION, data);
    } else if (SET_ONCE_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(SET_ONCE_ACTION, data);
    } else if (UNSET_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(UNSET_ACTION, data);
    } else if (ADD_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(ADD_ACTION, data);
    } else if (APPEND_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(APPEND_ACTION, data);
    } else if (REMOVE_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(REMOVE_ACTION, data);
    } else if (UNION_ACTION in data) {
      this._mixpanel["persistence"]._add_to_people_queue(UNION_ACTION, data);
    } else {
      console$1.error("Invalid call to _enqueue():", data);
    }
  };
  MixpanelPeople.prototype._flush_one_queue = function(action, action_method, callback, queue_to_params_fn) {
    var _this = this;
    var queued_data = _3.extend({}, this._mixpanel["persistence"].load_queue(action));
    var action_params = queued_data;
    if (!_3.isUndefined(queued_data) && _3.isObject(queued_data) && !_3.isEmptyObject(queued_data)) {
      _this._mixpanel["persistence"]._pop_from_people_queue(action, queued_data);
      _this._mixpanel["persistence"].save();
      if (queue_to_params_fn) {
        action_params = queue_to_params_fn(queued_data);
      }
      action_method.call(_this, action_params, function(response, data) {
        if (response === 0) {
          _this._mixpanel["persistence"]._add_to_people_queue(action, queued_data);
        }
        if (!_3.isUndefined(callback)) {
          callback(response, data);
        }
      });
    }
  };
  MixpanelPeople.prototype._flush = function(_set_callback, _add_callback, _append_callback, _set_once_callback, _union_callback, _unset_callback, _remove_callback) {
    var _this = this;
    this._flush_one_queue(SET_ACTION, this.set, _set_callback);
    this._flush_one_queue(SET_ONCE_ACTION, this.set_once, _set_once_callback);
    this._flush_one_queue(UNSET_ACTION, this.unset, _unset_callback, function(queue) {
      return _3.keys(queue);
    });
    this._flush_one_queue(ADD_ACTION, this.increment, _add_callback);
    this._flush_one_queue(UNION_ACTION, this.union, _union_callback);
    var $append_queue = this._mixpanel["persistence"].load_queue(APPEND_ACTION);
    if (!_3.isUndefined($append_queue) && _3.isArray($append_queue) && $append_queue.length) {
      var $append_item;
      var append_callback = function(response, data) {
        if (response === 0) {
          _this._mixpanel["persistence"]._add_to_people_queue(APPEND_ACTION, $append_item);
        }
        if (!_3.isUndefined(_append_callback)) {
          _append_callback(response, data);
        }
      };
      for (var i3 = $append_queue.length - 1; i3 >= 0; i3--) {
        $append_queue = this._mixpanel["persistence"].load_queue(APPEND_ACTION);
        $append_item = $append_queue.pop();
        _this._mixpanel["persistence"].save();
        if (!_3.isEmptyObject($append_item)) {
          _this.append($append_item, append_callback);
        }
      }
    }
    var $remove_queue = this._mixpanel["persistence"].load_queue(REMOVE_ACTION);
    if (!_3.isUndefined($remove_queue) && _3.isArray($remove_queue) && $remove_queue.length) {
      var $remove_item;
      var remove_callback = function(response, data) {
        if (response === 0) {
          _this._mixpanel["persistence"]._add_to_people_queue(REMOVE_ACTION, $remove_item);
        }
        if (!_3.isUndefined(_remove_callback)) {
          _remove_callback(response, data);
        }
      };
      for (var j4 = $remove_queue.length - 1; j4 >= 0; j4--) {
        $remove_queue = this._mixpanel["persistence"].load_queue(REMOVE_ACTION);
        $remove_item = $remove_queue.pop();
        _this._mixpanel["persistence"].save();
        if (!_3.isEmptyObject($remove_item)) {
          _this.remove($remove_item, remove_callback);
        }
      }
    }
  };
  MixpanelPeople.prototype._is_reserved_property = function(prop) {
    return prop === "$distinct_id" || prop === "$token" || prop === "$device_id" || prop === "$user_id" || prop === "$had_persisted_distinct_id";
  };
  MixpanelPeople.prototype["set"] = MixpanelPeople.prototype.set;
  MixpanelPeople.prototype["set_once"] = MixpanelPeople.prototype.set_once;
  MixpanelPeople.prototype["unset"] = MixpanelPeople.prototype.unset;
  MixpanelPeople.prototype["increment"] = MixpanelPeople.prototype.increment;
  MixpanelPeople.prototype["append"] = MixpanelPeople.prototype.append;
  MixpanelPeople.prototype["remove"] = MixpanelPeople.prototype.remove;
  MixpanelPeople.prototype["union"] = MixpanelPeople.prototype.union;
  MixpanelPeople.prototype["track_charge"] = MixpanelPeople.prototype.track_charge;
  MixpanelPeople.prototype["clear_charges"] = MixpanelPeople.prototype.clear_charges;
  MixpanelPeople.prototype["delete_user"] = MixpanelPeople.prototype.delete_user;
  MixpanelPeople.prototype["toString"] = MixpanelPeople.prototype.toString;
  var SET_QUEUE_KEY = "__mps";
  var SET_ONCE_QUEUE_KEY = "__mpso";
  var UNSET_QUEUE_KEY = "__mpus";
  var ADD_QUEUE_KEY = "__mpa";
  var APPEND_QUEUE_KEY = "__mpap";
  var REMOVE_QUEUE_KEY = "__mpr";
  var UNION_QUEUE_KEY = "__mpu";
  var PEOPLE_DISTINCT_ID_KEY = "$people_distinct_id";
  var ALIAS_ID_KEY = "__alias";
  var EVENT_TIMERS_KEY = "__timers";
  var RESERVED_PROPERTIES = [
    SET_QUEUE_KEY,
    SET_ONCE_QUEUE_KEY,
    UNSET_QUEUE_KEY,
    ADD_QUEUE_KEY,
    APPEND_QUEUE_KEY,
    REMOVE_QUEUE_KEY,
    UNION_QUEUE_KEY,
    PEOPLE_DISTINCT_ID_KEY,
    ALIAS_ID_KEY,
    EVENT_TIMERS_KEY
  ];
  var MixpanelPersistence = function(config) {
    this["props"] = {};
    this.campaign_params_saved = false;
    if (config["persistence_name"]) {
      this.name = "mp_" + config["persistence_name"];
    } else {
      this.name = "mp_" + config["token"] + "_mixpanel";
    }
    var storage_type = config["persistence"];
    if (storage_type !== "cookie" && storage_type !== "localStorage") {
      console$1.critical("Unknown persistence type " + storage_type + "; falling back to cookie");
      storage_type = config["persistence"] = "cookie";
    }
    if (storage_type === "localStorage" && _3.localStorage.is_supported()) {
      this.storage = _3.localStorage;
    } else {
      this.storage = _3.cookie;
    }
    this.load();
    this.update_config(config);
    this.upgrade();
    this.save();
  };
  MixpanelPersistence.prototype.properties = function() {
    var p3 = {};
    this.load();
    _3.each(this["props"], function(v3, k4) {
      if (!_3.include(RESERVED_PROPERTIES, k4)) {
        p3[k4] = v3;
      }
    });
    return p3;
  };
  MixpanelPersistence.prototype.load = function() {
    if (this.disabled) {
      return;
    }
    var entry = this.storage.parse(this.name);
    if (entry) {
      this["props"] = _3.extend({}, entry);
    }
  };
  MixpanelPersistence.prototype.upgrade = function() {
    var old_cookie, old_localstorage;
    if (this.storage === _3.localStorage) {
      old_cookie = _3.cookie.parse(this.name);
      _3.cookie.remove(this.name);
      _3.cookie.remove(this.name, true);
      if (old_cookie) {
        this.register_once(old_cookie);
      }
    } else if (this.storage === _3.cookie) {
      old_localstorage = _3.localStorage.parse(this.name);
      _3.localStorage.remove(this.name);
      if (old_localstorage) {
        this.register_once(old_localstorage);
      }
    }
  };
  MixpanelPersistence.prototype.save = function() {
    if (this.disabled) {
      return;
    }
    this.storage.set(
      this.name,
      JSONStringify(this["props"]),
      this.expire_days,
      this.cross_subdomain,
      this.secure,
      this.cross_site,
      this.cookie_domain
    );
  };
  MixpanelPersistence.prototype.load_prop = function(key) {
    this.load();
    return this["props"][key];
  };
  MixpanelPersistence.prototype.remove = function() {
    this.storage.remove(this.name, false, this.cookie_domain);
    this.storage.remove(this.name, true, this.cookie_domain);
  };
  MixpanelPersistence.prototype.clear = function() {
    this.remove();
    this["props"] = {};
  };
  MixpanelPersistence.prototype.register_once = function(props, default_value, days) {
    if (_3.isObject(props)) {
      if (typeof default_value === "undefined") {
        default_value = "None";
      }
      this.expire_days = typeof days === "undefined" ? this.default_expiry : days;
      this.load();
      _3.each(props, function(val, prop) {
        if (!this["props"].hasOwnProperty(prop) || this["props"][prop] === default_value) {
          this["props"][prop] = val;
        }
      }, this);
      this.save();
      return true;
    }
    return false;
  };
  MixpanelPersistence.prototype.register = function(props, days) {
    if (_3.isObject(props)) {
      this.expire_days = typeof days === "undefined" ? this.default_expiry : days;
      this.load();
      _3.extend(this["props"], props);
      this.save();
      return true;
    }
    return false;
  };
  MixpanelPersistence.prototype.unregister = function(prop) {
    this.load();
    if (prop in this["props"]) {
      delete this["props"][prop];
      this.save();
    }
  };
  MixpanelPersistence.prototype.update_search_keyword = function(referrer) {
    this.register(_3.info.searchInfo(referrer));
  };
  MixpanelPersistence.prototype.update_referrer_info = function(referrer) {
    this.register_once({
      "$initial_referrer": referrer || "$direct",
      "$initial_referring_domain": _3.info.referringDomain(referrer) || "$direct"
    }, "");
  };
  MixpanelPersistence.prototype.get_referrer_info = function() {
    return _3.strip_empty_properties({
      "$initial_referrer": this["props"]["$initial_referrer"],
      "$initial_referring_domain": this["props"]["$initial_referring_domain"]
    });
  };
  MixpanelPersistence.prototype.update_config = function(config) {
    this.default_expiry = this.expire_days = config["cookie_expiration"];
    this.set_disabled(config["disable_persistence"]);
    this.set_cookie_domain(config["cookie_domain"]);
    this.set_cross_site(config["cross_site_cookie"]);
    this.set_cross_subdomain(config["cross_subdomain_cookie"]);
    this.set_secure(config["secure_cookie"]);
  };
  MixpanelPersistence.prototype.set_disabled = function(disabled) {
    this.disabled = disabled;
    if (this.disabled) {
      this.remove();
    } else {
      this.save();
    }
  };
  MixpanelPersistence.prototype.set_cookie_domain = function(cookie_domain) {
    if (cookie_domain !== this.cookie_domain) {
      this.remove();
      this.cookie_domain = cookie_domain;
      this.save();
    }
  };
  MixpanelPersistence.prototype.set_cross_site = function(cross_site) {
    if (cross_site !== this.cross_site) {
      this.cross_site = cross_site;
      this.remove();
      this.save();
    }
  };
  MixpanelPersistence.prototype.set_cross_subdomain = function(cross_subdomain) {
    if (cross_subdomain !== this.cross_subdomain) {
      this.cross_subdomain = cross_subdomain;
      this.remove();
      this.save();
    }
  };
  MixpanelPersistence.prototype.get_cross_subdomain = function() {
    return this.cross_subdomain;
  };
  MixpanelPersistence.prototype.set_secure = function(secure) {
    if (secure !== this.secure) {
      this.secure = secure ? true : false;
      this.remove();
      this.save();
    }
  };
  MixpanelPersistence.prototype._add_to_people_queue = function(queue, data) {
    var q_key = this._get_queue_key(queue), q_data = data[queue], set_q = this._get_or_create_queue(SET_ACTION), set_once_q = this._get_or_create_queue(SET_ONCE_ACTION), unset_q = this._get_or_create_queue(UNSET_ACTION), add_q = this._get_or_create_queue(ADD_ACTION), union_q = this._get_or_create_queue(UNION_ACTION), remove_q = this._get_or_create_queue(REMOVE_ACTION, []), append_q = this._get_or_create_queue(APPEND_ACTION, []);
    if (q_key === SET_QUEUE_KEY) {
      _3.extend(set_q, q_data);
      this._pop_from_people_queue(ADD_ACTION, q_data);
      this._pop_from_people_queue(UNION_ACTION, q_data);
      this._pop_from_people_queue(UNSET_ACTION, q_data);
    } else if (q_key === SET_ONCE_QUEUE_KEY) {
      _3.each(q_data, function(v3, k4) {
        if (!(k4 in set_once_q)) {
          set_once_q[k4] = v3;
        }
      });
      this._pop_from_people_queue(UNSET_ACTION, q_data);
    } else if (q_key === UNSET_QUEUE_KEY) {
      _3.each(q_data, function(prop) {
        _3.each([set_q, set_once_q, add_q, union_q], function(enqueued_obj) {
          if (prop in enqueued_obj) {
            delete enqueued_obj[prop];
          }
        });
        _3.each(append_q, function(append_obj) {
          if (prop in append_obj) {
            delete append_obj[prop];
          }
        });
        unset_q[prop] = true;
      });
    } else if (q_key === ADD_QUEUE_KEY) {
      _3.each(q_data, function(v3, k4) {
        if (k4 in set_q) {
          set_q[k4] += v3;
        } else {
          if (!(k4 in add_q)) {
            add_q[k4] = 0;
          }
          add_q[k4] += v3;
        }
      }, this);
      this._pop_from_people_queue(UNSET_ACTION, q_data);
    } else if (q_key === UNION_QUEUE_KEY) {
      _3.each(q_data, function(v3, k4) {
        if (_3.isArray(v3)) {
          if (!(k4 in union_q)) {
            union_q[k4] = [];
          }
          _3.each(v3, function(item) {
            if (!_3.include(union_q[k4], item)) {
              union_q[k4].push(item);
            }
          });
        }
      });
      this._pop_from_people_queue(UNSET_ACTION, q_data);
    } else if (q_key === REMOVE_QUEUE_KEY) {
      remove_q.push(q_data);
      this._pop_from_people_queue(APPEND_ACTION, q_data);
    } else if (q_key === APPEND_QUEUE_KEY) {
      append_q.push(q_data);
      this._pop_from_people_queue(UNSET_ACTION, q_data);
    }
    console$1.log("MIXPANEL PEOPLE REQUEST (QUEUED, PENDING IDENTIFY):");
    console$1.log(data);
    this.save();
  };
  MixpanelPersistence.prototype._pop_from_people_queue = function(queue, data) {
    var q4 = this["props"][this._get_queue_key(queue)];
    if (!_3.isUndefined(q4)) {
      _3.each(data, function(v3, k4) {
        if (queue === APPEND_ACTION || queue === REMOVE_ACTION) {
          _3.each(q4, function(queued_action) {
            if (queued_action[k4] === v3) {
              delete queued_action[k4];
            }
          });
        } else {
          delete q4[k4];
        }
      }, this);
    }
  };
  MixpanelPersistence.prototype.load_queue = function(queue) {
    return this.load_prop(this._get_queue_key(queue));
  };
  MixpanelPersistence.prototype._get_queue_key = function(queue) {
    if (queue === SET_ACTION) {
      return SET_QUEUE_KEY;
    } else if (queue === SET_ONCE_ACTION) {
      return SET_ONCE_QUEUE_KEY;
    } else if (queue === UNSET_ACTION) {
      return UNSET_QUEUE_KEY;
    } else if (queue === ADD_ACTION) {
      return ADD_QUEUE_KEY;
    } else if (queue === APPEND_ACTION) {
      return APPEND_QUEUE_KEY;
    } else if (queue === REMOVE_ACTION) {
      return REMOVE_QUEUE_KEY;
    } else if (queue === UNION_ACTION) {
      return UNION_QUEUE_KEY;
    } else {
      console$1.error("Invalid queue:", queue);
    }
  };
  MixpanelPersistence.prototype._get_or_create_queue = function(queue, default_val) {
    var key = this._get_queue_key(queue);
    default_val = _3.isUndefined(default_val) ? {} : default_val;
    return this["props"][key] || (this["props"][key] = default_val);
  };
  MixpanelPersistence.prototype.set_event_timer = function(event_name, timestamp) {
    var timers = this.load_prop(EVENT_TIMERS_KEY) || {};
    timers[event_name] = timestamp;
    this["props"][EVENT_TIMERS_KEY] = timers;
    this.save();
  };
  MixpanelPersistence.prototype.remove_event_timer = function(event_name) {
    var timers = this.load_prop(EVENT_TIMERS_KEY) || {};
    var timestamp = timers[event_name];
    if (!_3.isUndefined(timestamp)) {
      delete this["props"][EVENT_TIMERS_KEY][event_name];
      this.save();
    }
    return timestamp;
  };
  var init_type;
  var load_extra_bundle = function(src, _onload) {
    throw new Error(src + " not available in this build.");
  };
  var mixpanel_master;
  var INIT_MODULE = 0;
  var INIT_SNIPPET = 1;
  var PRIMARY_INSTANCE_NAME = "mixpanel";
  var PAYLOAD_TYPE_BASE64 = "base64";
  var PAYLOAD_TYPE_JSON = "json";
  var DEVICE_ID_PREFIX = "$device:";
  var SETTING_STRICT = "strict";
  var SETTING_FALLBACK = "fallback";
  var SETTING_DISABLED = "disabled";
  var USE_XHR = win.XMLHttpRequest && "withCredentials" in new XMLHttpRequest();
  var ENQUEUE_REQUESTS = !USE_XHR && userAgent.indexOf("MSIE") === -1 && userAgent.indexOf("Mozilla") === -1;
  var sendBeacon = null;
  if (navigator2["sendBeacon"]) {
    sendBeacon = function() {
      return navigator2["sendBeacon"].apply(navigator2, arguments);
    };
  }
  var DEFAULT_API_ROUTES = {
    "track": "track/",
    "engage": "engage/",
    "groups": "groups/",
    "record": "record/",
    "flags": "flags/",
    "settings": "settings/"
  };
  var DEFAULT_CONFIG = {
    "api_host": "https://api-js.mixpanel.com",
    "api_hosts": {},
    "api_routes": DEFAULT_API_ROUTES,
    "api_extra_query_params": {},
    "api_method": "POST",
    "api_transport": "XHR",
    "api_payload_format": PAYLOAD_TYPE_BASE64,
    "app_host": "https://mixpanel.com",
    "autocapture": false,
    "cdn": "https://cdn.mxpnl.com",
    "cross_site_cookie": false,
    "cross_subdomain_cookie": true,
    "error_reporter": NOOP_FUNC,
    "flags": false,
    "persistence": "cookie",
    "persistence_name": "",
    "cookie_domain": "",
    "cookie_name": "",
    "loaded": NOOP_FUNC,
    "mp_loader": null,
    "track_marketing": true,
    "track_pageview": false,
    "skip_first_touch_marketing": false,
    "store_google": true,
    "stop_utm_persistence": false,
    "save_referrer": true,
    "test": false,
    "verbose": false,
    "img": false,
    "debug": false,
    "track_links_timeout": 300,
    "cookie_expiration": 365,
    "upgrade": false,
    "disable_persistence": false,
    "disable_cookie": false,
    "secure_cookie": false,
    "ip": true,
    "opt_out_tracking_by_default": false,
    "opt_out_persistence_by_default": false,
    "opt_out_tracking_persistence_type": "localStorage",
    "opt_out_tracking_cookie_prefix": null,
    "property_blacklist": [],
    "xhr_headers": {},
    // { header: value, header2: value }
    "ignore_dnt": false,
    "batch_requests": true,
    "batch_size": 50,
    "batch_flush_interval_ms": 5e3,
    "batch_request_timeout_ms": 9e4,
    "batch_autostart": true,
    "hooks": {},
    "record_block_class": new RegExp("^(mp-block|fs-exclude|amp-block|rr-block|ph-no-capture)$"),
    "record_block_selector": "img, video, audio",
    "record_canvas": false,
    "record_collect_fonts": false,
    "record_console": true,
    "record_heatmap_data": false,
    "record_idle_timeout_ms": 30 * 60 * 1e3,
    // 30 minutes
    "record_mask_inputs": true,
    "record_max_ms": MAX_RECORDING_MS,
    "record_min_ms": 0,
    "record_sessions_percent": 0,
    "recorder_src": "https://cdn.mxpnl.com/libs/mixpanel-recorder.min.js",
    "remote_settings_mode": SETTING_DISABLED
    // 'strict', 'fallback', 'disabled'
  };
  var DOM_LOADED = false;
  var MixpanelLib = function() {
  };
  var create_mplib = function(token, config, name5) {
    var instance, target = name5 === PRIMARY_INSTANCE_NAME ? mixpanel_master : mixpanel_master[name5];
    if (target && init_type === INIT_MODULE) {
      instance = target;
    } else {
      if (target && !_3.isArray(target)) {
        console$1.error("You have already initialized " + name5);
        return;
      }
      instance = new MixpanelLib();
    }
    instance._cached_groups = {};
    instance._init(token, config, name5);
    instance["people"] = new MixpanelPeople();
    instance["people"]._init(instance);
    if (!instance.get_config("skip_first_touch_marketing")) {
      var utm_params = _3.info.campaignParams(null);
      var initial_utm_params = {};
      var has_utm = false;
      _3.each(utm_params, function(utm_value, utm_key) {
        initial_utm_params["initial_" + utm_key] = utm_value;
        if (utm_value) {
          has_utm = true;
        }
      });
      if (has_utm) {
        instance["people"].set_once(initial_utm_params);
      }
    }
    Config.DEBUG = Config.DEBUG || instance.get_config("debug");
    var source = init_type === INIT_MODULE ? "module" : "snippet";
    win.dispatchEvent(new win.CustomEvent("$mp_sdk_to_extension_event", {
      "detail": {
        "instance": instance,
        "source": source,
        "token": token,
        "name": name5,
        "info": _3.info
      }
    }));
    if (!_3.isUndefined(target) && _3.isArray(target)) {
      instance._execute_array.call(instance["people"], target["people"]);
      instance._execute_array(target);
    }
    return instance;
  };
  MixpanelLib.prototype.init = function(token, config, name5) {
    if (_3.isUndefined(name5)) {
      this.report_error("You must name your new library: init(token, config, name)");
      return;
    }
    if (name5 === PRIMARY_INSTANCE_NAME) {
      this.report_error("You must initialize the main mixpanel object right after you include the Mixpanel js snippet");
      return;
    }
    var instance = create_mplib(token, config, name5);
    mixpanel_master[name5] = instance;
    instance._loaded();
    return instance;
  };
  MixpanelLib.prototype._init = function(token, config, name5) {
    config = config || {};
    this["__loaded"] = true;
    this["config"] = {};
    var variable_features = {};
    if (!("api_payload_format" in config)) {
      var api_host = config["api_host"] || DEFAULT_CONFIG["api_host"];
      if (api_host.match(/\.mixpanel\.com/)) {
        variable_features["api_payload_format"] = PAYLOAD_TYPE_JSON;
      }
    }
    this.hooks = {};
    this.set_config(_3.extend({}, DEFAULT_CONFIG, variable_features, config, {
      "name": name5,
      "token": token,
      "callback_fn": (name5 === PRIMARY_INSTANCE_NAME ? name5 : PRIMARY_INSTANCE_NAME + "." + name5) + "._jsc"
    }));
    this["_jsc"] = NOOP_FUNC;
    this.__dom_loaded_queue = [];
    this.__request_queue = [];
    this.__disabled_events = [];
    this._flags = {
      "disable_all_events": false,
      "identify_called": false
    };
    this.request_batchers = {};
    this._batch_requests = this.get_config("batch_requests");
    if (this._batch_requests) {
      if (!_3.localStorage.is_supported(true) || !USE_XHR) {
        this._batch_requests = false;
        console$1.log("Turning off Mixpanel request-queueing; needs XHR and localStorage support");
        _3.each(this.get_batcher_configs(), function(batcher_config) {
          console$1.log("Clearing batch queue " + batcher_config.queue_key);
          _3.localStorage.remove(batcher_config.queue_key);
        });
      } else {
        this.init_batchers();
        if (sendBeacon && win.addEventListener) {
          var flush_on_unload = _3.bind(function() {
            if (!this.request_batchers.events.stopped) {
              this.request_batchers.events.flush({ unloading: true });
            }
          }, this);
          win.addEventListener("pagehide", function(ev) {
            if (ev["persisted"]) {
              flush_on_unload();
            }
          });
          win.addEventListener("visibilitychange", function() {
            if (document$1["visibilityState"] === "hidden") {
              flush_on_unload();
            }
          });
        }
      }
    }
    this["persistence"] = this["cookie"] = new MixpanelPersistence(this["config"]);
    this.unpersisted_superprops = {};
    this._gdpr_init();
    var uuid = _3.UUID();
    if (!this.get_distinct_id()) {
      this.register_once({
        "distinct_id": DEVICE_ID_PREFIX + uuid,
        "$device_id": uuid
      }, "");
    }
    this.flags = new FeatureFlagManager({
      getFullApiRoute: _3.bind(function() {
        return this.get_api_host("flags") + "/" + this.get_config("api_routes")["flags"];
      }, this),
      getConfigFunc: _3.bind(this.get_config, this),
      setConfigFunc: _3.bind(this.set_config, this),
      getPropertyFunc: _3.bind(this.get_property, this),
      trackingFunc: _3.bind(this.track, this)
    });
    this.flags.init();
    this["flags"] = this.flags;
    this.autocapture = new Autocapture(this);
    this.autocapture.init();
    this._init_tab_id();
    var mode = this.get_config("remote_settings_mode");
    if (mode === SETTING_STRICT || mode === SETTING_FALLBACK) {
      this._fetch_remote_settings(mode).then(_3.bind(function() {
        this._check_and_start_session_recording();
      }, this));
    } else {
      this._check_and_start_session_recording();
    }
  };
  MixpanelLib.prototype._init_tab_id = function() {
    if (this.get_config("disable_persistence")) {
      console$1.log("Tab ID initialization skipped due to disable_persistence config");
    } else if (_3.sessionStorage.is_supported()) {
      try {
        var key_suffix = this.get_config("name") + "_" + this.get_config("token");
        var tab_id_key = "mp_tab_id_" + key_suffix;
        var should_generate_new_tab_id_key = "mp_gen_new_tab_id_" + key_suffix;
        if (_3.sessionStorage.get(should_generate_new_tab_id_key) || !_3.sessionStorage.get(tab_id_key)) {
          _3.sessionStorage.set(tab_id_key, "$tab-" + _3.UUID());
        }
        _3.sessionStorage.set(should_generate_new_tab_id_key, "1");
        this.tab_id = _3.sessionStorage.get(tab_id_key);
        win.addEventListener("beforeunload", function() {
          _3.sessionStorage.remove(should_generate_new_tab_id_key);
        });
      } catch (err) {
        this.report_error("Error initializing tab id", err);
      }
    } else {
      this.report_error("Session storage is not supported, cannot keep track of unique tab ID.");
    }
  };
  MixpanelLib.prototype.get_tab_id = function() {
    return this.tab_id || null;
  };
  MixpanelLib.prototype._should_load_recorder = function() {
    if (this.get_config("disable_persistence")) {
      console$1.log("Load recorder check skipped due to disable_persistence config");
      return Promise.resolve(false);
    }
    var recording_registry_idb = new IDBStorageWrapper(RECORDING_REGISTRY_STORE_NAME);
    var tab_id = this.get_tab_id();
    return recording_registry_idb.init().then(function() {
      return recording_registry_idb.getAll();
    }).then(function(recordings) {
      for (var i3 = 0; i3 < recordings.length; i3++) {
        if (isRecordingExpired(recordings[i3]) || recordings[i3]["tabId"] === tab_id) {
          return true;
        }
      }
      return false;
    }).catch(_3.bind(function(err) {
      this.report_error("Error checking recording registry", err);
    }, this));
  };
  MixpanelLib.prototype._check_and_start_session_recording = addOptOutCheckMixpanelLib(function(force_start) {
    if (!win["MutationObserver"]) {
      console$1.critical("Browser does not support MutationObserver; skipping session recording");
      return;
    }
    var loadRecorder = _3.bind(function(startNewIfInactive) {
      var handleLoadedRecorder = _3.bind(function() {
        this._recorder = this._recorder || new win["__mp_recorder"](this);
        this._recorder["resumeRecording"](startNewIfInactive);
      }, this);
      if (_3.isUndefined(win["__mp_recorder"])) {
        load_extra_bundle(this.get_config("recorder_src"), handleLoadedRecorder);
      } else {
        handleLoadedRecorder();
      }
    }, this);
    var is_sampled = this.get_config("record_sessions_percent") > 0 && Math.random() * 100 <= this.get_config("record_sessions_percent");
    if (force_start || is_sampled) {
      loadRecorder(true);
    } else {
      this._should_load_recorder().then(function(shouldLoad) {
        if (shouldLoad) {
          loadRecorder(false);
        }
      });
    }
  });
  MixpanelLib.prototype.start_session_recording = function() {
    this._check_and_start_session_recording(true);
  };
  MixpanelLib.prototype.stop_session_recording = function() {
    if (this._recorder) {
      return this._recorder["stopRecording"]();
    }
    return Promise.resolve();
  };
  MixpanelLib.prototype.pause_session_recording = function() {
    if (this._recorder) {
      return this._recorder["pauseRecording"]();
    }
    return Promise.resolve();
  };
  MixpanelLib.prototype.resume_session_recording = function() {
    if (this._recorder) {
      return this._recorder["resumeRecording"]();
    }
    return Promise.resolve();
  };
  MixpanelLib.prototype.is_recording_heatmap_data = function() {
    return this._get_session_replay_id() && this.get_config("record_heatmap_data");
  };
  MixpanelLib.prototype.get_session_recording_properties = function() {
    var props = {};
    var replay_id = this._get_session_replay_id();
    if (replay_id) {
      props["$mp_replay_id"] = replay_id;
    }
    return props;
  };
  MixpanelLib.prototype.get_session_replay_url = function() {
    var replay_url = null;
    var replay_id = this._get_session_replay_id();
    if (replay_id) {
      var query_params = _3.HTTPBuildQuery({
        "replay_id": replay_id,
        "distinct_id": this.get_distinct_id(),
        "token": this.get_config("token")
      });
      replay_url = "https://mixpanel.com/projects/replay-redirect?" + query_params;
    }
    return replay_url;
  };
  MixpanelLib.prototype._get_session_replay_id = function() {
    var replay_id = null;
    if (this._recorder) {
      replay_id = this._recorder["replayId"];
    }
    return replay_id || null;
  };
  MixpanelLib.prototype.__get_recorder = function() {
    return this._recorder;
  };
  MixpanelLib.prototype._loaded = function() {
    this.get_config("loaded")(this);
    this._set_default_superprops();
    this["people"].set_once(this["persistence"].get_referrer_info());
    if (this.get_config("store_google") && this.get_config("stop_utm_persistence")) {
      var utm_params = _3.info.campaignParams(null);
      _3.each(utm_params, function(_utm_value, utm_key) {
        this.unregister(utm_key);
      }.bind(this));
    }
  };
  MixpanelLib.prototype._set_default_superprops = function() {
    this["persistence"].update_search_keyword(document$1.referrer);
    if (this.get_config("store_google") && !this.get_config("stop_utm_persistence")) {
      this.register(_3.info.campaignParams());
    }
    if (this.get_config("save_referrer")) {
      this["persistence"].update_referrer_info(document$1.referrer);
    }
  };
  MixpanelLib.prototype._dom_loaded = function() {
    _3.each(this.__dom_loaded_queue, function(item) {
      this._track_dom.apply(this, item);
    }, this);
    if (!this.has_opted_out_tracking()) {
      _3.each(this.__request_queue, function(item) {
        this._send_request.apply(this, item);
      }, this);
    }
    delete this.__dom_loaded_queue;
    delete this.__request_queue;
  };
  MixpanelLib.prototype._track_dom = function(DomClass, args) {
    if (this.get_config("img")) {
      this.report_error("You can't use DOM tracking functions with img = true.");
      return false;
    }
    if (!DOM_LOADED) {
      this.__dom_loaded_queue.push([DomClass, args]);
      return false;
    }
    var dt = new DomClass().init(this);
    return dt.track.apply(dt, args);
  };
  MixpanelLib.prototype._prepare_callback = function(callback, data) {
    if (_3.isUndefined(callback)) {
      return null;
    }
    if (USE_XHR) {
      var callback_function = function(response) {
        callback(response, data);
      };
      return callback_function;
    } else {
      var jsc = this["_jsc"];
      var randomized_cb = "" + Math.floor(Math.random() * 1e8);
      var callback_string = this.get_config("callback_fn") + "[" + randomized_cb + "]";
      jsc[randomized_cb] = function(response) {
        delete jsc[randomized_cb];
        callback(response, data);
      };
      return callback_string;
    }
  };
  MixpanelLib.prototype._send_request = function(url, data, options, callback) {
    var succeeded = true;
    if (ENQUEUE_REQUESTS) {
      this.__request_queue.push(arguments);
      return succeeded;
    }
    var DEFAULT_OPTIONS = {
      method: this.get_config("api_method"),
      transport: this.get_config("api_transport"),
      verbose: this.get_config("verbose")
    };
    var body_data = null;
    if (!callback && (_3.isFunction(options) || typeof options === "string")) {
      callback = options;
      options = null;
    }
    options = _3.extend(DEFAULT_OPTIONS, options || {});
    if (!USE_XHR) {
      options.method = "GET";
    }
    var use_post = options.method === "POST";
    var use_sendBeacon = sendBeacon && use_post && options.transport.toLowerCase() === "sendbeacon";
    var verbose_mode = options.verbose;
    if (data["verbose"]) {
      verbose_mode = true;
    }
    if (this.get_config("test")) {
      data["test"] = 1;
    }
    if (verbose_mode) {
      data["verbose"] = 1;
    }
    if (this.get_config("img")) {
      data["img"] = 1;
    }
    if (!USE_XHR) {
      if (callback) {
        data["callback"] = callback;
      } else if (verbose_mode || this.get_config("test")) {
        data["callback"] = "(function(){})";
      }
    }
    data["ip"] = this.get_config("ip") ? 1 : 0;
    data["_"] = (/* @__PURE__ */ new Date()).getTime().toString();
    if (use_post) {
      body_data = "data=" + encodeURIComponent(data["data"]);
      delete data["data"];
    }
    _3.extend(data, this.get_config("api_extra_query_params"));
    url += "?" + _3.HTTPBuildQuery(data);
    var lib = this;
    if ("img" in data) {
      var img = document$1.createElement("img");
      img.src = url;
      document$1.body.appendChild(img);
    } else if (use_sendBeacon) {
      try {
        succeeded = sendBeacon(url, body_data);
      } catch (e3) {
        lib.report_error(e3);
        succeeded = false;
      }
      try {
        if (callback) {
          callback(succeeded ? 1 : 0);
        }
      } catch (e3) {
        lib.report_error(e3);
      }
    } else if (USE_XHR) {
      try {
        var req = new XMLHttpRequest();
        req.open(options.method, url, true);
        var headers = this.get_config("xhr_headers");
        if (use_post) {
          headers["Content-Type"] = "application/x-www-form-urlencoded";
        }
        _3.each(headers, function(headerValue, headerName) {
          req.setRequestHeader(headerName, headerValue);
        });
        if (options.timeout_ms && typeof req.timeout !== "undefined") {
          req.timeout = options.timeout_ms;
          var start_time = (/* @__PURE__ */ new Date()).getTime();
        }
        req.withCredentials = true;
        req.onreadystatechange = function() {
          if (req.readyState === 4) {
            if (req.status === 200) {
              if (callback) {
                if (verbose_mode) {
                  var response;
                  try {
                    response = _3.JSONDecode(req.responseText);
                  } catch (e3) {
                    lib.report_error(e3);
                    if (options.ignore_json_errors) {
                      response = req.responseText;
                    } else {
                      return;
                    }
                  }
                  callback(response);
                } else {
                  callback(Number(req.responseText));
                }
              }
            } else {
              var error;
              if (req.timeout && !req.status && (/* @__PURE__ */ new Date()).getTime() - start_time >= req.timeout) {
                error = "timeout";
              } else {
                error = "Bad HTTP status: " + req.status + " " + req.statusText;
              }
              lib.report_error(error);
              if (callback) {
                if (verbose_mode) {
                  var response_headers = req["responseHeaders"] || {};
                  callback({ status: 0, httpStatusCode: req["status"], error, retryAfter: response_headers["Retry-After"] });
                } else {
                  callback(0);
                }
              }
            }
          }
        };
        req.send(body_data);
      } catch (e3) {
        lib.report_error(e3);
        succeeded = false;
      }
    } else {
      var script = document$1.createElement("script");
      script.type = "text/javascript";
      script.async = true;
      script.defer = true;
      script.src = url;
      var s3 = document$1.getElementsByTagName("script")[0];
      s3.parentNode.insertBefore(script, s3);
    }
    return succeeded;
  };
  MixpanelLib.prototype._fetch_remote_settings = function(mode) {
    var disableRecordingIfStrict = function() {
      if (mode === "strict") {
        self2.set_config({ "record_sessions_percent": 0 });
      }
    };
    if (!win["AbortController"]) {
      console$1.critical("Remote settings unavailable: missing minimum required APIs");
      disableRecordingIfStrict();
      return Promise.resolve();
    }
    var settings_endpoint = this.get_api_host("settings") + "/" + this.get_config("api_routes")["settings"];
    var request_params = {
      "$lib_version": Config.LIB_VERSION,
      "mp_lib": "web",
      "sdk_config": "1"
    };
    var query_string = _3.HTTPBuildQuery(request_params);
    var full_url = settings_endpoint + "?" + query_string;
    var self2 = this;
    var abortController = new AbortController();
    var timeout_id = setTimeout(function() {
      abortController.abort();
    }, 500);
    var fetchOptions = {
      "method": "GET",
      "headers": {
        "Authorization": "Basic " + btoa(self2.get_config("token") + ":")
      },
      "signal": abortController.signal
    };
    return win["fetch"](full_url, fetchOptions).then(function(response) {
      clearTimeout(timeout_id);
      if (!response["ok"]) {
        console$1.critical("Network response was not ok");
        disableRecordingIfStrict();
        return;
      }
      return response.json();
    }).then(function(result2) {
      if (result2 && result2["sdk_config"] && result2["sdk_config"]["config"]) {
        var remote_config = result2["sdk_config"]["config"];
        var valid_config = {};
        _3.each(remote_config, function(value, key) {
          if (DEFAULT_CONFIG.hasOwnProperty(key)) {
            valid_config[key] = value;
          }
        });
        if (_3.isEmptyObject(valid_config)) {
          console$1.critical("No valid config keys found in remote settings.");
          disableRecordingIfStrict();
        } else {
          self2.set_config(valid_config);
        }
      } else {
        disableRecordingIfStrict();
      }
    }).catch(function(err) {
      clearTimeout(timeout_id);
      console$1.critical("Failed to fetch remote settings", err);
      disableRecordingIfStrict();
    });
  };
  MixpanelLib.prototype._execute_array = function(array) {
    var fn_name, alias_calls = [], other_calls = [], tracking_calls = [];
    _3.each(array, function(item) {
      if (item) {
        fn_name = item[0];
        if (_3.isArray(fn_name)) {
          tracking_calls.push(item);
        } else if (typeof item === "function") {
          item.call(this);
        } else if (_3.isArray(item) && fn_name === "alias") {
          alias_calls.push(item);
        } else if (_3.isArray(item) && fn_name.indexOf("track") !== -1 && typeof this[fn_name] === "function") {
          tracking_calls.push(item);
        } else {
          other_calls.push(item);
        }
      }
    }, this);
    var execute = function(calls, context) {
      _3.each(calls, function(item) {
        if (_3.isArray(item[0])) {
          var caller = context;
          _3.each(item, function(call) {
            caller = caller[call[0]].apply(caller, call.slice(1));
          });
        } else {
          this[item[0]].apply(this, item.slice(1));
        }
      }, context);
    };
    execute(alias_calls, this);
    execute(other_calls, this);
    execute(tracking_calls, this);
  };
  MixpanelLib.prototype.are_batchers_initialized = function() {
    return !!this.request_batchers.events;
  };
  MixpanelLib.prototype.get_batcher_configs = function() {
    var queue_prefix = "__mpq_" + this.get_config("token");
    this._batcher_configs = this._batcher_configs || {
      events: { type: "events", api_name: "track", queue_key: queue_prefix + "_ev" },
      people: { type: "people", api_name: "engage", queue_key: queue_prefix + "_pp" },
      groups: { type: "groups", api_name: "groups", queue_key: queue_prefix + "_gr" }
    };
    return this._batcher_configs;
  };
  MixpanelLib.prototype.init_batchers = function() {
    if (!this.are_batchers_initialized()) {
      var batcher_for = _3.bind(function(attrs) {
        return new RequestBatcher(
          attrs.queue_key,
          {
            libConfig: this["config"],
            errorReporter: this.get_config("error_reporter"),
            sendRequestFunc: _3.bind(function(data, options, cb) {
              var api_routes = this.get_config("api_routes");
              this._send_request(
                this.get_api_host(attrs.api_name) + "/" + api_routes[attrs.api_name],
                this._encode_data_for_request(data),
                options,
                this._prepare_callback(cb, data)
              );
            }, this),
            beforeSendHook: _3.bind(function(item) {
              var ret = this._run_hook("before_send_" + attrs.type, item);
              if (ret) {
                return ret[0];
              } else {
                return null;
              }
            }, this),
            stopAllBatchingFunc: _3.bind(this.stop_batch_senders, this),
            usePersistence: true
          }
        );
      }, this);
      var batcher_configs = this.get_batcher_configs();
      this.request_batchers = {
        events: batcher_for(batcher_configs.events),
        people: batcher_for(batcher_configs.people),
        groups: batcher_for(batcher_configs.groups)
      };
    }
    if (this.get_config("batch_autostart")) {
      this.start_batch_senders();
    }
  };
  MixpanelLib.prototype.start_batch_senders = function() {
    this._batchers_were_started = true;
    if (this.are_batchers_initialized()) {
      this._batch_requests = true;
      _3.each(this.request_batchers, function(batcher) {
        batcher.start();
      });
    }
  };
  MixpanelLib.prototype.stop_batch_senders = function() {
    this._batch_requests = false;
    _3.each(this.request_batchers, function(batcher) {
      batcher.stop();
      batcher.clear();
    });
  };
  MixpanelLib.prototype.push = function(item) {
    this._execute_array([item]);
  };
  MixpanelLib.prototype.disable = function(events) {
    if (typeof events === "undefined") {
      this._flags.disable_all_events = true;
    } else {
      this.__disabled_events = this.__disabled_events.concat(events);
    }
  };
  MixpanelLib.prototype._encode_data_for_request = function(data) {
    var encoded_data = JSONStringify(data);
    if (this.get_config("api_payload_format") === PAYLOAD_TYPE_BASE64) {
      encoded_data = _3.base64Encode(encoded_data);
    }
    return { "data": encoded_data };
  };
  MixpanelLib.prototype._track_or_batch = function(options, callback) {
    var truncated_data = _3.truncate(options.data, 255);
    var endpoint = options.endpoint;
    var batcher = options.batcher;
    var should_send_immediately = options.should_send_immediately;
    var send_request_options = options.send_request_options || {};
    callback = callback || NOOP_FUNC;
    var request_enqueued_or_initiated = true;
    var send_request_immediately = _3.bind(function() {
      if (!send_request_options.skip_hooks) {
        truncated_data = this._run_hook("before_send_" + options.type, truncated_data);
        if (truncated_data) {
          truncated_data = truncated_data[0];
        }
      }
      if (truncated_data) {
        console$1.log("MIXPANEL REQUEST:");
        console$1.log(truncated_data);
        return this._send_request(
          endpoint,
          this._encode_data_for_request(truncated_data),
          send_request_options,
          this._prepare_callback(callback, truncated_data)
        );
      } else {
        return null;
      }
    }, this);
    if (this._batch_requests && !should_send_immediately) {
      batcher.enqueue(truncated_data).then(function(succeeded) {
        if (succeeded) {
          callback(1, truncated_data);
        } else {
          send_request_immediately();
        }
      });
    } else {
      request_enqueued_or_initiated = send_request_immediately();
    }
    return request_enqueued_or_initiated && truncated_data;
  };
  MixpanelLib.prototype.track = addOptOutCheckMixpanelLib(function(event_name, properties, options, callback) {
    var ret;
    if (!(options && options.skip_hooks)) {
      ret = this._run_hook("before_track", event_name, properties);
      if (ret === null) {
        return;
      } else {
        event_name = ret[0];
        properties = ret[1];
      }
    }
    if (!callback && typeof options === "function") {
      callback = options;
      options = null;
    }
    options = options || {};
    var transport = options["transport"];
    if (transport) {
      options.transport = transport;
    }
    var should_send_immediately = options["send_immediately"];
    if (typeof callback !== "function") {
      callback = NOOP_FUNC;
    }
    if (_3.isUndefined(event_name)) {
      this.report_error("No event name provided to mixpanel.track");
      return;
    }
    if (this._event_is_disabled(event_name)) {
      callback(0);
      return;
    }
    properties = _3.extend({}, properties);
    properties["token"] = this.get_config("token");
    var start_timestamp = this["persistence"].remove_event_timer(event_name);
    if (!_3.isUndefined(start_timestamp)) {
      var duration_in_ms = (/* @__PURE__ */ new Date()).getTime() - start_timestamp;
      properties["$duration"] = parseFloat((duration_in_ms / 1e3).toFixed(3));
    }
    this._set_default_superprops();
    var marketing_properties = this.get_config("track_marketing") ? _3.info.marketingParams() : {};
    properties = _3.extend(
      {},
      _3.info.properties({ "mp_loader": this.get_config("mp_loader") }),
      marketing_properties,
      this["persistence"].properties(),
      this.unpersisted_superprops,
      this.get_session_recording_properties(),
      properties
    );
    var property_blacklist = this.get_config("property_blacklist");
    if (_3.isArray(property_blacklist)) {
      _3.each(property_blacklist, function(blacklisted_prop) {
        delete properties[blacklisted_prop];
      });
    } else {
      this.report_error("Invalid value for property_blacklist config: " + property_blacklist);
    }
    var data = {
      "event": event_name,
      "properties": properties
    };
    ret = this._track_or_batch({
      type: "events",
      data,
      endpoint: this.get_api_host("events") + "/" + this.get_config("api_routes")["track"],
      batcher: this.request_batchers.events,
      should_send_immediately,
      send_request_options: options
    }, callback);
    return ret;
  });
  MixpanelLib.prototype.set_group = addOptOutCheckMixpanelLib(function(group_key, group_ids, callback) {
    if (!_3.isArray(group_ids)) {
      group_ids = [group_ids];
    }
    var prop = {};
    prop[group_key] = group_ids;
    this.register(prop);
    return this["people"].set(group_key, group_ids, callback);
  });
  MixpanelLib.prototype.add_group = addOptOutCheckMixpanelLib(function(group_key, group_id, callback) {
    var old_values = this.get_property(group_key);
    var prop = {};
    if (old_values === void 0) {
      prop[group_key] = [group_id];
      this.register(prop);
    } else {
      if (old_values.indexOf(group_id) === -1) {
        old_values.push(group_id);
        prop[group_key] = old_values;
        this.register(prop);
      }
    }
    return this["people"].union(group_key, group_id, callback);
  });
  MixpanelLib.prototype.remove_group = addOptOutCheckMixpanelLib(function(group_key, group_id, callback) {
    var old_value = this.get_property(group_key);
    if (old_value !== void 0) {
      var idx = old_value.indexOf(group_id);
      if (idx > -1) {
        old_value.splice(idx, 1);
        this.register({ group_key: old_value });
      }
      if (old_value.length === 0) {
        this.unregister(group_key);
      }
    }
    return this["people"].remove(group_key, group_id, callback);
  });
  MixpanelLib.prototype.track_with_groups = addOptOutCheckMixpanelLib(function(event_name, properties, groups, callback) {
    var tracking_props = _3.extend({}, properties || {});
    _3.each(groups, function(v3, k4) {
      if (v3 !== null && v3 !== void 0) {
        tracking_props[k4] = v3;
      }
    });
    return this.track(event_name, tracking_props, callback);
  });
  MixpanelLib.prototype._create_map_key = function(group_key, group_id) {
    return group_key + "_" + JSON.stringify(group_id);
  };
  MixpanelLib.prototype._remove_group_from_cache = function(group_key, group_id) {
    delete this._cached_groups[this._create_map_key(group_key, group_id)];
  };
  MixpanelLib.prototype.get_group = function(group_key, group_id) {
    var map_key = this._create_map_key(group_key, group_id);
    var group = this._cached_groups[map_key];
    if (group === void 0 || group._group_key !== group_key || group._group_id !== group_id) {
      group = new MixpanelGroup();
      group._init(this, group_key, group_id);
      this._cached_groups[map_key] = group;
    }
    return group;
  };
  MixpanelLib.prototype.track_pageview = addOptOutCheckMixpanelLib(function(properties, options) {
    if (typeof properties !== "object") {
      properties = {};
    }
    options = options || {};
    var event_name = options["event_name"] || "$mp_web_page_view";
    var default_page_properties = _3.extend(
      _3.info.mpPageViewProperties(),
      _3.info.campaignParams(),
      _3.info.clickParams()
    );
    var event_properties = _3.extend(
      {},
      default_page_properties,
      properties
    );
    return this.track(event_name, event_properties);
  });
  MixpanelLib.prototype.track_links = function() {
    return this._track_dom.call(this, LinkTracker, arguments);
  };
  MixpanelLib.prototype.track_forms = function() {
    return this._track_dom.call(this, FormTracker, arguments);
  };
  MixpanelLib.prototype.time_event = function(event_name) {
    if (_3.isUndefined(event_name)) {
      this.report_error("No event name provided to mixpanel.time_event");
      return;
    }
    if (this._event_is_disabled(event_name)) {
      return;
    }
    this["persistence"].set_event_timer(event_name, (/* @__PURE__ */ new Date()).getTime());
  };
  var REGISTER_DEFAULTS = {
    "persistent": true
  };
  var options_for_register = function(days_or_options) {
    var options;
    if (_3.isObject(days_or_options)) {
      options = days_or_options;
    } else if (!_3.isUndefined(days_or_options)) {
      options = { "days": days_or_options };
    } else {
      options = {};
    }
    return _3.extend({}, REGISTER_DEFAULTS, options);
  };
  MixpanelLib.prototype.register = function(props, days_or_options) {
    var ret = this._run_hook("before_register", props, days_or_options);
    if (ret === null) {
      return;
    } else {
      props = ret[0];
      days_or_options = ret[1];
    }
    var options = options_for_register(days_or_options);
    if (options["persistent"]) {
      this["persistence"].register(props, options["days"]);
    } else {
      _3.extend(this.unpersisted_superprops, props);
    }
  };
  MixpanelLib.prototype.register_once = function(props, default_value, days_or_options) {
    var ret = this._run_hook("before_register_once", props, default_value, days_or_options);
    if (ret === null) {
      return;
    } else {
      props = ret[0];
      default_value = ret[1];
      days_or_options = ret[2];
    }
    var options = options_for_register(days_or_options);
    if (options["persistent"]) {
      this["persistence"].register_once(props, default_value, options["days"]);
    } else {
      if (typeof default_value === "undefined") {
        default_value = "None";
      }
      _3.each(props, function(val, prop) {
        if (!this.unpersisted_superprops.hasOwnProperty(prop) || this.unpersisted_superprops[prop] === default_value) {
          this.unpersisted_superprops[prop] = val;
        }
      }, this);
    }
  };
  MixpanelLib.prototype.unregister = function(property, options) {
    var ret = this._run_hook("before_unregister", property, options);
    if (ret === null) {
      return;
    } else {
      property = ret[0];
      options = ret[1];
    }
    options = options_for_register(options);
    if (options["persistent"]) {
      this["persistence"].unregister(property);
    } else {
      delete this.unpersisted_superprops[property];
    }
  };
  MixpanelLib.prototype._register_single = function(prop, value) {
    var props = {};
    props[prop] = value;
    this.register(props);
  };
  MixpanelLib.prototype.identify = function(new_distinct_id, _set_callback, _add_callback, _append_callback, _set_once_callback, _union_callback, _unset_callback, _remove_callback) {
    var ret = this._run_hook("before_identify", new_distinct_id);
    if (ret === null) {
      return -1;
    } else {
      new_distinct_id = ret[0];
    }
    var previous_distinct_id = this.get_distinct_id();
    if (new_distinct_id && previous_distinct_id !== new_distinct_id) {
      if (typeof new_distinct_id === "string" && new_distinct_id.indexOf(DEVICE_ID_PREFIX) === 0) {
        this.report_error("distinct_id cannot have $device: prefix");
        return -1;
      }
      this.register({ "$user_id": new_distinct_id });
    }
    if (!this.get_property("$device_id")) {
      var device_id = previous_distinct_id;
      this.register_once({
        "$had_persisted_distinct_id": true,
        "$device_id": device_id
      }, "");
    }
    if (new_distinct_id !== previous_distinct_id && new_distinct_id !== this.get_property(ALIAS_ID_KEY)) {
      this.unregister(ALIAS_ID_KEY);
      this.register({ "distinct_id": new_distinct_id });
    }
    this._flags.identify_called = true;
    this["people"]._flush(_set_callback, _add_callback, _append_callback, _set_once_callback, _union_callback, _unset_callback, _remove_callback);
    if (new_distinct_id !== previous_distinct_id) {
      this.track("$identify", {
        "distinct_id": new_distinct_id,
        "$anon_distinct_id": previous_distinct_id
      }, { skip_hooks: true });
    }
    if (new_distinct_id !== previous_distinct_id) {
      this.flags.fetchFlags();
    }
  };
  MixpanelLib.prototype.reset = function() {
    this.stop_session_recording();
    this["persistence"].clear();
    this._flags.identify_called = false;
    var uuid = _3.UUID();
    this.register_once({
      "distinct_id": DEVICE_ID_PREFIX + uuid,
      "$device_id": uuid
    }, "");
    this._check_and_start_session_recording();
  };
  MixpanelLib.prototype.get_distinct_id = function() {
    return this.get_property("distinct_id");
  };
  MixpanelLib.prototype.alias = function(alias, original) {
    if (alias === this.get_property(PEOPLE_DISTINCT_ID_KEY)) {
      this.report_error("Attempting to create alias for existing People user - aborting.");
      return -2;
    }
    var _this = this;
    if (_3.isUndefined(original)) {
      original = this.get_distinct_id();
    }
    if (alias !== original) {
      this._register_single(ALIAS_ID_KEY, alias);
      return this.track("$create_alias", {
        "alias": alias,
        "distinct_id": original
      }, {
        skip_hooks: true
      }, function() {
        _this.identify(alias);
      });
    } else {
      this.report_error("alias matches current distinct_id - skipping api call.");
      this.identify(alias);
      return -1;
    }
  };
  MixpanelLib.prototype.name_tag = function(name_tag) {
    this._register_single("mp_name_tag", name_tag);
  };
  MixpanelLib.prototype.set_config = function(config) {
    if (_3.isObject(config)) {
      _3.extend(this["config"], config);
      var new_batch_size = config["batch_size"];
      if (new_batch_size) {
        _3.each(this.request_batchers, function(batcher) {
          batcher.resetBatchSize();
        });
      }
      if (!this.get_config("persistence_name")) {
        this["config"]["persistence_name"] = this["config"]["cookie_name"];
      }
      if (!this.get_config("disable_persistence")) {
        this["config"]["disable_persistence"] = this["config"]["disable_cookie"];
      }
      if (this["persistence"]) {
        this["persistence"].update_config(this["config"]);
      }
      Config.DEBUG = Config.DEBUG || this.get_config("debug");
      if (("autocapture" in config || "record_heatmap_data" in config) && this.autocapture) {
        this.autocapture.init();
      }
      if (_3.isObject(config["hooks"])) {
        this.hooks = {};
        _3.each(config["hooks"], function(hook_value, hook_name) {
          if (_3.isFunction(hook_value)) {
            this.hooks[hook_name] = [hook_value];
          } else if (_3.isArray(hook_value)) {
            this.hooks[hook_name] = [];
            for (var i3 = 0; i3 < hook_value.length; i3++) {
              if (!_3.isFunction(hook_value[i3])) {
                console$1.critical("Invalid hook added. Hook is not a function");
              }
              this.hooks[hook_name].push(hook_value[i3]);
            }
          } else {
            console$1.critical("Invalid hooks added. Ensure that the hook values passed into config.hooks are functions or arrays of functions.");
          }
        }, this);
      }
    }
  };
  MixpanelLib.prototype.get_config = function(prop_name) {
    return this["config"][prop_name];
  };
  MixpanelLib.prototype._run_hook = function(hook_name) {
    var hook_data = slice.call(arguments, 1);
    _3.each(this.hooks[hook_name], function(hook) {
      if (hook_data === null) {
        return null;
      }
      var ret = hook.apply(this, hook_data);
      if (typeof ret === "undefined") {
        this.report_error(hook_name + " hook did not return a valid value");
        hook_data = null;
      } else {
        if (!_3.isArray(ret)) {
          ret = [ret];
        }
        hook_data.splice.apply(hook_data, [0, ret.length].concat(ret));
      }
    }, this);
    return hook_data;
  };
  MixpanelLib.prototype.get_property = function(property_name) {
    return this["persistence"].load_prop([property_name]);
  };
  MixpanelLib.prototype.get_api_host = function(endpoint_type) {
    return this.get_config("api_hosts")[endpoint_type] || this.get_config("api_host");
  };
  MixpanelLib.prototype.toString = function() {
    var name5 = this.get_config("name");
    if (name5 !== PRIMARY_INSTANCE_NAME) {
      name5 = PRIMARY_INSTANCE_NAME + "." + name5;
    }
    return name5;
  };
  MixpanelLib.prototype._event_is_disabled = function(event_name) {
    return _3.isBlockedUA(userAgent) || this._flags.disable_all_events || _3.include(this.__disabled_events, event_name);
  };
  MixpanelLib.prototype._gdpr_init = function() {
    var is_localStorage_requested = this.get_config("opt_out_tracking_persistence_type") === "localStorage";
    if (is_localStorage_requested && _3.localStorage.is_supported()) {
      if (!this.has_opted_in_tracking() && this.has_opted_in_tracking({ "persistence_type": "cookie" })) {
        this.opt_in_tracking({ "enable_persistence": false });
      }
      if (!this.has_opted_out_tracking() && this.has_opted_out_tracking({ "persistence_type": "cookie" })) {
        this.opt_out_tracking({ "clear_persistence": false });
      }
      this.clear_opt_in_out_tracking({
        "persistence_type": "cookie",
        "enable_persistence": false
      });
    }
    if (this.has_opted_out_tracking()) {
      this._gdpr_update_persistence({ "clear_persistence": true });
    } else if (!this.has_opted_in_tracking() && (this.get_config("opt_out_tracking_by_default") || _3.cookie.get("mp_optout"))) {
      _3.cookie.remove("mp_optout");
      this.opt_out_tracking({
        "clear_persistence": this.get_config("opt_out_persistence_by_default")
      });
    }
  };
  MixpanelLib.prototype._gdpr_update_persistence = function(options) {
    var disabled;
    if (options && options["clear_persistence"]) {
      disabled = true;
    } else if (options && options["enable_persistence"]) {
      disabled = false;
    } else {
      return;
    }
    if (!this.get_config("disable_persistence") && this["persistence"].disabled !== disabled) {
      this["persistence"].set_disabled(disabled);
    }
    if (disabled) {
      this.stop_batch_senders();
      this.stop_session_recording();
    } else {
      if (this._batchers_were_started) {
        this.start_batch_senders();
      }
    }
  };
  MixpanelLib.prototype._gdpr_call_func = function(func, options) {
    options = _3.extend({
      "track": _3.bind(this.track, this),
      "persistence_type": this.get_config("opt_out_tracking_persistence_type"),
      "cookie_prefix": this.get_config("opt_out_tracking_cookie_prefix"),
      "cookie_expiration": this.get_config("cookie_expiration"),
      "cross_site_cookie": this.get_config("cross_site_cookie"),
      "cross_subdomain_cookie": this.get_config("cross_subdomain_cookie"),
      "cookie_domain": this.get_config("cookie_domain"),
      "secure_cookie": this.get_config("secure_cookie"),
      "ignore_dnt": this.get_config("ignore_dnt")
    }, options);
    if (!_3.localStorage.is_supported()) {
      options["persistence_type"] = "cookie";
    }
    return func(this.get_config("token"), {
      track: options["track"],
      trackEventName: options["track_event_name"],
      trackProperties: options["track_properties"],
      persistenceType: options["persistence_type"],
      persistencePrefix: options["cookie_prefix"],
      cookieDomain: options["cookie_domain"],
      cookieExpiration: options["cookie_expiration"],
      crossSiteCookie: options["cross_site_cookie"],
      crossSubdomainCookie: options["cross_subdomain_cookie"],
      secureCookie: options["secure_cookie"],
      ignoreDnt: options["ignore_dnt"]
    });
  };
  MixpanelLib.prototype.opt_in_tracking = function(options) {
    options = _3.extend({
      "enable_persistence": true
    }, options);
    this._gdpr_call_func(optIn, options);
    this._gdpr_update_persistence(options);
  };
  MixpanelLib.prototype.opt_out_tracking = function(options) {
    options = _3.extend({
      "clear_persistence": true,
      "delete_user": true
    }, options);
    if (options["delete_user"] && this["people"] && this["people"]._identify_called()) {
      this["people"].delete_user();
      this["people"].clear_charges();
    }
    this._gdpr_call_func(optOut, options);
    this._gdpr_update_persistence(options);
  };
  MixpanelLib.prototype.has_opted_in_tracking = function(options) {
    return this._gdpr_call_func(hasOptedIn, options);
  };
  MixpanelLib.prototype.has_opted_out_tracking = function(options) {
    return this._gdpr_call_func(hasOptedOut, options);
  };
  MixpanelLib.prototype.clear_opt_in_out_tracking = function(options) {
    options = _3.extend({
      "enable_persistence": true
    }, options);
    this._gdpr_call_func(clearOptInOut, options);
    this._gdpr_update_persistence(options);
  };
  MixpanelLib.prototype.report_error = function(msg, err) {
    console$1.error.apply(console$1.error, arguments);
    try {
      if (!err && !(msg instanceof Error)) {
        msg = new Error(msg);
      }
      this.get_config("error_reporter")(msg, err);
    } catch (err2) {
      console$1.error(err2);
    }
  };
  MixpanelLib.prototype.add_hook = function(hook_name, hook_fn) {
    if (!this.hooks[hook_name]) {
      this.hooks[hook_name] = [];
    }
    this.hooks[hook_name].push(hook_fn);
  };
  MixpanelLib.prototype.remove_hook = function(hook_name, hook_fn) {
    var fn_index;
    if (this.hooks[hook_name]) {
      fn_index = this.hooks[hook_name].indexOf(hook_fn);
      if (fn_index !== -1) {
        this.hooks[hook_name].splice(fn_index, 1);
      } else {
        console$1.log("remove_hook failed. Matching hook was not found");
      }
    }
  };
  MixpanelLib.prototype["init"] = MixpanelLib.prototype.init;
  MixpanelLib.prototype["reset"] = MixpanelLib.prototype.reset;
  MixpanelLib.prototype["disable"] = MixpanelLib.prototype.disable;
  MixpanelLib.prototype["time_event"] = MixpanelLib.prototype.time_event;
  MixpanelLib.prototype["track"] = MixpanelLib.prototype.track;
  MixpanelLib.prototype["track_links"] = MixpanelLib.prototype.track_links;
  MixpanelLib.prototype["track_forms"] = MixpanelLib.prototype.track_forms;
  MixpanelLib.prototype["track_pageview"] = MixpanelLib.prototype.track_pageview;
  MixpanelLib.prototype["register"] = MixpanelLib.prototype.register;
  MixpanelLib.prototype["register_once"] = MixpanelLib.prototype.register_once;
  MixpanelLib.prototype["unregister"] = MixpanelLib.prototype.unregister;
  MixpanelLib.prototype["identify"] = MixpanelLib.prototype.identify;
  MixpanelLib.prototype["alias"] = MixpanelLib.prototype.alias;
  MixpanelLib.prototype["name_tag"] = MixpanelLib.prototype.name_tag;
  MixpanelLib.prototype["set_config"] = MixpanelLib.prototype.set_config;
  MixpanelLib.prototype["get_config"] = MixpanelLib.prototype.get_config;
  MixpanelLib.prototype["get_api_host"] = MixpanelLib.prototype.get_api_host;
  MixpanelLib.prototype["get_property"] = MixpanelLib.prototype.get_property;
  MixpanelLib.prototype["get_distinct_id"] = MixpanelLib.prototype.get_distinct_id;
  MixpanelLib.prototype["toString"] = MixpanelLib.prototype.toString;
  MixpanelLib.prototype["opt_out_tracking"] = MixpanelLib.prototype.opt_out_tracking;
  MixpanelLib.prototype["opt_in_tracking"] = MixpanelLib.prototype.opt_in_tracking;
  MixpanelLib.prototype["has_opted_out_tracking"] = MixpanelLib.prototype.has_opted_out_tracking;
  MixpanelLib.prototype["has_opted_in_tracking"] = MixpanelLib.prototype.has_opted_in_tracking;
  MixpanelLib.prototype["clear_opt_in_out_tracking"] = MixpanelLib.prototype.clear_opt_in_out_tracking;
  MixpanelLib.prototype["get_group"] = MixpanelLib.prototype.get_group;
  MixpanelLib.prototype["set_group"] = MixpanelLib.prototype.set_group;
  MixpanelLib.prototype["add_group"] = MixpanelLib.prototype.add_group;
  MixpanelLib.prototype["remove_group"] = MixpanelLib.prototype.remove_group;
  MixpanelLib.prototype["add_hook"] = MixpanelLib.prototype.add_hook;
  MixpanelLib.prototype["remove_hook"] = MixpanelLib.prototype.remove_hook;
  MixpanelLib.prototype["track_with_groups"] = MixpanelLib.prototype.track_with_groups;
  MixpanelLib.prototype["start_batch_senders"] = MixpanelLib.prototype.start_batch_senders;
  MixpanelLib.prototype["stop_batch_senders"] = MixpanelLib.prototype.stop_batch_senders;
  MixpanelLib.prototype["start_session_recording"] = MixpanelLib.prototype.start_session_recording;
  MixpanelLib.prototype["stop_session_recording"] = MixpanelLib.prototype.stop_session_recording;
  MixpanelLib.prototype["pause_session_recording"] = MixpanelLib.prototype.pause_session_recording;
  MixpanelLib.prototype["resume_session_recording"] = MixpanelLib.prototype.resume_session_recording;
  MixpanelLib.prototype["get_session_recording_properties"] = MixpanelLib.prototype.get_session_recording_properties;
  MixpanelLib.prototype["get_session_replay_url"] = MixpanelLib.prototype.get_session_replay_url;
  MixpanelLib.prototype["get_tab_id"] = MixpanelLib.prototype.get_tab_id;
  MixpanelLib.prototype["DEFAULT_API_ROUTES"] = DEFAULT_API_ROUTES;
  MixpanelLib.prototype["__get_recorder"] = MixpanelLib.prototype.__get_recorder;
  MixpanelPersistence.prototype["properties"] = MixpanelPersistence.prototype.properties;
  MixpanelPersistence.prototype["update_search_keyword"] = MixpanelPersistence.prototype.update_search_keyword;
  MixpanelPersistence.prototype["update_referrer_info"] = MixpanelPersistence.prototype.update_referrer_info;
  MixpanelPersistence.prototype["get_cross_subdomain"] = MixpanelPersistence.prototype.get_cross_subdomain;
  MixpanelPersistence.prototype["clear"] = MixpanelPersistence.prototype.clear;
  var instances = {};
  var extend_mp = function() {
    _3.each(instances, function(instance, name5) {
      if (name5 !== PRIMARY_INSTANCE_NAME) {
        mixpanel_master[name5] = instance;
      }
    });
    mixpanel_master["_"] = _3;
  };
  var override_mp_init_func = function() {
    mixpanel_master["init"] = function(token, config, name5) {
      if (name5) {
        if (!mixpanel_master[name5]) {
          mixpanel_master[name5] = instances[name5] = create_mplib(token, config, name5);
          mixpanel_master[name5]._loaded();
        }
        return mixpanel_master[name5];
      } else {
        var instance = mixpanel_master;
        if (instances[PRIMARY_INSTANCE_NAME]) {
          instance = instances[PRIMARY_INSTANCE_NAME];
        } else if (token) {
          instance = create_mplib(token, config, PRIMARY_INSTANCE_NAME);
          instance._loaded();
          instances[PRIMARY_INSTANCE_NAME] = instance;
        }
        mixpanel_master = instance;
        if (init_type === INIT_SNIPPET) {
          win[PRIMARY_INSTANCE_NAME] = mixpanel_master;
        }
        extend_mp();
      }
    };
  };
  var add_dom_loaded_handler = function() {
    function dom_loaded_handler() {
      if (dom_loaded_handler.done) {
        return;
      }
      dom_loaded_handler.done = true;
      DOM_LOADED = true;
      ENQUEUE_REQUESTS = false;
      _3.each(instances, function(inst) {
        inst._dom_loaded();
      });
    }
    function do_scroll_check() {
      try {
        document$1.documentElement.doScroll("left");
      } catch (e3) {
        setTimeout(do_scroll_check, 1);
        return;
      }
      dom_loaded_handler();
    }
    if (document$1.addEventListener) {
      if (document$1.readyState === "complete") {
        dom_loaded_handler();
      } else {
        document$1.addEventListener("DOMContentLoaded", dom_loaded_handler, false);
      }
    } else if (document$1.attachEvent) {
      document$1.attachEvent("onreadystatechange", dom_loaded_handler);
      var toplevel = false;
      try {
        toplevel = win.frameElement === null;
      } catch (e3) {
      }
      if (document$1.documentElement.doScroll && toplevel) {
        do_scroll_check();
      }
    }
    _3.register_event(win, "load", dom_loaded_handler, true);
  };
  function init_as_module(bundle_loader) {
    load_extra_bundle = bundle_loader;
    init_type = INIT_MODULE;
    mixpanel_master = new MixpanelLib();
    override_mp_init_func();
    mixpanel_master["init"]();
    add_dom_loaded_handler();
    return mixpanel_master;
  }
  function loadNoop(_src, onload) {
    onload();
  }
  var mixpanel = init_as_module(loadNoop);

  // src/utils/bubble.js
  var sendToBubble = (fnName, action, data = {}) => {
    const payload = {
      output1: String(action),
      output2: JSON.stringify(data)
    };
    if (window.BubbleBridge) {
      console.log(`\u{1F4E1} [${fnName}]`, { action, ...data });
      window.BubbleBridge.send(fnName, payload);
    } else {
      console.warn(`\u26A0\uFE0F BubbleBridge not found [${fnName}]`, { action, ...data });
    }
  };

  // src/config.js
  var BUBBLE_CDN = "https://0fc323560b9c4d8afc3a7d487716abb6.cdn.bubble.io";
  var APP_VERSION = "v1.0.0-alpha-ebd60d919768";
  var FIREBASE_CONFIG = {
    apiKey: "",
    authDomain: "",
    projectId: "",
    storageBucket: "",
    messagingSenderId: "",
    appId: "",
    measurementId: ""
  };

  // src/components/WelcomeScreen.jsx
  var WelcomeScreen = ({ deviceId: _deviceId, onAction }) => {
    const bgImage = `${BUBBLE_CDN}/f1744960311608x780031988693140400/BG%20%281%29.png`;
    const handleAction = (action) => {
      if (action !== "signin") {
        sendToBubble("bubble_fn_welcome", action);
      }
      if (onAction) onAction(action);
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "relative w-full h-full bg-black font-jakarta overflow-hidden" }, /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 z-0" }, /* @__PURE__ */ xn.createElement("img", { src: bgImage, className: "w-full h-full object-cover opacity-80", alt: "Couple" }), /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 bg-gradient-to-b from-black/30 via-transparent to-black/90" })), /* @__PURE__ */ xn.createElement("div", { className: "relative z-10 flex flex-col h-full px-6 py-10" }, /* @__PURE__ */ xn.createElement("div", { className: "mx-auto flex flex-col items-center" }, /* @__PURE__ */ xn.createElement("svg", { width: "56", height: "56", viewBox: "0 0 64 64", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "32", cy: "32", r: "32", fill: "white" }), /* @__PURE__ */ xn.createElement("rect", { x: "19", y: "19", width: "26", height: "26", fill: "#FF2258" })), /* @__PURE__ */ xn.createElement("div", { className: "text-white text-center mt-2 font-bold tracking-widest text-[10px]" }, "BONDS")), /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-h-[20px]" }), /* @__PURE__ */ xn.createElement("div", { className: "mb-8" }, /* @__PURE__ */ xn.createElement("h1", { className: "text-white text-[32px] leading-[38px] font-normal mb-3" }, "Your Relationship", /* @__PURE__ */ xn.createElement("br", null), "Superpower"), /* @__PURE__ */ xn.createElement("p", { className: "text-white/80 text-base font-light leading-relaxed" }, "We learn your dynamics and tailor expert built, AI powered insights & actions")), /* @__PURE__ */ xn.createElement("div", { className: "space-y-6" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: () => handleAction("go"),
        className: "w-full h-[58px] rounded-[40px] bg-gradient-to-l from-[#B900B0] to-[#D8003F] flex items-center justify-center shadow-lg transform transition active:scale-95 btn-pressed"
      },
      /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-semibold text-[18px] text-white tracking-[3px] uppercase" }, "LET\u2019S GO")
    ), /* @__PURE__ */ xn.createElement("div", { className: "text-center" }, /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta text-[16px] text-white tracking-[0.2px]" }, "Got an account? ", " ", /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: () => handleAction("signin"),
        className: "font-bold border-b border-white hover:opacity-80 transition"
      },
      "Sign In here"
    ))), /* @__PURE__ */ xn.createElement("p", { className: "text-center text-white/20 text-[10px] mt-4" }, APP_VERSION))));
  };
  var WelcomeScreen_default = WelcomeScreen;

  // src/components/SignInScreen.jsx
  var isIOS = /iPad|iPhone|iPod/.test(navigator.userAgent) || navigator.platform === "MacIntel" && navigator.maxTouchPoints > 1;
  var GoogleIcon = () => /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 48 48" }, /* @__PURE__ */ xn.createElement("path", { fill: "#EA4335", d: "M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z" }), /* @__PURE__ */ xn.createElement("path", { fill: "#4285F4", d: "M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z" }), /* @__PURE__ */ xn.createElement("path", { fill: "#FBBC05", d: "M10.53 28.59a14.5 14.5 0 0 1 0-9.18l-7.98-6.19a24.1 24.1 0 0 0 0 21.56l7.98-6.19z" }), /* @__PURE__ */ xn.createElement("path", { fill: "#34A853", d: "M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z" }));
  var AppleIcon = ({ color }) => /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: color }, /* @__PURE__ */ xn.createElement("path", { d: "M17.05 20.28c-.98.95-2.05.88-3.08.4-1.09-.5-2.08-.52-3.23 0-1.44.64-2.2.52-3.06-.4C3.79 16.17 4.36 9.63 8.7 9.38c1.28.06 2.15.72 2.9.76.98-.2 1.92-.77 2.98-.7 1.27.1 2.23.6 2.84 1.53-2.6 1.54-1.98 4.93.37 5.87-.46 1.17-.67 1.7-1.27 2.72-.84 1.4-2.02 2.8-3.47 2.72zm-.1-14.2c-1.7.13-3.16 1.86-2.96 3.67 1.58.13 3.24-1.63 2.96-3.67z" }));
  var SignInScreen = ({ theme, onBack }) => {
    const handleGoogleSignIn = () => {
      localStorage.setItem("bonds_auth_pending", "true");
      sendToBubble("bubble_fn_signin", "google_signin");
    };
    const handleAppleSignIn = () => {
      if (typeof NativelyAppleSignInService === "undefined") return;
      const appleService = new NativelyAppleSignInService();
      appleService.signin((response) => {
        if (!response.status) return;
        localStorage.setItem("bonds_auth_pending", "true");
        sendToBubble("bubble_fn_signin", "apple_signin", {
          email: response.email,
          subject: response.subject,
          givenname: response.givenname,
          familyname: response.familyname,
          initial: response.initial
        });
      });
    };
    return /* @__PURE__ */ xn.createElement("div", { style: { background: theme.bg }, className: "relative w-full h-full font-jakarta overflow-hidden" }, /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full px-6 py-10" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: onBack,
        className: "w-10 h-10 rounded-full flex items-center justify-center transition-opacity active:opacity-60",
        style: { background: theme.surface, border: `1px solid ${theme.border}` }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 20 20", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M12.5 15L7.5 10L12.5 5", stroke: theme.textPrimary, strokeWidth: "1.8", strokeLinecap: "round", strokeLinejoin: "round" }))
    ), /* @__PURE__ */ xn.createElement("div", { className: "mx-auto flex flex-col items-center mt-8" }, /* @__PURE__ */ xn.createElement("svg", { width: "56", height: "56", viewBox: "0 0 64 64", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "32", cy: "32", r: "32", fill: theme.textPrimary }), /* @__PURE__ */ xn.createElement("rect", { x: "19", y: "19", width: "26", height: "26", fill: "#FF2258" })), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "text-center mt-2 font-bold tracking-widest text-[0.625rem]",
        style: { color: theme.textPrimary }
      },
      "BONDS"
    )), /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-h-[1.25rem]" }), /* @__PURE__ */ xn.createElement("div", { className: "mb-8" }, /* @__PURE__ */ xn.createElement(
      "h1",
      {
        className: "text-[2rem] leading-[2.375rem] font-normal mb-3",
        style: { color: theme.textPrimary }
      },
      "Welcome back"
    ), /* @__PURE__ */ xn.createElement(
      "p",
      {
        className: "text-base font-light leading-relaxed",
        style: { color: theme.textSecondary }
      },
      "Sign in to continue your relationship journey"
    )), /* @__PURE__ */ xn.createElement("div", { className: "space-y-3" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleGoogleSignIn,
        className: "w-full h-[3.625rem] rounded-[2.5rem] bg-white flex items-center justify-center gap-3 shadow-md transform transition active:scale-95",
        style: { border: "1px solid rgba(0,0,0,0.1)" }
      },
      /* @__PURE__ */ xn.createElement(GoogleIcon, null),
      /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-semibold text-[1rem] text-[#3c4043] tracking-[0.25px]" }, "Continue with Google")
    ), isIOS && /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleAppleSignIn,
        className: "w-full h-[3.625rem] rounded-[2.5rem] bg-black flex items-center justify-center gap-3 shadow-md transform transition active:scale-95",
        style: { border: "1px solid rgba(255,255,255,0.1)" }
      },
      /* @__PURE__ */ xn.createElement(AppleIcon, { color: "#FFFFFF" }),
      /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-semibold text-[1rem] text-white tracking-[0.25px]" }, "Continue with Apple")
    )), /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-h-[1.25rem]" }), /* @__PURE__ */ xn.createElement(
      "p",
      {
        className: "text-center text-[0.6875rem] leading-relaxed",
        style: { color: theme.textMuted }
      },
      "By continuing, you agree to our Terms of Service and Privacy Policy"
    )));
  };
  var SignInScreen_default = SignInScreen;

  // node_modules/@tanstack/virtual-core/dist/esm/utils.js
  function memo(getDeps, fn2, opts) {
    let deps = opts.initialDeps ?? [];
    let result2;
    let isInitial = true;
    function memoizedFunction() {
      var _a2, _b, _c;
      let depTime;
      if (opts.key && ((_a2 = opts.debug) == null ? void 0 : _a2.call(opts))) depTime = Date.now();
      const newDeps = getDeps();
      const depsChanged = newDeps.length !== deps.length || newDeps.some((dep, index2) => deps[index2] !== dep);
      if (!depsChanged) {
        return result2;
      }
      deps = newDeps;
      let resultTime;
      if (opts.key && ((_b = opts.debug) == null ? void 0 : _b.call(opts))) resultTime = Date.now();
      result2 = fn2(...newDeps);
      if (opts.key && ((_c = opts.debug) == null ? void 0 : _c.call(opts))) {
        const depEndTime = Math.round((Date.now() - depTime) * 100) / 100;
        const resultEndTime = Math.round((Date.now() - resultTime) * 100) / 100;
        const resultFpsPercentage = resultEndTime / 16;
        const pad = (str, num) => {
          str = String(str);
          while (str.length < num) {
            str = " " + str;
          }
          return str;
        };
        console.info(
          `%c\u23F1 ${pad(resultEndTime, 5)} /${pad(depEndTime, 5)} ms`,
          `
            font-size: .6rem;
            font-weight: bold;
            color: hsl(${Math.max(
            0,
            Math.min(120 - 120 * resultFpsPercentage, 120)
          )}deg 100% 31%);`,
          opts == null ? void 0 : opts.key
        );
      }
      if ((opts == null ? void 0 : opts.onChange) && !(isInitial && opts.skipInitialOnChange)) {
        opts.onChange(result2);
      }
      isInitial = false;
      return result2;
    }
    memoizedFunction.updateDeps = (newDeps) => {
      deps = newDeps;
    };
    return memoizedFunction;
  }
  function notUndefined(value, msg) {
    if (value === void 0) {
      throw new Error(`Unexpected undefined${msg ? `: ${msg}` : ""}`);
    } else {
      return value;
    }
  }
  var approxEqual = (a3, b2) => Math.abs(a3 - b2) < 1.01;
  var debounce = (targetWindow, fn2, ms) => {
    let timeoutId;
    return function(...args) {
      targetWindow.clearTimeout(timeoutId);
      timeoutId = targetWindow.setTimeout(() => fn2.apply(this, args), ms);
    };
  };

  // node_modules/@tanstack/virtual-core/dist/esm/index.js
  var getRect = (element) => {
    const { offsetWidth, offsetHeight } = element;
    return { width: offsetWidth, height: offsetHeight };
  };
  var defaultKeyExtractor = (index2) => index2;
  var defaultRangeExtractor = (range) => {
    const start = Math.max(range.startIndex - range.overscan, 0);
    const end = Math.min(range.endIndex + range.overscan, range.count - 1);
    const arr = [];
    for (let i3 = start; i3 <= end; i3++) {
      arr.push(i3);
    }
    return arr;
  };
  var observeElementRect = (instance, cb) => {
    const element = instance.scrollElement;
    if (!element) {
      return;
    }
    const targetWindow = instance.targetWindow;
    if (!targetWindow) {
      return;
    }
    const handler = (rect) => {
      const { width, height } = rect;
      cb({ width: Math.round(width), height: Math.round(height) });
    };
    handler(getRect(element));
    if (!targetWindow.ResizeObserver) {
      return () => {
      };
    }
    const observer = new targetWindow.ResizeObserver((entries) => {
      const run = () => {
        const entry = entries[0];
        if (entry == null ? void 0 : entry.borderBoxSize) {
          const box = entry.borderBoxSize[0];
          if (box) {
            handler({ width: box.inlineSize, height: box.blockSize });
            return;
          }
        }
        handler(getRect(element));
      };
      instance.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
    });
    observer.observe(element, { box: "border-box" });
    return () => {
      observer.unobserve(element);
    };
  };
  var addEventListenerOptions = {
    passive: true
  };
  var supportsScrollend = typeof window == "undefined" ? true : "onscrollend" in window;
  var observeElementOffset = (instance, cb) => {
    const element = instance.scrollElement;
    if (!element) {
      return;
    }
    const targetWindow = instance.targetWindow;
    if (!targetWindow) {
      return;
    }
    let offset = 0;
    const fallback = instance.options.useScrollendEvent && supportsScrollend ? () => void 0 : debounce(
      targetWindow,
      () => {
        cb(offset, false);
      },
      instance.options.isScrollingResetDelay
    );
    const createHandler = (isScrolling) => () => {
      const { horizontal, isRtl } = instance.options;
      offset = horizontal ? element["scrollLeft"] * (isRtl && -1 || 1) : element["scrollTop"];
      fallback();
      cb(offset, isScrolling);
    };
    const handler = createHandler(true);
    const endHandler = createHandler(false);
    element.addEventListener("scroll", handler, addEventListenerOptions);
    const registerScrollendEvent = instance.options.useScrollendEvent && supportsScrollend;
    if (registerScrollendEvent) {
      element.addEventListener("scrollend", endHandler, addEventListenerOptions);
    }
    return () => {
      element.removeEventListener("scroll", handler);
      if (registerScrollendEvent) {
        element.removeEventListener("scrollend", endHandler);
      }
    };
  };
  var measureElement = (element, entry, instance) => {
    if (entry == null ? void 0 : entry.borderBoxSize) {
      const box = entry.borderBoxSize[0];
      if (box) {
        const size = Math.round(
          box[instance.options.horizontal ? "inlineSize" : "blockSize"]
        );
        return size;
      }
    }
    return element[instance.options.horizontal ? "offsetWidth" : "offsetHeight"];
  };
  var elementScroll = (offset, {
    adjustments = 0,
    behavior
  }, instance) => {
    var _a2, _b;
    const toOffset = offset + adjustments;
    (_b = (_a2 = instance.scrollElement) == null ? void 0 : _a2.scrollTo) == null ? void 0 : _b.call(_a2, {
      [instance.options.horizontal ? "left" : "top"]: toOffset,
      behavior
    });
  };
  var Virtualizer = class {
    constructor(opts) {
      this.unsubs = [];
      this.scrollElement = null;
      this.targetWindow = null;
      this.isScrolling = false;
      this.currentScrollToIndex = null;
      this.measurementsCache = [];
      this.itemSizeCache = /* @__PURE__ */ new Map();
      this.laneAssignments = /* @__PURE__ */ new Map();
      this.pendingMeasuredCacheIndexes = [];
      this.prevLanes = void 0;
      this.lanesChangedFlag = false;
      this.lanesSettling = false;
      this.scrollRect = null;
      this.scrollOffset = null;
      this.scrollDirection = null;
      this.scrollAdjustments = 0;
      this.elementsCache = /* @__PURE__ */ new Map();
      this.observer = /* @__PURE__ */ (() => {
        let _ro = null;
        const get = () => {
          if (_ro) {
            return _ro;
          }
          if (!this.targetWindow || !this.targetWindow.ResizeObserver) {
            return null;
          }
          return _ro = new this.targetWindow.ResizeObserver((entries) => {
            entries.forEach((entry) => {
              const run = () => {
                this._measureElement(entry.target, entry);
              };
              this.options.useAnimationFrameWithResizeObserver ? requestAnimationFrame(run) : run();
            });
          });
        };
        return {
          disconnect: () => {
            var _a2;
            (_a2 = get()) == null ? void 0 : _a2.disconnect();
            _ro = null;
          },
          observe: (target) => {
            var _a2;
            return (_a2 = get()) == null ? void 0 : _a2.observe(target, { box: "border-box" });
          },
          unobserve: (target) => {
            var _a2;
            return (_a2 = get()) == null ? void 0 : _a2.unobserve(target);
          }
        };
      })();
      this.range = null;
      this.setOptions = (opts2) => {
        Object.entries(opts2).forEach(([key, value]) => {
          if (typeof value === "undefined") delete opts2[key];
        });
        this.options = {
          debug: false,
          initialOffset: 0,
          overscan: 1,
          paddingStart: 0,
          paddingEnd: 0,
          scrollPaddingStart: 0,
          scrollPaddingEnd: 0,
          horizontal: false,
          getItemKey: defaultKeyExtractor,
          rangeExtractor: defaultRangeExtractor,
          onChange: () => {
          },
          measureElement,
          initialRect: { width: 0, height: 0 },
          scrollMargin: 0,
          gap: 0,
          indexAttribute: "data-index",
          initialMeasurementsCache: [],
          lanes: 1,
          isScrollingResetDelay: 150,
          enabled: true,
          isRtl: false,
          useScrollendEvent: false,
          useAnimationFrameWithResizeObserver: false,
          ...opts2
        };
      };
      this.notify = (sync) => {
        var _a2, _b;
        (_b = (_a2 = this.options).onChange) == null ? void 0 : _b.call(_a2, this, sync);
      };
      this.maybeNotify = memo(
        () => {
          this.calculateRange();
          return [
            this.isScrolling,
            this.range ? this.range.startIndex : null,
            this.range ? this.range.endIndex : null
          ];
        },
        (isScrolling) => {
          this.notify(isScrolling);
        },
        {
          key: "maybeNotify",
          debug: () => this.options.debug,
          initialDeps: [
            this.isScrolling,
            this.range ? this.range.startIndex : null,
            this.range ? this.range.endIndex : null
          ]
        }
      );
      this.cleanup = () => {
        this.unsubs.filter(Boolean).forEach((d3) => d3());
        this.unsubs = [];
        this.observer.disconnect();
        this.scrollElement = null;
        this.targetWindow = null;
      };
      this._didMount = () => {
        return () => {
          this.cleanup();
        };
      };
      this._willUpdate = () => {
        var _a2;
        const scrollElement = this.options.enabled ? this.options.getScrollElement() : null;
        if (this.scrollElement !== scrollElement) {
          this.cleanup();
          if (!scrollElement) {
            this.maybeNotify();
            return;
          }
          this.scrollElement = scrollElement;
          if (this.scrollElement && "ownerDocument" in this.scrollElement) {
            this.targetWindow = this.scrollElement.ownerDocument.defaultView;
          } else {
            this.targetWindow = ((_a2 = this.scrollElement) == null ? void 0 : _a2.window) ?? null;
          }
          this.elementsCache.forEach((cached) => {
            this.observer.observe(cached);
          });
          this.unsubs.push(
            this.options.observeElementRect(this, (rect) => {
              this.scrollRect = rect;
              this.maybeNotify();
            })
          );
          this.unsubs.push(
            this.options.observeElementOffset(this, (offset, isScrolling) => {
              this.scrollAdjustments = 0;
              this.scrollDirection = isScrolling ? this.getScrollOffset() < offset ? "forward" : "backward" : null;
              this.scrollOffset = offset;
              this.isScrolling = isScrolling;
              this.maybeNotify();
            })
          );
          this._scrollToOffset(this.getScrollOffset(), {
            adjustments: void 0,
            behavior: void 0
          });
        }
      };
      this.getSize = () => {
        if (!this.options.enabled) {
          this.scrollRect = null;
          return 0;
        }
        this.scrollRect = this.scrollRect ?? this.options.initialRect;
        return this.scrollRect[this.options.horizontal ? "width" : "height"];
      };
      this.getScrollOffset = () => {
        if (!this.options.enabled) {
          this.scrollOffset = null;
          return 0;
        }
        this.scrollOffset = this.scrollOffset ?? (typeof this.options.initialOffset === "function" ? this.options.initialOffset() : this.options.initialOffset);
        return this.scrollOffset;
      };
      this.getFurthestMeasurement = (measurements, index2) => {
        const furthestMeasurementsFound = /* @__PURE__ */ new Map();
        const furthestMeasurements = /* @__PURE__ */ new Map();
        for (let m3 = index2 - 1; m3 >= 0; m3--) {
          const measurement = measurements[m3];
          if (furthestMeasurementsFound.has(measurement.lane)) {
            continue;
          }
          const previousFurthestMeasurement = furthestMeasurements.get(
            measurement.lane
          );
          if (previousFurthestMeasurement == null || measurement.end > previousFurthestMeasurement.end) {
            furthestMeasurements.set(measurement.lane, measurement);
          } else if (measurement.end < previousFurthestMeasurement.end) {
            furthestMeasurementsFound.set(measurement.lane, true);
          }
          if (furthestMeasurementsFound.size === this.options.lanes) {
            break;
          }
        }
        return furthestMeasurements.size === this.options.lanes ? Array.from(furthestMeasurements.values()).sort((a3, b2) => {
          if (a3.end === b2.end) {
            return a3.index - b2.index;
          }
          return a3.end - b2.end;
        })[0] : void 0;
      };
      this.getMeasurementOptions = memo(
        () => [
          this.options.count,
          this.options.paddingStart,
          this.options.scrollMargin,
          this.options.getItemKey,
          this.options.enabled,
          this.options.lanes
        ],
        (count, paddingStart, scrollMargin, getItemKey, enabled, lanes) => {
          const lanesChanged = this.prevLanes !== void 0 && this.prevLanes !== lanes;
          if (lanesChanged) {
            this.lanesChangedFlag = true;
          }
          this.prevLanes = lanes;
          this.pendingMeasuredCacheIndexes = [];
          return {
            count,
            paddingStart,
            scrollMargin,
            getItemKey,
            enabled,
            lanes
          };
        },
        {
          key: false
        }
      );
      this.getMeasurements = memo(
        () => [this.getMeasurementOptions(), this.itemSizeCache],
        ({ count, paddingStart, scrollMargin, getItemKey, enabled, lanes }, itemSizeCache) => {
          if (!enabled) {
            this.measurementsCache = [];
            this.itemSizeCache.clear();
            this.laneAssignments.clear();
            return [];
          }
          if (this.laneAssignments.size > count) {
            for (const index2 of this.laneAssignments.keys()) {
              if (index2 >= count) {
                this.laneAssignments.delete(index2);
              }
            }
          }
          if (this.lanesChangedFlag) {
            this.lanesChangedFlag = false;
            this.lanesSettling = true;
            this.measurementsCache = [];
            this.itemSizeCache.clear();
            this.laneAssignments.clear();
            this.pendingMeasuredCacheIndexes = [];
          }
          if (this.measurementsCache.length === 0 && !this.lanesSettling) {
            this.measurementsCache = this.options.initialMeasurementsCache;
            this.measurementsCache.forEach((item) => {
              this.itemSizeCache.set(item.key, item.size);
            });
          }
          const min = this.lanesSettling ? 0 : this.pendingMeasuredCacheIndexes.length > 0 ? Math.min(...this.pendingMeasuredCacheIndexes) : 0;
          this.pendingMeasuredCacheIndexes = [];
          if (this.lanesSettling && this.measurementsCache.length === count) {
            this.lanesSettling = false;
          }
          const measurements = this.measurementsCache.slice(0, min);
          const laneLastIndex = new Array(lanes).fill(
            void 0
          );
          for (let m3 = 0; m3 < min; m3++) {
            const item = measurements[m3];
            if (item) {
              laneLastIndex[item.lane] = m3;
            }
          }
          for (let i3 = min; i3 < count; i3++) {
            const key = getItemKey(i3);
            const cachedLane = this.laneAssignments.get(i3);
            let lane;
            let start;
            if (cachedLane !== void 0 && this.options.lanes > 1) {
              lane = cachedLane;
              const prevIndex = laneLastIndex[lane];
              const prevInLane = prevIndex !== void 0 ? measurements[prevIndex] : void 0;
              start = prevInLane ? prevInLane.end + this.options.gap : paddingStart + scrollMargin;
            } else {
              const furthestMeasurement = this.options.lanes === 1 ? measurements[i3 - 1] : this.getFurthestMeasurement(measurements, i3);
              start = furthestMeasurement ? furthestMeasurement.end + this.options.gap : paddingStart + scrollMargin;
              lane = furthestMeasurement ? furthestMeasurement.lane : i3 % this.options.lanes;
              if (this.options.lanes > 1) {
                this.laneAssignments.set(i3, lane);
              }
            }
            const measuredSize = itemSizeCache.get(key);
            const size = typeof measuredSize === "number" ? measuredSize : this.options.estimateSize(i3);
            const end = start + size;
            measurements[i3] = {
              index: i3,
              start,
              size,
              end,
              key,
              lane
            };
            laneLastIndex[lane] = i3;
          }
          this.measurementsCache = measurements;
          return measurements;
        },
        {
          key: "getMeasurements",
          debug: () => this.options.debug
        }
      );
      this.calculateRange = memo(
        () => [
          this.getMeasurements(),
          this.getSize(),
          this.getScrollOffset(),
          this.options.lanes
        ],
        (measurements, outerSize, scrollOffset, lanes) => {
          return this.range = measurements.length > 0 && outerSize > 0 ? calculateRange({
            measurements,
            outerSize,
            scrollOffset,
            lanes
          }) : null;
        },
        {
          key: "calculateRange",
          debug: () => this.options.debug
        }
      );
      this.getVirtualIndexes = memo(
        () => {
          let startIndex = null;
          let endIndex = null;
          const range = this.calculateRange();
          if (range) {
            startIndex = range.startIndex;
            endIndex = range.endIndex;
          }
          this.maybeNotify.updateDeps([this.isScrolling, startIndex, endIndex]);
          return [
            this.options.rangeExtractor,
            this.options.overscan,
            this.options.count,
            startIndex,
            endIndex
          ];
        },
        (rangeExtractor, overscan, count, startIndex, endIndex) => {
          return startIndex === null || endIndex === null ? [] : rangeExtractor({
            startIndex,
            endIndex,
            overscan,
            count
          });
        },
        {
          key: "getVirtualIndexes",
          debug: () => this.options.debug
        }
      );
      this.indexFromElement = (node2) => {
        const attributeName = this.options.indexAttribute;
        const indexStr = node2.getAttribute(attributeName);
        if (!indexStr) {
          console.warn(
            `Missing attribute name '${attributeName}={index}' on measured element.`
          );
          return -1;
        }
        return parseInt(indexStr, 10);
      };
      this._measureElement = (node2, entry) => {
        const index2 = this.indexFromElement(node2);
        const item = this.measurementsCache[index2];
        if (!item) {
          return;
        }
        const key = item.key;
        const prevNode = this.elementsCache.get(key);
        if (prevNode !== node2) {
          if (prevNode) {
            this.observer.unobserve(prevNode);
          }
          this.observer.observe(node2);
          this.elementsCache.set(key, node2);
        }
        if (node2.isConnected) {
          this.resizeItem(index2, this.options.measureElement(node2, entry, this));
        }
      };
      this.resizeItem = (index2, size) => {
        const item = this.measurementsCache[index2];
        if (!item) {
          return;
        }
        const itemSize = this.itemSizeCache.get(item.key) ?? item.size;
        const delta = size - itemSize;
        if (delta !== 0) {
          if (this.shouldAdjustScrollPositionOnItemSizeChange !== void 0 ? this.shouldAdjustScrollPositionOnItemSizeChange(item, delta, this) : item.start < this.getScrollOffset() + this.scrollAdjustments) {
            if (this.options.debug) {
              console.info("correction", delta);
            }
            this._scrollToOffset(this.getScrollOffset(), {
              adjustments: this.scrollAdjustments += delta,
              behavior: void 0
            });
          }
          this.pendingMeasuredCacheIndexes.push(item.index);
          this.itemSizeCache = new Map(this.itemSizeCache.set(item.key, size));
          this.notify(false);
        }
      };
      this.measureElement = (node2) => {
        if (!node2) {
          this.elementsCache.forEach((cached, key) => {
            if (!cached.isConnected) {
              this.observer.unobserve(cached);
              this.elementsCache.delete(key);
            }
          });
          return;
        }
        this._measureElement(node2, void 0);
      };
      this.getVirtualItems = memo(
        () => [this.getVirtualIndexes(), this.getMeasurements()],
        (indexes, measurements) => {
          const virtualItems = [];
          for (let k4 = 0, len = indexes.length; k4 < len; k4++) {
            const i3 = indexes[k4];
            const measurement = measurements[i3];
            virtualItems.push(measurement);
          }
          return virtualItems;
        },
        {
          key: "getVirtualItems",
          debug: () => this.options.debug
        }
      );
      this.getVirtualItemForOffset = (offset) => {
        const measurements = this.getMeasurements();
        if (measurements.length === 0) {
          return void 0;
        }
        return notUndefined(
          measurements[findNearestBinarySearch(
            0,
            measurements.length - 1,
            (index2) => notUndefined(measurements[index2]).start,
            offset
          )]
        );
      };
      this.getMaxScrollOffset = () => {
        if (!this.scrollElement) return 0;
        if ("scrollHeight" in this.scrollElement) {
          return this.options.horizontal ? this.scrollElement.scrollWidth - this.scrollElement.clientWidth : this.scrollElement.scrollHeight - this.scrollElement.clientHeight;
        } else {
          const doc = this.scrollElement.document.documentElement;
          return this.options.horizontal ? doc.scrollWidth - this.scrollElement.innerWidth : doc.scrollHeight - this.scrollElement.innerHeight;
        }
      };
      this.getOffsetForAlignment = (toOffset, align, itemSize = 0) => {
        if (!this.scrollElement) return 0;
        const size = this.getSize();
        const scrollOffset = this.getScrollOffset();
        if (align === "auto") {
          align = toOffset >= scrollOffset + size ? "end" : "start";
        }
        if (align === "center") {
          toOffset += (itemSize - size) / 2;
        } else if (align === "end") {
          toOffset -= size;
        }
        const maxOffset = this.getMaxScrollOffset();
        return Math.max(Math.min(maxOffset, toOffset), 0);
      };
      this.getOffsetForIndex = (index2, align = "auto") => {
        index2 = Math.max(0, Math.min(index2, this.options.count - 1));
        const item = this.measurementsCache[index2];
        if (!item) {
          return void 0;
        }
        const size = this.getSize();
        const scrollOffset = this.getScrollOffset();
        if (align === "auto") {
          if (item.end >= scrollOffset + size - this.options.scrollPaddingEnd) {
            align = "end";
          } else if (item.start <= scrollOffset + this.options.scrollPaddingStart) {
            align = "start";
          } else {
            return [scrollOffset, align];
          }
        }
        if (align === "end" && index2 === this.options.count - 1) {
          return [this.getMaxScrollOffset(), align];
        }
        const toOffset = align === "end" ? item.end + this.options.scrollPaddingEnd : item.start - this.options.scrollPaddingStart;
        return [
          this.getOffsetForAlignment(toOffset, align, item.size),
          align
        ];
      };
      this.isDynamicMode = () => this.elementsCache.size > 0;
      this.scrollToOffset = (toOffset, { align = "start", behavior } = {}) => {
        if (behavior === "smooth" && this.isDynamicMode()) {
          console.warn(
            "The `smooth` scroll behavior is not fully supported with dynamic size."
          );
        }
        this._scrollToOffset(this.getOffsetForAlignment(toOffset, align), {
          adjustments: void 0,
          behavior
        });
      };
      this.scrollToIndex = (index2, { align: initialAlign = "auto", behavior } = {}) => {
        if (behavior === "smooth" && this.isDynamicMode()) {
          console.warn(
            "The `smooth` scroll behavior is not fully supported with dynamic size."
          );
        }
        index2 = Math.max(0, Math.min(index2, this.options.count - 1));
        this.currentScrollToIndex = index2;
        let attempts = 0;
        const maxAttempts = 10;
        const tryScroll = (currentAlign) => {
          if (!this.targetWindow) return;
          const offsetInfo = this.getOffsetForIndex(index2, currentAlign);
          if (!offsetInfo) {
            console.warn("Failed to get offset for index:", index2);
            return;
          }
          const [offset, align] = offsetInfo;
          this._scrollToOffset(offset, { adjustments: void 0, behavior });
          this.targetWindow.requestAnimationFrame(() => {
            const verify = () => {
              if (this.currentScrollToIndex !== index2) return;
              const currentOffset = this.getScrollOffset();
              const afterInfo = this.getOffsetForIndex(index2, align);
              if (!afterInfo) {
                console.warn("Failed to get offset for index:", index2);
                return;
              }
              if (!approxEqual(afterInfo[0], currentOffset)) {
                scheduleRetry(align);
              }
            };
            if (this.isDynamicMode()) {
              this.targetWindow.requestAnimationFrame(verify);
            } else {
              verify();
            }
          });
        };
        const scheduleRetry = (align) => {
          if (!this.targetWindow) return;
          if (this.currentScrollToIndex !== index2) return;
          attempts++;
          if (attempts < maxAttempts) {
            if (this.options.debug) {
              console.info("Schedule retry", attempts, maxAttempts);
            }
            this.targetWindow.requestAnimationFrame(() => tryScroll(align));
          } else {
            console.warn(
              `Failed to scroll to index ${index2} after ${maxAttempts} attempts.`
            );
          }
        };
        tryScroll(initialAlign);
      };
      this.scrollBy = (delta, { behavior } = {}) => {
        if (behavior === "smooth" && this.isDynamicMode()) {
          console.warn(
            "The `smooth` scroll behavior is not fully supported with dynamic size."
          );
        }
        this._scrollToOffset(this.getScrollOffset() + delta, {
          adjustments: void 0,
          behavior
        });
      };
      this.getTotalSize = () => {
        var _a2;
        const measurements = this.getMeasurements();
        let end;
        if (measurements.length === 0) {
          end = this.options.paddingStart;
        } else if (this.options.lanes === 1) {
          end = ((_a2 = measurements[measurements.length - 1]) == null ? void 0 : _a2.end) ?? 0;
        } else {
          const endByLane = Array(this.options.lanes).fill(null);
          let endIndex = measurements.length - 1;
          while (endIndex >= 0 && endByLane.some((val) => val === null)) {
            const item = measurements[endIndex];
            if (endByLane[item.lane] === null) {
              endByLane[item.lane] = item.end;
            }
            endIndex--;
          }
          end = Math.max(...endByLane.filter((val) => val !== null));
        }
        return Math.max(
          end - this.options.scrollMargin + this.options.paddingEnd,
          0
        );
      };
      this._scrollToOffset = (offset, {
        adjustments,
        behavior
      }) => {
        this.options.scrollToFn(offset, { behavior, adjustments }, this);
      };
      this.measure = () => {
        this.itemSizeCache = /* @__PURE__ */ new Map();
        this.laneAssignments = /* @__PURE__ */ new Map();
        this.notify(false);
      };
      this.setOptions(opts);
    }
  };
  var findNearestBinarySearch = (low, high, getCurrentValue, value) => {
    while (low <= high) {
      const middle = (low + high) / 2 | 0;
      const currentValue = getCurrentValue(middle);
      if (currentValue < value) {
        low = middle + 1;
      } else if (currentValue > value) {
        high = middle - 1;
      } else {
        return middle;
      }
    }
    if (low > 0) {
      return low - 1;
    } else {
      return 0;
    }
  };
  function calculateRange({
    measurements,
    outerSize,
    scrollOffset,
    lanes
  }) {
    const lastIndex = measurements.length - 1;
    const getOffset = (index2) => measurements[index2].start;
    if (measurements.length <= lanes) {
      return {
        startIndex: 0,
        endIndex: lastIndex
      };
    }
    let startIndex = findNearestBinarySearch(
      0,
      lastIndex,
      getOffset,
      scrollOffset
    );
    let endIndex = startIndex;
    if (lanes === 1) {
      while (endIndex < lastIndex && measurements[endIndex].end < scrollOffset + outerSize) {
        endIndex++;
      }
    } else if (lanes > 1) {
      const endPerLane = Array(lanes).fill(0);
      while (endIndex < lastIndex && endPerLane.some((pos) => pos < scrollOffset + outerSize)) {
        const item = measurements[endIndex];
        endPerLane[item.lane] = item.end;
        endIndex++;
      }
      const startPerLane = Array(lanes).fill(scrollOffset + outerSize);
      while (startIndex >= 0 && startPerLane.some((pos) => pos >= scrollOffset)) {
        const item = measurements[startIndex];
        startPerLane[item.lane] = item.start;
        startIndex--;
      }
      startIndex = Math.max(0, startIndex - startIndex % lanes);
      endIndex = Math.min(lastIndex, endIndex + (lanes - 1 - endIndex % lanes));
    }
    return { startIndex, endIndex };
  }

  // node_modules/@tanstack/react-virtual/dist/esm/index.js
  var useIsomorphicLayoutEffect = typeof document !== "undefined" ? _2 : y2;
  function useVirtualizerBase({
    useFlushSync = true,
    ...options
  }) {
    const rerender = h2(() => ({}), {})[1];
    const resolvedOptions = {
      ...options,
      onChange: (instance2, sync) => {
        var _a2;
        if (useFlushSync && sync) {
          En(rerender);
        } else {
          rerender();
        }
        (_a2 = options.onChange) == null ? void 0 : _a2.call(options, instance2, sync);
      }
    };
    const [instance] = d2(
      () => new Virtualizer(resolvedOptions)
    );
    instance.setOptions(resolvedOptions);
    useIsomorphicLayoutEffect(() => {
      return instance._didMount();
    }, []);
    useIsomorphicLayoutEffect(() => {
      return instance._willUpdate();
    });
    return instance;
  }
  function useVirtualizer(options) {
    return useVirtualizerBase({
      observeElementRect,
      observeElementOffset,
      scrollToFn: elementScroll,
      ...options
    });
  }

  // src/hooks/useNativelyStorage.js
  var verified = /* @__PURE__ */ new Set();
  var useNativelyStorage = () => {
    const isNativeApp = typeof window.$agent !== "undefined";
    const storage = T2(() => {
      if (isNativeApp) {
        return new NativelyStorage();
      }
      return null;
    }, [isNativeApp]);
    const setItem = q2((key, value) => {
      localStorage.setItem(key, value);
      if (storage) {
        storage.setStorageValue(key, value);
      }
    }, [storage]);
    const getItem = q2((key) => {
      const localValue = localStorage.getItem(key);
      if (localValue != null) {
        if (storage && !verified.has(key)) {
          verified.add(key);
          storage.getStorageValue(key, (resp) => {
            const nativeValue = resp.value;
            if (nativeValue != null && nativeValue !== localValue) {
              console.log(`\u{1F504} [Storage] Background reconciled "${key}": native="${nativeValue}" wins over local="${localValue}".`);
              localStorage.setItem(key, nativeValue);
            }
          });
        }
        return Promise.resolve(localValue);
      }
      if (storage && !verified.has(key)) {
        verified.add(key);
        return new Promise((resolve2) => {
          const timeout = setTimeout(() => {
            console.warn(`\u23F1\uFE0F [Storage] Recovery timeout for "${key}" (2s). Returning null.`);
            resolve2(null);
          }, 2e3);
          storage.getStorageValue(key, (resp) => {
            clearTimeout(timeout);
            const nativeValue = resp.value;
            if (nativeValue != null) {
              console.log(`\u{1F504} [Storage] Recovered "${key}" from native="${nativeValue}".`);
              localStorage.setItem(key, nativeValue);
              resolve2(nativeValue);
            } else {
              resolve2(null);
            }
          });
        });
      }
      return Promise.resolve(null);
    }, [storage]);
    const removeItem = q2((key) => {
      localStorage.removeItem(key);
      if (storage) {
        storage.removeStorageValue(key);
      }
    }, [storage]);
    const clear = q2(() => {
      localStorage.clear();
      if (storage) {
        storage.resetStorage();
      }
    }, [storage]);
    return T2(() => ({
      setItem,
      getItem,
      removeItem,
      clear
    }), [setItem, getItem, removeItem, clear]);
  };

  // src/contexts/UserContext.jsx
  var STORAGE_KEY = "bonds_user_data";
  var DEFAULT_USER = {
    name: "",
    email: "",
    avatar: null,
    coins: 0,
    partner: null,
    pillars: []
  };
  var UserContext = Q(DEFAULT_USER);
  var useUser = () => x2(UserContext);
  var UserProvider = ({ children }) => {
    const [user, setUser] = d2(() => {
      try {
        const cached = localStorage.getItem(STORAGE_KEY);
        if (cached) {
          const parsed = { ...DEFAULT_USER, ...JSON.parse(cached) };
          if ("credits" in parsed) {
            parsed.coins = parsed.credits;
            delete parsed.credits;
          }
          return parsed;
        }
        return DEFAULT_USER;
      } catch {
        return DEFAULT_USER;
      }
    });
    const { getItem, setItem, removeItem } = useNativelyStorage();
    const userRef = A2(user);
    userRef.current = user;
    y2(() => {
      getItem(STORAGE_KEY).then((val) => {
        if (val) {
          try {
            const parsed = { ...DEFAULT_USER, ...JSON.parse(val) };
            if ("credits" in parsed) {
              parsed.coins = parsed.credits;
              delete parsed.credits;
            }
            setUser((prev) => JSON.stringify(prev) !== JSON.stringify(parsed) ? parsed : prev);
          } catch {
          }
        }
      });
    }, [getItem]);
    const persist = q2((data) => {
      const json = JSON.stringify(data);
      localStorage.setItem(STORAGE_KEY, json);
      setItem(STORAGE_KEY, json);
    }, [setItem]);
    const updateUser = q2((partial) => {
      setUser((prev) => {
        if ("credits" in partial) {
          partial = { ...partial, coins: partial.credits };
          delete partial.credits;
        }
        const next = { ...prev, ...partial };
        persist(next);
        return next;
      });
    }, [persist]);
    const clearUser = q2(() => {
      setUser(DEFAULT_USER);
      localStorage.removeItem(STORAGE_KEY);
      removeItem(STORAGE_KEY);
    }, [removeItem]);
    y2(() => {
      window.appUI = window.appUI || {};
      window.appUI.setUserData = updateUser;
      return () => {
        delete window.appUI.setUserData;
      };
    }, [updateUser]);
    y2(() => {
      const q4 = window.appUI._q;
      if (q4) {
        delete window.appUI._q;
        window.appUI.whenReady = function(fn2) {
          fn2();
        };
        q4.forEach((fn2) => fn2());
      }
    }, []);
    return /* @__PURE__ */ xn.createElement(UserContext.Provider, { value: { ...user, updateUser, clearUser } }, children);
  };

  // src/components/JourneyNode.jsx
  var STATIC_ICONS = {
    check: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("polyline", { points: "4 12 10 18 20 6", stroke: "white", strokeWidth: "3", strokeLinecap: "round", strokeLinejoin: "round" })),
    star: /* @__PURE__ */ xn.createElement("svg", { width: "32", height: "32", viewBox: "0 0 24 24", fill: "white" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 2l3.09 6.26L22 9.27l-5 4.87L18.18 22 12 18.27 5.82 22 7 14.14l-5-4.87 6.91-1.01L12 2z" })),
    trophy: /* @__PURE__ */ xn.createElement("svg", { width: "32", height: "32", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M6 2h12v6a6 6 0 0 1-12 0V2z", fill: "white" }), /* @__PURE__ */ xn.createElement("path", { d: "M6 4H3a1 1 0 0 0-1 1v1a4 4 0 0 0 4 4m12-6h3a1 1 0 0 1 1 1v1a4 4 0 0 1-4 4", stroke: "white", strokeWidth: "1.5" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 21h8m-4-7v7", stroke: "white", strokeWidth: "2", strokeLinecap: "round" })),
    clock: /* @__PURE__ */ xn.createElement("svg", { width: "34", height: "34", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10", stroke: "white", strokeWidth: "1.8" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 6v6l4 2", stroke: "white", strokeWidth: "2", strokeLinecap: "round" }), /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "1.2", fill: "white" }))
  };
  var STEP_TYPE_ICONS = {
    learn: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M4 19.5A2.5 2.5 0 0 1 6.5 17H20", stroke: "white", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }), /* @__PURE__ */ xn.createElement("path", { d: "M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z", stroke: "white", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 7h8M8 11h5", stroke: "white", strokeWidth: "2", strokeLinecap: "round" })),
    practice: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z", stroke: "white", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 10h8M8 14h4", stroke: "white", strokeWidth: "2", strokeLinecap: "round" })),
    insight: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M9 18h6M10 22h4", stroke: "white", strokeWidth: "2", strokeLinecap: "round" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 2a7 7 0 0 0-4 12.7V16h8v-1.3A7 7 0 0 0 12 2z", stroke: "white", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" })),
    act: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 22c5.5 0 10-4.5 10-10S17.5 2 12 2 2 6.5 2 12s4.5 10 10 10z", stroke: "white", strokeWidth: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 12l3 3 5-5", stroke: "white", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }))
  };
  var STEP_TYPE_SUBTITLES = {
    learn: "Watch & Learn",
    practice: "Practice the Skill",
    insight: "Dr Leo's Insight",
    act: "Take Action",
    milestone: "Chapter Milestone"
  };
  var themedIcons = (theme) => ({
    lock: /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("rect", { x: "5", y: "11", width: "14", height: "10", rx: "2", stroke: theme.lockedIcon, strokeWidth: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 11V7a4 4 0 1 1 8 0v4", stroke: theme.lockedIcon, strokeWidth: "2", strokeLinecap: "round" })),
    diamond: /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M6 3h12l4 7-10 12L2 10l4-7z", stroke: theme.lockedIcon, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }))
  });
  var SCREEN_WIDTH = 375;
  var POPOVER_WIDTH = 220;
  var POPOVER_PADDING = 12;
  var FloatingLabel = ({ text, theme }) => /* @__PURE__ */ xn.createElement("div", { className: "absolute -top-10 left-1/2 z-10 animate-float" }, /* @__PURE__ */ xn.createElement("div", { className: "relative border-2 border-solid rounded-xl px-4 py-1.5 whitespace-nowrap", style: { background: theme.floatBg, borderColor: theme.floatBorder } }, /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[13px] tracking-[1.5px] uppercase", style: { color: theme.textPrimary } }, text), /* @__PURE__ */ xn.createElement("div", { className: "absolute -bottom-[7px] left-1/2 -translate-x-1/2 w-0 h-0 border-solid border-l-[7px] border-l-transparent border-r-[7px] border-r-transparent border-t-[7px]", style: { borderTopColor: theme.floatBorder } }), /* @__PURE__ */ xn.createElement("div", { className: "absolute -bottom-[5px] left-1/2 -translate-x-1/2 w-0 h-0 border-solid border-l-[6px] border-l-transparent border-r-[6px] border-r-transparent border-t-[6px]", style: { borderTopColor: theme.floatBg } })));
  var NodePopover = ({ node: node2, color, onStart, nodeX }) => {
    const halfPopover = POPOVER_WIDTH / 2;
    let popoverLeft = nodeX - halfPopover;
    let popoverRight = nodeX + halfPopover;
    let offsetX = 0;
    if (popoverLeft < POPOVER_PADDING) {
      offsetX = POPOVER_PADDING - popoverLeft;
    } else if (popoverRight > SCREEN_WIDTH - POPOVER_PADDING) {
      offsetX = SCREEN_WIDTH - POPOVER_PADDING - popoverRight;
    }
    const triangleOffset = -offsetX;
    return /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "absolute z-20 animate-popover-in",
        style: {
          top: "100%",
          marginTop: 12,
          left: "50%",
          "--pop-offset": `${offsetX}px`,
          width: POPOVER_WIDTH
        }
      },
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "absolute -top-[7px] w-0 h-0 border-solid border-l-[8px] border-l-transparent border-r-[8px] border-r-transparent border-b-[8px]",
          style: { borderBottomColor: color, left: "50%", transform: `translateX(calc(-50% + ${triangleOffset}px))` }
        }
      ),
      /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl px-4 py-3", style: { backgroundColor: color } }, /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-extrabold text-[15px] text-white leading-tight" }, node2.title), /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-bold text-[11px] text-white/70 mt-0.5" }, node2.status === "paused" ? "Unlocks tomorrow" : node2.status === "locked" ? "Locked" : STEP_TYPE_SUBTITLES[node2.type] || (node2.status === "completed" ? "Completed" : "In progress")), node2.type !== "milestone" && node2.status !== "locked" && node2.status !== "paused" && /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: (e3) => {
            e3.stopPropagation();
            onStart && onStart(node2);
          },
          className: "mt-2.5 w-full rounded-xl py-2 font-jakarta font-extrabold text-[13px] uppercase tracking-wide",
          style: {
            backgroundColor: "white",
            color,
            boxShadow: "0 2px 0 0 rgba(0,0,0,0.12)"
          }
        },
        "START +10 XP"
      ))
    );
  };
  var JourneyNode = M2(({ node: node2, nodeX, style, isSelected, showFloatingLabel, onTap, onStart, accentColor, accentDark, theme }) => {
    const { title, status, type } = node2;
    const isMilestone = type === "milestone";
    const isCompleted = status === "completed";
    const isCurrent = status === "current";
    const isLocked = status === "locked";
    const isPaused = status === "paused";
    const size = isMilestone ? 82 : isCurrent ? 78 : 70;
    const depth = isMilestone ? 6 : 5;
    const chapterColor = accentColor || "#E44B8E";
    const chapterDark = accentDark || "#B83A72";
    const lockedColor = theme.lockedBg;
    const lockedDark = theme.lockedShadow;
    const goldColor = "#FFB800";
    const goldDark = "#CC9300";
    const themed = themedIcons(theme);
    let bgColor, shadowColor, icon;
    if (isMilestone) {
      if (isCompleted) {
        bgColor = goldColor;
        shadowColor = goldDark;
        icon = STATIC_ICONS.trophy;
      } else {
        bgColor = lockedColor;
        shadowColor = lockedDark;
        icon = themed.diamond;
      }
    } else if (isLocked) {
      bgColor = lockedColor;
      shadowColor = lockedDark;
      icon = themed.lock;
    } else if (isPaused) {
      bgColor = chapterColor + "80";
      shadowColor = chapterDark + "80";
      icon = STATIC_ICONS.clock;
    } else {
      bgColor = chapterColor;
      shadowColor = chapterDark;
      icon = STEP_TYPE_ICONS[type] || STATIC_ICONS.star;
    }
    const popoverColor = accentColor || bgColor;
    return /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "absolute flex flex-col items-center",
        style: { ...style, width: size + 40, marginLeft: -(size + 40) / 2 }
      },
      showFloatingLabel && !isSelected && /* @__PURE__ */ xn.createElement(FloatingLabel, { text: "START", theme }),
      /* @__PURE__ */ xn.createElement("div", { className: "relative" }, /* @__PURE__ */ xn.createElement(
        "button",
        {
          disabled: isLocked,
          onClick: !isLocked ? () => onTap && onTap(node2) : void 0,
          className: `relative rounded-full flex items-center justify-center overflow-hidden ${isLocked ? "cursor-default" : isPaused ? "cursor-pointer animate-paused-clock" : "cursor-pointer transition-transform duration-100 active:translate-y-[2px]"}`,
          style: {
            width: size,
            height: size,
            backgroundColor: bgColor,
            boxShadow: `0 ${depth}px 0 0 ${shadowColor}`
          }
        },
        isCompleted && /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 rounded-full pointer-events-none", style: {
          background: "radial-gradient(ellipse 70% 50% at 30% 15%, rgba(255,255,255,0.25), transparent 65%)"
        } }),
        icon
      ), isSelected && /* @__PURE__ */ xn.createElement(NodePopover, { node: node2, color: popoverColor, onStart, nodeX })),
      !isMilestone && /* @__PURE__ */ xn.createElement("span", { className: "mt-2 text-[10px] font-jakarta font-bold text-center leading-tight tracking-wide", style: {
        color: isCompleted ? theme.labelCompleted : isCurrent || isPaused ? theme.labelCurrent : theme.labelLocked
      } }, title)
    );
  });
  JourneyNode.displayName = "JourneyNode";
  var JourneyNode_default = JourneyNode;

  // src/data/mockJourneyData.js
  var mockJourneyData = {
    chapters: [
      // ── Chapter 1: Communication (completed) ────────────────────────
      {
        title: "Communication",
        index: 1,
        nodes: [
          { id: 1, type: "learn", title: "Learn", status: "completed" },
          { id: 2, type: "practice", title: "Practice", status: "completed" },
          { id: 3, type: "insight", title: "Insight", status: "completed" },
          { id: 4, type: "act", title: "Act", status: "completed" },
          { id: 5, type: "milestone", title: "Milestone", status: "completed" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/communication-lesson.mp4",
          lessonScript: "Communication is the foundation of every strong relationship. In this lesson, you'll learn the difference between hearing and truly listening \u2014 and how small shifts in how you speak and respond can transform your connection with your partner.",
          lessonQuestions: [
            {
              question: "What is active listening?",
              answers: [
                "Waiting for your turn to speak",
                "Fully focusing on and understanding the speaker",
                "Repeating everything back word for word"
              ],
              correctAnswer: 1
            },
            {
              question: "Which response shows empathetic communication?",
              answers: [
                '"I understand why that would be frustrating for you"',
                `"You shouldn't feel that way"`,
                '"Just calm down and think logically"'
              ],
              correctAnswer: 0
            },
            {
              question: 'What is a "you" statement vs an "I" statement?',
              answers: [
                '"You" statements express ownership, "I" statements assign blame',
                "They mean the same thing",
                '"You" statements assign blame, "I" statements express ownership'
              ],
              correctAnswer: 2
            }
          ],
          practiceSkills: [
            "Reflective listening during a disagreement",
            "Asking open-ended questions instead of yes/no",
            "Paraphrasing your partner's feelings before responding"
          ],
          insightQuestions: [
            "Do you feel truly heard in your relationship?",
            "Can you identify your communication triggers?",
            "Do you tend to shut down or escalate during conflict?"
          ],
          insightVideo: "https://example.com/videos/communication-insight.mp4",
          actDescription: "Have a 10-minute conversation with your partner where you only ask open-ended questions and reflect back what they say.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 2: Emotional Safety (completed) ─────────────────────
      {
        title: "Emotional Safety",
        index: 2,
        nodes: [
          { id: 6, type: "learn", title: "Learn", status: "completed" },
          { id: 7, type: "practice", title: "Practice", status: "completed" },
          { id: 8, type: "insight", title: "Insight", status: "completed" },
          { id: 9, type: "act", title: "Act", status: "completed" },
          { id: 10, type: "milestone", title: "Milestone", status: "completed" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/emotional-safety-lesson.mp4",
          lessonScript: "Emotional safety is the invisible foundation of a healthy relationship. When both partners feel safe to be vulnerable, honest, and imperfect, real intimacy can grow. This lesson explores what emotional safety looks like and how to nurture it daily.",
          lessonQuestions: [
            {
              question: "What is emotional safety in a relationship?",
              answers: [
                "Feeling secure enough to be vulnerable without fear of judgment",
                "Never disagreeing with your partner",
                "Avoiding difficult conversations"
              ],
              correctAnswer: 0
            },
            {
              question: "Which behavior undermines emotional safety?",
              answers: [
                "Validating your partner's feelings even when you disagree",
                "Asking clarifying questions during disagreements",
                "Dismissing your partner's concerns as overreacting"
              ],
              correctAnswer: 2
            },
            {
              question: "How can you rebuild emotional safety after a breach?",
              answers: [
                "Pretend nothing happened",
                "Acknowledge the hurt, take responsibility, and show changed behavior",
                "Give your partner space indefinitely"
              ],
              correctAnswer: 1
            }
          ],
          practiceSkills: [
            "Naming your emotions out loud to your partner",
            "Responding to vulnerability with curiosity instead of advice",
            "Creating a daily check-in ritual (2 minutes each)"
          ],
          insightQuestions: [
            "When was the last time you felt truly safe being vulnerable?",
            "What makes you pull away emotionally?",
            "Do you trust your partner to hold your feelings gently?"
          ],
          insightVideo: "https://example.com/videos/emotional-safety-insight.mp4",
          actDescription: `Share one thing you've been afraid to tell your partner. Start with "I feel..." and let them respond without interruption.`,
          actDaysToPerform: 1
        }
      },
      // ── Chapter 3: Intimacy (completed) ─────────────────────────────
      {
        title: "Intimacy",
        index: 3,
        nodes: [
          { id: 11, type: "learn", title: "Learn", status: "completed" },
          { id: 12, type: "practice", title: "Practice", status: "completed" },
          { id: 13, type: "insight", title: "Insight", status: "completed" },
          { id: 14, type: "act", title: "Act", status: "completed" },
          { id: 15, type: "milestone", title: "Milestone", status: "completed" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/intimacy-lesson.mp4",
          lessonScript: "Intimacy goes far beyond physical closeness. Emotional, intellectual, and experiential intimacy are the threads that weave two lives together. Learn the five types of intimacy and how to cultivate each one.",
          lessonQuestions: [
            {
              question: "Which is NOT one of the five types of intimacy?",
              answers: [
                "Emotional intimacy",
                "Intellectual intimacy",
                "Financial intimacy"
              ],
              correctAnswer: 2
            },
            {
              question: "What often causes intimacy to fade over time?",
              answers: [
                "Spending too much time together",
                "Taking each other for granted and stopping intentional connection",
                "Having different hobbies"
              ],
              correctAnswer: 1
            },
            {
              question: "How can you deepen emotional intimacy?",
              answers: [
                "Share your fears and dreams regularly",
                "Only talk about practical matters",
                "Wait for your partner to open up first"
              ],
              correctAnswer: 0
            }
          ],
          practiceSkills: [
            "Sharing a childhood memory you've never told your partner",
            "Planning a surprise experience based on your partner's love language",
            "Spending 15 minutes together with no screens, just talking"
          ],
          insightQuestions: [
            "Which type of intimacy feels strongest in your relationship?",
            "Which type of intimacy do you crave more of?",
            "When do you feel closest to your partner?"
          ],
          insightVideo: "https://example.com/videos/intimacy-insight.mp4",
          actDescription: 'Plan and execute a "connection date" \u2014 no phones, no distractions. Spend one hour doing something your partner loves, fully present.',
          actDaysToPerform: 3
        }
      },
      // ── Chapter 4: Conflict Resolution (paused — waiting for tomorrow) ──
      {
        title: "Conflict Resolution",
        index: 4,
        nodes: [
          { id: 16, type: "learn", title: "Learn", status: "paused" },
          { id: 17, type: "practice", title: "Practice", status: "locked" },
          { id: 18, type: "insight", title: "Insight", status: "locked" },
          { id: 19, type: "act", title: "Act", status: "locked" },
          { id: 20, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/conflict-resolution-lesson.mp4",
          lessonScript: "Every couple fights \u2014 but how you fight determines whether conflict strengthens or weakens your bond. This lesson covers the four horsemen of relationship conflict, de-escalation techniques, and how to repair after a rupture.",
          lessonQuestions: [
            {
              question: `Which is one of Gottman's "Four Horsemen" of conflict?`,
              answers: [
                "Active listening",
                "Contempt",
                "Compromise"
              ],
              correctAnswer: 1
            },
            {
              question: 'What is a "repair attempt" during conflict?',
              answers: [
                "Apologizing even when you're right",
                "Changing the subject to avoid the argument",
                "Any effort to de-escalate tension and reconnect"
              ],
              correctAnswer: 2
            },
            {
              question: "When is the best time to discuss a recurring conflict?",
              answers: [
                "When both partners are calm and have agreed to talk",
                "Never \u2014 just let it go",
                "In the heat of the moment"
              ],
              correctAnswer: 0
            }
          ],
          practiceSkills: [
            'Using a "soft start-up" instead of a harsh opener',
            "Taking a 20-minute break when flooded, then returning",
            'Expressing needs without blaming ("I need..." not "You always...")'
          ],
          insightQuestions: [
            "What is your default conflict style \u2014 fight, flight, or freeze?",
            "Can you think of a fight that actually brought you closer?",
            "What does your partner do that helps you calm down during conflict?"
          ],
          insightVideo: "https://example.com/videos/conflict-resolution-insight.mp4",
          actDescription: "Choose one recurring minor conflict. Sit down with your partner and use the soft start-up technique to discuss it. Focus on understanding, not winning.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 5: Attachment Styles (locked) ───────────────────────
      {
        title: "Attachment Styles",
        index: 5,
        nodes: [
          { id: 21, type: "learn", title: "Learn", status: "locked" },
          { id: 22, type: "practice", title: "Practice", status: "locked" },
          { id: 23, type: "insight", title: "Insight", status: "locked" },
          { id: 24, type: "act", title: "Act", status: "locked" },
          { id: 25, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/attachment-styles-lesson.mp4",
          lessonScript: "Your attachment style \u2014 formed in childhood \u2014 shapes how you love, fight, and connect as an adult. Understanding your own and your partner's attachment patterns is the key to breaking unhelpful cycles.",
          lessonQuestions: [
            {
              question: "How many primary attachment styles are there?",
              answers: [
                "Four",
                "Two",
                "Six"
              ],
              correctAnswer: 0
            },
            {
              question: "What characterizes an anxious attachment style?",
              answers: [
                "Preferring independence and avoiding vulnerability",
                "Craving closeness and fearing abandonment",
                "Feeling comfortable with both closeness and independence"
              ],
              correctAnswer: 1
            },
            {
              question: "Can attachment styles change over time?",
              answers: [
                "No, they are fixed for life",
                "Only with medication",
                "Yes, through awareness, therapy, and secure relationships"
              ],
              correctAnswer: 2
            }
          ],
          practiceSkills: [
            "Identifying your attachment triggers in real time",
            "Communicating your needs without testing your partner",
            "Self-soothing when your attachment alarm goes off"
          ],
          insightQuestions: [
            "Which attachment style do you most identify with?",
            "How does your attachment style show up when you feel threatened?",
            "What did love look like in your childhood home?"
          ],
          insightVideo: "https://example.com/videos/attachment-styles-insight.mp4",
          actDescription: "Take an attachment style quiz together with your partner. Discuss your results openly \u2014 what surprised you and what felt familiar.",
          actDaysToPerform: 1
        }
      },
      // ── Chapter 6: Shared Values (locked) ──────────────────────────
      {
        title: "Shared Values",
        index: 6,
        nodes: [
          { id: 26, type: "learn", title: "Learn", status: "locked" },
          { id: 27, type: "practice", title: "Practice", status: "locked" },
          { id: 28, type: "insight", title: "Insight", status: "locked" },
          { id: 29, type: "act", title: "Act", status: "locked" },
          { id: 30, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/shared-values-lesson.mp4",
          lessonScript: "Shared values are the compass of your relationship. They guide big decisions \u2014 where to live, how to raise kids, what to prioritize. This lesson helps you uncover your core values and find alignment with your partner.",
          lessonQuestions: [
            {
              question: "Why are shared values important in a relationship?",
              answers: [
                "Partners should agree on everything",
                "They eliminate all disagreements",
                "They provide a foundation for major life decisions together"
              ],
              correctAnswer: 2
            },
            {
              question: "What should you do when core values differ?",
              answers: [
                "Have honest conversations about non-negotiables and find compromises",
                "Ignore the differences and hope they resolve",
                "One partner should give in completely"
              ],
              correctAnswer: 0
            },
            {
              question: "Which is an example of a core value?",
              answers: [
                "Preferring Italian food over Mexican",
                "Commitment to family and community",
                "Liking the same TV shows"
              ],
              correctAnswer: 1
            }
          ],
          practiceSkills: [
            "Listing your top 5 non-negotiable values independently",
            "Comparing value lists with your partner without judgment",
            'Creating a shared "relationship mission statement"'
          ],
          insightQuestions: [
            "What values did you grow up with that you still hold?",
            "Are there values you and your partner disagree on?",
            "What does your ideal life together look like in 5 years?"
          ],
          insightVideo: "https://example.com/videos/shared-values-insight.mp4",
          actDescription: "Each write down your top 5 values independently, then compare. Discuss where you align and where you differ \u2014 without trying to change each other.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 7: Trust Building (locked) ─────────────────────────
      {
        title: "Trust Building",
        index: 7,
        nodes: [
          { id: 31, type: "learn", title: "Learn", status: "locked" },
          { id: 32, type: "practice", title: "Practice", status: "locked" },
          { id: 33, type: "insight", title: "Insight", status: "locked" },
          { id: 34, type: "act", title: "Act", status: "locked" },
          { id: 35, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/trust-building-lesson.mp4",
          lessonScript: "Trust isn't built in grand gestures \u2014 it's built in hundreds of small moments. Every time you show up, keep a promise, or choose honesty, you're depositing into your relationship's trust bank. Learn how trust works and how to rebuild it when broken.",
          lessonQuestions: [
            {
              question: "How is trust primarily built in a relationship?",
              answers: [
                "Through one big romantic gesture",
                "Through consistent small actions over time",
                "By never making mistakes"
              ],
              correctAnswer: 1
            },
            {
              question: "What is the first step in rebuilding broken trust?",
              answers: [
                "The person who broke trust taking full accountability",
                "Forgetting what happened",
                "Setting strict rules for the other person"
              ],
              correctAnswer: 0
            },
            {
              question: "What destroys trust the fastest?",
              answers: [
                "Honest disagreements",
                "Spending time apart",
                "Deception and hidden information"
              ],
              correctAnswer: 2
            }
          ],
          practiceSkills: [
            "Following through on one small promise today",
            "Being transparent about something you'd normally keep to yourself",
            "Acknowledging when your partner trusts you with something difficult"
          ],
          insightQuestions: [
            "On a scale of 1-10, how much do you trust your partner?",
            "Has trust ever been broken in your relationship?",
            "What does your partner do that makes you trust them more?"
          ],
          insightVideo: "https://example.com/videos/trust-building-insight.mp4",
          actDescription: "Make three small promises to your partner this week and keep every one. At the end of the week, talk about how it felt.",
          actDaysToPerform: 3
        }
      },
      // ── Chapter 8: Emotional Intelligence (locked) ──────────────────
      {
        title: "Emotional Intelligence",
        index: 8,
        nodes: [
          { id: 36, type: "learn", title: "Learn", status: "locked" },
          { id: 37, type: "practice", title: "Practice", status: "locked" },
          { id: 38, type: "insight", title: "Insight", status: "locked" },
          { id: 39, type: "act", title: "Act", status: "locked" },
          { id: 40, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/emotional-intelligence-lesson.mp4",
          lessonScript: "Emotional intelligence (EQ) is your ability to recognize, understand, and manage your own emotions \u2014 and to recognize and influence the emotions of others. In relationships, high EQ means fewer misunderstandings and deeper connection.",
          lessonQuestions: [
            {
              question: "What are the four pillars of emotional intelligence?",
              answers: [
                "Self-awareness, self-management, social awareness, relationship management",
                "Happiness, sadness, anger, fear",
                "Thinking, feeling, acting, reflecting"
              ],
              correctAnswer: 0
            },
            {
              question: "How does emotional intelligence help in relationships?",
              answers: [
                "It helps you win arguments",
                "It makes you feel less emotion overall",
                "It helps you understand and regulate both your own and your partner's emotions"
              ],
              correctAnswer: 2
            },
            {
              question: "What is emotional regulation?",
              answers: [
                "Suppressing your emotions",
                "The ability to manage your emotional responses appropriately",
                "Always staying calm no matter what"
              ],
              correctAnswer: 1
            }
          ],
          practiceSkills: [
            `Labeling your emotion before reacting ("I notice I'm feeling...")`,
            "Pausing for 5 seconds before responding when triggered",
            'Asking your partner "What are you feeling right now?" with genuine curiosity'
          ],
          insightQuestions: [
            "Which emotions are hardest for you to sit with?",
            "Do you tend to intellectualize feelings instead of feeling them?",
            "How did your family handle emotions when you were growing up?"
          ],
          insightVideo: "https://example.com/videos/emotional-intelligence-insight.mp4",
          actDescription: 'For two days, practice the "pause and label" technique: before reacting to any emotional trigger, pause, name the emotion out loud, then choose your response.',
          actDaysToPerform: 2
        }
      },
      // ── Chapter 9: Independence (locked) ───────────────────────────
      {
        title: "Independence",
        index: 9,
        nodes: [
          { id: 41, type: "learn", title: "Learn", status: "locked" },
          { id: 42, type: "practice", title: "Practice", status: "locked" },
          { id: 43, type: "insight", title: "Insight", status: "locked" },
          { id: 44, type: "act", title: "Act", status: "locked" },
          { id: 45, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/independence-lesson.mp4",
          lessonScript: "Healthy relationships need two whole individuals, not two halves making a whole. Maintaining your own identity, friendships, and passions doesn't weaken your bond \u2014 it strengthens it. Learn how to balance togetherness with individuality.",
          lessonQuestions: [
            {
              question: "Why is maintaining independence important in a relationship?",
              answers: [
                "To keep your partner guessing",
                "So you can leave at any time",
                "Because codependency leads to resentment and loss of self"
              ],
              correctAnswer: 2
            },
            {
              question: "What is a sign of unhealthy codependency?",
              answers: [
                "Enjoying alone time",
                "Feeling anxious when your partner has their own plans",
                "Having separate friend groups"
              ],
              correctAnswer: 1
            },
            {
              question: "How can you support your partner's independence?",
              answers: [
                "Encourage their hobbies and friendships outside the relationship",
                "Make sure they include you in everything",
                "Feel hurt when they want alone time"
              ],
              correctAnswer: 0
            }
          ],
          practiceSkills: [
            "Spending an evening doing your own thing without guilt",
            "Encouraging your partner to go out with friends",
            "Identifying one personal goal that's just for you"
          ],
          insightQuestions: [
            "Do you feel like you've lost parts of yourself in this relationship?",
            "What hobbies or interests have you given up since being together?",
            "Does your partner's independence feel threatening or reassuring?"
          ],
          insightVideo: "https://example.com/videos/independence-insight.mp4",
          actDescription: "Each partner spends a full evening pursuing a solo interest \u2014 a hobby, seeing friends, or a personal project. Share what you did afterward.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 10: Romance (locked) ────────────────────────────────
      {
        title: "Romance",
        index: 10,
        nodes: [
          { id: 46, type: "learn", title: "Learn", status: "locked" },
          { id: 47, type: "practice", title: "Practice", status: "locked" },
          { id: 48, type: "insight", title: "Insight", status: "locked" },
          { id: 49, type: "act", title: "Act", status: "locked" },
          { id: 50, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/romance-lesson.mp4",
          lessonScript: "Romance isn't just for new relationships \u2014 it's a practice that keeps love alive. The butterflies may fade, but intentional romance creates a different, deeper kind of magic. Learn how to keep the spark alive through everyday actions.",
          lessonQuestions: [
            {
              question: "What is the biggest myth about romance?",
              answers: [
                "Romance requires effort",
                "Romance should be effortless and spontaneous if you truly love someone",
                "Romance looks different for everyone"
              ],
              correctAnswer: 1
            },
            {
              question: 'What are "bids for connection" (Gottman)?',
              answers: [
                "Formal requests for dates",
                "Arguments that need resolution",
                "Small everyday moments where one partner reaches out for attention or affection"
              ],
              correctAnswer: 2
            },
            {
              question: "How do love languages relate to romance?",
              answers: [
                "Understanding your partner's love language helps you be romantic in ways they actually feel",
                "Everyone should express love the same way",
                "Love languages are not scientifically valid"
              ],
              correctAnswer: 0
            }
          ],
          practiceSkills: [
            "Leaving a handwritten note for your partner to find",
            'Responding to a "bid for connection" with full attention',
            "Planning a date night based on your partner's love language"
          ],
          insightQuestions: [
            "When did you last feel truly romanced by your partner?",
            "What small gestures mean the most to you?",
            "Do you feel like romance has faded, or is it just different?"
          ],
          insightVideo: "https://example.com/videos/romance-insight.mp4",
          actDescription: "Plan a surprise for your partner using their love language. It doesn't need to be expensive \u2014 just thoughtful and personal.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 11: Life Transitions (locked) ──────────────────────
      {
        title: "Life Transitions",
        index: 11,
        nodes: [
          { id: 51, type: "learn", title: "Learn", status: "locked" },
          { id: 52, type: "practice", title: "Practice", status: "locked" },
          { id: 53, type: "insight", title: "Insight", status: "locked" },
          { id: 54, type: "act", title: "Act", status: "locked" },
          { id: 55, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/life-transitions-lesson.mp4",
          lessonScript: "Moving, changing jobs, becoming parents, dealing with loss \u2014 life transitions test even the strongest relationships. This lesson teaches you how to navigate major changes as a team, not as two individuals struggling separately.",
          lessonQuestions: [
            {
              question: "Why are life transitions especially hard on relationships?",
              answers: [
                "They create stress that disrupts established routines and roles",
                "They're boring and mundane",
                "They don't affect relationships much"
              ],
              correctAnswer: 0
            },
            {
              question: "What is the most important thing during a transition?",
              answers: [
                "Making decisions quickly",
                "Maintaining open communication and checking in often",
                "Handling everything independently to reduce burden"
              ],
              correctAnswer: 1
            },
            {
              question: "How should couples approach major decisions during transitions?",
              answers: [
                "The more decisive partner should lead",
                "Avoid decisions until the transition is over",
                "Make decisions together with both perspectives valued"
              ],
              correctAnswer: 2
            }
          ],
          practiceSkills: [
            'Creating a "transition plan" together for an upcoming change',
            'Checking in daily during stressful periods with a simple "How are you really?"',
            "Dividing new responsibilities fairly and revisiting the split weekly"
          ],
          insightQuestions: [
            "What was the hardest transition you've faced as a couple?",
            "Did a life change bring you closer or push you apart?",
            "What upcoming transition are you most nervous about?"
          ],
          insightVideo: "https://example.com/videos/life-transitions-insight.mp4",
          actDescription: "Discuss an upcoming change or challenge together. Create a simple plan: who handles what, how you'll check in, and what support each person needs.",
          actDaysToPerform: 2
        }
      },
      // ── Chapter 12: Partnership (locked) ───────────────────────────
      {
        title: "Partnership",
        index: 12,
        nodes: [
          { id: 56, type: "learn", title: "Learn", status: "locked" },
          { id: 57, type: "practice", title: "Practice", status: "locked" },
          { id: 58, type: "insight", title: "Insight", status: "locked" },
          { id: 59, type: "act", title: "Act", status: "locked" },
          { id: 60, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/partnership-lesson.mp4",
          lessonScript: "Partnership is where love meets logistics. It's about building a life together \u2014 managing finances, dividing labor, making decisions, and supporting each other's growth. True partnership means both people feel like equals.",
          lessonQuestions: [
            {
              question: 'What does "equal partnership" mean?',
              answers: [
                "Splitting everything exactly 50/50",
                "One partner handling all responsibilities",
                "Both partners feeling valued and heard, contributing in ways that play to their strengths"
              ],
              correctAnswer: 2
            },
            {
              question: 'What is "mental load" in a relationship?',
              answers: [
                "The invisible work of planning, organizing, and remembering things for the household",
                "The stress of work deadlines",
                "Thinking too much about the relationship"
              ],
              correctAnswer: 0
            },
            {
              question: "How should financial decisions be handled?",
              answers: [
                "The higher earner decides",
                "Transparently, with shared goals and regular money conversations",
                "Separately \u2014 what's mine is mine"
              ],
              correctAnswer: 1
            }
          ],
          practiceSkills: [
            "Doing an audit of who handles which household tasks",
            "Having a money date \u2014 reviewing finances together without blame",
            "Creating a shared goals list for the next year"
          ],
          insightQuestions: [
            'Does one partner carry more of the "mental load"?',
            "Do you feel like equal partners or is one person leading?",
            "What would a more balanced partnership look like for you?"
          ],
          insightVideo: "https://example.com/videos/partnership-insight.mp4",
          actDescription: "Sit down and list every recurring household task. Discuss who currently owns each one and rebalance together. Check in after one week.",
          actDaysToPerform: 3
        }
      },
      // ── Chapter 13: Mastery (locked) ────────────────────────────────
      {
        title: "Mastery",
        index: 13,
        nodes: [
          { id: 61, type: "learn", title: "Learn", status: "locked" },
          { id: 62, type: "practice", title: "Practice", status: "locked" },
          { id: 63, type: "insight", title: "Insight", status: "locked" },
          { id: 64, type: "act", title: "Act", status: "locked" },
          { id: 65, type: "milestone", title: "Milestone", status: "locked" }
        ],
        content: {
          lessonVideo: "https://example.com/videos/mastery-lesson.mp4",
          lessonScript: "You've built the skills. Now it's time to bring them all together. Mastery isn't perfection \u2014 it's the ability to catch yourself, repair quickly, and keep growing together. This final lesson is about sustaining the relationship you've worked so hard to build.",
          lessonQuestions: [
            {
              question: 'What does "relationship mastery" mean?',
              answers: [
                "Never having problems",
                "Having the skills to navigate challenges and continuously grow together",
                "Being the perfect partner"
              ],
              correctAnswer: 1
            },
            {
              question: "What is the most important relationship skill from this journey?",
              answers: [
                "Consistent, intentional effort to understand and support each other",
                "Winning arguments",
                "Avoiding conflict at all costs"
              ],
              correctAnswer: 0
            },
            {
              question: "How do you maintain relationship growth long-term?",
              answers: [
                "Set it and forget it \u2014 you've done the work",
                "Only work on it when things go wrong",
                "Regular check-ins, continued learning, and never taking each other for granted"
              ],
              correctAnswer: 2
            }
          ],
          practiceSkills: [
            'Creating a monthly "state of the relationship" check-in ritual',
            "Writing a letter of appreciation to your partner",
            "Identifying one area to keep growing in together"
          ],
          insightQuestions: [
            "What has changed the most in your relationship during this journey?",
            "What are you most proud of as a couple?",
            "What is one thing you want to keep working on?"
          ],
          insightVideo: "https://example.com/videos/mastery-insight.mp4",
          actDescription: "Write each other a letter about what you've learned during this journey and what you appreciate most about your partner. Read them to each other.",
          actDaysToPerform: 1
        }
      }
    ]
  };
  var mockJourneyData_default = mockJourneyData;

  // src/components/JourneyPath.jsx
  var NODE_SPACING = 130;
  var CHAPTER_GAP = 80;
  var PADDING_TOP = 15;
  var PADDING_BOTTOM = 80;
  var PATH_CENTER = 187;
  var PATH_AMPLITUDE = 68;
  var WAVE_PERIOD = 10;
  var getNodeX = (globalIndex) => PATH_CENTER + Math.sin(globalIndex * 2 * Math.PI / WAVE_PERIOD) * PATH_AMPLITUDE;
  var SECTION_COLORS = [
    { bg: "#E44B8E", dark: "#B83A72" },
    { bg: "#F06862", dark: "#C0534E" },
    { bg: "#8558C8", dark: "#6A46A0" },
    { bg: "#4545B5", dark: "#363690" },
    { bg: "#42A8C8", dark: "#348698" }
  ];
  var getChapterStatus = (chapter) => {
    const allCompleted = chapter.nodes.every((n3) => n3.status === "completed");
    const hasCurrent = chapter.nodes.some((n3) => n3.status === "current");
    const hasPaused = chapter.nodes.some((n3) => n3.status === "paused");
    if (allCompleted) return "completed";
    if (hasCurrent) return "current";
    if (hasPaused) return "paused";
    return "locked";
  };
  var ChapterMenuItem = ({ chapter, color, status, isActive, onSelect, theme }) => /* @__PURE__ */ xn.createElement(
    "button",
    {
      onClick: () => onSelect(),
      className: "w-full flex items-center gap-3 px-4 py-3 rounded-xl transition-colors duration-200",
      style: {
        background: isActive ? theme.menuActive : "transparent"
      }
    },
    /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "rounded-full flex items-center justify-center",
        style: {
          width: 32,
          height: 32,
          backgroundColor: status === "locked" ? theme.lockedBg : color.bg,
          boxShadow: status === "locked" ? `0 2px 0 0 ${theme.lockedShadow}` : `0 2px 0 0 ${color.dark}`
        }
      },
      status === "completed" ? /* @__PURE__ */ xn.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("polyline", { points: "4 12 10 18 20 6", stroke: "white", strokeWidth: "3", strokeLinecap: "round", strokeLinejoin: "round" })) : status === "current" ? /* @__PURE__ */ xn.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "white" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 2l3.09 6.26L22 9.27l-5 4.87L18.18 22 12 18.27 5.82 22 7 14.14l-5-4.87 6.91-1.01L12 2z" })) : status === "paused" ? /* @__PURE__ */ xn.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10", stroke: "white", strokeWidth: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 6v6l4 2", stroke: "white", strokeWidth: "2", strokeLinecap: "round" })) : /* @__PURE__ */ xn.createElement("svg", { width: "14", height: "14", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("rect", { x: "5", y: "11", width: "14", height: "10", rx: "2", stroke: theme.lockedIcon, strokeWidth: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 11V7a4 4 0 1 1 8 0v4", stroke: theme.lockedIcon, strokeWidth: "2", strokeLinecap: "round" }))
    ),
    /* @__PURE__ */ xn.createElement("div", { className: "flex-1 text-left" }, /* @__PURE__ */ xn.createElement("p", { className: "font-bold text-[10px] uppercase tracking-widest", style: { color: status === "locked" ? theme.textMuted : theme.textSecondary } }, "Chapter ", chapter.index, status === "paused" ? " \xB7 Paused" : ""), /* @__PURE__ */ xn.createElement("p", { className: "font-extrabold text-[14px] leading-tight", style: { color: status === "locked" ? theme.lockedIcon : theme.textPrimary } }, chapter.title)),
    isActive && /* @__PURE__ */ xn.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: theme.textPrimary, strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "9 18 15 12 9 6" }))
  );
  var JourneyPath = ({ theme }) => {
    const { coins } = useUser();
    const { chapters } = mockJourneyData_default;
    const scrollRef = A2(null);
    const [activeChapterIdx, setActiveChapterIdx] = d2(() => {
      for (let i3 = 0; i3 < chapters.length; i3++) {
        if (chapters[i3].nodes.some((n3) => n3.status === "current" || n3.status === "paused")) return i3;
      }
      return 0;
    });
    const [selectedNodeId, setSelectedNodeId] = d2(null);
    const [menuOpen, setMenuOpen] = d2(false);
    const { nodePositions, chapterYStarts, chapterSeparators, slotSizes, totalHeight } = T2(() => {
      const nodePositions2 = [];
      const chapterYStarts2 = [];
      const chapterSeparators2 = [];
      let y3 = PADDING_TOP;
      let globalNodeIdx = 0;
      chapters.forEach((chapter, chapterIdx) => {
        if (chapterIdx > 0) {
          chapterSeparators2.push({
            y: y3 + CHAPTER_GAP / 2,
            title: chapter.title,
            chapterIdx
          });
          y3 += CHAPTER_GAP;
        }
        chapterYStarts2.push(y3);
        const color = SECTION_COLORS[chapterIdx % SECTION_COLORS.length];
        chapter.nodes.forEach((node2) => {
          const x5 = getNodeX(globalNodeIdx);
          globalNodeIdx++;
          nodePositions2.push({
            node: node2,
            x: x5,
            y: y3,
            chapterIdx,
            color,
            posStyle: { left: x5, top: y3 }
          });
          y3 += NODE_SPACING;
        });
      });
      const endY = y3 + PADDING_BOTTOM;
      const slotSizes2 = nodePositions2.map(
        (item, i3) => i3 < nodePositions2.length - 1 ? nodePositions2[i3 + 1].y - item.y : endY - item.y
      );
      return {
        nodePositions: nodePositions2,
        chapterYStarts: chapterYStarts2,
        chapterSeparators: chapterSeparators2,
        slotSizes: slotSizes2,
        totalHeight: endY
      };
    }, [chapters]);
    const activeChapterRef = A2(activeChapterIdx);
    const selectedNodeRef = A2(null);
    const virtualizer = useVirtualizer({
      count: nodePositions.length,
      getScrollElement: () => scrollRef.current,
      estimateSize: (i3) => slotSizes[i3],
      overscan: 4,
      onChange: () => {
        const el = scrollRef.current;
        if (!el) return;
        const scrollTop = el.scrollTop + 150;
        let idx = 0;
        for (let i3 = chapterYStarts.length - 1; i3 >= 0; i3--) {
          if (scrollTop >= chapterYStarts[i3]) {
            idx = i3;
            break;
          }
        }
        if (idx !== activeChapterRef.current) {
          activeChapterRef.current = idx;
          setActiveChapterIdx(idx);
        }
        if (selectedNodeRef.current !== null) {
          selectedNodeRef.current = null;
          setSelectedNodeId(null);
        }
      }
    });
    y2(() => {
      let targetIdx = nodePositions.findIndex((n3) => n3.node.status === "current");
      if (targetIdx < 0) targetIdx = nodePositions.findIndex((n3) => n3.node.status === "paused");
      if (targetIdx >= 0) {
        virtualizer.scrollToIndex(targetIdx, { align: "center" });
      }
    }, [nodePositions, virtualizer]);
    const handleNodeTap = q2((node2) => {
      setSelectedNodeId((prev) => {
        const next = prev === node2.id ? null : node2.id;
        selectedNodeRef.current = next;
        return next;
      });
      setMenuOpen(false);
    }, []);
    const handleStartLesson = q2((_node) => {
      setSelectedNodeId(null);
    }, []);
    const handleChapterSelect = q2((chapterIdx) => {
      setMenuOpen(false);
      setSelectedNodeId(null);
      const firstNodeIdx = nodePositions.findIndex((n3) => n3.chapterIdx === chapterIdx);
      if (firstNodeIdx >= 0) {
        virtualizer.scrollToIndex(firstNodeIdx, { align: "start", behavior: "smooth" });
      }
    }, [nodePositions, virtualizer]);
    const activeChapter = chapters[activeChapterIdx];
    const activeColor = SECTION_COLORS[activeChapterIdx % SECTION_COLORS.length];
    return /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "font-jakarta",
        style: {
          position: "absolute",
          top: 0,
          left: 0,
          right: 0,
          bottom: 0,
          display: "flex",
          flexDirection: "column",
          overflow: "hidden",
          background: theme.bg,
          transition: "background 0.3s ease"
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-center gap-7 px-4 pt-3 pb-4", style: { flexShrink: 0 } }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ xn.createElement("div", { className: "coin-shimmer", style: { width: 36, height: 36, borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", background: "radial-gradient(circle at 35% 30%, #C8C8C8, #8A8A8A 70%, #6E6E6E)", boxShadow: "0 2px 0 0 #555" } }, /* @__PURE__ */ xn.createElement("span", { className: "font-extrabold text-[14px] text-white", style: { textShadow: "0 1px 1px rgba(0,0,0,0.25)" } }, "B")), /* @__PURE__ */ xn.createElement("span", { className: "font-extrabold text-[18px]", style: { color: theme.coinsText } }, coins)), /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ xn.createElement("div", { style: { width: 36, height: 36, borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", background: "linear-gradient(180deg, #FF9600 0%, #FF7A00 100%)", boxShadow: "0 2px 0 0 #CC6600" } }, /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 22c-4.4 0-7-2.7-7-6.2 0-2.2 1.3-4.2 2.6-5.7l1.3 1.3c.35.35.9.1.9-.45V4c0-.5.35-.85.8-.8C14.5 4 19 7.8 19 12.8c0 4.8-3 9.2-7 9.2z", fill: "white" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 22c-2.2 0-3.5-1.6-3.5-3.5 0-1.3.7-2.5 1.6-3.4l.6.6c.18.18.45.08.45-.18V12.5c0-.25.18-.45.45-.42 1.8.45 4 2.5 4 5.7 0 2.5-1.3 4.2-3.6 4.2z", fill: "#FFF3D6" }))), /* @__PURE__ */ xn.createElement("span", { className: "font-extrabold text-[18px] text-[#FF9600]" }, "3")), /* @__PURE__ */ xn.createElement("div", { style: { width: 36, height: 36, borderRadius: "50%", display: "flex", alignItems: "center", justifyContent: "center", background: "linear-gradient(135deg, #8558C8 0%, #4545B5 100%)", boxShadow: "0 2px 0 0 #363690" } }, /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "white" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 2l3.09 6.26L22 9.27l-5 4.87L18.18 22 12 18.27 5.82 22 7 14.14l-5-4.87 6.91-1.01L12 2z" })))),
      /* @__PURE__ */ xn.createElement("div", { style: { flexShrink: 0, marginLeft: 12, marginRight: 12, marginBottom: 8, position: "relative", zIndex: 20 } }, /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: () => {
            setMenuOpen((prev) => !prev);
            setSelectedNodeId(null);
          },
          className: "w-full rounded-2xl px-5 py-3 flex items-center justify-between",
          style: {
            position: "relative",
            zIndex: 22,
            backgroundColor: activeColor.bg,
            boxShadow: menuOpen ? "none" : `0 4px 0 0 ${activeColor.dark}`,
            borderRadius: menuOpen ? "16px 16px 0 0" : "16px"
          }
        },
        /* @__PURE__ */ xn.createElement("div", { className: "text-left" }, /* @__PURE__ */ xn.createElement("p", { className: "font-bold text-[11px] text-white/70 uppercase tracking-widest" }, "Chapter ", activeChapter.index), /* @__PURE__ */ xn.createElement("p", { className: "font-extrabold text-[17px] text-white leading-tight mt-0.5" }, activeChapter.title)),
        /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "w-10 h-10 rounded-xl bg-white/20 flex items-center justify-center",
            style: {
              transform: menuOpen ? "rotate(180deg)" : "rotate(0deg)",
              transition: "transform 0.3s cubic-bezier(0.4, 0, 0.2, 1)"
            }
          },
          /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: "white", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "6 9 12 15 18 9" }))
        )
      ), /* @__PURE__ */ xn.createElement(
        "div",
        {
          style: {
            position: "absolute",
            left: 0,
            right: 0,
            top: "100%",
            zIndex: 21,
            maxHeight: menuOpen ? 400 : 0,
            opacity: menuOpen ? 1 : 0,
            overflowY: menuOpen ? "auto" : "hidden",
            overflowX: "hidden",
            transition: "opacity 0.25s ease",
            backgroundColor: theme.glassBg,
            borderRadius: "0 0 16px 16px",
            border: `1px solid ${theme.glassBorder}`,
            borderTop: "none",
            boxShadow: menuOpen ? theme.menuShadow : "none"
          }
        },
        /* @__PURE__ */ xn.createElement("div", { className: "py-2 px-1" }, chapters.map((chapter, idx) => {
          const color = SECTION_COLORS[idx % SECTION_COLORS.length];
          const status = getChapterStatus(chapter);
          return /* @__PURE__ */ xn.createElement(
            ChapterMenuItem,
            {
              key: chapter.index,
              chapter,
              color,
              status,
              isActive: idx === activeChapterIdx,
              onSelect: () => handleChapterSelect(idx),
              theme
            }
          );
        }))
      )),
      menuOpen && /* @__PURE__ */ xn.createElement(
        "div",
        {
          onClick: () => setMenuOpen(false),
          style: {
            position: "absolute",
            top: 0,
            left: 0,
            right: 0,
            bottom: 0,
            zIndex: 15,
            background: theme.backdrop
          }
        }
      ),
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          ref: scrollRef,
          className: "scrollbar-hide",
          style: {
            flex: 1,
            minHeight: 0,
            overflowY: "auto",
            overflowX: "hidden",
            contain: "strict"
          }
        },
        /* @__PURE__ */ xn.createElement("div", { className: "relative w-full", style: { height: totalHeight } }, chapterSeparators.map((sep2) => /* @__PURE__ */ xn.createElement(
          "div",
          {
            key: `sep-${sep2.chapterIdx}`,
            style: {
              position: "absolute",
              top: sep2.y,
              left: 0,
              right: 0,
              transform: "translateY(-50%)",
              display: "flex",
              alignItems: "center",
              gap: 12,
              paddingLeft: 24,
              paddingRight: 24
            }
          },
          /* @__PURE__ */ xn.createElement("div", { style: { flex: 1, height: 1, background: theme.separatorLine } }),
          /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-bold", style: { fontSize: 13, letterSpacing: 0.3, color: theme.separatorText, whiteSpace: "nowrap" } }, sep2.title),
          /* @__PURE__ */ xn.createElement("div", { style: { flex: 1, height: 1, background: theme.separatorLine } })
        )), virtualizer.getVirtualItems().map((vItem) => {
          const item = nodePositions[vItem.index];
          return /* @__PURE__ */ xn.createElement(
            JourneyNode_default,
            {
              key: item.node.id,
              node: item.node,
              nodeX: item.x,
              isSelected: selectedNodeId === item.node.id,
              showFloatingLabel: item.node.status === "current",
              onTap: handleNodeTap,
              onStart: handleStartLesson,
              accentColor: item.color.bg,
              accentDark: item.color.dark,
              theme,
              style: item.posStyle
            }
          );
        }))
      )
    );
  };
  var JourneyPath_default = JourneyPath;

  // src/contexts/DailyQuestionContext.jsx
  var STORAGE_KEY2 = "bonds_daily_question";
  var DEFAULT_DQ = {
    questionId: null,
    category: "",
    question: "",
    options: [],
    selectedAnswer: null,
    date: null
    // ISO date string, e.g. '2026-03-01'
  };
  var DailyQuestionContext = Q(DEFAULT_DQ);
  var useDailyQuestion = () => x2(DailyQuestionContext);
  var todayStr = () => {
    const d3 = /* @__PURE__ */ new Date();
    return `${d3.getFullYear()}-${String(d3.getMonth() + 1).padStart(2, "0")}-${String(d3.getDate()).padStart(2, "0")}`;
  };
  var DailyQuestionProvider = ({ children }) => {
    const [dq, setDq] = d2(() => {
      try {
        const cached = localStorage.getItem(STORAGE_KEY2);
        return cached ? { ...DEFAULT_DQ, ...JSON.parse(cached) } : DEFAULT_DQ;
      } catch {
        return DEFAULT_DQ;
      }
    });
    const { getItem, setItem, removeItem } = useNativelyStorage();
    const fetchedRef = A2(false);
    y2(() => {
      getItem(STORAGE_KEY2).then((val) => {
        if (val) {
          try {
            const parsed = { ...DEFAULT_DQ, ...JSON.parse(val) };
            setDq((prev) => JSON.stringify(prev) !== JSON.stringify(parsed) ? parsed : prev);
          } catch {
          }
        }
      });
    }, [getItem]);
    const persist = q2((data) => {
      const json = JSON.stringify(data);
      localStorage.setItem(STORAGE_KEY2, json);
      setItem(STORAGE_KEY2, json);
    }, [setItem]);
    const setDailyQuestion = q2((data) => {
      const overrides = {};
      if ("selectedAnswer" in data) {
        const sa = data.selectedAnswer;
        overrides.selectedAnswer = sa != null && sa !== "" ? Number(sa) : null;
      }
      if ("options" in data) {
        let opts = data.options || [];
        if (typeof opts === "string") {
          try {
            opts = JSON.parse(opts);
          } catch {
            try {
              opts = JSON.parse(`[${opts}]`);
            } catch {
              opts = [];
            }
          }
          if (!Array.isArray(opts)) opts = [];
        }
        if (opts.length && opts[0].index !== void 0) {
          opts.sort((a3, b2) => a3.index - b2.index);
        }
        overrides.options = opts;
      }
      setDq((prev) => {
        const next = { ...prev, ...data, ...overrides, date: todayStr() };
        persist(next);
        return next;
      });
    }, [persist]);
    const markAnswered = q2((answerIndex) => {
      setDq((prev) => {
        const next = { ...prev, selectedAnswer: answerIndex };
        persist(next);
        return next;
      });
    }, [persist]);
    const clearDailyQuestion = q2(() => {
      setDq(DEFAULT_DQ);
      localStorage.removeItem(STORAGE_KEY2);
      removeItem(STORAGE_KEY2);
    }, [removeItem]);
    const fetchIfStale = q2(() => {
      if (fetchedRef.current) return;
      fetchedRef.current = true;
      sendToBubble("bubble_fn_daily_question", "fetch");
    }, []);
    const isStale = dq.date !== todayStr();
    y2(() => {
      const onVisible = () => {
        if (document.visibilityState !== "visible") return;
        fetchedRef.current = false;
        if (dq.date !== todayStr()) {
          setDq((prev) => ({ ...prev }));
        }
      };
      document.addEventListener("visibilitychange", onVisible);
      return () => document.removeEventListener("visibilitychange", onVisible);
    }, [dq.date]);
    y2(() => {
      window.appUI = window.appUI || {};
      window.appUI.setDailyQuestion = setDailyQuestion;
      return () => {
        delete window.appUI.setDailyQuestion;
      };
    }, [setDailyQuestion]);
    const value = T2(() => ({
      ...dq,
      isStale,
      setDailyQuestion,
      markAnswered,
      clearDailyQuestion,
      fetchIfStale
    }), [dq, isStale, setDailyQuestion, markAnswered, clearDailyQuestion, fetchIfStale]);
    return /* @__PURE__ */ xn.createElement(DailyQuestionContext.Provider, { value }, children);
  };

  // src/components/DailyQuestion.jsx
  var DUOLINGO_GREEN = "#58CC02";
  var GREEN_SHADOW = "#46A302";
  var COIN_BG = "radial-gradient(circle at 35% 30%, #C8C8C8, #8A8A8A 70%, #6E6E6E)";
  var COIN_SHADOW = "0 2px 0 0 #555";
  var DailyQuestion = ({ theme, pop: _pop }) => {
    const { name: userName, coins: userCoins, updateUser } = useUser();
    const { questionId, category, question, options, selectedAnswer: initialSelectedAnswer, markAnswered } = useDailyQuestion();
    const [coins, setCoins] = d2(userCoins || 0);
    const [selectedAnswer, setSelectedAnswer] = d2(initialSelectedAnswer);
    const [isVoted, setIsVoted] = d2(initialSelectedAnswer !== void 0 && initialSelectedAnswer !== null);
    const [showFooterAfter, setShowFooterAfter] = d2(isVoted);
    const coinRef = A2(null);
    const targetCoinsRef = A2(null);
    const animatingCoinsRef = A2(false);
    const timersRef = A2([]);
    const overlaysRef = A2([]);
    const rafRef = A2(null);
    y2(() => {
      if (initialSelectedAnswer !== void 0 && initialSelectedAnswer !== null) {
        setSelectedAnswer(initialSelectedAnswer);
        setIsVoted(true);
      }
    }, [initialSelectedAnswer]);
    y2(() => {
      if (!animatingCoinsRef.current) setCoins(userCoins || 0);
    }, [userCoins]);
    y2(() => {
      return () => {
        timersRef.current.forEach(clearTimeout);
        timersRef.current = [];
        overlaysRef.current.forEach((el) => {
          try {
            el.remove();
          } catch {
          }
        });
        overlaysRef.current = [];
        if (rafRef.current) cancelAnimationFrame(rafRef.current);
        animatingCoinsRef.current = false;
      };
    }, []);
    const addTimer = (fn2, delay) => {
      const id = setTimeout(fn2, delay);
      timersRef.current.push(id);
      return id;
    };
    const handleVote = (_answerText, index2) => {
      if (isVoted) return;
      setIsVoted(true);
      setSelectedAnswer(index2);
      addTimer(() => {
        setShowFooterAfter(true);
      }, 800);
      sendToBubble("bubble_fn_daily_question", "vote", { questionId, index: index2 });
      markAnswered(index2);
      const newCoins = (userCoins || 0) + 1;
      targetCoinsRef.current = newCoins;
      animatingCoinsRef.current = true;
      updateUser({ coins: newCoins });
      addTimer(() => {
        triggerCoinAnimation();
      }, 2e3);
    };
    const triggerCoinAnimation = () => {
      const dimOverlay = document.createElement("div");
      dimOverlay.className = "dim-overlay active";
      document.body.appendChild(dimOverlay);
      overlaysRef.current.push(dimOverlay);
      const overlay = document.createElement("div");
      overlay.className = "coin-center-animation";
      overlay.style.cssText = `
            position: fixed;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            z-index: 9999;
            pointer-events: none;
        `;
      overlay.innerHTML = `
            <div class="overlay-coin" style="width: 80px; height: 80px; border-radius: 50%; background: ${COIN_BG}; display: flex; align-items: center; justify-content: center; box-shadow: 0 6px 24px rgba(0,0,0,0.3), ${COIN_SHADOW};">
                <span style="font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 800; font-size: 2rem; color: #fff; text-shadow: 0 1px 2px rgba(0,0,0,0.3); line-height: 1;">B</span>
            </div>
            <span class="overlay-plus" style="position: absolute; left: 100%; top: 50%; transform: translateY(-50%); margin-left: 10px; white-space: nowrap; font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 800; font-size: 2rem; color: #fff; text-shadow: 0 2px 8px rgba(0,0,0,0.4); opacity: 0; transition: opacity 300ms ease;">+1</span>
        `;
      document.body.appendChild(overlay);
      overlaysRef.current.push(overlay);
      addTimer(() => {
        const plus = overlay.querySelector(".overlay-plus");
        if (plus) plus.style.opacity = "1";
      }, 500);
      addTimer(() => {
        const plus = overlay.querySelector(".overlay-plus");
        if (plus) plus.style.opacity = "0";
      }, 1100);
      addTimer(() => {
        const startRect = overlay.getBoundingClientRect();
        overlay.classList.remove("coin-center-animation");
        overlay.style.top = startRect.top + "px";
        overlay.style.left = startRect.left + "px";
        overlay.style.transform = "none";
        overlay.classList.add("coin-move-animation");
        dimOverlay.classList.remove("active");
        overlay.offsetHeight;
        if (coinRef.current) {
          const targetRect = coinRef.current.getBoundingClientRect();
          const dx = targetRect.left - startRect.left;
          const dy = targetRect.top - startRect.top;
          overlay.style.transform = `translate(${dx}px, ${dy}px)`;
          const coinEl = overlay.querySelector(".overlay-coin");
          if (coinEl) {
            coinEl.style.transition = "transform 600ms cubic-bezier(0.4, 0, 0.2, 1)";
            coinEl.style.transformOrigin = "top left";
            coinEl.style.transform = "scale(0.4)";
          }
        }
      }, 1300);
      addTimer(() => {
        overlay.remove();
        dimOverlay.remove();
        overlaysRef.current = overlaysRef.current.filter((el) => el !== overlay && el !== dimOverlay);
        if (coinRef.current) {
          coinRef.current.style.transition = "transform 0.3s ease";
          coinRef.current.style.transform = "scale(1.3)";
          addTimer(() => {
            if (coinRef.current) coinRef.current.style.transform = "scale(1)";
          }, 300);
        }
        const numEl = document.getElementById("coinsNumber");
        if (numEl) {
          let tick = function(timestamp) {
            if (!startTime) startTime = timestamp;
            const progress = Math.min((timestamp - startTime) / duration, 1);
            const easeOut = 1 - Math.pow(1 - progress, 3);
            const current = Math.round(start + (end - start) * easeOut);
            numEl.textContent = current;
            if (progress < 1) {
              rafRef.current = requestAnimationFrame(tick);
            } else {
              animatingCoinsRef.current = false;
              setCoins(end);
            }
          };
          const end = targetCoinsRef.current ?? coins;
          const start = end - 1;
          const duration = 400;
          let startTime = null;
          rafRef.current = requestAnimationFrame(tick);
        }
      }, 1900);
    };
    const handleStart = () => {
      sendToBubble("bubble_fn_daily_question", "start_planning");
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "relative w-full h-full overflow-x-hidden font-poppins flex flex-col", style: { background: theme.bg } }, /* @__PURE__ */ xn.createElement("div", { className: "absolute top-5 right-5 z-20 flex items-center gap-2" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        ref: coinRef,
        className: "coin-shimmer",
        style: {
          width: 32,
          height: 32,
          borderRadius: "50%",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          background: COIN_BG,
          boxShadow: COIN_SHADOW
        }
      },
      /* @__PURE__ */ xn.createElement("span", { className: "font-extrabold text-[12px] text-white", style: { textShadow: "0 1px 1px rgba(0,0,0,0.25)" } }, "B")
    ), /* @__PURE__ */ xn.createElement("span", { id: "coinsNumber", className: "font-extrabold text-[16px]", style: { color: theme.coinsText } }, coins)), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col flex-1 min-h-0 px-7 pt-16 pb-8 max-w-[375px] mx-auto relative overflow-y-auto" }, /* @__PURE__ */ xn.createElement("div", { className: "font-jakarta font-medium text-lg mb-6", style: { color: theme.textSecondary } }, category || "Time Together"), /* @__PURE__ */ xn.createElement("div", { className: "font-poppins font-bold text-xl leading-[30px] tracking-[0.02em] mb-8 max-w-[303px]", style: { color: theme.textPrimary } }, question), /* @__PURE__ */ xn.createElement("div", { className: "space-y-3" }, options.map((opt, i3) => {
      const optIndex = opt.index !== void 0 ? opt.index : i3 + 1;
      const isSelected = isVoted && selectedAnswer === optIndex;
      return /* @__PURE__ */ xn.createElement(
        "button",
        {
          key: optIndex,
          className: `relative w-full h-12 rounded-xl border-2 border-solid cursor-pointer overflow-hidden transition-[transform,border-color] duration-200 text-left ${isVoted ? "pointer-events-none" : "active:translate-y-[2px]"}`,
          style: {
            background: theme.surface,
            borderColor: isSelected ? DUOLINGO_GREEN : theme.border,
            boxShadow: isSelected ? `0 4px 0 ${GREEN_SHADOW}` : isVoted ? "none" : `0 4px 0 ${theme.cardShadow}`
          },
          onClick: () => handleVote(opt.text, optIndex)
        },
        /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "option-bar absolute left-0 top-0 h-full w-full rounded-xl",
            style: {
              transform: isVoted ? `scaleX(${opt.percent / 100})` : void 0,
              background: isSelected ? `${DUOLINGO_GREEN}4D` : `${DUOLINGO_GREEN}30`
            }
          }
        ),
        /* @__PURE__ */ xn.createElement("div", { className: "relative flex items-center justify-between h-full px-4 z-10" }, /* @__PURE__ */ xn.createElement("span", { className: "font-poppins font-bold text-sm tracking-[0.02em]", style: { color: theme.textPrimary } }, opt.text), /* @__PURE__ */ xn.createElement(
          "span",
          {
            className: `percentage font-poppins text-xs tracking-[0.02em] ${isVoted ? "opacity-100" : "opacity-0"}`,
            style: { color: theme.textPrimary, fontWeight: isSelected ? "bold" : "normal" }
          },
          opt.percent,
          "%"
        ))
      );
    })), /* @__PURE__ */ xn.createElement("div", { className: "flex-1" }), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col items-center justify-center py-4" }, !showFooterAfter ? /* @__PURE__ */ xn.createElement("div", { className: "font-poppins text-base text-center leading-6 tracking-[0.02em] max-w-[295px]", style: { color: theme.textSecondary } }, "Vote and see the live results and also gain 1 coin") : /* @__PURE__ */ xn.createElement(xn.Fragment, null, /* @__PURE__ */ xn.createElement("div", { className: "font-poppins text-base text-center leading-6 tracking-[0.02em] max-w-[309px] mb-6 animate-fade-in", style: { color: theme.textSecondary } }, userName, ", We would love to answer any follow up question you might have about ", category), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleStart,
        className: "px-10 py-3 rounded-xl border-b-[3px] border-solid border-[#D4D4D4] active:border-b-0 active:mt-[3px] animate-fade-in pointer-events-auto",
        style: { background: "#FFFFFF", color: DUOLINGO_GREEN }
      },
      /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[15px] pointer-events-none" }, "Start")
    )))));
  };
  var DailyQuestion_default = DailyQuestion;

  // src/components/FunZoneSection.jsx
  var leoThink = `${BUBBLE_CDN}/f1772112702436x988725373375085300/leo_think.png`;
  var leoYay = `${BUBBLE_CDN}/f1772112696167x295067163375400700/leo_yay.png`;
  var cardsAnimated = false;
  var ACTIVITIES = [
    { id: "funky-date", name: "Funky Date", emoji: "\u{1F4C5}", subtitle: "Spark something new together", cost: 4, accent: "#FF9600" },
    { id: "dialogue-starter", name: "Dialogue Starter", emoji: "\u{1F4AC}", subtitle: "Great conversations start here", cost: 4, accent: "#1CB0F6" },
    { id: "gift-inspiration", name: "Gift Inspiration", emoji: "\u{1F381}", subtitle: "Find the perfect surprise", cost: 4, accent: "#CE82FF" }
  ];
  var DailyQuestionSkeleton = ({ theme }) => /* @__PURE__ */ xn.createElement(
    "div",
    {
      className: "w-full rounded-2xl overflow-hidden border-2 border-solid animate-pulse",
      style: { background: theme.surface, borderColor: theme.border }
    },
    /* @__PURE__ */ xn.createElement("div", { className: "flex items-center p-4 pr-2" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-w-0" }, /* @__PURE__ */ xn.createElement("div", { className: "h-3 w-24 rounded-full mb-3", style: { background: theme.border } }), /* @__PURE__ */ xn.createElement("div", { className: "h-4 w-full rounded-full mb-2", style: { background: theme.border } }), /* @__PURE__ */ xn.createElement("div", { className: "h-4 w-3/4 rounded-full mb-3", style: { background: theme.border } }), /* @__PURE__ */ xn.createElement("div", { className: "h-9 w-28 rounded-xl", style: { background: theme.border } })), /* @__PURE__ */ xn.createElement("div", { className: "w-24 h-24 rounded-full flex-shrink-0 -mr-1", style: { background: theme.border, opacity: 0.5 } }))
  );
  var DailyQuestionBanner = ({ dailyQuestion, push, theme }) => {
    if (!dailyQuestion?.question && dailyQuestion?.isStale) return /* @__PURE__ */ xn.createElement(DailyQuestionSkeleton, { theme });
    if (!dailyQuestion?.question) return null;
    const isAnswered = dailyQuestion?.selectedAnswer !== void 0 && dailyQuestion?.selectedAnswer !== null;
    const handleTap = () => {
      sendToBubble("bubble_fn_fun", "open_daily_question");
      push("daily-question");
    };
    const isDark = theme.isDark;
    if (isAnswered) {
      return /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleTap,
          className: "w-full rounded-2xl text-left relative overflow-hidden border-2 border-solid",
          style: {
            background: isDark ? "#1A3A2A" : "#EEFBF0",
            borderColor: "#2ECC71",
            boxShadow: isDark ? "0 4px 0 #145A2A" : "0 4px 0 #C1E8C9"
          }
        },
        /* @__PURE__ */ xn.createElement("div", { className: "flex items-center p-4 pr-2" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-w-0" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2 mb-1" }, /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10", fill: "#2ECC71" }), /* @__PURE__ */ xn.createElement("polyline", { points: "8 12 11 15 16 9", fill: "none", stroke: "white", strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" })), /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[14px] text-[#2ECC71]" }, "Completed!")), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[13px] leading-snug", style: { color: theme.textSecondary } }, "Daily Question"), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins font-medium text-[13px] leading-snug mt-1 line-clamp-1", style: { color: theme.textMuted } }, "Come back tomorrow for a new one")), /* @__PURE__ */ xn.createElement(
          "img",
          {
            src: leoYay,
            alt: "Leo celebrating",
            className: "w-20 h-20 object-contain flex-shrink-0 -mr-1"
          }
        ))
      );
    }
    return /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleTap,
        className: "w-full rounded-2xl text-left relative border-2 border-solid animate-breathing-glow",
        style: {
          background: "linear-gradient(135deg, #58CC02 0%, #46A302 100%)",
          borderColor: "#58CC02",
          boxShadow: "0 4px 0 #3A8A01"
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "flex items-center p-4 pr-2" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 min-w-0" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2 mb-2" }, /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[11px] text-white/80 uppercase tracking-widest" }, "Daily Question"), /* @__PURE__ */ xn.createElement("span", { className: "flex items-center gap-1 bg-white/20 rounded-full px-2.5 py-0.5" }, /* @__PURE__ */ xn.createElement("svg", { width: "12", height: "12", viewBox: "0 0 24 24", fill: "none" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10", fill: "#FFD700", stroke: "#DAA520", strokeWidth: "1" }), /* @__PURE__ */ xn.createElement("text", { x: "12", y: "16", textAnchor: "middle", fill: "#8B6914", fontSize: "12", fontWeight: "bold" }, "$")), /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-bold text-[10px] text-white" }, "+1"))), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins font-bold text-[15px] text-white leading-snug line-clamp-2 mb-3 pr-2" }, dailyQuestion?.question || "No question available today"), /* @__PURE__ */ xn.createElement(
        "span",
        {
          className: "inline-block font-jakarta font-extrabold text-[13px] text-[#58CC02] rounded-xl px-5 py-2 border-b-[3px] border-solid border-[#D4D4D4] active:border-b-0 active:mt-[3px]",
          style: { background: "#FFFFFF" }
        },
        "Answer Now"
      )), /* @__PURE__ */ xn.createElement(
        "img",
        {
          src: leoThink,
          alt: "Dr. Leo thinking",
          className: "w-24 h-24 object-contain flex-shrink-0 -mr-1 -mb-2"
        }
      ))
    );
  };
  var ActivityCard = ({ activity, index: index2, push, theme, animate }) => {
    const handleTap = () => {
      sendToBubble("bubble_fn_fun", "open_activity", { activityId: activity.id });
      push("insight-flow", { type: "activity", activityId: activity.id });
    };
    return /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleTap,
        className: `w-full rounded-2xl text-left relative overflow-hidden border-2 border-solid ${animate ? "animate-fade-in" : ""}`,
        style: {
          opacity: animate ? 0 : 1,
          animationDelay: animate ? `${index2 * 100}ms` : void 0,
          background: theme.surface,
          borderColor: theme.border,
          boxShadow: `0 4px 0 ${theme.cardShadow}`
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "py-5 pl-5 pr-24 min-h-[100px]" }, /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-extrabold text-[18px] mb-1.5", style: { color: theme.textPrimary } }, activity.name), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[13px] leading-snug", style: { color: theme.textSecondary } }, activity.subtitle), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[11px] mt-2", style: { color: theme.textMuted } }, activity.cost, " coins")),
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "absolute -bottom-2 -right-2 w-[80px] h-[80px] rounded-full flex items-center justify-center",
          style: { background: activity.accent }
        },
        /* @__PURE__ */ xn.createElement("span", { className: "text-[36px] leading-none" }, activity.emoji)
      )
    );
  };
  var FunZoneSection = ({ theme, push }) => {
    const dailyQuestion = useDailyQuestion();
    const { fetchIfStale } = dailyQuestion;
    y2(() => {
      fetchIfStale();
    }, [fetchIfStale]);
    const animate = !cardsAnimated;
    if (!cardsAnimated) cardsAnimated = true;
    return /* @__PURE__ */ xn.createElement("div", { className: "px-5 pt-6 pb-10 font-poppins" }, /* @__PURE__ */ xn.createElement(DailyQuestionBanner, { dailyQuestion, push, theme }), /* @__PURE__ */ xn.createElement("div", { className: "mt-8" }, /* @__PURE__ */ xn.createElement("h2", { className: "font-jakarta font-bold text-[18px] mb-4", style: { color: theme.textPrimary } }, "Activities"), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col gap-4" }, ACTIVITIES.map((activity, i3) => /* @__PURE__ */ xn.createElement(
      ActivityCard,
      {
        key: activity.id,
        activity,
        index: i3,
        push,
        theme,
        animate
      }
    )))), /* @__PURE__ */ xn.createElement("div", { className: "mt-8" }, /* @__PURE__ */ xn.createElement("h2", { className: "font-jakarta font-bold text-[18px] mb-4", style: { color: theme.textPrimary } }, "Learn"), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: () => {
          sendToBubble("bubble_fn_fun", "open_learn");
          push("insight-flow", { type: "learn" });
        },
        className: `w-full rounded-2xl text-left relative overflow-hidden border-2 border-solid ${animate ? "animate-fade-in" : ""}`,
        style: {
          opacity: animate ? 0 : 1,
          animationDelay: animate ? "300ms" : void 0,
          background: theme.surface,
          borderColor: "#E44B8E",
          boxShadow: `0 4px 0 ${theme.cardShadow}`
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "py-5 pl-5 pr-24 min-h-[100px]" }, /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-extrabold text-[18px] mb-1.5", style: { color: theme.textPrimary } }, "Get an Insight"), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[13px] leading-snug", style: { color: theme.textSecondary } }, "Answer 3 quick questions and receive a personalized insight")),
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "absolute -bottom-2 -right-2 w-[80px] h-[80px] rounded-full flex items-center justify-center",
          style: { background: "#E44B8E" }
        },
        /* @__PURE__ */ xn.createElement("span", { className: "text-[36px] leading-none" }, "\u{1F4A1}")
      )
    )));
  };
  var FunZoneSection_default = FunZoneSection;

  // src/theme.js
  var THEMES = {
    dark: {
      isDark: true,
      bg: "#1B1B2F",
      surface: "#252538",
      textPrimary: "#FFFFFF",
      textSecondary: "rgba(255,255,255,0.6)",
      textMuted: "rgba(255,255,255,0.3)",
      border: "rgba(255,255,255,0.1)",
      navInactive: "rgba(255,255,255,0.6)",
      lockedBg: "#3C3C4E",
      lockedShadow: "#2A2A3A",
      lockedIcon: "rgba(255,255,255,0.35)",
      labelCompleted: "rgba(255,255,255,0.5)",
      labelCurrent: "#FFFFFF",
      labelLocked: "rgba(255,255,255,0.25)",
      floatBg: "#2B2B3D",
      floatBorder: "#4A4A5E",
      menuActive: "rgba(255,255,255,0.1)",
      menuShadow: "0 8px 24px rgba(0,0,0,0.4)",
      backdrop: "rgba(0,0,0,0.4)",
      coinsText: "#D4D4D4",
      glassBg: "rgba(37,37,56,0.97)",
      glassBorder: "rgba(255,255,255,0.08)",
      cardShadow: "#0A0A16",
      separatorLine: "rgba(255,255,255,0.2)",
      separatorText: "rgba(255,255,255,0.4)",
      dialogBg: "#252538",
      dialogBorder: "rgba(255,255,255,0.1)",
      dialogTitle: "#FFFFFF",
      dialogText: "rgba(255,255,255,0.7)",
      dialogDimBg: "rgba(0,0,0,0.6)",
      onboarding: {
        bg: "#1B1B2F",
        text: "#FFFFFF",
        textSecondary: "rgba(255,255,255,0.6)",
        textMuted: "rgba(255,255,255,0.3)",
        optionBg: "#252538",
        optionBorder: "rgba(255,255,255,0.1)",
        optionSelectedBg: "#252538",
        optionSelectedBorder: "#E44B8E",
        optionShadow: "0 4px 0 #0A0A16",
        optionSelectedShadow: "0 4px 0 #B83A72",
        optionDimmedBg: "#252538",
        optionDimmedBorder: "rgba(255,255,255,0.06)",
        optionDimmedShadow: "none",
        progressBarBg: "#252538",
        progressBarBorder: "rgba(255,255,255,0.08)",
        progressBarFill: "#E44B8E",
        backBg: "rgba(37,37,56,0.97)",
        backBorder: "rgba(255,255,255,0.08)",
        backIcon: "#FFFFFF",
        divider: "rgba(255,255,255,0.08)",
        ctaBg: "#E44B8E",
        ctaShadow: "0 4px 0 #B83A72",
        ctaDisabledBg: "#252538",
        ctaDisabledBorder: "rgba(255,255,255,0.1)",
        ctaDisabledShadow: "0 4px 0 #0A0A16",
        ctaDisabledText: "rgba(255,255,255,0.3)",
        ctaText: "#FFFFFF",
        sliderFilled: "#E44B8E",
        sliderUnfilled: "rgba(255,255,255,0.1)",
        dotEmpty: "#252538",
        dotEmptyBorder: "rgba(255,255,255,0.1)",
        dotEmptyShadow: "0 3px 0 #0A0A16",
        dotFilledShadow: "0 3px 0 #B83A72",
        labelFilled: "rgba(255,255,255,0.8)",
        labelUnfilled: "rgba(255,255,255,0.4)",
        inputBg: "#252538",
        inputBorder: "rgba(255,255,255,0.1)",
        inputFocusBorder: "#E44B8E",
        inputPlaceholder: "rgba(255,255,255,0.3)",
        charCount: "rgba(255,255,255,0.25)",
        refreshIcon: "#E44B8E"
      },
      simulator: {
        landingBg: "#252538",
        landingBorder: "rgba(255,255,255,0.08)",
        landingTitle: "#FFFFFF",
        landingSubtitle: "rgba(255,255,255,0.6)",
        landingHint: "rgba(255,255,255,0.4)",
        coinCost: "rgba(255,255,255,0.35)",
        sessionBg: "#1B1B2F",
        statusText: "#FFFFFF",
        statusSubtext: "rgba(255,255,255,0.5)",
        waveformLine: "#E44B8E",
        waveformBg: "transparent",
        micBg: "rgba(255,255,255,0.08)",
        micBorder: "rgba(255,255,255,0.2)",
        micIcon: "#FFFFFF",
        micActiveBg: "#E44B8E",
        micActiveBorder: "#B83A72",
        micActiveIcon: "#FFFFFF",
        micStopBg: "rgba(228,75,142,0.15)",
        micStopIcon: "#E44B8E",
        transcriptBg: "rgba(255,255,255,0.06)",
        transcriptBorder: "rgba(255,255,255,0.1)",
        transcriptText: "#FFFFFF",
        dotActive: "#E44B8E",
        dotInactive: "rgba(255,255,255,0.15)",
        dotBorder: "rgba(255,255,255,0.2)",
        transitionText: "#FFFFFF",
        transitionSubtext: "rgba(255,255,255,0.5)",
        resultsBg: "#252538",
        resultsBorder: "rgba(255,255,255,0.08)",
        resultsTitle: "#FFFFFF",
        scoreFraction: "rgba(255,255,255,0.5)",
        metricUp: "#58CC02",
        metricDown: "#FF4B4B",
        metricNeutral: "rgba(255,255,255,0.4)",
        metricText: "#FFFFFF",
        sectionTitle: "#58CC02",
        sectionTitleImprove: "#FF9600",
        bulletText: "rgba(255,255,255,0.7)",
        readMoreText: "#E44B8E",
        dialogBg: "#252538",
        dialogBorder: "rgba(255,255,255,0.1)",
        dialogTitle: "#FFFFFF",
        dialogText: "rgba(255,255,255,0.7)",
        dialogDimBg: "rgba(0,0,0,0.6)",
        facePhase1Fill: "#6D6987",
        facePhase1Opacity: 0.7,
        facePartnerFill: "#E1327F",
        facePartnerOpacity: 0.3,
        faceUserFill: "#6D6987",
        faceUserOpacity: 0.3,
        glowPartner: "radial-gradient(ellipse at left center, rgba(225,50,127,0.35) 0%, transparent 70%)",
        glowUser: "radial-gradient(ellipse at right center, rgba(109,105,135,0.45) 0%, transparent 70%)",
        micWaitingGlow: "drop-shadow(0 0 4px rgba(255,255,255,0.7)) drop-shadow(0 0 8px rgba(255,255,255,0.3))",
        micRecordingBg: "rgba(255,255,255,0.2)"
      },
      insight: {
        btnYesBg: "#58CC02",
        btnYesShadow: "#46A302",
        btnNoBg: "#FF4B4B",
        btnNoShadow: "#CC3333",
        btnOtherBg: "rgba(255,255,255,0.15)",
        btnOtherBorder: "rgba(255,255,255,0.3)",
        progressFill: "#E44B8E",
        progressBg: "rgba(255,255,255,0.2)",
        navBg: "rgba(0,0,0,0.3)",
        navIcon: "#FFFFFF",
        loaderBg: "#1B1B2F",
        loaderText: "#FFFFFF",
        loaderSpinner: "#E44B8E",
        controlBg: "rgba(255,255,255,0.15)",
        controlIcon: "#FFFFFF",
        subtitleText: "#FFFFFF",
        subtitleBg: "rgba(0,0,0,0.5)",
        continueBg: "#58CC02",
        continueShadow: "#46A302",
        feedbackBg: "rgba(255,255,255,0.1)",
        feedbackText: "rgba(255,255,255,0.7)",
        micBg: "rgba(228,75,142,0.15)",
        micIcon: "#E44B8E",
        micActiveBg: "#E44B8E",
        micActiveIcon: "#FFFFFF"
      }
    },
    light: {
      isDark: false,
      bg: "#F0F0F5",
      surface: "#FFFFFF",
      textPrimary: "#1B1B2F",
      textSecondary: "rgba(0,0,0,0.55)",
      textMuted: "rgba(0,0,0,0.3)",
      border: "rgba(0,0,0,0.08)",
      navInactive: "rgba(0,0,0,0.45)",
      lockedBg: "#D0D0D8",
      lockedShadow: "#B0B0B8",
      lockedIcon: "rgba(0,0,0,0.25)",
      labelCompleted: "rgba(0,0,0,0.45)",
      labelCurrent: "#1B1B2F",
      labelLocked: "rgba(0,0,0,0.2)",
      floatBg: "#FFFFFF",
      floatBorder: "#D0D0D8",
      menuActive: "rgba(0,0,0,0.06)",
      menuShadow: "0 8px 24px rgba(0,0,0,0.12)",
      backdrop: "rgba(0,0,0,0.25)",
      coinsText: "#555555",
      glassBg: "rgba(240,240,245,0.97)",
      glassBorder: "rgba(0,0,0,0.06)",
      cardShadow: "#D0D0D8",
      separatorLine: "rgba(0,0,0,0.15)",
      separatorText: "rgba(0,0,0,0.35)",
      dialogBg: "#FFFFFF",
      dialogBorder: "rgba(0,0,0,0.06)",
      dialogTitle: "#1B1B2F",
      dialogText: "rgba(0,0,0,0.6)",
      dialogDimBg: "rgba(0,0,0,0.4)",
      onboarding: {
        bg: "#F0F0F5",
        text: "#1B1B2F",
        textSecondary: "rgba(0,0,0,0.55)",
        textMuted: "rgba(0,0,0,0.3)",
        optionBg: "#FFFFFF",
        optionBorder: "rgba(0,0,0,0.08)",
        optionSelectedBg: "#FFFFFF",
        optionSelectedBorder: "#E44B8E",
        optionShadow: "0 4px 0 #D0D0D8",
        optionSelectedShadow: "0 4px 0 #B83A72",
        optionDimmedBg: "#FFFFFF",
        optionDimmedBorder: "rgba(0,0,0,0.04)",
        optionDimmedShadow: "none",
        progressBarBg: "#FFFFFF",
        progressBarBorder: "rgba(0,0,0,0.06)",
        progressBarFill: "#E44B8E",
        backBg: "rgba(240,240,245,0.97)",
        backBorder: "rgba(0,0,0,0.06)",
        backIcon: "#1B1B2F",
        divider: "rgba(0,0,0,0.06)",
        ctaBg: "#E44B8E",
        ctaShadow: "0 4px 0 #B83A72",
        ctaDisabledBg: "#FFFFFF",
        ctaDisabledBorder: "rgba(0,0,0,0.08)",
        ctaDisabledShadow: "0 4px 0 #D0D0D8",
        ctaDisabledText: "rgba(0,0,0,0.25)",
        ctaText: "#FFFFFF",
        sliderFilled: "#E44B8E",
        sliderUnfilled: "rgba(0,0,0,0.08)",
        dotEmpty: "#FFFFFF",
        dotEmptyBorder: "rgba(0,0,0,0.08)",
        dotEmptyShadow: "0 3px 0 #D0D0D8",
        dotFilledShadow: "0 3px 0 #B83A72",
        labelFilled: "rgba(0,0,0,0.6)",
        labelUnfilled: "rgba(0,0,0,0.3)",
        inputBg: "#FFFFFF",
        inputBorder: "rgba(0,0,0,0.08)",
        inputFocusBorder: "#E44B8E",
        inputPlaceholder: "rgba(0,0,0,0.3)",
        charCount: "rgba(0,0,0,0.2)",
        refreshIcon: "#E44B8E"
      },
      simulator: {
        landingBg: "#FFFFFF",
        landingBorder: "rgba(0,0,0,0.06)",
        landingTitle: "#1B1B2F",
        landingSubtitle: "rgba(0,0,0,0.55)",
        landingHint: "rgba(0,0,0,0.35)",
        coinCost: "rgba(0,0,0,0.3)",
        sessionBg: "#F0F0F5",
        statusText: "#1B1B2F",
        statusSubtext: "rgba(0,0,0,0.4)",
        waveformLine: "#E44B8E",
        waveformBg: "transparent",
        micBg: "rgba(0,0,0,0.04)",
        micBorder: "rgba(0,0,0,0.12)",
        micIcon: "#1B1B2F",
        micActiveBg: "#E44B8E",
        micActiveBorder: "#B83A72",
        micActiveIcon: "#FFFFFF",
        micStopBg: "rgba(228,75,142,0.1)",
        micStopIcon: "#E44B8E",
        transcriptBg: "rgba(0,0,0,0.03)",
        transcriptBorder: "rgba(0,0,0,0.06)",
        transcriptText: "#1B1B2F",
        dotActive: "#E44B8E",
        dotInactive: "rgba(0,0,0,0.08)",
        dotBorder: "rgba(0,0,0,0.12)",
        transitionText: "#1B1B2F",
        transitionSubtext: "rgba(0,0,0,0.4)",
        resultsBg: "#FFFFFF",
        resultsBorder: "rgba(0,0,0,0.06)",
        resultsTitle: "#1B1B2F",
        scoreFraction: "rgba(0,0,0,0.4)",
        metricUp: "#58CC02",
        metricDown: "#FF4B4B",
        metricNeutral: "rgba(0,0,0,0.3)",
        metricText: "#1B1B2F",
        sectionTitle: "#58CC02",
        sectionTitleImprove: "#FF9600",
        bulletText: "rgba(0,0,0,0.6)",
        readMoreText: "#E44B8E",
        dialogBg: "#FFFFFF",
        dialogBorder: "rgba(0,0,0,0.06)",
        dialogTitle: "#1B1B2F",
        dialogText: "rgba(0,0,0,0.6)",
        dialogDimBg: "rgba(0,0,0,0.4)",
        facePhase1Fill: "#6D6987",
        facePhase1Opacity: 0.7,
        facePartnerFill: "#E1327F",
        facePartnerOpacity: 0.6,
        faceUserFill: "#6D6987",
        faceUserOpacity: 0.55,
        glowPartner: "radial-gradient(ellipse at left center, rgba(225,50,127,0.4) 0%, transparent 70%)",
        glowUser: "radial-gradient(ellipse at right center, rgba(109,105,135,0.4) 0%, transparent 70%)",
        micWaitingGlow: "drop-shadow(0 0 4px rgba(228,75,142,0.5)) drop-shadow(0 0 8px rgba(228,75,142,0.2))",
        micRecordingBg: "rgba(0,0,0,0.06)"
      },
      insight: {
        btnYesBg: "#58CC02",
        btnYesShadow: "#46A302",
        btnNoBg: "#FF4B4B",
        btnNoShadow: "#CC3333",
        btnOtherBg: "rgba(255,255,255,0.15)",
        btnOtherBorder: "rgba(255,255,255,0.3)",
        progressFill: "#E44B8E",
        progressBg: "rgba(255,255,255,0.2)",
        navBg: "rgba(0,0,0,0.3)",
        navIcon: "#FFFFFF",
        loaderBg: "#F0F0F5",
        loaderText: "#1B1B2F",
        loaderSpinner: "#E44B8E",
        controlBg: "rgba(255,255,255,0.15)",
        controlIcon: "#FFFFFF",
        subtitleText: "#FFFFFF",
        subtitleBg: "rgba(0,0,0,0.5)",
        continueBg: "#58CC02",
        continueShadow: "#46A302",
        feedbackBg: "rgba(255,255,255,0.1)",
        feedbackText: "rgba(255,255,255,0.7)",
        micBg: "rgba(228,75,142,0.15)",
        micIcon: "#E44B8E",
        micActiveBg: "#E44B8E",
        micActiveIcon: "#FFFFFF"
      }
    }
  };
  var DARK_MODE_OPTIONS = ["system", "on", "off"];
  var DARK_MODE_LABELS = { system: "System", on: "On", off: "Off" };
  var getSystemTheme = () => window.matchMedia && window.matchMedia("(prefers-color-scheme: dark)").matches ? "dark" : "light";

  // src/utils/realtime.js
  var RT = {};
  var EVENT_TYPES = {
    OUTPUT_AUDIO_STARTED: "output_audio_buffer.started",
    OUTPUT_AUDIO_STOPPED: "output_audio_buffer.stopped",
    RESPONSE_DONE: "response.done",
    RESPONSE_CANCEL: "response.cancel",
    OUTPUT_AUDIO_TRANSCRIPT_DELTA: "response.output_audio_transcript.delta",
    OUTPUT_AUDIO_TRANSCRIPT_DONE: "response.output_audio_transcript.done",
    OUTPUT_TEXT_DELTA: "response.output_text.delta",
    OUTPUT_TEXT_DONE: "response.output_text.done",
    INPUT_AUDIO_TRANSCRIPTION_COMPLETED: "conversation.item.input_audio_transcription.completed",
    CONVERSATION_ITEM_ADDED: "conversation.item.added",
    SESSION_UPDATED: "session.updated",
    SESSION_UPDATE_DONE: "session.update.done"
  };
  var JSON_TYPES = {
    ISSUE: "issue",
    EVALUATION: "evaluation"
  };
  var VALID_STAGES = [1, 2];
  function getUserName() {
    try {
      const raw = localStorage.getItem("bonds_user_data");
      if (raw) {
        const u3 = JSON.parse(raw);
        if (u3.name) return u3.name;
      }
    } catch {
    }
    return "User";
  }
  var TEMPLATES_KEY = "bonds_simulator_templates";
  function getTemplate(name5) {
    try {
      const raw = localStorage.getItem(TEMPLATES_KEY);
      if (raw) {
        const t3 = JSON.parse(raw);
        if (t3[name5]) return t3[name5];
      }
    } catch {
    }
    return null;
  }
  RT.config = {
    AZURE_RESOURCE: "daphn-m8o8ki4f-eastus2",
    WEBRTC_URL: null,
    API_KEY: null,
    CLIENT_SECRETS_URL: null,
    TRANSCRIPTION_MODEL: "whisper-1",
    // Voice settings
    VOICE_STAGE1: "echo",
    VOICE_STAGE2: "marin",
    // VAD settings
    VAD_THRESHOLD: 0.5,
    VAD_PREFIX_PADDING_MS: 300,
    VAD_SILENCE_DURATION_MS: 1200,
    VAD_CREATE_RESPONSE: true,
    // Trigger phrase
    TRIGGER_PHRASE: "ok, beginning simulation",
    // Timeouts
    TIMEOUT_FIRST_RESPONSE: 1e3,
    STAGE4_CLOSE_TIMEOUT: 2e3
  };
  RT.state = {
    // Connection
    connection: {
      pc: null,
      dc: null,
      micStream: null,
      micTracks: [],
      audioEl: null,
      connNum: 1
    },
    // Conversation
    conversation: {
      messages: [],
      assistantLines: [],
      currentAssistantLine: "",
      userLines: []
    },
    // Stages
    stages: {
      current: null,
      simulation: {
        mode: false,
        triggered: false,
        partnerTurnCount: 0,
        userTurnCount: 0,
        startIndex: -1
      }
    },
    // JSON requests
    json: {
      waiting: false,
      buffer: "",
      type: null,
      evaluationData: null
    },
    // Other
    assistantSpeaking: false,
    evaluationRequested: false,
    currentIssue: "",
    currentIssueData: null,
    // Store full issue JSON data for retry
    // Push-to-talk
    pushToTalk: {
      isActive: false,
      canActivate: false
    },
    // Cleanup guard
    cleaningUp: false
  };
  var _listeners = /* @__PURE__ */ new Set();
  RT.emit = function(type, data) {
    const evt = { type, ts: Date.now(), data: data || {} };
    _listeners.forEach((fn2) => {
      try {
        fn2(evt);
      } catch (e3) {
        console.error("[RT] Listener error:", e3);
      }
    });
  };
  function onRTEvent(callback) {
    _listeners.add(callback);
  }
  function offRTEvent(callback) {
    _listeners.delete(callback);
  }
  RT.emitWarning = function(message, code) {
    RT.emit("warning", {
      message,
      code: code || "WARNING"
    });
  };
  function ts() {
    return (/* @__PURE__ */ new Date()).toLocaleTimeString();
  }
  function safeSend(message) {
    const state2 = RT.state;
    if (!state2.connection.dc) {
      RT.emit("error", {
        message: "Data channel is null",
        code: "DATA_CHANNEL_NULL"
      });
      return false;
    }
    if (state2.connection.dc.readyState !== "open") {
      RT.emit("error", {
        message: `Data channel state is '${state2.connection.dc.readyState}' (expected 'open')`,
        code: "DATA_CHANNEL_NOT_READY"
      });
      stopSession("error");
      return false;
    }
    try {
      const messageStr = typeof message === "string" ? message : JSON.stringify(message);
      state2.connection.dc.send(messageStr);
      return true;
    } catch (e3) {
      RT.emit("error", {
        message: e3.message,
        code: "DATA_CHANNEL_SEND_ERROR"
      });
      stopSession("error");
      return false;
    }
  }
  function getState() {
    return RT.state;
  }
  function getConfig() {
    return RT.config;
  }
  function getDisplayTurnCounts(eventType) {
    const state2 = getState();
    const sim = state2.stages.simulation;
    if (eventType === "PARTNER_SPEAKING") {
      return {
        partnerTurnCount: sim.partnerTurnCount + 1,
        // Partner's current turn
        userTurnCount: sim.userTurnCount
        // User's completed turns (may lag by 1 if transcription delayed)
      };
    } else if (eventType === "PUSH_TO_TALK_READY") {
      return {
        partnerTurnCount: sim.partnerTurnCount,
        // Partner's completed turns
        userTurnCount: sim.userTurnCount + 1
        // User's upcoming turn
      };
    }
    return {
      partnerTurnCount: sim.partnerTurnCount,
      userTurnCount: sim.userTurnCount
    };
  }
  function isSimulationTriggerPhrase(text) {
    if (!text) return false;
    const norm = text.toLowerCase();
    if (!norm.includes("simulation")) return false;
    const hasOk = norm.includes("ok,") || norm.includes("ok ") || norm.includes("okay");
    if (!hasOk) return false;
    if (!norm.includes("begin")) return false;
    return true;
  }
  RT.setApiKey = function(apiKey) {
    if (!apiKey) {
      RT.emit("error", {
        message: "API key cannot be empty"
      });
      return;
    }
    RT.config.API_KEY = apiKey;
  };
  RT.setClientSecretsUrl = function(url) {
    if (!url) {
      RT.emit("error", {
        message: "Client secrets URL cannot be empty"
      });
      return;
    }
    RT.config.CLIENT_SECRETS_URL = url;
  };
  RT.setTriggerPhrase = function(phrase) {
    RT.config.TRIGGER_PHRASE = phrase || "ok, beginning simulation";
  };
  RT.setVoice = function(stage, voice) {
    if (!VALID_STAGES.includes(stage)) {
      RT.emit("error", {
        message: `Invalid stage: ${stage}. Valid stages: ${VALID_STAGES.join(
          ", "
        )}`
      });
      return;
    }
    if (!voice) {
      RT.emit("error", {
        message: "Voice cannot be empty"
      });
      return;
    }
    if (stage === 1) RT.config.VOICE_STAGE1 = voice;
    else if (stage === 2) RT.config.VOICE_STAGE2 = voice;
  };
  RT.setVadConfig = function(config) {
    if (config.threshold !== void 0) {
      if (config.threshold < 0 || config.threshold > 1) {
        RT.emit("error", {
          message: "VAD threshold must be between 0 and 1"
        });
        return;
      }
      RT.config.VAD_THRESHOLD = config.threshold;
    }
    if (config.prefixPaddingMs !== void 0)
      RT.config.VAD_PREFIX_PADDING_MS = config.prefixPaddingMs;
    if (config.silenceDurationMs !== void 0)
      RT.config.VAD_SILENCE_DURATION_MS = config.silenceDurationMs;
    if (config.createResponse !== void 0)
      RT.config.VAD_CREATE_RESPONSE = config.createResponse;
  };
  function getInstructionsFromBubble(type, context) {
    if (type === "issue_json") {
      const tpl = getTemplate("simulationJsonInstructions");
      if (tpl) {
        return tpl.replace(/\[USERNAME\]/g, getUserName());
      }
      RT.emit("error", { message: "simulationJsonInstructions not cached" });
      return null;
    }
    if (type === "evaluation_json") {
      const tpl = getTemplate("scoreInstructions");
      if (tpl) {
        let instructions = tpl.replace(
          /\[ISSUE\]/g,
          context.issue || "the main conflict in the relationship"
        );
        instructions += "\n\n" + (context.conversationHistory || "");
        return instructions;
      }
      RT.emit("error", { message: "scoreInstructions not cached" });
      return null;
    }
    RT.emit("error", {
      message: `Unknown instruction type: ${type}`
    });
    return null;
  }
  function formatStage2Conversation() {
    const state2 = getState();
    const startIdx = state2.stages.simulation.startIndex >= 0 ? state2.stages.simulation.startIndex : 0;
    const stage2Messages = state2.conversation.messages.slice(startIdx);
    return stage2Messages.map((msg) => {
      const role = msg.type === "user" ? "User" : "Assistant";
      return `${role}: ${msg.text}`;
    }).join("\n");
  }
  function handleSessionUpdated(_evt) {
  }
  function setMicEnabled(on3) {
    const state2 = getState();
    if (!state2.connection.micTracks) {
      return;
    }
    state2.connection.micTracks.forEach((t3) => {
      t3.enabled = on3;
    });
  }
  RT.startPushToTalk = function() {
    const state2 = getState();
    if (state2.pushToTalk.isActive) {
      return false;
    }
    if (!state2.pushToTalk.canActivate) {
      RT.emitWarning(
        "Push-to-talk is not available at this time",
        "PUSH_TO_TALK_NOT_AVAILABLE"
      );
      return false;
    }
    if (state2.assistantSpeaking) {
      RT.emitWarning(
        "Cannot start push-to-talk while assistant is speaking",
        "ASSISTANT_SPEAKING"
      );
      return false;
    }
    if (!state2.connection.dc || state2.connection.dc.readyState !== "open") {
      RT.emit("error", {
        message: "Data channel is not ready",
        code: "DATA_CHANNEL_NOT_READY"
      });
      stopSession("error");
      return false;
    }
    setMicEnabled(true);
    state2.pushToTalk.isActive = true;
    RT.emit("state", { state: "PUSH_TO_TALK_ACTIVE" });
    RT.emit("user_speaking", { text: "..." });
    return true;
  };
  RT.stopPushToTalk = function() {
    const state2 = getState();
    if (!state2.pushToTalk.isActive) {
      return false;
    }
    state2.pushToTalk.isActive = false;
    setMicEnabled(false);
    if (state2.connection.dc && state2.connection.dc.readyState === "open") {
      safeSend({ type: "input_audio_buffer.commit" });
      if (state2.stages.simulation.mode && !state2.evaluationRequested) {
        state2.stages.simulation.userTurnCount++;
        if (state2.stages.simulation.userTurnCount === 2) {
          state2.evaluationRequested = true;
          if (state2.connection.audioEl && !state2.connection.audioEl.muted) {
            state2.connection.audioEl.muted = true;
          }
          RT.emit("state", { state: "PUSH_TO_TALK_STOPPED" });
          setTimeout(() => {
            requestEvaluationJson();
          }, 100);
        } else {
          safeSend({ type: "response.create" });
          RT.emit("state", { state: "PUSH_TO_TALK_STOPPED" });
        }
      } else {
        safeSend({ type: "response.create" });
        RT.emit("state", { state: "PUSH_TO_TALK_STOPPED" });
      }
    } else {
      RT.emit("error", {
        message: "Cannot send audio commit/response: data channel not ready",
        code: "DATA_CHANNEL_NOT_READY"
      });
      stopSession("error");
    }
    return true;
  };
  function interruptAssistant() {
    const state2 = getState();
    if (state2.assistantSpeaking) {
      safeSend({ type: EVENT_TYPES.RESPONSE_CANCEL });
      safeSend({ type: "output_audio_buffer.clear" });
    }
    if (state2.connection.audioEl && !state2.connection.audioEl.paused) {
      state2.connection.audioEl.pause();
      state2.connection.audioEl.currentTime = 0;
    }
  }
  function requestSilentSimulationJson() {
    const state2 = getState();
    if (!state2.connection.dc || state2.connection.dc.readyState !== "open") {
      RT.emit("error", {
        message: "Cannot request issue JSON: data channel not ready",
        code: "DATA_CHANNEL_NOT_READY"
      });
      stopSession("error");
      return;
    }
    state2.json.waiting = true;
    state2.json.buffer = "";
    state2.json.type = JSON_TYPES.ISSUE;
    RT.emit("state", { state: "LOADING_SIMULATION" });
    setTimeout(() => {
      if (state2.connection.audioEl) {
        state2.connection.audioEl.muted = true;
      }
    }, 100);
    const instructions = getInstructionsFromBubble("issue_json", {});
    if (!instructions) {
      RT.emit("error", {
        message: "Failed to get issue JSON instructions"
      });
      state2.json.waiting = false;
      state2.json.type = null;
      return;
    }
    safeSend({
      type: "response.create",
      response: {
        conversation: "none",
        output_modalities: ["text"],
        instructions
      }
    });
  }
  function requestEvaluationJson() {
    const state2 = getState();
    if (!state2.connection.dc || state2.connection.dc.readyState !== "open") {
      RT.emit("error", {
        message: "Cannot request evaluation JSON: data channel not ready",
        code: "DATA_CHANNEL_NOT_READY"
      });
      stopSession("error");
      return;
    }
    if (state2.connection.audioEl && !state2.connection.audioEl.muted) {
      state2.connection.audioEl.muted = true;
    }
    state2.json.waiting = true;
    state2.json.buffer = "";
    state2.json.type = JSON_TYPES.EVALUATION;
    const stage2Conversation = formatStage2Conversation();
    const instructions = getInstructionsFromBubble("evaluation_json", {
      issue: state2.currentIssue,
      conversationHistory: stage2Conversation
    });
    if (!instructions) {
      RT.emit("error", {
        message: "Failed to get evaluation JSON instructions"
      });
      state2.json.waiting = false;
      state2.json.type = null;
      return;
    }
    safeSend({
      type: "response.create",
      response: {
        conversation: "none",
        output_modalities: ["text"],
        instructions
      }
    });
  }
  async function startPartnerSimulation(data) {
    const state2 = getState();
    state2.currentIssue = data.issue || state2.currentIssue;
    state2.stages.simulation.mode = true;
    state2.stages.simulation.partnerTurnCount = 0;
    state2.stages.simulation.userTurnCount = 0;
    state2.stages.simulation.startIndex = state2.conversation.messages.length;
    await stopSession("transition");
    const jsonContext = JSON.stringify(data, null, 2);
    RT.emit("stage2_token_needed", {
      issue: state2.currentIssue,
      jsonContext
    });
  }
  async function retrySimulation() {
    const state2 = getState();
    if (!state2.currentIssueData && !state2.currentIssue) {
      RT.emit("error", {
        message: "Cannot retry simulation: no issue data available. Start from Stage 1 first."
      });
      return false;
    }
    state2.json.evaluationData = null;
    state2.json.waiting = false;
    state2.json.buffer = "";
    state2.json.type = null;
    state2.evaluationRequested = false;
    state2.stages.simulation.mode = true;
    state2.stages.simulation.triggered = false;
    state2.stages.simulation.partnerTurnCount = 0;
    state2.stages.simulation.userTurnCount = 0;
    state2.stages.simulation.startIndex = 0;
    state2.conversation.messages = [];
    state2.conversation.assistantLines = [];
    state2.conversation.currentAssistantLine = "";
    state2.conversation.userLines = [];
    state2.assistantSpeaking = false;
    state2.pushToTalk.isActive = false;
    state2.pushToTalk.canActivate = false;
    await stopSession("transition");
    return true;
  }
  function handleStreamingDelta(evt) {
    const state2 = getState();
    const chunk = evt.delta || evt.transcript || "";
    if (state2.json.waiting) {
      if (evt.type === EVENT_TYPES.OUTPUT_TEXT_DELTA || evt.type === "response.text.delta" || evt.type === "response.output_text.delta") {
        state2.json.buffer += chunk;
      }
      return;
    }
    state2.conversation.currentAssistantLine += chunk;
    const deltaType = state2.stages.simulation.mode ? "partner_text_delta" : "coach_text_delta";
    RT.emit(deltaType, { text: state2.conversation.currentAssistantLine, isFinal: false, durationMs: evt.duration_ms });
    if (!state2.json.waiting && !state2.stages.simulation.triggered && isSimulationTriggerPhrase(state2.conversation.currentAssistantLine)) {
      state2.stages.simulation.triggered = true;
    }
  }
  function handleFinalText(evt) {
    const state2 = getState();
    if (state2.json.waiting) {
      if (evt.type === EVENT_TYPES.OUTPUT_TEXT_DONE || evt.type === "response.text.done" || evt.type === "response.output_text.done") {
        const finalText2 = evt.text || state2.json.buffer || "";
        state2.json.waiting = false;
        state2.json.buffer = "";
        if (state2.connection.audioEl) {
          state2.connection.audioEl.muted = false;
        }
        try {
          let jsonText = finalText2.trim();
          const jsonMatch = jsonText.match(/```(?:json)?\s*([\s\S]*?)\s*```/) || jsonText.match(/\{[\s\S]*\}/);
          if (jsonMatch) {
            jsonText = jsonMatch[1] || jsonMatch[0];
          }
          const data = JSON.parse(jsonText);
          if (state2.json.type === JSON_TYPES.ISSUE) {
            state2.currentIssue = data.issue || state2.currentIssue;
            state2.currentIssueData = data;
            startPartnerSimulation(data);
          } else if (state2.json.type === JSON_TYPES.EVALUATION) {
            state2.json.evaluationData = data;
            RT.emit("evaluation_json_received", {
              evaluationJson: data
            });
            setTimeout(() => {
              RT.emit("state", { state: "SESSION_COMPLETED" });
              stopSession("completed");
            }, 500);
          }
        } catch {
        }
        state2.json.type = null;
        state2.conversation.currentAssistantLine = "";
        return;
      }
      return;
    }
    const finalText = evt.text || evt.transcript || state2.conversation.currentAssistantLine || "";
    if (!finalText) return;
    if (!state2.json.waiting && !state2.stages.simulation.triggered && isSimulationTriggerPhrase(finalText)) {
      state2.stages.simulation.triggered = true;
      return;
    }
    if (state2.stages.simulation.mode && !state2.evaluationRequested) {
      if (state2.stages.simulation.userTurnCount >= 2) {
        RT.emit("state", {
          state: "PARTNER_RESPONSE_BLOCKED",
          reason: "user_has_2_turns"
        });
        interruptAssistant();
        state2.conversation.currentAssistantLine = "";
        return;
      }
      state2.stages.simulation.partnerTurnCount++;
      if (state2.stages.simulation.partnerTurnCount === 2 && state2.stages.simulation.userTurnCount === 0) {
        state2.stages.simulation.userTurnCount = 1;
      }
    }
    state2.conversation.assistantLines.push(
      "[" + ts() + "] Assistant: " + finalText
    );
    state2.conversation.currentAssistantLine = "";
    state2.conversation.messages.push({
      type: "assistant",
      text: finalText,
      timestamp: ts()
    });
    if (state2.stages.simulation.mode) {
      RT.emit("partner_text", { text: finalText });
    } else {
      RT.emit("coach_text", { text: finalText });
    }
  }
  function handleUserTranscription(evt) {
    const state2 = getState();
    const text = evt.transcript || "";
    if (!text) return;
    const lowerText = text.toLowerCase().trim();
    const hallucinations = ["you.", "you", "you you", "you you you", "thank you.", "thank you"];
    if (hallucinations.includes(lowerText)) {
      console.warn("[RT] Filtered potential STT hallucination:", text);
      return;
    }
    state2.conversation.userLines.push("[" + ts() + "] User: " + text);
    state2.conversation.messages.push({
      type: "user",
      text,
      timestamp: ts()
    });
    RT.emit("user_transcript", { text });
  }
  function handleAudioStarted(_evt) {
    const state2 = getState();
    if (state2.stages.simulation.mode && !state2.evaluationRequested && (state2.stages.simulation.userTurnCount >= 2 || state2.stages.simulation.partnerTurnCount >= 2)) {
      if (state2.connection.audioEl && !state2.connection.audioEl.muted) {
        state2.connection.audioEl.muted = true;
      }
      interruptAssistant();
      return;
    }
    state2.assistantSpeaking = true;
    setMicEnabled(false);
    if (state2.stages.simulation.mode) {
      if (!state2.evaluationRequested) {
        const counts = getDisplayTurnCounts("PARTNER_SPEAKING");
        RT.emit("state", {
          state: "PARTNER_SPEAKING",
          partnerTurnCount: counts.partnerTurnCount,
          userTurnCount: counts.userTurnCount
        });
      }
    } else {
      RT.emit("state", { state: "COACH_SPEAKING" });
    }
  }
  function handleAudioStopped(evt) {
    const state2 = getState();
    state2.assistantSpeaking = false;
    if (state2.stages.simulation.mode && !state2.evaluationRequested && state2.stages.simulation.partnerTurnCount === 2 && state2.stages.simulation.userTurnCount === 1 && evt.type === EVENT_TYPES.RESPONSE_DONE) {
      setTimeout(() => {
        if (state2.connection.audioEl && !state2.connection.audioEl.muted) {
          state2.connection.audioEl.muted = true;
        }
      }, 500);
    }
    const userCompleted2Turns = state2.stages.simulation.userTurnCount >= 2;
    if (state2.json.waiting && state2.json.type === JSON_TYPES.EVALUATION || state2.stages.simulation.mode && userCompleted2Turns || userCompleted2Turns || // Keep push-to-talk disabled if user completed 2 turns, regardless of mode
    // Also block between Stage 1 -> 2 transition once trigger phrase fired;
    // we immediately go into LOADING_SIMULATION and don't want another user turn.
    state2.stages.simulation.triggered && !state2.stages.simulation.mode) {
      state2.pushToTalk.canActivate = false;
    } else if (!state2.json.waiting || state2.json.type !== JSON_TYPES.EVALUATION) {
      state2.pushToTalk.canActivate = true;
      if (state2.stages.simulation.mode && !state2.evaluationRequested) {
        const counts = getDisplayTurnCounts("PUSH_TO_TALK_READY");
        RT.emit("state", {
          state: "PUSH_TO_TALK_READY",
          partnerTurnCount: counts.partnerTurnCount,
          userTurnCount: counts.userTurnCount
        });
      } else {
        RT.emit("state", { state: "PUSH_TO_TALK_READY" });
      }
    }
    if (state2.stages.simulation.triggered && !state2.json.waiting && !state2.stages.simulation.mode) {
      requestSilentSimulationJson();
      return;
    }
    if (state2.stages.simulation.mode) {
    } else if (state2.json.waiting) {
    }
  }
  function handleError(evt) {
    RT.emit("error", {
      message: "SERVER_ERROR",
      code: "SERVER_ERROR",
      raw: evt
    });
    stopSession("error");
  }
  var EVENT_HANDLERS = {
    [EVENT_TYPES.OUTPUT_AUDIO_STARTED]: handleAudioStarted,
    [EVENT_TYPES.OUTPUT_AUDIO_STOPPED]: handleAudioStopped,
    [EVENT_TYPES.RESPONSE_DONE]: handleAudioStopped,
    [EVENT_TYPES.OUTPUT_AUDIO_TRANSCRIPT_DELTA]: handleStreamingDelta,
    ["response.audio_transcript.delta"]: handleStreamingDelta,
    [EVENT_TYPES.OUTPUT_TEXT_DELTA]: handleStreamingDelta,
    ["response.text.delta"]: handleStreamingDelta,
    ["response.output_text.delta"]: handleStreamingDelta,
    [EVENT_TYPES.OUTPUT_AUDIO_TRANSCRIPT_DONE]: handleFinalText,
    ["response.audio_transcript.done"]: handleFinalText,
    [EVENT_TYPES.OUTPUT_TEXT_DONE]: handleFinalText,
    ["response.text.done"]: handleFinalText,
    ["response.output_text.done"]: handleFinalText,
    [EVENT_TYPES.INPUT_AUDIO_TRANSCRIPTION_COMPLETED]: handleUserTranscription,
    [EVENT_TYPES.SESSION_UPDATED]: handleSessionUpdated,
    [EVENT_TYPES.SESSION_UPDATE_DONE]: handleSessionUpdated,
    error: handleError,
    "error.occurred": handleError
  };
  function handleDataChannelMessage(e3) {
    const state2 = getState();
    if (!state2.connection.dc || state2.connection.dc.readyState !== "open") {
      return;
    }
    try {
      const evt = JSON.parse(e3.data);
      const handler = EVENT_HANDLERS[evt.type];
      if (handler) {
        handler(evt);
      }
    } catch (err) {
      if (err instanceof SyntaxError) {
        RT.emit("error", {
          message: `JSON parse error: ${err.message}`,
          code: "JSON_PARSE_ERROR"
        });
      } else {
        RT.emit("error", {
          message: `Error parsing event: ${err.message}`,
          code: "EVENT_PARSE_ERROR"
        });
      }
      stopSession("error");
    }
  }
  function createAudioElement() {
    const existingEl = document.getElementById("rtAudio");
    if (existingEl) {
      try {
        if (existingEl.srcObject) {
          const stream = existingEl.srcObject;
          if (stream && typeof stream.getTracks === "function") {
            stream.getTracks().forEach((track) => {
              try {
                track.stop();
              } catch {
              }
            });
          }
          existingEl.srcObject = null;
        }
        existingEl.pause();
        existingEl.load();
        if (existingEl.parentNode) {
          existingEl.remove();
        }
      } catch {
      }
    }
    const audioEl = document.createElement("audio");
    audioEl.id = "rtAudio";
    audioEl.autoplay = true;
    audioEl.setAttribute("playsinline", "true");
    audioEl.setAttribute("webkit-playsinline", "true");
    audioEl.muted = false;
    audioEl.volume = 1;
    audioEl.currentTime = 0;
    document.body.appendChild(audioEl);
    return audioEl;
  }
  async function setupMicrophone() {
    const state2 = getState();
    if (state2.connection.micStream || state2.connection.micTracks && state2.connection.micTracks.length > 0) {
      try {
        const tracksToStop = state2.connection.micTracks || (state2.connection.micStream ? state2.connection.micStream.getTracks() : []);
        tracksToStop.forEach((track) => {
          try {
            track.onended = null;
            track.onmute = null;
            track.onunmute = null;
            track.stop();
          } catch {
          }
        });
      } catch {
      }
      state2.connection.micStream = null;
      state2.connection.micTracks = [];
    }
    if (navigator.permissions && navigator.permissions.query) {
      try {
        const permissionStatus = await navigator.permissions.query({ name: "microphone" });
        if (permissionStatus.state === "denied") {
          const error = new Error("Microphone permission denied");
          error.name = "NotAllowedError";
          throw error;
        }
      } catch {
      }
    }
    try {
      state2.connection.micStream = await navigator.mediaDevices.getUserMedia({
        audio: {
          echoCancellation: true,
          noiseSuppression: true,
          autoGainControl: true
        }
      });
      state2.connection.micTracks = state2.connection.micStream.getTracks();
      if (!state2.connection.micTracks || state2.connection.micTracks.length === 0) {
        throw new Error("No audio tracks returned from getUserMedia");
      }
      state2.connection.micTracks.forEach((track) => {
        track.onended = () => {
          const currentState = getState();
          if (currentState.cleaningUp) {
            return;
          }
          if (currentState.assistantSpeaking) {
            interruptAssistant();
          }
          RT.emit("error", {
            message: "MICROPHONE_TRACK_ENDED",
            code: "TRACK_ENDED",
            userMessage: "Microphone access was revoked or disconnected. Please check your microphone permissions."
          });
          stopSession("error");
        };
      });
      return state2.connection.micStream;
    } catch (err) {
      if (state2.connection.micStream) {
        state2.connection.micStream.getTracks().forEach((t3) => t3.stop());
      }
      state2.connection.micStream = null;
      state2.connection.micTracks = [];
      throw err;
    }
  }
  function setupDataChannel(pc) {
    const state2 = getState();
    const config = getConfig();
    const dc = pc.createDataChannel("oai-events");
    state2.connection.dc = dc;
    dc.onopen = () => {
      if (!state2.stages.simulation.mode) {
        safeSend({
          type: "conversation.item.create",
          item: {
            type: "message",
            role: "user",
            content: [{ type: "input_text", text: "Hi!" }]
          }
        });
        safeSend({ type: "response.create" });
      } else if (state2.stages.simulation.mode) {
        setTimeout(() => {
          safeSend({ type: "response.create" });
        }, config.TIMEOUT_FIRST_RESPONSE);
      }
    };
    dc.onclose = () => {
    };
    dc.onerror = () => {
    };
    dc.onmessage = handleDataChannelMessage;
    return dc;
  }
  function setupConnectionHandlers(pc) {
    const state2 = getState();
    pc.onconnectionstatechange = function() {
      const connState = pc.connectionState;
      if (connState === "failed" || connState === "disconnected") {
        RT.emit("error", {
          message: `Connection ${connState}`,
          code: "CONNECTION_FAILED"
        });
        if (state2.connection.pc === pc) {
          stopSession("error");
        }
      }
    };
    pc.oniceconnectionstatechange = function() {
      const iceState = pc.iceConnectionState;
      if (iceState === "failed" || iceState === "disconnected" || iceState === "closed") {
        RT.emit("error", {
          message: `ICE ${iceState}`,
          code: iceState === "failed" ? "ICE_CONNECTION_FAILED" : "ICE_CONNECTION_STATE_CHANGE"
        });
        if (state2.connection.pc === pc && iceState === "failed") {
          stopSession("error");
        }
      }
    };
    pc.onicegatheringstatechange = function() {
    };
    pc.onsignalingstatechange = function() {
    };
  }
  async function performSdpExchange(ephemeralToken, offer) {
    const config = getConfig();
    const WEBRTC_URL = `https://${config.AZURE_RESOURCE}.openai.azure.com/openai/v1/realtime/calls?webrtcfilter=on`;
    const sdpRes = await fetch(WEBRTC_URL, {
      method: "POST",
      headers: {
        Authorization: "Bearer " + ephemeralToken,
        "Content-Type": "application/sdp"
      },
      body: offer.sdp
    });
    if (!sdpRes.ok) {
      const errorText = await sdpRes.text().catch(() => "Unknown error");
      throw new Error(`SDP exchange failed: ${sdpRes.status} - ${errorText}`);
    }
    const answerSdp = await sdpRes.text();
    return answerSdp;
  }
  function resetSessionState() {
    const state2 = getState();
    if (!state2.stages.simulation.mode) {
      state2.conversation.messages = [];
    }
    state2.conversation.assistantLines = [];
    state2.conversation.currentAssistantLine = "";
    state2.conversation.userLines = [];
    state2.json.buffer = "";
    state2.assistantSpeaking = false;
    state2.stages.simulation.triggered = false;
    state2.json.waiting = false;
    state2.json.type = null;
    if (!state2.stages.simulation.mode) {
      state2.evaluationRequested = false;
    }
    if (!state2.stages.simulation.mode) {
      state2.currentIssue = "";
      state2.currentIssueData = null;
      state2.stages.simulation.partnerTurnCount = 0;
      state2.stages.simulation.userTurnCount = 0;
      state2.stages.simulation.startIndex = -1;
    }
    state2.pushToTalk.isActive = false;
    state2.pushToTalk.canActivate = false;
    state2.pushToTalk.startTime = null;
    state2.cleaningUp = false;
  }
  function emitInitialState() {
    const state2 = getState();
    if (!state2.stages.simulation.mode) {
      RT.emit("state", { state: "SESSION_STARTED" });
    } else if (state2.stages.simulation.mode) {
      RT.emit("state", { state: "BEGIN_SIMULATION" });
    }
  }
  async function setupWebRTCConnection(ephemeralToken) {
    const state2 = getState();
    const pc = new RTCPeerConnection();
    state2.connection.pc = pc;
    state2.connection.connNum = state2.stages.simulation.mode ? 2 : 1;
    const audioEl = createAudioElement();
    state2.connection.audioEl = audioEl;
    pc.ontrack = (e3) => {
      if (e3.streams?.[0]) {
        try {
          const stream = e3.streams[0];
          const audioTracks = stream.getAudioTracks();
          if (audioTracks.length === 0) {
            RT.emitWarning(
              "Received stream has no audio tracks",
              "NO_AUDIO_TRACKS_IN_STREAM"
            );
            return;
          }
          const inactiveTracks = audioTracks.filter(
            (t3) => t3.readyState !== "live"
          );
          if (inactiveTracks.length > 0) {
            RT.emitWarning(
              `Some audio tracks are not live (states: ${inactiveTracks.map((t3) => t3.readyState).join(", ")})`,
              "INACTIVE_AUDIO_TRACKS"
            );
          }
          audioTracks.forEach((track) => {
            track.onended = () => {
            };
          });
          audioEl.srcObject = stream;
          try {
            const playPromise = audioEl.play();
            if (playPromise !== void 0) {
              playPromise.then(() => {
              }).catch(() => {
              });
            }
          } catch {
          }
        } catch (err) {
          RT.emit("error", {
            message: `Failed to set audio stream: ${err.message}`,
            code: "AUDIO_STREAM_SETUP_ERROR",
            rawError: err.message || String(err)
          });
        }
      }
    };
    const micStream = await setupMicrophone();
    if (!state2.connection.micTracks || state2.connection.micTracks.length === 0) {
      throw new Error(
        "No microphone tracks available to add to WebRTC connection"
      );
    }
    const invalidTracks = state2.connection.micTracks.filter(
      (t3) => t3.readyState !== "live"
    );
    if (invalidTracks.length > 0) {
      const states = invalidTracks.map((t3) => t3.readyState).join(", ");
      throw new Error(
        `Microphone tracks are not in 'live' state (states: ${states})`
      );
    }
    state2.connection.micTracks.forEach((t3) => {
      pc.addTrack(t3, micStream);
    });
    setMicEnabled(false);
    setupDataChannel(pc);
    setupConnectionHandlers(pc);
    const offer = await pc.createOffer();
    await pc.setLocalDescription(offer);
    const answerSdp = await performSdpExchange(ephemeralToken, offer);
    await pc.setRemoteDescription({ type: "answer", sdp: answerSdp });
  }
  async function stopSession(reason) {
    const state2 = getState();
    const stopReason = reason || "stopped";
    if (state2.cleaningUp) {
      return;
    }
    if (!state2.connection.pc && !state2.connection.dc && !state2.connection.micStream) {
      return;
    }
    state2.cleaningUp = true;
    try {
      if (state2.connection.dc) {
        try {
          state2.connection.dc.onopen = null;
          state2.connection.dc.onclose = null;
          state2.connection.dc.onmessage = null;
          state2.connection.dc.onerror = null;
          state2.connection.dc.close();
        } catch {
        }
        state2.connection.dc = null;
      }
      if (state2.connection.pc) {
        try {
          state2.connection.pc.onconnectionstatechange = null;
          state2.connection.pc.oniceconnectionstatechange = null;
          state2.connection.pc.onicegatheringstatechange = null;
          state2.connection.pc.onsignalingstatechange = null;
          state2.connection.pc.ontrack = null;
          state2.connection.pc.onnegotiationneeded = null;
          state2.connection.pc.ondatachannel = null;
          const senders = state2.connection.pc.getSenders();
          senders.forEach((sender) => {
            try {
              if (sender.track) {
                sender.track.stop();
              }
            } catch {
            }
          });
          try {
            const receivers = state2.connection.pc.getReceivers ? state2.connection.pc.getReceivers() : [];
            receivers.forEach((receiver) => {
              try {
                if (receiver.track) {
                  receiver.track.stop();
                }
              } catch {
              }
            });
          } catch {
          }
          state2.connection.pc.close();
        } catch {
        }
        state2.connection.pc = null;
      }
      if (state2.connection.micTracks) {
        state2.connection.micTracks.forEach((t3) => {
          try {
            t3.stop();
          } catch {
          }
        });
        state2.connection.micTracks = [];
      }
      state2.connection.micStream = null;
      if (state2.connection.audioEl) {
        try {
          const audioElToClean = state2.connection.audioEl;
          if (audioElToClean.srcObject) {
            const stream = audioElToClean.srcObject;
            if (stream && typeof stream.getTracks === "function") {
              stream.getTracks().forEach((track) => {
                try {
                  track.stop();
                } catch {
                }
              });
            }
          }
          audioElToClean.srcObject = null;
          audioElToClean.pause();
          audioElToClean.load();
          audioElToClean.currentTime = 0;
          if (audioElToClean.parentNode) {
            audioElToClean.remove();
          }
          const existingById = document.getElementById("rtAudio");
          if (existingById && existingById !== audioElToClean) {
            try {
              if (existingById.srcObject) {
                existingById.srcObject = null;
              }
              existingById.pause();
              existingById.load();
              if (existingById.parentNode) {
                existingById.remove();
              }
            } catch {
            }
          }
        } catch {
        }
        state2.connection.audioEl = null;
      }
      state2.pushToTalk.isActive = false;
      state2.pushToTalk.canActivate = false;
      state2.pushToTalk.startTime = null;
      if (stopReason === "transition") {
        return;
      }
      if (stopReason === "stopped") {
        resetSessionState();
        state2.connection.pc = null;
        state2.connection.dc = null;
        state2.connection.micStream = null;
        state2.connection.micTracks = [];
        state2.connection.audioEl = null;
        state2.connection.connNum = 1;
      }
      if (stopReason === "error") {
        RT.emit("state", { state: "SESSION_ERROR" });
      } else if (stopReason === "stopped") {
        RT.emit("state", { state: "SESSION_STOPPED" });
      } else if (stopReason === "completed") {
      }
    } catch (e3) {
      if (!state2.cleaningUp) {
        RT.emit("error", {
          message: e3.message || String(e3),
          code: "CLEANUP_ERROR"
        });
      }
    } finally {
      state2.cleaningUp = false;
    }
  }
  RT.preWarmDelay = async function(delayMs = 2e3) {
    const isFirstSession = !RT._firstSessionCompleted;
    if (isFirstSession) {
      RT._firstSessionCompleted = true;
      await new Promise((resolve2) => setTimeout(resolve2, delayMs));
    }
  };
  RT.start = async function(ephemeralToken) {
    if (!ephemeralToken || ephemeralToken === "__TOKEN__") {
      RT.emit("error", {
        message: "Ephemeral token is required. Provide a valid token.",
        code: "INVALID_TOKEN"
      });
      return;
    }
    if (document.readyState !== "complete") {
      await new Promise((resolve2) => {
        if (document.readyState === "complete") {
          resolve2();
        } else {
          window.addEventListener("load", resolve2, { once: true });
        }
      });
    }
    try {
      emitInitialState();
      resetSessionState();
      await setupWebRTCConnection(ephemeralToken);
    } catch (err) {
      let errorCode = "UNKNOWN_ERROR";
      let userMessage = err.message || String(err);
      if (err.name === "NotAllowedError" || err.name === "PermissionDeniedError" || err.name === "NotReadableError" || err.name === "TrackStartError" || err.name === "AbortError" || err.message && err.message.toLowerCase().includes("permission denied")) {
        errorCode = "PERMISSION_DENIED";
        userMessage = "Microphone access denied. Please allow microphone access in your browser and system settings.";
      } else if (err.name === "NotFoundError" || err.name === "DevicesNotFoundError") {
        errorCode = "NO_MICROPHONE";
        userMessage = "No microphone found. Please connect a microphone and try again.";
      } else if (err.message && err.message.includes("readyState")) {
        errorCode = "MICROPHONE_INVALID";
        userMessage = "Microphone is not ready. Please try again.";
      }
      RT.emit("error", {
        message: userMessage,
        code: errorCode,
        rawError: err.name + ": " + (err.message || String(err))
      });
    }
  };
  RT.stop = stopSession;
  RT.retrySimulation = retrySimulation;
  RT.getMicStream = function() {
    const state2 = getState();
    if (!state2.connection.micStream) {
      return null;
    }
    const clonedStream = new MediaStream();
    state2.connection.micTracks.forEach((track) => {
      clonedStream.addTrack(track.clone());
    });
    return clonedStream;
  };
  var realtime_default = RT;

  // src/components/Dialog.jsx
  var Dialog = ({ open, onClose, title, children, actions = [], theme, closeOnBackdrop = true }) => {
    const [visible, setVisible] = d2(false);
    const [closing, setClosing] = d2(false);
    y2(() => {
      if (open) {
        setVisible(true);
        setClosing(false);
      } else if (visible) {
        setClosing(true);
        const timer3 = setTimeout(() => {
          setVisible(false);
          setClosing(false);
        }, 150);
        return () => clearTimeout(timer3);
      }
    }, [open, visible]);
    const handleBackdrop = q2(() => {
      if (closeOnBackdrop && onClose) onClose();
    }, [closeOnBackdrop, onClose]);
    if (!visible) return null;
    const d3 = {
      dialogBg: theme.dialogBg || theme.simulator?.dialogBg,
      dialogBorder: theme.dialogBorder || theme.simulator?.dialogBorder,
      dialogTitle: theme.dialogTitle || theme.simulator?.dialogTitle,
      dialogText: theme.dialogText || theme.simulator?.dialogText,
      dialogDimBg: theme.dialogDimBg || theme.simulator?.dialogDimBg
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "fixed top-0 left-0 right-0 bottom-0 z-[9999] flex items-center justify-center p-6" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `absolute top-0 left-0 right-0 bottom-0 ${closing ? "dialog-overlay-out" : "dialog-overlay-in"}`,
        style: { background: d3.dialogDimBg },
        onClick: handleBackdrop
      }
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `relative w-full max-w-sm rounded-2xl p-6 border border-solid ${closing ? "dialog-card-out" : "dialog-card-in"}`,
        style: { background: d3.dialogBg, borderColor: d3.dialogBorder }
      },
      title && /* @__PURE__ */ xn.createElement("h3", { className: "font-jakarta font-extrabold text-[18px] mb-3 text-center", style: { color: d3.dialogTitle } }, title),
      /* @__PURE__ */ xn.createElement("div", { className: "font-poppins text-[14px] leading-relaxed text-center", style: { color: d3.dialogText } }, children),
      actions.length > 0 && /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col gap-3 mt-6" }, actions.map((action, i3) => /* @__PURE__ */ xn.createElement(
        "button",
        {
          key: i3,
          onClick: action.onClick,
          disabled: action.disabled,
          className: `w-full py-3 rounded-xl font-jakarta font-extrabold text-[14px] border-b-[4px] border-solid active:border-b-0 active:translate-y-[1px] transition-[transform] duration-100 ${action.disabled ? "opacity-40" : ""}`,
          style: action.primary ? {
            background: "#58CC02",
            borderColor: "#46A302",
            color: "#FFFFFF"
          } : {
            background: theme.isDark ? "rgba(255,255,255,0.06)" : "rgba(0,0,0,0.04)",
            borderColor: theme.isDark ? "rgba(255,255,255,0.15)" : "rgba(0,0,0,0.12)",
            color: d3.dialogTitle
          }
        },
        action.label
      )))
    ));
  };
  var Dialog_default = Dialog;

  // src/components/simulator/SimulatorLanding.jsx
  var audioCheckShown = false;
  var isAndroid = () => /android/i.test(navigator.userAgent);
  var SimulatorLanding = ({ onStart, coinCost = 4, theme, disabled }) => {
    const [showAudioCheck, setShowAudioCheck] = d2(false);
    const sim = theme.simulator;
    const handleStart = () => {
      if (isAndroid() && !audioCheckShown) {
        audioCheckShown = true;
        setShowAudioCheck(true);
        return;
      }
      onStart();
    };
    const handleAudioCheckDismiss = () => {
      setShowAudioCheck(false);
      onStart();
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col items-center justify-center h-full px-6 py-8" }, /* @__PURE__ */ xn.createElement("div", { className: "w-24 h-24 rounded-full flex items-center justify-center mb-8", style: { background: sim.micBg, borderColor: sim.micBorder } }, /* @__PURE__ */ xn.createElement("svg", { width: "40", height: "40", viewBox: "0 0 24 24", fill: "none", stroke: sim.micIcon, strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" }))), /* @__PURE__ */ xn.createElement("h1", { className: "font-jakarta font-extrabold text-[28px] text-center mb-3", style: { color: sim.landingTitle } }, "Conversation"), /* @__PURE__ */ xn.createElement("h1", { className: "font-jakarta font-extrabold text-[28px] text-center mb-6 -mt-2", style: { color: sim.landingTitle } }, "Simulator"), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[15px] text-center leading-relaxed mb-auto px-4", style: { color: sim.landingSubtitle } }, "Practice what you want to say, and how you say it. Get honest and objective guidance."), /* @__PURE__ */ xn.createElement("div", { className: "w-full flex flex-col items-center gap-3 mt-8" }, /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[13px]", style: { color: sim.landingHint } }, "Find a quiet place to start"), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleStart,
        disabled,
        className: "w-full max-w-[260px] py-3.5 rounded-xl font-jakarta font-extrabold text-[16px] text-white border-b-[4px] border-solid active:border-b-0 active:translate-y-[1px] transition-[transform,opacity] duration-100 disabled:opacity-50 disabled:pointer-events-none",
        style: { background: "#58CC02", borderColor: "#46A302" }
      },
      "Start"
    ), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[12px]", style: { color: sim.coinCost } }, "Uses ", coinCost, " coins")), /* @__PURE__ */ xn.createElement(
      Dialog_default,
      {
        open: showAudioCheck,
        onClose: handleAudioCheckDismiss,
        title: "Let's make sure you can hear us!",
        theme,
        closeOnBackdrop: false,
        actions: [{ label: "Got it", onClick: handleAudioCheckDismiss, primary: true }]
      },
      /* @__PURE__ */ xn.createElement("p", { className: "mb-3" }, "This experience includes audio. Some devices may experience low or no audio output."),
      /* @__PURE__ */ xn.createElement("p", null, "Use the side buttons to turn up your volume. Please ensure both Media and Ringer levels are active.")
    ));
  };
  var SimulatorLanding_default = SimulatorLanding;

  // src/utils/tts.js
  var TTS = {};
  var TTS_URL = "https://daphn-m8o8ki4f-eastus2.openai.azure.com/openai/deployments/gpt-4o-mini-tts/audio/speech?api-version=2025-03-01-preview";
  var TTS_VOICE = "echo";
  var PCM_SAMPLE_RATE = 24e3;
  var MSConstructor = typeof ManagedMediaSource !== "undefined" ? ManagedMediaSource : typeof MediaSource !== "undefined" ? MediaSource : null;
  var _isManagedMS = typeof ManagedMediaSource !== "undefined";
  var TTS_BACKEND = _isManagedMS ? "mms" : MSConstructor ? "mse" : "pcm";
  console.log("[TTS] Audio backend:", TTS_BACKEND);
  var _listeners2 = /* @__PURE__ */ new Set();
  TTS.emit = function(type, data) {
    const evt = { type, ts: Date.now(), data: data || {} };
    _listeners2.forEach((fn2) => {
      try {
        fn2(evt);
      } catch (e3) {
        console.error("[TTS] Listener error:", e3);
      }
    });
  };
  function onTTSEvent(callback) {
    _listeners2.add(callback);
  }
  function offTTSEvent(callback) {
    _listeners2.delete(callback);
  }
  var state = {
    // Shared
    abortController: null,
    animFrameId: null,
    timings: [],
    totalDuration: 0,
    status: "idle",
    cleaningUp: false,
    speed: 1,
    // MediaSource path
    audio: null,
    mediaSource: null,
    sourceBuffer: null,
    objectUrl: null,
    pendingChunks: [],
    streamDone: false,
    durationChangeHandler: null,
    endedHandler: null,
    // PCM / Web Audio path
    audioCtx: null,
    pcmNextPlayTime: 0,
    // AudioContext time for next chunk
    pcmStartCtxTime: 0,
    // AudioContext time when first chunk started
    pcmAudioOffset: 0,
    // accumulated audio seconds at last speed change
    pcmScheduledDuration: 0,
    // total audio seconds scheduled
    pcmStreamDone: false,
    pcmLeftover: null,
    // leftover byte from odd-length chunk
    pcmSources: []
    // active AudioBufferSourceNodes (for speed changes)
  };
  function splitSentences(text) {
    const matches = text.match(/[^.!?]*[.!?]+[\s]?|[^.!?]+$/g);
    return matches ? matches.map((s3) => s3.trim()).filter(Boolean) : [text];
  }
  function calculateTimings(sentences, totalDuration) {
    const totalChars = sentences.reduce((sum, s3) => sum + s3.length, 0) || 1;
    let cursor = 0;
    return sentences.map((text) => {
      const dur = text.length / totalChars * totalDuration;
      const start = cursor;
      cursor += dur;
      return { text, start, end: cursor };
    });
  }
  function flushPendingChunks() {
    const sb = state.sourceBuffer;
    if (!sb || sb.updating || state.pendingChunks.length === 0) return;
    const chunk = state.pendingChunks.shift();
    try {
      sb.appendBuffer(chunk);
    } catch {
    }
  }
  function getElapsed() {
    if (state.audioCtx) {
      return state.pcmAudioOffset + (state.audioCtx.currentTime - state.pcmStartCtxTime) * state.speed;
    }
    return state.audio ? state.audio.currentTime : 0;
  }
  function startAnimationLoop() {
    const tick = () => {
      const elapsed = getElapsed();
      const total = state.totalDuration || 1;
      const percent = Math.min(elapsed / total, 1);
      TTS.emit("progress", { elapsed, total, percent });
      if (state.timings.length) {
        TTS.emit("transcript", {
          sentences: state.timings.map((t3) => ({
            text: t3.text,
            state: elapsed < t3.start ? "upcoming" : elapsed < t3.end ? "active" : "spoken"
          }))
        });
      }
      if (state.audioCtx && state.pcmStreamDone) {
        if (state.audioCtx.currentTime >= state.pcmNextPlayTime) {
          if (state.status === "streaming" || state.status === "paused") {
            state.status = "done";
            TTS.emit("progress", { elapsed: state.totalDuration, total: state.totalDuration, percent: 1 });
            TTS.emit("status", { status: "done" });
          }
          return;
        }
      }
      if (state.status === "done") return;
      state.animFrameId = requestAnimationFrame(tick);
    };
    tick();
  }
  function cleanup() {
    if (state.cleaningUp) return;
    state.cleaningUp = true;
    if (state.abortController) {
      try {
        state.abortController.abort();
      } catch {
      }
      state.abortController = null;
    }
    if (state.animFrameId) {
      cancelAnimationFrame(state.animFrameId);
      state.animFrameId = null;
    }
    state.pendingChunks = [];
    state.streamDone = false;
    if (state.sourceBuffer) {
      try {
        state.sourceBuffer.removeEventListener("updateend", flushPendingChunks);
      } catch {
      }
    }
    if (state.audio) {
      if (state.durationChangeHandler) {
        try {
          state.audio.removeEventListener("durationchange", state.durationChangeHandler);
        } catch {
        }
        state.durationChangeHandler = null;
      }
      if (state.endedHandler) {
        try {
          state.audio.removeEventListener("ended", state.endedHandler);
        } catch {
        }
        state.endedHandler = null;
      }
      try {
        state.audio.pause();
        state.audio.src = "";
      } catch {
      }
    }
    if (state.mediaSource && state.mediaSource.readyState === "open") {
      try {
        state.mediaSource.endOfStream();
      } catch {
      }
    }
    state.mediaSource = null;
    state.sourceBuffer = null;
    if (state.objectUrl) {
      URL.revokeObjectURL(state.objectUrl);
      state.objectUrl = null;
    }
    if (state.audio && state.audio.parentNode) {
      state.audio.remove();
    }
    state.audio = null;
    if (state.audioCtx) {
      try {
        state.audioCtx.close();
      } catch {
      }
      state.audioCtx = null;
    }
    state.pcmSources = [];
    state.pcmNextPlayTime = 0;
    state.pcmStartCtxTime = 0;
    state.pcmAudioOffset = 0;
    state.pcmScheduledDuration = 0;
    state.pcmStreamDone = false;
    state.pcmLeftover = null;
    state.timings = [];
    state.totalDuration = 0;
    state.status = "idle";
    state.cleaningUp = false;
    TTS.emit("status", { status: "idle" });
  }
  function getOrCreateAudio() {
    let el = document.getElementById("ttsAudio");
    if (el) {
      state.audio = el;
      return el;
    }
    el = document.createElement("audio");
    el.id = "ttsAudio";
    el.setAttribute("playsinline", "true");
    el.setAttribute("webkit-playsinline", "true");
    document.body.appendChild(el);
    state.audio = el;
    return el;
  }
  TTS.unlockAudio = function() {
    const audio = getOrCreateAudio();
    audio.src = "data:audio/wav;base64,UklGRiYAAABXQVZFZm10IBAAAAABAAEARKwAAIhYAQACABAAZGF0YQIAAAAA";
    const p3 = audio.play();
    if (p3) p3.then(() => {
      audio.pause();
      audio.src = "";
    }).catch(() => {
    });
  };
  TTS.setSpeed = function(rate) {
    const prev = state.speed;
    state.speed = rate;
    if (state.audio) {
      state.audio.playbackRate = rate;
    } else if (state.audioCtx) {
      state.pcmAudioOffset += (state.audioCtx.currentTime - state.pcmStartCtxTime) * prev;
      state.pcmStartCtxTime = state.audioCtx.currentTime;
      state.pcmSources.forEach((s3) => {
        s3.playbackRate.value = rate;
      });
    }
  };
  TTS.stop = function() {
    cleanup();
  };
  TTS.pause = function() {
    if (state.status !== "streaming") return;
    if (state.audioCtx) {
      state.audioCtx.suspend();
    } else if (state.audio) {
      state.audio.pause();
    }
    if (state.animFrameId) {
      cancelAnimationFrame(state.animFrameId);
      state.animFrameId = null;
    }
    state.status = "paused";
    TTS.emit("status", { status: "paused" });
  };
  TTS.resume = function() {
    if (state.status !== "paused") return;
    if (state.audioCtx) {
      state.audioCtx.resume();
    } else if (state.audio) {
      state.audio.play().catch(() => {
      });
    }
    startAnimationLoop();
    state.status = "streaming";
    TTS.emit("status", { status: "streaming" });
  };
  TTS.start = async function({ apiKey, text }) {
    if (!text || !text.trim()) {
      state.status = "error";
      TTS.emit("status", { status: "error" });
      TTS.emit("error", { message: "No text provided." });
      return;
    }
    if (state.status === "streaming" || state.status === "paused") {
      cleanup();
    }
    state.status = "streaming";
    state.streamDone = false;
    state.pendingChunks = [];
    TTS.emit("status", { status: "streaming" });
    const sentences = splitSentences(text);
    const wordCount = text.split(/\s+/).length;
    const estimatedDuration = wordCount / 150 * 60;
    state.timings = calculateTimings(sentences, estimatedDuration);
    state.totalDuration = estimatedDuration;
    TTS.emit("transcript", {
      sentences: sentences.map((s3) => ({ text: s3, state: "upcoming" }))
    });
    if (state.mediaSource && state.mediaSource.readyState === "open") {
      try {
        state.mediaSource.endOfStream();
      } catch {
      }
    }
    if (state.objectUrl) {
      URL.revokeObjectURL(state.objectUrl);
      state.objectUrl = null;
    }
    const abort = new AbortController();
    state.abortController = abort;
    if (state.animFrameId) cancelAnimationFrame(state.animFrameId);
    const startTime = performance.now();
    try {
      if (MSConstructor) {
        const audio = getOrCreateAudio();
        await _startStreaming(audio, sentences, apiKey, text, abort, startTime);
      } else {
        await _startPCM(sentences, apiKey, text, abort, startTime);
      }
    } catch (e3) {
      if (e3.name === "AbortError") {
        if (state.animFrameId) cancelAnimationFrame(state.animFrameId);
        state.animFrameId = null;
        return;
      }
      console.error("TTS error:", e3);
      state.status = "error";
      TTS.emit("status", { status: "error" });
      TTS.emit("error", { message: e3.message });
    }
  };
  async function _startStreaming(audio, sentences, apiKey, text, abort, startTime) {
    const mediaSource = new MSConstructor();
    state.mediaSource = mediaSource;
    if (_isManagedMS) audio.disableRemotePlayback = true;
    const url = URL.createObjectURL(mediaSource);
    state.objectUrl = url;
    audio.src = url;
    let firstByteTime = null;
    let totalBytes = 0;
    await new Promise((resolve2, reject2) => {
      if (mediaSource.readyState === "open") {
        resolve2();
        return;
      }
      const onOpen = () => {
        clearTimeout(timer3);
        resolve2();
      };
      const timer3 = setTimeout(() => {
        mediaSource.removeEventListener("sourceopen", onOpen);
        reject2(new Error("MediaSource sourceopen timed out"));
      }, 1e4);
      abort.signal.addEventListener("abort", () => {
        clearTimeout(timer3);
        mediaSource.removeEventListener("sourceopen", onOpen);
        reject2(new DOMException("Aborted", "AbortError"));
      }, { once: true });
      mediaSource.addEventListener("sourceopen", onOpen, { once: true });
    });
    const sourceBuffer = mediaSource.addSourceBuffer("audio/mpeg");
    state.sourceBuffer = sourceBuffer;
    sourceBuffer.addEventListener("updateend", flushPendingChunks);
    const res = await _fetchTTS(apiKey, text, abort, "mp3");
    const reader = res.body.getReader();
    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      if (!firstByteTime) {
        firstByteTime = performance.now();
        _attachEndedHandler(audio);
        audio.playbackRate = state.speed;
        audio.play().catch((err) => {
          if (err.name === "NotAllowedError") TTS.emit("autoplay_blocked", {});
        });
        startAnimationLoop();
      }
      totalBytes += value.length;
      state.pendingChunks.push(value);
      flushPendingChunks();
    }
    await new Promise((resolve2) => {
      const check = () => {
        if (state.pendingChunks.length === 0 && state.sourceBuffer && !state.sourceBuffer.updating) {
          resolve2();
        } else {
          setTimeout(check, 50);
        }
      };
      check();
    });
    if (mediaSource.readyState === "open" && state.sourceBuffer && !state.sourceBuffer.updating) {
      try {
        mediaSource.endOfStream();
      } catch {
      }
    }
    sourceBuffer.removeEventListener("updateend", flushPendingChunks);
    _finalizeStreamingDuration(audio, sentences, startTime, firstByteTime, totalBytes);
  }
  async function _startPCM(sentences, apiKey, text, abort, startTime) {
    const AudioCtx = window.AudioContext || window.webkitAudioContext;
    const audioCtx = new AudioCtx({ sampleRate: PCM_SAMPLE_RATE });
    state.audioCtx = audioCtx;
    state.pcmSources = [];
    state.pcmNextPlayTime = 0;
    state.pcmStartCtxTime = 0;
    state.pcmAudioOffset = 0;
    state.pcmScheduledDuration = 0;
    state.pcmStreamDone = false;
    state.pcmLeftover = null;
    if (audioCtx.state === "suspended") {
      await audioCtx.resume();
    }
    const res = await _fetchTTS(apiKey, text, abort, "pcm");
    const reader = res.body.getReader();
    let firstByteTime = null;
    let totalBytes = 0;
    while (true) {
      const { done, value } = await reader.read();
      if (done) break;
      if (!firstByteTime) {
        firstByteTime = performance.now();
        state.pcmStartCtxTime = audioCtx.currentTime;
        state.pcmNextPlayTime = audioCtx.currentTime;
        startAnimationLoop();
      }
      totalBytes += value.length;
      _schedulePCMChunk(audioCtx, value);
    }
    state.pcmLeftover = null;
    state.pcmStreamDone = true;
    const realDuration = state.pcmScheduledDuration;
    state.totalDuration = realDuration;
    state.timings = calculateTimings(sentences, realDuration);
    state.streamDone = true;
    const fetchDone = performance.now();
    TTS.emit("stats", {
      firstByte: firstByteTime ? Math.round(firstByteTime - startTime) : null,
      totalFetch: Math.round(fetchDone - startTime),
      duration: realDuration.toFixed(1),
      size: `${(totalBytes / 1024).toFixed(0)}KB`,
      sentenceCount: sentences.length
    });
  }
  function _schedulePCMChunk(audioCtx, rawChunk) {
    let data;
    if (state.pcmLeftover) {
      data = new Uint8Array(state.pcmLeftover.length + rawChunk.length);
      data.set(state.pcmLeftover, 0);
      data.set(rawChunk, state.pcmLeftover.length);
      state.pcmLeftover = null;
    } else {
      data = rawChunk;
    }
    if (data.length % 2 !== 0) {
      state.pcmLeftover = data.slice(-1);
      data = data.slice(0, -1);
    }
    if (data.length === 0) return;
    const aligned = new Uint8Array(data.length);
    aligned.set(data);
    const int16 = new Int16Array(aligned.buffer, 0, aligned.length / 2);
    const float32 = new Float32Array(int16.length);
    for (let i3 = 0; i3 < int16.length; i3++) {
      float32[i3] = int16[i3] / 32768;
    }
    const audioBuffer = audioCtx.createBuffer(1, float32.length, PCM_SAMPLE_RATE);
    audioBuffer.getChannelData(0).set(float32);
    const source = audioCtx.createBufferSource();
    source.buffer = audioBuffer;
    source.playbackRate.value = state.speed;
    source.connect(audioCtx.destination);
    state.pcmSources.push(source);
    source.onended = () => {
      const idx = state.pcmSources.indexOf(source);
      if (idx !== -1) state.pcmSources.splice(idx, 1);
    };
    const currentTime = audioCtx.currentTime;
    if (state.pcmNextPlayTime < currentTime) {
      state.pcmNextPlayTime = currentTime;
    }
    source.start(state.pcmNextPlayTime);
    state.pcmNextPlayTime += audioBuffer.duration / state.speed;
    state.pcmScheduledDuration += audioBuffer.duration;
  }
  async function _fetchTTS(apiKey, text, abort, format) {
    const res = await fetch(TTS_URL, {
      method: "POST",
      headers: { "Content-Type": "application/json", "api-key": apiKey },
      body: JSON.stringify({
        model: "gpt-4o-mini-tts",
        input: text,
        voice: TTS_VOICE,
        response_format: format
      }),
      signal: abort.signal
    });
    if (!res.ok) {
      const errText = await res.text();
      throw new Error(`${res.status}: ${errText.slice(0, 300)}`);
    }
    return res;
  }
  function _attachEndedHandler(audio) {
    const onEnded = () => {
      if (state.status === "streaming" || state.status === "paused") {
        if (state.animFrameId) {
          cancelAnimationFrame(state.animFrameId);
          state.animFrameId = null;
        }
        state.status = "done";
        TTS.emit("progress", { elapsed: state.totalDuration, total: state.totalDuration, percent: 1 });
        TTS.emit("status", { status: "done" });
      }
    };
    state.endedHandler = onEnded;
    audio.addEventListener("ended", onEnded, { once: true });
  }
  function _finalizeStreamingDuration(audio, sentences, startTime, firstByteTime, totalBytes) {
    const fetchDone = performance.now();
    const realDuration = audio.duration && isFinite(audio.duration) ? audio.duration : state.totalDuration;
    state.totalDuration = realDuration;
    state.timings = calculateTimings(sentences, realDuration);
    state.streamDone = true;
    const onDurationChange = () => {
      if (isFinite(audio.duration) && audio.duration > 0 && audio.duration !== state.totalDuration) {
        state.totalDuration = audio.duration;
        state.timings = calculateTimings(sentences, audio.duration);
      }
    };
    state.durationChangeHandler = onDurationChange;
    audio.addEventListener("durationchange", onDurationChange);
    TTS.emit("stats", {
      firstByte: firstByteTime ? Math.round(firstByteTime - startTime) : null,
      totalFetch: Math.round(fetchDone - startTime),
      duration: realDuration.toFixed(1),
      size: `${(totalBytes / 1024).toFixed(0)}KB`,
      sentenceCount: sentences.length
    });
  }
  var tts_default = TTS;

  // src/components/simulator/Waveform.jsx
  var CONFIG = {
    FFT_SIZE: 2048,
    SIMULATED_SAMPLES: 512,
    WAVEFORM_AMPLITUDE: 30,
    FADE_DISTANCE: 20,
    LINE_WIDTH: 2,
    BUTTON_RADIUS: 30,
    BUTTON_STROKE_WIDTH: 1,
    BUTTON_VISUAL_GAP: 2,
    PHASE_INCREMENT: 0.05,
    PHASE_MULTIPLIER: 0.3,
    ENVELOPE_AMPLITUDE: 0.4,
    ENVELOPE_OFFSET: 0.6,
    WAVE_FREQUENCIES: [5, 13, 21],
    WAVE_AMPLITUDES: [0.25, 0.15, 0.1],
    LOCAL_PHASE_MULTIPLIER: 0.1,
    NOISE_AMPLITUDE: 0.08,
    SIMULATED_RANGE: 70
  };
  var Waveform = ({ rtState, buttonVisible = false, theme }) => {
    const canvasRef = A2(null);
    const stateRef = A2({
      mode: "idle",
      // idle | baseline | simulated | real
      running: false,
      rafId: null,
      phase: 0,
      audioCtx: null,
      analyser: null,
      dataArray: null,
      micStream: null,
      simulatedData: new Array(CONFIG.SIMULATED_SAMPLES).fill(128),
      buttonVisible: false
    });
    stateRef.current.buttonVisible = buttonVisible;
    stateRef.current.theme = theme;
    const cleanupAudioSource = q2(() => {
      const s3 = stateRef.current;
      if (s3.analyser?._source) {
        try {
          s3.analyser._source.disconnect();
        } catch {
        }
        s3.analyser._source = null;
      }
    }, []);
    const getAudioContext = q2(async () => {
      const s3 = stateRef.current;
      if (!s3.audioCtx || s3.audioCtx.state === "closed") {
        s3.audioCtx = new (window.AudioContext || window.webkitAudioContext)();
      }
      if (s3.audioCtx.state === "suspended") {
        try {
          await s3.audioCtx.resume();
        } catch {
        }
      }
      return s3.audioCtx;
    }, []);
    const startRealAudioFromElement = q2(async () => {
      const s3 = stateRef.current;
      try {
        const audioEl = window.RT?.state?.connection?.audioEl || document.getElementById("rtAudio");
        if (!audioEl?.srcObject) {
          s3.mode = "simulated";
          return;
        }
        const mediaStream = audioEl.srcObject;
        const tracks = mediaStream?.getAudioTracks() || [];
        if (!mediaStream || tracks.length === 0) {
          s3.mode = "simulated";
          return;
        }
        const context = await getAudioContext();
        cleanupAudioSource();
        const src = context.createMediaStreamSource(mediaStream);
        s3.analyser = context.createAnalyser();
        s3.analyser.fftSize = CONFIG.FFT_SIZE;
        src.connect(s3.analyser);
        s3.analyser._source = src;
        s3.dataArray = new Uint8Array(s3.analyser.frequencyBinCount);
        if (context.state === "suspended") {
          try {
            await context.resume();
          } catch {
          }
        }
        s3.mode = "real";
      } catch {
        s3.mode = "simulated";
      }
    }, [getAudioContext, cleanupAudioSource]);
    const startRealAudioFromMic = q2(async () => {
      const s3 = stateRef.current;
      try {
        const stream = window.RT?.getMicStream?.() || window.RT?.state?.connection?.micStream;
        if (!stream) return;
        s3.micStream = stream;
        const context = await getAudioContext();
        cleanupAudioSource();
        const src = context.createMediaStreamSource(stream);
        s3.analyser = context.createAnalyser();
        s3.analyser.fftSize = CONFIG.FFT_SIZE;
        src.connect(s3.analyser);
        s3.analyser._source = src;
        s3.dataArray = new Uint8Array(s3.analyser.frequencyBinCount);
        s3.mode = "real";
      } catch {
      }
    }, [getAudioContext, cleanupAudioSource]);
    const stopAnimation = q2(() => {
      const s3 = stateRef.current;
      s3.running = false;
      s3.mode = "idle";
      if (s3.rafId) {
        cancelAnimationFrame(s3.rafId);
        s3.rafId = null;
      }
      cleanupAudioSource();
    }, [cleanupAudioSource]);
    const drawFrame = q2(() => {
      const s3 = stateRef.current;
      const canvas = canvasRef.current;
      if (!canvas || !s3.running) return;
      const ctx = canvas.getContext("2d");
      const dpr = window.devicePixelRatio || 1;
      const w4 = canvas.width / dpr;
      const h3 = canvas.height / dpr;
      const centerY = h3 / 2;
      const strokeColor = s3.theme.simulator.waveformLine;
      const strokeColorTransparent = "rgba(255,255,255,0)";
      ctx.clearRect(0, 0, canvas.width, canvas.height);
      ctx.strokeStyle = strokeColor;
      ctx.lineWidth = CONFIG.LINE_WIDTH;
      ctx.lineCap = "round";
      const btnVisible = s3.buttonVisible;
      const buttonCenterX = w4 / 2;
      const buttonGap = CONFIG.BUTTON_RADIUS + CONFIG.BUTTON_STROKE_WIDTH / 2 + CONFIG.LINE_WIDTH / 2 + CONFIG.BUTTON_VISUAL_GAP;
      const leftGap = btnVisible ? buttonCenterX - buttonGap : w4;
      const rightGap = btnVisible ? buttonCenterX + buttonGap : w4;
      const createFadeGradient = (startX, endX, fadeLeft, fadeRight) => {
        const length = endX - startX;
        if (length <= CONFIG.FADE_DISTANCE * 2 && (fadeLeft || fadeRight)) return null;
        const gradient = ctx.createLinearGradient(startX, centerY, endX, centerY);
        const fadeRatio = CONFIG.FADE_DISTANCE / length;
        if (fadeLeft && fadeRight) {
          gradient.addColorStop(0, strokeColorTransparent);
          gradient.addColorStop(fadeRatio, strokeColor);
          gradient.addColorStop(1 - fadeRatio, strokeColor);
          gradient.addColorStop(1, strokeColorTransparent);
        } else if (fadeLeft) {
          gradient.addColorStop(0, strokeColorTransparent);
          gradient.addColorStop(fadeRatio, strokeColor);
          gradient.addColorStop(1, strokeColor);
        } else if (fadeRight) {
          gradient.addColorStop(0, strokeColor);
          gradient.addColorStop(1 - fadeRatio, strokeColor);
          gradient.addColorStop(1, strokeColorTransparent);
        } else {
          return null;
        }
        return gradient;
      };
      const drawBaseline = (startX, endX, fadeLeft, fadeRight) => {
        const gradient = createFadeGradient(startX, endX, fadeLeft, fadeRight);
        ctx.strokeStyle = gradient || strokeColor;
        ctx.beginPath();
        ctx.moveTo(startX, centerY);
        ctx.lineTo(endX, centerY);
        ctx.stroke();
      };
      const drawWaveform = (audioData, startX, endX, fadeLeft, fadeRight) => {
        const length = endX - startX;
        if (length <= 0 || audioData.length === 0) return;
        const gradient = createFadeGradient(startX, endX, fadeLeft, fadeRight);
        ctx.strokeStyle = gradient || strokeColor;
        ctx.beginPath();
        for (let x5 = startX; x5 < endX; x5++) {
          const i3 = Math.floor(x5 / w4 * audioData.length);
          const value = (audioData[i3] - 128) / 128;
          const y3 = centerY + value * CONFIG.WAVEFORM_AMPLITUDE;
          if (x5 === startX) ctx.moveTo(x5, y3);
          else ctx.lineTo(x5, y3);
        }
        ctx.stroke();
      };
      if (s3.mode === "baseline") {
        if (btnVisible) {
          if (leftGap > 0) drawBaseline(0, leftGap, true, false);
          if (rightGap < w4) drawBaseline(rightGap, w4, false, true);
        } else {
          drawBaseline(0, w4, true, true);
        }
      } else if (s3.mode === "simulated" || s3.mode === "real") {
        let audioData = [];
        if (s3.mode === "real" && s3.analyser && s3.dataArray) {
          s3.analyser.getByteTimeDomainData(s3.dataArray);
          audioData = Array.from(s3.dataArray);
        } else if (s3.mode === "simulated") {
          s3.phase += CONFIG.PHASE_INCREMENT;
          const timeEnvelope = Math.sin(s3.phase * CONFIG.PHASE_MULTIPLIER) * CONFIG.ENVELOPE_AMPLITUDE + CONFIG.ENVELOPE_OFFSET;
          for (let i3 = 0; i3 < CONFIG.SIMULATED_SAMPLES; i3++) {
            const t3 = i3 / CONFIG.SIMULATED_SAMPLES * Math.PI * 2;
            let combined = 0;
            for (let j4 = 0; j4 < CONFIG.WAVE_FREQUENCIES.length; j4++) {
              combined += Math.sin(t3 * CONFIG.WAVE_FREQUENCIES[j4]) * CONFIG.WAVE_AMPLITUDES[j4];
            }
            const localPhase = Math.sin(i3 * CONFIG.LOCAL_PHASE_MULTIPLIER + s3.phase * 0.2) * 0.1;
            const noise = (Math.random() - 0.5) * CONFIG.NOISE_AMPLITUDE;
            combined = (combined + localPhase + noise) * timeEnvelope;
            s3.simulatedData[i3] = Math.round(128 + combined * CONFIG.SIMULATED_RANGE);
          }
          audioData = s3.simulatedData;
        }
        if (audioData.length > 0) {
          if (btnVisible) {
            if (leftGap > 0) drawWaveform(audioData, 0, leftGap, true, false);
            if (rightGap < w4) drawWaveform(audioData, rightGap, w4, false, true);
          } else {
            drawWaveform(audioData, 0, w4, true, true);
          }
        }
      }
      ctx.strokeStyle = strokeColor;
      s3.rafId = requestAnimationFrame(drawFrame);
    }, []);
    y2(() => {
      const s3 = stateRef.current;
      if (s3.audioCtx && s3.audioCtx.state === "suspended") {
        s3.audioCtx.resume().catch(() => {
        });
      }
      const startLoop = () => {
        if (s3.running) return;
        s3.running = true;
        s3.rafId = requestAnimationFrame(drawFrame);
      };
      switch (rtState) {
        case "COACH_SPEAKING":
        case "PARTNER_SPEAKING":
          startRealAudioFromElement().then(() => {
            startLoop();
          });
          break;
        case "PUSH_TO_TALK_READY":
        case "PUSH_TO_TALK_STOPPED":
          cleanupAudioSource();
          s3.mode = "baseline";
          startLoop();
          break;
        case "PUSH_TO_TALK_ACTIVE":
          startRealAudioFromMic().then(() => {
            startLoop();
          });
          break;
        case "LOADING_SIMULATION":
        case "SESSION_STOPPED":
        case "SESSION_ERROR":
        case "SESSION_COMPLETED":
        case "SESSION_STARTED":
        case "BEGIN_SIMULATION":
          stopAnimation();
          break;
        default:
          if (!s3.running) {
            s3.mode = "idle";
          }
          break;
      }
    }, [rtState, drawFrame, startRealAudioFromElement, startRealAudioFromMic, cleanupAudioSource, stopAnimation]);
    y2(() => {
      const canvas = canvasRef.current;
      if (!canvas) return;
      const resize = () => {
        const rect = canvas.getBoundingClientRect();
        if (rect.width === 0 || rect.height === 0) return;
        const dpr = window.devicePixelRatio || 1;
        canvas.width = rect.width * dpr;
        canvas.height = rect.height * dpr;
        const ctx = canvas.getContext("2d");
        ctx.scale(dpr, dpr);
      };
      resize();
      window.addEventListener("resize", resize);
      return () => window.removeEventListener("resize", resize);
    }, []);
    y2(() => {
      const s3 = stateRef.current;
      return () => {
        s3.running = false;
        if (s3.rafId) cancelAnimationFrame(s3.rafId);
        cleanupAudioSource();
        if (s3.audioCtx && s3.audioCtx.state !== "closed") {
          try {
            s3.audioCtx.close();
          } catch {
          }
        }
      };
    }, [cleanupAudioSource]);
    return /* @__PURE__ */ xn.createElement(
      "canvas",
      {
        ref: canvasRef,
        className: "w-full",
        style: { height: 72, display: "block" }
      }
    );
  };
  var Waveform_default = Waveform;

  // src/components/simulator/MicButton.jsx
  var MicButton = ({ state: state2 = "hidden", onPress, theme }) => {
    const sim = theme.simulator;
    if (state2 === "hidden") return null;
    const isRecording = state2 === "recording";
    const isWaiting = state2 === "waiting";
    return /* @__PURE__ */ xn.createElement("div", { style: { width: 60, height: 70, position: "relative", flexShrink: 0 } }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: onPress,
        style: {
          width: 60,
          height: 70,
          border: "none",
          background: "transparent",
          cursor: "pointer",
          padding: 0,
          position: "relative"
        }
      },
      /* @__PURE__ */ xn.createElement(
        "svg",
        {
          width: "60",
          height: "70",
          viewBox: "0 0 60 70",
          fill: "none",
          style: {
            position: "absolute",
            top: 0,
            left: 0,
            width: "100%",
            height: "100%",
            zIndex: 2,
            opacity: isRecording ? 0 : 1,
            transition: "opacity 0.2s",
            filter: isWaiting ? sim.micWaitingGlow : "none",
            animation: isWaiting ? "borderPulse 1.5s ease-in-out infinite" : "none"
          }
        },
        /* @__PURE__ */ xn.createElement("rect", { x: "0.5", y: "0.5", width: "59", height: "69", rx: "29.5", stroke: sim.micIcon }),
        /* @__PURE__ */ xn.createElement(
          "path",
          {
            d: "M21.5625 35V23.75C21.5625 21.5122 22.4514 19.3661 24.0338 17.7838C25.6161 16.2014 27.7622 15.3125 30 15.3125C32.2378 15.3125 34.3839 16.2014 35.9662 17.7838C37.5486 19.3661 38.4375 21.5122 38.4375 23.75V35C38.4375 37.2378 37.5486 39.3839 35.9662 40.9662C34.3839 42.5486 32.2378 43.4375 30 43.4375C27.7622 43.4375 25.6161 42.5486 24.0338 40.9662C22.4514 39.3839 21.5625 37.2378 21.5625 35ZM44.0625 35C44.0625 34.627 43.9143 34.2694 43.6506 34.0056C43.3869 33.7419 43.0292 33.5938 42.6562 33.5938C42.2833 33.5938 41.9256 33.7419 41.6619 34.0056C41.3982 34.2694 41.25 34.627 41.25 35C41.25 37.9837 40.0647 40.8452 37.955 42.955C35.8452 45.0647 32.9837 46.25 30 46.25C27.0163 46.25 24.1548 45.0647 22.045 42.955C19.9353 40.8452 18.75 37.9837 18.75 35C18.75 34.627 18.6018 34.2694 18.3381 34.0056C18.0744 33.7419 17.7167 33.5938 17.3438 33.5938C16.9708 33.5938 16.6131 33.7419 16.3494 34.0056C16.0857 34.2694 15.9375 34.627 15.9375 35C15.9418 38.4849 17.2381 41.8445 19.5759 44.429C21.9136 47.0135 25.1267 48.6394 28.5938 48.9922V53.2812C28.5938 53.6542 28.7419 54.0119 29.0056 54.2756C29.2694 54.5393 29.627 54.6875 30 54.6875C30.373 54.6875 30.7306 54.5393 30.9944 54.2756C31.2581 54.0119 31.4062 53.6542 31.4062 53.2812V48.9922C34.8733 48.6394 38.0863 47.0135 40.4241 44.429C42.7619 41.8445 44.0582 38.4849 44.0625 35Z",
            fill: sim.micIcon
          }
        )
      ),
      /* @__PURE__ */ xn.createElement(
        "svg",
        {
          width: "60",
          height: "70",
          viewBox: "0 0 60 70",
          fill: "none",
          style: {
            position: "absolute",
            top: 0,
            left: 0,
            width: "100%",
            height: "100%",
            zIndex: 2,
            opacity: isRecording ? 1 : 0,
            transition: "opacity 0.2s"
          }
        },
        /* @__PURE__ */ xn.createElement("rect", { x: "0.5", y: "0.5", width: "59", height: "69", rx: "29.5", fill: sim.micRecordingBg }),
        /* @__PURE__ */ xn.createElement("rect", { x: "0.5", y: "0.5", width: "59", height: "69", rx: "29.5", stroke: sim.micIcon }),
        /* @__PURE__ */ xn.createElement("rect", { x: "15", y: "20", width: "30", height: "30", rx: "5", fill: "#D70146" })
      )
    ));
  };
  var MicButton_default = MicButton;

  // src/components/simulator/Transcript.jsx
  var PACING_RATE = 18;
  var Transcript = D3(({ theme }, ref) => {
    const containerRef = A2(null);
    const textColorRef = A2(theme.simulator.transcriptText);
    const stateRef = A2({
      currentLines: { user: null, assistant: null, coach: null },
      pacingBuffer: "",
      pacingTimer: null,
      isFinalLinePending: false,
      lastFullText: ""
    });
    const apiRef = A2({
      updateText: null,
      clearText: null
    });
    y2(() => {
      if (typeof ref === "function") {
        ref(containerRef.current);
      } else if (ref) {
        ref.current = containerRef.current;
      }
    }, [ref]);
    y2(() => {
      const container2 = containerRef.current;
      if (!container2) return;
      const s3 = stateRef.current;
      function updateDOM(type, text, isFinal) {
        let inner = container2.querySelector(".ts-inner");
        if (!inner) {
          container2.innerHTML = '<div class="ts-inner" style="width:100%;height:100%;padding:0.8em 30px;overflow-y:auto;scrollbar-width:none;-webkit-mask-image:linear-gradient(to bottom,transparent 0%,black 15px,black 100%);mask-image:linear-gradient(to bottom,transparent 0%,black 15px,black 100%);box-sizing:border-box;"></div>';
          inner = container2.querySelector(".ts-inner");
        }
        let lineEl = s3.currentLines[type];
        if (!lineEl) {
          while (inner.children.length >= 2) {
            inner.removeChild(inner.firstChild);
          }
          lineEl = document.createElement("div");
          lineEl.style.cssText = `margin:0;line-height:1.6;font-size:inherit;opacity:0.8;word-wrap:break-word;display:block;text-align:center;color:${textColorRef.current};font-family:Poppins,sans-serif;transition:opacity 0.3s ease;`;
          lineEl.style.opacity = "1";
          lineEl.style.fontWeight = "500";
          inner.appendChild(lineEl);
          s3.currentLines[type] = lineEl;
        }
        if (type === "user" && text === "...") {
          lineEl.style.opacity = "0.5";
          lineEl.style.fontStyle = "italic";
          lineEl.textContent = text;
        } else {
          lineEl.style.opacity = "1";
          lineEl.style.fontStyle = "normal";
          lineEl.textContent = text;
        }
        inner.style.scrollBehavior = "smooth";
        inner.scrollTop = inner.scrollHeight;
        if (isFinal) {
          lineEl.style.opacity = "0.8";
          lineEl.style.fontWeight = "400";
          s3.currentLines[type] = null;
        }
      }
      function startPacing(type, interval) {
        if (s3.pacingTimer) return;
        let displayedText = s3.currentLines[type]?.textContent || "";
        s3.pacingTimer = setInterval(() => {
          if (s3.pacingBuffer.length > 0) {
            displayedText += s3.pacingBuffer.charAt(0);
            s3.pacingBuffer = s3.pacingBuffer.substring(1);
            updateDOM(type, displayedText, false);
          } else if (s3.isFinalLinePending) {
            clearInterval(s3.pacingTimer);
            s3.pacingTimer = null;
            updateDOM(type, displayedText, true);
            s3.isFinalLinePending = false;
            s3.lastFullText = "";
          } else {
            clearInterval(s3.pacingTimer);
            s3.pacingTimer = null;
          }
        }, interval);
      }
      apiRef.current.updateText = function(type, text, isFinal, options) {
        if (type === "user") {
          updateDOM(type, text, isFinal);
          return;
        }
        const newContent = text.substring(s3.lastFullText.length);
        s3.pacingBuffer += newContent;
        s3.lastFullText = text;
        if (isFinal) {
          s3.isFinalLinePending = true;
        }
        if (!s3.pacingTimer && s3.pacingBuffer.length > 0) {
          const msPerChar = options?.duration_ms ? options.duration_ms / s3.pacingBuffer.length : 1e3 / PACING_RATE;
          startPacing(type, Math.max(10, msPerChar));
        }
      };
      apiRef.current.clearText = function() {
        const inner = container2.querySelector(".ts-inner");
        if (inner) inner.innerHTML = "";
        s3.currentLines.user = null;
        s3.currentLines.assistant = null;
        s3.currentLines.coach = null;
        if (s3.pacingTimer) clearInterval(s3.pacingTimer);
        s3.pacingTimer = null;
        s3.pacingBuffer = "";
        s3.isFinalLinePending = false;
        s3.lastFullText = "";
      };
      container2._transcriptAPI = apiRef.current;
      return () => {
        if (s3.pacingTimer) clearInterval(s3.pacingTimer);
      };
    }, []);
    const sim = theme.simulator;
    return /* @__PURE__ */ xn.createElement(
      "div",
      {
        ref: containerRef,
        style: {
          width: "95%",
          maxWidth: 900,
          fontSize: 15,
          lineHeight: "1.6",
          height: "6.4em",
          margin: "15px auto 10px",
          padding: 0,
          textAlign: "center",
          background: sim.transcriptBg,
          borderRadius: 14,
          overflow: "hidden",
          position: "relative",
          border: `1px solid ${sim.transcriptBorder}`
        }
      },
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "ts-inner",
          style: {
            width: "100%",
            height: "100%",
            padding: "0.8em 30px",
            overflowY: "auto",
            scrollbarWidth: "none",
            WebkitMaskImage: "linear-gradient(to bottom, transparent 0%, black 15px, black 100%)",
            maskImage: "linear-gradient(to bottom, transparent 0%, black 15px, black 100%)",
            boxSizing: "border-box"
          }
        }
      )
    );
  });
  Transcript.displayName = "Transcript";
  var Transcript_default = Transcript;

  // src/assets/coin-sound.js
  var coin_sound_default = "data:audio/mpeg;base64,";

  // src/components/simulator/CoinDeduction.jsx
  var coinAudioCtx = null;
  function getCoinAudioCtx() {
    if (coinAudioCtx && coinAudioCtx.state === "closed") coinAudioCtx = null;
    if (!coinAudioCtx) {
      try {
        coinAudioCtx = new (window.AudioContext || window.webkitAudioContext)();
        if (coinAudioCtx.state === "suspended") coinAudioCtx.resume();
      } catch {
      }
    }
    return coinAudioCtx;
  }
  var coinBuffer = null;
  async function loadCoinBuffer() {
    try {
      const ctx = getCoinAudioCtx();
      if (!ctx || coinBuffer) return;
      const res = await fetch(coin_sound_default);
      const arrayBuf = await res.arrayBuffer();
      coinBuffer = await ctx.decodeAudioData(arrayBuf);
    } catch {
    }
  }
  var CoinDeduction = ({ coinCount, coinCost = 4, onComplete }) => {
    const [phase, setPhase] = d2("enter");
    const [showCoin, setShowCoin] = d2(false);
    const [showCost, setShowCost] = d2(false);
    const [showCount, setShowCount] = d2(false);
    const [displayCount, setDisplayCount] = d2(coinCount);
    const [countPulse, setCountPulse] = d2(false);
    const timersRef = A2([]);
    const addTimer = q2((fn2, ms) => {
      const id = setTimeout(fn2, ms);
      timersRef.current.push(id);
      return id;
    }, []);
    y2(() => {
      loadCoinBuffer();
    }, []);
    const sourcesRef = A2([]);
    const scheduleAllSounds = q2(() => {
      try {
        const ctx = getCoinAudioCtx();
        if (!ctx || !coinBuffer) return;
        if (ctx.state === "suspended") ctx.resume();
        const now = ctx.currentTime;
        for (let i3 = 0; i3 < coinCost; i3++) {
          const when = now + i3 * 0.22;
          const source = ctx.createBufferSource();
          source.buffer = coinBuffer;
          source.connect(ctx.destination);
          source.start(when);
          sourcesRef.current.push(source);
        }
      } catch {
      }
    }, [coinCost]);
    y2(() => {
      timersRef.current.forEach(clearTimeout);
      timersRef.current = [];
      let t3 = 150;
      addTimer(() => setShowCoin(true), t3);
      t3 += 450;
      addTimer(() => setShowCost(true), t3);
      t3 += 300;
      addTimer(() => setShowCount(true), t3);
      t3 += 300;
      addTimer(() => scheduleAllSounds(), t3);
      for (let i3 = 0; i3 < coinCost; i3++) {
        const tickIndex = i3;
        addTimer(() => {
          setDisplayCount(coinCount - tickIndex - 1);
          setCountPulse(true);
          addTimer(() => setCountPulse(false), 120);
        }, t3);
        t3 += 220;
      }
      t3 += 300;
      addTimer(() => setPhase("exit"), t3);
      t3 += 400;
      addTimer(() => onComplete(), t3);
      return () => {
        timersRef.current.forEach(clearTimeout);
        sourcesRef.current.forEach((s3) => {
          try {
            s3.stop();
          } catch {
          }
        });
        sourcesRef.current = [];
      };
    }, [coinCount, coinCost, addTimer, onComplete, scheduleAllSounds]);
    const isExit = phase === "exit";
    return /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "flex flex-col items-center justify-center h-full",
        style: {
          background: isExit ? "rgba(0,0,0,0)" : "rgba(0,0,0,0.85)",
          transition: "background 0.6s ease"
        }
      },
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          style: {
            display: "flex",
            alignItems: "center",
            gap: 12,
            opacity: isExit ? 0 : showCoin ? 1 : 0,
            transition: "opacity 0.4s ease"
          }
        },
        /* @__PURE__ */ xn.createElement("div", { className: showCoin ? "coin-pop-in" : "" }, /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "coin-shimmer",
            style: {
              width: 80,
              height: 80,
              borderRadius: "50%",
              background: "radial-gradient(circle at 35% 30%, #C8C8C8, #8A8A8A 70%, #6E6E6E)",
              display: "flex",
              alignItems: "center",
              justifyContent: "center",
              boxShadow: "0 6px 24px rgba(0,0,0,0.3), 0 2px 0 0 #555"
            }
          },
          /* @__PURE__ */ xn.createElement(
            "span",
            {
              style: {
                fontFamily: "'Plus Jakarta Sans', sans-serif",
                fontWeight: 800,
                fontSize: "2rem",
                color: "#fff",
                textShadow: "0 1px 2px rgba(0,0,0,0.3)"
              }
            },
            "B"
          )
        )),
        /* @__PURE__ */ xn.createElement(
          "span",
          {
            className: "font-jakarta font-extrabold",
            style: {
              fontSize: 32,
              color: "#FF4D6A",
              opacity: showCost ? 1 : 0,
              transition: "opacity 0.3s ease"
            }
          },
          "-",
          coinCost
        )
      ),
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          style: {
            opacity: isExit ? 0 : showCount ? 1 : 0,
            transform: `scale(${countPulse ? 1.15 : 1})`,
            transition: "opacity 0.3s ease, transform 0.15s ease",
            marginTop: 24
          }
        },
        /* @__PURE__ */ xn.createElement(
          "span",
          {
            className: "font-jakarta font-extrabold",
            style: { fontSize: 48, color: "#fff" }
          },
          displayCount
        )
      ),
      /* @__PURE__ */ xn.createElement(
        "p",
        {
          style: {
            opacity: isExit ? 0 : showCount ? 1 : 0,
            transition: "opacity 0.3s ease",
            marginTop: 8,
            color: "rgba(255,255,255,0.5)"
          },
          className: "font-poppins text-[0.875rem]"
        },
        "coins"
      )
    );
  };
  var CoinDeduction_default = CoinDeduction;

  // src/components/simulator/SimulatorSession.jsx
  var TOTAL_TURNS = 4;
  var SimulatorSession = ({ theme, coinCount, onComplete, onClose, onStage2Start, onStageChange, onActiveSpeakerChange, onError }) => {
    const sim = theme.simulator;
    const transcriptRef = A2(null);
    const [stage, setStage] = d2(1);
    const [rtState, setRtState] = d2("loading");
    const [micState, setMicState] = d2("hidden");
    const [turnProgress, setTurnProgress] = d2(0);
    const turnProgressRef = A2(0);
    const [coinAnimDone, setCoinAnimDone] = d2(false);
    const handleCoinAnimComplete = q2(() => setCoinAnimDone(true), []);
    y2(() => {
      turnProgressRef.current = turnProgress;
    }, [turnProgress]);
    y2(() => {
      if (onStageChange) onStageChange(stage);
    }, [stage, onStageChange]);
    const statusText = micState === "recording" ? "Tap when done speaking" : micState === "ready" ? "Tap to speak" : ["COACH_SPEAKING", "PARTNER_SPEAKING"].includes(rtState) ? "Listen" : rtState === "loading" ? "Connecting..." : "";
    const getTranscriptAPI = q2(() => {
      return transcriptRef.current?._transcriptAPI;
    }, []);
    const handleRTEvent = q2((evt) => {
      const { type, data } = evt;
      const api = getTranscriptAPI();
      switch (type) {
        case "state": {
          const s3 = data.state;
          setRtState(s3);
          switch (s3) {
            case "SESSION_STARTED":
            case "BEGIN_SIMULATION":
              setMicState("hidden");
              if (api) api.clearText();
              break;
            case "COACH_SPEAKING":
              setMicState("hidden");
              break;
            case "PARTNER_SPEAKING":
              setMicState("hidden");
              if (onActiveSpeakerChange) onActiveSpeakerChange("partner");
              if (data.partnerTurnCount) {
                setTurnProgress((data.partnerTurnCount - 1) * 2 + 1);
              }
              break;
            case "PUSH_TO_TALK_READY":
              setMicState("ready");
              if (onActiveSpeakerChange) onActiveSpeakerChange("user");
              if (data.userTurnCount) {
                setTurnProgress(data.userTurnCount * 2);
              }
              break;
            case "PUSH_TO_TALK_ACTIVE":
              setMicState("recording");
              break;
            case "PUSH_TO_TALK_STOPPED":
              setMicState("waiting");
              if (onActiveSpeakerChange) {
                onActiveSpeakerChange(turnProgressRef.current >= TOTAL_TURNS ? null : "partner");
              }
              break;
            case "LOADING_SIMULATION":
              setStage("transition");
              setMicState("hidden");
              if (onActiveSpeakerChange) onActiveSpeakerChange(null);
              if (api) api.clearText();
              break;
            case "SESSION_COMPLETED":
              setStage(3);
              setMicState("hidden");
              if (onActiveSpeakerChange) onActiveSpeakerChange(null);
              break;
            case "SESSION_ERROR":
            case "SESSION_STOPPED":
              setMicState("hidden");
              break;
            default:
              break;
          }
          break;
        }
        case "coach_text_delta":
        case "partner_text_delta": {
          const textType = type === "partner_text_delta" ? "assistant" : "coach";
          if (api) api.updateText(textType, data.text || "", false, { duration_ms: data.durationMs });
          break;
        }
        case "coach_text":
          if (api) api.updateText("coach", data.text || "", true);
          sendToBubble("bubble_fn_simulator", "session_event", { role: "coach", payload: data.text || "" });
          break;
        case "partner_text":
          if (api) api.updateText("assistant", data.text || "", true);
          sendToBubble("bubble_fn_simulator", "session_event", { role: "partner", payload: data.text || "" });
          break;
        case "user_speaking":
          if (api) api.updateText("user", data.text || "...", false);
          break;
        case "user_transcript":
          if (api) api.updateText("user", data.text || "", true);
          sendToBubble("bubble_fn_simulator", "session_event", { role: "user", payload: data.text || "" });
          break;
        case "stage2_token_needed":
          if (onStage2Start) onStage2Start();
          sendToBubble("bubble_fn_simulator", "session_event", {
            role: "prompt",
            payload: JSON.stringify({ issue: data.issue, jsonContext: data.jsonContext })
          });
          sendToBubble("bubble_fn_simulator", "stage2_token_needed", {
            issue: data.issue,
            jsonContext: data.jsonContext
          });
          break;
        case "evaluation_json_received":
          console.log("[Simulator] evaluation data:", JSON.stringify(data));
          if (onComplete) onComplete(data.evaluationJson || data);
          break;
        case "error": {
          console.error("[Simulator] RT error:", data.message, data.code);
          const code = data.code || "";
          let msg;
          if (code === "PERMISSION_DENIED") {
            msg = "Microphone access is required. Please enable it in your device settings and try again.";
          } else if (code === "NO_MICROPHONE") {
            msg = "No microphone detected. Please connect a microphone and try again.";
          } else if (code === "CONNECTION_FAILED" || code === "ICE_CONNECTION_FAILED") {
            msg = "Connection lost. Please check your internet connection and try again.";
          } else if (code === "TRACK_ENDED") {
            msg = "Microphone was disconnected during the session. Please reconnect and try again.";
          } else {
            msg = "Something went wrong. Please try again.";
          }
          if (onError) onError(msg, code);
          break;
        }
        default:
          break;
      }
    }, [onComplete, onStage2Start, onActiveSpeakerChange, onError, getTranscriptAPI]);
    y2(() => {
      onRTEvent(handleRTEvent);
      return () => offRTEvent(handleRTEvent);
    }, [handleRTEvent]);
    y2(() => {
      if (rtState === "PARTNER_SPEAKING" && stage === "transition") {
        setCoinAnimDone(true);
        setStage(2);
      }
    }, [rtState, stage]);
    const handleMicPress = () => {
      if (micState === "ready") {
        tts_default.unlockAudio();
        realtime_default.startPushToTalk();
      } else if (micState === "recording") {
        realtime_default.stopPushToTalk();
      }
    };
    if (stage === "transition") {
      if (!coinAnimDone) {
        return /* @__PURE__ */ xn.createElement(
          CoinDeduction_default,
          {
            coinCount,
            coinCost: 4,
            onComplete: handleCoinAnimComplete
          }
        );
      }
      return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col items-center justify-center h-full px-6", style: { background: sim.sessionBg } }, /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-bold text-[1.25rem] text-center", style: { color: sim.transitionText } }, "Your simulation will begin shortly"), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement("svg", { width: "32", height: "32", viewBox: "0 0 24 24", fill: "none", className: "animate-spin", style: { animationDuration: "1.5s" } }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10", stroke: sim.transitionSubtext, strokeWidth: "2", strokeDasharray: "50 20", strokeLinecap: "round" }))));
    }
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full", style: { background: sim.sessionBg } }, /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 flex items-center justify-between px-4 pt-4 pb-2" }, /* @__PURE__ */ xn.createElement("button", { onClick: onClose, className: "p-2 rounded-full", style: { color: sim.statusText } }, /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("line", { x1: "18", y1: "6", x2: "6", y2: "18" }), /* @__PURE__ */ xn.createElement("line", { x1: "6", y1: "6", x2: "18", y2: "18" }))), stage === 2 && /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-1.5" }, Array.from({ length: TOTAL_TURNS }).map((_4, i3) => {
      const turnColor = i3 % 2 === 0 ? "#E1327F" : "#6D6987";
      const active = i3 < turnProgress;
      return /* @__PURE__ */ xn.createElement(
        "div",
        {
          key: i3,
          className: "rounded-full border border-solid transition-[background-color] duration-300",
          style: {
            width: 20,
            height: 8,
            borderRadius: 4,
            background: active ? turnColor : sim.dotInactive,
            borderColor: active ? turnColor : sim.dotBorder
          }
        }
      );
    })), /* @__PURE__ */ xn.createElement("div", { className: "w-10" })), /* @__PURE__ */ xn.createElement("div", { className: "flex-1" }), /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 flex flex-col items-center px-6" }, /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-bold text-[20px] mb-4", style: { color: sim.statusText } }, statusText), /* @__PURE__ */ xn.createElement("div", { className: "relative w-4/5", style: { height: 72 } }, /* @__PURE__ */ xn.createElement(
      Waveform_default,
      {
        rtState,
        buttonVisible: micState !== "hidden",
        theme
      }
    ), micState !== "hidden" && /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 flex items-center justify-center", style: { zIndex: 10 } }, /* @__PURE__ */ xn.createElement(MicButton_default, { state: micState, onPress: handleMicPress, theme })))), /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 px-3 pt-3", style: { paddingBottom: "1.875em" } }, /* @__PURE__ */ xn.createElement(Transcript_default, { ref: transcriptRef, theme })));
  };
  var SimulatorSession_default = SimulatorSession;

  // src/hooks/useTTS.js
  function useTTS() {
    const [status, setStatus] = d2("idle");
    const [progress, setProgress] = d2({ elapsed: 0, total: 0, percent: 0 });
    const [sentences, setSentences] = d2([]);
    const [stats, setStats] = d2(null);
    const [error, setError] = d2(null);
    y2(() => {
      const handler = (evt) => {
        switch (evt.type) {
          case "status":
            setStatus(evt.data.status);
            if (evt.data.status === "streaming") {
              setError(null);
              setStats(null);
            }
            break;
          case "progress":
            setProgress(evt.data);
            break;
          case "transcript":
            setSentences(evt.data.sentences);
            break;
          case "stats":
            setStats(evt.data);
            break;
          case "error":
            setError(evt.data.message);
            break;
        }
      };
      onTTSEvent(handler);
      return () => offTTSEvent(handler);
    }, []);
    return { status, progress, sentences, stats, error };
  }

  // src/components/simulator/SimulatorResults.jsx
  var SCORE_COLORS = ["#FF4B4B", "#FF8C00", "#FFD700", "#9ACD32", "#58CC02"];
  var SPEED_OPTIONS = [1, 1.25, 1.5, 1.75, 2];
  var beepAudioCtx = null;
  function getBeepAudioCtx() {
    if (beepAudioCtx && beepAudioCtx.state === "closed") beepAudioCtx = null;
    if (!beepAudioCtx) {
      try {
        beepAudioCtx = new (window.AudioContext || window.webkitAudioContext)();
        if (beepAudioCtx.state === "suspended") beepAudioCtx.resume();
      } catch {
      }
    }
    return beepAudioCtx;
  }
  function playScoreBeep(index2) {
    try {
      const ctx = getBeepAudioCtx();
      if (!ctx) return;
      if (ctx.state === "suspended") ctx.resume();
      const osc = ctx.createOscillator();
      const gain = ctx.createGain();
      osc.connect(gain);
      gain.connect(ctx.destination);
      osc.frequency.value = 400 + index2 * 100;
      osc.type = "sine";
      gain.gain.value = 0.12;
      gain.gain.exponentialRampToValueAtTime(1e-3, ctx.currentTime + 0.15);
      osc.start(ctx.currentTime);
      osc.stop(ctx.currentTime + 0.15);
    } catch {
    }
  }
  function buildTTSText({ skill_level, strengths, improvements, summary }) {
    const strip = (s3) => s3.replace(/[.!?]+\s*$/, "");
    const parts = [skill_level + "."];
    if (strengths.length) {
      parts.push("What you did well: " + strengths.map(strip).join(". ") + ".");
    }
    if (improvements.length) {
      parts.push("Areas to improve: " + improvements.map(strip).join(". ") + ".");
    }
    if (summary) parts.push(summary);
    return parts.join(" ");
  }
  var SimulatorResults = ({ evaluation, theme, onRetry, onDone }) => {
    const sim = theme.simulator;
    const [expanded, setExpanded] = d2(false);
    const { status: ttsStatus } = useTTS();
    const [litDots, setLitDots] = d2(0);
    const [showTitle, setShowTitle] = d2(false);
    const [showFraction, setShowFraction] = d2(false);
    const [visibleMetrics, setVisibleMetrics] = d2(0);
    const [showStrengthsHeader, setShowStrengthsHeader] = d2(false);
    const [visibleStrengths, setVisibleStrengths] = d2(0);
    const [showImprovementsHeader, setShowImprovementsHeader] = d2(false);
    const [visibleImprovements, setVisibleImprovements] = d2(0);
    const [showSummary, setShowSummary] = d2(false);
    const [showFooter, setShowFooter] = d2(false);
    const [speedIndex, setSpeedIndex] = d2(0);
    const timersRef = A2([]);
    const addTimer = q2((fn2, ms) => {
      const id = setTimeout(fn2, ms);
      timersRef.current.push(id);
      return id;
    }, []);
    const { overall_score = 1, skill_level = "Getting Started", metrics = [], strengths = [], improvements = [], summary = "" } = evaluation || {};
    y2(() => {
      const timers = timersRef.current;
      return () => {
        timers.forEach(clearTimeout);
        tts_default.stop();
      };
    }, []);
    y2(() => {
      if (!evaluation) return;
      let t3 = 200;
      addTimer(() => setShowTitle(true), t3);
      t3 += 400;
      for (let i3 = 1; i3 <= overall_score; i3++) {
        addTimer(() => {
          setLitDots(i3);
          playScoreBeep(i3 - 1);
        }, t3);
        t3 += 300;
      }
      t3 += 200;
      addTimer(() => setShowFraction(true), t3);
      t3 += 300;
      for (let i3 = 1; i3 <= metrics.length; i3++) {
        addTimer(() => setVisibleMetrics(i3), t3);
        t3 += 200;
      }
      t3 += 200;
      if (strengths.length > 0) {
        addTimer(() => setShowStrengthsHeader(true), t3);
        t3 += 250;
        for (let i3 = 1; i3 <= strengths.length; i3++) {
          addTimer(() => setVisibleStrengths(i3), t3);
          t3 += 200;
        }
        t3 += 200;
      }
      if (improvements.length > 0) {
        addTimer(() => setShowImprovementsHeader(true), t3);
        t3 += 250;
        for (let i3 = 1; i3 <= improvements.length; i3++) {
          addTimer(() => setVisibleImprovements(i3), t3);
          t3 += 200;
        }
        t3 += 200;
      }
      if (summary) {
        addTimer(() => setShowSummary(true), t3);
        t3 += 200;
      }
      addTimer(() => setShowFooter(true), t3);
      t3 += 500;
      addTimer(() => {
        try {
          const raw = localStorage.getItem("bonds_simulator_templates");
          const apiKey = raw ? JSON.parse(raw).ttsApiKey : null;
          if (apiKey) {
            const text = buildTTSText({ skill_level, strengths, improvements, summary });
            tts_default.start({ apiKey, text });
          }
        } catch {
        }
      }, t3);
    }, [evaluation, overall_score, metrics.length, strengths, improvements, summary, skill_level, addTimer]);
    if (!evaluation) return null;
    const fadeIn = (visible) => ({
      opacity: visible ? 1 : 0,
      transform: visible ? "translateY(0) translateZ(0)" : "translateY(0.5em) translateZ(0)",
      transition: "opacity 0.4s ease, transform 0.4s ease"
    });
    const handlePlayPause = () => {
      if (ttsStatus === "streaming") tts_default.pause();
      else if (ttsStatus === "paused") tts_default.resume();
    };
    const handleSpeedCycle = () => {
      const next = (speedIndex + 1) % SPEED_OPTIONS.length;
      setSpeedIndex(next);
      tts_default.setSpeed(SPEED_OPTIONS[next]);
    };
    const handleReplay = () => {
      try {
        const raw = localStorage.getItem("bonds_simulator_templates");
        const apiKey = raw ? JSON.parse(raw).ttsApiKey : null;
        if (apiKey) {
          const text = buildTTSText({ skill_level, strengths, improvements, summary });
          tts_default.start({ apiKey, text });
        }
      } catch {
      }
    };
    const handleRetry = () => {
      if (!onRetry) return;
      tts_default.stop();
      onRetry();
    };
    const handleDone = () => {
      tts_default.stop();
      onDone();
    };
    const showPlayPause = ttsStatus === "streaming" || ttsStatus === "paused";
    const showReplay = ttsStatus === "done" || ttsStatus === "idle";
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full", style: { background: sim.sessionBg } }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 overflow-y-auto scrollbar-hide px-5 pt-6 pb-4" }, /* @__PURE__ */ xn.createElement("div", { className: "text-center mb-6" }, /* @__PURE__ */ xn.createElement(
      "h2",
      {
        className: "font-jakarta font-extrabold text-[24px] mb-3",
        style: { color: sim.resultsTitle, ...fadeIn(showTitle) }
      },
      skill_level
    ), /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-center gap-2 mb-2" }, SCORE_COLORS.map((color, i3) => {
      const lit = i3 < litDots;
      const isScoreDot = i3 === overall_score - 1;
      return /* @__PURE__ */ xn.createElement(
        "div",
        {
          key: i3,
          className: "rounded-full",
          style: {
            width: lit && isScoreDot ? 20 : 16,
            height: lit && isScoreDot ? 20 : 16,
            background: lit ? color : sim.dotInactive,
            border: lit ? "none" : `1px solid ${sim.dotBorder}`,
            transition: "background 0.3s ease, width 0.3s ease, height 0.3s ease"
          }
        }
      );
    })), /* @__PURE__ */ xn.createElement(
      "p",
      {
        className: "font-poppins text-[14px]",
        style: { color: sim.scoreFraction, ...fadeIn(showFraction) }
      },
      overall_score,
      "/5"
    )), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col gap-2 mb-6" }, metrics.map((metric, i3) => /* @__PURE__ */ xn.createElement(
      "div",
      {
        key: i3,
        className: "flex items-center gap-3 py-2",
        style: fadeIn(i3 < visibleMetrics)
      },
      /* @__PURE__ */ xn.createElement("span", { className: "text-[18px]", style: { color: metric.direction === "up" ? sim.metricUp : sim.metricDown } }, metric.direction === "up" ? "\u2191" : "\u2193"),
      /* @__PURE__ */ xn.createElement("span", { className: "font-poppins font-medium text-[15px]", style: { color: sim.metricText } }, metric.name)
    ))), strengths.length > 0 && /* @__PURE__ */ xn.createElement("div", { className: "mb-5" }, /* @__PURE__ */ xn.createElement(
      "h3",
      {
        className: "font-jakarta font-bold text-[14px] mb-2",
        style: { color: sim.sectionTitle, ...fadeIn(showStrengthsHeader) }
      },
      "What you did well"
    ), /* @__PURE__ */ xn.createElement("ul", { className: "flex flex-col gap-1.5" }, strengths.map((item, i3) => /* @__PURE__ */ xn.createElement(
      "li",
      {
        key: i3,
        className: "font-poppins text-[13px] leading-relaxed",
        style: { color: sim.bulletText, ...fadeIn(i3 < visibleStrengths) }
      },
      "\u2022",
      " ",
      item
    )))), improvements.length > 0 && /* @__PURE__ */ xn.createElement("div", { className: "mb-5" }, /* @__PURE__ */ xn.createElement(
      "h3",
      {
        className: "font-jakarta font-bold text-[14px] mb-2",
        style: { color: sim.sectionTitleImprove, ...fadeIn(showImprovementsHeader) }
      },
      "Areas to improve"
    ), /* @__PURE__ */ xn.createElement("ul", { className: "flex flex-col gap-1.5" }, improvements.map((item, i3) => /* @__PURE__ */ xn.createElement(
      "li",
      {
        key: i3,
        className: "font-poppins text-[13px] leading-relaxed",
        style: { color: sim.bulletText, ...fadeIn(i3 < visibleImprovements) }
      },
      "\u2022",
      " ",
      item
    )))), summary && /* @__PURE__ */ xn.createElement("div", { className: "mb-4", style: fadeIn(showSummary) }, expanded && /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[13px] leading-relaxed mb-2", style: { color: sim.bulletText } }, summary), /* @__PURE__ */ xn.createElement("button", { onClick: () => setExpanded(!expanded), className: "font-jakarta font-bold text-[13px] underline", style: { color: sim.readMoreText } }, expanded ? "Read less" : "Read more"))), showPlayPause && /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 flex items-center justify-center gap-3 pb-3" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handlePlayPause,
        className: "w-11 h-11 rounded-full flex items-center justify-center",
        style: { background: sim.dotInactive, border: `1px solid ${sim.resultsBorder}` }
      },
      ttsStatus === "streaming" ? /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: sim.resultsTitle }, /* @__PURE__ */ xn.createElement("rect", { x: "6", y: "4", width: "4", height: "16", rx: "1" }), /* @__PURE__ */ xn.createElement("rect", { x: "14", y: "4", width: "4", height: "16", rx: "1" })) : /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: sim.resultsTitle }, /* @__PURE__ */ xn.createElement("polygon", { points: "6,4 20,12 6,20" }))
    ), TTS_BACKEND !== "pcm" && /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleSpeedCycle,
        className: "h-11 px-3 rounded-full flex items-center justify-center font-jakarta font-bold text-[13px]",
        style: { background: sim.dotInactive, border: `1px solid ${sim.resultsBorder}`, color: sim.resultsTitle }
      },
      SPEED_OPTIONS[speedIndex],
      "x"
    )), showReplay && showFooter && /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 flex items-center justify-center pb-3" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleReplay,
        className: "w-11 h-11 rounded-full flex items-center justify-center",
        style: { background: sim.dotInactive, border: `1px solid ${sim.resultsBorder}` }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: sim.resultsTitle, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "1 4 1 10 7 10" }), /* @__PURE__ */ xn.createElement("path", { d: "M3.51 15a9 9 0 1 0 2.13-9.36L1 10" }))
    )), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "shrink-0 flex items-center border-t border-solid",
        style: {
          borderColor: sim.resultsBorder,
          background: sim.resultsBg,
          transform: showFooter ? "translateY(0) translateZ(0)" : "translateY(100%) translateZ(0)",
          transition: "transform 0.5s cubic-bezier(0.16, 1, 0.3, 1)"
        }
      },
      onRetry && /* @__PURE__ */ xn.createElement(xn.Fragment, null, /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleRetry,
          className: "flex-1 py-4 text-center"
        },
        /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[14px] block", style: { color: sim.resultsTitle } }, "TRY AGAIN"),
        /* @__PURE__ */ xn.createElement("span", { className: "font-poppins text-[11px]", style: { color: sim.scoreFraction } }, "Same topic")
      ), /* @__PURE__ */ xn.createElement("div", { style: { width: 1, height: 40, background: sim.resultsBorder } })),
      /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleDone,
          className: "flex-1 py-4 text-center"
        },
        /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[14px]", style: { color: sim.resultsTitle } }, "DONE")
      )
    ));
  };
  var SimulatorResults_default = SimulatorResults;

  // src/components/simulator/FaceOverlay.jsx
  var beamAudioCtx = null;
  function getBeamAudioCtx() {
    if (beamAudioCtx && beamAudioCtx.state === "closed") beamAudioCtx = null;
    if (!beamAudioCtx) {
      try {
        beamAudioCtx = new (window.AudioContext || window.webkitAudioContext)();
        if (beamAudioCtx.state === "suspended") beamAudioCtx.resume();
      } catch {
      }
    }
    return beamAudioCtx;
  }
  function playBeamIn() {
    try {
      const ctx = getBeamAudioCtx();
      if (!ctx) return;
      const now = ctx.currentTime;
      const dur = 0.7;
      const bufLen = ctx.sampleRate * 0.15;
      const noiseBuf = ctx.createBuffer(1, bufLen, ctx.sampleRate);
      const noiseData = noiseBuf.getChannelData(0);
      for (let i3 = 0; i3 < bufLen; i3++) noiseData[i3] = (Math.random() * 2 - 1) * 0.3;
      const noise = ctx.createBufferSource();
      noise.buffer = noiseBuf;
      const noiseFilt = ctx.createBiquadFilter();
      noiseFilt.type = "bandpass";
      noiseFilt.frequency.setValueAtTime(2e3, now);
      noiseFilt.frequency.exponentialRampToValueAtTime(6e3, now + 0.12);
      noiseFilt.Q.value = 1.5;
      const noiseGain = ctx.createGain();
      noiseGain.gain.setValueAtTime(0.06, now);
      noiseGain.gain.exponentialRampToValueAtTime(1e-3, now + 0.15);
      noise.connect(noiseFilt).connect(noiseGain).connect(ctx.destination);
      noise.start(now);
      const osc1 = ctx.createOscillator();
      const g1 = ctx.createGain();
      osc1.type = "sine";
      osc1.frequency.setValueAtTime(261, now);
      osc1.frequency.exponentialRampToValueAtTime(784, now + dur);
      g1.gain.setValueAtTime(0, now);
      g1.gain.linearRampToValueAtTime(0.07, now + 0.08);
      g1.gain.exponentialRampToValueAtTime(1e-3, now + dur);
      osc1.connect(g1).connect(ctx.destination);
      const osc2 = ctx.createOscillator();
      const g22 = ctx.createGain();
      osc2.type = "triangle";
      osc2.frequency.setValueAtTime(330, now);
      osc2.frequency.exponentialRampToValueAtTime(988, now + dur);
      g22.gain.setValueAtTime(0, now);
      g22.gain.linearRampToValueAtTime(0.05, now + 0.1);
      g22.gain.exponentialRampToValueAtTime(1e-3, now + dur);
      osc2.connect(g22).connect(ctx.destination);
      const osc3 = ctx.createOscillator();
      const g32 = ctx.createGain();
      osc3.type = "sine";
      osc3.frequency.setValueAtTime(392, now + 0.05);
      osc3.frequency.exponentialRampToValueAtTime(1175, now + dur);
      g32.gain.setValueAtTime(0, now);
      g32.gain.linearRampToValueAtTime(0.04, now + 0.15);
      g32.gain.exponentialRampToValueAtTime(1e-3, now + dur);
      osc3.connect(g32).connect(ctx.destination);
      const osc4 = ctx.createOscillator();
      const g4 = ctx.createGain();
      osc4.type = "sine";
      osc4.frequency.setValueAtTime(523, now + 0.1);
      osc4.frequency.exponentialRampToValueAtTime(2093, now + dur);
      g4.gain.setValueAtTime(0, now);
      g4.gain.linearRampToValueAtTime(0.025, now + 0.2);
      g4.gain.exponentialRampToValueAtTime(1e-3, now + dur);
      osc4.connect(g4).connect(ctx.destination);
      [osc1, osc2, osc3, osc4].forEach((o3) => {
        o3.start(now);
        o3.stop(now + dur);
      });
    } catch {
    }
  }
  function playBeamOut() {
    try {
      const ctx = getBeamAudioCtx();
      if (!ctx) return;
      const now = ctx.currentTime;
      const dur = 0.5;
      const osc1 = ctx.createOscillator();
      const g1 = ctx.createGain();
      osc1.type = "sine";
      osc1.frequency.setValueAtTime(784, now);
      osc1.frequency.exponentialRampToValueAtTime(196, now + dur);
      g1.gain.setValueAtTime(0.06, now);
      g1.gain.exponentialRampToValueAtTime(1e-3, now + dur);
      osc1.connect(g1).connect(ctx.destination);
      const osc2 = ctx.createOscillator();
      const g22 = ctx.createGain();
      osc2.type = "triangle";
      osc2.frequency.setValueAtTime(988, now);
      osc2.frequency.exponentialRampToValueAtTime(247, now + dur);
      g22.gain.setValueAtTime(0.04, now);
      g22.gain.exponentialRampToValueAtTime(1e-3, now + dur * 0.8);
      osc2.connect(g22).connect(ctx.destination);
      const bufLen = ctx.sampleRate * 0.3;
      const noiseBuf = ctx.createBuffer(1, bufLen, ctx.sampleRate);
      const noiseData = noiseBuf.getChannelData(0);
      for (let i3 = 0; i3 < bufLen; i3++) noiseData[i3] = (Math.random() * 2 - 1) * 0.15;
      const noise = ctx.createBufferSource();
      noise.buffer = noiseBuf;
      const filt = ctx.createBiquadFilter();
      filt.type = "lowpass";
      filt.frequency.setValueAtTime(4e3, now);
      filt.frequency.exponentialRampToValueAtTime(200, now + 0.3);
      const ng = ctx.createGain();
      ng.gain.setValueAtTime(0.04, now);
      ng.gain.exponentialRampToValueAtTime(1e-3, now + 0.3);
      noise.connect(filt).connect(ng).connect(ctx.destination);
      noise.start(now + 0.02);
      [osc1, osc2].forEach((o3) => {
        o3.start(now);
        o3.stop(now + dur);
      });
    } catch {
    }
  }
  var Phase1Left = ({ fill = "#6D6987", fillOpacity = 0.7 }) => /* @__PURE__ */ xn.createElement("svg", { width: "127", height: "276", viewBox: "0 0 127 276", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ xn.createElement("path", { d: "M122.919 58.6497L81.0704 0H0V276H16.5548C18.4716 276 19.4299 276 20.3842 275.939C21.2322 275.885 22.0776 275.795 22.918 275.669C23.8634 275.527 24.8 275.325 26.6727 274.921L32.7096 273.62C37.2347 272.644 39.4973 272.156 41.6315 271.342C43.5265 270.619 45.3434 269.706 47.0541 268.616C48.9807 267.389 50.7223 265.865 54.2052 262.816L55.8845 261.346C57.8955 259.586 58.9006 258.706 59.8178 257.747C61.0747 256.434 62.2095 255.009 63.2084 253.49C63.9375 252.381 64.5701 251.204 65.8353 248.851C67.2239 246.268 67.9181 244.977 68.4796 243.635C69.2488 241.797 69.8331 239.888 70.2237 237.934C70.5088 236.508 70.6558 235.049 70.9499 232.132L71.0654 230.986C71.2537 229.118 71.3478 228.184 71.3834 227.249C71.4151 226.417 71.4121 225.584 71.3745 224.753C71.3322 223.817 71.2315 222.884 71.0299 221.018L70.3609 214.823C70.1583 212.948 69.7378 211.102 69.1079 209.323L67.5803 205.01C65.1272 198.083 67.2177 190.362 72.83 185.619L79.54 179.948C85.1426 175.213 84.6554 166.433 78.5634 162.347L76.709 161.103C68.9965 155.93 71.6771 143.977 80.8599 142.593C87.7076 141.562 91.5457 134.138 88.429 127.954L79.7675 110.769C73.6455 98.622 85.0616 85.0585 98.0752 89.0177C100.203 89.6649 102.445 89.8457 104.648 89.5476L109.33 88.9142C111.359 88.6397 113.334 88.0548 115.186 87.1801L115.528 87.0186C120.282 84.7728 123.9 80.6671 125.53 75.6685C127.41 69.9027 126.441 63.5866 122.919 58.6497Z", fill, fillOpacity }));
  var Phase1Right = ({ fill = "#6D6987", fillOpacity = 0.7 }) => /* @__PURE__ */ xn.createElement("svg", { width: "127", height: "276", viewBox: "0 0 127 276", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ xn.createElement("path", { d: "M3.55213 58.6497L45.4004 0H126.471V276H109.916C107.999 276 107.041 276 106.087 275.939C105.238 275.885 104.393 275.795 103.553 275.669C102.607 275.527 101.671 275.325 99.798 274.921L93.7611 273.62C89.236 272.644 86.9734 272.156 84.8392 271.342C82.9442 270.619 81.1273 269.706 79.4166 268.616C77.49 267.389 75.7484 265.865 72.2655 262.816L70.5862 261.346C68.5752 259.586 67.5701 258.706 66.6529 257.747C65.396 256.434 64.2612 255.009 63.2623 253.49C62.5332 252.381 61.9006 251.204 60.6354 248.851C59.2468 246.268 58.5526 244.977 57.9911 243.635C57.2219 241.797 56.6376 239.888 56.247 237.934C55.9619 236.508 55.8149 235.049 55.5208 232.132L55.4053 230.986C55.217 229.118 55.1229 228.184 55.0873 227.249C55.0556 226.417 55.0586 225.584 55.0962 224.753C55.1385 223.817 55.2392 222.884 55.4408 221.018L56.1098 214.823C56.3124 212.948 56.7329 211.102 57.3628 209.323L58.8904 205.01C61.3435 198.083 59.253 190.362 53.6407 185.619L46.9307 179.948C41.3281 175.213 41.8153 166.433 47.9073 162.347L49.7617 161.103C57.4742 155.93 54.7936 143.977 45.6108 142.593C38.7631 141.562 34.925 134.138 38.0417 127.954L46.7032 110.769C52.8252 98.622 41.4091 85.0585 28.3955 89.0177C26.268 89.6649 24.026 89.8457 21.8223 89.5476L17.1408 88.9142C15.1115 88.6397 13.1364 88.0548 11.2848 87.1801L10.9429 87.0186C6.18901 84.7728 2.57088 80.6671 0.940765 75.6685C-0.939575 69.9027 0.0295715 63.5866 3.55213 58.6497Z", fill, fillOpacity }));
  var Phase2Left = ({ fill = "#E1327F", fillOpacity = 0.3 }) => /* @__PURE__ */ xn.createElement("svg", { width: "122", height: "511", viewBox: "0 0 122 511", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ xn.createElement("path", { d: "M117.739 299.361L69.3659 231.025C66.1098 226.425 64.4817 224.125 63.5621 221.587C62.7473 219.339 62.3423 216.963 62.3663 214.572C62.3933 211.874 63.1674 209.164 64.7156 203.745L66.5 197.5L70 175L72.0228 159.223C72.2015 157.828 72.2908 157.131 72.3311 156.432C72.3668 155.81 72.3736 155.187 72.3512 154.565C72.326 153.864 72.2517 153.166 72.103 151.768L70 132L61.4583 96.9341C61.0995 95.4611 60.9201 94.7247 60.6864 94.0062C60.4788 93.3677 60.2391 92.7401 59.9683 92.1257C59.6635 91.4343 59.3064 90.7657 58.592 89.4286L41 56.5L20.5 25L0 0V507.5L20.3595 510.195C22.8917 510.53 24.1579 510.697 25.411 510.784C31.0144 511.17 36.6365 510.372 41.911 508.441C43.0903 508.009 44.2595 507.496 46.5976 506.469C48.0591 505.828 48.7893 505.507 49.4744 505.141C52.5438 503.498 55.1313 501.083 56.981 498.134C57.3939 497.475 57.7638 496.769 58.5037 495.357L62.6225 487.493C63.6975 485.441 64.2351 484.415 64.6483 483.342C65.0155 482.389 65.3091 481.409 65.5265 480.411C65.7713 479.287 65.8865 478.135 66.1171 475.829L66.6702 470.298C66.7938 469.062 66.8555 468.445 66.8789 467.826C66.8997 467.276 66.8977 466.725 66.8731 466.175C66.8453 465.556 66.7792 464.939 66.647 463.705L65.9846 457.523C65.8475 456.243 65.779 455.604 65.6704 454.974C65.4493 453.691 65.1033 452.432 64.6375 451.216C64.4087 450.619 64.1408 450.034 63.6049 448.865L59.5858 440.096C56.5985 433.578 58.4407 425.864 64.0505 421.399L68.6263 417.757C73.9841 413.492 74.0834 405.386 68.8316 400.992L68.3237 400.567C62.1708 395.418 63.8852 385.542 71.4132 382.769C77.4738 380.536 80.0719 373.39 76.8607 367.786L72.0905 359.462C65.3164 347.64 73.1749 332.794 86.7596 331.749L94.5273 331.152C94.8187 331.129 94.9644 331.118 95.1097 331.105C96.0331 331.02 96.9494 330.872 97.8521 330.66C97.9941 330.626 98.1359 330.591 98.4195 330.52L106.984 328.379C112.945 326.889 117.797 322.573 119.971 316.826C122.177 310.996 121.341 304.449 117.739 299.361Z", fill, fillOpacity }));
  var Phase2Right = ({ fill = "#6D6987", fillOpacity = 0.3 }) => /* @__PURE__ */ xn.createElement("svg", { width: "126", height: "518", viewBox: "0 0 126 518", fill: "none", xmlns: "http://www.w3.org/2000/svg" }, /* @__PURE__ */ xn.createElement("path", { d: "M3.5253 300.159L48.5929 236.492C55.9971 226.032 55.9251 212.02 48.4139 201.637C45.5002 197.609 43.635 192.919 42.9865 187.991L42.2985 182.762C41.9901 180.418 41.8359 179.246 41.7743 178.069C41.7195 177.024 41.7195 175.976 41.7743 174.931C41.8359 173.754 41.9901 172.582 42.2985 170.238L43.9746 157.5L48.3418 132.607C48.579 131.255 48.6975 130.579 48.8467 129.91C48.9793 129.316 49.1299 128.725 49.2985 128.14C49.4881 127.481 49.7078 126.831 50.1473 125.531L59.706 97.253C60.1814 95.8466 60.4191 95.1434 60.6909 94.4538C60.9324 93.8408 61.1941 93.2359 61.4755 92.6402C61.792 91.9699 62.1418 91.3152 62.8413 90.0057L80.4746 57L98.5001 29.3024C99.4272 27.8778 99.8907 27.1655 100.393 26.4817C100.839 25.874 101.308 25.2834 101.799 24.7111C102.351 24.0671 102.94 23.4543 104.118 22.2286L125.475 0V517.5H109.132C107.2 517.5 106.234 517.5 105.273 517.438C104.418 517.383 103.566 517.292 102.719 517.164C101.767 517.02 100.824 516.815 98.9378 516.405L93.1743 515.152C88.6296 514.164 86.3572 513.67 84.2152 512.847C82.3133 512.117 80.4907 511.194 78.7757 510.094C76.8443 508.856 75.1005 507.317 71.6131 504.24L70.0654 502.874C68.0499 501.096 67.0422 500.207 66.1236 499.238C64.8843 497.931 63.7651 496.515 62.7798 495.008C62.0494 493.89 61.417 492.704 60.1521 490.333C58.7705 487.742 58.0799 486.447 57.5223 485.102C56.77 483.288 56.1978 481.403 55.814 479.476C55.5295 478.049 55.3834 476.588 55.0913 473.667L54.9693 472.447C54.784 470.594 54.6913 469.667 54.6563 468.739C54.6251 467.914 54.628 467.087 54.665 466.263C54.7066 465.334 54.8058 464.409 55.0042 462.557L55.6724 456.32C55.8732 454.446 56.2901 452.602 56.9149 450.824L58.443 446.474C60.8721 439.561 58.7946 431.865 53.2163 427.113L46.5512 421.436C40.983 416.692 41.4692 407.947 47.529 403.851L49.4437 402.556C57.1055 397.376 54.4233 385.486 45.2798 384.097C38.462 383.061 34.6353 375.679 37.7193 369.511L46.4442 352.061C52.4834 339.982 41.1155 326.55 28.2048 330.509C26.0768 331.162 23.8323 331.344 21.6269 331.043L17.0337 330.417C15.0065 330.141 13.0343 329.552 11.1871 328.673L10.9325 328.551C6.17203 326.284 2.55495 322.157 0.931908 317.141C-0.929359 311.388 0.0317917 305.094 3.5253 300.159Z", fill, fillOpacity }));
  var FaceOverlay = ({ facePhase, glowSide, theme }) => {
    const sim = theme.simulator;
    const [displayPhase, setDisplayPhase] = d2(null);
    const [animState, setAnimState] = d2("idle");
    const prevPhaseRef = A2(null);
    const timeoutRef = A2(null);
    y2(() => {
      const prev = prevPhaseRef.current;
      prevPhaseRef.current = facePhase;
      if (timeoutRef.current) {
        clearTimeout(timeoutRef.current);
        timeoutRef.current = null;
      }
      if (facePhase && !prev) {
        setDisplayPhase(facePhase);
        setAnimState("entering");
        playBeamIn();
      } else if (facePhase && prev && facePhase !== prev) {
        setAnimState("exiting");
        playBeamOut();
        timeoutRef.current = setTimeout(() => {
          setDisplayPhase(facePhase);
          setAnimState("entering");
          playBeamIn();
        }, 400);
      } else if (!facePhase && prev) {
        setAnimState("exiting");
        playBeamOut();
        timeoutRef.current = setTimeout(() => {
          setDisplayPhase(null);
          setAnimState("idle");
        }, 400);
      }
      return () => {
        if (timeoutRef.current) clearTimeout(timeoutRef.current);
      };
    }, [facePhase]);
    if (!displayPhase && animState === "idle") return null;
    const leftClass = animState === "entering" ? "face-slide-in-left" : animState === "exiting" ? "face-slide-out-left" : "";
    const rightClass = animState === "entering" ? "face-slide-in-right" : animState === "exiting" ? "face-slide-out-right" : "";
    const isPhase2 = displayPhase === 2;
    const glowBase = {
      position: "absolute",
      top: 0,
      width: "40%",
      height: "70%",
      pointerEvents: "none",
      transition: "opacity 0.5s ease"
    };
    return /* @__PURE__ */ xn.createElement(xn.Fragment, null, isPhase2 && /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `pointer-events-none ${glowSide === "left" ? "face-glow-pulse" : ""}`,
        style: {
          ...glowBase,
          left: 0,
          zIndex: 0,
          opacity: glowSide === "left" ? 1 : 0,
          background: sim.glowPartner
        }
      }
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `absolute left-0 top-0 pointer-events-none ${leftClass}`,
        style: { zIndex: 1 }
      },
      isPhase2 ? /* @__PURE__ */ xn.createElement(Phase2Left, { fill: sim.facePartnerFill, fillOpacity: sim.facePartnerOpacity }) : /* @__PURE__ */ xn.createElement(Phase1Left, { fill: sim.facePhase1Fill, fillOpacity: sim.facePhase1Opacity })
    ), isPhase2 && /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `pointer-events-none ${glowSide === "right" ? "face-glow-pulse" : ""}`,
        style: {
          ...glowBase,
          right: 0,
          zIndex: 0,
          opacity: glowSide === "right" ? 1 : 0,
          background: sim.glowUser
        }
      }
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `absolute right-0 top-0 pointer-events-none ${rightClass}`,
        style: { zIndex: 1 }
      },
      isPhase2 ? /* @__PURE__ */ xn.createElement(Phase2Right, { fill: sim.faceUserFill, fillOpacity: sim.faceUserOpacity }) : /* @__PURE__ */ xn.createElement(Phase1Right, { fill: sim.facePhase1Fill, fillOpacity: sim.facePhase1Opacity })
    ));
  };
  var FaceOverlay_default = FaceOverlay;

  // src/components/SimulatorSection.jsx
  function normalizeEvaluation(raw) {
    if (!raw) return null;
    const rawScore = raw.score ?? raw.overall_score ?? 0;
    const score = raw.overall_score ?? Math.max(1, Math.min(5, Math.ceil(rawScore / 2)));
    const levels = ["Getting Started", "Warming Up", "Finding Your Groove", "Nice work!", "Nailed It"];
    const skillLevel = raw.skill_level || levels[Math.max(0, Math.min(score, 5) - 1)] || levels[0];
    const metrics = raw.metrics ?? (raw.dimensions ? Object.entries(raw.dimensions).map(([name5, direction]) => ({ name: name5, direction })) : []);
    return {
      overall_score: score,
      skill_level: skillLevel,
      metrics,
      strengths: raw.strengths ?? raw.what_went_well ?? [],
      improvements: raw.improvements ?? raw.what_could_be_better ?? [],
      summary: raw.summary ?? raw.next_time_tip ?? ""
    };
  }
  var templatesFetched = false;
  var SimulatorSection = ({ theme, onSessionChange, onFullScreenChange }) => {
    const [phase, setPhase] = d2("landing");
    const [hasRetried, setHasRetried] = d2(false);
    const [evaluation, setEvaluation] = d2(null);
    const [showCloseDialog, setShowCloseDialog] = d2(false);
    const [showErrorDialog, setShowErrorDialog] = d2(false);
    const [errorMessage, setErrorMessage] = d2("");
    const [errorCode, setErrorCode] = d2("");
    const [errorHasRefund, setErrorHasRefund] = d2(false);
    const [simStage, setSimStage] = d2(null);
    const [activeSpeaker, setActiveSpeaker] = d2(null);
    const coinsDeductedRef = A2(false);
    const pendingLeaveRef = A2(null);
    const [instructionsReady, setInstructionsReady] = d2(() => {
      try {
        const raw = localStorage.getItem("bonds_simulator_templates");
        if (raw) {
          const t3 = JSON.parse(raw);
          return !!(t3.simulationJsonInstructions && t3.scoreInstructions);
        }
      } catch {
      }
      return false;
    });
    y2(() => {
      if (templatesFetched) return;
      templatesFetched = true;
      sendToBubble("bubble_fn_simulator", "fetch_instructions");
    }, []);
    y2(() => {
      window.appUI.setSimulatorTemplates = (data) => {
        if (!data) return;
        const templates = {
          simulationJsonInstructions: data.simulationJsonInstructions || null,
          scoreInstructions: data.scoreInstructions || null,
          ttsApiKey: data.ttsApiKey || null
        };
        localStorage.setItem("bonds_simulator_templates", JSON.stringify(templates));
        setInstructionsReady(!!(templates.simulationJsonInstructions && templates.scoreInstructions));
      };
      return () => {
        delete window.appUI.setSimulatorTemplates;
      };
    }, []);
    const isFullScreen = phase === "session" || phase === "results";
    y2(() => {
      if (onFullScreenChange) onFullScreenChange(isFullScreen);
    }, [isFullScreen, onFullScreenChange]);
    y2(() => {
      if (onSessionChange) onSessionChange(phase === "session");
    }, [phase, onSessionChange]);
    const facePhase = phase === "session" ? simStage === 1 ? 1 : simStage === 2 ? 2 : null : null;
    const glowSide = facePhase === 2 ? activeSpeaker === "partner" ? "left" : activeSpeaker === "user" ? "right" : null : null;
    const handleStart = q2(() => {
      setPhase("session");
      setHasRetried(false);
      coinsDeductedRef.current = false;
      sendToBubble("bubble_fn_simulator", "start_session");
    }, []);
    y2(() => {
      window.appUI.onSimulatorToken = (token) => {
        if (token) realtime_default.start(token);
      };
      window.appUI.onSimulatorStage2Token = (token) => {
        if (token) realtime_default.start(token);
      };
      return () => {
        delete window.appUI.onSimulatorToken;
        delete window.appUI.onSimulatorStage2Token;
      };
    }, []);
    const handleStageChange = q2((stage) => {
      setSimStage(stage);
    }, []);
    const handleComplete = q2((evalData) => {
      const normalized = normalizeEvaluation(evalData);
      setEvaluation(normalized);
      setSimStage(null);
      setPhase("results");
      sendToBubble("bubble_fn_simulator", "session_complete", {
        score: normalized?.overall_score || 0,
        skillLevel: normalized?.skill_level || "",
        nextTimeTip: normalized?.summary || "",
        whatYouDidWell: normalized?.strengths || [],
        whatCouldBeBetter: normalized?.improvements || [],
        dimensions: JSON.stringify(normalized?.metrics || [])
      });
    }, []);
    const handleClose = q2(() => {
      setShowCloseDialog(true);
    }, []);
    const confirmClose = q2(() => {
      realtime_default.stop("stopped");
      setShowCloseDialog(false);
      setSimStage(null);
      setPhase("landing");
      setEvaluation(null);
      sendToBubble("bubble_fn_simulator", "session_closed");
      if (pendingLeaveRef.current) {
        pendingLeaveRef.current();
        pendingLeaveRef.current = null;
      }
    }, []);
    const cancelClose = q2(() => {
      setShowCloseDialog(false);
      pendingLeaveRef.current = null;
    }, []);
    const handleRetry = q2(async () => {
      setPhase("session");
      setEvaluation(null);
      setHasRetried(true);
      await realtime_default.retrySimulation();
      const state2 = realtime_default.state;
      const issueData = state2.currentIssueData || { issue: state2.currentIssue };
      sendToBubble("bubble_fn_simulator", "stage2_token_needed", {
        issue: state2.currentIssue,
        jsonContext: JSON.stringify(issueData, null, 2),
        isRetry: true
      });
    }, []);
    const handleDone = q2(() => {
      setSimStage(null);
      setPhase("landing");
      setEvaluation(null);
    }, []);
    const handleStage2Start = q2(() => {
      coinsDeductedRef.current = true;
      sendToBubble("bubble_fn_simulator", "deduct_coins");
    }, []);
    const handleSessionError = q2((message, code) => {
      realtime_default.stop("error");
      setErrorMessage(message);
      setErrorCode(code || "UNKNOWN");
      setErrorHasRefund(coinsDeductedRef.current);
      setShowErrorDialog(true);
    }, []);
    const dismissError = q2(() => {
      setShowErrorDialog(false);
      setPhase("landing");
      setEvaluation(null);
      setSimStage(null);
      sendToBubble("bubble_fn_simulator", "session_error", { errorCode, errorMessage });
      if (coinsDeductedRef.current) {
        sendToBubble("bubble_fn_simulator", "refund_coins");
        coinsDeductedRef.current = false;
      }
    }, [errorCode, errorMessage]);
    y2(() => {
      window.appUI._simulatorRequestLeave = (onConfirm) => {
        if (phase !== "session") return true;
        pendingLeaveRef.current = onConfirm;
        setShowCloseDialog(true);
        return false;
      };
      return () => {
        delete window.appUI._simulatorRequestLeave;
      };
    }, [phase]);
    const sim = theme.simulator;
    const [coinCount] = d2(() => {
      try {
        const raw = localStorage.getItem("bonds_user_data");
        if (raw) return JSON.parse(raw).coins || 0;
      } catch {
      }
      return 0;
    });
    const fullScreenContent = isFullScreen ? $2(
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          style: { position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 50, overflow: "hidden", background: sim.sessionBg }
        },
        phase === "session" && /* @__PURE__ */ xn.createElement(
          SimulatorSession_default,
          {
            theme,
            coinCount,
            onComplete: handleComplete,
            onClose: handleClose,
            onStage2Start: handleStage2Start,
            onStageChange: handleStageChange,
            onActiveSpeakerChange: setActiveSpeaker,
            onError: handleSessionError
          }
        ),
        /* @__PURE__ */ xn.createElement(FaceOverlay_default, { facePhase, glowSide, theme }),
        phase === "results" && /* @__PURE__ */ xn.createElement(
          SimulatorResults_default,
          {
            evaluation,
            theme,
            onRetry: hasRetried ? null : handleRetry,
            onDone: handleDone
          }
        ),
        /* @__PURE__ */ xn.createElement(
          Dialog_default,
          {
            open: showCloseDialog,
            onClose: cancelClose,
            title: "End session?",
            theme,
            closeOnBackdrop: false,
            actions: [
              { label: "End Session", onClick: confirmClose, primary: true },
              { label: "Continue", onClick: cancelClose }
            ]
          },
          /* @__PURE__ */ xn.createElement("p", null, "This will close your current simulation. Your progress will be lost.")
        ),
        /* @__PURE__ */ xn.createElement(
          Dialog_default,
          {
            open: showErrorDialog,
            onClose: dismissError,
            title: "Session Error",
            theme,
            closeOnBackdrop: false,
            actions: [
              { label: "OK", onClick: dismissError, primary: true }
            ]
          },
          /* @__PURE__ */ xn.createElement("p", null, errorMessage),
          errorHasRefund && /* @__PURE__ */ xn.createElement("p", { className: "mt-3 font-bold", style: { color: "#58CC02" } }, "Your coins have been refunded.")
        )
      ),
      document.body
    ) : null;
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full relative" }, phase === "landing" && /* @__PURE__ */ xn.createElement(SimulatorLanding_default, { onStart: handleStart, theme, disabled: !instructionsReady }), fullScreenContent);
  };
  var SimulatorSection_default = SimulatorSection;

  // src/components/insight/MicIcon.jsx
  var MicIcon = ({ size = 24, stroke = "#FFFFFF", strokeWidth = 2 }) => /* @__PURE__ */ xn.createElement("svg", { width: size, height: size, viewBox: "0 0 24 24", fill: "none", stroke, strokeWidth, strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M12 1a3 3 0 0 0-3 3v8a3 3 0 0 0 6 0V4a3 3 0 0 0-3-3z" }), /* @__PURE__ */ xn.createElement("path", { d: "M19 10v2a7 7 0 0 1-14 0v-2" }), /* @__PURE__ */ xn.createElement("line", { x1: "12", y1: "19", x2: "12", y2: "23" }), /* @__PURE__ */ xn.createElement("line", { x1: "8", y1: "23", x2: "16", y2: "23" }));
  var MicIcon_default = MicIcon;

  // src/components/insight/InsightOtherDialog.jsx
  var InsightOtherDialog = ({ open, questionText, theme, onSubmit, onClose }) => {
    const ins = theme.insight;
    const isDark = theme.isDark;
    const [text, setText] = d2("");
    const [isRecording, setIsRecording] = d2(false);
    const [showTextarea, setShowTextarea] = d2(false);
    const recognitionRef = A2(null);
    const finalTranscriptRef = A2("");
    const wantRecordingRef = A2(false);
    const [visible, setVisible] = d2(false);
    const [closing, setClosing] = d2(false);
    const sheetBg = isDark ? "rgba(37,37,56,0.95)" : "rgba(255,255,255,0.95)";
    const inputBg = isDark ? "rgba(255,255,255,0.08)" : "rgba(0,0,0,0.04)";
    const inputBorder = isDark ? "rgba(255,255,255,0.12)" : "rgba(0,0,0,0.08)";
    const hasSpeechApi = typeof window !== "undefined" && (window.webkitSpeechRecognition || window.SpeechRecognition);
    y2(() => {
      if (open) {
        setVisible(true);
        setClosing(false);
        setText("");
        finalTranscriptRef.current = "";
        setShowTextarea(!hasSpeechApi);
        if (hasSpeechApi) {
          const startTimer = setTimeout(() => startRecording(), 200);
          return () => clearTimeout(startTimer);
        }
      } else if (visible) {
        setClosing(true);
        const timer3 = setTimeout(() => {
          setVisible(false);
          setClosing(false);
          setShowTextarea(false);
        }, 300);
        return () => clearTimeout(timer3);
      }
    }, [open]);
    y2(() => {
      return () => {
        wantRecordingRef.current = false;
        if (recognitionRef.current) {
          try {
            recognitionRef.current.stop();
          } catch {
          }
        }
      };
    }, []);
    const stopRecording = q2(() => {
      wantRecordingRef.current = false;
      if (recognitionRef.current) {
        try {
          recognitionRef.current.stop();
        } catch {
        }
      }
      setIsRecording(false);
      setShowTextarea(true);
    }, []);
    const createRecognition = q2(() => {
      const SpeechRecognition = window.webkitSpeechRecognition || window.SpeechRecognition;
      if (!SpeechRecognition) return null;
      const recognition = new SpeechRecognition();
      recognition.continuous = false;
      recognition.interimResults = true;
      recognition.lang = "en-US";
      recognition.onresult = (event) => {
        let interim = "";
        for (let i3 = 0; i3 < event.results.length; i3++) {
          if (event.results[i3].isFinal) {
            finalTranscriptRef.current += event.results[i3][0].transcript;
          } else {
            interim += event.results[i3][0].transcript;
          }
        }
        setText(finalTranscriptRef.current + interim);
      };
      recognition.onerror = (e3) => {
        if (e3.error === "no-speech" && wantRecordingRef.current) return;
        wantRecordingRef.current = false;
        setIsRecording(false);
        setShowTextarea(true);
      };
      recognition.onend = () => {
        if (wantRecordingRef.current) {
          const next = createRecognition();
          if (next) {
            recognitionRef.current = next;
            try {
              next.start();
            } catch {
              wantRecordingRef.current = false;
              setIsRecording(false);
              setShowTextarea(true);
            }
          } else {
            wantRecordingRef.current = false;
            setIsRecording(false);
            setShowTextarea(true);
          }
          return;
        }
        setIsRecording(false);
        setShowTextarea(true);
      };
      return recognition;
    }, []);
    const startRecording = q2(() => {
      finalTranscriptRef.current = "";
      wantRecordingRef.current = true;
      const recognition = createRecognition();
      if (!recognition) return;
      recognitionRef.current = recognition;
      recognition.start();
      setIsRecording(true);
      setShowTextarea(false);
    }, [createRecognition]);
    const toggleMic = q2(() => {
      if (isRecording) {
        stopRecording();
      } else {
        startRecording();
      }
    }, [isRecording, stopRecording, startRecording]);
    const handleSubmit = q2(() => {
      if (!text.trim()) return;
      stopRecording();
      onSubmit(text.trim());
    }, [text, onSubmit, stopRecording]);
    const handleClose = q2(() => {
      stopRecording();
      onClose();
    }, [onClose, stopRecording]);
    if (!visible) return null;
    return /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 z-[9999] flex flex-col justify-end" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `absolute top-0 left-0 right-0 bottom-0 ${closing ? "dialog-overlay-out" : "dialog-overlay-in"}`,
        style: { background: theme.dialogDimBg },
        onClick: handleClose
      }
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `relative w-full rounded-t-3xl px-6 pt-3 pb-8 ${closing ? "insight-sheet-out" : "insight-sheet-in"}`,
        style: {
          background: sheetBg,
          backdropFilter: "blur(20px)",
          WebkitBackdropFilter: "blur(20px)"
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "flex justify-center mb-4" }, /* @__PURE__ */ xn.createElement("div", { className: "w-10 h-1 rounded-full", style: { background: theme.textMuted } })),
      questionText && /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[0.8125rem] leading-relaxed text-center mb-5", style: { color: theme.dialogText } }, questionText),
      isRecording && /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col items-center" }, /* @__PURE__ */ xn.createElement("div", { className: "relative flex items-center justify-center mb-4", style: { width: "5rem", height: "5rem" } }, /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "insight-ripple absolute rounded-full border-2 border-solid",
          style: { width: "4rem", height: "4rem", borderColor: ins.micActiveBg, inset: 0, margin: "auto" }
        }
      ), /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: toggleMic,
          className: "animate-insight-pulse relative z-10 w-16 h-16 rounded-full flex items-center justify-center",
          style: { background: ins.micActiveBg }
        },
        /* @__PURE__ */ xn.createElement(MicIcon_default, { size: 28, stroke: ins.micActiveIcon })
      )), /* @__PURE__ */ xn.createElement("span", { className: "font-poppins text-[0.75rem] mb-4", style: { color: ins.micActiveBg } }, "Listening..."), /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "w-full min-h-[4rem] rounded-xl p-3 font-poppins text-[0.875rem] leading-relaxed",
          style: {
            background: inputBg,
            color: theme.textPrimary
          }
        },
        text || /* @__PURE__ */ xn.createElement("span", { style: { color: theme.textMuted } }, "Start speaking...")
      ), /* @__PURE__ */ xn.createElement("p", { className: "font-poppins text-[0.6875rem] mt-3", style: { color: theme.textMuted } }, "Tap mic to stop recording")),
      !isRecording && showTextarea && /* @__PURE__ */ xn.createElement("div", null, hasSpeechApi && /* @__PURE__ */ xn.createElement("div", { className: "flex justify-center mb-3" }, /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: toggleMic,
          className: "w-10 h-10 rounded-full flex items-center justify-center",
          style: { background: ins.micBg }
        },
        /* @__PURE__ */ xn.createElement(MicIcon_default, { size: 20, stroke: ins.micIcon })
      )), /* @__PURE__ */ xn.createElement(
        "textarea",
        {
          value: text,
          onChange: (e3) => setText(e3.target.value),
          placeholder: hasSpeechApi ? "Or type your answer..." : "Type your answer...",
          rows: 3,
          className: "w-full rounded-xl p-3 font-poppins text-[0.875rem] leading-relaxed resize-none border border-solid outline-none",
          style: {
            background: inputBg,
            borderColor: inputBorder,
            color: theme.textPrimary
          },
          onFocus: (e3) => {
            e3.target.style.borderColor = ins.micIcon;
          },
          onBlur: (e3) => {
            e3.target.style.borderColor = inputBorder;
          }
        }
      ), /* @__PURE__ */ xn.createElement("div", { className: "mt-4" }, /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleSubmit,
          disabled: !text.trim(),
          className: `w-full py-3 rounded-xl font-jakarta font-extrabold text-[0.875rem] border-b-[4px] border-solid active:border-b-0 active:translate-y-[1px] transition-[transform] duration-100 ${!text.trim() ? "opacity-40" : ""}`,
          style: { background: "#58CC02", borderColor: "#46A302", color: "#FFFFFF" }
        },
        "Submit"
      ))),
      !isRecording && !showTextarea && /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col items-center py-4" }, /* @__PURE__ */ xn.createElement("div", { className: "w-16 h-16 rounded-full flex items-center justify-center", style: { background: ins.micBg } }, /* @__PURE__ */ xn.createElement(MicIcon_default, { size: 28, stroke: ins.micIcon })), /* @__PURE__ */ xn.createElement("span", { className: "font-poppins text-[0.75rem] mt-2", style: { color: theme.textMuted } }, "Starting..."))
    ));
  };
  var InsightOtherDialog_default = InsightOtherDialog;

  // src/components/insight/InsightQuestions.jsx
  var InsightQuestions = ({ questions, theme, onAnswer, onBack, onClose }) => {
    const ins = theme.insight;
    const [currentIndex, setCurrentIndex] = d2(0);
    const [showOtherDialog, setShowOtherDialog] = d2(false);
    const [micExpanding, setMicExpanding] = d2(false);
    const [pillsReady, setPillsReady] = d2(false);
    const [nudgeAnswer, setNudgeAnswer] = d2(null);
    const [nudgeClosing, setNudgeClosing] = d2(false);
    const [nudgeVisible, setNudgeVisible] = d2(false);
    const videoARef = A2(null);
    const videoBRef = A2(null);
    const [activeVideo, setActiveVideo] = d2("a");
    const pillsTimerRef = A2(null);
    const micTapTimerRef = A2(null);
    const nudgeShownRef = A2(false);
    const nudgeTimerRef = A2(null);
    y2(() => {
      if (nudgeAnswer) {
        setNudgeVisible(true);
        setNudgeClosing(false);
      } else if (nudgeVisible) {
        setNudgeClosing(true);
        nudgeTimerRef.current = setTimeout(() => {
          setNudgeVisible(false);
          setNudgeClosing(false);
        }, 150);
      }
      return () => clearTimeout(nudgeTimerRef.current);
    }, [nudgeAnswer]);
    const question = questions[currentIndex];
    const total = questions.length;
    const videoASrcRef = A2(null);
    const videoBSrcRef = A2(null);
    const hasSpeechApi = typeof window !== "undefined" && (window.webkitSpeechRecognition || window.SpeechRecognition);
    y2(() => {
      setPillsReady(false);
      pillsTimerRef.current = setTimeout(() => setPillsReady(true), 5e3);
      return () => clearTimeout(pillsTimerRef.current);
    }, [currentIndex]);
    y2(() => {
      return () => clearTimeout(micTapTimerRef.current);
    }, []);
    y2(() => {
      const url = question?.videoUrl;
      if (!url) return;
      const ref = activeVideo === "a" ? videoARef : videoBRef;
      const srcRef = activeVideo === "a" ? videoASrcRef : videoBSrcRef;
      if (ref.current && srcRef.current !== url) {
        ref.current.src = url;
        ref.current.load();
        ref.current.play().catch(() => {
        });
        srcRef.current = url;
      }
    }, [currentIndex, question?.videoUrl, activeVideo]);
    const handleAnswer = q2((answer, otherText) => {
      const result2 = {
        questionIndex: currentIndex,
        questionId: question.id,
        answer,
        ...otherText ? { otherText } : {}
      };
      onAnswer(result2);
      if (currentIndex < total - 1) {
        setActiveVideo((prev) => prev === "a" ? "b" : "a");
        setCurrentIndex((prev) => prev + 1);
      }
    }, [currentIndex, question, total, onAnswer]);
    const handleBack = q2(() => {
      if (currentIndex === 0) {
        onBack();
      } else {
        setActiveVideo((prev) => prev === "a" ? "b" : "a");
        setCurrentIndex((prev) => prev - 1);
      }
    }, [currentIndex, onBack]);
    const handleOtherSubmit = q2((text) => {
      setShowOtherDialog(false);
      handleAnswer("other", text);
    }, [handleAnswer]);
    const handleOtherClose = q2(() => {
      setShowOtherDialog(false);
    }, []);
    const handleMicTap = q2(() => {
      setMicExpanding(true);
      micTapTimerRef.current = setTimeout(() => {
        setShowOtherDialog(true);
        setMicExpanding(false);
      }, 150);
    }, []);
    const handleYesNo = q2((answer) => {
      if (!nudgeShownRef.current) {
        setNudgeAnswer(answer);
        return;
      }
      handleAnswer(answer);
    }, [handleAnswer]);
    const handleNudgeConfirm = q2(() => {
      nudgeShownRef.current = true;
      const answer = nudgeAnswer;
      setNudgeAnswer(null);
      handleAnswer(answer);
    }, [nudgeAnswer, handleAnswer]);
    const handleNudgeCustom = q2(() => {
      nudgeShownRef.current = true;
      setNudgeAnswer(null);
      handleMicTap();
    }, [handleMicTap]);
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full relative overflow-hidden" }, /* @__PURE__ */ xn.createElement(
      "video",
      {
        ref: videoARef,
        className: "absolute top-0 left-0 w-full h-full object-cover",
        style: {
          opacity: activeVideo === "a" ? 1 : 0,
          transition: "opacity 0.5s ease"
        },
        muted: true,
        loop: true,
        playsInline: true,
        "webkit-playsinline": "true"
      }
    ), /* @__PURE__ */ xn.createElement(
      "video",
      {
        ref: videoBRef,
        className: "absolute top-0 left-0 w-full h-full object-cover",
        style: {
          opacity: activeVideo === "b" ? 1 : 0,
          transition: "opacity 0.5s ease"
        },
        muted: true,
        loop: true,
        playsInline: true,
        "webkit-playsinline": "true"
      }
    ), /* @__PURE__ */ xn.createElement("div", { className: "absolute top-0 left-0 w-full h-full", style: { background: "rgba(0,0,0,0.55)" } }), /* @__PURE__ */ xn.createElement("div", { className: "relative z-10 w-full h-full flex flex-col" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-between px-4 pt-14 pb-4" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleBack,
        className: "w-10 h-10 rounded-full flex items-center justify-center",
        style: { background: ins.navBg }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: ins.navIcon, strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "15 18 9 12 15 6" }))
    ), /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2" }, Array.from({ length: total }).map((_4, i3) => /* @__PURE__ */ xn.createElement(
      "div",
      {
        key: i3,
        className: "rounded-full",
        style: {
          width: i3 === currentIndex ? 28 : 8,
          height: 8,
          borderRadius: i3 === currentIndex ? 4 : "50%",
          background: i3 <= currentIndex ? ins.progressFill : ins.progressBg,
          transition: "width 0.3s ease, background 0.3s ease"
        }
      }
    ))), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: onClose,
        className: "w-10 h-10 rounded-full flex items-center justify-center",
        style: { background: ins.navBg }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: ins.navIcon, strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("line", { x1: "18", y1: "6", x2: "6", y2: "18" }), /* @__PURE__ */ xn.createElement("line", { x1: "6", y1: "6", x2: "18", y2: "18" }))
    )), /* @__PURE__ */ xn.createElement("div", { key: currentIndex, className: "flex-1 flex flex-col items-center justify-center px-6" }, /* @__PURE__ */ xn.createElement(
      "h2",
      {
        className: "animate-fade-in font-jakarta font-extrabold text-[1.375rem] text-center leading-tight mb-8",
        style: { color: "#FFFFFF" }
      },
      question?.text || ""
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `insight-mic-in flex flex-col items-center ${micExpanding ? "insight-mic-expand" : ""}`,
        style: {
          animationDelay: micExpanding ? "0ms" : "500ms",
          visibility: showOtherDialog ? "hidden" : "visible",
          pointerEvents: showOtherDialog ? "none" : "auto"
        }
      },
      /* @__PURE__ */ xn.createElement("div", { className: "relative flex items-center justify-center", style: { width: "9rem", height: "9rem" } }, /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "insight-ripple absolute rounded-full border-2 border-solid",
          style: { width: "4.5rem", height: "4.5rem", borderColor: ins.micIcon, inset: 0, margin: "auto" }
        }
      ), /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: "insight-ripple-2 absolute rounded-full border-2 border-solid",
          style: { width: "4.5rem", height: "4.5rem", borderColor: ins.micIcon, inset: 0, margin: "auto" }
        }
      ), /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleMicTap,
          className: "insight-mic-breathe relative z-10 rounded-full flex items-center justify-center",
          style: { width: "4.5rem", height: "4.5rem", background: ins.micIcon }
        },
        /* @__PURE__ */ xn.createElement(MicIcon_default, { size: 30 })
      )),
      /* @__PURE__ */ xn.createElement(
        "p",
        {
          className: "animate-fade-in font-poppins text-[0.8125rem] mt-3",
          style: { color: "rgba(255,255,255,0.7)", animationDelay: "700ms", opacity: 0 }
        },
        hasSpeechApi ? "Tap to share your thoughts" : "Tap to type your thoughts"
      )
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "insight-pills-in flex items-center gap-4 mt-8",
        style: { animationDelay: "4500ms", pointerEvents: pillsReady ? "auto" : "none" }
      },
      /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: () => handleYesNo("yes"),
          className: "px-6 py-2 rounded-full font-jakarta font-bold text-[0.8125rem] border border-solid active:scale-95 transition-transform duration-100",
          style: { background: "transparent", borderColor: "rgba(255,255,255,0.3)", color: "#FFFFFF" }
        },
        "YES"
      ),
      /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: () => handleYesNo("no"),
          className: "px-6 py-2 rounded-full font-jakarta font-bold text-[0.8125rem] border border-solid active:scale-95 transition-transform duration-100",
          style: { background: "transparent", borderColor: "rgba(255,255,255,0.3)", color: "#FFFFFF" }
        },
        "NO"
      )
    ))), /* @__PURE__ */ xn.createElement(
      InsightOtherDialog_default,
      {
        open: showOtherDialog,
        questionText: question?.text,
        theme,
        onSubmit: handleOtherSubmit,
        onClose: handleOtherClose
      }
    ), nudgeVisible && /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 z-[9999] flex items-center justify-center p-6" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `absolute inset-0 ${nudgeClosing ? "dialog-overlay-out" : "dialog-overlay-in"}`,
        style: { background: theme.dialogDimBg },
        onClick: () => setNudgeAnswer(null)
      }
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: `relative w-full max-w-sm rounded-2xl p-6 ${nudgeClosing ? "dialog-card-out" : "dialog-card-in"} border border-solid`,
        style: {
          background: theme.isDark ? "rgba(37,37,56,0.97)" : "rgba(255,255,255,0.97)",
          borderColor: theme.dialogBorder,
          backdropFilter: "blur(20px)",
          WebkitBackdropFilter: "blur(20px)"
        }
      },
      /* @__PURE__ */ xn.createElement(
        "h3",
        {
          className: "font-jakarta font-extrabold text-[1.125rem] text-center mb-3",
          style: { color: theme.dialogTitle }
        },
        "Your thoughts make the difference"
      ),
      /* @__PURE__ */ xn.createElement(
        "p",
        {
          className: "font-poppins text-[0.8125rem] leading-relaxed text-center mb-6",
          style: { color: theme.dialogText }
        },
        "Quick answers are fine, but sharing your perspective in your own words helps us create insights that truly reflect your relationship."
      ),
      /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleNudgeCustom,
          className: "w-full py-3 rounded-xl font-jakarta font-bold text-[0.875rem] mb-3 active:scale-[0.98] transition-transform duration-100",
          style: { background: ins.micIcon, color: "#FFFFFF" }
        },
        "Share my thoughts instead"
      ),
      /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: handleNudgeConfirm,
          className: "w-full py-3 rounded-xl font-jakarta font-bold text-[0.875rem] border border-solid active:scale-[0.98] transition-transform duration-100",
          style: {
            background: "transparent",
            borderColor: theme.isDark ? "rgba(255,255,255,0.15)" : "rgba(0,0,0,0.1)",
            color: theme.textSecondary
          }
        },
        "Continue with ",
        nudgeAnswer === "yes" ? "Yes" : "No"
      )
    )));
  };
  var InsightQuestions_default = InsightQuestions;

  // src/components/insight/InsightLoader.jsx
  var InsightLoader = ({ theme, message }) => {
    const ins = theme.insight;
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full flex flex-col items-center justify-center", style: { background: ins.loaderBg } }, /* @__PURE__ */ xn.createElement("div", { className: "animate-insight-spin mb-6" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "w-14 h-14 rounded-full border-[3px] border-solid",
        style: { borderColor: `${ins.loaderSpinner}33`, borderTopColor: ins.loaderSpinner }
      }
    )), /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-bold text-[16px]", style: { color: ins.loaderText } }, message || "Loading..."));
  };
  var InsightLoader_default = InsightLoader;

  // src/components/insight/InsightPlayback.jsx
  var SPEED_OPTIONS2 = [1, 1.25, 1.5, 1.75, 2];
  var InsightPlayback = ({ insightData, ttsApiKey, theme, onDone }) => {
    const ins = theme.insight;
    const videoRef = A2(null);
    const ambientRef = A2(null);
    const [speedIndex, setSpeedIndex] = d2(0);
    const [isPlaying, setIsPlaying] = d2(true);
    const [audioReady, setAudioReady] = d2(false);
    const { status: ttsStatus, sentences } = useTTS();
    const { title, text, videoUrl, audioUrl } = insightData || {};
    y2(() => {
      if (ttsApiKey && text) {
        tts_default.start({ apiKey: ttsApiKey, text });
      }
      return () => {
        tts_default.stop();
      };
    }, [ttsApiKey, text]);
    y2(() => {
      if (!audioReady && sentences.some((s3) => s3.state === "active")) {
        setAudioReady(true);
      }
    }, [audioReady, sentences]);
    y2(() => {
      if (ttsStatus === "done") {
        setIsPlaying(false);
      }
    }, [ttsStatus]);
    const handlePlayPause = q2(() => {
      if (isPlaying) {
        if (ttsStatus === "streaming") tts_default.pause();
        if (videoRef.current) videoRef.current.pause();
        if (ambientRef.current) ambientRef.current.pause();
        setIsPlaying(false);
      } else {
        if (ttsStatus === "paused") {
          tts_default.resume();
        } else if (ttsStatus === "done" || ttsStatus === "idle") {
          if (ttsApiKey && text) tts_default.start({ apiKey: ttsApiKey, text });
        }
        if (videoRef.current) videoRef.current.play().catch(() => {
        });
        if (ambientRef.current) ambientRef.current.play().catch(() => {
        });
        setIsPlaying(true);
      }
    }, [isPlaying, ttsStatus, ttsApiKey, text]);
    const handleSpeedCycle = q2(() => {
      const next = (speedIndex + 1) % SPEED_OPTIONS2.length;
      setSpeedIndex(next);
      tts_default.setSpeed(SPEED_OPTIONS2[next]);
    }, [speedIndex]);
    const handleReplay = q2(() => {
      if (ttsApiKey && text) {
        tts_default.start({ apiKey: ttsApiKey, text });
        if (videoRef.current) videoRef.current.play().catch(() => {
        });
        if (ambientRef.current) ambientRef.current.play().catch(() => {
        });
        setIsPlaying(true);
      }
    }, [ttsApiKey, text]);
    const handleDone = q2(() => {
      tts_default.stop();
      if (ambientRef.current) {
        ambientRef.current.pause();
        ambientRef.current.src = "";
      }
      onDone();
    }, [onDone]);
    const activeSentence = sentences.find((s3) => s3.state === "active");
    const showSubtitle = activeSentence && (ttsStatus === "streaming" || ttsStatus === "paused");
    const showPlayPause = ttsStatus === "streaming" || ttsStatus === "paused";
    const showSpeed = TTS_BACKEND !== "pcm";
    const showReplay = ttsStatus === "done";
    if (!audioReady) {
      return /* @__PURE__ */ xn.createElement(InsightLoader_default, { theme });
    }
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full relative overflow-hidden flex flex-col" }, videoUrl && /* @__PURE__ */ xn.createElement(
      "video",
      {
        ref: videoRef,
        className: "absolute top-0 left-0 w-full h-full object-cover",
        src: videoUrl,
        autoPlay: true,
        loop: true,
        playsInline: true,
        "webkit-playsinline": "true"
      }
    ), audioUrl && /* @__PURE__ */ xn.createElement(
      "audio",
      {
        ref: ambientRef,
        src: audioUrl,
        loop: true,
        autoPlay: true,
        volume: "0.2",
        onLoadedMetadata: (e3) => {
          e3.target.volume = 0.2;
        }
      }
    ), /* @__PURE__ */ xn.createElement("div", { className: "absolute top-0 left-0 w-full h-full", style: { background: "rgba(0,0,0,0.35)" } }), /* @__PURE__ */ xn.createElement("div", { className: "relative z-10 flex flex-col h-full" }, title && /* @__PURE__ */ xn.createElement("div", { className: "pt-14 px-6" }, /* @__PURE__ */ xn.createElement("h2", { className: "font-jakarta font-extrabold text-[20px] text-center", style: { color: "#FFFFFF" } }, title)), /* @__PURE__ */ xn.createElement("div", { className: "flex-1" }), /* @__PURE__ */ xn.createElement("div", { className: "px-6 mb-4", style: { minHeight: "4em" } }, showSubtitle && /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "animate-subtitle-slide-up rounded-xl px-4 py-3 text-center",
        style: { background: ins.subtitleBg }
      },
      /* @__PURE__ */ xn.createElement("p", { className: "font-poppins font-medium text-[16px] leading-relaxed", style: { color: ins.subtitleText } }, activeSentence.text)
    )), /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-center gap-3 mb-4 px-6" }, showPlayPause && /* @__PURE__ */ xn.createElement(xn.Fragment, null, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handlePlayPause,
        className: "w-12 h-12 rounded-full flex items-center justify-center",
        style: { background: ins.controlBg }
      },
      isPlaying ? /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: ins.controlIcon }, /* @__PURE__ */ xn.createElement("rect", { x: "6", y: "4", width: "4", height: "16", rx: "1" }), /* @__PURE__ */ xn.createElement("rect", { x: "14", y: "4", width: "4", height: "16", rx: "1" })) : /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: ins.controlIcon }, /* @__PURE__ */ xn.createElement("polygon", { points: "6,4 20,12 6,20" }))
    ), showSpeed && /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleSpeedCycle,
        className: "h-12 px-4 rounded-full flex items-center justify-center font-jakarta font-bold text-[13px]",
        style: { background: ins.controlBg, color: ins.controlIcon }
      },
      SPEED_OPTIONS2[speedIndex],
      "x"
    )), showReplay && /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleReplay,
        className: "w-12 h-12 rounded-full flex items-center justify-center",
        style: { background: ins.controlBg }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "20", height: "20", viewBox: "0 0 24 24", fill: "none", stroke: ins.controlIcon, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "1 4 1 10 7 10" }), /* @__PURE__ */ xn.createElement("path", { d: "M3.51 15a9 9 0 1 0 2.13-9.36L1 10" }))
    )), /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-3 px-6 pb-10" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        className: "flex-1 py-4 rounded-2xl font-jakarta font-extrabold text-[14px] border border-solid active:translate-y-[1px] transition-[transform] duration-100",
        style: { background: ins.feedbackBg, borderColor: "transparent", color: ins.feedbackText }
      },
      "Feedback"
    ), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleDone,
        className: "flex-1 py-4 rounded-2xl font-jakarta font-extrabold text-[16px] text-white border-b-[4px] border-solid active:border-b-0 active:translate-y-[1px] transition-[transform] duration-100",
        style: { background: ins.continueBg, borderColor: ins.continueShadow }
      },
      "Continue"
    ))));
  };
  var InsightPlayback_default = InsightPlayback;

  // src/components/insight/insightMocks.js
  var INSIGHT_MOCK_ENABLED = true;
  var V1 = "https://4240869bec55ba9919a61b94d2a6974f.cdn.bubble.io/f1711632753753x828041153237036000/pexels-rodnae-productions-8230614%20%28360p%29.mp4";
  var V22 = "https://4240869bec55ba9919a61b94d2a6974f.cdn.bubble.io/f1711632678992x377672133566015800/pexels-cottonbro-7496175%20%28360p%29.mp4";
  var V3 = "https://4240869bec55ba9919a61b94d2a6974f.cdn.bubble.io/f1711631963407x465209335449551900/pexels-cottonbro-5329478%20%28360p%29.mp4";
  var MOCK_QUESTIONS = [
    {
      id: "q1",
      text: "Do you and your partner share your daily highs and lows with each other?",
      videoUrl: V1
    },
    {
      id: "q2",
      text: "When was the last time you tried something completely new together?",
      videoUrl: V22
    },
    {
      id: "q3",
      text: "Do you feel comfortable being vulnerable around your partner?",
      videoUrl: V3
    }
  ];
  var INSIGHT_VIDEO = "https://4240869bec55ba9919a61b94d2a6974f.cdn.bubble.io/f1735211080655x840492595516242600/Generic%201St%20Insight%20Vid%20%281%29%20-%20smaller.mp4";
  var MOCK_INSIGHT = {
    insightId: "mock-insight-1",
    title: "Communication & Connection",
    text: "Your answers suggest a strong foundation of emotional openness in your relationship. Sharing daily experiences builds a habit of connection that many couples overlook. The willingness to try new things together indicates a growth mindset as a couple. However, vulnerability can always be deepened. Consider setting aside ten minutes each evening for uninterrupted conversation without screens. Small rituals of connection compound over time into something extraordinary.",
    videoUrl: INSIGHT_VIDEO,
    audioUrl: null
  };
  var MOCK_GENERATE_DELAY = 2500;
  var MOCK_QUESTIONS_DELAY = 800;
  function setupInsightMocks() {
    if (!INSIGHT_MOCK_ENABLED) return;
    const origSend = window.BubbleBridge?.send;
    if (!origSend) return;
    const mockSend = (fnName, payload) => {
      if (fnName !== "bubble_fn_insight") {
        origSend(fnName, payload);
        return;
      }
      const action = payload.output1;
      console.log(`\u{1F9EA} [MOCK] bubble_fn_insight / ${action}`, JSON.parse(payload.output2 || "{}"));
      if (action === "fetch_questions") {
        setTimeout(() => {
          if (window.appUI.onInsightQuestions) {
            window.appUI.onInsightQuestions({
              questions: MOCK_QUESTIONS,
              ttsApiKey: getMockTtsApiKey()
            });
          }
        }, MOCK_QUESTIONS_DELAY);
        return;
      }
      if (action === "generate") {
        setTimeout(() => {
          if (window.appUI.onInsightData) {
            window.appUI.onInsightData(MOCK_INSIGHT);
          }
        }, MOCK_GENERATE_DELAY);
        return;
      }
    };
    window.BubbleBridge.send = mockSend;
    console.log("\u{1F9EA} Insight mocks ENABLED \u2014 set INSIGHT_MOCK_ENABLED = false to disable");
    return () => {
      window.BubbleBridge.send = origSend;
    };
  }
  function getMockTtsApiKey() {
    try {
      const raw = localStorage.getItem("bonds_simulator_templates");
      return raw ? JSON.parse(raw).ttsApiKey : null;
    } catch {
      return null;
    }
  }

  // src/components/insight/InsightFlow.jsx
  var InsightFlow = ({ type, activityId, theme, pop, onFullScreenChange }) => {
    const [phase, setPhase] = d2(() => type === "learn" ? "questions" : "loading");
    const [questions, setQuestions] = d2([]);
    const [answers, setAnswers] = d2([]);
    const [insightData, setInsightData] = d2(null);
    const [ttsApiKey, setTtsApiKey] = d2(() => {
      try {
        const raw = localStorage.getItem("bonds_simulator_templates");
        return raw ? JSON.parse(raw).ttsApiKey : null;
      } catch {
        return null;
      }
    });
    const [showCloseDialog, setShowCloseDialog] = d2(false);
    const ambientRef = A2(null);
    y2(() => {
      if (onFullScreenChange) onFullScreenChange(true);
      return () => {
        if (onFullScreenChange) onFullScreenChange(false);
      };
    }, [onFullScreenChange]);
    y2(() => {
      if (!INSIGHT_MOCK_ENABLED) return;
      return setupInsightMocks();
    }, []);
    y2(() => {
      window.appUI.onInsightQuestions = (data) => {
        if (data?.questions) setQuestions(data.questions);
        if (data?.ttsApiKey) setTtsApiKey(data.ttsApiKey);
      };
      window.appUI.onInsightData = (data) => {
        setInsightData(data);
        if (data?.ttsApiKey) setTtsApiKey(data.ttsApiKey);
        setPhase("playback");
      };
      return () => {
        delete window.appUI.onInsightQuestions;
        delete window.appUI.onInsightData;
      };
    }, []);
    y2(() => {
      if (type === "learn") {
        sendToBubble("bubble_fn_insight", "fetch_questions");
      }
    }, [type]);
    y2(() => {
      if (type === "activity") {
        sendToBubble("bubble_fn_insight", "generate", { type: "activity", activityId });
      }
    }, [type, activityId]);
    const handleAnswer = q2((answer) => {
      const newAnswers = [...answers, answer];
      setAnswers(newAnswers);
      sendToBubble("bubble_fn_insight", "answer", answer);
      if (newAnswers.length >= questions.length) {
        setPhase("loading");
        sendToBubble("bubble_fn_insight", "generate", {
          type: "learn",
          answers: newAnswers
        });
      }
    }, [answers, questions.length]);
    const handleBack = q2(() => {
      setShowCloseDialog(true);
    }, []);
    const handleClose = q2(() => {
      setShowCloseDialog(true);
    }, []);
    const confirmClose = q2(() => {
      tts_default.stop();
      if (ambientRef.current) {
        ambientRef.current.pause();
        ambientRef.current.src = "";
      }
      setShowCloseDialog(false);
      sendToBubble("bubble_fn_insight", "closed");
      pop();
    }, [pop]);
    const cancelClose = q2(() => {
      setShowCloseDialog(false);
    }, []);
    const handlePlaybackDone = q2(() => {
      sendToBubble("bubble_fn_insight", "playback_complete");
      pop();
    }, [pop]);
    const ins = theme.insight;
    const portalTarget = T2(
      () => document.getElementById("app-content-area") || document.getElementById("app-root") || document.body,
      []
    );
    y2(() => {
      if (portalTarget === document.body) return;
      const prev = portalTarget.style.position;
      portalTarget.style.position = "relative";
      return () => {
        portalTarget.style.position = prev;
      };
    }, []);
    const content = /* @__PURE__ */ xn.createElement(
      "div",
      {
        style: {
          position: portalTarget === document.body ? "fixed" : "absolute",
          top: 0,
          left: 0,
          right: 0,
          bottom: 0,
          zIndex: 50,
          overflow: "hidden",
          background: ins.loaderBg
        }
      },
      phase === "questions" && questions.length > 0 && /* @__PURE__ */ xn.createElement(
        InsightQuestions_default,
        {
          questions,
          theme,
          onAnswer: handleAnswer,
          onBack: handleBack,
          onClose: handleClose
        }
      ),
      phase === "questions" && questions.length === 0 && /* @__PURE__ */ xn.createElement(InsightLoader_default, { theme, message: "Loading..." }),
      phase === "loading" && /* @__PURE__ */ xn.createElement(InsightLoader_default, { theme, message: "Generating your insight..." }),
      phase === "playback" && insightData && /* @__PURE__ */ xn.createElement(
        InsightPlayback_default,
        {
          insightData,
          ttsApiKey,
          theme,
          onDone: handlePlaybackDone
        }
      ),
      /* @__PURE__ */ xn.createElement(
        Dialog_default,
        {
          open: showCloseDialog,
          onClose: cancelClose,
          title: "Leave insight?",
          theme,
          closeOnBackdrop: false,
          actions: [
            { label: "Leave", onClick: confirmClose, primary: true },
            { label: "Stay", onClick: cancelClose }
          ]
        },
        /* @__PURE__ */ xn.createElement("p", null, "Your progress will be lost.")
      )
    );
    return $2(content, portalTarget);
  };
  var InsightFlow_default = InsightFlow;

  // src/components/TestTTSScreen.jsx
  var TestTTSScreen = ({ pop, theme }) => {
    const { status, progress, sentences, stats, error } = useTTS();
    const [waitingForBubble, setWaitingForBubble] = d2(false);
    y2(() => {
      if (status === "streaming") setWaitingForBubble(false);
    }, [status]);
    const displayStatus = waitingForBubble ? "waiting" : status;
    const isActive = displayStatus === "waiting" || displayStatus === "streaming";
    const handleStart = () => {
      tts_default.unlockAudio();
      setWaitingForBubble(true);
      sendToBubble("bubble_fn_test", "start_tts");
    };
    const handleStop = () => tts_default.stop();
    const formatTime = (sec) => {
      const m3 = Math.floor(sec / 60);
      const s3 = (sec % 60).toFixed(1);
      return `${m3}:${s3.padStart(4, "0")}`;
    };
    const sentenceColor = (state2) => state2 === "active" ? theme.textPrimary : state2 === "spoken" ? theme.textMuted : theme.textSecondary;
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full font-jakarta overflow-y-auto", style: { background: theme.bg } }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-3 px-5 pt-5 pb-4" }, /* @__PURE__ */ xn.createElement("button", { onClick: pop, className: "w-9 h-9 rounded-full flex items-center justify-center border border-solid", style: { borderColor: theme.border, background: theme.surface } }, /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textPrimary, strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "15 18 9 12 15 6" }))), /* @__PURE__ */ xn.createElement("h1", { className: "font-extrabold text-[18px]", style: { color: theme.textPrimary } }, "Test TTS Streaming")), /* @__PURE__ */ xn.createElement("div", { className: "px-5 pb-10" }, /* @__PURE__ */ xn.createElement("div", { className: "mb-4" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-between mb-2" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-2" }, /* @__PURE__ */ xn.createElement("div", { className: "w-2.5 h-2.5 rounded-full", style: {
      background: displayStatus === "streaming" ? "#58CC02" : displayStatus === "waiting" ? "#FF9600" : displayStatus === "done" ? "#1CB0F6" : displayStatus === "error" ? "#FF4B4B" : theme.textMuted
    } }), /* @__PURE__ */ xn.createElement("span", { className: "text-[13px] font-semibold", style: { color: theme.textSecondary } }, displayStatus === "idle" ? "Ready" : displayStatus === "waiting" ? "Waiting for Bubble..." : displayStatus === "streaming" ? "Streaming..." : displayStatus === "done" ? "Done" : "Error")), (displayStatus === "streaming" || displayStatus === "done") && /* @__PURE__ */ xn.createElement("span", { className: "text-[12px] font-mono", style: { color: theme.textMuted } }, formatTime(progress.elapsed), " / ", formatTime(progress.total))), (displayStatus === "streaming" || displayStatus === "done") && /* @__PURE__ */ xn.createElement("div", { className: "h-1 rounded-full overflow-hidden", style: { background: theme.border } }, /* @__PURE__ */ xn.createElement("div", { className: "h-full rounded-full transition-[width] duration-100", style: { width: `${progress.percent * 100}%`, background: "#E44B8E" } }))), isActive ? /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleStop,
        className: "w-full py-3.5 rounded-2xl font-extrabold text-[15px] border-b-[3px] border-solid active:border-b-0 active:mt-[3px]",
        style: { background: "#FF4B4B", borderColor: "#CC3333", color: "#FFFFFF" }
      },
      "Stop"
    ) : /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleStart,
        className: "w-full py-3.5 rounded-2xl font-extrabold text-[15px] border-b-[3px] border-solid active:border-b-0 active:mt-[3px]",
        style: { background: "#E44B8E", borderColor: "#B83A72", color: "#FFFFFF" }
      },
      displayStatus === "done" ? "Play Again" : "Start TTS"
    ), /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "mt-5 rounded-2xl p-4 min-h-[10em] border border-solid",
        style: { background: theme.surface, borderColor: theme.border }
      },
      /* @__PURE__ */ xn.createElement("p", { className: "text-[11px] font-bold uppercase tracking-widest mb-3", style: { color: theme.textMuted } }, "Transcript"),
      /* @__PURE__ */ xn.createElement("div", { className: "font-poppins text-[14px] leading-relaxed" }, sentences.length > 0 ? sentences.map((s3, i3) => /* @__PURE__ */ xn.createElement(
        "span",
        {
          key: i3,
          className: "transition-colors duration-200",
          style: {
            color: sentenceColor(s3.state),
            fontWeight: s3.state === "active" ? 600 : 400
          }
        },
        s3.text,
        " "
      )) : /* @__PURE__ */ xn.createElement("span", { style: { color: theme.textMuted } }, displayStatus === "idle" ? "Press Start to begin..." : displayStatus === "waiting" ? "Waiting..." : ""))
    ), error && /* @__PURE__ */ xn.createElement("div", { className: "mt-4 rounded-xl p-3 border border-solid", style: { borderColor: "#FF4B4B", background: "rgba(255,75,75,0.1)" } }, /* @__PURE__ */ xn.createElement("p", { className: "text-[12px] font-mono break-all", style: { color: "#FF4B4B" } }, error)), stats && /* @__PURE__ */ xn.createElement("div", { className: "mt-5 grid grid-cols-2 gap-3" }, [
      { label: "First Byte", value: stats.firstByte != null ? `${stats.firstByte}ms` : "-" },
      { label: "Total Fetch", value: `${stats.totalFetch}ms` },
      { label: "Audio Duration", value: `${stats.duration}s` },
      { label: "Size", value: stats.size },
      { label: "Sentences", value: stats.sentenceCount }
    ].map((s3) => /* @__PURE__ */ xn.createElement("div", { key: s3.label, className: "rounded-xl p-3 border border-solid", style: { borderColor: theme.border, background: theme.surface } }, /* @__PURE__ */ xn.createElement("p", { className: "text-[10px] font-bold uppercase tracking-widest mb-1", style: { color: theme.textMuted } }, s3.label), /* @__PURE__ */ xn.createElement("p", { className: "text-[16px] font-extrabold font-mono", style: { color: theme.textPrimary } }, s3.value)))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6 rounded-xl p-4 border border-solid", style: { borderColor: theme.border, background: theme.surface } }, /* @__PURE__ */ xn.createElement("p", { className: "text-[11px] font-bold uppercase tracking-widest mb-2", style: { color: theme.textMuted } }, "Bubble Run JavaScript"), /* @__PURE__ */ xn.createElement("pre", { className: "text-[11px] font-mono leading-relaxed overflow-x-auto", style: { color: theme.textSecondary } }, `window.appUI.startTTSStream({
  apiKey: "<api-key>",
  text: "Text to speak..."
})`))));
  };
  var TestTTSScreen_default = TestTTSScreen;

  // src/components/MainTabs.jsx
  var glassStyle = (theme) => ({
    background: theme.glassBg,
    borderColor: theme.glassBorder
  });
  var SectionLabel = ({ children, theme }) => /* @__PURE__ */ xn.createElement("p", { className: "font-jakarta font-bold text-[10px] uppercase tracking-[2px] mb-2 px-1", style: { color: theme.textMuted } }, children);
  var SettingsRow = ({ icon, label, right, theme, last }) => /* @__PURE__ */ xn.createElement(
    "div",
    {
      className: `flex items-center justify-between py-3 px-1 ${last ? "" : "border-b border-solid"}`,
      style: { borderColor: last ? "transparent" : theme.border }
    },
    /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-3" }, icon, /* @__PURE__ */ xn.createElement("span", { className: "font-semibold text-[14px]", style: { color: theme.textPrimary } }, label)),
    right
  );
  var Chevron = ({ theme }) => /* @__PURE__ */ xn.createElement("svg", { width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: theme.textMuted, strokeWidth: "2.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polyline", { points: "9 18 15 12 9 6" }));
  var ProfileSection = ({ theme, darkModePref, setDarkModePref, onLogout, push }) => {
    const [showCoinAnim, setShowCoinAnim] = d2(false);
    const glass = glassStyle(theme);
    const { name: name5, email, pillars, coins } = useUser();
    const handleLogout = () => {
      sendToBubble("bubble_fn_profile", "logout");
      localStorage.clear();
      if (onLogout) onLogout();
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "px-5 pt-6 pb-10 font-jakarta" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-4 mb-2" }, /* @__PURE__ */ xn.createElement("div", { className: "rounded-full flex items-center justify-center", style: { width: 56, height: 56, background: theme.border } }, /* @__PURE__ */ xn.createElement("svg", { width: "28", height: "28", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" }), /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "7", r: "4" }))), /* @__PURE__ */ xn.createElement("div", null, /* @__PURE__ */ xn.createElement("p", { className: "font-extrabold text-[20px]", style: { color: theme.textPrimary } }, name5), /* @__PURE__ */ xn.createElement("p", { className: "text-[12px]", style: { color: theme.textSecondary } }, email))), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl p-4 mt-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center gap-3" }, /* @__PURE__ */ xn.createElement("div", { className: "rounded-full flex items-center justify-center", style: { width: 36, height: 36, border: `1.5px dashed ${theme.textMuted}` } }, /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textMuted, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("line", { x1: "12", y1: "5", x2: "12", y2: "19" }), /* @__PURE__ */ xn.createElement("line", { x1: "5", y1: "12", x2: "19", y2: "12" }))), /* @__PURE__ */ xn.createElement("span", { className: "font-semibold text-[14px]", style: { color: theme.textSecondary } }, "No partner connected")), /* @__PURE__ */ xn.createElement("button", { className: "rounded-full px-4 py-1.5 text-[12px] font-bold border border-solid", style: { borderColor: "#E44B8E", color: "#E44B8E" } }, "Invite"))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement(SectionLabel, { theme }, "Relationship"), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl p-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-between" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1" }, /* @__PURE__ */ xn.createElement("p", { className: "font-semibold text-[14px] mb-2", style: { color: theme.textPrimary } }, "Your Pillars"), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-wrap gap-2" }, pillars.map((p3) => /* @__PURE__ */ xn.createElement("span", { key: p3, className: "rounded-full px-3 py-1 text-[11px] font-bold border border-solid", style: { borderColor: theme.border, color: theme.textSecondary } }, p3)))), /* @__PURE__ */ xn.createElement(Chevron, { theme })))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement(SectionLabel, { theme }, "Preferences"), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl px-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        last: true,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" })),
        label: "Dark Mode",
        right: /* @__PURE__ */ xn.createElement("div", { className: "flex rounded-xl overflow-hidden", style: { background: theme.border } }, DARK_MODE_OPTIONS.map((opt, i3) => {
          const active = darkModePref === opt;
          const nextActive = darkModePref === DARK_MODE_OPTIONS[i3 + 1];
          const showDivider = i3 < DARK_MODE_OPTIONS.length - 1 && !active && !nextActive;
          return /* @__PURE__ */ xn.createElement("div", { key: opt, className: "flex items-center" }, /* @__PURE__ */ xn.createElement(
            "button",
            {
              onClick: () => setDarkModePref(opt),
              className: "px-3 py-1.5 text-[12px] font-bold transition-colors duration-200",
              style: {
                background: active ? "#E44B8E" : "transparent",
                color: active ? "#fff" : theme.textSecondary
              }
            },
            DARK_MODE_LABELS[opt]
          ), showDivider && /* @__PURE__ */ xn.createElement("div", { style: { width: 1, height: 14, background: theme.textMuted } }));
        }))
      }
    ))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement(SectionLabel, { theme }, "Account"), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl px-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        last: true,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("rect", { x: "2", y: "4", width: "20", height: "16", rx: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M22 7l-10 7L2 7" })),
        label: "Account Info",
        right: /* @__PURE__ */ xn.createElement(Chevron, { theme })
      }
    ))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement(SectionLabel, { theme }, "Support"), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl px-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" })),
        label: "Share Feedback",
        right: /* @__PURE__ */ xn.createElement(Chevron, { theme })
      }
    ), /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z" }), /* @__PURE__ */ xn.createElement("polyline", { points: "14 2 14 8 20 8" })),
        label: "Terms of Service",
        right: /* @__PURE__ */ xn.createElement(Chevron, { theme })
      }
    ), /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        last: true,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("rect", { x: "3", y: "11", width: "18", height: "11", rx: "2", ry: "2" }), /* @__PURE__ */ xn.createElement("path", { d: "M7 11V7a5 5 0 0 1 10 0v4" })),
        label: "Privacy Policy",
        right: /* @__PURE__ */ xn.createElement(Chevron, { theme })
      }
    ))), /* @__PURE__ */ xn.createElement("div", { className: "mt-6" }, /* @__PURE__ */ xn.createElement(SectionLabel, { theme }, "Dev"), /* @__PURE__ */ xn.createElement("div", { className: "rounded-2xl px-4 border border-solid", style: glass }, /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("polygon", { points: "11 5 6 9 2 9 2 15 6 15 11 19 11 5" }), /* @__PURE__ */ xn.createElement("path", { d: "M19.07 4.93a10 10 0 0 1 0 14.14M15.54 8.46a5 5 0 0 1 0 7.07" })),
        label: "Test TTS Streaming",
        right: /* @__PURE__ */ xn.createElement("button", { onClick: () => push("test-tts"), className: "rounded-full px-4 py-1.5 text-[12px] font-bold border border-solid", style: { borderColor: "#E44B8E", color: "#E44B8E" } }, "Open")
      }
    ), /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 6v6l4 2" })),
        label: "Test Coin Animation",
        right: /* @__PURE__ */ xn.createElement("button", { onClick: () => setShowCoinAnim(true), className: "rounded-full px-4 py-1.5 text-[12px] font-bold border border-solid", style: { borderColor: "#E44B8E", color: "#E44B8E" } }, "Play")
      }
    ), /* @__PURE__ */ xn.createElement(
      SettingsRow,
      {
        theme,
        last: true,
        icon: /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: theme.textSecondary, strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "5" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" })),
        label: "Add 30 Coins",
        right: /* @__PURE__ */ xn.createElement("button", { onClick: () => sendToBubble("bubble_fn_test", "add_coins", { amount: 30 }), className: "rounded-full px-4 py-1.5 text-[12px] font-bold border border-solid", style: { borderColor: "#E44B8E", color: "#E44B8E" } }, "Add")
      }
    ))), showCoinAnim && $2(
      /* @__PURE__ */ xn.createElement("div", { style: { position: "fixed", top: 0, left: 0, right: 0, bottom: 0, zIndex: 9999 } }, /* @__PURE__ */ xn.createElement(
        CoinDeduction_default,
        {
          coinCount: coins,
          coinCost: 4,
          onComplete: () => setShowCoinAnim(false)
        }
      )),
      document.body
    ), /* @__PURE__ */ xn.createElement("div", { className: "mt-8 flex flex-col items-center gap-3" }, /* @__PURE__ */ xn.createElement("button", { onClick: handleLogout, className: "font-bold text-[14px] py-2 px-8 rounded-full border border-solid", style: { borderColor: theme.border, color: theme.textSecondary } }, "Log Out"), /* @__PURE__ */ xn.createElement("button", { className: "text-[12px] font-semibold", style: { color: theme.textMuted } }, "Delete Account"), /* @__PURE__ */ xn.createElement("p", { className: "text-[10px] mt-2", style: { color: theme.textMuted } }, APP_VERSION)));
  };
  var PlaceholderSection = ({ title, theme }) => /* @__PURE__ */ xn.createElement("div", { className: "p-8 text-center", style: { color: theme.textPrimary } }, /* @__PURE__ */ xn.createElement("h2", { className: "text-2xl font-bold mb-4" }, title), /* @__PURE__ */ xn.createElement("p", { style: { opacity: 0.7 } }, "Coming Soon"));
  var STORAGE_KEY3 = "bonds_dark_mode";
  var MainTabs = ({ onLogout }) => {
    const storage = useNativelyStorage();
    const [darkModePref, setDarkModePrefRaw] = d2(() => {
      return localStorage.getItem(STORAGE_KEY3) || "system";
    });
    const setDarkModePref = (val) => {
      setDarkModePrefRaw(val);
      storage.setItem(STORAGE_KEY3, val);
    };
    y2(() => {
      storage.getItem(STORAGE_KEY3).then((val) => {
        if (val) setDarkModePrefRaw((prev) => val !== prev ? val : prev);
      });
    }, [storage]);
    const [systemTheme, setSystemTheme] = d2(getSystemTheme);
    y2(() => {
      const mq = window.matchMedia("(prefers-color-scheme: dark)");
      const handler = (e3) => setSystemTheme(e3.matches ? "dark" : "light");
      mq.addEventListener("change", handler);
      return () => mq.removeEventListener("change", handler);
    }, []);
    const themeKey = darkModePref === "on" ? "dark" : darkModePref === "off" ? "light" : systemTheme;
    const t3 = THEMES[themeKey];
    const [activeTab, setActiveTab] = d2("journey");
    const [stacks, setStacks] = d2({
      journey: ["journey"],
      simulator: ["simulator"],
      fun: ["fun"],
      profile: ["profile"]
    });
    const [navAction, setNavAction] = d2(null);
    const [simulatorActive, setSimulatorActive] = d2(false);
    const [simulatorFullScreen, setSimulatorFullScreen] = d2(false);
    const [insightFullScreen, setInsightFullScreen] = d2(false);
    const prevFullScreenRef = A2(false);
    const [tabBarAnim, setTabBarAnim] = d2("");
    const anyFullScreen = simulatorFullScreen || insightFullScreen;
    y2(() => {
      if (anyFullScreen && !prevFullScreenRef.current) {
        setTabBarAnim("tab-bar-slide-down");
      } else if (!anyFullScreen && prevFullScreenRef.current) {
        setTabBarAnim("tab-bar-slide-up");
      }
      prevFullScreenRef.current = anyFullScreen;
    }, [anyFullScreen]);
    const push = (viewId, props = {}) => {
      setNavAction("push");
      setStacks((prev) => ({
        ...prev,
        [activeTab]: [...prev[activeTab], { id: viewId, ...props }]
      }));
    };
    const pop = () => {
      setNavAction("pop");
      setStacks((prev) => {
        const currentStack2 = prev[activeTab];
        if (currentStack2.length <= 1) return prev;
        return { ...prev, [activeTab]: currentStack2.slice(0, -1) };
      });
    };
    const scrollRef = A2(null);
    const scrollPositions = A2({});
    const saveScrollPosition = () => {
      if (scrollRef.current) {
        scrollPositions.current[activeTab] = scrollRef.current.scrollTop;
      }
    };
    const switchTab = (tabId) => {
      if (tabId === activeTab) return;
      if (simulatorActive && activeTab === "simulator") {
        if (window.appUI._simulatorRequestLeave) {
          const allowed = window.appUI._simulatorRequestLeave(() => {
            saveScrollPosition();
            setNavAction("tab");
            setActiveTab(tabId);
          });
          if (!allowed) return;
        }
      }
      saveScrollPosition();
      setNavAction("tab");
      setActiveTab(tabId);
    };
    const prevTab = A2(activeTab);
    y2(() => {
      if (prevTab.current !== activeTab && scrollRef.current) {
        scrollRef.current.scrollTop = scrollPositions.current[activeTab] || 0;
        prevTab.current = activeTab;
      }
    }, [activeTab]);
    const currentStack = stacks[activeTab];
    const currentViewItem = currentStack[currentStack.length - 1];
    const currentViewId = typeof currentViewItem === "string" ? currentViewItem : currentViewItem.id;
    const currentViewProps = typeof currentViewItem === "object" ? currentViewItem : {};
    const renderView = () => {
      const commonProps = {
        push,
        pop,
        theme: t3,
        ...currentViewProps
      };
      let content;
      switch (activeTab) {
        case "journey":
          content = /* @__PURE__ */ xn.createElement(JourneyPath_default, { ...commonProps });
          break;
        case "simulator":
          content = /* @__PURE__ */ xn.createElement(SimulatorSection_default, { theme: t3, onSessionChange: setSimulatorActive, onFullScreenChange: setSimulatorFullScreen });
          break;
        case "fun":
          if (currentViewId === "daily-question") {
            content = /* @__PURE__ */ xn.createElement(DailyQuestion_default, { ...commonProps });
          } else if (currentViewId === "insight-flow") {
            content = /* @__PURE__ */ xn.createElement(InsightFlow_default, { type: currentViewProps.type, activityId: currentViewProps.activityId, theme: t3, pop, onFullScreenChange: setInsightFullScreen });
          } else if (currentViewId === "activity-detail") {
            content = /* @__PURE__ */ xn.createElement(PlaceholderSection, { title: currentViewProps.name || "Activity", theme: t3 });
          } else {
            content = /* @__PURE__ */ xn.createElement(FunZoneSection_default, { ...commonProps });
          }
          break;
        case "profile":
          if (currentViewId === "test-tts") {
            content = /* @__PURE__ */ xn.createElement(TestTTSScreen_default, { ...commonProps });
          } else {
            content = /* @__PURE__ */ xn.createElement(ProfileSection, { theme: t3, darkModePref, setDarkModePref, onLogout, push });
          }
          break;
        default:
          content = /* @__PURE__ */ xn.createElement(PlaceholderSection, { title: "Not Found", theme: t3 });
      }
      const animationClass = navAction === "push" ? "animate-slide-in-right" : navAction === "pop" ? "animate-slide-in-left" : navAction === "tab" ? "animate-tab-switch" : "";
      return /* @__PURE__ */ xn.createElement("div", { key: `${activeTab}-${currentStack.length}`, className: `w-full flex-1 ${animationClass}` }, content);
    };
    const NavButton = ({ id, label, icon }) => {
      const isActive = activeTab === id;
      return /* @__PURE__ */ xn.createElement(
        "button",
        {
          onClick: () => switchTab(id),
          className: `flex flex-col items-center justify-center gap-1 transition-[color,transform,opacity] duration-200 ease-out ${isActive ? "font-bold scale-110 active-tab-shimmer" : "font-medium scale-100"}`,
          style: { color: isActive ? "#E44B8E" : t3.navInactive, opacity: isActive ? 1 : 0.7 }
        },
        icon,
        /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta text-[10px] tracking-wide" }, label)
      );
    };
    const Icons = {
      journey: /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "12", r: "10" }), /* @__PURE__ */ xn.createElement("path", { d: "M16.24 7.76l-2.12 6.36-6.36 2.12 2.12-6.36 6.36-2.12z" })),
      simulator: /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z" }), /* @__PURE__ */ xn.createElement("path", { d: "M8 10h.01" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 10h.01" }), /* @__PURE__ */ xn.createElement("path", { d: "M16 10h.01" })),
      fun: /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M20 12v10H4V12" }), /* @__PURE__ */ xn.createElement("path", { d: "M2 7h20v5H2z" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 22V7" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 7H7.5a2.5 2.5 0 0 1 0-5C11 2 12 7 12 7z" }), /* @__PURE__ */ xn.createElement("path", { d: "M12 7h4.5a2.5 2.5 0 0 0 0-5C13 2 12 7 12 7z" })),
      profile: /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, /* @__PURE__ */ xn.createElement("path", { d: "M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2" }), /* @__PURE__ */ xn.createElement("circle", { cx: "12", cy: "7", r: "4" }))
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full font-poppins flex flex-col overflow-hidden", style: { background: t3.bg, transition: "background 0.3s ease" } }, /* @__PURE__ */ xn.createElement("div", { ref: scrollRef, className: "flex-1 min-h-0 overflow-y-auto scrollbar-hide relative overflow-x-hidden flex flex-col" }, currentStack.length > 1 && /* @__PURE__ */ xn.createElement("div", { className: "absolute top-4 left-4 z-50" }, /* @__PURE__ */ xn.createElement("button", { onClick: pop, className: "p-2 rounded-full border border-solid transition-colors", style: { background: t3.glassBg, borderColor: t3.glassBorder, color: t3.textPrimary } }, /* @__PURE__ */ xn.createElement("svg", { width: "24", height: "24", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2" }, /* @__PURE__ */ xn.createElement("polyline", { points: "15 18 9 12 15 6" })))), renderView()), /* @__PURE__ */ xn.createElement("div", { className: `shrink-0 h-20 border-t border-solid ${tabBarAnim}`, style: { background: t3.glassBg, borderColor: t3.glassBorder, transition: "background 0.3s ease, border-color 0.3s ease" } }, /* @__PURE__ */ xn.createElement("div", { className: "flex items-center justify-around h-full max-w-[500px] mx-auto px-4" }, /* @__PURE__ */ xn.createElement(NavButton, { id: "journey", label: "Journey", icon: Icons.journey }), /* @__PURE__ */ xn.createElement(NavButton, { id: "simulator", label: "Simulator", icon: Icons.simulator }), /* @__PURE__ */ xn.createElement(NavButton, { id: "fun", label: "Fun", icon: Icons.fun }), /* @__PURE__ */ xn.createElement(NavButton, { id: "profile", label: "Profile", icon: Icons.profile }))));
  };
  var MainTabs_default = MainTabs;

  // src/components/onboarding/SingleSelect.jsx
  var SingleSelect = ({ question, options = [], refreshable = true, previousAnswer, theme, onAnswer, onRefresh }) => {
    const ob = theme?.onboarding || {};
    const [selectedIndex, setSelectedIndex] = d2(previousAnswer?.index ?? null);
    const [refreshAnim, setRefreshAnim] = d2("");
    const [refreshKey, setRefreshKey] = d2(0);
    const iconRef = A2(null);
    const handleSelect = (opt, index2) => {
      setSelectedIndex(index2);
      const text = typeof opt === "string" ? opt : opt.text;
      const variable = typeof opt === "string" ? "" : opt.variable || "";
      if (onAnswer) onAnswer({ answer: text, variable, index: index2 });
    };
    const handleRefresh = () => {
      setRefreshAnim("out");
      if (iconRef.current) {
        iconRef.current.classList.remove("refresh-spin");
        void iconRef.current.offsetWidth;
        iconRef.current.classList.add("refresh-spin");
      }
      setTimeout(() => {
        setSelectedIndex(null);
        if (onRefresh) onRefresh();
        setRefreshKey((k4) => k4 + 1);
        setRefreshAnim("in");
        setTimeout(() => setRefreshAnim(""), 450);
      }, 300);
    };
    const listClass = refreshAnim === "out" ? "refresh-out" : refreshAnim === "in" ? "refresh-in" : "";
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full w-full" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 px-7 pt-8 pb-6 w-full overflow-hidden" }, /* @__PURE__ */ xn.createElement("h1", { className: "font-bold text-[22px] leading-[30px] tracking-[0.02em] mb-8", style: { color: ob.text } }, question), /* @__PURE__ */ xn.createElement("div", { className: `space-y-5 ${listClass}`, key: refreshKey }, options.map((opt, i3) => {
      const text = typeof opt === "string" ? opt : opt.text;
      const isSelected = selectedIndex === i3;
      return /* @__PURE__ */ xn.createElement(
        "button",
        {
          key: i3,
          onClick: () => handleSelect(opt, i3),
          className: "w-full text-left px-5 py-4 rounded-2xl border-2 border-solid transition-[background-color,border-color,box-shadow,transform] duration-200 active:translate-y-[2px]",
          style: {
            background: isSelected ? ob.optionSelectedBg : ob.optionBg,
            borderColor: isSelected ? ob.optionSelectedBorder : ob.optionBorder,
            boxShadow: isSelected ? ob.optionSelectedShadow : ob.optionShadow
          }
        },
        /* @__PURE__ */ xn.createElement("span", { className: "font-bold text-[14px] leading-[22px] tracking-[0.02em]", style: { color: ob.text } }, text)
      );
    }))), refreshable && /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 pb-10 w-full flex flex-col items-center gap-1" }, /* @__PURE__ */ xn.createElement("span", { className: "font-medium text-[14px] tracking-[0.5px]", style: { color: ob.textSecondary } }, "Choose the most relevant option"), /* @__PURE__ */ xn.createElement("span", { className: "font-medium text-[10px] tracking-[0.5px]", style: { color: ob.textMuted } }, "or"), /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleRefresh,
        className: "flex items-center gap-2 mt-1 group",
        disabled: refreshAnim !== ""
      },
      /* @__PURE__ */ xn.createElement("svg", { ref: iconRef, width: "20", height: "20", viewBox: "0 0 20 20", fill: "none", style: { willChange: "transform" } }, /* @__PURE__ */ xn.createElement("path", { d: "M1.667 3.333v5h5", stroke: ob.refreshIcon, strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }), /* @__PURE__ */ xn.createElement("path", { d: "M3.51 12.5a7.5 7.5 0 1 0 1.14-7.833L1.667 8.333", stroke: ob.refreshIcon, strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" })),
      /* @__PURE__ */ xn.createElement("span", { className: "font-bold text-[14px] tracking-[0.5px] underline decoration-solid", style: { color: ob.refreshIcon } }, "Refresh answers")
    )));
  };
  var SingleSelect_default = SingleSelect;

  // src/components/onboarding/SliderSelect.jsx
  var SliderSelect = ({ question, stops = [], previousAnswer, theme, onAnswer }) => {
    const ob = theme?.onboarding || {};
    const [selectedIndex, setSelectedIndex] = d2(previousAnswer?.index ?? null);
    const [revealed, setRevealed] = d2(previousAnswer ? stops.length : 0);
    const [bounceIndex, setBounceIndex] = d2(null);
    const prevSelectedRef = A2(previousAnswer?.index ?? null);
    y2(() => {
      if (previousAnswer) return;
      stops.forEach((_4, i3) => {
        setTimeout(() => setRevealed((r3) => Math.max(r3, i3 + 1)), 120 * (i3 + 1));
      });
    }, [previousAnswer, stops]);
    const handleSelect = (stop, index2) => {
      prevSelectedRef.current = selectedIndex;
      setSelectedIndex(index2);
      setBounceIndex(index2);
      setTimeout(() => setBounceIndex(null), 600);
    };
    const handleContinue = () => {
      if (selectedIndex === null) return;
      const stop = stops[selectedIndex];
      onAnswer({ answer: stop.label, variable: stop.variable, index: selectedIndex });
    };
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full w-full" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 px-7 pt-8 pb-6 w-full flex flex-col" }, /* @__PURE__ */ xn.createElement("h1", { className: "font-bold text-[22px] leading-[30px] tracking-[0.02em] mb-6", style: { color: ob.text } }, question), /* @__PURE__ */ xn.createElement("div", { className: "flex-1 flex items-center" }, /* @__PURE__ */ xn.createElement("div", { className: "relative w-full py-2" }, stops.map((stop, i3) => {
      const isSelected = selectedIndex === i3;
      const isFilled = selectedIndex !== null && i3 <= selectedIndex;
      const isRevealed = i3 < revealed;
      const isBouncing = bounceIndex === i3;
      return /* @__PURE__ */ xn.createElement(
        "div",
        {
          key: i3,
          className: `stop-reveal flex items-center gap-4 py-[10px] ${isRevealed ? "" : "invisible"}`,
          style: { animationDelay: previousAnswer ? "0ms" : `${120 * i3}ms` }
        },
        /* @__PURE__ */ xn.createElement("div", { className: "relative flex flex-col items-center shrink-0 w-6" }, i3 > 0 && /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "absolute -top-[10px] w-[3px] h-[10px] rounded-full transition-[background-color] duration-300",
            style: { backgroundColor: isFilled ? ob.sliderFilled : ob.sliderUnfilled }
          }
        ), /* @__PURE__ */ xn.createElement(
          "button",
          {
            onClick: () => handleSelect(stop, i3),
            className: `relative w-6 h-6 rounded-full border-2 border-solid transition-[background-color,border-color,box-shadow] duration-200 ${isBouncing ? "dot-pop" : ""}`,
            style: {
              backgroundColor: isFilled ? ob.sliderFilled : ob.dotEmpty,
              borderColor: isFilled ? ob.sliderFilled : ob.dotEmptyBorder,
              boxShadow: isFilled ? ob.dotFilledShadow : ob.dotEmptyShadow
            }
          }
        ), i3 < stops.length - 1 && /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "absolute -bottom-[10px] w-[3px] h-[10px] rounded-full transition-[background-color] duration-300",
            style: { backgroundColor: isFilled && i3 < selectedIndex ? ob.sliderFilled : ob.sliderUnfilled }
          }
        )),
        /* @__PURE__ */ xn.createElement(
          "button",
          {
            onClick: () => handleSelect(stop, i3),
            className: "flex items-center gap-3 flex-1 min-w-0"
          },
          /* @__PURE__ */ xn.createElement(
            "span",
            {
              className: `text-2xl select-none ${isBouncing ? "emoji-bounce" : ""}`,
              style: { display: "inline-block" }
            },
            stop.emoji || ""
          ),
          /* @__PURE__ */ xn.createElement(
            "span",
            {
              className: `text-[15px] leading-snug transition-colors duration-200 ${isSelected ? "font-bold" : "font-medium"}`,
              style: { color: isSelected ? ob.text : isFilled ? ob.labelFilled : ob.labelUnfilled }
            },
            stop.label
          )
        )
      );
    })))), /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 pb-10 px-7 w-full" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleContinue,
        disabled: selectedIndex === null,
        className: `w-full h-[54px] rounded-2xl flex items-center justify-center transition-[background-color,border-color,box-shadow,transform] duration-200 border-2 border-solid ${selectedIndex !== null ? "active:translate-y-[2px]" : ""}`,
        style: selectedIndex !== null ? { background: ob.ctaBg, borderColor: ob.ctaBg, boxShadow: ob.ctaShadow } : { background: ob.ctaDisabledBg, borderColor: ob.ctaDisabledBorder, boxShadow: ob.ctaDisabledShadow }
      },
      /* @__PURE__ */ xn.createElement(
        "span",
        {
          className: "font-extrabold text-[16px] tracking-[2px] uppercase",
          style: { color: selectedIndex !== null ? ob.ctaText : ob.ctaDisabledText }
        },
        "Continue"
      )
    )));
  };
  var SliderSelect_default = SliderSelect;

  // src/components/onboarding/MultiSelect.jsx
  var MultiSelect = ({ question, options = [], maxSelections = 3, previousAnswer, theme, onAnswer }) => {
    const ob = theme?.onboarding || {};
    const [selectedIndices, setSelectedIndices] = d2(
      () => previousAnswer?.indices ? new Set(previousAnswer.indices) : /* @__PURE__ */ new Set()
    );
    const toggleOption = (index2) => {
      setSelectedIndices((prev) => {
        const next = new Set(prev);
        if (next.has(index2)) {
          next.delete(index2);
        } else if (next.size < maxSelections) {
          next.add(index2);
        }
        return next;
      });
    };
    const handleContinue = () => {
      if (selectedIndices.size !== maxSelections) return;
      const indices = [...selectedIndices].sort((a3, b2) => a3 - b2);
      const texts = indices.map((i3) => {
        const opt = options[i3];
        return typeof opt === "string" ? opt : opt.text;
      });
      const vars = indices.map((i3) => {
        const opt = options[i3];
        return typeof opt === "string" ? "" : opt.variable || "";
      });
      onAnswer({ answer: texts.join(","), variable: vars.join(","), indices });
    };
    const atMax = selectedIndices.size >= maxSelections;
    const ready = selectedIndices.size === maxSelections;
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full w-full" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 px-7 pt-8 pb-6 w-full overflow-y-auto" }, /* @__PURE__ */ xn.createElement("h1", { className: "font-bold text-[22px] leading-[30px] tracking-[0.02em] mb-2", style: { color: ob.text } }, question), /* @__PURE__ */ xn.createElement("p", { className: "text-[13px] font-medium mb-6", style: { color: ob.textMuted } }, selectedIndices.size, " / ", maxSelections, " selected"), /* @__PURE__ */ xn.createElement("div", { className: "flex flex-wrap gap-3" }, options.map((opt, i3) => {
      const text = typeof opt === "string" ? opt : opt.text;
      const isSelected = selectedIndices.has(i3);
      const isDimmed = atMax && !isSelected;
      return /* @__PURE__ */ xn.createElement(
        "button",
        {
          key: i3,
          onClick: () => toggleOption(i3),
          className: "px-5 py-3 rounded-full border-2 border-solid transition-[background-color,border-color,box-shadow,opacity,transform] duration-200 active:translate-y-[1px]",
          style: {
            background: isSelected ? ob.optionSelectedBg : isDimmed ? ob.optionDimmedBg : ob.optionBg,
            borderColor: isSelected ? ob.optionSelectedBorder : isDimmed ? ob.optionDimmedBorder : ob.optionBorder,
            boxShadow: isSelected ? ob.optionSelectedShadow : isDimmed ? ob.optionDimmedShadow : ob.optionShadow,
            opacity: isDimmed ? 0.4 : 1
          }
        },
        /* @__PURE__ */ xn.createElement(
          "span",
          {
            className: "font-bold text-[14px] leading-[20px] tracking-[0.02em]",
            style: { color: isSelected ? ob.text : ob.textSecondary }
          },
          text
        )
      );
    }))), /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 pb-10 px-7 w-full" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleContinue,
        disabled: !ready,
        className: `w-full h-[54px] rounded-2xl flex items-center justify-center transition-[background-color,border-color,box-shadow,transform] duration-200 border-2 border-solid ${ready ? "active:translate-y-[2px]" : ""}`,
        style: ready ? { background: ob.ctaBg, borderColor: ob.ctaBg, boxShadow: ob.ctaShadow } : { background: ob.ctaDisabledBg, borderColor: ob.ctaDisabledBorder, boxShadow: ob.ctaDisabledShadow }
      },
      /* @__PURE__ */ xn.createElement(
        "span",
        {
          className: "font-extrabold text-[16px] tracking-[2px] uppercase",
          style: { color: ready ? ob.ctaText : ob.ctaDisabledText }
        },
        "Continue"
      )
    )));
  };
  var MultiSelect_default = MultiSelect;

  // src/components/onboarding/OpenQuestion.jsx
  var OpenQuestion = ({ question, placeholder, charGuidance, previousAnswer, theme, onAnswer }) => {
    const ob = theme?.onboarding || {};
    const [text, setText] = d2(previousAnswer?.answer || "");
    const handleContinue = () => {
      if (!text.trim()) return;
      onAnswer({ answer: text.trim(), variable: "" });
    };
    const hasText = !!text.trim();
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col h-full w-full" }, /* @__PURE__ */ xn.createElement("div", { className: "flex-1 px-7 pt-8 pb-6 w-full flex flex-col" }, /* @__PURE__ */ xn.createElement("h1", { className: "font-bold text-[22px] leading-[30px] tracking-[0.02em] mb-6", style: { color: ob.text } }, question), /* @__PURE__ */ xn.createElement("div", { className: "relative flex-1 max-h-[220px]" }, /* @__PURE__ */ xn.createElement("style", null, `.ob-textarea::placeholder { color: ${ob.inputPlaceholder}; }`), /* @__PURE__ */ xn.createElement(
      "textarea",
      {
        value: text,
        onChange: (e3) => setText(e3.target.value),
        placeholder: placeholder || "Type your answer...",
        className: "ob-textarea w-full h-full resize-none rounded-2xl px-5 py-4 border-2 border-solid text-[15px] leading-[24px] font-medium focus:outline-none transition-colors",
        style: {
          background: ob.inputBg,
          borderColor: ob.inputBorder,
          color: ob.text
        },
        onFocus: (e3) => {
          e3.target.style.borderColor = ob.inputFocusBorder;
        },
        onBlur: (e3) => {
          e3.target.style.borderColor = ob.inputBorder;
        }
      }
    ), charGuidance && /* @__PURE__ */ xn.createElement(
      "span",
      {
        className: "absolute bottom-3 right-4 text-[11px] font-medium",
        style: { color: text.length > charGuidance ? "#E44B8E" : ob.charCount }
      },
      text.length,
      " / ",
      charGuidance
    ))), /* @__PURE__ */ xn.createElement("div", { className: "shrink-0 pb-10 px-7 w-full" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: handleContinue,
        disabled: !hasText,
        className: `w-full h-[54px] rounded-2xl flex items-center justify-center transition-[background-color,border-color,box-shadow,transform] duration-200 border-2 border-solid ${hasText ? "active:translate-y-[2px]" : ""}`,
        style: hasText ? { background: ob.ctaBg, borderColor: ob.ctaBg, boxShadow: ob.ctaShadow } : { background: ob.ctaDisabledBg, borderColor: ob.ctaDisabledBorder, boxShadow: ob.ctaDisabledShadow }
      },
      /* @__PURE__ */ xn.createElement(
        "span",
        {
          className: "font-extrabold text-[16px] tracking-[2px] uppercase",
          style: { color: hasText ? ob.ctaText : ob.ctaDisabledText }
        },
        "Continue"
      )
    )));
  };
  var OpenQuestion_default = OpenQuestion;

  // src/components/onboarding/OnboardingFlow.jsx
  var SCREEN_COMPONENTS = {
    "single-select": SingleSelect_default,
    "slider": SliderSelect_default,
    "multi-select": MultiSelect_default,
    "open-question": OpenQuestion_default
  };
  var STORAGE_KEY4 = "onboarding_state";
  var OnboardingFlow = ({
    steps = [],
    coins: initialCoins = 0,
    initialStep = 0,
    initialAnswers = {},
    showCoins = true,
    theme,
    onComplete,
    onBack: onBackOut
  }) => {
    const ob = theme?.onboarding || {};
    const [currentStep, setCurrentStep] = d2(initialStep);
    const [coins, setCoins] = d2(initialCoins);
    const [answers, setAnswers] = d2(initialAnswers);
    const [direction, setDirection] = d2("forward");
    const [animating, setAnimating] = d2(false);
    const [answeredThisVisit, setAnsweredThisVisit] = d2(false);
    const [rotationOffsets, setRotationOffsets] = d2({});
    const [ready, setReady] = d2(false);
    const [coinIntroSeen, setCoinIntroSeen] = d2(false);
    const coinsCircleRef = A2(null);
    const coinIntroRunningRef = A2(false);
    const timersRef = A2([]);
    const overlaysRef = A2([]);
    const rafRef = A2(null);
    const storage = useNativelyStorage();
    y2(() => {
      const timers = timersRef;
      const overlays = overlaysRef;
      const raf = rafRef;
      return () => {
        timers.current.forEach(clearTimeout);
        timers.current = [];
        overlays.current.forEach((el) => {
          try {
            el.remove();
          } catch {
          }
        });
        overlays.current = [];
        if (raf.current) cancelAnimationFrame(raf.current);
      };
    }, []);
    const addTimer = (fn2, delay) => {
      const id = setTimeout(fn2, delay);
      timersRef.current.push(id);
      return id;
    };
    y2(() => {
      let cancelled = false;
      Promise.all([
        storage.getItem(STORAGE_KEY4),
        storage.getItem("coins_intro_seen")
      ]).then(([raw, introSeen]) => {
        if (cancelled) return;
        if (introSeen === "true") setCoinIntroSeen(true);
        if (raw) {
          try {
            const saved = JSON.parse(raw);
            if (saved.currentStep != null) setCurrentStep(saved.currentStep);
            if (saved.answers) setAnswers(saved.answers);
            if (saved.coins != null) setCoins(saved.coins);
            if (saved.rotationOffsets) setRotationOffsets(saved.rotationOffsets);
          } catch (e3) {
            console.warn("Failed to restore onboarding state:", e3);
          }
        }
        setReady(true);
      });
      return () => {
        cancelled = true;
      };
    }, [storage]);
    const step = steps[currentStep];
    const totalSteps = steps.length;
    const progress = (currentStep + 1) / totalSteps * 100;
    const getVisibleOptions = q2((s3, stepIndex) => {
      if (s3.optionPool && s3.visibleCount) {
        const pool = s3.optionPool;
        const count = s3.visibleCount;
        const offset = rotationOffsets[stepIndex] || 0;
        const result2 = [];
        for (let i3 = 0; i3 < count; i3++) {
          result2.push(pool[(offset + i3) % pool.length]);
        }
        return result2;
      }
      return s3.options;
    }, [rotationOffsets]);
    const persistState = q2((stepIndex, allAnswers, currentCoins, offsets) => {
      const state2 = {
        currentStep: stepIndex,
        answers: allAnswers,
        coins: currentCoins,
        rotationOffsets: offsets
      };
      storage.setItem(STORAGE_KEY4, JSON.stringify(state2));
    }, [storage]);
    const goForward = q2(() => {
      if (animating || currentStep >= totalSteps - 1) return;
      setDirection("forward");
      setAnimating(true);
      addTimer(() => {
        setCurrentStep((prev) => prev + 1);
        setAnimating(false);
        setAnsweredThisVisit(false);
      }, 300);
    }, [currentStep, totalSteps, animating]);
    const goBack = q2(() => {
      if (animating || currentStep === 0) return;
      setDirection("back");
      setAnimating(true);
      addTimer(() => {
        setCurrentStep((prev) => prev - 1);
        setAnimating(false);
        setAnsweredThisVisit(false);
      }, 300);
    }, [currentStep, animating]);
    const triggerCoinPulse = q2(() => {
      if (showCoins && coinsCircleRef.current) {
        coinsCircleRef.current.style.transition = "transform 0.3s ease";
        coinsCircleRef.current.style.transform = "scale(1.3)";
        addTimer(() => {
          if (coinsCircleRef.current) {
            coinsCircleRef.current.style.transform = "scale(1)";
          }
        }, 300);
      }
    }, [showCoins]);
    const triggerCoinIntro = q2(() => {
      return new Promise((resolve2) => {
        if (!coinsCircleRef.current || coinIntroRunningRef.current) {
          resolve2();
          return;
        }
        coinIntroRunningRef.current = true;
        const COIN_BG2 = "radial-gradient(circle at 35% 30%, #C8C8C8, #8A8A8A 70%, #6E6E6E)";
        const COIN_SHADOW2 = "0 2px 0 0 #555";
        let dimOverlay, coinOverlay, msgPanel;
        let safetyTimer;
        const dismiss = () => {
          clearTimeout(safetyTimer);
          const plusEl = coinOverlay.querySelector(".overlay-plus");
          if (plusEl) plusEl.style.opacity = "0";
          msgPanel.style.opacity = "0";
          const coinRect = coinOverlay.getBoundingClientRect();
          coinOverlay.classList.remove("coin-center-animation");
          coinOverlay.style.top = coinRect.top + "px";
          coinOverlay.style.left = coinRect.left + "px";
          coinOverlay.style.transform = "none";
          coinOverlay.classList.add("coin-move-animation");
          dimOverlay.classList.remove("active");
          coinOverlay.offsetHeight;
          if (coinsCircleRef.current) {
            const targetRect = coinsCircleRef.current.getBoundingClientRect();
            const dx = targetRect.left - coinRect.left;
            const dy = targetRect.top - coinRect.top;
            coinOverlay.style.transform = `translate(${dx}px, ${dy}px)`;
            const coinEl = coinOverlay.querySelector(".overlay-coin");
            if (coinEl) {
              coinEl.style.transition = "transform 600ms cubic-bezier(0.4, 0, 0.2, 1)";
              coinEl.style.transformOrigin = "top left";
              coinEl.style.transform = "scale(0.4)";
            }
          }
          addTimer(() => {
            coinOverlay.remove();
            dimOverlay.remove();
            msgPanel.remove();
            overlaysRef.current = overlaysRef.current.filter((el) => el !== coinOverlay && el !== dimOverlay && el !== msgPanel);
            if (coinsCircleRef.current) {
              coinsCircleRef.current.style.transition = "transform 0.3s ease";
              coinsCircleRef.current.style.transform = "scale(1.3)";
              addTimer(() => {
                if (coinsCircleRef.current) coinsCircleRef.current.style.transform = "scale(1)";
              }, 300);
            }
            setCoins(1);
            setCoinIntroSeen(true);
            storage.setItem("coins_intro_seen", "true");
            coinIntroRunningRef.current = false;
            resolve2();
          }, 700);
        };
        safetyTimer = addTimer(() => {
          dismiss();
        }, 1e4);
        dimOverlay = document.createElement("div");
        dimOverlay.className = "dim-overlay";
        dimOverlay.addEventListener("click", dismiss);
        dimOverlay.addEventListener("touchend", (e3) => {
          e3.preventDefault();
          dismiss();
        });
        document.body.appendChild(dimOverlay);
        overlaysRef.current.push(dimOverlay);
        requestAnimationFrame(() => dimOverlay.classList.add("active"));
        coinOverlay = document.createElement("div");
        coinOverlay.className = "coin-center-animation";
        coinOverlay.style.cssText = `
                position: fixed;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%);
                z-index: 10000;
                pointer-events: none;
            `;
        coinOverlay.innerHTML = `
                <div class="overlay-coin" style="width: 80px; height: 80px; border-radius: 50%; background: ${COIN_BG2}; display: flex; align-items: center; justify-content: center; box-shadow: 0 6px 24px rgba(0,0,0,0.3), ${COIN_SHADOW2};">
                    <span style="font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 800; font-size: 2rem; color: #fff; text-shadow: 0 1px 2px rgba(0,0,0,0.3); line-height: 1;">B</span>
                </div>
                <span class="overlay-plus" style="position: absolute; left: 100%; top: 50%; transform: translateY(-50%); margin-left: 10px; white-space: nowrap; font-family: 'Plus Jakarta Sans', sans-serif; font-weight: 800; font-size: 2rem; color: #fff; text-shadow: 0 2px 8px rgba(0,0,0,0.4); opacity: 0; transition: opacity 300ms ease;">+1</span>
            `;
        document.body.appendChild(coinOverlay);
        overlaysRef.current.push(coinOverlay);
        msgPanel = document.createElement("div");
        msgPanel.style.cssText = `
                position: fixed;
                top: calc(50% + 64px);
                left: 50%;
                transform: translateX(-50%);
                z-index: 9999;
                display: flex;
                flex-direction: column;
                align-items: center;
                opacity: 0;
                transition: opacity 0.4s ease;
                pointer-events: none;
            `;
        msgPanel.innerHTML = `
                <p style="font-family:'Plus Jakarta Sans',sans-serif;font-weight:700;font-size:18px;color:#FFFFFF;margin:0 0 8px 0;line-height:1.4;text-align:center;">
                    Meet your Bonds coins!
                </p>
                <p style="font-family:'Poppins',sans-serif;font-weight:400;font-size:14px;color:rgba(255,255,255,0.7);margin:0 0 24px 0;line-height:1.5;text-align:center;max-width:280px;padding:0 20px;">
                    Every answer earns you a coin.<br/>Use them to unlock experiences in Bonds.
                </p>
                <button style="
                    pointer-events:auto;
                    font-family:'Plus Jakarta Sans',sans-serif;
                    font-weight:700;
                    font-size:16px;
                    color:white;
                    background:#E44B8E;
                    border:none;
                    border-radius:16px;
                    padding:14px 48px;
                    cursor:pointer;
                    letter-spacing:0.5px;
                    box-shadow:0 4px 0 #B83A72;
                    transition:transform 0.15s ease, box-shadow 0.15s ease;
                ">OK, Cool!</button>
            `;
        document.body.appendChild(msgPanel);
        overlaysRef.current.push(msgPanel);
        const cta = msgPanel.querySelector("button");
        if (cta) {
          cta.addEventListener("click", dismiss);
          cta.addEventListener("touchstart", (e3) => {
            e3.preventDefault();
            dismiss();
          });
        }
        addTimer(() => {
          const plusEl = coinOverlay.querySelector(".overlay-plus");
          if (plusEl) plusEl.style.opacity = "1";
        }, 500);
        addTimer(() => {
          msgPanel.style.opacity = "1";
          msgPanel.style.pointerEvents = "auto";
        }, 1e3);
      });
    }, [storage]);
    const handleAnswer = q2((data) => {
      if (answeredThisVisit) return;
      setAnsweredThisVisit(true);
      const isNewAnswer = !answers[currentStep];
      const newCoins = isNewAnswer ? coins + 1 : coins;
      const stepAnswer = {
        questionId: step?.questionId,
        type: step?.type,
        ...data
      };
      const newAnswers = { ...answers, [currentStep]: stepAnswer };
      const willPlayIntro = isNewAnswer && coins === 0 && !coinIntroSeen && !coinIntroRunningRef.current;
      setAnswers(newAnswers);
      if (isNewAnswer && !willPlayIntro) setCoins(newCoins);
      const nextStep = currentStep + 1;
      persistState(nextStep, newAnswers, newCoins, rotationOffsets);
      sendToBubble("bubble_fn_onboarding", "step_complete", {
        step: currentStep,
        answer: data.answer || "",
        variable: data.variable || ""
      });
      const advanceOrComplete = () => {
        if (currentStep >= totalSteps - 1) {
          storage.removeItem(STORAGE_KEY4);
          sendToBubble("bubble_fn_onboarding", "complete", {
            answers: JSON.stringify(newAnswers),
            coins: newCoins
          });
          if (onComplete) onComplete(newAnswers, newCoins);
        } else {
          addTimer(() => goForward(), 600);
        }
      };
      if (willPlayIntro) {
        addTimer(() => triggerCoinIntro().then(advanceOrComplete), 500);
      } else {
        if (isNewAnswer) triggerCoinPulse();
        advanceOrComplete();
      }
    }, [currentStep, step, answers, coins, totalSteps, goForward, persistState, storage, answeredThisVisit, rotationOffsets, onComplete, coinIntroSeen, triggerCoinIntro, triggerCoinPulse]);
    const handleRefresh = q2(() => {
      const s3 = steps[currentStep];
      if (!s3 || !s3.optionPool || !s3.visibleCount) return;
      setRotationOffsets((prev) => {
        const currentOffset = prev[currentStep] || 0;
        return { ...prev, [currentStep]: currentOffset + s3.visibleCount };
      });
      sendToBubble("bubble_fn_onboarding", "refresh", { step: currentStep });
    }, [currentStep, steps]);
    if (!ready) return /* @__PURE__ */ xn.createElement("div", { className: "w-full h-full", style: { background: ob.bg } });
    const ScreenComponent = step ? SCREEN_COMPONENTS[step.type] : null;
    const getAnimClass = () => {
      if (!animating) return "animate-fade-in";
      return direction === "forward" ? "animate-slide-out-left" : "animate-slide-out-right";
    };
    const previousAnswer = answers[currentStep] || null;
    const visibleOptions = step ? getVisibleOptions(step, currentStep) : [];
    return /* @__PURE__ */ xn.createElement("div", { className: "flex flex-col w-full h-full overflow-hidden font-jakarta", style: {
      background: ob.bg
    } }, /* @__PURE__ */ xn.createElement("div", { className: "relative z-20 shrink-0 pt-[18px] pb-4 px-[18px]" }, /* @__PURE__ */ xn.createElement(
      "button",
      {
        onClick: currentStep === 0 && onBackOut ? onBackOut : goBack,
        className: `w-8 h-8 rounded-full border border-solid flex items-center justify-center transition-opacity duration-200 ${currentStep === 0 && !onBackOut ? "opacity-0 pointer-events-none" : ""}`,
        style: { background: ob.backBg, borderColor: ob.backBorder }
      },
      /* @__PURE__ */ xn.createElement("svg", { width: "7", height: "12", viewBox: "0 0 7 12", fill: "none" }, /* @__PURE__ */ xn.createElement("path", { d: "M6 1L1 6L6 11", stroke: ob.backIcon, strokeWidth: "1.5", strokeLinecap: "round", strokeLinejoin: "round" }))
    ), /* @__PURE__ */ xn.createElement("div", { className: "absolute left-1/2 top-[18px] -translate-x-1/2 flex flex-col gap-1.5 items-center" }, /* @__PURE__ */ xn.createElement("span", { className: "font-semibold text-[10px] tracking-[0.5px]", style: { color: ob.text } }, currentStep + 1, "/", totalSteps), /* @__PURE__ */ xn.createElement("div", { className: "h-[14px] w-[180px] rounded-[90px] border border-solid overflow-hidden", style: { borderColor: ob.progressBarBorder, background: ob.progressBarBg } }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        className: "w-full h-full rounded-[90px] transition-transform duration-500 ease-out origin-left",
        style: { transform: `scaleX(${progress / 100})`, background: ob.progressBarFill }
      }
    ))), showCoins && /* @__PURE__ */ xn.createElement("div", { className: "absolute top-[18px] right-[18px] z-10 flex items-center gap-2" }, /* @__PURE__ */ xn.createElement(
      "div",
      {
        ref: coinsCircleRef,
        className: "coin-shimmer",
        style: {
          width: 32,
          height: 32,
          borderRadius: "50%",
          display: "flex",
          alignItems: "center",
          justifyContent: "center",
          background: "radial-gradient(circle at 35% 30%, #C8C8C8, #8A8A8A 70%, #6E6E6E)",
          boxShadow: "0 2px 0 0 #555"
        }
      },
      /* @__PURE__ */ xn.createElement("span", { className: "font-jakarta font-extrabold text-[12px] text-white", style: { textShadow: "0 1px 1px rgba(0,0,0,0.25)" } }, "B")
    ), /* @__PURE__ */ xn.createElement("span", { className: "font-extrabold text-[16px] min-w-[16px] text-center", style: { color: theme?.coinsText } }, coins))), /* @__PURE__ */ xn.createElement("div", { className: "w-full h-px", style: { background: ob.divider } }), /* @__PURE__ */ xn.createElement("div", { className: `flex-1 flex flex-col overflow-hidden ${getAnimClass()}`, key: currentStep }, ScreenComponent ? /* @__PURE__ */ xn.createElement(
      ScreenComponent,
      {
        ...step,
        options: visibleOptions,
        previousAnswer,
        theme,
        onAnswer: handleAnswer,
        onRefresh: handleRefresh
      }
    ) : /* @__PURE__ */ xn.createElement("div", { className: "flex-1 flex items-center justify-center" }, /* @__PURE__ */ xn.createElement("span", { className: "text-sm", style: { color: ob.textMuted } }, "Unknown step type: ", step?.type))));
  };
  var OnboardingFlow_default = OnboardingFlow;

  // src/data/mockOnboardingSteps.js
  var mockOnboardingSteps = [
    // Q1 — Gender (single-select, no refresh)
    {
      type: "single-select",
      questionId: "gender",
      question: "What's your gender?",
      refreshable: false,
      options: [
        { text: "Male", variable: "male" },
        { text: "Female", variable: "female" },
        { text: "Non-binary", variable: "non_binary" },
        { text: "Prefer not to say", variable: "prefer_not_to_say" }
      ]
    },
    // Q2 — Relationship duration (slider)
    {
      type: "slider",
      questionId: "relationship_duration",
      question: "How long have you been together?",
      stops: [
        { label: "Less than 6 months", variable: "lt_6mo", emoji: "\u{1F970}" },
        { label: "6 months \u2013 1 year", variable: "6mo_1yr", emoji: "\u{1F48C}" },
        { label: "1 \u2013 3 years", variable: "1_3yr", emoji: "\u{1F91D}" },
        { label: "3 \u2013 5 years", variable: "3_5yr", emoji: "\u{1F525}" },
        { label: "5 \u2013 10 years", variable: "5_10yr", emoji: "\u{1F48E}" },
        { label: "10+ years", variable: "10plus_yr", emoji: "\u{1F3C6}" }
      ]
    },
    // Q3 — Areas to strengthen (multi-select, pick 3)
    {
      type: "multi-select",
      questionId: "strengthen_areas",
      question: "Choose 3 Areas to Strengthen Your Bond",
      maxSelections: 3,
      options: [
        { text: "Communication", variable: "communication" },
        { text: "Trust", variable: "trust" },
        { text: "Intimacy", variable: "intimacy" },
        { text: "Quality Time", variable: "quality_time" },
        { text: "Conflict Resolution", variable: "conflict_resolution" },
        { text: "Emotional Support", variable: "emotional_support" },
        { text: "Shared Goals", variable: "shared_goals" },
        { text: "Fun & Adventure", variable: "fun_adventure" }
      ]
    },
    // Q4 — Last argument (single-select, refreshable, pool of 8)
    {
      type: "single-select",
      questionId: "last_argument",
      question: "Think about your last argument... what was it about?",
      refreshable: true,
      visibleCount: 4,
      optionPool: [
        { text: "Household chores", variable: "chores" },
        { text: "Money or finances", variable: "finances" },
        { text: "Quality time together", variable: "quality_time" },
        { text: "Parenting decisions", variable: "parenting" },
        { text: "Communication style", variable: "communication" },
        { text: "Social plans or friends", variable: "social" },
        { text: "Work-life balance", variable: "work_life" },
        { text: "Personal habits", variable: "habits" }
      ]
    },
    // Q5 — Partner doing more (single-select, refreshable, pool of 9)
    {
      type: "single-select",
      questionId: "partner_do_more",
      question: "What do you wish your partner would do more of?",
      refreshable: true,
      visibleCount: 4,
      optionPool: [
        { text: "Listen without fixing", variable: "listen" },
        { text: "Plan surprise dates", variable: "surprise_dates" },
        { text: "Help around the house", variable: "house_help" },
        { text: 'Say "I love you" more', variable: "say_love" },
        { text: "Give compliments", variable: "compliments" },
        { text: "Initiate physical affection", variable: "affection" },
        { text: "Ask about my day", variable: "ask_day" },
        { text: "Put the phone down", variable: "phone_down" },
        { text: "Share their feelings", variable: "share_feelings" }
      ]
    },
    // Q6 — What matters to partner (single-select, refreshable, pool of 10)
    {
      type: "single-select",
      questionId: "partner_matters",
      question: "What do you think matters most to your partner?",
      refreshable: true,
      visibleCount: 4,
      optionPool: [
        { text: "Feeling respected", variable: "respected" },
        { text: "Quality time together", variable: "quality_time" },
        { text: "Physical affection", variable: "physical_affection" },
        { text: "Words of encouragement", variable: "encouragement" },
        { text: "Financial security", variable: "financial" },
        { text: "Acts of service", variable: "service" },
        { text: "Having fun together", variable: "fun" },
        { text: "Being listened to", variable: "listened" },
        { text: "Personal space", variable: "space" },
        { text: "Shared goals and dreams", variable: "shared_goals" }
      ]
    },
    // Q7 — Most loving gesture (single-select, refreshable, pool of 8)
    {
      type: "single-select",
      questionId: "loving_gesture",
      question: "Which would feel most loving from your partner?",
      refreshable: true,
      visibleCount: 4,
      optionPool: [
        { text: "A handwritten note", variable: "note" },
        { text: "A long hug after a tough day", variable: "hug" },
        { text: "Cooking my favorite meal", variable: "cooking" },
        { text: "Planning a weekend getaway", variable: "getaway" },
        { text: "Saying they're proud of me", variable: "proud" },
        { text: "Taking over my responsibilities", variable: "responsibilities" },
        { text: "A thoughtful gift", variable: "gift" },
        { text: "An undistracted evening together", variable: "evening" }
      ]
    },
    // Q8 — Instant smile (single-select, refreshable, pool of 8)
    {
      type: "single-select",
      questionId: "instant_smile",
      question: "Which would instantly make you smile?",
      refreshable: true,
      visibleCount: 4,
      optionPool: [
        { text: 'A random "thinking of you" text', variable: "text_msg" },
        { text: "Flowers for no reason", variable: "flowers" },
        { text: "My favorite snack waiting at home", variable: "snack" },
        { text: "A playlist made just for me", variable: "playlist" },
        { text: "A spontaneous dance in the kitchen", variable: "dance" },
        { text: "A compliment in front of friends", variable: "public_compliment" },
        { text: "Breakfast in bed", variable: "breakfast" },
        { text: "A silly inside joke callback", variable: "inside_joke" }
      ]
    },
    // Q9 — Open question: truly loved
    {
      type: "open-question",
      questionId: "truly_loved",
      question: "What makes you feel truly loved?",
      placeholder: "Take your time, there's no wrong answer...",
      charGuidance: 150
    },
    // Q10 — Open question: communication change
    {
      type: "open-question",
      questionId: "communication_change",
      question: "If you could change one thing about how you and your partner communicate, what would it be?",
      placeholder: "Be as honest as you'd like...",
      charGuidance: 200
    }
  ];
  var mockOnboardingSteps_default = mockOnboardingSteps;

  // src/hooks/useNetworkStatus.js
  function useNetworkStatus() {
    const [isOnline2, setIsOnline] = d2(
      typeof navigator !== "undefined" ? navigator.onLine : true
    );
    y2(() => {
      const goOnline = () => setIsOnline(true);
      const goOffline = () => setIsOnline(false);
      window.addEventListener("online", goOnline);
      window.addEventListener("offline", goOffline);
      return () => {
        window.removeEventListener("online", goOnline);
        window.removeEventListener("offline", goOffline);
      };
    }, []);
    return { isOnline: isOnline2 };
  }

  // src/components/NetworkBanner.jsx
  var NetworkBanner = ({ theme }) => {
    const { isOnline: isOnline2 } = useNetworkStatus();
    const [visible, setVisible] = d2(false);
    const [dismissing, setDismissing] = d2(false);
    y2(() => {
      if (!isOnline2) {
        setVisible(true);
        setDismissing(false);
      } else if (visible) {
        setDismissing(true);
        const timer3 = setTimeout(() => {
          setVisible(false);
          setDismissing(false);
        }, 400);
        return () => clearTimeout(timer3);
      }
    }, [isOnline2, visible]);
    if (!visible) return null;
    const isDark = theme.isDark;
    return $2(
      /* @__PURE__ */ xn.createElement(
        "div",
        {
          className: `fixed top-0 left-0 right-0 z-[10000] ${dismissing ? "network-banner-out" : "network-banner-in"}`,
          style: { paddingTop: "env(safe-area-inset-top, 0px)" }
        },
        /* @__PURE__ */ xn.createElement(
          "div",
          {
            className: "flex items-center justify-center gap-2 px-4 py-3 font-poppins text-[0.8125rem] font-medium",
            style: {
              background: isDark ? "rgba(180,40,40,0.95)" : "rgba(220,50,50,0.95)",
              color: "#FFFFFF"
            }
          },
          /* @__PURE__ */ xn.createElement("svg", { width: "18", height: "18", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round", className: "shrink-0" }, /* @__PURE__ */ xn.createElement("path", { d: "M10.29 3.86L1.82 18a2 2 0 001.71 3h16.94a2 2 0 001.71-3L13.71 3.86a2 2 0 00-3.42 0z" }), /* @__PURE__ */ xn.createElement("line", { x1: "12", y1: "9", x2: "12", y2: "13" }), /* @__PURE__ */ xn.createElement("line", { x1: "12", y1: "17", x2: "12.01", y2: "17" })),
          /* @__PURE__ */ xn.createElement("span", null, "No internet connection")
        )
      ),
      document.body
    );
  };
  var NetworkBanner_default = NetworkBanner;

  // src/App.jsx
  var TRANSITIONS = {
    welcome: {
      signin: { exit: "phase-fade-out", enter: "phase-slide-up", exitDuration: 300 },
      onboarding: { exit: "phase-zoom-out", enter: "phase-slide-up", exitDuration: 450 },
      main: { exit: "phase-zoom-out", enter: "phase-scale-in", exitDuration: 450 }
    },
    signin: {
      welcome: { exit: "phase-slide-down", enter: "phase-zoom-in", exitDuration: 400 },
      main: { exit: "phase-zoom-out", enter: "phase-scale-in", exitDuration: 450 },
      onboarding: { exit: "phase-zoom-out", enter: "phase-slide-up", exitDuration: 450 }
    },
    onboarding: {
      main: { exit: "phase-complete-out", enter: "phase-scale-in", exitDuration: 600 },
      welcome: { exit: "phase-slide-down", enter: "phase-zoom-in", exitDuration: 400 }
    },
    main: {
      welcome: { exit: "phase-fade-out", enter: "phase-fade-in", exitDuration: 300 },
      onboarding: { exit: "phase-fade-out", enter: "phase-slide-up", exitDuration: 300 }
    }
  };
  var DEFAULT_TRANSITION = { exit: "phase-fade-out", enter: "phase-fade-in", exitDuration: 300 };
  var AppInner = () => {
    const { clearUser } = useUser();
    const { clearDailyQuestion } = useDailyQuestion();
    const [displayedPhase, setDisplayedPhase] = d2("loading");
    const [animClass, setAnimClass] = d2("");
    const [deviceId, setDeviceId] = d2(null);
    const transitionRef = A2(false);
    const displayedPhaseRef = A2("loading");
    const { getItem, setItem, removeItem } = useNativelyStorage();
    const SESSION_KEY = "bonds_session_active";
    const DEVICE_ID_KEY = "bonds_device_id";
    const ONBOARDING_KEY = "onboarding_complete";
    const AUTH_PENDING_KEY = "bonds_auth_pending";
    const [darkModePref, setDarkModePref] = d2(() => localStorage.getItem("bonds_dark_mode") || "system");
    const [systemTheme, setSystemTheme] = d2(getSystemTheme);
    y2(() => {
      getItem("bonds_dark_mode").then((val) => {
        if (val) setDarkModePref((prev) => val !== prev ? val : prev);
      });
    }, [getItem]);
    y2(() => {
      const mq = window.matchMedia("(prefers-color-scheme: dark)");
      const handler = (e3) => setSystemTheme(e3.matches ? "dark" : "light");
      mq.addEventListener("change", handler);
      return () => mq.removeEventListener("change", handler);
    }, []);
    const themeKey = darkModePref === "on" ? "dark" : darkModePref === "off" ? "light" : systemTheme;
    const theme = THEMES[themeKey];
    const transitionTo = q2((nextPhase) => {
      if (transitionRef.current) return;
      const fromPhase = displayedPhaseRef.current;
      if (fromPhase === nextPhase) return;
      if (fromPhase === "loading") {
        displayedPhaseRef.current = nextPhase;
        setDisplayedPhase(nextPhase);
        setAnimClass("phase-fade-in");
        return;
      }
      const t3 = TRANSITIONS[fromPhase]?.[nextPhase] || DEFAULT_TRANSITION;
      transitionRef.current = true;
      setAnimClass(t3.exit);
      setTimeout(() => {
        displayedPhaseRef.current = nextPhase;
        setDisplayedPhase(nextPhase);
        setAnimClass(t3.enter);
        transitionRef.current = false;
      }, t3.exitDuration);
    }, []);
    y2(() => {
      const initializeAnalytics2 = async () => {
        try {
          const token = window.APP_CONFIG?.MIXPANEL_TOKEN;
          if (!token) {
            console.warn("\u26A0\uFE0F Mixpanel Token NOT found in window.APP_CONFIG. Analytics disabled.");
            return;
          }
          mixpanel.init(token, { debug: true, track_pageview: false, persistence: "localStorage" });
          console.log("\u{1F4CA} Mixpanel Initialized");
          let storedId = await getItem(DEVICE_ID_KEY);
          if (storedId) {
            if (storedId.startsWith("$device:")) {
              storedId = storedId.replace(/^\$device:/, "");
              await setItem(DEVICE_ID_KEY, storedId);
            }
            mixpanel.identify(storedId);
          } else {
            const rawId = mixpanel.get_distinct_id();
            storedId = rawId.replace(/^\$device:/, "");
            await setItem(DEVICE_ID_KEY, storedId);
            mixpanel.identify(storedId);
          }
          setDeviceId(storedId);
        } catch (err) {
          console.error("\u274C Analytics Init Failed:", err);
        }
      };
      const checkSession = async () => {
        try {
          const [sessionResult, obResult, , onboardingState] = await Promise.all([
            getItem(SESSION_KEY),
            getItem(ONBOARDING_KEY),
            getItem(DEVICE_ID_KEY),
            getItem("onboarding_state")
          ]);
          if (displayedPhaseRef.current !== "loading") {
            console.log(`\u{1F4CB} [App] Session check skipped \u2014 already transitioned to ${displayedPhaseRef.current}`);
            return;
          }
          console.log(`\u{1F4CB} [App] Session check \u2014 session=${sessionResult}, onboarding=${obResult}, state=${onboardingState ? "saved" : "none"}`);
          if (sessionResult === "true") {
            transitionTo(obResult === "true" ? "main" : "onboarding");
          } else if (onboardingState) {
            setItem(SESSION_KEY, "true");
            transitionTo("onboarding");
          } else if (localStorage.getItem(AUTH_PENDING_KEY) === "true") {
          } else {
            transitionTo("welcome");
          }
        } catch (err) {
          console.error("\u274C App: Failed to check session:", err);
          if (displayedPhaseRef.current === "loading") transitionTo("welcome");
        }
      };
      initializeAnalytics2();
      window.appUI = window.appUI || {};
      window.appUI.setLoginState = (isLogged) => {
        console.log(`\u{1F511} [Auth] setLoginState called: isLogged=${isLogged}, currentPhase=${displayedPhaseRef.current}`);
        localStorage.removeItem(AUTH_PENDING_KEY);
        if (isLogged) {
          setItem(SESSION_KEY, "true");
          setItem(ONBOARDING_KEY, "true");
          transitionTo("main");
        } else {
          removeItem(SESSION_KEY);
          transitionTo("welcome");
        }
      };
      checkSession();
    }, [getItem, setItem, removeItem, transitionTo]);
    const handleWelcomeAction = (action) => {
      if (action === "go") {
        setItem(SESSION_KEY, "true");
        transitionTo("onboarding");
      } else if (action === "signin") {
        transitionTo("signin");
      }
    };
    const handleOnboardingComplete = () => {
      setItem(ONBOARDING_KEY, "true");
      transitionTo("main");
    };
    const handleLogout = () => {
      clearUser();
      clearDailyQuestion();
      removeItem(SESSION_KEY);
      removeItem(ONBOARDING_KEY);
      removeItem("onboarding_state");
      removeItem(DEVICE_ID_KEY);
      removeItem("bonds_dark_mode");
      transitionTo("welcome");
    };
    if (displayedPhase === "loading") {
      return /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0 bg-black flex flex-col items-center justify-center font-jakarta" }, /* @__PURE__ */ xn.createElement("div", { className: "animate-pulse flex flex-col items-center" }, /* @__PURE__ */ xn.createElement("svg", { width: "64", height: "64", viewBox: "0 0 64 64", fill: "none", className: "mb-4" }, /* @__PURE__ */ xn.createElement("circle", { cx: "32", cy: "32", r: "32", fill: "white", opacity: "0.2" }), /* @__PURE__ */ xn.createElement("rect", { x: "24", y: "24", width: "16", height: "16", fill: "#FF2258", className: "animate-spin-slow" })), /* @__PURE__ */ xn.createElement("span", { className: "text-white/40 text-xs tracking-widest uppercase" }, "Loading Bonds...")));
    }
    return /* @__PURE__ */ xn.createElement("div", { className: "absolute inset-0" }, /* @__PURE__ */ xn.createElement(NetworkBanner_default, { theme }), /* @__PURE__ */ xn.createElement("div", { className: `w-full h-full ${animClass}` }, displayedPhase === "welcome" && /* @__PURE__ */ xn.createElement(WelcomeScreen_default, { deviceId, onAction: handleWelcomeAction }), displayedPhase === "signin" && /* @__PURE__ */ xn.createElement(
      SignInScreen_default,
      {
        theme,
        onBack: () => transitionTo("welcome")
      }
    ), displayedPhase === "onboarding" && /* @__PURE__ */ xn.createElement(
      OnboardingFlow_default,
      {
        steps: mockOnboardingSteps_default,
        coins: 0,
        showCoins: true,
        theme,
        onComplete: handleOnboardingComplete,
        onBack: () => transitionTo("welcome")
      }
    ), displayedPhase === "main" && /* @__PURE__ */ xn.createElement(MainTabs_default, { onLogout: handleLogout })));
  };
  var App = () => /* @__PURE__ */ xn.createElement(UserProvider, null, /* @__PURE__ */ xn.createElement(DailyQuestionProvider, null, /* @__PURE__ */ xn.createElement(AppInner, null)));
  var App_default = App;

  // node_modules/@firebase/util/dist/postinstall.mjs
  var getDefaultsFromPostinstall = () => void 0;

  // node_modules/@firebase/util/dist/index.esm.js
  var stringToByteArray$1 = function(str) {
    const out = [];
    let p3 = 0;
    for (let i3 = 0; i3 < str.length; i3++) {
      let c3 = str.charCodeAt(i3);
      if (c3 < 128) {
        out[p3++] = c3;
      } else if (c3 < 2048) {
        out[p3++] = c3 >> 6 | 192;
        out[p3++] = c3 & 63 | 128;
      } else if ((c3 & 64512) === 55296 && i3 + 1 < str.length && (str.charCodeAt(i3 + 1) & 64512) === 56320) {
        c3 = 65536 + ((c3 & 1023) << 10) + (str.charCodeAt(++i3) & 1023);
        out[p3++] = c3 >> 18 | 240;
        out[p3++] = c3 >> 12 & 63 | 128;
        out[p3++] = c3 >> 6 & 63 | 128;
        out[p3++] = c3 & 63 | 128;
      } else {
        out[p3++] = c3 >> 12 | 224;
        out[p3++] = c3 >> 6 & 63 | 128;
        out[p3++] = c3 & 63 | 128;
      }
    }
    return out;
  };
  var byteArrayToString = function(bytes) {
    const out = [];
    let pos = 0, c3 = 0;
    while (pos < bytes.length) {
      const c1 = bytes[pos++];
      if (c1 < 128) {
        out[c3++] = String.fromCharCode(c1);
      } else if (c1 > 191 && c1 < 224) {
        const c22 = bytes[pos++];
        out[c3++] = String.fromCharCode((c1 & 31) << 6 | c22 & 63);
      } else if (c1 > 239 && c1 < 365) {
        const c22 = bytes[pos++];
        const c32 = bytes[pos++];
        const c4 = bytes[pos++];
        const u3 = ((c1 & 7) << 18 | (c22 & 63) << 12 | (c32 & 63) << 6 | c4 & 63) - 65536;
        out[c3++] = String.fromCharCode(55296 + (u3 >> 10));
        out[c3++] = String.fromCharCode(56320 + (u3 & 1023));
      } else {
        const c22 = bytes[pos++];
        const c32 = bytes[pos++];
        out[c3++] = String.fromCharCode((c1 & 15) << 12 | (c22 & 63) << 6 | c32 & 63);
      }
    }
    return out.join("");
  };
  var base64 = {
    /**
     * Maps bytes to characters.
     */
    byteToCharMap_: null,
    /**
     * Maps characters to bytes.
     */
    charToByteMap_: null,
    /**
     * Maps bytes to websafe characters.
     * @private
     */
    byteToCharMapWebSafe_: null,
    /**
     * Maps websafe characters to bytes.
     * @private
     */
    charToByteMapWebSafe_: null,
    /**
     * Our default alphabet, shared between
     * ENCODED_VALS and ENCODED_VALS_WEBSAFE
     */
    ENCODED_VALS_BASE: "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789",
    /**
     * Our default alphabet. Value 64 (=) is special; it means "nothing."
     */
    get ENCODED_VALS() {
      return this.ENCODED_VALS_BASE + "+/=";
    },
    /**
     * Our websafe alphabet.
     */
    get ENCODED_VALS_WEBSAFE() {
      return this.ENCODED_VALS_BASE + "-_.";
    },
    /**
     * Whether this browser supports the atob and btoa functions. This extension
     * started at Mozilla but is now implemented by many browsers. We use the
     * ASSUME_* variables to avoid pulling in the full useragent detection library
     * but still allowing the standard per-browser compilations.
     *
     */
    HAS_NATIVE_SUPPORT: typeof atob === "function",
    /**
     * Base64-encode an array of bytes.
     *
     * @param input An array of bytes (numbers with
     *     value in [0, 255]) to encode.
     * @param webSafe Boolean indicating we should use the
     *     alternative alphabet.
     * @return The base64 encoded string.
     */
    encodeByteArray(input2, webSafe) {
      if (!Array.isArray(input2)) {
        throw Error("encodeByteArray takes an array as a parameter");
      }
      this.init_();
      const byteToCharMap = webSafe ? this.byteToCharMapWebSafe_ : this.byteToCharMap_;
      const output = [];
      for (let i3 = 0; i3 < input2.length; i3 += 3) {
        const byte1 = input2[i3];
        const haveByte2 = i3 + 1 < input2.length;
        const byte2 = haveByte2 ? input2[i3 + 1] : 0;
        const haveByte3 = i3 + 2 < input2.length;
        const byte3 = haveByte3 ? input2[i3 + 2] : 0;
        const outByte1 = byte1 >> 2;
        const outByte2 = (byte1 & 3) << 4 | byte2 >> 4;
        let outByte3 = (byte2 & 15) << 2 | byte3 >> 6;
        let outByte4 = byte3 & 63;
        if (!haveByte3) {
          outByte4 = 64;
          if (!haveByte2) {
            outByte3 = 64;
          }
        }
        output.push(byteToCharMap[outByte1], byteToCharMap[outByte2], byteToCharMap[outByte3], byteToCharMap[outByte4]);
      }
      return output.join("");
    },
    /**
     * Base64-encode a string.
     *
     * @param input A string to encode.
     * @param webSafe If true, we should use the
     *     alternative alphabet.
     * @return The base64 encoded string.
     */
    encodeString(input2, webSafe) {
      if (this.HAS_NATIVE_SUPPORT && !webSafe) {
        return btoa(input2);
      }
      return this.encodeByteArray(stringToByteArray$1(input2), webSafe);
    },
    /**
     * Base64-decode a string.
     *
     * @param input to decode.
     * @param webSafe True if we should use the
     *     alternative alphabet.
     * @return string representing the decoded value.
     */
    decodeString(input2, webSafe) {
      if (this.HAS_NATIVE_SUPPORT && !webSafe) {
        return atob(input2);
      }
      return byteArrayToString(this.decodeStringToByteArray(input2, webSafe));
    },
    /**
     * Base64-decode a string.
     *
     * In base-64 decoding, groups of four characters are converted into three
     * bytes.  If the encoder did not apply padding, the input length may not
     * be a multiple of 4.
     *
     * In this case, the last group will have fewer than 4 characters, and
     * padding will be inferred.  If the group has one or two characters, it decodes
     * to one byte.  If the group has three characters, it decodes to two bytes.
     *
     * @param input Input to decode.
     * @param webSafe True if we should use the web-safe alphabet.
     * @return bytes representing the decoded value.
     */
    decodeStringToByteArray(input2, webSafe) {
      this.init_();
      const charToByteMap = webSafe ? this.charToByteMapWebSafe_ : this.charToByteMap_;
      const output = [];
      for (let i3 = 0; i3 < input2.length; ) {
        const byte1 = charToByteMap[input2.charAt(i3++)];
        const haveByte2 = i3 < input2.length;
        const byte2 = haveByte2 ? charToByteMap[input2.charAt(i3)] : 0;
        ++i3;
        const haveByte3 = i3 < input2.length;
        const byte3 = haveByte3 ? charToByteMap[input2.charAt(i3)] : 64;
        ++i3;
        const haveByte4 = i3 < input2.length;
        const byte4 = haveByte4 ? charToByteMap[input2.charAt(i3)] : 64;
        ++i3;
        if (byte1 == null || byte2 == null || byte3 == null || byte4 == null) {
          throw new DecodeBase64StringError();
        }
        const outByte1 = byte1 << 2 | byte2 >> 4;
        output.push(outByte1);
        if (byte3 !== 64) {
          const outByte2 = byte2 << 4 & 240 | byte3 >> 2;
          output.push(outByte2);
          if (byte4 !== 64) {
            const outByte3 = byte3 << 6 & 192 | byte4;
            output.push(outByte3);
          }
        }
      }
      return output;
    },
    /**
     * Lazy static initialization function. Called before
     * accessing any of the static map variables.
     * @private
     */
    init_() {
      if (!this.byteToCharMap_) {
        this.byteToCharMap_ = {};
        this.charToByteMap_ = {};
        this.byteToCharMapWebSafe_ = {};
        this.charToByteMapWebSafe_ = {};
        for (let i3 = 0; i3 < this.ENCODED_VALS.length; i3++) {
          this.byteToCharMap_[i3] = this.ENCODED_VALS.charAt(i3);
          this.charToByteMap_[this.byteToCharMap_[i3]] = i3;
          this.byteToCharMapWebSafe_[i3] = this.ENCODED_VALS_WEBSAFE.charAt(i3);
          this.charToByteMapWebSafe_[this.byteToCharMapWebSafe_[i3]] = i3;
          if (i3 >= this.ENCODED_VALS_BASE.length) {
            this.charToByteMap_[this.ENCODED_VALS_WEBSAFE.charAt(i3)] = i3;
            this.charToByteMapWebSafe_[this.ENCODED_VALS.charAt(i3)] = i3;
          }
        }
      }
    }
  };
  var DecodeBase64StringError = class extends Error {
    constructor() {
      super(...arguments);
      this.name = "DecodeBase64StringError";
    }
  };
  var base64Encode = function(str) {
    const utf8Bytes = stringToByteArray$1(str);
    return base64.encodeByteArray(utf8Bytes, true);
  };
  var base64urlEncodeWithoutPadding = function(str) {
    return base64Encode(str).replace(/\./g, "");
  };
  var base64Decode = function(str) {
    try {
      return base64.decodeString(str, true);
    } catch (e3) {
      console.error("base64Decode failed: ", e3);
    }
    return null;
  };
  function getGlobal() {
    if (typeof self !== "undefined") {
      return self;
    }
    if (typeof window !== "undefined") {
      return window;
    }
    if (typeof global !== "undefined") {
      return global;
    }
    throw new Error("Unable to locate global object.");
  }
  var getDefaultsFromGlobal = () => getGlobal().__FIREBASE_DEFAULTS__;
  var getDefaultsFromEnvVariable = () => {
    if (typeof process === "undefined" || typeof process.env === "undefined") {
      return;
    }
    const defaultsJsonString = process.env.__FIREBASE_DEFAULTS__;
    if (defaultsJsonString) {
      return JSON.parse(defaultsJsonString);
    }
  };
  var getDefaultsFromCookie = () => {
    if (typeof document === "undefined") {
      return;
    }
    let match;
    try {
      match = document.cookie.match(/__FIREBASE_DEFAULTS__=([^;]+)/);
    } catch (e3) {
      return;
    }
    const decoded = match && base64Decode(match[1]);
    return decoded && JSON.parse(decoded);
  };
  var getDefaults = () => {
    try {
      return getDefaultsFromPostinstall() || getDefaultsFromGlobal() || getDefaultsFromEnvVariable() || getDefaultsFromCookie();
    } catch (e3) {
      console.info(`Unable to get __FIREBASE_DEFAULTS__ due to: ${e3}`);
      return;
    }
  };
  var getDefaultAppConfig = () => getDefaults()?.config;
  var Deferred = class {
    constructor() {
      this.reject = () => {
      };
      this.resolve = () => {
      };
      this.promise = new Promise((resolve2, reject2) => {
        this.resolve = resolve2;
        this.reject = reject2;
      });
    }
    /**
     * Our API internals are not promisified and cannot because our callback APIs have subtle expectations around
     * invoking promises inline, which Promises are forbidden to do. This method accepts an optional node-style callback
     * and returns a node-style callback which will resolve or reject the Deferred's promise.
     */
    wrapCallback(callback) {
      return (error, value) => {
        if (error) {
          this.reject(error);
        } else {
          this.resolve(value);
        }
        if (typeof callback === "function") {
          this.promise.catch(() => {
          });
          if (callback.length === 1) {
            callback(error);
          } else {
            callback(error, value);
          }
        }
      };
    }
  };
  function isBrowserExtension() {
    const runtime = typeof chrome === "object" ? chrome.runtime : typeof browser === "object" ? browser.runtime : void 0;
    return typeof runtime === "object" && runtime.id !== void 0;
  }
  function isIndexedDBAvailable() {
    try {
      return typeof indexedDB === "object";
    } catch (e3) {
      return false;
    }
  }
  function validateIndexedDBOpenable() {
    return new Promise((resolve2, reject2) => {
      try {
        let preExist = true;
        const DB_CHECK_NAME = "validate-browser-context-for-indexeddb-analytics-module";
        const request = self.indexedDB.open(DB_CHECK_NAME);
        request.onsuccess = () => {
          request.result.close();
          if (!preExist) {
            self.indexedDB.deleteDatabase(DB_CHECK_NAME);
          }
          resolve2(true);
        };
        request.onupgradeneeded = () => {
          preExist = false;
        };
        request.onerror = () => {
          reject2(request.error?.message || "");
        };
      } catch (error) {
        reject2(error);
      }
    });
  }
  function areCookiesEnabled() {
    if (typeof navigator === "undefined" || !navigator.cookieEnabled) {
      return false;
    }
    return true;
  }
  var ERROR_NAME = "FirebaseError";
  var FirebaseError = class _FirebaseError extends Error {
    constructor(code, message, customData) {
      super(message);
      this.code = code;
      this.customData = customData;
      this.name = ERROR_NAME;
      Object.setPrototypeOf(this, _FirebaseError.prototype);
      if (Error.captureStackTrace) {
        Error.captureStackTrace(this, ErrorFactory.prototype.create);
      }
    }
  };
  var ErrorFactory = class {
    constructor(service, serviceName, errors) {
      this.service = service;
      this.serviceName = serviceName;
      this.errors = errors;
    }
    create(code, ...data) {
      const customData = data[0] || {};
      const fullCode = `${this.service}/${code}`;
      const template = this.errors[code];
      const message = template ? replaceTemplate(template, customData) : "Error";
      const fullMessage = `${this.serviceName}: ${message} (${fullCode}).`;
      const error = new FirebaseError(fullCode, fullMessage, customData);
      return error;
    }
  };
  function replaceTemplate(template, data) {
    return template.replace(PATTERN, (_4, key) => {
      const value = data[key];
      return value != null ? String(value) : `<${key}?>`;
    });
  }
  var PATTERN = /\{\$([^}]+)}/g;
  function deepEqual(a3, b2) {
    if (a3 === b2) {
      return true;
    }
    const aKeys = Object.keys(a3);
    const bKeys = Object.keys(b2);
    for (const k4 of aKeys) {
      if (!bKeys.includes(k4)) {
        return false;
      }
      const aProp = a3[k4];
      const bProp = b2[k4];
      if (isObject2(aProp) && isObject2(bProp)) {
        if (!deepEqual(aProp, bProp)) {
          return false;
        }
      } else if (aProp !== bProp) {
        return false;
      }
    }
    for (const k4 of bKeys) {
      if (!aKeys.includes(k4)) {
        return false;
      }
    }
    return true;
  }
  function isObject2(thing) {
    return thing !== null && typeof thing === "object";
  }
  var DEFAULT_INTERVAL_MILLIS = 1e3;
  var DEFAULT_BACKOFF_FACTOR = 2;
  var MAX_VALUE_MILLIS = 4 * 60 * 60 * 1e3;
  var RANDOM_FACTOR = 0.5;
  function calculateBackoffMillis(backoffCount, intervalMillis = DEFAULT_INTERVAL_MILLIS, backoffFactor = DEFAULT_BACKOFF_FACTOR) {
    const currBaseValue = intervalMillis * Math.pow(backoffFactor, backoffCount);
    const randomWait = Math.round(
      // A fraction of the backoff value to add/subtract.
      // Deviation: changes multiplication order to improve readability.
      RANDOM_FACTOR * currBaseValue * // A random float (rounded to int by Math.round above) in the range [-1, 1]. Determines
      // if we add or subtract.
      (Math.random() - 0.5) * 2
    );
    return Math.min(MAX_VALUE_MILLIS, currBaseValue + randomWait);
  }
  function getModularInstance(service) {
    if (service && service._delegate) {
      return service._delegate;
    } else {
      return service;
    }
  }

  // node_modules/@firebase/component/dist/esm/index.esm.js
  var Component = class {
    /**
     *
     * @param name The public service name, e.g. app, auth, firestore, database
     * @param instanceFactory Service factory responsible for creating the public interface
     * @param type whether the service provided by the component is public or private
     */
    constructor(name5, instanceFactory, type) {
      this.name = name5;
      this.instanceFactory = instanceFactory;
      this.type = type;
      this.multipleInstances = false;
      this.serviceProps = {};
      this.instantiationMode = "LAZY";
      this.onInstanceCreated = null;
    }
    setInstantiationMode(mode) {
      this.instantiationMode = mode;
      return this;
    }
    setMultipleInstances(multipleInstances) {
      this.multipleInstances = multipleInstances;
      return this;
    }
    setServiceProps(props) {
      this.serviceProps = props;
      return this;
    }
    setInstanceCreatedCallback(callback) {
      this.onInstanceCreated = callback;
      return this;
    }
  };
  var DEFAULT_ENTRY_NAME = "[DEFAULT]";
  var Provider = class {
    constructor(name5, container2) {
      this.name = name5;
      this.container = container2;
      this.component = null;
      this.instances = /* @__PURE__ */ new Map();
      this.instancesDeferred = /* @__PURE__ */ new Map();
      this.instancesOptions = /* @__PURE__ */ new Map();
      this.onInitCallbacks = /* @__PURE__ */ new Map();
    }
    /**
     * @param identifier A provider can provide multiple instances of a service
     * if this.component.multipleInstances is true.
     */
    get(identifier) {
      const normalizedIdentifier = this.normalizeInstanceIdentifier(identifier);
      if (!this.instancesDeferred.has(normalizedIdentifier)) {
        const deferred = new Deferred();
        this.instancesDeferred.set(normalizedIdentifier, deferred);
        if (this.isInitialized(normalizedIdentifier) || this.shouldAutoInitialize()) {
          try {
            const instance = this.getOrInitializeService({
              instanceIdentifier: normalizedIdentifier
            });
            if (instance) {
              deferred.resolve(instance);
            }
          } catch (e3) {
          }
        }
      }
      return this.instancesDeferred.get(normalizedIdentifier).promise;
    }
    getImmediate(options) {
      const normalizedIdentifier = this.normalizeInstanceIdentifier(options?.identifier);
      const optional = options?.optional ?? false;
      if (this.isInitialized(normalizedIdentifier) || this.shouldAutoInitialize()) {
        try {
          return this.getOrInitializeService({
            instanceIdentifier: normalizedIdentifier
          });
        } catch (e3) {
          if (optional) {
            return null;
          } else {
            throw e3;
          }
        }
      } else {
        if (optional) {
          return null;
        } else {
          throw Error(`Service ${this.name} is not available`);
        }
      }
    }
    getComponent() {
      return this.component;
    }
    setComponent(component) {
      if (component.name !== this.name) {
        throw Error(`Mismatching Component ${component.name} for Provider ${this.name}.`);
      }
      if (this.component) {
        throw Error(`Component for ${this.name} has already been provided`);
      }
      this.component = component;
      if (!this.shouldAutoInitialize()) {
        return;
      }
      if (isComponentEager(component)) {
        try {
          this.getOrInitializeService({ instanceIdentifier: DEFAULT_ENTRY_NAME });
        } catch (e3) {
        }
      }
      for (const [instanceIdentifier, instanceDeferred] of this.instancesDeferred.entries()) {
        const normalizedIdentifier = this.normalizeInstanceIdentifier(instanceIdentifier);
        try {
          const instance = this.getOrInitializeService({
            instanceIdentifier: normalizedIdentifier
          });
          instanceDeferred.resolve(instance);
        } catch (e3) {
        }
      }
    }
    clearInstance(identifier = DEFAULT_ENTRY_NAME) {
      this.instancesDeferred.delete(identifier);
      this.instancesOptions.delete(identifier);
      this.instances.delete(identifier);
    }
    // app.delete() will call this method on every provider to delete the services
    // TODO: should we mark the provider as deleted?
    async delete() {
      const services = Array.from(this.instances.values());
      await Promise.all([
        ...services.filter((service) => "INTERNAL" in service).map((service) => service.INTERNAL.delete()),
        ...services.filter((service) => "_delete" in service).map((service) => service._delete())
      ]);
    }
    isComponentSet() {
      return this.component != null;
    }
    isInitialized(identifier = DEFAULT_ENTRY_NAME) {
      return this.instances.has(identifier);
    }
    getOptions(identifier = DEFAULT_ENTRY_NAME) {
      return this.instancesOptions.get(identifier) || {};
    }
    initialize(opts = {}) {
      const { options = {} } = opts;
      const normalizedIdentifier = this.normalizeInstanceIdentifier(opts.instanceIdentifier);
      if (this.isInitialized(normalizedIdentifier)) {
        throw Error(`${this.name}(${normalizedIdentifier}) has already been initialized`);
      }
      if (!this.isComponentSet()) {
        throw Error(`Component ${this.name} has not been registered yet`);
      }
      const instance = this.getOrInitializeService({
        instanceIdentifier: normalizedIdentifier,
        options
      });
      for (const [instanceIdentifier, instanceDeferred] of this.instancesDeferred.entries()) {
        const normalizedDeferredIdentifier = this.normalizeInstanceIdentifier(instanceIdentifier);
        if (normalizedIdentifier === normalizedDeferredIdentifier) {
          instanceDeferred.resolve(instance);
        }
      }
      return instance;
    }
    /**
     *
     * @param callback - a function that will be invoked  after the provider has been initialized by calling provider.initialize().
     * The function is invoked SYNCHRONOUSLY, so it should not execute any longrunning tasks in order to not block the program.
     *
     * @param identifier An optional instance identifier
     * @returns a function to unregister the callback
     */
    onInit(callback, identifier) {
      const normalizedIdentifier = this.normalizeInstanceIdentifier(identifier);
      const existingCallbacks = this.onInitCallbacks.get(normalizedIdentifier) ?? /* @__PURE__ */ new Set();
      existingCallbacks.add(callback);
      this.onInitCallbacks.set(normalizedIdentifier, existingCallbacks);
      const existingInstance = this.instances.get(normalizedIdentifier);
      if (existingInstance) {
        callback(existingInstance, normalizedIdentifier);
      }
      return () => {
        existingCallbacks.delete(callback);
      };
    }
    /**
     * Invoke onInit callbacks synchronously
     * @param instance the service instance`
     */
    invokeOnInitCallbacks(instance, identifier) {
      const callbacks = this.onInitCallbacks.get(identifier);
      if (!callbacks) {
        return;
      }
      for (const callback of callbacks) {
        try {
          callback(instance, identifier);
        } catch {
        }
      }
    }
    getOrInitializeService({ instanceIdentifier, options = {} }) {
      let instance = this.instances.get(instanceIdentifier);
      if (!instance && this.component) {
        instance = this.component.instanceFactory(this.container, {
          instanceIdentifier: normalizeIdentifierForFactory(instanceIdentifier),
          options
        });
        this.instances.set(instanceIdentifier, instance);
        this.instancesOptions.set(instanceIdentifier, options);
        this.invokeOnInitCallbacks(instance, instanceIdentifier);
        if (this.component.onInstanceCreated) {
          try {
            this.component.onInstanceCreated(this.container, instanceIdentifier, instance);
          } catch {
          }
        }
      }
      return instance || null;
    }
    normalizeInstanceIdentifier(identifier = DEFAULT_ENTRY_NAME) {
      if (this.component) {
        return this.component.multipleInstances ? identifier : DEFAULT_ENTRY_NAME;
      } else {
        return identifier;
      }
    }
    shouldAutoInitialize() {
      return !!this.component && this.component.instantiationMode !== "EXPLICIT";
    }
  };
  function normalizeIdentifierForFactory(identifier) {
    return identifier === DEFAULT_ENTRY_NAME ? void 0 : identifier;
  }
  function isComponentEager(component) {
    return component.instantiationMode === "EAGER";
  }
  var ComponentContainer = class {
    constructor(name5) {
      this.name = name5;
      this.providers = /* @__PURE__ */ new Map();
    }
    /**
     *
     * @param component Component being added
     * @param overwrite When a component with the same name has already been registered,
     * if overwrite is true: overwrite the existing component with the new component and create a new
     * provider with the new component. It can be useful in tests where you want to use different mocks
     * for different tests.
     * if overwrite is false: throw an exception
     */
    addComponent(component) {
      const provider = this.getProvider(component.name);
      if (provider.isComponentSet()) {
        throw new Error(`Component ${component.name} has already been registered with ${this.name}`);
      }
      provider.setComponent(component);
    }
    addOrOverwriteComponent(component) {
      const provider = this.getProvider(component.name);
      if (provider.isComponentSet()) {
        this.providers.delete(component.name);
      }
      this.addComponent(component);
    }
    /**
     * getProvider provides a type safe interface where it can only be called with a field name
     * present in NameServiceMapping interface.
     *
     * Firebase SDKs providing services should extend NameServiceMapping interface to register
     * themselves.
     */
    getProvider(name5) {
      if (this.providers.has(name5)) {
        return this.providers.get(name5);
      }
      const provider = new Provider(name5, this);
      this.providers.set(name5, provider);
      return provider;
    }
    getProviders() {
      return Array.from(this.providers.values());
    }
  };

  // node_modules/@firebase/logger/dist/esm/index.esm.js
  var instances2 = [];
  var LogLevel;
  (function(LogLevel2) {
    LogLevel2[LogLevel2["DEBUG"] = 0] = "DEBUG";
    LogLevel2[LogLevel2["VERBOSE"] = 1] = "VERBOSE";
    LogLevel2[LogLevel2["INFO"] = 2] = "INFO";
    LogLevel2[LogLevel2["WARN"] = 3] = "WARN";
    LogLevel2[LogLevel2["ERROR"] = 4] = "ERROR";
    LogLevel2[LogLevel2["SILENT"] = 5] = "SILENT";
  })(LogLevel || (LogLevel = {}));
  var levelStringToEnum = {
    "debug": LogLevel.DEBUG,
    "verbose": LogLevel.VERBOSE,
    "info": LogLevel.INFO,
    "warn": LogLevel.WARN,
    "error": LogLevel.ERROR,
    "silent": LogLevel.SILENT
  };
  var defaultLogLevel = LogLevel.INFO;
  var ConsoleMethod = {
    [LogLevel.DEBUG]: "log",
    [LogLevel.VERBOSE]: "log",
    [LogLevel.INFO]: "info",
    [LogLevel.WARN]: "warn",
    [LogLevel.ERROR]: "error"
  };
  var defaultLogHandler = (instance, logType, ...args) => {
    if (logType < instance.logLevel) {
      return;
    }
    const now = (/* @__PURE__ */ new Date()).toISOString();
    const method = ConsoleMethod[logType];
    if (method) {
      console[method](`[${now}]  ${instance.name}:`, ...args);
    } else {
      throw new Error(`Attempted to log a message with an invalid logType (value: ${logType})`);
    }
  };
  var Logger = class {
    /**
     * Gives you an instance of a Logger to capture messages according to
     * Firebase's logging scheme.
     *
     * @param name The name that the logs will be associated with
     */
    constructor(name5) {
      this.name = name5;
      this._logLevel = defaultLogLevel;
      this._logHandler = defaultLogHandler;
      this._userLogHandler = null;
      instances2.push(this);
    }
    get logLevel() {
      return this._logLevel;
    }
    set logLevel(val) {
      if (!(val in LogLevel)) {
        throw new TypeError(`Invalid value "${val}" assigned to \`logLevel\``);
      }
      this._logLevel = val;
    }
    // Workaround for setter/getter having to be the same type.
    setLogLevel(val) {
      this._logLevel = typeof val === "string" ? levelStringToEnum[val] : val;
    }
    get logHandler() {
      return this._logHandler;
    }
    set logHandler(val) {
      if (typeof val !== "function") {
        throw new TypeError("Value assigned to `logHandler` must be a function");
      }
      this._logHandler = val;
    }
    get userLogHandler() {
      return this._userLogHandler;
    }
    set userLogHandler(val) {
      this._userLogHandler = val;
    }
    /**
     * The functions below are all based on the `console` interface
     */
    debug(...args) {
      this._userLogHandler && this._userLogHandler(this, LogLevel.DEBUG, ...args);
      this._logHandler(this, LogLevel.DEBUG, ...args);
    }
    log(...args) {
      this._userLogHandler && this._userLogHandler(this, LogLevel.VERBOSE, ...args);
      this._logHandler(this, LogLevel.VERBOSE, ...args);
    }
    info(...args) {
      this._userLogHandler && this._userLogHandler(this, LogLevel.INFO, ...args);
      this._logHandler(this, LogLevel.INFO, ...args);
    }
    warn(...args) {
      this._userLogHandler && this._userLogHandler(this, LogLevel.WARN, ...args);
      this._logHandler(this, LogLevel.WARN, ...args);
    }
    error(...args) {
      this._userLogHandler && this._userLogHandler(this, LogLevel.ERROR, ...args);
      this._logHandler(this, LogLevel.ERROR, ...args);
    }
  };

  // node_modules/idb/build/wrap-idb-value.js
  var instanceOfAny = (object, constructors) => constructors.some((c3) => object instanceof c3);
  var idbProxyableTypes;
  var cursorAdvanceMethods;
  function getIdbProxyableTypes() {
    return idbProxyableTypes || (idbProxyableTypes = [
      IDBDatabase,
      IDBObjectStore,
      IDBIndex,
      IDBCursor,
      IDBTransaction
    ]);
  }
  function getCursorAdvanceMethods() {
    return cursorAdvanceMethods || (cursorAdvanceMethods = [
      IDBCursor.prototype.advance,
      IDBCursor.prototype.continue,
      IDBCursor.prototype.continuePrimaryKey
    ]);
  }
  var cursorRequestMap = /* @__PURE__ */ new WeakMap();
  var transactionDoneMap = /* @__PURE__ */ new WeakMap();
  var transactionStoreNamesMap = /* @__PURE__ */ new WeakMap();
  var transformCache = /* @__PURE__ */ new WeakMap();
  var reverseTransformCache = /* @__PURE__ */ new WeakMap();
  function promisifyRequest(request) {
    const promise = new Promise((resolve2, reject2) => {
      const unlisten = () => {
        request.removeEventListener("success", success);
        request.removeEventListener("error", error);
      };
      const success = () => {
        resolve2(wrap(request.result));
        unlisten();
      };
      const error = () => {
        reject2(request.error);
        unlisten();
      };
      request.addEventListener("success", success);
      request.addEventListener("error", error);
    });
    promise.then((value) => {
      if (value instanceof IDBCursor) {
        cursorRequestMap.set(value, request);
      }
    }).catch(() => {
    });
    reverseTransformCache.set(promise, request);
    return promise;
  }
  function cacheDonePromiseForTransaction(tx) {
    if (transactionDoneMap.has(tx))
      return;
    const done = new Promise((resolve2, reject2) => {
      const unlisten = () => {
        tx.removeEventListener("complete", complete);
        tx.removeEventListener("error", error);
        tx.removeEventListener("abort", error);
      };
      const complete = () => {
        resolve2();
        unlisten();
      };
      const error = () => {
        reject2(tx.error || new DOMException("AbortError", "AbortError"));
        unlisten();
      };
      tx.addEventListener("complete", complete);
      tx.addEventListener("error", error);
      tx.addEventListener("abort", error);
    });
    transactionDoneMap.set(tx, done);
  }
  var idbProxyTraps = {
    get(target, prop, receiver) {
      if (target instanceof IDBTransaction) {
        if (prop === "done")
          return transactionDoneMap.get(target);
        if (prop === "objectStoreNames") {
          return target.objectStoreNames || transactionStoreNamesMap.get(target);
        }
        if (prop === "store") {
          return receiver.objectStoreNames[1] ? void 0 : receiver.objectStore(receiver.objectStoreNames[0]);
        }
      }
      return wrap(target[prop]);
    },
    set(target, prop, value) {
      target[prop] = value;
      return true;
    },
    has(target, prop) {
      if (target instanceof IDBTransaction && (prop === "done" || prop === "store")) {
        return true;
      }
      return prop in target;
    }
  };
  function replaceTraps(callback) {
    idbProxyTraps = callback(idbProxyTraps);
  }
  function wrapFunction(func) {
    if (func === IDBDatabase.prototype.transaction && !("objectStoreNames" in IDBTransaction.prototype)) {
      return function(storeNames, ...args) {
        const tx = func.call(unwrap(this), storeNames, ...args);
        transactionStoreNamesMap.set(tx, storeNames.sort ? storeNames.sort() : [storeNames]);
        return wrap(tx);
      };
    }
    if (getCursorAdvanceMethods().includes(func)) {
      return function(...args) {
        func.apply(unwrap(this), args);
        return wrap(cursorRequestMap.get(this));
      };
    }
    return function(...args) {
      return wrap(func.apply(unwrap(this), args));
    };
  }
  function transformCachableValue(value) {
    if (typeof value === "function")
      return wrapFunction(value);
    if (value instanceof IDBTransaction)
      cacheDonePromiseForTransaction(value);
    if (instanceOfAny(value, getIdbProxyableTypes()))
      return new Proxy(value, idbProxyTraps);
    return value;
  }
  function wrap(value) {
    if (value instanceof IDBRequest)
      return promisifyRequest(value);
    if (transformCache.has(value))
      return transformCache.get(value);
    const newValue = transformCachableValue(value);
    if (newValue !== value) {
      transformCache.set(value, newValue);
      reverseTransformCache.set(newValue, value);
    }
    return newValue;
  }
  var unwrap = (value) => reverseTransformCache.get(value);

  // node_modules/idb/build/index.js
  function openDB(name5, version4, { blocked, upgrade, blocking, terminated } = {}) {
    const request = indexedDB.open(name5, version4);
    const openPromise = wrap(request);
    if (upgrade) {
      request.addEventListener("upgradeneeded", (event) => {
        upgrade(wrap(request.result), event.oldVersion, event.newVersion, wrap(request.transaction), event);
      });
    }
    if (blocked) {
      request.addEventListener("blocked", (event) => blocked(
        // Casting due to https://github.com/microsoft/TypeScript-DOM-lib-generator/pull/1405
        event.oldVersion,
        event.newVersion,
        event
      ));
    }
    openPromise.then((db) => {
      if (terminated)
        db.addEventListener("close", () => terminated());
      if (blocking) {
        db.addEventListener("versionchange", (event) => blocking(event.oldVersion, event.newVersion, event));
      }
    }).catch(() => {
    });
    return openPromise;
  }
  var readMethods = ["get", "getKey", "getAll", "getAllKeys", "count"];
  var writeMethods = ["put", "add", "delete", "clear"];
  var cachedMethods = /* @__PURE__ */ new Map();
  function getMethod(target, prop) {
    if (!(target instanceof IDBDatabase && !(prop in target) && typeof prop === "string")) {
      return;
    }
    if (cachedMethods.get(prop))
      return cachedMethods.get(prop);
    const targetFuncName = prop.replace(/FromIndex$/, "");
    const useIndex = prop !== targetFuncName;
    const isWrite = writeMethods.includes(targetFuncName);
    if (
      // Bail if the target doesn't exist on the target. Eg, getAll isn't in Edge.
      !(targetFuncName in (useIndex ? IDBIndex : IDBObjectStore).prototype) || !(isWrite || readMethods.includes(targetFuncName))
    ) {
      return;
    }
    const method = async function(storeName, ...args) {
      const tx = this.transaction(storeName, isWrite ? "readwrite" : "readonly");
      let target2 = tx.store;
      if (useIndex)
        target2 = target2.index(args.shift());
      return (await Promise.all([
        target2[targetFuncName](...args),
        isWrite && tx.done
      ]))[0];
    };
    cachedMethods.set(prop, method);
    return method;
  }
  replaceTraps((oldTraps) => ({
    ...oldTraps,
    get: (target, prop, receiver) => getMethod(target, prop) || oldTraps.get(target, prop, receiver),
    has: (target, prop) => !!getMethod(target, prop) || oldTraps.has(target, prop)
  }));

  // node_modules/@firebase/app/dist/esm/index.esm.js
  var PlatformLoggerServiceImpl = class {
    constructor(container2) {
      this.container = container2;
    }
    // In initial implementation, this will be called by installations on
    // auth token refresh, and installations will send this string.
    getPlatformInfoString() {
      const providers = this.container.getProviders();
      return providers.map((provider) => {
        if (isVersionServiceProvider(provider)) {
          const service = provider.getImmediate();
          return `${service.library}/${service.version}`;
        } else {
          return null;
        }
      }).filter((logString) => logString).join(" ");
    }
  };
  function isVersionServiceProvider(provider) {
    const component = provider.getComponent();
    return component?.type === "VERSION";
  }
  var name$q = "@firebase/app";
  var version$1 = "0.14.9";
  var logger2 = new Logger("@firebase/app");
  var name$p = "@firebase/app-compat";
  var name$o = "@firebase/analytics-compat";
  var name$n = "@firebase/analytics";
  var name$m = "@firebase/app-check-compat";
  var name$l = "@firebase/app-check";
  var name$k = "@firebase/auth";
  var name$j = "@firebase/auth-compat";
  var name$i = "@firebase/database";
  var name$h = "@firebase/data-connect";
  var name$g = "@firebase/database-compat";
  var name$f = "@firebase/functions";
  var name$e = "@firebase/functions-compat";
  var name$d = "@firebase/installations";
  var name$c = "@firebase/installations-compat";
  var name$b = "@firebase/messaging";
  var name$a = "@firebase/messaging-compat";
  var name$9 = "@firebase/performance";
  var name$8 = "@firebase/performance-compat";
  var name$7 = "@firebase/remote-config";
  var name$6 = "@firebase/remote-config-compat";
  var name$5 = "@firebase/storage";
  var name$4 = "@firebase/storage-compat";
  var name$3 = "@firebase/firestore";
  var name$2 = "@firebase/ai";
  var name$1 = "@firebase/firestore-compat";
  var name = "firebase";
  var DEFAULT_ENTRY_NAME2 = "[DEFAULT]";
  var PLATFORM_LOG_STRING = {
    [name$q]: "fire-core",
    [name$p]: "fire-core-compat",
    [name$n]: "fire-analytics",
    [name$o]: "fire-analytics-compat",
    [name$l]: "fire-app-check",
    [name$m]: "fire-app-check-compat",
    [name$k]: "fire-auth",
    [name$j]: "fire-auth-compat",
    [name$i]: "fire-rtdb",
    [name$h]: "fire-data-connect",
    [name$g]: "fire-rtdb-compat",
    [name$f]: "fire-fn",
    [name$e]: "fire-fn-compat",
    [name$d]: "fire-iid",
    [name$c]: "fire-iid-compat",
    [name$b]: "fire-fcm",
    [name$a]: "fire-fcm-compat",
    [name$9]: "fire-perf",
    [name$8]: "fire-perf-compat",
    [name$7]: "fire-rc",
    [name$6]: "fire-rc-compat",
    [name$5]: "fire-gcs",
    [name$4]: "fire-gcs-compat",
    [name$3]: "fire-fst",
    [name$1]: "fire-fst-compat",
    [name$2]: "fire-vertex",
    "fire-js": "fire-js",
    // Platform identifier for JS SDK.
    [name]: "fire-js-all"
  };
  var _apps = /* @__PURE__ */ new Map();
  var _serverApps = /* @__PURE__ */ new Map();
  var _components = /* @__PURE__ */ new Map();
  function _addComponent(app, component) {
    try {
      app.container.addComponent(component);
    } catch (e3) {
      logger2.debug(`Component ${component.name} failed to register with FirebaseApp ${app.name}`, e3);
    }
  }
  function _registerComponent(component) {
    const componentName = component.name;
    if (_components.has(componentName)) {
      logger2.debug(`There were multiple attempts to register component ${componentName}.`);
      return false;
    }
    _components.set(componentName, component);
    for (const app of _apps.values()) {
      _addComponent(app, component);
    }
    for (const serverApp of _serverApps.values()) {
      _addComponent(serverApp, component);
    }
    return true;
  }
  function _getProvider(app, name5) {
    const heartbeatController = app.container.getProvider("heartbeat").getImmediate({ optional: true });
    if (heartbeatController) {
      void heartbeatController.triggerHeartbeat();
    }
    return app.container.getProvider(name5);
  }
  var ERRORS = {
    [
      "no-app"
      /* AppError.NO_APP */
    ]: "No Firebase App '{$appName}' has been created - call initializeApp() first",
    [
      "bad-app-name"
      /* AppError.BAD_APP_NAME */
    ]: "Illegal App name: '{$appName}'",
    [
      "duplicate-app"
      /* AppError.DUPLICATE_APP */
    ]: "Firebase App named '{$appName}' already exists with different options or config",
    [
      "app-deleted"
      /* AppError.APP_DELETED */
    ]: "Firebase App named '{$appName}' already deleted",
    [
      "server-app-deleted"
      /* AppError.SERVER_APP_DELETED */
    ]: "Firebase Server App has been deleted",
    [
      "no-options"
      /* AppError.NO_OPTIONS */
    ]: "Need to provide options, when not being deployed to hosting via source.",
    [
      "invalid-app-argument"
      /* AppError.INVALID_APP_ARGUMENT */
    ]: "firebase.{$appName}() takes either no argument or a Firebase App instance.",
    [
      "invalid-log-argument"
      /* AppError.INVALID_LOG_ARGUMENT */
    ]: "First argument to `onLog` must be null or a function.",
    [
      "idb-open"
      /* AppError.IDB_OPEN */
    ]: "Error thrown when opening IndexedDB. Original error: {$originalErrorMessage}.",
    [
      "idb-get"
      /* AppError.IDB_GET */
    ]: "Error thrown when reading from IndexedDB. Original error: {$originalErrorMessage}.",
    [
      "idb-set"
      /* AppError.IDB_WRITE */
    ]: "Error thrown when writing to IndexedDB. Original error: {$originalErrorMessage}.",
    [
      "idb-delete"
      /* AppError.IDB_DELETE */
    ]: "Error thrown when deleting from IndexedDB. Original error: {$originalErrorMessage}.",
    [
      "finalization-registry-not-supported"
      /* AppError.FINALIZATION_REGISTRY_NOT_SUPPORTED */
    ]: "FirebaseServerApp deleteOnDeref field defined but the JS runtime does not support FinalizationRegistry.",
    [
      "invalid-server-app-environment"
      /* AppError.INVALID_SERVER_APP_ENVIRONMENT */
    ]: "FirebaseServerApp is not for use in browser environments."
  };
  var ERROR_FACTORY = new ErrorFactory("app", "Firebase", ERRORS);
  var FirebaseAppImpl = class {
    constructor(options, config, container2) {
      this._isDeleted = false;
      this._options = { ...options };
      this._config = { ...config };
      this._name = config.name;
      this._automaticDataCollectionEnabled = config.automaticDataCollectionEnabled;
      this._container = container2;
      this.container.addComponent(new Component(
        "app",
        () => this,
        "PUBLIC"
        /* ComponentType.PUBLIC */
      ));
    }
    get automaticDataCollectionEnabled() {
      this.checkDestroyed();
      return this._automaticDataCollectionEnabled;
    }
    set automaticDataCollectionEnabled(val) {
      this.checkDestroyed();
      this._automaticDataCollectionEnabled = val;
    }
    get name() {
      this.checkDestroyed();
      return this._name;
    }
    get options() {
      this.checkDestroyed();
      return this._options;
    }
    get config() {
      this.checkDestroyed();
      return this._config;
    }
    get container() {
      return this._container;
    }
    get isDeleted() {
      return this._isDeleted;
    }
    set isDeleted(val) {
      this._isDeleted = val;
    }
    /**
     * This function will throw an Error if the App has already been deleted -
     * use before performing API actions on the App.
     */
    checkDestroyed() {
      if (this.isDeleted) {
        throw ERROR_FACTORY.create("app-deleted", { appName: this._name });
      }
    }
  };
  function initializeApp(_options, rawConfig = {}) {
    let options = _options;
    if (typeof rawConfig !== "object") {
      const name6 = rawConfig;
      rawConfig = { name: name6 };
    }
    const config = {
      name: DEFAULT_ENTRY_NAME2,
      automaticDataCollectionEnabled: true,
      ...rawConfig
    };
    const name5 = config.name;
    if (typeof name5 !== "string" || !name5) {
      throw ERROR_FACTORY.create("bad-app-name", {
        appName: String(name5)
      });
    }
    options || (options = getDefaultAppConfig());
    if (!options) {
      throw ERROR_FACTORY.create(
        "no-options"
        /* AppError.NO_OPTIONS */
      );
    }
    const existingApp = _apps.get(name5);
    if (existingApp) {
      if (deepEqual(options, existingApp.options) && deepEqual(config, existingApp.config)) {
        return existingApp;
      } else {
        throw ERROR_FACTORY.create("duplicate-app", { appName: name5 });
      }
    }
    const container2 = new ComponentContainer(name5);
    for (const component of _components.values()) {
      container2.addComponent(component);
    }
    const newApp = new FirebaseAppImpl(options, config, container2);
    _apps.set(name5, newApp);
    return newApp;
  }
  function getApp(name5 = DEFAULT_ENTRY_NAME2) {
    const app = _apps.get(name5);
    if (!app && name5 === DEFAULT_ENTRY_NAME2 && getDefaultAppConfig()) {
      return initializeApp();
    }
    if (!app) {
      throw ERROR_FACTORY.create("no-app", { appName: name5 });
    }
    return app;
  }
  function registerVersion(libraryKeyOrName, version4, variant) {
    let library = PLATFORM_LOG_STRING[libraryKeyOrName] ?? libraryKeyOrName;
    if (variant) {
      library += `-${variant}`;
    }
    const libraryMismatch = library.match(/\s|\//);
    const versionMismatch = version4.match(/\s|\//);
    if (libraryMismatch || versionMismatch) {
      const warning2 = [
        `Unable to register library "${library}" with version "${version4}":`
      ];
      if (libraryMismatch) {
        warning2.push(`library name "${library}" contains illegal characters (whitespace or "/")`);
      }
      if (libraryMismatch && versionMismatch) {
        warning2.push("and");
      }
      if (versionMismatch) {
        warning2.push(`version name "${version4}" contains illegal characters (whitespace or "/")`);
      }
      logger2.warn(warning2.join(" "));
      return;
    }
    _registerComponent(new Component(
      `${library}-version`,
      () => ({ library, version: version4 }),
      "VERSION"
      /* ComponentType.VERSION */
    ));
  }
  var DB_NAME = "firebase-heartbeat-database";
  var DB_VERSION2 = 1;
  var STORE_NAME = "firebase-heartbeat-store";
  var dbPromise = null;
  function getDbPromise() {
    if (!dbPromise) {
      dbPromise = openDB(DB_NAME, DB_VERSION2, {
        upgrade: (db, oldVersion) => {
          switch (oldVersion) {
            case 0:
              try {
                db.createObjectStore(STORE_NAME);
              } catch (e3) {
                console.warn(e3);
              }
          }
        }
      }).catch((e3) => {
        throw ERROR_FACTORY.create("idb-open", {
          originalErrorMessage: e3.message
        });
      });
    }
    return dbPromise;
  }
  async function readHeartbeatsFromIndexedDB(app) {
    try {
      const db = await getDbPromise();
      const tx = db.transaction(STORE_NAME);
      const result2 = await tx.objectStore(STORE_NAME).get(computeKey(app));
      await tx.done;
      return result2;
    } catch (e3) {
      if (e3 instanceof FirebaseError) {
        logger2.warn(e3.message);
      } else {
        const idbGetError = ERROR_FACTORY.create("idb-get", {
          originalErrorMessage: e3?.message
        });
        logger2.warn(idbGetError.message);
      }
    }
  }
  async function writeHeartbeatsToIndexedDB(app, heartbeatObject) {
    try {
      const db = await getDbPromise();
      const tx = db.transaction(STORE_NAME, "readwrite");
      const objectStore = tx.objectStore(STORE_NAME);
      await objectStore.put(heartbeatObject, computeKey(app));
      await tx.done;
    } catch (e3) {
      if (e3 instanceof FirebaseError) {
        logger2.warn(e3.message);
      } else {
        const idbGetError = ERROR_FACTORY.create("idb-set", {
          originalErrorMessage: e3?.message
        });
        logger2.warn(idbGetError.message);
      }
    }
  }
  function computeKey(app) {
    return `${app.name}!${app.options.appId}`;
  }
  var MAX_HEADER_BYTES = 1024;
  var MAX_NUM_STORED_HEARTBEATS = 30;
  var HeartbeatServiceImpl = class {
    constructor(container2) {
      this.container = container2;
      this._heartbeatsCache = null;
      const app = this.container.getProvider("app").getImmediate();
      this._storage = new HeartbeatStorageImpl(app);
      this._heartbeatsCachePromise = this._storage.read().then((result2) => {
        this._heartbeatsCache = result2;
        return result2;
      });
    }
    /**
     * Called to report a heartbeat. The function will generate
     * a HeartbeatsByUserAgent object, update heartbeatsCache, and persist it
     * to IndexedDB.
     * Note that we only store one heartbeat per day. So if a heartbeat for today is
     * already logged, subsequent calls to this function in the same day will be ignored.
     */
    async triggerHeartbeat() {
      try {
        const platformLogger = this.container.getProvider("platform-logger").getImmediate();
        const agent = platformLogger.getPlatformInfoString();
        const date = getUTCDateString();
        if (this._heartbeatsCache?.heartbeats == null) {
          this._heartbeatsCache = await this._heartbeatsCachePromise;
          if (this._heartbeatsCache?.heartbeats == null) {
            return;
          }
        }
        if (this._heartbeatsCache.lastSentHeartbeatDate === date || this._heartbeatsCache.heartbeats.some((singleDateHeartbeat) => singleDateHeartbeat.date === date)) {
          return;
        } else {
          this._heartbeatsCache.heartbeats.push({ date, agent });
          if (this._heartbeatsCache.heartbeats.length > MAX_NUM_STORED_HEARTBEATS) {
            const earliestHeartbeatIdx = getEarliestHeartbeatIdx(this._heartbeatsCache.heartbeats);
            this._heartbeatsCache.heartbeats.splice(earliestHeartbeatIdx, 1);
          }
        }
        return this._storage.overwrite(this._heartbeatsCache);
      } catch (e3) {
        logger2.warn(e3);
      }
    }
    /**
     * Returns a base64 encoded string which can be attached to the heartbeat-specific header directly.
     * It also clears all heartbeats from memory as well as in IndexedDB.
     *
     * NOTE: Consuming product SDKs should not send the header if this method
     * returns an empty string.
     */
    async getHeartbeatsHeader() {
      try {
        if (this._heartbeatsCache === null) {
          await this._heartbeatsCachePromise;
        }
        if (this._heartbeatsCache?.heartbeats == null || this._heartbeatsCache.heartbeats.length === 0) {
          return "";
        }
        const date = getUTCDateString();
        const { heartbeatsToSend, unsentEntries } = extractHeartbeatsForHeader(this._heartbeatsCache.heartbeats);
        const headerString = base64urlEncodeWithoutPadding(JSON.stringify({ version: 2, heartbeats: heartbeatsToSend }));
        this._heartbeatsCache.lastSentHeartbeatDate = date;
        if (unsentEntries.length > 0) {
          this._heartbeatsCache.heartbeats = unsentEntries;
          await this._storage.overwrite(this._heartbeatsCache);
        } else {
          this._heartbeatsCache.heartbeats = [];
          void this._storage.overwrite(this._heartbeatsCache);
        }
        return headerString;
      } catch (e3) {
        logger2.warn(e3);
        return "";
      }
    }
  };
  function getUTCDateString() {
    const today = /* @__PURE__ */ new Date();
    return today.toISOString().substring(0, 10);
  }
  function extractHeartbeatsForHeader(heartbeatsCache, maxSize = MAX_HEADER_BYTES) {
    const heartbeatsToSend = [];
    let unsentEntries = heartbeatsCache.slice();
    for (const singleDateHeartbeat of heartbeatsCache) {
      const heartbeatEntry = heartbeatsToSend.find((hb) => hb.agent === singleDateHeartbeat.agent);
      if (!heartbeatEntry) {
        heartbeatsToSend.push({
          agent: singleDateHeartbeat.agent,
          dates: [singleDateHeartbeat.date]
        });
        if (countBytes(heartbeatsToSend) > maxSize) {
          heartbeatsToSend.pop();
          break;
        }
      } else {
        heartbeatEntry.dates.push(singleDateHeartbeat.date);
        if (countBytes(heartbeatsToSend) > maxSize) {
          heartbeatEntry.dates.pop();
          break;
        }
      }
      unsentEntries = unsentEntries.slice(1);
    }
    return {
      heartbeatsToSend,
      unsentEntries
    };
  }
  var HeartbeatStorageImpl = class {
    constructor(app) {
      this.app = app;
      this._canUseIndexedDBPromise = this.runIndexedDBEnvironmentCheck();
    }
    async runIndexedDBEnvironmentCheck() {
      if (!isIndexedDBAvailable()) {
        return false;
      } else {
        return validateIndexedDBOpenable().then(() => true).catch(() => false);
      }
    }
    /**
     * Read all heartbeats.
     */
    async read() {
      const canUseIndexedDB = await this._canUseIndexedDBPromise;
      if (!canUseIndexedDB) {
        return { heartbeats: [] };
      } else {
        const idbHeartbeatObject = await readHeartbeatsFromIndexedDB(this.app);
        if (idbHeartbeatObject?.heartbeats) {
          return idbHeartbeatObject;
        } else {
          return { heartbeats: [] };
        }
      }
    }
    // overwrite the storage with the provided heartbeats
    async overwrite(heartbeatsObject) {
      const canUseIndexedDB = await this._canUseIndexedDBPromise;
      if (!canUseIndexedDB) {
        return;
      } else {
        const existingHeartbeatsObject = await this.read();
        return writeHeartbeatsToIndexedDB(this.app, {
          lastSentHeartbeatDate: heartbeatsObject.lastSentHeartbeatDate ?? existingHeartbeatsObject.lastSentHeartbeatDate,
          heartbeats: heartbeatsObject.heartbeats
        });
      }
    }
    // add heartbeats
    async add(heartbeatsObject) {
      const canUseIndexedDB = await this._canUseIndexedDBPromise;
      if (!canUseIndexedDB) {
        return;
      } else {
        const existingHeartbeatsObject = await this.read();
        return writeHeartbeatsToIndexedDB(this.app, {
          lastSentHeartbeatDate: heartbeatsObject.lastSentHeartbeatDate ?? existingHeartbeatsObject.lastSentHeartbeatDate,
          heartbeats: [
            ...existingHeartbeatsObject.heartbeats,
            ...heartbeatsObject.heartbeats
          ]
        });
      }
    }
  };
  function countBytes(heartbeatsCache) {
    return base64urlEncodeWithoutPadding(
      // heartbeatsCache wrapper properties
      JSON.stringify({ version: 2, heartbeats: heartbeatsCache })
    ).length;
  }
  function getEarliestHeartbeatIdx(heartbeats) {
    if (heartbeats.length === 0) {
      return -1;
    }
    let earliestHeartbeatIdx = 0;
    let earliestHeartbeatDate = heartbeats[0].date;
    for (let i3 = 1; i3 < heartbeats.length; i3++) {
      if (heartbeats[i3].date < earliestHeartbeatDate) {
        earliestHeartbeatDate = heartbeats[i3].date;
        earliestHeartbeatIdx = i3;
      }
    }
    return earliestHeartbeatIdx;
  }
  function registerCoreComponents(variant) {
    _registerComponent(new Component(
      "platform-logger",
      (container2) => new PlatformLoggerServiceImpl(container2),
      "PRIVATE"
      /* ComponentType.PRIVATE */
    ));
    _registerComponent(new Component(
      "heartbeat",
      (container2) => new HeartbeatServiceImpl(container2),
      "PRIVATE"
      /* ComponentType.PRIVATE */
    ));
    registerVersion(name$q, version$1, variant);
    registerVersion(name$q, version$1, "esm2020");
    registerVersion("fire-js", "");
  }
  registerCoreComponents("");

  // node_modules/firebase/app/dist/esm/index.esm.js
  var name2 = "firebase";
  var version = "12.10.0";
  registerVersion(name2, version, "app");

  // node_modules/@firebase/installations/dist/esm/index.esm.js
  var name3 = "@firebase/installations";
  var version2 = "0.6.20";
  var PENDING_TIMEOUT_MS = 1e4;
  var PACKAGE_VERSION = `w:${version2}`;
  var INTERNAL_AUTH_VERSION = "FIS_v2";
  var INSTALLATIONS_API_URL = "https://firebaseinstallations.googleapis.com/v1";
  var TOKEN_EXPIRATION_BUFFER = 60 * 60 * 1e3;
  var SERVICE = "installations";
  var SERVICE_NAME = "Installations";
  var ERROR_DESCRIPTION_MAP = {
    [
      "missing-app-config-values"
      /* ErrorCode.MISSING_APP_CONFIG_VALUES */
    ]: 'Missing App configuration value: "{$valueName}"',
    [
      "not-registered"
      /* ErrorCode.NOT_REGISTERED */
    ]: "Firebase Installation is not registered.",
    [
      "installation-not-found"
      /* ErrorCode.INSTALLATION_NOT_FOUND */
    ]: "Firebase Installation not found.",
    [
      "request-failed"
      /* ErrorCode.REQUEST_FAILED */
    ]: '{$requestName} request failed with error "{$serverCode} {$serverStatus}: {$serverMessage}"',
    [
      "app-offline"
      /* ErrorCode.APP_OFFLINE */
    ]: "Could not process request. Application offline.",
    [
      "delete-pending-registration"
      /* ErrorCode.DELETE_PENDING_REGISTRATION */
    ]: "Can't delete installation while there is a pending registration request."
  };
  var ERROR_FACTORY2 = new ErrorFactory(SERVICE, SERVICE_NAME, ERROR_DESCRIPTION_MAP);
  function isServerError(error) {
    return error instanceof FirebaseError && error.code.includes(
      "request-failed"
      /* ErrorCode.REQUEST_FAILED */
    );
  }
  function getInstallationsEndpoint({ projectId }) {
    return `${INSTALLATIONS_API_URL}/projects/${projectId}/installations`;
  }
  function extractAuthTokenInfoFromResponse(response) {
    return {
      token: response.token,
      requestStatus: 2,
      expiresIn: getExpiresInFromResponseExpiresIn(response.expiresIn),
      creationTime: Date.now()
    };
  }
  async function getErrorFromResponse(requestName, response) {
    const responseJson = await response.json();
    const errorData = responseJson.error;
    return ERROR_FACTORY2.create("request-failed", {
      requestName,
      serverCode: errorData.code,
      serverMessage: errorData.message,
      serverStatus: errorData.status
    });
  }
  function getHeaders({ apiKey }) {
    return new Headers({
      "Content-Type": "application/json",
      Accept: "application/json",
      "x-goog-api-key": apiKey
    });
  }
  function getHeadersWithAuth(appConfig, { refreshToken }) {
    const headers = getHeaders(appConfig);
    headers.append("Authorization", getAuthorizationHeader(refreshToken));
    return headers;
  }
  async function retryIfServerError(fn2) {
    const result2 = await fn2();
    if (result2.status >= 500 && result2.status < 600) {
      return fn2();
    }
    return result2;
  }
  function getExpiresInFromResponseExpiresIn(responseExpiresIn) {
    return Number(responseExpiresIn.replace("s", "000"));
  }
  function getAuthorizationHeader(refreshToken) {
    return `${INTERNAL_AUTH_VERSION} ${refreshToken}`;
  }
  async function createInstallationRequest({ appConfig, heartbeatServiceProvider }, { fid }) {
    const endpoint = getInstallationsEndpoint(appConfig);
    const headers = getHeaders(appConfig);
    const heartbeatService = heartbeatServiceProvider.getImmediate({
      optional: true
    });
    if (heartbeatService) {
      const heartbeatsHeader = await heartbeatService.getHeartbeatsHeader();
      if (heartbeatsHeader) {
        headers.append("x-firebase-client", heartbeatsHeader);
      }
    }
    const body = {
      fid,
      authVersion: INTERNAL_AUTH_VERSION,
      appId: appConfig.appId,
      sdkVersion: PACKAGE_VERSION
    };
    const request = {
      method: "POST",
      headers,
      body: JSON.stringify(body)
    };
    const response = await retryIfServerError(() => fetch(endpoint, request));
    if (response.ok) {
      const responseValue = await response.json();
      const registeredInstallationEntry = {
        fid: responseValue.fid || fid,
        registrationStatus: 2,
        refreshToken: responseValue.refreshToken,
        authToken: extractAuthTokenInfoFromResponse(responseValue.authToken)
      };
      return registeredInstallationEntry;
    } else {
      throw await getErrorFromResponse("Create Installation", response);
    }
  }
  function sleep(ms) {
    return new Promise((resolve2) => {
      setTimeout(resolve2, ms);
    });
  }
  function bufferToBase64UrlSafe(array) {
    const b64 = btoa(String.fromCharCode(...array));
    return b64.replace(/\+/g, "-").replace(/\//g, "_");
  }
  var VALID_FID_PATTERN = /^[cdef][\w-]{21}$/;
  var INVALID_FID = "";
  function generateFid() {
    try {
      const fidByteArray = new Uint8Array(17);
      const crypto2 = self.crypto || self.msCrypto;
      crypto2.getRandomValues(fidByteArray);
      fidByteArray[0] = 112 + fidByteArray[0] % 16;
      const fid = encode3(fidByteArray);
      return VALID_FID_PATTERN.test(fid) ? fid : INVALID_FID;
    } catch {
      return INVALID_FID;
    }
  }
  function encode3(fidByteArray) {
    const b64String = bufferToBase64UrlSafe(fidByteArray);
    return b64String.substr(0, 22);
  }
  function getKey(appConfig) {
    return `${appConfig.appName}!${appConfig.appId}`;
  }
  var fidChangeCallbacks = /* @__PURE__ */ new Map();
  function fidChanged(appConfig, fid) {
    const key = getKey(appConfig);
    callFidChangeCallbacks(key, fid);
    broadcastFidChange(key, fid);
  }
  function callFidChangeCallbacks(key, fid) {
    const callbacks = fidChangeCallbacks.get(key);
    if (!callbacks) {
      return;
    }
    for (const callback of callbacks) {
      callback(fid);
    }
  }
  function broadcastFidChange(key, fid) {
    const channel = getBroadcastChannel();
    if (channel) {
      channel.postMessage({ key, fid });
    }
    closeBroadcastChannel();
  }
  var broadcastChannel = null;
  function getBroadcastChannel() {
    if (!broadcastChannel && "BroadcastChannel" in self) {
      broadcastChannel = new BroadcastChannel("[Firebase] FID Change");
      broadcastChannel.onmessage = (e3) => {
        callFidChangeCallbacks(e3.data.key, e3.data.fid);
      };
    }
    return broadcastChannel;
  }
  function closeBroadcastChannel() {
    if (fidChangeCallbacks.size === 0 && broadcastChannel) {
      broadcastChannel.close();
      broadcastChannel = null;
    }
  }
  var DATABASE_NAME = "firebase-installations-database";
  var DATABASE_VERSION = 1;
  var OBJECT_STORE_NAME = "firebase-installations-store";
  var dbPromise2 = null;
  function getDbPromise2() {
    if (!dbPromise2) {
      dbPromise2 = openDB(DATABASE_NAME, DATABASE_VERSION, {
        upgrade: (db, oldVersion) => {
          switch (oldVersion) {
            case 0:
              db.createObjectStore(OBJECT_STORE_NAME);
          }
        }
      });
    }
    return dbPromise2;
  }
  async function set(appConfig, value) {
    const key = getKey(appConfig);
    const db = await getDbPromise2();
    const tx = db.transaction(OBJECT_STORE_NAME, "readwrite");
    const objectStore = tx.objectStore(OBJECT_STORE_NAME);
    const oldValue = await objectStore.get(key);
    await objectStore.put(value, key);
    await tx.done;
    if (!oldValue || oldValue.fid !== value.fid) {
      fidChanged(appConfig, value.fid);
    }
    return value;
  }
  async function remove(appConfig) {
    const key = getKey(appConfig);
    const db = await getDbPromise2();
    const tx = db.transaction(OBJECT_STORE_NAME, "readwrite");
    await tx.objectStore(OBJECT_STORE_NAME).delete(key);
    await tx.done;
  }
  async function update(appConfig, updateFn) {
    const key = getKey(appConfig);
    const db = await getDbPromise2();
    const tx = db.transaction(OBJECT_STORE_NAME, "readwrite");
    const store = tx.objectStore(OBJECT_STORE_NAME);
    const oldValue = await store.get(key);
    const newValue = updateFn(oldValue);
    if (newValue === void 0) {
      await store.delete(key);
    } else {
      await store.put(newValue, key);
    }
    await tx.done;
    if (newValue && (!oldValue || oldValue.fid !== newValue.fid)) {
      fidChanged(appConfig, newValue.fid);
    }
    return newValue;
  }
  async function getInstallationEntry(installations) {
    let registrationPromise;
    const installationEntry = await update(installations.appConfig, (oldEntry) => {
      const installationEntry2 = updateOrCreateInstallationEntry(oldEntry);
      const entryWithPromise = triggerRegistrationIfNecessary(installations, installationEntry2);
      registrationPromise = entryWithPromise.registrationPromise;
      return entryWithPromise.installationEntry;
    });
    if (installationEntry.fid === INVALID_FID) {
      return { installationEntry: await registrationPromise };
    }
    return {
      installationEntry,
      registrationPromise
    };
  }
  function updateOrCreateInstallationEntry(oldEntry) {
    const entry = oldEntry || {
      fid: generateFid(),
      registrationStatus: 0
      /* RequestStatus.NOT_STARTED */
    };
    return clearTimedOutRequest(entry);
  }
  function triggerRegistrationIfNecessary(installations, installationEntry) {
    if (installationEntry.registrationStatus === 0) {
      if (!navigator.onLine) {
        const registrationPromiseWithError = Promise.reject(ERROR_FACTORY2.create(
          "app-offline"
          /* ErrorCode.APP_OFFLINE */
        ));
        return {
          installationEntry,
          registrationPromise: registrationPromiseWithError
        };
      }
      const inProgressEntry = {
        fid: installationEntry.fid,
        registrationStatus: 1,
        registrationTime: Date.now()
      };
      const registrationPromise = registerInstallation(installations, inProgressEntry);
      return { installationEntry: inProgressEntry, registrationPromise };
    } else if (installationEntry.registrationStatus === 1) {
      return {
        installationEntry,
        registrationPromise: waitUntilFidRegistration(installations)
      };
    } else {
      return { installationEntry };
    }
  }
  async function registerInstallation(installations, installationEntry) {
    try {
      const registeredInstallationEntry = await createInstallationRequest(installations, installationEntry);
      return set(installations.appConfig, registeredInstallationEntry);
    } catch (e3) {
      if (isServerError(e3) && e3.customData.serverCode === 409) {
        await remove(installations.appConfig);
      } else {
        await set(installations.appConfig, {
          fid: installationEntry.fid,
          registrationStatus: 0
          /* RequestStatus.NOT_STARTED */
        });
      }
      throw e3;
    }
  }
  async function waitUntilFidRegistration(installations) {
    let entry = await updateInstallationRequest(installations.appConfig);
    while (entry.registrationStatus === 1) {
      await sleep(100);
      entry = await updateInstallationRequest(installations.appConfig);
    }
    if (entry.registrationStatus === 0) {
      const { installationEntry, registrationPromise } = await getInstallationEntry(installations);
      if (registrationPromise) {
        return registrationPromise;
      } else {
        return installationEntry;
      }
    }
    return entry;
  }
  function updateInstallationRequest(appConfig) {
    return update(appConfig, (oldEntry) => {
      if (!oldEntry) {
        throw ERROR_FACTORY2.create(
          "installation-not-found"
          /* ErrorCode.INSTALLATION_NOT_FOUND */
        );
      }
      return clearTimedOutRequest(oldEntry);
    });
  }
  function clearTimedOutRequest(entry) {
    if (hasInstallationRequestTimedOut(entry)) {
      return {
        fid: entry.fid,
        registrationStatus: 0
        /* RequestStatus.NOT_STARTED */
      };
    }
    return entry;
  }
  function hasInstallationRequestTimedOut(installationEntry) {
    return installationEntry.registrationStatus === 1 && installationEntry.registrationTime + PENDING_TIMEOUT_MS < Date.now();
  }
  async function generateAuthTokenRequest({ appConfig, heartbeatServiceProvider }, installationEntry) {
    const endpoint = getGenerateAuthTokenEndpoint(appConfig, installationEntry);
    const headers = getHeadersWithAuth(appConfig, installationEntry);
    const heartbeatService = heartbeatServiceProvider.getImmediate({
      optional: true
    });
    if (heartbeatService) {
      const heartbeatsHeader = await heartbeatService.getHeartbeatsHeader();
      if (heartbeatsHeader) {
        headers.append("x-firebase-client", heartbeatsHeader);
      }
    }
    const body = {
      installation: {
        sdkVersion: PACKAGE_VERSION,
        appId: appConfig.appId
      }
    };
    const request = {
      method: "POST",
      headers,
      body: JSON.stringify(body)
    };
    const response = await retryIfServerError(() => fetch(endpoint, request));
    if (response.ok) {
      const responseValue = await response.json();
      const completedAuthToken = extractAuthTokenInfoFromResponse(responseValue);
      return completedAuthToken;
    } else {
      throw await getErrorFromResponse("Generate Auth Token", response);
    }
  }
  function getGenerateAuthTokenEndpoint(appConfig, { fid }) {
    return `${getInstallationsEndpoint(appConfig)}/${fid}/authTokens:generate`;
  }
  async function refreshAuthToken(installations, forceRefresh = false) {
    let tokenPromise;
    const entry = await update(installations.appConfig, (oldEntry) => {
      if (!isEntryRegistered(oldEntry)) {
        throw ERROR_FACTORY2.create(
          "not-registered"
          /* ErrorCode.NOT_REGISTERED */
        );
      }
      const oldAuthToken = oldEntry.authToken;
      if (!forceRefresh && isAuthTokenValid(oldAuthToken)) {
        return oldEntry;
      } else if (oldAuthToken.requestStatus === 1) {
        tokenPromise = waitUntilAuthTokenRequest(installations, forceRefresh);
        return oldEntry;
      } else {
        if (!navigator.onLine) {
          throw ERROR_FACTORY2.create(
            "app-offline"
            /* ErrorCode.APP_OFFLINE */
          );
        }
        const inProgressEntry = makeAuthTokenRequestInProgressEntry(oldEntry);
        tokenPromise = fetchAuthTokenFromServer(installations, inProgressEntry);
        return inProgressEntry;
      }
    });
    const authToken = tokenPromise ? await tokenPromise : entry.authToken;
    return authToken;
  }
  async function waitUntilAuthTokenRequest(installations, forceRefresh) {
    let entry = await updateAuthTokenRequest(installations.appConfig);
    while (entry.authToken.requestStatus === 1) {
      await sleep(100);
      entry = await updateAuthTokenRequest(installations.appConfig);
    }
    const authToken = entry.authToken;
    if (authToken.requestStatus === 0) {
      return refreshAuthToken(installations, forceRefresh);
    } else {
      return authToken;
    }
  }
  function updateAuthTokenRequest(appConfig) {
    return update(appConfig, (oldEntry) => {
      if (!isEntryRegistered(oldEntry)) {
        throw ERROR_FACTORY2.create(
          "not-registered"
          /* ErrorCode.NOT_REGISTERED */
        );
      }
      const oldAuthToken = oldEntry.authToken;
      if (hasAuthTokenRequestTimedOut(oldAuthToken)) {
        return {
          ...oldEntry,
          authToken: {
            requestStatus: 0
            /* RequestStatus.NOT_STARTED */
          }
        };
      }
      return oldEntry;
    });
  }
  async function fetchAuthTokenFromServer(installations, installationEntry) {
    try {
      const authToken = await generateAuthTokenRequest(installations, installationEntry);
      const updatedInstallationEntry = {
        ...installationEntry,
        authToken
      };
      await set(installations.appConfig, updatedInstallationEntry);
      return authToken;
    } catch (e3) {
      if (isServerError(e3) && (e3.customData.serverCode === 401 || e3.customData.serverCode === 404)) {
        await remove(installations.appConfig);
      } else {
        const updatedInstallationEntry = {
          ...installationEntry,
          authToken: {
            requestStatus: 0
            /* RequestStatus.NOT_STARTED */
          }
        };
        await set(installations.appConfig, updatedInstallationEntry);
      }
      throw e3;
    }
  }
  function isEntryRegistered(installationEntry) {
    return installationEntry !== void 0 && installationEntry.registrationStatus === 2;
  }
  function isAuthTokenValid(authToken) {
    return authToken.requestStatus === 2 && !isAuthTokenExpired(authToken);
  }
  function isAuthTokenExpired(authToken) {
    const now = Date.now();
    return now < authToken.creationTime || authToken.creationTime + authToken.expiresIn < now + TOKEN_EXPIRATION_BUFFER;
  }
  function makeAuthTokenRequestInProgressEntry(oldEntry) {
    const inProgressAuthToken = {
      requestStatus: 1,
      requestTime: Date.now()
    };
    return {
      ...oldEntry,
      authToken: inProgressAuthToken
    };
  }
  function hasAuthTokenRequestTimedOut(authToken) {
    return authToken.requestStatus === 1 && authToken.requestTime + PENDING_TIMEOUT_MS < Date.now();
  }
  async function getId2(installations) {
    const installationsImpl = installations;
    const { installationEntry, registrationPromise } = await getInstallationEntry(installationsImpl);
    if (registrationPromise) {
      registrationPromise.catch(console.error);
    } else {
      refreshAuthToken(installationsImpl).catch(console.error);
    }
    return installationEntry.fid;
  }
  async function getToken(installations, forceRefresh = false) {
    const installationsImpl = installations;
    await completeInstallationRegistration(installationsImpl);
    const authToken = await refreshAuthToken(installationsImpl, forceRefresh);
    return authToken.token;
  }
  async function completeInstallationRegistration(installations) {
    const { registrationPromise } = await getInstallationEntry(installations);
    if (registrationPromise) {
      await registrationPromise;
    }
  }
  function extractAppConfig(app) {
    if (!app || !app.options) {
      throw getMissingValueError("App Configuration");
    }
    if (!app.name) {
      throw getMissingValueError("App Name");
    }
    const configKeys = [
      "projectId",
      "apiKey",
      "appId"
    ];
    for (const keyName of configKeys) {
      if (!app.options[keyName]) {
        throw getMissingValueError(keyName);
      }
    }
    return {
      appName: app.name,
      projectId: app.options.projectId,
      apiKey: app.options.apiKey,
      appId: app.options.appId
    };
  }
  function getMissingValueError(valueName) {
    return ERROR_FACTORY2.create("missing-app-config-values", {
      valueName
    });
  }
  var INSTALLATIONS_NAME = "installations";
  var INSTALLATIONS_NAME_INTERNAL = "installations-internal";
  var publicFactory = (container2) => {
    const app = container2.getProvider("app").getImmediate();
    const appConfig = extractAppConfig(app);
    const heartbeatServiceProvider = _getProvider(app, "heartbeat");
    const installationsImpl = {
      app,
      appConfig,
      heartbeatServiceProvider,
      _delete: () => Promise.resolve()
    };
    return installationsImpl;
  };
  var internalFactory = (container2) => {
    const app = container2.getProvider("app").getImmediate();
    const installations = _getProvider(app, INSTALLATIONS_NAME).getImmediate();
    const installationsInternal = {
      getId: () => getId2(installations),
      getToken: (forceRefresh) => getToken(installations, forceRefresh)
    };
    return installationsInternal;
  };
  function registerInstallations() {
    _registerComponent(new Component(
      INSTALLATIONS_NAME,
      publicFactory,
      "PUBLIC"
      /* ComponentType.PUBLIC */
    ));
    _registerComponent(new Component(
      INSTALLATIONS_NAME_INTERNAL,
      internalFactory,
      "PRIVATE"
      /* ComponentType.PRIVATE */
    ));
  }
  registerInstallations();
  registerVersion(name3, version2);
  registerVersion(name3, version2, "esm2020");

  // node_modules/@firebase/analytics/dist/esm/index.esm.js
  var ANALYTICS_TYPE = "analytics";
  var GA_FID_KEY = "firebase_id";
  var ORIGIN_KEY = "origin";
  var FETCH_TIMEOUT_MILLIS = 60 * 1e3;
  var DYNAMIC_CONFIG_URL = "https://firebase.googleapis.com/v1alpha/projects/-/apps/{app-id}/webConfig";
  var GTAG_URL = "https://www.googletagmanager.com/gtag/js";
  var logger3 = new Logger("@firebase/analytics");
  var ERRORS2 = {
    [
      "already-exists"
      /* AnalyticsError.ALREADY_EXISTS */
    ]: "A Firebase Analytics instance with the appId {$id}  already exists. Only one Firebase Analytics instance can be created for each appId.",
    [
      "already-initialized"
      /* AnalyticsError.ALREADY_INITIALIZED */
    ]: "initializeAnalytics() cannot be called again with different options than those it was initially called with. It can be called again with the same options to return the existing instance, or getAnalytics() can be used to get a reference to the already-initialized instance.",
    [
      "already-initialized-settings"
      /* AnalyticsError.ALREADY_INITIALIZED_SETTINGS */
    ]: "Firebase Analytics has already been initialized.settings() must be called before initializing any Analytics instanceor it will have no effect.",
    [
      "interop-component-reg-failed"
      /* AnalyticsError.INTEROP_COMPONENT_REG_FAILED */
    ]: "Firebase Analytics Interop Component failed to instantiate: {$reason}",
    [
      "invalid-analytics-context"
      /* AnalyticsError.INVALID_ANALYTICS_CONTEXT */
    ]: "Firebase Analytics is not supported in this environment. Wrap initialization of analytics in analytics.isSupported() to prevent initialization in unsupported environments. Details: {$errorInfo}",
    [
      "indexeddb-unavailable"
      /* AnalyticsError.INDEXEDDB_UNAVAILABLE */
    ]: "IndexedDB unavailable or restricted in this environment. Wrap initialization of analytics in analytics.isSupported() to prevent initialization in unsupported environments. Details: {$errorInfo}",
    [
      "fetch-throttle"
      /* AnalyticsError.FETCH_THROTTLE */
    ]: "The config fetch request timed out while in an exponential backoff state. Unix timestamp in milliseconds when fetch request throttling ends: {$throttleEndTimeMillis}.",
    [
      "config-fetch-failed"
      /* AnalyticsError.CONFIG_FETCH_FAILED */
    ]: "Dynamic config fetch failed: [{$httpStatus}] {$responseMessage}",
    [
      "no-api-key"
      /* AnalyticsError.NO_API_KEY */
    ]: 'The "apiKey" field is empty in the local Firebase config. Firebase Analytics requires this field tocontain a valid API key.',
    [
      "no-app-id"
      /* AnalyticsError.NO_APP_ID */
    ]: 'The "appId" field is empty in the local Firebase config. Firebase Analytics requires this field tocontain a valid app ID.',
    [
      "no-client-id"
      /* AnalyticsError.NO_CLIENT_ID */
    ]: 'The "client_id" field is empty.',
    [
      "invalid-gtag-resource"
      /* AnalyticsError.INVALID_GTAG_RESOURCE */
    ]: "Trusted Types detected an invalid gtag resource: {$gtagURL}."
  };
  var ERROR_FACTORY3 = new ErrorFactory("analytics", "Analytics", ERRORS2);
  function createGtagTrustedTypesScriptURL(url) {
    if (!url.startsWith(GTAG_URL)) {
      const err = ERROR_FACTORY3.create("invalid-gtag-resource", {
        gtagURL: url
      });
      logger3.warn(err.message);
      return "";
    }
    return url;
  }
  function promiseAllSettled(promises) {
    return Promise.all(promises.map((promise) => promise.catch((e3) => e3)));
  }
  function createTrustedTypesPolicy(policyName, policyOptions) {
    let trustedTypesPolicy;
    if (window.trustedTypes) {
      trustedTypesPolicy = window.trustedTypes.createPolicy(policyName, policyOptions);
    }
    return trustedTypesPolicy;
  }
  function insertScriptTag(dataLayerName2, measurementId) {
    const trustedTypesPolicy = createTrustedTypesPolicy("firebase-js-sdk-policy", {
      createScriptURL: createGtagTrustedTypesScriptURL
    });
    const script = document.createElement("script");
    const gtagScriptURL = `${GTAG_URL}?l=${dataLayerName2}&id=${measurementId}`;
    script.src = trustedTypesPolicy ? trustedTypesPolicy?.createScriptURL(gtagScriptURL) : gtagScriptURL;
    script.async = true;
    document.head.appendChild(script);
  }
  function getOrCreateDataLayer(dataLayerName2) {
    let dataLayer = [];
    if (Array.isArray(window[dataLayerName2])) {
      dataLayer = window[dataLayerName2];
    } else {
      window[dataLayerName2] = dataLayer;
    }
    return dataLayer;
  }
  async function gtagOnConfig(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementIdToAppId2, measurementId, gtagParams) {
    const correspondingAppId = measurementIdToAppId2[measurementId];
    try {
      if (correspondingAppId) {
        await initializationPromisesMap2[correspondingAppId];
      } else {
        const dynamicConfigResults = await promiseAllSettled(dynamicConfigPromisesList2);
        const foundConfig = dynamicConfigResults.find((config) => config.measurementId === measurementId);
        if (foundConfig) {
          await initializationPromisesMap2[foundConfig.appId];
        }
      }
    } catch (e3) {
      logger3.error(e3);
    }
    gtagCore("config", measurementId, gtagParams);
  }
  async function gtagOnEvent(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementId, gtagParams) {
    try {
      let initializationPromisesToWaitFor = [];
      if (gtagParams && gtagParams["send_to"]) {
        let gaSendToList = gtagParams["send_to"];
        if (!Array.isArray(gaSendToList)) {
          gaSendToList = [gaSendToList];
        }
        const dynamicConfigResults = await promiseAllSettled(dynamicConfigPromisesList2);
        for (const sendToId of gaSendToList) {
          const foundConfig = dynamicConfigResults.find((config) => config.measurementId === sendToId);
          const initializationPromise = foundConfig && initializationPromisesMap2[foundConfig.appId];
          if (initializationPromise) {
            initializationPromisesToWaitFor.push(initializationPromise);
          } else {
            initializationPromisesToWaitFor = [];
            break;
          }
        }
      }
      if (initializationPromisesToWaitFor.length === 0) {
        initializationPromisesToWaitFor = Object.values(initializationPromisesMap2);
      }
      await Promise.all(initializationPromisesToWaitFor);
      gtagCore("event", measurementId, gtagParams || {});
    } catch (e3) {
      logger3.error(e3);
    }
  }
  function wrapGtag(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementIdToAppId2) {
    async function gtagWrapper(command, ...args) {
      try {
        if (command === "event") {
          const [measurementId, gtagParams] = args;
          await gtagOnEvent(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementId, gtagParams);
        } else if (command === "config") {
          const [measurementId, gtagParams] = args;
          await gtagOnConfig(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementIdToAppId2, measurementId, gtagParams);
        } else if (command === "consent") {
          const [consentAction, gtagParams] = args;
          gtagCore("consent", consentAction, gtagParams);
        } else if (command === "get") {
          const [measurementId, fieldName, callback] = args;
          gtagCore("get", measurementId, fieldName, callback);
        } else if (command === "set") {
          const [customParams] = args;
          gtagCore("set", customParams);
        } else {
          gtagCore(command, ...args);
        }
      } catch (e3) {
        logger3.error(e3);
      }
    }
    return gtagWrapper;
  }
  function wrapOrCreateGtag(initializationPromisesMap2, dynamicConfigPromisesList2, measurementIdToAppId2, dataLayerName2, gtagFunctionName) {
    let gtagCore = function(..._args) {
      window[dataLayerName2].push(arguments);
    };
    if (window[gtagFunctionName] && typeof window[gtagFunctionName] === "function") {
      gtagCore = window[gtagFunctionName];
    }
    window[gtagFunctionName] = wrapGtag(gtagCore, initializationPromisesMap2, dynamicConfigPromisesList2, measurementIdToAppId2);
    return {
      gtagCore,
      wrappedGtag: window[gtagFunctionName]
    };
  }
  function findGtagScriptOnPage(dataLayerName2) {
    const scriptTags = window.document.getElementsByTagName("script");
    for (const tag of Object.values(scriptTags)) {
      if (tag.src && tag.src.includes(GTAG_URL) && tag.src.includes(dataLayerName2)) {
        return tag;
      }
    }
    return null;
  }
  var LONG_RETRY_FACTOR = 30;
  var BASE_INTERVAL_MILLIS = 1e3;
  var RetryData = class {
    constructor(throttleMetadata = {}, intervalMillis = BASE_INTERVAL_MILLIS) {
      this.throttleMetadata = throttleMetadata;
      this.intervalMillis = intervalMillis;
    }
    getThrottleMetadata(appId) {
      return this.throttleMetadata[appId];
    }
    setThrottleMetadata(appId, metadata) {
      this.throttleMetadata[appId] = metadata;
    }
    deleteThrottleMetadata(appId) {
      delete this.throttleMetadata[appId];
    }
  };
  var defaultRetryData = new RetryData();
  function getHeaders2(apiKey) {
    return new Headers({
      Accept: "application/json",
      "x-goog-api-key": apiKey
    });
  }
  async function fetchDynamicConfig(appFields) {
    const { appId, apiKey } = appFields;
    const request = {
      method: "GET",
      headers: getHeaders2(apiKey)
    };
    const appUrl = DYNAMIC_CONFIG_URL.replace("{app-id}", appId);
    const response = await fetch(appUrl, request);
    if (response.status !== 200 && response.status !== 304) {
      let errorMessage = "";
      try {
        const jsonResponse = await response.json();
        if (jsonResponse.error?.message) {
          errorMessage = jsonResponse.error.message;
        }
      } catch (_ignored) {
      }
      throw ERROR_FACTORY3.create("config-fetch-failed", {
        httpStatus: response.status,
        responseMessage: errorMessage
      });
    }
    return response.json();
  }
  async function fetchDynamicConfigWithRetry(app, retryData = defaultRetryData, timeoutMillis) {
    const { appId, apiKey, measurementId } = app.options;
    if (!appId) {
      throw ERROR_FACTORY3.create(
        "no-app-id"
        /* AnalyticsError.NO_APP_ID */
      );
    }
    if (!apiKey) {
      if (measurementId) {
        return {
          measurementId,
          appId
        };
      }
      throw ERROR_FACTORY3.create(
        "no-api-key"
        /* AnalyticsError.NO_API_KEY */
      );
    }
    const throttleMetadata = retryData.getThrottleMetadata(appId) || {
      backoffCount: 0,
      throttleEndTimeMillis: Date.now()
    };
    const signal = new AnalyticsAbortSignal();
    setTimeout(async () => {
      signal.abort();
    }, timeoutMillis !== void 0 ? timeoutMillis : FETCH_TIMEOUT_MILLIS);
    return attemptFetchDynamicConfigWithRetry({ appId, apiKey, measurementId }, throttleMetadata, signal, retryData);
  }
  async function attemptFetchDynamicConfigWithRetry(appFields, { throttleEndTimeMillis, backoffCount }, signal, retryData = defaultRetryData) {
    const { appId, measurementId } = appFields;
    try {
      await setAbortableTimeout(signal, throttleEndTimeMillis);
    } catch (e3) {
      if (measurementId) {
        logger3.warn(`Timed out fetching this Firebase app's measurement ID from the server. Falling back to the measurement ID ${measurementId} provided in the "measurementId" field in the local Firebase config. [${e3?.message}]`);
        return { appId, measurementId };
      }
      throw e3;
    }
    try {
      const response = await fetchDynamicConfig(appFields);
      retryData.deleteThrottleMetadata(appId);
      return response;
    } catch (e3) {
      const error = e3;
      if (!isRetriableError(error)) {
        retryData.deleteThrottleMetadata(appId);
        if (measurementId) {
          logger3.warn(`Failed to fetch this Firebase app's measurement ID from the server. Falling back to the measurement ID ${measurementId} provided in the "measurementId" field in the local Firebase config. [${error?.message}]`);
          return { appId, measurementId };
        } else {
          throw e3;
        }
      }
      const backoffMillis = Number(error?.customData?.httpStatus) === 503 ? calculateBackoffMillis(backoffCount, retryData.intervalMillis, LONG_RETRY_FACTOR) : calculateBackoffMillis(backoffCount, retryData.intervalMillis);
      const throttleMetadata = {
        throttleEndTimeMillis: Date.now() + backoffMillis,
        backoffCount: backoffCount + 1
      };
      retryData.setThrottleMetadata(appId, throttleMetadata);
      logger3.debug(`Calling attemptFetch again in ${backoffMillis} millis`);
      return attemptFetchDynamicConfigWithRetry(appFields, throttleMetadata, signal, retryData);
    }
  }
  function setAbortableTimeout(signal, throttleEndTimeMillis) {
    return new Promise((resolve2, reject2) => {
      const backoffMillis = Math.max(throttleEndTimeMillis - Date.now(), 0);
      const timeout = setTimeout(resolve2, backoffMillis);
      signal.addEventListener(() => {
        clearTimeout(timeout);
        reject2(ERROR_FACTORY3.create("fetch-throttle", {
          throttleEndTimeMillis
        }));
      });
    });
  }
  function isRetriableError(e3) {
    if (!(e3 instanceof FirebaseError) || !e3.customData) {
      return false;
    }
    const httpStatus = Number(e3.customData["httpStatus"]);
    return httpStatus === 429 || httpStatus === 500 || httpStatus === 503 || httpStatus === 504;
  }
  var AnalyticsAbortSignal = class {
    constructor() {
      this.listeners = [];
    }
    addEventListener(listener) {
      this.listeners.push(listener);
    }
    abort() {
      this.listeners.forEach((listener) => listener());
    }
  };
  var defaultEventParametersForInit;
  async function logEvent$1(gtagFunction, initializationPromise, eventName, eventParams, options) {
    if (options && options.global) {
      gtagFunction("event", eventName, eventParams);
      return;
    } else {
      const measurementId = await initializationPromise;
      const params = {
        ...eventParams,
        "send_to": measurementId
      };
      gtagFunction("event", eventName, params);
    }
  }
  async function setUserProperties$1(gtagFunction, initializationPromise, properties, options) {
    if (options && options.global) {
      const flatProperties = {};
      for (const key of Object.keys(properties)) {
        flatProperties[`user_properties.${key}`] = properties[key];
      }
      gtagFunction("set", flatProperties);
      return Promise.resolve();
    } else {
      const measurementId = await initializationPromise;
      gtagFunction("config", measurementId, {
        update: true,
        "user_properties": properties
      });
    }
  }
  var defaultConsentSettingsForInit;
  function _setConsentDefaultForInit(consentSettings) {
    defaultConsentSettingsForInit = consentSettings;
  }
  function _setDefaultEventParametersForInit(customParams) {
    defaultEventParametersForInit = customParams;
  }
  async function validateIndexedDB() {
    if (!isIndexedDBAvailable()) {
      logger3.warn(ERROR_FACTORY3.create("indexeddb-unavailable", {
        errorInfo: "IndexedDB is not available in this environment."
      }).message);
      return false;
    } else {
      try {
        await validateIndexedDBOpenable();
      } catch (e3) {
        logger3.warn(ERROR_FACTORY3.create("indexeddb-unavailable", {
          errorInfo: e3?.toString()
        }).message);
        return false;
      }
    }
    return true;
  }
  async function _initializeAnalytics(app, dynamicConfigPromisesList2, measurementIdToAppId2, installations, gtagCore, dataLayerName2, options) {
    const dynamicConfigPromise = fetchDynamicConfigWithRetry(app);
    dynamicConfigPromise.then((config) => {
      measurementIdToAppId2[config.measurementId] = config.appId;
      if (app.options.measurementId && config.measurementId !== app.options.measurementId) {
        logger3.warn(`The measurement ID in the local Firebase config (${app.options.measurementId}) does not match the measurement ID fetched from the server (${config.measurementId}). To ensure analytics events are always sent to the correct Analytics property, update the measurement ID field in the local config or remove it from the local config.`);
      }
    }).catch((e3) => logger3.error(e3));
    dynamicConfigPromisesList2.push(dynamicConfigPromise);
    const fidPromise = validateIndexedDB().then((envIsValid) => {
      if (envIsValid) {
        return installations.getId();
      } else {
        return void 0;
      }
    });
    const [dynamicConfig, fid] = await Promise.all([
      dynamicConfigPromise,
      fidPromise
    ]);
    if (!findGtagScriptOnPage(dataLayerName2)) {
      insertScriptTag(dataLayerName2, dynamicConfig.measurementId);
    }
    if (defaultConsentSettingsForInit) {
      gtagCore("consent", "default", defaultConsentSettingsForInit);
      _setConsentDefaultForInit(void 0);
    }
    gtagCore("js", /* @__PURE__ */ new Date());
    const configProperties = options?.config ?? {};
    configProperties[ORIGIN_KEY] = "firebase";
    configProperties.update = true;
    if (fid != null) {
      configProperties[GA_FID_KEY] = fid;
    }
    gtagCore("config", dynamicConfig.measurementId, configProperties);
    if (defaultEventParametersForInit) {
      gtagCore("set", defaultEventParametersForInit);
      _setDefaultEventParametersForInit(void 0);
    }
    return dynamicConfig.measurementId;
  }
  var AnalyticsService = class {
    constructor(app) {
      this.app = app;
    }
    _delete() {
      delete initializationPromisesMap[this.app.options.appId];
      return Promise.resolve();
    }
  };
  var initializationPromisesMap = {};
  var dynamicConfigPromisesList = [];
  var measurementIdToAppId = {};
  var dataLayerName = "dataLayer";
  var gtagName = "gtag";
  var gtagCoreFunction;
  var wrappedGtagFunction;
  var globalInitDone = false;
  function warnOnBrowserContextMismatch() {
    const mismatchedEnvMessages = [];
    if (isBrowserExtension()) {
      mismatchedEnvMessages.push("This is a browser extension environment.");
    }
    if (!areCookiesEnabled()) {
      mismatchedEnvMessages.push("Cookies are not available.");
    }
    if (mismatchedEnvMessages.length > 0) {
      const details = mismatchedEnvMessages.map((message, index2) => `(${index2 + 1}) ${message}`).join(" ");
      const err = ERROR_FACTORY3.create("invalid-analytics-context", {
        errorInfo: details
      });
      logger3.warn(err.message);
    }
  }
  function factory(app, installations, options) {
    warnOnBrowserContextMismatch();
    const appId = app.options.appId;
    if (!appId) {
      throw ERROR_FACTORY3.create(
        "no-app-id"
        /* AnalyticsError.NO_APP_ID */
      );
    }
    if (!app.options.apiKey) {
      if (app.options.measurementId) {
        logger3.warn(`The "apiKey" field is empty in the local Firebase config. This is needed to fetch the latest measurement ID for this Firebase app. Falling back to the measurement ID ${app.options.measurementId} provided in the "measurementId" field in the local Firebase config.`);
      } else {
        throw ERROR_FACTORY3.create(
          "no-api-key"
          /* AnalyticsError.NO_API_KEY */
        );
      }
    }
    if (initializationPromisesMap[appId] != null) {
      throw ERROR_FACTORY3.create("already-exists", {
        id: appId
      });
    }
    if (!globalInitDone) {
      getOrCreateDataLayer(dataLayerName);
      const { wrappedGtag, gtagCore } = wrapOrCreateGtag(initializationPromisesMap, dynamicConfigPromisesList, measurementIdToAppId, dataLayerName, gtagName);
      wrappedGtagFunction = wrappedGtag;
      gtagCoreFunction = gtagCore;
      globalInitDone = true;
    }
    initializationPromisesMap[appId] = _initializeAnalytics(app, dynamicConfigPromisesList, measurementIdToAppId, installations, gtagCoreFunction, dataLayerName, options);
    const analyticsInstance = new AnalyticsService(app);
    return analyticsInstance;
  }
  function getAnalytics(app = getApp()) {
    app = getModularInstance(app);
    const analyticsProvider = _getProvider(app, ANALYTICS_TYPE);
    if (analyticsProvider.isInitialized()) {
      return analyticsProvider.getImmediate();
    }
    return initializeAnalytics(app);
  }
  function initializeAnalytics(app, options = {}) {
    const analyticsProvider = _getProvider(app, ANALYTICS_TYPE);
    if (analyticsProvider.isInitialized()) {
      const existingInstance = analyticsProvider.getImmediate();
      if (deepEqual(options, analyticsProvider.getOptions())) {
        return existingInstance;
      } else {
        throw ERROR_FACTORY3.create(
          "already-initialized"
          /* AnalyticsError.ALREADY_INITIALIZED */
        );
      }
    }
    const analyticsInstance = analyticsProvider.initialize({ options });
    return analyticsInstance;
  }
  function setUserProperties(analyticsInstance, properties, options) {
    analyticsInstance = getModularInstance(analyticsInstance);
    setUserProperties$1(wrappedGtagFunction, initializationPromisesMap[analyticsInstance.app.options.appId], properties, options).catch((e3) => logger3.error(e3));
  }
  function logEvent(analyticsInstance, eventName, eventParams, options) {
    analyticsInstance = getModularInstance(analyticsInstance);
    logEvent$1(wrappedGtagFunction, initializationPromisesMap[analyticsInstance.app.options.appId], eventName, eventParams, options).catch((e3) => logger3.error(e3));
  }
  var name4 = "@firebase/analytics";
  var version3 = "0.10.20";
  function registerAnalytics() {
    _registerComponent(new Component(
      ANALYTICS_TYPE,
      (container2, { options: analyticsOptions }) => {
        const app = container2.getProvider("app").getImmediate();
        const installations = container2.getProvider("installations-internal").getImmediate();
        return factory(app, installations, analyticsOptions);
      },
      "PUBLIC"
      /* ComponentType.PUBLIC */
    ));
    _registerComponent(new Component(
      "analytics-internal",
      internalFactory2,
      "PRIVATE"
      /* ComponentType.PRIVATE */
    ));
    registerVersion(name4, version3);
    registerVersion(name4, version3, "esm2020");
    function internalFactory2(container2) {
      try {
        const analytics2 = container2.getProvider(ANALYTICS_TYPE).getImmediate();
        return {
          logEvent: (eventName, eventParams, options) => logEvent(analytics2, eventName, eventParams, options),
          setUserProperties: (properties, options) => setUserProperties(analytics2, properties, options)
        };
      } catch (e3) {
        throw ERROR_FACTORY3.create("interop-component-reg-failed", {
          reason: e3
        });
      }
    }
  }
  registerAnalytics();

  // src/utils/firebase.js
  var analytics = null;
  var recentErrors = /* @__PURE__ */ new Map();
  var DEDUP_WINDOW_MS = 6e4;
  function isDuplicate(key) {
    const now = Date.now();
    const lastSeen = recentErrors.get(key);
    if (lastSeen && now - lastSeen < DEDUP_WINDOW_MS) return true;
    recentErrors.set(key, now);
    if (recentErrors.size > 100) {
      for (const [k4, ts2] of recentErrors) {
        if (now - ts2 > DEDUP_WINDOW_MS) recentErrors.delete(k4);
      }
    }
    return false;
  }
  function truncate(str, max = 100) {
    if (!str) return "";
    const s3 = String(str);
    return s3.length > max ? s3.slice(0, max) : s3;
  }
  function initFirebase() {
    if (analytics) return;
    if (!FIREBASE_CONFIG.apiKey) {
      console.warn("[Firebase] No apiKey configured \u2014 crash tracking disabled.");
      return;
    }
    try {
      const app = initializeApp(FIREBASE_CONFIG);
      analytics = getAnalytics(app);
      console.log("[Firebase] Analytics initialized");
    } catch (err) {
      console.error("[Firebase] Init failed:", err);
    }
  }
  function logCrash(crashType, error, sourceFile, sourceLine) {
    const message = truncate(error?.message || String(error));
    const stack = truncate(error?.stack || "");
    const key = `crash:${message}`;
    if (isDuplicate(key)) return;
    console.error(`[Firebase] app_crash (${crashType}):`, message);
    if (!analytics) return;
    logEvent(analytics, "app_crash", {
      error_message: message,
      stack_trace: stack,
      source_file: truncate(sourceFile || "", 100),
      source_line: truncate(sourceLine || "", 20),
      app_version: APP_VERSION,
      crash_type: crashType
    });
  }

  // src/index.jsx
  window.appUI = window.appUI || {};
  if (!window.set_storage_value_response) window.set_storage_value_response = () => {
  };
  if (window.natively) window.natively.showProgress(false);
  var initGlobals = () => {
    const fonts = [
      "https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&family=Poppins:wght@300;400;500;600;700&display=swap"
    ];
    fonts.forEach((url) => {
      if (!document.querySelector(`link[href="${url}"]`)) {
        const link = document.createElement("link");
        link.href = url;
        link.rel = "stylesheet";
        document.head.appendChild(link);
      }
    });
    window.BubbleBridge = {
      send: (fnName, data) => {
        if (window[fnName]) window[fnName](data);
      }
    };
    window.appUI.resetAllStorage = () => {
      const keys = ["bonds_session_active", "bonds_device_id", "onboarding_complete", "onboarding_state", "coins_intro_seen", "bonds_user_data", "bonds_daily_question", "bonds_dark_mode", "bonds_simulator_templates"];
      keys.forEach((k4) => localStorage.removeItem(k4));
      try {
        const ns = new NativelyStorage();
        keys.forEach((k4) => ns.removeStorageValue(k4));
      } catch (e3) {
        console.warn("NativelyStorage unavailable:", e3.message);
      }
      window.location.reload();
    };
    console.log("\u{1F30D} Globals Initialized");
  };
  initGlobals();
  initFirebase();
  window.addEventListener("error", (event) => {
    logCrash("uncaught", event.error || event.message, event.filename, String(event.lineno));
  });
  window.addEventListener("unhandledrejection", (event) => {
    const error = event.reason instanceof Error ? event.reason : new Error(String(event.reason));
    logCrash("unhandled_rejection", error);
  });
  window.appUI.mountMainApp = (container2) => {
    const root2 = createRoot(container2);
    root2.render(/* @__PURE__ */ xn.createElement(App_default, null));
    return root2;
  };
  window.appUI.mountWelcome = (container2, props = {}) => {
    const root2 = createRoot(container2);
    root2.render(/* @__PURE__ */ xn.createElement(WelcomeScreen_default, { ...props }));
    return root2;
  };
  window.appUI.mountDailyQuestion = (container2, props = {}) => {
    const root2 = createRoot(container2);
    root2.render(
      /* @__PURE__ */ xn.createElement(UserProvider, null, /* @__PURE__ */ xn.createElement(DailyQuestionProvider, null, /* @__PURE__ */ xn.createElement(DailyQuestion_default, { ...props })))
    );
    return root2;
  };
  window.appUI.mountOnboarding = (container2, props = {}) => {
    const defaults = { steps: mockOnboardingSteps_default, coins: 0, showCoins: true };
    const root2 = createRoot(container2);
    root2.render(/* @__PURE__ */ xn.createElement(OnboardingFlow_default, { ...defaults, ...props }));
    return root2;
  };
  window.appUI.mountJourney = (container2, props = {}) => {
    const defaults = { coins: 5 };
    const root2 = createRoot(container2);
    root2.render(/* @__PURE__ */ xn.createElement(JourneyPath_default, { ...defaults, ...props }));
    return root2;
  };
  var appContainer = document.getElementById("app-content-area");
  if (appContainer) {
    window.appUI.mountMainApp(appContainer);
    console.log("\u{1F680} Main App Auto-Mounted into #app-content-area");
  } else {
    console.log("\u{1F4E6} UI Kit loaded. Mount functions ready on window.appUI");
  }
  window.RT = realtime_default;
  window.TTS = tts_default;
  window.appUI.startTTSStream = ({ apiKey, text }) => tts_default.start({ apiKey, text });
})();
/*! Bundled license information:

@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
@firebase/logger/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/util/dist/index.esm.js:
@firebase/util/dist/index.esm.js:
  (**
   * @license
   * Copyright 2022 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/util/dist/index.esm.js:
  (**
   * @license
   * Copyright 2017 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2025 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/util/dist/index.esm.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/util/dist/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
@firebase/analytics/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2020 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/util/dist/index.esm.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2025 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/component/dist/esm/index.esm.js:
@firebase/app/dist/esm/index.esm.js:
@firebase/app/dist/esm/index.esm.js:
@firebase/app/dist/esm/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/app/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2019 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2023 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/app/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2021 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2019 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

firebase/app/dist/esm/index.esm.js:
@firebase/installations/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2020 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)

@firebase/analytics/dist/esm/index.esm.js:
  (**
   * @license
   * Copyright 2020 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
  (**
   * @license
   * Copyright 2019 Google LLC
   *
   * Licensed under the Apache License, Version 2.0 (the "License");
   * you may not use this file except in compliance with the License.
   * You may obtain a copy of the License at
   *
   *   http://www.apache.org/licenses/LICENSE-2.0
   *
   * Unless required by applicable law or agreed to in writing, software
   * distributed under the License is distributed on an "AS IS" BASIS,
   * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
   * See the License for the specific language governing permissions and
   * limitations under the License.
   *)
*/
